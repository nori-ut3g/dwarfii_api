/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
// @ts-nocheck
import $protobuf from "protobufjs/minimal.js";

// Common aliases
var $Reader = $protobuf.Reader,
  $Writer = $protobuf.Writer,
  $util = $protobuf.util;

// Exported root namespace
var $root =
  $protobuf.roots["dwarfCurrent"] || ($protobuf.roots["dwarfCurrent"] = {});

$root.ReqStartCalibration = (function () {
  /**
   * Properties of a ReqStartCalibration.
   * @exports IReqStartCalibration
   * @interface IReqStartCalibration
   * @property {number|null} [lon] ReqStartCalibration lon
   * @property {number|null} [lat] ReqStartCalibration lat
   */

  /**
   * Constructs a new ReqStartCalibration.
   * @exports ReqStartCalibration
   * @classdesc Represents a ReqStartCalibration.
   * @implements IReqStartCalibration
   * @constructor
   * @param {IReqStartCalibration=} [properties] Properties to set
   */
  function ReqStartCalibration(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartCalibration lon.
   * @member {number} lon
   * @memberof ReqStartCalibration
   * @instance
   */
  ReqStartCalibration.prototype.lon = 0;

  /**
   * ReqStartCalibration lat.
   * @member {number} lat
   * @memberof ReqStartCalibration
   * @instance
   */
  ReqStartCalibration.prototype.lat = 0;

  /**
   * Creates a new ReqStartCalibration instance using the specified properties.
   * @function create
   * @memberof ReqStartCalibration
   * @static
   * @param {IReqStartCalibration=} [properties] Properties to set
   * @returns {ReqStartCalibration} ReqStartCalibration instance
   */
  ReqStartCalibration.create = function create(properties) {
    return new ReqStartCalibration(properties);
  };

  /**
   * Encodes the specified ReqStartCalibration message. Does not implicitly {@link ReqStartCalibration.verify|verify} messages.
   * @function encode
   * @memberof ReqStartCalibration
   * @static
   * @param {IReqStartCalibration} message ReqStartCalibration message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartCalibration.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lat);
    return writer;
  };

  /**
   * Encodes the specified ReqStartCalibration message, length delimited. Does not implicitly {@link ReqStartCalibration.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartCalibration
   * @static
   * @param {IReqStartCalibration} message ReqStartCalibration message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartCalibration.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartCalibration message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartCalibration
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartCalibration} ReqStartCalibration
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartCalibration.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartCalibration();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.lon = reader.double();
          break;
        }
        case 2: {
          message.lat = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartCalibration message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartCalibration
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartCalibration} ReqStartCalibration
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartCalibration.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartCalibration message.
   * @function verify
   * @memberof ReqStartCalibration
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartCalibration.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    return null;
  };

  /**
   * Creates a ReqStartCalibration message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartCalibration
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartCalibration} ReqStartCalibration
   */
  ReqStartCalibration.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartCalibration) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartCalibration: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartCalibration();
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartCalibration message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartCalibration
   * @static
   * @param {ReqStartCalibration} message ReqStartCalibration
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartCalibration.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.lon = 0;
      object.lat = 0;
    }
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    return object;
  };

  /**
   * Converts this ReqStartCalibration to JSON.
   * @function toJSON
   * @memberof ReqStartCalibration
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartCalibration.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartCalibration
   * @function getTypeUrl
   * @memberof ReqStartCalibration
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartCalibration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartCalibration";
  };

  return ReqStartCalibration;
})();

$root.ReqStopCalibration = (function () {
  /**
   * Properties of a ReqStopCalibration.
   * @exports IReqStopCalibration
   * @interface IReqStopCalibration
   */

  /**
   * Constructs a new ReqStopCalibration.
   * @exports ReqStopCalibration
   * @classdesc Represents a ReqStopCalibration.
   * @implements IReqStopCalibration
   * @constructor
   * @param {IReqStopCalibration=} [properties] Properties to set
   */
  function ReqStopCalibration(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCalibration instance using the specified properties.
   * @function create
   * @memberof ReqStopCalibration
   * @static
   * @param {IReqStopCalibration=} [properties] Properties to set
   * @returns {ReqStopCalibration} ReqStopCalibration instance
   */
  ReqStopCalibration.create = function create(properties) {
    return new ReqStopCalibration(properties);
  };

  /**
   * Encodes the specified ReqStopCalibration message. Does not implicitly {@link ReqStopCalibration.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCalibration
   * @static
   * @param {IReqStopCalibration} message ReqStopCalibration message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCalibration.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCalibration message, length delimited. Does not implicitly {@link ReqStopCalibration.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCalibration
   * @static
   * @param {IReqStopCalibration} message ReqStopCalibration message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCalibration.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCalibration message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCalibration
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCalibration} ReqStopCalibration
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCalibration.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCalibration();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCalibration message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCalibration
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCalibration} ReqStopCalibration
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCalibration.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCalibration message.
   * @function verify
   * @memberof ReqStopCalibration
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCalibration.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCalibration message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCalibration
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCalibration} ReqStopCalibration
   */
  ReqStopCalibration.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopCalibration) return object;
    return new $root.ReqStopCalibration();
  };

  /**
   * Creates a plain object from a ReqStopCalibration message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCalibration
   * @static
   * @param {ReqStopCalibration} message ReqStopCalibration
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCalibration.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCalibration to JSON.
   * @function toJSON
   * @memberof ReqStopCalibration
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCalibration.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCalibration
   * @function getTypeUrl
   * @memberof ReqStopCalibration
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCalibration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCalibration";
  };

  return ReqStopCalibration;
})();

$root.ReqGotoDSO = (function () {
  /**
   * Properties of a ReqGotoDSO.
   * @exports IReqGotoDSO
   * @interface IReqGotoDSO
   * @property {number|null} [ra] ReqGotoDSO ra
   * @property {number|null} [dec] ReqGotoDSO dec
   * @property {string|null} [targetName] ReqGotoDSO targetName
   * @property {boolean|null} [gotoOnly] ReqGotoDSO gotoOnly
   * @property {number|null} [rotation] ReqGotoDSO rotation
   */

  /**
   * Constructs a new ReqGotoDSO.
   * @exports ReqGotoDSO
   * @classdesc Represents a ReqGotoDSO.
   * @implements IReqGotoDSO
   * @constructor
   * @param {IReqGotoDSO=} [properties] Properties to set
   */
  function ReqGotoDSO(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGotoDSO ra.
   * @member {number} ra
   * @memberof ReqGotoDSO
   * @instance
   */
  ReqGotoDSO.prototype.ra = 0;

  /**
   * ReqGotoDSO dec.
   * @member {number} dec
   * @memberof ReqGotoDSO
   * @instance
   */
  ReqGotoDSO.prototype.dec = 0;

  /**
   * ReqGotoDSO targetName.
   * @member {string} targetName
   * @memberof ReqGotoDSO
   * @instance
   */
  ReqGotoDSO.prototype.targetName = "";

  /**
   * ReqGotoDSO gotoOnly.
   * @member {boolean} gotoOnly
   * @memberof ReqGotoDSO
   * @instance
   */
  ReqGotoDSO.prototype.gotoOnly = false;

  /**
   * ReqGotoDSO rotation.
   * @member {number|null|undefined} rotation
   * @memberof ReqGotoDSO
   * @instance
   */
  ReqGotoDSO.prototype.rotation = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqGotoDSO.prototype, "_rotation", {
    get: $util.oneOfGetter(($oneOfFields = ["rotation"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqGotoDSO instance using the specified properties.
   * @function create
   * @memberof ReqGotoDSO
   * @static
   * @param {IReqGotoDSO=} [properties] Properties to set
   * @returns {ReqGotoDSO} ReqGotoDSO instance
   */
  ReqGotoDSO.create = function create(properties) {
    return new ReqGotoDSO(properties);
  };

  /**
   * Encodes the specified ReqGotoDSO message. Does not implicitly {@link ReqGotoDSO.verify|verify} messages.
   * @function encode
   * @memberof ReqGotoDSO
   * @static
   * @param {IReqGotoDSO} message ReqGotoDSO message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGotoDSO.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.ra);
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.dec);
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.targetName);
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).bool(message.gotoOnly);
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.rotation);
    return writer;
  };

  /**
   * Encodes the specified ReqGotoDSO message, length delimited. Does not implicitly {@link ReqGotoDSO.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGotoDSO
   * @static
   * @param {IReqGotoDSO} message ReqGotoDSO message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGotoDSO.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGotoDSO message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGotoDSO
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGotoDSO} ReqGotoDSO
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGotoDSO.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGotoDSO();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.ra = reader.double();
          break;
        }
        case 2: {
          message.dec = reader.double();
          break;
        }
        case 3: {
          message.targetName = reader.string();
          break;
        }
        case 4: {
          message.gotoOnly = reader.bool();
          break;
        }
        case 5: {
          message.rotation = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGotoDSO message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGotoDSO
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGotoDSO} ReqGotoDSO
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGotoDSO.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGotoDSO message.
   * @function verify
   * @memberof ReqGotoDSO
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGotoDSO.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      if (typeof message.ra !== "number") return "ra: number expected";
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      if (typeof message.dec !== "number") return "dec: number expected";
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      if (!$util.isString(message.targetName))
        return "targetName: string expected";
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      if (typeof message.gotoOnly !== "boolean")
        return "gotoOnly: boolean expected";
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    ) {
      properties._rotation = 1;
      if (!$util.isInteger(message.rotation))
        return "rotation: integer expected";
    }
    return null;
  };

  /**
   * Creates a ReqGotoDSO message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGotoDSO
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGotoDSO} ReqGotoDSO
   */
  ReqGotoDSO.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGotoDSO) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGotoDSO: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGotoDSO();
    if (object.ra != null) message.ra = Number(object.ra);
    if (object.dec != null) message.dec = Number(object.dec);
    if (object.targetName != null)
      message.targetName = String(object.targetName);
    if (object.gotoOnly != null) message.gotoOnly = Boolean(object.gotoOnly);
    if (object.rotation != null) message.rotation = object.rotation | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqGotoDSO message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGotoDSO
   * @static
   * @param {ReqGotoDSO} message ReqGotoDSO
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGotoDSO.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.ra = 0;
      object.dec = 0;
      object.targetName = "";
      object.gotoOnly = false;
    }
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      object.ra =
        options.json && !isFinite(message.ra) ? String(message.ra) : message.ra;
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      object.dec =
        options.json && !isFinite(message.dec)
          ? String(message.dec)
          : message.dec;
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      object.targetName = message.targetName;
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      object.gotoOnly = message.gotoOnly;
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    ) {
      object.rotation = message.rotation;
      if (options.oneofs) object._rotation = "rotation";
    }
    return object;
  };

  /**
   * Converts this ReqGotoDSO to JSON.
   * @function toJSON
   * @memberof ReqGotoDSO
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGotoDSO.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGotoDSO
   * @function getTypeUrl
   * @memberof ReqGotoDSO
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGotoDSO.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGotoDSO";
  };

  return ReqGotoDSO;
})();

$root.ReqGotoSolarSystem = (function () {
  /**
   * Properties of a ReqGotoSolarSystem.
   * @exports IReqGotoSolarSystem
   * @interface IReqGotoSolarSystem
   * @property {number|null} [index] ReqGotoSolarSystem index
   * @property {number|null} [lon] ReqGotoSolarSystem lon
   * @property {number|null} [lat] ReqGotoSolarSystem lat
   * @property {string|null} [targetName] ReqGotoSolarSystem targetName
   * @property {boolean|null} [forceStart] ReqGotoSolarSystem forceStart
   */

  /**
   * Constructs a new ReqGotoSolarSystem.
   * @exports ReqGotoSolarSystem
   * @classdesc Represents a ReqGotoSolarSystem.
   * @implements IReqGotoSolarSystem
   * @constructor
   * @param {IReqGotoSolarSystem=} [properties] Properties to set
   */
  function ReqGotoSolarSystem(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGotoSolarSystem index.
   * @member {number} index
   * @memberof ReqGotoSolarSystem
   * @instance
   */
  ReqGotoSolarSystem.prototype.index = 0;

  /**
   * ReqGotoSolarSystem lon.
   * @member {number} lon
   * @memberof ReqGotoSolarSystem
   * @instance
   */
  ReqGotoSolarSystem.prototype.lon = 0;

  /**
   * ReqGotoSolarSystem lat.
   * @member {number} lat
   * @memberof ReqGotoSolarSystem
   * @instance
   */
  ReqGotoSolarSystem.prototype.lat = 0;

  /**
   * ReqGotoSolarSystem targetName.
   * @member {string} targetName
   * @memberof ReqGotoSolarSystem
   * @instance
   */
  ReqGotoSolarSystem.prototype.targetName = "";

  /**
   * ReqGotoSolarSystem forceStart.
   * @member {boolean} forceStart
   * @memberof ReqGotoSolarSystem
   * @instance
   */
  ReqGotoSolarSystem.prototype.forceStart = false;

  /**
   * Creates a new ReqGotoSolarSystem instance using the specified properties.
   * @function create
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {IReqGotoSolarSystem=} [properties] Properties to set
   * @returns {ReqGotoSolarSystem} ReqGotoSolarSystem instance
   */
  ReqGotoSolarSystem.create = function create(properties) {
    return new ReqGotoSolarSystem(properties);
  };

  /**
   * Encodes the specified ReqGotoSolarSystem message. Does not implicitly {@link ReqGotoSolarSystem.verify|verify} messages.
   * @function encode
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {IReqGotoSolarSystem} message ReqGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGotoSolarSystem.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.lat);
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.targetName);
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).bool(message.forceStart);
    return writer;
  };

  /**
   * Encodes the specified ReqGotoSolarSystem message, length delimited. Does not implicitly {@link ReqGotoSolarSystem.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {IReqGotoSolarSystem} message ReqGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGotoSolarSystem.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGotoSolarSystem message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGotoSolarSystem} ReqGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGotoSolarSystem.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGotoSolarSystem();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        case 2: {
          message.lon = reader.double();
          break;
        }
        case 3: {
          message.lat = reader.double();
          break;
        }
        case 4: {
          message.targetName = reader.string();
          break;
        }
        case 5: {
          message.forceStart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGotoSolarSystem message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGotoSolarSystem} ReqGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGotoSolarSystem.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGotoSolarSystem message.
   * @function verify
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGotoSolarSystem.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      if (!$util.isString(message.targetName))
        return "targetName: string expected";
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      if (typeof message.forceStart !== "boolean")
        return "forceStart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqGotoSolarSystem message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGotoSolarSystem} ReqGotoSolarSystem
   */
  ReqGotoSolarSystem.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGotoSolarSystem) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGotoSolarSystem: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGotoSolarSystem();
    if (object.index != null) message.index = object.index | 0;
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    if (object.targetName != null)
      message.targetName = String(object.targetName);
    if (object.forceStart != null)
      message.forceStart = Boolean(object.forceStart);
    return message;
  };

  /**
   * Creates a plain object from a ReqGotoSolarSystem message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {ReqGotoSolarSystem} message ReqGotoSolarSystem
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGotoSolarSystem.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.index = 0;
      object.lon = 0;
      object.lat = 0;
      object.targetName = "";
      object.forceStart = false;
    }
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      object.targetName = message.targetName;
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      object.forceStart = message.forceStart;
    return object;
  };

  /**
   * Converts this ReqGotoSolarSystem to JSON.
   * @function toJSON
   * @memberof ReqGotoSolarSystem
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGotoSolarSystem.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGotoSolarSystem
   * @function getTypeUrl
   * @memberof ReqGotoSolarSystem
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGotoSolarSystem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGotoSolarSystem";
  };

  return ReqGotoSolarSystem;
})();

$root.ResGotoSolarSystem = (function () {
  /**
   * Properties of a ResGotoSolarSystem.
   * @exports IResGotoSolarSystem
   * @interface IResGotoSolarSystem
   * @property {number|null} [code] ResGotoSolarSystem code
   * @property {IReqGotoSolarSystem|null} [req] ResGotoSolarSystem req
   */

  /**
   * Constructs a new ResGotoSolarSystem.
   * @exports ResGotoSolarSystem
   * @classdesc Represents a ResGotoSolarSystem.
   * @implements IResGotoSolarSystem
   * @constructor
   * @param {IResGotoSolarSystem=} [properties] Properties to set
   */
  function ResGotoSolarSystem(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGotoSolarSystem code.
   * @member {number} code
   * @memberof ResGotoSolarSystem
   * @instance
   */
  ResGotoSolarSystem.prototype.code = 0;

  /**
   * ResGotoSolarSystem req.
   * @member {IReqGotoSolarSystem|null|undefined} req
   * @memberof ResGotoSolarSystem
   * @instance
   */
  ResGotoSolarSystem.prototype.req = null;

  /**
   * Creates a new ResGotoSolarSystem instance using the specified properties.
   * @function create
   * @memberof ResGotoSolarSystem
   * @static
   * @param {IResGotoSolarSystem=} [properties] Properties to set
   * @returns {ResGotoSolarSystem} ResGotoSolarSystem instance
   */
  ResGotoSolarSystem.create = function create(properties) {
    return new ResGotoSolarSystem(properties);
  };

  /**
   * Encodes the specified ResGotoSolarSystem message. Does not implicitly {@link ResGotoSolarSystem.verify|verify} messages.
   * @function encode
   * @memberof ResGotoSolarSystem
   * @static
   * @param {IResGotoSolarSystem} message ResGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGotoSolarSystem.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.req != null && Object.hasOwnProperty.call(message, "req"))
      $root.ReqGotoSolarSystem.encode(
        message.req,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResGotoSolarSystem message, length delimited. Does not implicitly {@link ResGotoSolarSystem.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGotoSolarSystem
   * @static
   * @param {IResGotoSolarSystem} message ResGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGotoSolarSystem.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGotoSolarSystem message from the specified reader or buffer.
   * @function decode
   * @memberof ResGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGotoSolarSystem} ResGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGotoSolarSystem.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGotoSolarSystem();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.req = $root.ReqGotoSolarSystem.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGotoSolarSystem message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGotoSolarSystem} ResGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGotoSolarSystem.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGotoSolarSystem message.
   * @function verify
   * @memberof ResGotoSolarSystem
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGotoSolarSystem.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.req != null && Object.hasOwnProperty.call(message, "req")) {
      var error = $root.ReqGotoSolarSystem.verify(message.req, long + 1);
      if (error) return "req." + error;
    }
    return null;
  };

  /**
   * Creates a ResGotoSolarSystem message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGotoSolarSystem
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGotoSolarSystem} ResGotoSolarSystem
   */
  ResGotoSolarSystem.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGotoSolarSystem) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGotoSolarSystem: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGotoSolarSystem();
    if (object.code != null) message.code = object.code | 0;
    if (object.req != null) {
      if (!$util.isObject(object.req))
        throw TypeError(".ResGotoSolarSystem.req: object expected");
      message.req = $root.ReqGotoSolarSystem.fromObject(object.req, long + 1);
    }
    return message;
  };

  /**
   * Creates a plain object from a ResGotoSolarSystem message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGotoSolarSystem
   * @static
   * @param {ResGotoSolarSystem} message ResGotoSolarSystem
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGotoSolarSystem.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.req = null;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.req != null && Object.hasOwnProperty.call(message, "req"))
      object.req = $root.ReqGotoSolarSystem.toObject(
        message.req,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ResGotoSolarSystem to JSON.
   * @function toJSON
   * @memberof ResGotoSolarSystem
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGotoSolarSystem.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGotoSolarSystem
   * @function getTypeUrl
   * @memberof ResGotoSolarSystem
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGotoSolarSystem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGotoSolarSystem";
  };

  return ResGotoSolarSystem;
})();

$root.ReqStopGoto = (function () {
  /**
   * Properties of a ReqStopGoto.
   * @exports IReqStopGoto
   * @interface IReqStopGoto
   */

  /**
   * Constructs a new ReqStopGoto.
   * @exports ReqStopGoto
   * @classdesc Represents a ReqStopGoto.
   * @implements IReqStopGoto
   * @constructor
   * @param {IReqStopGoto=} [properties] Properties to set
   */
  function ReqStopGoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopGoto instance using the specified properties.
   * @function create
   * @memberof ReqStopGoto
   * @static
   * @param {IReqStopGoto=} [properties] Properties to set
   * @returns {ReqStopGoto} ReqStopGoto instance
   */
  ReqStopGoto.create = function create(properties) {
    return new ReqStopGoto(properties);
  };

  /**
   * Encodes the specified ReqStopGoto message. Does not implicitly {@link ReqStopGoto.verify|verify} messages.
   * @function encode
   * @memberof ReqStopGoto
   * @static
   * @param {IReqStopGoto} message ReqStopGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopGoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopGoto message, length delimited. Does not implicitly {@link ReqStopGoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopGoto
   * @static
   * @param {IReqStopGoto} message ReqStopGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopGoto.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopGoto message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopGoto} ReqStopGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopGoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopGoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopGoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopGoto} ReqStopGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopGoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopGoto message.
   * @function verify
   * @memberof ReqStopGoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopGoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopGoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopGoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopGoto} ReqStopGoto
   */
  ReqStopGoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopGoto) return object;
    return new $root.ReqStopGoto();
  };

  /**
   * Creates a plain object from a ReqStopGoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopGoto
   * @static
   * @param {ReqStopGoto} message ReqStopGoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopGoto.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopGoto to JSON.
   * @function toJSON
   * @memberof ReqStopGoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopGoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopGoto
   * @function getTypeUrl
   * @memberof ReqStopGoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopGoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopGoto";
  };

  return ReqStopGoto;
})();

$root.ReqCaptureRawLiveStacking = (function () {
  /**
   * Properties of a ReqCaptureRawLiveStacking.
   * @exports IReqCaptureRawLiveStacking
   * @interface IReqCaptureRawLiveStacking
   * @property {number|null} [irIndex] ReqCaptureRawLiveStacking irIndex
   * @property {boolean|null} [forceStart] ReqCaptureRawLiveStacking forceStart
   */

  /**
   * Constructs a new ReqCaptureRawLiveStacking.
   * @exports ReqCaptureRawLiveStacking
   * @classdesc Represents a ReqCaptureRawLiveStacking.
   * @implements IReqCaptureRawLiveStacking
   * @constructor
   * @param {IReqCaptureRawLiveStacking=} [properties] Properties to set
   */
  function ReqCaptureRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCaptureRawLiveStacking irIndex.
   * @member {number} irIndex
   * @memberof ReqCaptureRawLiveStacking
   * @instance
   */
  ReqCaptureRawLiveStacking.prototype.irIndex = 0;

  /**
   * ReqCaptureRawLiveStacking forceStart.
   * @member {boolean} forceStart
   * @memberof ReqCaptureRawLiveStacking
   * @instance
   */
  ReqCaptureRawLiveStacking.prototype.forceStart = false;

  /**
   * Creates a new ReqCaptureRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {IReqCaptureRawLiveStacking=} [properties] Properties to set
   * @returns {ReqCaptureRawLiveStacking} ReqCaptureRawLiveStacking instance
   */
  ReqCaptureRawLiveStacking.create = function create(properties) {
    return new ReqCaptureRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqCaptureRawLiveStacking message. Does not implicitly {@link ReqCaptureRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {IReqCaptureRawLiveStacking} message ReqCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureRawLiveStacking.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.irIndex);
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.forceStart);
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureRawLiveStacking message, length delimited. Does not implicitly {@link ReqCaptureRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {IReqCaptureRawLiveStacking} message ReqCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureRawLiveStacking.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureRawLiveStacking} ReqCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.irIndex = reader.int32();
          break;
        }
        case 2: {
          message.forceStart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureRawLiveStacking} ReqCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureRawLiveStacking.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureRawLiveStacking message.
   * @function verify
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureRawLiveStacking.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      if (!$util.isInteger(message.irIndex)) return "irIndex: integer expected";
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      if (typeof message.forceStart !== "boolean")
        return "forceStart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqCaptureRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureRawLiveStacking} ReqCaptureRawLiveStacking
   */
  ReqCaptureRawLiveStacking.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureRawLiveStacking) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCaptureRawLiveStacking: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCaptureRawLiveStacking();
    if (object.irIndex != null) message.irIndex = object.irIndex | 0;
    if (object.forceStart != null)
      message.forceStart = Boolean(object.forceStart);
    return message;
  };

  /**
   * Creates a plain object from a ReqCaptureRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {ReqCaptureRawLiveStacking} message ReqCaptureRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureRawLiveStacking.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.irIndex = 0;
      object.forceStart = false;
    }
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      object.irIndex = message.irIndex;
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      object.forceStart = message.forceStart;
    return object;
  };

  /**
   * Converts this ReqCaptureRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqCaptureRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqCaptureRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureRawLiveStacking.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureRawLiveStacking";
  };

  return ReqCaptureRawLiveStacking;
})();

$root.ReqStopCaptureRawLiveStacking = (function () {
  /**
   * Properties of a ReqStopCaptureRawLiveStacking.
   * @exports IReqStopCaptureRawLiveStacking
   * @interface IReqStopCaptureRawLiveStacking
   */

  /**
   * Constructs a new ReqStopCaptureRawLiveStacking.
   * @exports ReqStopCaptureRawLiveStacking
   * @classdesc Represents a ReqStopCaptureRawLiveStacking.
   * @implements IReqStopCaptureRawLiveStacking
   * @constructor
   * @param {IReqStopCaptureRawLiveStacking=} [properties] Properties to set
   */
  function ReqStopCaptureRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCaptureRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {IReqStopCaptureRawLiveStacking=} [properties] Properties to set
   * @returns {ReqStopCaptureRawLiveStacking} ReqStopCaptureRawLiveStacking instance
   */
  ReqStopCaptureRawLiveStacking.create = function create(properties) {
    return new ReqStopCaptureRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqStopCaptureRawLiveStacking message. Does not implicitly {@link ReqStopCaptureRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {IReqStopCaptureRawLiveStacking} message ReqStopCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureRawLiveStacking.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCaptureRawLiveStacking message, length delimited. Does not implicitly {@link ReqStopCaptureRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {IReqStopCaptureRawLiveStacking} message ReqStopCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureRawLiveStacking.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCaptureRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCaptureRawLiveStacking} ReqStopCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCaptureRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCaptureRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCaptureRawLiveStacking} ReqStopCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureRawLiveStacking.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCaptureRawLiveStacking message.
   * @function verify
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCaptureRawLiveStacking.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCaptureRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCaptureRawLiveStacking} ReqStopCaptureRawLiveStacking
   */
  ReqStopCaptureRawLiveStacking.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopCaptureRawLiveStacking) return object;
    return new $root.ReqStopCaptureRawLiveStacking();
  };

  /**
   * Creates a plain object from a ReqStopCaptureRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {ReqStopCaptureRawLiveStacking} message ReqStopCaptureRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCaptureRawLiveStacking.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCaptureRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqStopCaptureRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCaptureRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCaptureRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqStopCaptureRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCaptureRawLiveStacking.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCaptureRawLiveStacking";
  };

  return ReqStopCaptureRawLiveStacking;
})();

$root.ReqFastStopCaptureRawLiveStacking = (function () {
  /**
   * Properties of a ReqFastStopCaptureRawLiveStacking.
   * @exports IReqFastStopCaptureRawLiveStacking
   * @interface IReqFastStopCaptureRawLiveStacking
   */

  /**
   * Constructs a new ReqFastStopCaptureRawLiveStacking.
   * @exports ReqFastStopCaptureRawLiveStacking
   * @classdesc Represents a ReqFastStopCaptureRawLiveStacking.
   * @implements IReqFastStopCaptureRawLiveStacking
   * @constructor
   * @param {IReqFastStopCaptureRawLiveStacking=} [properties] Properties to set
   */
  function ReqFastStopCaptureRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqFastStopCaptureRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureRawLiveStacking=} [properties] Properties to set
   * @returns {ReqFastStopCaptureRawLiveStacking} ReqFastStopCaptureRawLiveStacking instance
   */
  ReqFastStopCaptureRawLiveStacking.create = function create(properties) {
    return new ReqFastStopCaptureRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqFastStopCaptureRawLiveStacking message. Does not implicitly {@link ReqFastStopCaptureRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureRawLiveStacking} message ReqFastStopCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFastStopCaptureRawLiveStacking.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqFastStopCaptureRawLiveStacking message, length delimited. Does not implicitly {@link ReqFastStopCaptureRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureRawLiveStacking} message ReqFastStopCaptureRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFastStopCaptureRawLiveStacking.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFastStopCaptureRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFastStopCaptureRawLiveStacking} ReqFastStopCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFastStopCaptureRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFastStopCaptureRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFastStopCaptureRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFastStopCaptureRawLiveStacking} ReqFastStopCaptureRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFastStopCaptureRawLiveStacking.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFastStopCaptureRawLiveStacking message.
   * @function verify
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFastStopCaptureRawLiveStacking.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqFastStopCaptureRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFastStopCaptureRawLiveStacking} ReqFastStopCaptureRawLiveStacking
   */
  ReqFastStopCaptureRawLiveStacking.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqFastStopCaptureRawLiveStacking)
      return object;
    return new $root.ReqFastStopCaptureRawLiveStacking();
  };

  /**
   * Creates a plain object from a ReqFastStopCaptureRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {ReqFastStopCaptureRawLiveStacking} message ReqFastStopCaptureRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFastStopCaptureRawLiveStacking.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqFastStopCaptureRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFastStopCaptureRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFastStopCaptureRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqFastStopCaptureRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFastStopCaptureRawLiveStacking.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFastStopCaptureRawLiveStacking";
  };

  return ReqFastStopCaptureRawLiveStacking;
})();

$root.ReqCheckDarkFrame = (function () {
  /**
   * Properties of a ReqCheckDarkFrame.
   * @exports IReqCheckDarkFrame
   * @interface IReqCheckDarkFrame
   */

  /**
   * Constructs a new ReqCheckDarkFrame.
   * @exports ReqCheckDarkFrame
   * @classdesc Represents a ReqCheckDarkFrame.
   * @implements IReqCheckDarkFrame
   * @constructor
   * @param {IReqCheckDarkFrame=} [properties] Properties to set
   */
  function ReqCheckDarkFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCheckDarkFrame instance using the specified properties.
   * @function create
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {IReqCheckDarkFrame=} [properties] Properties to set
   * @returns {ReqCheckDarkFrame} ReqCheckDarkFrame instance
   */
  ReqCheckDarkFrame.create = function create(properties) {
    return new ReqCheckDarkFrame(properties);
  };

  /**
   * Encodes the specified ReqCheckDarkFrame message. Does not implicitly {@link ReqCheckDarkFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {IReqCheckDarkFrame} message ReqCheckDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCheckDarkFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCheckDarkFrame message, length delimited. Does not implicitly {@link ReqCheckDarkFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {IReqCheckDarkFrame} message ReqCheckDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCheckDarkFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCheckDarkFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCheckDarkFrame} ReqCheckDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCheckDarkFrame.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCheckDarkFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCheckDarkFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCheckDarkFrame} ReqCheckDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCheckDarkFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCheckDarkFrame message.
   * @function verify
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCheckDarkFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCheckDarkFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCheckDarkFrame} ReqCheckDarkFrame
   */
  ReqCheckDarkFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCheckDarkFrame) return object;
    return new $root.ReqCheckDarkFrame();
  };

  /**
   * Creates a plain object from a ReqCheckDarkFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {ReqCheckDarkFrame} message ReqCheckDarkFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCheckDarkFrame.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCheckDarkFrame to JSON.
   * @function toJSON
   * @memberof ReqCheckDarkFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCheckDarkFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCheckDarkFrame
   * @function getTypeUrl
   * @memberof ReqCheckDarkFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCheckDarkFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCheckDarkFrame";
  };

  return ReqCheckDarkFrame;
})();

$root.ResCheckDarkFrame = (function () {
  /**
   * Properties of a ResCheckDarkFrame.
   * @exports IResCheckDarkFrame
   * @interface IResCheckDarkFrame
   * @property {number|null} [progress] ResCheckDarkFrame progress
   * @property {number|null} [code] ResCheckDarkFrame code
   */

  /**
   * Constructs a new ResCheckDarkFrame.
   * @exports ResCheckDarkFrame
   * @classdesc Represents a ResCheckDarkFrame.
   * @implements IResCheckDarkFrame
   * @constructor
   * @param {IResCheckDarkFrame=} [properties] Properties to set
   */
  function ResCheckDarkFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResCheckDarkFrame progress.
   * @member {number} progress
   * @memberof ResCheckDarkFrame
   * @instance
   */
  ResCheckDarkFrame.prototype.progress = 0;

  /**
   * ResCheckDarkFrame code.
   * @member {number} code
   * @memberof ResCheckDarkFrame
   * @instance
   */
  ResCheckDarkFrame.prototype.code = 0;

  /**
   * Creates a new ResCheckDarkFrame instance using the specified properties.
   * @function create
   * @memberof ResCheckDarkFrame
   * @static
   * @param {IResCheckDarkFrame=} [properties] Properties to set
   * @returns {ResCheckDarkFrame} ResCheckDarkFrame instance
   */
  ResCheckDarkFrame.create = function create(properties) {
    return new ResCheckDarkFrame(properties);
  };

  /**
   * Encodes the specified ResCheckDarkFrame message. Does not implicitly {@link ResCheckDarkFrame.verify|verify} messages.
   * @function encode
   * @memberof ResCheckDarkFrame
   * @static
   * @param {IResCheckDarkFrame} message ResCheckDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCheckDarkFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.progress);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResCheckDarkFrame message, length delimited. Does not implicitly {@link ResCheckDarkFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResCheckDarkFrame
   * @static
   * @param {IResCheckDarkFrame} message ResCheckDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCheckDarkFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResCheckDarkFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ResCheckDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResCheckDarkFrame} ResCheckDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCheckDarkFrame.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResCheckDarkFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.progress = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResCheckDarkFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResCheckDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResCheckDarkFrame} ResCheckDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCheckDarkFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResCheckDarkFrame message.
   * @function verify
   * @memberof ResCheckDarkFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResCheckDarkFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      if (!$util.isInteger(message.progress))
        return "progress: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResCheckDarkFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResCheckDarkFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResCheckDarkFrame} ResCheckDarkFrame
   */
  ResCheckDarkFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResCheckDarkFrame) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResCheckDarkFrame: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResCheckDarkFrame();
    if (object.progress != null) message.progress = object.progress | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResCheckDarkFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResCheckDarkFrame
   * @static
   * @param {ResCheckDarkFrame} message ResCheckDarkFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResCheckDarkFrame.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.progress = 0;
      object.code = 0;
    }
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      object.progress = message.progress;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResCheckDarkFrame to JSON.
   * @function toJSON
   * @memberof ResCheckDarkFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResCheckDarkFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResCheckDarkFrame
   * @function getTypeUrl
   * @memberof ResCheckDarkFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResCheckDarkFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResCheckDarkFrame";
  };

  return ResCheckDarkFrame;
})();

$root.ReqCaptureDarkFrame = (function () {
  /**
   * Properties of a ReqCaptureDarkFrame.
   * @exports IReqCaptureDarkFrame
   * @interface IReqCaptureDarkFrame
   * @property {number|null} [reshoot] ReqCaptureDarkFrame reshoot
   */

  /**
   * Constructs a new ReqCaptureDarkFrame.
   * @exports ReqCaptureDarkFrame
   * @classdesc Represents a ReqCaptureDarkFrame.
   * @implements IReqCaptureDarkFrame
   * @constructor
   * @param {IReqCaptureDarkFrame=} [properties] Properties to set
   */
  function ReqCaptureDarkFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCaptureDarkFrame reshoot.
   * @member {number} reshoot
   * @memberof ReqCaptureDarkFrame
   * @instance
   */
  ReqCaptureDarkFrame.prototype.reshoot = 0;

  /**
   * Creates a new ReqCaptureDarkFrame instance using the specified properties.
   * @function create
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {IReqCaptureDarkFrame=} [properties] Properties to set
   * @returns {ReqCaptureDarkFrame} ReqCaptureDarkFrame instance
   */
  ReqCaptureDarkFrame.create = function create(properties) {
    return new ReqCaptureDarkFrame(properties);
  };

  /**
   * Encodes the specified ReqCaptureDarkFrame message. Does not implicitly {@link ReqCaptureDarkFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {IReqCaptureDarkFrame} message ReqCaptureDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureDarkFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.reshoot != null &&
      Object.hasOwnProperty.call(message, "reshoot")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.reshoot);
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureDarkFrame message, length delimited. Does not implicitly {@link ReqCaptureDarkFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {IReqCaptureDarkFrame} message ReqCaptureDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureDarkFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureDarkFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureDarkFrame} ReqCaptureDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureDarkFrame.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureDarkFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.reshoot = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureDarkFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureDarkFrame} ReqCaptureDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureDarkFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureDarkFrame message.
   * @function verify
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureDarkFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.reshoot != null &&
      Object.hasOwnProperty.call(message, "reshoot")
    )
      if (!$util.isInteger(message.reshoot)) return "reshoot: integer expected";
    return null;
  };

  /**
   * Creates a ReqCaptureDarkFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureDarkFrame} ReqCaptureDarkFrame
   */
  ReqCaptureDarkFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureDarkFrame) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCaptureDarkFrame: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCaptureDarkFrame();
    if (object.reshoot != null) message.reshoot = object.reshoot | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqCaptureDarkFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {ReqCaptureDarkFrame} message ReqCaptureDarkFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureDarkFrame.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.reshoot = 0;
    if (
      message.reshoot != null &&
      Object.hasOwnProperty.call(message, "reshoot")
    )
      object.reshoot = message.reshoot;
    return object;
  };

  /**
   * Converts this ReqCaptureDarkFrame to JSON.
   * @function toJSON
   * @memberof ReqCaptureDarkFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureDarkFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureDarkFrame
   * @function getTypeUrl
   * @memberof ReqCaptureDarkFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureDarkFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureDarkFrame";
  };

  return ReqCaptureDarkFrame;
})();

$root.ReqStopCaptureDarkFrame = (function () {
  /**
   * Properties of a ReqStopCaptureDarkFrame.
   * @exports IReqStopCaptureDarkFrame
   * @interface IReqStopCaptureDarkFrame
   */

  /**
   * Constructs a new ReqStopCaptureDarkFrame.
   * @exports ReqStopCaptureDarkFrame
   * @classdesc Represents a ReqStopCaptureDarkFrame.
   * @implements IReqStopCaptureDarkFrame
   * @constructor
   * @param {IReqStopCaptureDarkFrame=} [properties] Properties to set
   */
  function ReqStopCaptureDarkFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCaptureDarkFrame instance using the specified properties.
   * @function create
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {IReqStopCaptureDarkFrame=} [properties] Properties to set
   * @returns {ReqStopCaptureDarkFrame} ReqStopCaptureDarkFrame instance
   */
  ReqStopCaptureDarkFrame.create = function create(properties) {
    return new ReqStopCaptureDarkFrame(properties);
  };

  /**
   * Encodes the specified ReqStopCaptureDarkFrame message. Does not implicitly {@link ReqStopCaptureDarkFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {IReqStopCaptureDarkFrame} message ReqStopCaptureDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureDarkFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCaptureDarkFrame message, length delimited. Does not implicitly {@link ReqStopCaptureDarkFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {IReqStopCaptureDarkFrame} message ReqStopCaptureDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureDarkFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCaptureDarkFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCaptureDarkFrame} ReqStopCaptureDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureDarkFrame.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCaptureDarkFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCaptureDarkFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCaptureDarkFrame} ReqStopCaptureDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureDarkFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCaptureDarkFrame message.
   * @function verify
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCaptureDarkFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCaptureDarkFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCaptureDarkFrame} ReqStopCaptureDarkFrame
   */
  ReqStopCaptureDarkFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopCaptureDarkFrame) return object;
    return new $root.ReqStopCaptureDarkFrame();
  };

  /**
   * Creates a plain object from a ReqStopCaptureDarkFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {ReqStopCaptureDarkFrame} message ReqStopCaptureDarkFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCaptureDarkFrame.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCaptureDarkFrame to JSON.
   * @function toJSON
   * @memberof ReqStopCaptureDarkFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCaptureDarkFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCaptureDarkFrame
   * @function getTypeUrl
   * @memberof ReqStopCaptureDarkFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCaptureDarkFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCaptureDarkFrame";
  };

  return ReqStopCaptureDarkFrame;
})();

$root.ReqCaptureDarkFrameWithParam = (function () {
  /**
   * Properties of a ReqCaptureDarkFrameWithParam.
   * @exports IReqCaptureDarkFrameWithParam
   * @interface IReqCaptureDarkFrameWithParam
   * @property {number|null} [expIndex] ReqCaptureDarkFrameWithParam expIndex
   * @property {number|null} [gainIndex] ReqCaptureDarkFrameWithParam gainIndex
   * @property {number|null} [binIndex] ReqCaptureDarkFrameWithParam binIndex
   * @property {number|null} [capSize] ReqCaptureDarkFrameWithParam capSize
   */

  /**
   * Constructs a new ReqCaptureDarkFrameWithParam.
   * @exports ReqCaptureDarkFrameWithParam
   * @classdesc Represents a ReqCaptureDarkFrameWithParam.
   * @implements IReqCaptureDarkFrameWithParam
   * @constructor
   * @param {IReqCaptureDarkFrameWithParam=} [properties] Properties to set
   */
  function ReqCaptureDarkFrameWithParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCaptureDarkFrameWithParam expIndex.
   * @member {number} expIndex
   * @memberof ReqCaptureDarkFrameWithParam
   * @instance
   */
  ReqCaptureDarkFrameWithParam.prototype.expIndex = 0;

  /**
   * ReqCaptureDarkFrameWithParam gainIndex.
   * @member {number} gainIndex
   * @memberof ReqCaptureDarkFrameWithParam
   * @instance
   */
  ReqCaptureDarkFrameWithParam.prototype.gainIndex = 0;

  /**
   * ReqCaptureDarkFrameWithParam binIndex.
   * @member {number} binIndex
   * @memberof ReqCaptureDarkFrameWithParam
   * @instance
   */
  ReqCaptureDarkFrameWithParam.prototype.binIndex = 0;

  /**
   * ReqCaptureDarkFrameWithParam capSize.
   * @member {number} capSize
   * @memberof ReqCaptureDarkFrameWithParam
   * @instance
   */
  ReqCaptureDarkFrameWithParam.prototype.capSize = 0;

  /**
   * Creates a new ReqCaptureDarkFrameWithParam instance using the specified properties.
   * @function create
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {IReqCaptureDarkFrameWithParam=} [properties] Properties to set
   * @returns {ReqCaptureDarkFrameWithParam} ReqCaptureDarkFrameWithParam instance
   */
  ReqCaptureDarkFrameWithParam.create = function create(properties) {
    return new ReqCaptureDarkFrameWithParam(properties);
  };

  /**
   * Encodes the specified ReqCaptureDarkFrameWithParam message. Does not implicitly {@link ReqCaptureDarkFrameWithParam.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {IReqCaptureDarkFrameWithParam} message ReqCaptureDarkFrameWithParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureDarkFrameWithParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expIndex);
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.gainIndex);
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.binIndex);
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.capSize);
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureDarkFrameWithParam message, length delimited. Does not implicitly {@link ReqCaptureDarkFrameWithParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {IReqCaptureDarkFrameWithParam} message ReqCaptureDarkFrameWithParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureDarkFrameWithParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureDarkFrameWithParam message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureDarkFrameWithParam} ReqCaptureDarkFrameWithParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureDarkFrameWithParam.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureDarkFrameWithParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expIndex = reader.int32();
          break;
        }
        case 2: {
          message.gainIndex = reader.int32();
          break;
        }
        case 3: {
          message.binIndex = reader.int32();
          break;
        }
        case 4: {
          message.capSize = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureDarkFrameWithParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureDarkFrameWithParam} ReqCaptureDarkFrameWithParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureDarkFrameWithParam.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureDarkFrameWithParam message.
   * @function verify
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureDarkFrameWithParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      if (!$util.isInteger(message.gainIndex))
        return "gainIndex: integer expected";
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      if (!$util.isInteger(message.binIndex))
        return "binIndex: integer expected";
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      if (!$util.isInteger(message.capSize)) return "capSize: integer expected";
    return null;
  };

  /**
   * Creates a ReqCaptureDarkFrameWithParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureDarkFrameWithParam} ReqCaptureDarkFrameWithParam
   */
  ReqCaptureDarkFrameWithParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureDarkFrameWithParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCaptureDarkFrameWithParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCaptureDarkFrameWithParam();
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
    if (object.binIndex != null) message.binIndex = object.binIndex | 0;
    if (object.capSize != null) message.capSize = object.capSize | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqCaptureDarkFrameWithParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {ReqCaptureDarkFrameWithParam} message ReqCaptureDarkFrameWithParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureDarkFrameWithParam.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expIndex = 0;
      object.gainIndex = 0;
      object.binIndex = 0;
      object.capSize = 0;
    }
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      object.gainIndex = message.gainIndex;
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      object.binIndex = message.binIndex;
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      object.capSize = message.capSize;
    return object;
  };

  /**
   * Converts this ReqCaptureDarkFrameWithParam to JSON.
   * @function toJSON
   * @memberof ReqCaptureDarkFrameWithParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureDarkFrameWithParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureDarkFrameWithParam
   * @function getTypeUrl
   * @memberof ReqCaptureDarkFrameWithParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureDarkFrameWithParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureDarkFrameWithParam";
  };

  return ReqCaptureDarkFrameWithParam;
})();

$root.ReqStopCaptureDarkFrameWithParam = (function () {
  /**
   * Properties of a ReqStopCaptureDarkFrameWithParam.
   * @exports IReqStopCaptureDarkFrameWithParam
   * @interface IReqStopCaptureDarkFrameWithParam
   */

  /**
   * Constructs a new ReqStopCaptureDarkFrameWithParam.
   * @exports ReqStopCaptureDarkFrameWithParam
   * @classdesc Represents a ReqStopCaptureDarkFrameWithParam.
   * @implements IReqStopCaptureDarkFrameWithParam
   * @constructor
   * @param {IReqStopCaptureDarkFrameWithParam=} [properties] Properties to set
   */
  function ReqStopCaptureDarkFrameWithParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCaptureDarkFrameWithParam instance using the specified properties.
   * @function create
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {IReqStopCaptureDarkFrameWithParam=} [properties] Properties to set
   * @returns {ReqStopCaptureDarkFrameWithParam} ReqStopCaptureDarkFrameWithParam instance
   */
  ReqStopCaptureDarkFrameWithParam.create = function create(properties) {
    return new ReqStopCaptureDarkFrameWithParam(properties);
  };

  /**
   * Encodes the specified ReqStopCaptureDarkFrameWithParam message. Does not implicitly {@link ReqStopCaptureDarkFrameWithParam.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {IReqStopCaptureDarkFrameWithParam} message ReqStopCaptureDarkFrameWithParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureDarkFrameWithParam.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCaptureDarkFrameWithParam message, length delimited. Does not implicitly {@link ReqStopCaptureDarkFrameWithParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {IReqStopCaptureDarkFrameWithParam} message ReqStopCaptureDarkFrameWithParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureDarkFrameWithParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCaptureDarkFrameWithParam message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCaptureDarkFrameWithParam} ReqStopCaptureDarkFrameWithParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureDarkFrameWithParam.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCaptureDarkFrameWithParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCaptureDarkFrameWithParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCaptureDarkFrameWithParam} ReqStopCaptureDarkFrameWithParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureDarkFrameWithParam.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCaptureDarkFrameWithParam message.
   * @function verify
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCaptureDarkFrameWithParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCaptureDarkFrameWithParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCaptureDarkFrameWithParam} ReqStopCaptureDarkFrameWithParam
   */
  ReqStopCaptureDarkFrameWithParam.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqStopCaptureDarkFrameWithParam) return object;
    return new $root.ReqStopCaptureDarkFrameWithParam();
  };

  /**
   * Creates a plain object from a ReqStopCaptureDarkFrameWithParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {ReqStopCaptureDarkFrameWithParam} message ReqStopCaptureDarkFrameWithParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCaptureDarkFrameWithParam.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCaptureDarkFrameWithParam to JSON.
   * @function toJSON
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCaptureDarkFrameWithParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCaptureDarkFrameWithParam
   * @function getTypeUrl
   * @memberof ReqStopCaptureDarkFrameWithParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCaptureDarkFrameWithParam.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCaptureDarkFrameWithParam";
  };

  return ReqStopCaptureDarkFrameWithParam;
})();

$root.ReqGetDarkFrameList = (function () {
  /**
   * Properties of a ReqGetDarkFrameList.
   * @exports IReqGetDarkFrameList
   * @interface IReqGetDarkFrameList
   */

  /**
   * Constructs a new ReqGetDarkFrameList.
   * @exports ReqGetDarkFrameList
   * @classdesc Represents a ReqGetDarkFrameList.
   * @implements IReqGetDarkFrameList
   * @constructor
   * @param {IReqGetDarkFrameList=} [properties] Properties to set
   */
  function ReqGetDarkFrameList(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetDarkFrameList instance using the specified properties.
   * @function create
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {IReqGetDarkFrameList=} [properties] Properties to set
   * @returns {ReqGetDarkFrameList} ReqGetDarkFrameList instance
   */
  ReqGetDarkFrameList.create = function create(properties) {
    return new ReqGetDarkFrameList(properties);
  };

  /**
   * Encodes the specified ReqGetDarkFrameList message. Does not implicitly {@link ReqGetDarkFrameList.verify|verify} messages.
   * @function encode
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {IReqGetDarkFrameList} message ReqGetDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetDarkFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetDarkFrameList message, length delimited. Does not implicitly {@link ReqGetDarkFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {IReqGetDarkFrameList} message ReqGetDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetDarkFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetDarkFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetDarkFrameList} ReqGetDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetDarkFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetDarkFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetDarkFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetDarkFrameList} ReqGetDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetDarkFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetDarkFrameList message.
   * @function verify
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetDarkFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetDarkFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetDarkFrameList} ReqGetDarkFrameList
   */
  ReqGetDarkFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetDarkFrameList) return object;
    return new $root.ReqGetDarkFrameList();
  };

  /**
   * Creates a plain object from a ReqGetDarkFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {ReqGetDarkFrameList} message ReqGetDarkFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetDarkFrameList.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetDarkFrameList to JSON.
   * @function toJSON
   * @memberof ReqGetDarkFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetDarkFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetDarkFrameList
   * @function getTypeUrl
   * @memberof ReqGetDarkFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetDarkFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetDarkFrameList";
  };

  return ReqGetDarkFrameList;
})();

$root.ResGetDarkFrameInfo = (function () {
  /**
   * Properties of a ResGetDarkFrameInfo.
   * @exports IResGetDarkFrameInfo
   * @interface IResGetDarkFrameInfo
   * @property {number|null} [expIndex] ResGetDarkFrameInfo expIndex
   * @property {number|null} [gainIndex] ResGetDarkFrameInfo gainIndex
   * @property {number|null} [binIndex] ResGetDarkFrameInfo binIndex
   * @property {string|null} [expName] ResGetDarkFrameInfo expName
   * @property {string|null} [gainName] ResGetDarkFrameInfo gainName
   * @property {string|null} [binName] ResGetDarkFrameInfo binName
   * @property {number|null} [temperature] ResGetDarkFrameInfo temperature
   */

  /**
   * Constructs a new ResGetDarkFrameInfo.
   * @exports ResGetDarkFrameInfo
   * @classdesc Represents a ResGetDarkFrameInfo.
   * @implements IResGetDarkFrameInfo
   * @constructor
   * @param {IResGetDarkFrameInfo=} [properties] Properties to set
   */
  function ResGetDarkFrameInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetDarkFrameInfo expIndex.
   * @member {number} expIndex
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.expIndex = 0;

  /**
   * ResGetDarkFrameInfo gainIndex.
   * @member {number} gainIndex
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.gainIndex = 0;

  /**
   * ResGetDarkFrameInfo binIndex.
   * @member {number} binIndex
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.binIndex = 0;

  /**
   * ResGetDarkFrameInfo expName.
   * @member {string} expName
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.expName = "";

  /**
   * ResGetDarkFrameInfo gainName.
   * @member {string} gainName
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.gainName = "";

  /**
   * ResGetDarkFrameInfo binName.
   * @member {string} binName
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.binName = "";

  /**
   * ResGetDarkFrameInfo temperature.
   * @member {number|null|undefined} temperature
   * @memberof ResGetDarkFrameInfo
   * @instance
   */
  ResGetDarkFrameInfo.prototype.temperature = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResGetDarkFrameInfo.prototype, "_temperature", {
    get: $util.oneOfGetter(($oneOfFields = ["temperature"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ResGetDarkFrameInfo instance using the specified properties.
   * @function create
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {IResGetDarkFrameInfo=} [properties] Properties to set
   * @returns {ResGetDarkFrameInfo} ResGetDarkFrameInfo instance
   */
  ResGetDarkFrameInfo.create = function create(properties) {
    return new ResGetDarkFrameInfo(properties);
  };

  /**
   * Encodes the specified ResGetDarkFrameInfo message. Does not implicitly {@link ResGetDarkFrameInfo.verify|verify} messages.
   * @function encode
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {IResGetDarkFrameInfo} message ResGetDarkFrameInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDarkFrameInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expIndex);
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.gainIndex);
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.binIndex);
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.expName);
    if (
      message.gainName != null &&
      Object.hasOwnProperty.call(message, "gainName")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.gainName);
    if (
      message.binName != null &&
      Object.hasOwnProperty.call(message, "binName")
    )
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.binName);
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.temperature);
    return writer;
  };

  /**
   * Encodes the specified ResGetDarkFrameInfo message, length delimited. Does not implicitly {@link ResGetDarkFrameInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {IResGetDarkFrameInfo} message ResGetDarkFrameInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDarkFrameInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetDarkFrameInfo message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetDarkFrameInfo} ResGetDarkFrameInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDarkFrameInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetDarkFrameInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expIndex = reader.int32();
          break;
        }
        case 2: {
          message.gainIndex = reader.int32();
          break;
        }
        case 3: {
          message.binIndex = reader.int32();
          break;
        }
        case 4: {
          message.expName = reader.string();
          break;
        }
        case 5: {
          message.gainName = reader.string();
          break;
        }
        case 6: {
          message.binName = reader.string();
          break;
        }
        case 7: {
          message.temperature = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetDarkFrameInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetDarkFrameInfo} ResGetDarkFrameInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDarkFrameInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetDarkFrameInfo message.
   * @function verify
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetDarkFrameInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      if (!$util.isInteger(message.gainIndex))
        return "gainIndex: integer expected";
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      if (!$util.isInteger(message.binIndex))
        return "binIndex: integer expected";
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      if (!$util.isString(message.expName)) return "expName: string expected";
    if (
      message.gainName != null &&
      Object.hasOwnProperty.call(message, "gainName")
    )
      if (!$util.isString(message.gainName)) return "gainName: string expected";
    if (
      message.binName != null &&
      Object.hasOwnProperty.call(message, "binName")
    )
      if (!$util.isString(message.binName)) return "binName: string expected";
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    ) {
      properties._temperature = 1;
      if (!$util.isInteger(message.temperature))
        return "temperature: integer expected";
    }
    return null;
  };

  /**
   * Creates a ResGetDarkFrameInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetDarkFrameInfo} ResGetDarkFrameInfo
   */
  ResGetDarkFrameInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetDarkFrameInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetDarkFrameInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetDarkFrameInfo();
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
    if (object.binIndex != null) message.binIndex = object.binIndex | 0;
    if (object.expName != null) message.expName = String(object.expName);
    if (object.gainName != null) message.gainName = String(object.gainName);
    if (object.binName != null) message.binName = String(object.binName);
    if (object.temperature != null)
      message.temperature = object.temperature | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetDarkFrameInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {ResGetDarkFrameInfo} message ResGetDarkFrameInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetDarkFrameInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expIndex = 0;
      object.gainIndex = 0;
      object.binIndex = 0;
      object.expName = "";
      object.gainName = "";
      object.binName = "";
    }
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      object.gainIndex = message.gainIndex;
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      object.binIndex = message.binIndex;
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      object.expName = message.expName;
    if (
      message.gainName != null &&
      Object.hasOwnProperty.call(message, "gainName")
    )
      object.gainName = message.gainName;
    if (
      message.binName != null &&
      Object.hasOwnProperty.call(message, "binName")
    )
      object.binName = message.binName;
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    ) {
      object.temperature = message.temperature;
      if (options.oneofs) object._temperature = "temperature";
    }
    return object;
  };

  /**
   * Converts this ResGetDarkFrameInfo to JSON.
   * @function toJSON
   * @memberof ResGetDarkFrameInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetDarkFrameInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetDarkFrameInfo
   * @function getTypeUrl
   * @memberof ResGetDarkFrameInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetDarkFrameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetDarkFrameInfo";
  };

  return ResGetDarkFrameInfo;
})();

$root.ResGetDarkFrameInfoList = (function () {
  /**
   * Properties of a ResGetDarkFrameInfoList.
   * @exports IResGetDarkFrameInfoList
   * @interface IResGetDarkFrameInfoList
   * @property {number|null} [code] ResGetDarkFrameInfoList code
   * @property {Array.<IResGetDarkFrameInfo>|null} [results] ResGetDarkFrameInfoList results
   */

  /**
   * Constructs a new ResGetDarkFrameInfoList.
   * @exports ResGetDarkFrameInfoList
   * @classdesc Represents a ResGetDarkFrameInfoList.
   * @implements IResGetDarkFrameInfoList
   * @constructor
   * @param {IResGetDarkFrameInfoList=} [properties] Properties to set
   */
  function ResGetDarkFrameInfoList(properties) {
    this.results = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetDarkFrameInfoList code.
   * @member {number} code
   * @memberof ResGetDarkFrameInfoList
   * @instance
   */
  ResGetDarkFrameInfoList.prototype.code = 0;

  /**
   * ResGetDarkFrameInfoList results.
   * @member {Array.<IResGetDarkFrameInfo>} results
   * @memberof ResGetDarkFrameInfoList
   * @instance
   */
  ResGetDarkFrameInfoList.prototype.results = $util.emptyArray;

  /**
   * Creates a new ResGetDarkFrameInfoList instance using the specified properties.
   * @function create
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {IResGetDarkFrameInfoList=} [properties] Properties to set
   * @returns {ResGetDarkFrameInfoList} ResGetDarkFrameInfoList instance
   */
  ResGetDarkFrameInfoList.create = function create(properties) {
    return new ResGetDarkFrameInfoList(properties);
  };

  /**
   * Encodes the specified ResGetDarkFrameInfoList message. Does not implicitly {@link ResGetDarkFrameInfoList.verify|verify} messages.
   * @function encode
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {IResGetDarkFrameInfoList} message ResGetDarkFrameInfoList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDarkFrameInfoList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.results != null && message.results.length)
      for (var i = 0; i < message.results.length; ++i)
        $root.ResGetDarkFrameInfo.encode(
          message.results[i],
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResGetDarkFrameInfoList message, length delimited. Does not implicitly {@link ResGetDarkFrameInfoList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {IResGetDarkFrameInfoList} message ResGetDarkFrameInfoList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDarkFrameInfoList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetDarkFrameInfoList message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetDarkFrameInfoList} ResGetDarkFrameInfoList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDarkFrameInfoList.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetDarkFrameInfoList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          if (!(message.results && message.results.length))
            message.results = [];
          message.results.push(
            $root.ResGetDarkFrameInfo.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetDarkFrameInfoList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetDarkFrameInfoList} ResGetDarkFrameInfoList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDarkFrameInfoList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetDarkFrameInfoList message.
   * @function verify
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetDarkFrameInfoList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.results != null &&
      Object.hasOwnProperty.call(message, "results")
    ) {
      if (!Array.isArray(message.results)) return "results: array expected";
      for (var i = 0; i < message.results.length; ++i) {
        var error = $root.ResGetDarkFrameInfo.verify(
          message.results[i],
          long + 1,
        );
        if (error) return "results." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ResGetDarkFrameInfoList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetDarkFrameInfoList} ResGetDarkFrameInfoList
   */
  ResGetDarkFrameInfoList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetDarkFrameInfoList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetDarkFrameInfoList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetDarkFrameInfoList();
    if (object.code != null) message.code = object.code | 0;
    if (object.results) {
      if (!Array.isArray(object.results))
        throw TypeError(".ResGetDarkFrameInfoList.results: array expected");
      message.results = [];
      for (var i = 0; i < object.results.length; ++i) {
        if (!$util.isObject(object.results[i]))
          throw TypeError(".ResGetDarkFrameInfoList.results: object expected");
        message.results[i] = $root.ResGetDarkFrameInfo.fromObject(
          object.results[i],
          long + 1,
        );
      }
    }
    return message;
  };

  /**
   * Creates a plain object from a ResGetDarkFrameInfoList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {ResGetDarkFrameInfoList} message ResGetDarkFrameInfoList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetDarkFrameInfoList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.results = [];
    if (options.defaults) object.code = 0;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.results && message.results.length) {
      object.results = [];
      for (var j = 0; j < message.results.length; ++j)
        object.results[j] = $root.ResGetDarkFrameInfo.toObject(
          message.results[j],
          options,
          q + 1,
        );
    }
    return object;
  };

  /**
   * Converts this ResGetDarkFrameInfoList to JSON.
   * @function toJSON
   * @memberof ResGetDarkFrameInfoList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetDarkFrameInfoList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetDarkFrameInfoList
   * @function getTypeUrl
   * @memberof ResGetDarkFrameInfoList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetDarkFrameInfoList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetDarkFrameInfoList";
  };

  return ResGetDarkFrameInfoList;
})();

$root.ReqDelDarkFrame = (function () {
  /**
   * Properties of a ReqDelDarkFrame.
   * @exports IReqDelDarkFrame
   * @interface IReqDelDarkFrame
   * @property {number|null} [expIndex] ReqDelDarkFrame expIndex
   * @property {number|null} [gainIndex] ReqDelDarkFrame gainIndex
   * @property {number|null} [binIndex] ReqDelDarkFrame binIndex
   * @property {number|null} [tempValue] ReqDelDarkFrame tempValue
   */

  /**
   * Constructs a new ReqDelDarkFrame.
   * @exports ReqDelDarkFrame
   * @classdesc Represents a ReqDelDarkFrame.
   * @implements IReqDelDarkFrame
   * @constructor
   * @param {IReqDelDarkFrame=} [properties] Properties to set
   */
  function ReqDelDarkFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqDelDarkFrame expIndex.
   * @member {number} expIndex
   * @memberof ReqDelDarkFrame
   * @instance
   */
  ReqDelDarkFrame.prototype.expIndex = 0;

  /**
   * ReqDelDarkFrame gainIndex.
   * @member {number} gainIndex
   * @memberof ReqDelDarkFrame
   * @instance
   */
  ReqDelDarkFrame.prototype.gainIndex = 0;

  /**
   * ReqDelDarkFrame binIndex.
   * @member {number} binIndex
   * @memberof ReqDelDarkFrame
   * @instance
   */
  ReqDelDarkFrame.prototype.binIndex = 0;

  /**
   * ReqDelDarkFrame tempValue.
   * @member {number} tempValue
   * @memberof ReqDelDarkFrame
   * @instance
   */
  ReqDelDarkFrame.prototype.tempValue = 0;

  /**
   * Creates a new ReqDelDarkFrame instance using the specified properties.
   * @function create
   * @memberof ReqDelDarkFrame
   * @static
   * @param {IReqDelDarkFrame=} [properties] Properties to set
   * @returns {ReqDelDarkFrame} ReqDelDarkFrame instance
   */
  ReqDelDarkFrame.create = function create(properties) {
    return new ReqDelDarkFrame(properties);
  };

  /**
   * Encodes the specified ReqDelDarkFrame message. Does not implicitly {@link ReqDelDarkFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqDelDarkFrame
   * @static
   * @param {IReqDelDarkFrame} message ReqDelDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelDarkFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expIndex);
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.gainIndex);
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.binIndex);
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.tempValue);
    return writer;
  };

  /**
   * Encodes the specified ReqDelDarkFrame message, length delimited. Does not implicitly {@link ReqDelDarkFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDelDarkFrame
   * @static
   * @param {IReqDelDarkFrame} message ReqDelDarkFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelDarkFrame.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDelDarkFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDelDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDelDarkFrame} ReqDelDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelDarkFrame.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDelDarkFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expIndex = reader.int32();
          break;
        }
        case 2: {
          message.gainIndex = reader.int32();
          break;
        }
        case 3: {
          message.binIndex = reader.int32();
          break;
        }
        case 4: {
          message.tempValue = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDelDarkFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDelDarkFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDelDarkFrame} ReqDelDarkFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelDarkFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDelDarkFrame message.
   * @function verify
   * @memberof ReqDelDarkFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDelDarkFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      if (!$util.isInteger(message.gainIndex))
        return "gainIndex: integer expected";
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      if (!$util.isInteger(message.binIndex))
        return "binIndex: integer expected";
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    )
      if (!$util.isInteger(message.tempValue))
        return "tempValue: integer expected";
    return null;
  };

  /**
   * Creates a ReqDelDarkFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDelDarkFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDelDarkFrame} ReqDelDarkFrame
   */
  ReqDelDarkFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDelDarkFrame) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqDelDarkFrame: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqDelDarkFrame();
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
    if (object.binIndex != null) message.binIndex = object.binIndex | 0;
    if (object.tempValue != null) message.tempValue = object.tempValue | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqDelDarkFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDelDarkFrame
   * @static
   * @param {ReqDelDarkFrame} message ReqDelDarkFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDelDarkFrame.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expIndex = 0;
      object.gainIndex = 0;
      object.binIndex = 0;
      object.tempValue = 0;
    }
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      object.gainIndex = message.gainIndex;
    if (
      message.binIndex != null &&
      Object.hasOwnProperty.call(message, "binIndex")
    )
      object.binIndex = message.binIndex;
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    )
      object.tempValue = message.tempValue;
    return object;
  };

  /**
   * Converts this ReqDelDarkFrame to JSON.
   * @function toJSON
   * @memberof ReqDelDarkFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDelDarkFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDelDarkFrame
   * @function getTypeUrl
   * @memberof ReqDelDarkFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDelDarkFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDelDarkFrame";
  };

  return ReqDelDarkFrame;
})();

$root.ReqDelDarkFrameList = (function () {
  /**
   * Properties of a ReqDelDarkFrameList.
   * @exports IReqDelDarkFrameList
   * @interface IReqDelDarkFrameList
   * @property {Array.<IReqDelDarkFrame>|null} [darkList] ReqDelDarkFrameList darkList
   */

  /**
   * Constructs a new ReqDelDarkFrameList.
   * @exports ReqDelDarkFrameList
   * @classdesc Represents a ReqDelDarkFrameList.
   * @implements IReqDelDarkFrameList
   * @constructor
   * @param {IReqDelDarkFrameList=} [properties] Properties to set
   */
  function ReqDelDarkFrameList(properties) {
    this.darkList = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqDelDarkFrameList darkList.
   * @member {Array.<IReqDelDarkFrame>} darkList
   * @memberof ReqDelDarkFrameList
   * @instance
   */
  ReqDelDarkFrameList.prototype.darkList = $util.emptyArray;

  /**
   * Creates a new ReqDelDarkFrameList instance using the specified properties.
   * @function create
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {IReqDelDarkFrameList=} [properties] Properties to set
   * @returns {ReqDelDarkFrameList} ReqDelDarkFrameList instance
   */
  ReqDelDarkFrameList.create = function create(properties) {
    return new ReqDelDarkFrameList(properties);
  };

  /**
   * Encodes the specified ReqDelDarkFrameList message. Does not implicitly {@link ReqDelDarkFrameList.verify|verify} messages.
   * @function encode
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {IReqDelDarkFrameList} message ReqDelDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelDarkFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.darkList != null && message.darkList.length)
      for (var i = 0; i < message.darkList.length; ++i)
        $root.ReqDelDarkFrame.encode(
          message.darkList[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqDelDarkFrameList message, length delimited. Does not implicitly {@link ReqDelDarkFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {IReqDelDarkFrameList} message ReqDelDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelDarkFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDelDarkFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDelDarkFrameList} ReqDelDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelDarkFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDelDarkFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.darkList && message.darkList.length))
            message.darkList = [];
          message.darkList.push(
            $root.ReqDelDarkFrame.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDelDarkFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDelDarkFrameList} ReqDelDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelDarkFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDelDarkFrameList message.
   * @function verify
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDelDarkFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.darkList != null &&
      Object.hasOwnProperty.call(message, "darkList")
    ) {
      if (!Array.isArray(message.darkList)) return "darkList: array expected";
      for (var i = 0; i < message.darkList.length; ++i) {
        var error = $root.ReqDelDarkFrame.verify(message.darkList[i], long + 1);
        if (error) return "darkList." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ReqDelDarkFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDelDarkFrameList} ReqDelDarkFrameList
   */
  ReqDelDarkFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDelDarkFrameList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqDelDarkFrameList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqDelDarkFrameList();
    if (object.darkList) {
      if (!Array.isArray(object.darkList))
        throw TypeError(".ReqDelDarkFrameList.darkList: array expected");
      message.darkList = [];
      for (var i = 0; i < object.darkList.length; ++i) {
        if (!$util.isObject(object.darkList[i]))
          throw TypeError(".ReqDelDarkFrameList.darkList: object expected");
        message.darkList[i] = $root.ReqDelDarkFrame.fromObject(
          object.darkList[i],
          long + 1,
        );
      }
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqDelDarkFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {ReqDelDarkFrameList} message ReqDelDarkFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDelDarkFrameList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.darkList = [];
    if (message.darkList && message.darkList.length) {
      object.darkList = [];
      for (var j = 0; j < message.darkList.length; ++j)
        object.darkList[j] = $root.ReqDelDarkFrame.toObject(
          message.darkList[j],
          options,
          q + 1,
        );
    }
    return object;
  };

  /**
   * Converts this ReqDelDarkFrameList to JSON.
   * @function toJSON
   * @memberof ReqDelDarkFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDelDarkFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDelDarkFrameList
   * @function getTypeUrl
   * @memberof ReqDelDarkFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDelDarkFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDelDarkFrameList";
  };

  return ReqDelDarkFrameList;
})();

$root.ResDelDarkFrameList = (function () {
  /**
   * Properties of a ResDelDarkFrameList.
   * @exports IResDelDarkFrameList
   * @interface IResDelDarkFrameList
   * @property {number|null} [code] ResDelDarkFrameList code
   */

  /**
   * Constructs a new ResDelDarkFrameList.
   * @exports ResDelDarkFrameList
   * @classdesc Represents a ResDelDarkFrameList.
   * @implements IResDelDarkFrameList
   * @constructor
   * @param {IResDelDarkFrameList=} [properties] Properties to set
   */
  function ResDelDarkFrameList(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResDelDarkFrameList code.
   * @member {number} code
   * @memberof ResDelDarkFrameList
   * @instance
   */
  ResDelDarkFrameList.prototype.code = 0;

  /**
   * Creates a new ResDelDarkFrameList instance using the specified properties.
   * @function create
   * @memberof ResDelDarkFrameList
   * @static
   * @param {IResDelDarkFrameList=} [properties] Properties to set
   * @returns {ResDelDarkFrameList} ResDelDarkFrameList instance
   */
  ResDelDarkFrameList.create = function create(properties) {
    return new ResDelDarkFrameList(properties);
  };

  /**
   * Encodes the specified ResDelDarkFrameList message. Does not implicitly {@link ResDelDarkFrameList.verify|verify} messages.
   * @function encode
   * @memberof ResDelDarkFrameList
   * @static
   * @param {IResDelDarkFrameList} message ResDelDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResDelDarkFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResDelDarkFrameList message, length delimited. Does not implicitly {@link ResDelDarkFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResDelDarkFrameList
   * @static
   * @param {IResDelDarkFrameList} message ResDelDarkFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResDelDarkFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResDelDarkFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ResDelDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResDelDarkFrameList} ResDelDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResDelDarkFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResDelDarkFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResDelDarkFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResDelDarkFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResDelDarkFrameList} ResDelDarkFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResDelDarkFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResDelDarkFrameList message.
   * @function verify
   * @memberof ResDelDarkFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResDelDarkFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResDelDarkFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResDelDarkFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResDelDarkFrameList} ResDelDarkFrameList
   */
  ResDelDarkFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResDelDarkFrameList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResDelDarkFrameList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResDelDarkFrameList();
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResDelDarkFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResDelDarkFrameList
   * @static
   * @param {ResDelDarkFrameList} message ResDelDarkFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResDelDarkFrameList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.code = 0;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResDelDarkFrameList to JSON.
   * @function toJSON
   * @memberof ResDelDarkFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResDelDarkFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResDelDarkFrameList
   * @function getTypeUrl
   * @memberof ResDelDarkFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResDelDarkFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResDelDarkFrameList";
  };

  return ResDelDarkFrameList;
})();

$root.ReqGoLive = (function () {
  /**
   * Properties of a ReqGoLive.
   * @exports IReqGoLive
   * @interface IReqGoLive
   */

  /**
   * Constructs a new ReqGoLive.
   * @exports ReqGoLive
   * @classdesc Represents a ReqGoLive.
   * @implements IReqGoLive
   * @constructor
   * @param {IReqGoLive=} [properties] Properties to set
   */
  function ReqGoLive(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGoLive instance using the specified properties.
   * @function create
   * @memberof ReqGoLive
   * @static
   * @param {IReqGoLive=} [properties] Properties to set
   * @returns {ReqGoLive} ReqGoLive instance
   */
  ReqGoLive.create = function create(properties) {
    return new ReqGoLive(properties);
  };

  /**
   * Encodes the specified ReqGoLive message. Does not implicitly {@link ReqGoLive.verify|verify} messages.
   * @function encode
   * @memberof ReqGoLive
   * @static
   * @param {IReqGoLive} message ReqGoLive message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGoLive.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGoLive message, length delimited. Does not implicitly {@link ReqGoLive.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGoLive
   * @static
   * @param {IReqGoLive} message ReqGoLive message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGoLive.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGoLive message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGoLive
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGoLive} ReqGoLive
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGoLive.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGoLive();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGoLive message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGoLive
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGoLive} ReqGoLive
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGoLive.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGoLive message.
   * @function verify
   * @memberof ReqGoLive
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGoLive.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGoLive message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGoLive
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGoLive} ReqGoLive
   */
  ReqGoLive.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGoLive) return object;
    return new $root.ReqGoLive();
  };

  /**
   * Creates a plain object from a ReqGoLive message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGoLive
   * @static
   * @param {ReqGoLive} message ReqGoLive
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGoLive.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGoLive to JSON.
   * @function toJSON
   * @memberof ReqGoLive
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGoLive.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGoLive
   * @function getTypeUrl
   * @memberof ReqGoLive
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGoLive.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGoLive";
  };

  return ReqGoLive;
})();

$root.ReqTrackSpecialTarget = (function () {
  /**
   * Properties of a ReqTrackSpecialTarget.
   * @exports IReqTrackSpecialTarget
   * @interface IReqTrackSpecialTarget
   * @property {number|null} [index] ReqTrackSpecialTarget index
   * @property {number|null} [lon] ReqTrackSpecialTarget lon
   * @property {number|null} [lat] ReqTrackSpecialTarget lat
   */

  /**
   * Constructs a new ReqTrackSpecialTarget.
   * @exports ReqTrackSpecialTarget
   * @classdesc Represents a ReqTrackSpecialTarget.
   * @implements IReqTrackSpecialTarget
   * @constructor
   * @param {IReqTrackSpecialTarget=} [properties] Properties to set
   */
  function ReqTrackSpecialTarget(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqTrackSpecialTarget index.
   * @member {number} index
   * @memberof ReqTrackSpecialTarget
   * @instance
   */
  ReqTrackSpecialTarget.prototype.index = 0;

  /**
   * ReqTrackSpecialTarget lon.
   * @member {number} lon
   * @memberof ReqTrackSpecialTarget
   * @instance
   */
  ReqTrackSpecialTarget.prototype.lon = 0;

  /**
   * ReqTrackSpecialTarget lat.
   * @member {number} lat
   * @memberof ReqTrackSpecialTarget
   * @instance
   */
  ReqTrackSpecialTarget.prototype.lat = 0;

  /**
   * Creates a new ReqTrackSpecialTarget instance using the specified properties.
   * @function create
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {IReqTrackSpecialTarget=} [properties] Properties to set
   * @returns {ReqTrackSpecialTarget} ReqTrackSpecialTarget instance
   */
  ReqTrackSpecialTarget.create = function create(properties) {
    return new ReqTrackSpecialTarget(properties);
  };

  /**
   * Encodes the specified ReqTrackSpecialTarget message. Does not implicitly {@link ReqTrackSpecialTarget.verify|verify} messages.
   * @function encode
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {IReqTrackSpecialTarget} message ReqTrackSpecialTarget message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTrackSpecialTarget.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.lat);
    return writer;
  };

  /**
   * Encodes the specified ReqTrackSpecialTarget message, length delimited. Does not implicitly {@link ReqTrackSpecialTarget.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {IReqTrackSpecialTarget} message ReqTrackSpecialTarget message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTrackSpecialTarget.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqTrackSpecialTarget message from the specified reader or buffer.
   * @function decode
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqTrackSpecialTarget} ReqTrackSpecialTarget
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTrackSpecialTarget.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqTrackSpecialTarget();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        case 2: {
          message.lon = reader.double();
          break;
        }
        case 3: {
          message.lat = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqTrackSpecialTarget message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqTrackSpecialTarget} ReqTrackSpecialTarget
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTrackSpecialTarget.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqTrackSpecialTarget message.
   * @function verify
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqTrackSpecialTarget.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    return null;
  };

  /**
   * Creates a ReqTrackSpecialTarget message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqTrackSpecialTarget} ReqTrackSpecialTarget
   */
  ReqTrackSpecialTarget.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqTrackSpecialTarget) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqTrackSpecialTarget: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqTrackSpecialTarget();
    if (object.index != null) message.index = object.index | 0;
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    return message;
  };

  /**
   * Creates a plain object from a ReqTrackSpecialTarget message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {ReqTrackSpecialTarget} message ReqTrackSpecialTarget
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqTrackSpecialTarget.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.index = 0;
      object.lon = 0;
      object.lat = 0;
    }
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    return object;
  };

  /**
   * Converts this ReqTrackSpecialTarget to JSON.
   * @function toJSON
   * @memberof ReqTrackSpecialTarget
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqTrackSpecialTarget.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqTrackSpecialTarget
   * @function getTypeUrl
   * @memberof ReqTrackSpecialTarget
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqTrackSpecialTarget.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqTrackSpecialTarget";
  };

  return ReqTrackSpecialTarget;
})();

$root.ReqStopTrackSpecialTarget = (function () {
  /**
   * Properties of a ReqStopTrackSpecialTarget.
   * @exports IReqStopTrackSpecialTarget
   * @interface IReqStopTrackSpecialTarget
   */

  /**
   * Constructs a new ReqStopTrackSpecialTarget.
   * @exports ReqStopTrackSpecialTarget
   * @classdesc Represents a ReqStopTrackSpecialTarget.
   * @implements IReqStopTrackSpecialTarget
   * @constructor
   * @param {IReqStopTrackSpecialTarget=} [properties] Properties to set
   */
  function ReqStopTrackSpecialTarget(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopTrackSpecialTarget instance using the specified properties.
   * @function create
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {IReqStopTrackSpecialTarget=} [properties] Properties to set
   * @returns {ReqStopTrackSpecialTarget} ReqStopTrackSpecialTarget instance
   */
  ReqStopTrackSpecialTarget.create = function create(properties) {
    return new ReqStopTrackSpecialTarget(properties);
  };

  /**
   * Encodes the specified ReqStopTrackSpecialTarget message. Does not implicitly {@link ReqStopTrackSpecialTarget.verify|verify} messages.
   * @function encode
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {IReqStopTrackSpecialTarget} message ReqStopTrackSpecialTarget message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTrackSpecialTarget.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopTrackSpecialTarget message, length delimited. Does not implicitly {@link ReqStopTrackSpecialTarget.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {IReqStopTrackSpecialTarget} message ReqStopTrackSpecialTarget message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTrackSpecialTarget.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopTrackSpecialTarget message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopTrackSpecialTarget} ReqStopTrackSpecialTarget
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTrackSpecialTarget.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopTrackSpecialTarget();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopTrackSpecialTarget message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopTrackSpecialTarget} ReqStopTrackSpecialTarget
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTrackSpecialTarget.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopTrackSpecialTarget message.
   * @function verify
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopTrackSpecialTarget.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopTrackSpecialTarget message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopTrackSpecialTarget} ReqStopTrackSpecialTarget
   */
  ReqStopTrackSpecialTarget.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopTrackSpecialTarget) return object;
    return new $root.ReqStopTrackSpecialTarget();
  };

  /**
   * Creates a plain object from a ReqStopTrackSpecialTarget message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {ReqStopTrackSpecialTarget} message ReqStopTrackSpecialTarget
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopTrackSpecialTarget.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopTrackSpecialTarget to JSON.
   * @function toJSON
   * @memberof ReqStopTrackSpecialTarget
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopTrackSpecialTarget.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopTrackSpecialTarget
   * @function getTypeUrl
   * @memberof ReqStopTrackSpecialTarget
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopTrackSpecialTarget.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopTrackSpecialTarget";
  };

  return ReqStopTrackSpecialTarget;
})();

$root.ReqOneClickGotoDSO = (function () {
  /**
   * Properties of a ReqOneClickGotoDSO.
   * @exports IReqOneClickGotoDSO
   * @interface IReqOneClickGotoDSO
   * @property {number|null} [ra] ReqOneClickGotoDSO ra
   * @property {number|null} [dec] ReqOneClickGotoDSO dec
   * @property {string|null} [targetName] ReqOneClickGotoDSO targetName
   * @property {number|null} [lon] ReqOneClickGotoDSO lon
   * @property {number|null} [lat] ReqOneClickGotoDSO lat
   * @property {number|null} [shootingMode] ReqOneClickGotoDSO shootingMode
   * @property {boolean|null} [gotoOnly] ReqOneClickGotoDSO gotoOnly
   * @property {number|null} [rotation] ReqOneClickGotoDSO rotation
   */

  /**
   * Constructs a new ReqOneClickGotoDSO.
   * @exports ReqOneClickGotoDSO
   * @classdesc Represents a ReqOneClickGotoDSO.
   * @implements IReqOneClickGotoDSO
   * @constructor
   * @param {IReqOneClickGotoDSO=} [properties] Properties to set
   */
  function ReqOneClickGotoDSO(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqOneClickGotoDSO ra.
   * @member {number} ra
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.ra = 0;

  /**
   * ReqOneClickGotoDSO dec.
   * @member {number} dec
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.dec = 0;

  /**
   * ReqOneClickGotoDSO targetName.
   * @member {string} targetName
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.targetName = "";

  /**
   * ReqOneClickGotoDSO lon.
   * @member {number} lon
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.lon = 0;

  /**
   * ReqOneClickGotoDSO lat.
   * @member {number} lat
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.lat = 0;

  /**
   * ReqOneClickGotoDSO shootingMode.
   * @member {number} shootingMode
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.shootingMode = 0;

  /**
   * ReqOneClickGotoDSO gotoOnly.
   * @member {boolean} gotoOnly
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.gotoOnly = false;

  /**
   * ReqOneClickGotoDSO rotation.
   * @member {number|null|undefined} rotation
   * @memberof ReqOneClickGotoDSO
   * @instance
   */
  ReqOneClickGotoDSO.prototype.rotation = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqOneClickGotoDSO.prototype, "_rotation", {
    get: $util.oneOfGetter(($oneOfFields = ["rotation"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqOneClickGotoDSO instance using the specified properties.
   * @function create
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {IReqOneClickGotoDSO=} [properties] Properties to set
   * @returns {ReqOneClickGotoDSO} ReqOneClickGotoDSO instance
   */
  ReqOneClickGotoDSO.create = function create(properties) {
    return new ReqOneClickGotoDSO(properties);
  };

  /**
   * Encodes the specified ReqOneClickGotoDSO message. Does not implicitly {@link ReqOneClickGotoDSO.verify|verify} messages.
   * @function encode
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {IReqOneClickGotoDSO} message ReqOneClickGotoDSO message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickGotoDSO.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.ra);
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.dec);
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.targetName);
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 5, wireType 1 =*/ 41).double(message.lat);
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.shootingMode);
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).bool(message.gotoOnly);
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.rotation);
    return writer;
  };

  /**
   * Encodes the specified ReqOneClickGotoDSO message, length delimited. Does not implicitly {@link ReqOneClickGotoDSO.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {IReqOneClickGotoDSO} message ReqOneClickGotoDSO message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickGotoDSO.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOneClickGotoDSO message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOneClickGotoDSO} ReqOneClickGotoDSO
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickGotoDSO.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOneClickGotoDSO();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.ra = reader.double();
          break;
        }
        case 2: {
          message.dec = reader.double();
          break;
        }
        case 3: {
          message.targetName = reader.string();
          break;
        }
        case 4: {
          message.lon = reader.double();
          break;
        }
        case 5: {
          message.lat = reader.double();
          break;
        }
        case 6: {
          message.shootingMode = reader.int32();
          break;
        }
        case 7: {
          message.gotoOnly = reader.bool();
          break;
        }
        case 8: {
          message.rotation = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOneClickGotoDSO message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOneClickGotoDSO} ReqOneClickGotoDSO
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickGotoDSO.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOneClickGotoDSO message.
   * @function verify
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOneClickGotoDSO.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      if (typeof message.ra !== "number") return "ra: number expected";
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      if (typeof message.dec !== "number") return "dec: number expected";
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      if (!$util.isString(message.targetName))
        return "targetName: string expected";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      if (typeof message.gotoOnly !== "boolean")
        return "gotoOnly: boolean expected";
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    ) {
      properties._rotation = 1;
      if (!$util.isInteger(message.rotation))
        return "rotation: integer expected";
    }
    return null;
  };

  /**
   * Creates a ReqOneClickGotoDSO message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOneClickGotoDSO} ReqOneClickGotoDSO
   */
  ReqOneClickGotoDSO.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOneClickGotoDSO) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqOneClickGotoDSO: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqOneClickGotoDSO();
    if (object.ra != null) message.ra = Number(object.ra);
    if (object.dec != null) message.dec = Number(object.dec);
    if (object.targetName != null)
      message.targetName = String(object.targetName);
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    if (object.gotoOnly != null) message.gotoOnly = Boolean(object.gotoOnly);
    if (object.rotation != null) message.rotation = object.rotation | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqOneClickGotoDSO message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {ReqOneClickGotoDSO} message ReqOneClickGotoDSO
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOneClickGotoDSO.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.ra = 0;
      object.dec = 0;
      object.targetName = "";
      object.lon = 0;
      object.lat = 0;
      object.shootingMode = 0;
      object.gotoOnly = false;
    }
    if (message.ra != null && Object.hasOwnProperty.call(message, "ra"))
      object.ra =
        options.json && !isFinite(message.ra) ? String(message.ra) : message.ra;
    if (message.dec != null && Object.hasOwnProperty.call(message, "dec"))
      object.dec =
        options.json && !isFinite(message.dec)
          ? String(message.dec)
          : message.dec;
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      object.targetName = message.targetName;
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    if (
      message.gotoOnly != null &&
      Object.hasOwnProperty.call(message, "gotoOnly")
    )
      object.gotoOnly = message.gotoOnly;
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    ) {
      object.rotation = message.rotation;
      if (options.oneofs) object._rotation = "rotation";
    }
    return object;
  };

  /**
   * Converts this ReqOneClickGotoDSO to JSON.
   * @function toJSON
   * @memberof ReqOneClickGotoDSO
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOneClickGotoDSO.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOneClickGotoDSO
   * @function getTypeUrl
   * @memberof ReqOneClickGotoDSO
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOneClickGotoDSO.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOneClickGotoDSO";
  };

  return ReqOneClickGotoDSO;
})();

$root.ResOneClickGoto = (function () {
  /**
   * Properties of a ResOneClickGoto.
   * @exports IResOneClickGoto
   * @interface IResOneClickGoto
   * @property {number|null} [step] ResOneClickGoto step
   * @property {number|null} [code] ResOneClickGoto code
   * @property {boolean|null} [allEnd] ResOneClickGoto allEnd
   */

  /**
   * Constructs a new ResOneClickGoto.
   * @exports ResOneClickGoto
   * @classdesc Represents a ResOneClickGoto.
   * @implements IResOneClickGoto
   * @constructor
   * @param {IResOneClickGoto=} [properties] Properties to set
   */
  function ResOneClickGoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResOneClickGoto step.
   * @member {number} step
   * @memberof ResOneClickGoto
   * @instance
   */
  ResOneClickGoto.prototype.step = 0;

  /**
   * ResOneClickGoto code.
   * @member {number} code
   * @memberof ResOneClickGoto
   * @instance
   */
  ResOneClickGoto.prototype.code = 0;

  /**
   * ResOneClickGoto allEnd.
   * @member {boolean} allEnd
   * @memberof ResOneClickGoto
   * @instance
   */
  ResOneClickGoto.prototype.allEnd = false;

  /**
   * Creates a new ResOneClickGoto instance using the specified properties.
   * @function create
   * @memberof ResOneClickGoto
   * @static
   * @param {IResOneClickGoto=} [properties] Properties to set
   * @returns {ResOneClickGoto} ResOneClickGoto instance
   */
  ResOneClickGoto.create = function create(properties) {
    return new ResOneClickGoto(properties);
  };

  /**
   * Encodes the specified ResOneClickGoto message. Does not implicitly {@link ResOneClickGoto.verify|verify} messages.
   * @function encode
   * @memberof ResOneClickGoto
   * @static
   * @param {IResOneClickGoto} message ResOneClickGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResOneClickGoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.step);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.allEnd);
    return writer;
  };

  /**
   * Encodes the specified ResOneClickGoto message, length delimited. Does not implicitly {@link ResOneClickGoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResOneClickGoto
   * @static
   * @param {IResOneClickGoto} message ResOneClickGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResOneClickGoto.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResOneClickGoto message from the specified reader or buffer.
   * @function decode
   * @memberof ResOneClickGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResOneClickGoto} ResOneClickGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResOneClickGoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResOneClickGoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.step = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.allEnd = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResOneClickGoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResOneClickGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResOneClickGoto} ResOneClickGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResOneClickGoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResOneClickGoto message.
   * @function verify
   * @memberof ResOneClickGoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResOneClickGoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      if (!$util.isInteger(message.step)) return "step: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      if (typeof message.allEnd !== "boolean")
        return "allEnd: boolean expected";
    return null;
  };

  /**
   * Creates a ResOneClickGoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResOneClickGoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResOneClickGoto} ResOneClickGoto
   */
  ResOneClickGoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResOneClickGoto) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResOneClickGoto: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResOneClickGoto();
    if (object.step != null) message.step = object.step | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.allEnd != null) message.allEnd = Boolean(object.allEnd);
    return message;
  };

  /**
   * Creates a plain object from a ResOneClickGoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResOneClickGoto
   * @static
   * @param {ResOneClickGoto} message ResOneClickGoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResOneClickGoto.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.step = 0;
      object.code = 0;
      object.allEnd = false;
    }
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      object.step = message.step;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      object.allEnd = message.allEnd;
    return object;
  };

  /**
   * Converts this ResOneClickGoto to JSON.
   * @function toJSON
   * @memberof ResOneClickGoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResOneClickGoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResOneClickGoto
   * @function getTypeUrl
   * @memberof ResOneClickGoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResOneClickGoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResOneClickGoto";
  };

  return ResOneClickGoto;
})();

$root.ReqOneClickGotoSolarSystem = (function () {
  /**
   * Properties of a ReqOneClickGotoSolarSystem.
   * @exports IReqOneClickGotoSolarSystem
   * @interface IReqOneClickGotoSolarSystem
   * @property {number|null} [index] ReqOneClickGotoSolarSystem index
   * @property {number|null} [lon] ReqOneClickGotoSolarSystem lon
   * @property {number|null} [lat] ReqOneClickGotoSolarSystem lat
   * @property {string|null} [targetName] ReqOneClickGotoSolarSystem targetName
   * @property {number|null} [shootingMode] ReqOneClickGotoSolarSystem shootingMode
   * @property {boolean|null} [forceStart] ReqOneClickGotoSolarSystem forceStart
   */

  /**
   * Constructs a new ReqOneClickGotoSolarSystem.
   * @exports ReqOneClickGotoSolarSystem
   * @classdesc Represents a ReqOneClickGotoSolarSystem.
   * @implements IReqOneClickGotoSolarSystem
   * @constructor
   * @param {IReqOneClickGotoSolarSystem=} [properties] Properties to set
   */
  function ReqOneClickGotoSolarSystem(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqOneClickGotoSolarSystem index.
   * @member {number} index
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.index = 0;

  /**
   * ReqOneClickGotoSolarSystem lon.
   * @member {number} lon
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.lon = 0;

  /**
   * ReqOneClickGotoSolarSystem lat.
   * @member {number} lat
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.lat = 0;

  /**
   * ReqOneClickGotoSolarSystem targetName.
   * @member {string} targetName
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.targetName = "";

  /**
   * ReqOneClickGotoSolarSystem shootingMode.
   * @member {number} shootingMode
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.shootingMode = 0;

  /**
   * ReqOneClickGotoSolarSystem forceStart.
   * @member {boolean} forceStart
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   */
  ReqOneClickGotoSolarSystem.prototype.forceStart = false;

  /**
   * Creates a new ReqOneClickGotoSolarSystem instance using the specified properties.
   * @function create
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {IReqOneClickGotoSolarSystem=} [properties] Properties to set
   * @returns {ReqOneClickGotoSolarSystem} ReqOneClickGotoSolarSystem instance
   */
  ReqOneClickGotoSolarSystem.create = function create(properties) {
    return new ReqOneClickGotoSolarSystem(properties);
  };

  /**
   * Encodes the specified ReqOneClickGotoSolarSystem message. Does not implicitly {@link ReqOneClickGotoSolarSystem.verify|verify} messages.
   * @function encode
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {IReqOneClickGotoSolarSystem} message ReqOneClickGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickGotoSolarSystem.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.lat);
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.targetName);
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.shootingMode);
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).bool(message.forceStart);
    return writer;
  };

  /**
   * Encodes the specified ReqOneClickGotoSolarSystem message, length delimited. Does not implicitly {@link ReqOneClickGotoSolarSystem.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {IReqOneClickGotoSolarSystem} message ReqOneClickGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickGotoSolarSystem.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOneClickGotoSolarSystem message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOneClickGotoSolarSystem} ReqOneClickGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickGotoSolarSystem.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOneClickGotoSolarSystem();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        case 2: {
          message.lon = reader.double();
          break;
        }
        case 3: {
          message.lat = reader.double();
          break;
        }
        case 4: {
          message.targetName = reader.string();
          break;
        }
        case 5: {
          message.shootingMode = reader.int32();
          break;
        }
        case 6: {
          message.forceStart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOneClickGotoSolarSystem message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOneClickGotoSolarSystem} ReqOneClickGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickGotoSolarSystem.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOneClickGotoSolarSystem message.
   * @function verify
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOneClickGotoSolarSystem.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      if (!$util.isString(message.targetName))
        return "targetName: string expected";
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      if (typeof message.forceStart !== "boolean")
        return "forceStart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqOneClickGotoSolarSystem message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOneClickGotoSolarSystem} ReqOneClickGotoSolarSystem
   */
  ReqOneClickGotoSolarSystem.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOneClickGotoSolarSystem) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqOneClickGotoSolarSystem: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqOneClickGotoSolarSystem();
    if (object.index != null) message.index = object.index | 0;
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    if (object.targetName != null)
      message.targetName = String(object.targetName);
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    if (object.forceStart != null)
      message.forceStart = Boolean(object.forceStart);
    return message;
  };

  /**
   * Creates a plain object from a ReqOneClickGotoSolarSystem message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {ReqOneClickGotoSolarSystem} message ReqOneClickGotoSolarSystem
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOneClickGotoSolarSystem.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.index = 0;
      object.lon = 0;
      object.lat = 0;
      object.targetName = "";
      object.shootingMode = 0;
      object.forceStart = false;
    }
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    if (
      message.targetName != null &&
      Object.hasOwnProperty.call(message, "targetName")
    )
      object.targetName = message.targetName;
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      object.forceStart = message.forceStart;
    return object;
  };

  /**
   * Converts this ReqOneClickGotoSolarSystem to JSON.
   * @function toJSON
   * @memberof ReqOneClickGotoSolarSystem
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOneClickGotoSolarSystem.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOneClickGotoSolarSystem
   * @function getTypeUrl
   * @memberof ReqOneClickGotoSolarSystem
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOneClickGotoSolarSystem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOneClickGotoSolarSystem";
  };

  return ReqOneClickGotoSolarSystem;
})();

$root.ResOneClickGotoSolarSystem = (function () {
  /**
   * Properties of a ResOneClickGotoSolarSystem.
   * @exports IResOneClickGotoSolarSystem
   * @interface IResOneClickGotoSolarSystem
   * @property {number|null} [step] ResOneClickGotoSolarSystem step
   * @property {number|null} [code] ResOneClickGotoSolarSystem code
   * @property {boolean|null} [allEnd] ResOneClickGotoSolarSystem allEnd
   * @property {IReqOneClickGotoSolarSystem|null} [req] ResOneClickGotoSolarSystem req
   */

  /**
   * Constructs a new ResOneClickGotoSolarSystem.
   * @exports ResOneClickGotoSolarSystem
   * @classdesc Represents a ResOneClickGotoSolarSystem.
   * @implements IResOneClickGotoSolarSystem
   * @constructor
   * @param {IResOneClickGotoSolarSystem=} [properties] Properties to set
   */
  function ResOneClickGotoSolarSystem(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResOneClickGotoSolarSystem step.
   * @member {number} step
   * @memberof ResOneClickGotoSolarSystem
   * @instance
   */
  ResOneClickGotoSolarSystem.prototype.step = 0;

  /**
   * ResOneClickGotoSolarSystem code.
   * @member {number} code
   * @memberof ResOneClickGotoSolarSystem
   * @instance
   */
  ResOneClickGotoSolarSystem.prototype.code = 0;

  /**
   * ResOneClickGotoSolarSystem allEnd.
   * @member {boolean} allEnd
   * @memberof ResOneClickGotoSolarSystem
   * @instance
   */
  ResOneClickGotoSolarSystem.prototype.allEnd = false;

  /**
   * ResOneClickGotoSolarSystem req.
   * @member {IReqOneClickGotoSolarSystem|null|undefined} req
   * @memberof ResOneClickGotoSolarSystem
   * @instance
   */
  ResOneClickGotoSolarSystem.prototype.req = null;

  /**
   * Creates a new ResOneClickGotoSolarSystem instance using the specified properties.
   * @function create
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {IResOneClickGotoSolarSystem=} [properties] Properties to set
   * @returns {ResOneClickGotoSolarSystem} ResOneClickGotoSolarSystem instance
   */
  ResOneClickGotoSolarSystem.create = function create(properties) {
    return new ResOneClickGotoSolarSystem(properties);
  };

  /**
   * Encodes the specified ResOneClickGotoSolarSystem message. Does not implicitly {@link ResOneClickGotoSolarSystem.verify|verify} messages.
   * @function encode
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {IResOneClickGotoSolarSystem} message ResOneClickGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResOneClickGotoSolarSystem.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.step);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.allEnd);
    if (message.req != null && Object.hasOwnProperty.call(message, "req"))
      $root.ReqOneClickGotoSolarSystem.encode(
        message.req,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResOneClickGotoSolarSystem message, length delimited. Does not implicitly {@link ResOneClickGotoSolarSystem.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {IResOneClickGotoSolarSystem} message ResOneClickGotoSolarSystem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResOneClickGotoSolarSystem.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResOneClickGotoSolarSystem message from the specified reader or buffer.
   * @function decode
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResOneClickGotoSolarSystem} ResOneClickGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResOneClickGotoSolarSystem.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResOneClickGotoSolarSystem();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.step = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.allEnd = reader.bool();
          break;
        }
        case 4: {
          message.req = $root.ReqOneClickGotoSolarSystem.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResOneClickGotoSolarSystem message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResOneClickGotoSolarSystem} ResOneClickGotoSolarSystem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResOneClickGotoSolarSystem.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResOneClickGotoSolarSystem message.
   * @function verify
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResOneClickGotoSolarSystem.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      if (!$util.isInteger(message.step)) return "step: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      if (typeof message.allEnd !== "boolean")
        return "allEnd: boolean expected";
    if (message.req != null && Object.hasOwnProperty.call(message, "req")) {
      var error = $root.ReqOneClickGotoSolarSystem.verify(
        message.req,
        long + 1,
      );
      if (error) return "req." + error;
    }
    return null;
  };

  /**
   * Creates a ResOneClickGotoSolarSystem message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResOneClickGotoSolarSystem} ResOneClickGotoSolarSystem
   */
  ResOneClickGotoSolarSystem.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResOneClickGotoSolarSystem) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResOneClickGotoSolarSystem: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResOneClickGotoSolarSystem();
    if (object.step != null) message.step = object.step | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.allEnd != null) message.allEnd = Boolean(object.allEnd);
    if (object.req != null) {
      if (!$util.isObject(object.req))
        throw TypeError(".ResOneClickGotoSolarSystem.req: object expected");
      message.req = $root.ReqOneClickGotoSolarSystem.fromObject(
        object.req,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ResOneClickGotoSolarSystem message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {ResOneClickGotoSolarSystem} message ResOneClickGotoSolarSystem
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResOneClickGotoSolarSystem.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.step = 0;
      object.code = 0;
      object.allEnd = false;
      object.req = null;
    }
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      object.step = message.step;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.allEnd != null && Object.hasOwnProperty.call(message, "allEnd"))
      object.allEnd = message.allEnd;
    if (message.req != null && Object.hasOwnProperty.call(message, "req"))
      object.req = $root.ReqOneClickGotoSolarSystem.toObject(
        message.req,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ResOneClickGotoSolarSystem to JSON.
   * @function toJSON
   * @memberof ResOneClickGotoSolarSystem
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResOneClickGotoSolarSystem.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResOneClickGotoSolarSystem
   * @function getTypeUrl
   * @memberof ResOneClickGotoSolarSystem
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResOneClickGotoSolarSystem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResOneClickGotoSolarSystem";
  };

  return ResOneClickGotoSolarSystem;
})();

$root.ReqStopOneClickGoto = (function () {
  /**
   * Properties of a ReqStopOneClickGoto.
   * @exports IReqStopOneClickGoto
   * @interface IReqStopOneClickGoto
   */

  /**
   * Constructs a new ReqStopOneClickGoto.
   * @exports ReqStopOneClickGoto
   * @classdesc Represents a ReqStopOneClickGoto.
   * @implements IReqStopOneClickGoto
   * @constructor
   * @param {IReqStopOneClickGoto=} [properties] Properties to set
   */
  function ReqStopOneClickGoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopOneClickGoto instance using the specified properties.
   * @function create
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {IReqStopOneClickGoto=} [properties] Properties to set
   * @returns {ReqStopOneClickGoto} ReqStopOneClickGoto instance
   */
  ReqStopOneClickGoto.create = function create(properties) {
    return new ReqStopOneClickGoto(properties);
  };

  /**
   * Encodes the specified ReqStopOneClickGoto message. Does not implicitly {@link ReqStopOneClickGoto.verify|verify} messages.
   * @function encode
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {IReqStopOneClickGoto} message ReqStopOneClickGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopOneClickGoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopOneClickGoto message, length delimited. Does not implicitly {@link ReqStopOneClickGoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {IReqStopOneClickGoto} message ReqStopOneClickGoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopOneClickGoto.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopOneClickGoto message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopOneClickGoto} ReqStopOneClickGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopOneClickGoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopOneClickGoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopOneClickGoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopOneClickGoto} ReqStopOneClickGoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopOneClickGoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopOneClickGoto message.
   * @function verify
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopOneClickGoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopOneClickGoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopOneClickGoto} ReqStopOneClickGoto
   */
  ReqStopOneClickGoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopOneClickGoto) return object;
    return new $root.ReqStopOneClickGoto();
  };

  /**
   * Creates a plain object from a ReqStopOneClickGoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {ReqStopOneClickGoto} message ReqStopOneClickGoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopOneClickGoto.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopOneClickGoto to JSON.
   * @function toJSON
   * @memberof ReqStopOneClickGoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopOneClickGoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopOneClickGoto
   * @function getTypeUrl
   * @memberof ReqStopOneClickGoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopOneClickGoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopOneClickGoto";
  };

  return ReqStopOneClickGoto;
})();

$root.ReqCaptureWideRawLiveStacking = (function () {
  /**
   * Properties of a ReqCaptureWideRawLiveStacking.
   * @exports IReqCaptureWideRawLiveStacking
   * @interface IReqCaptureWideRawLiveStacking
   * @property {boolean|null} [forceStart] ReqCaptureWideRawLiveStacking forceStart
   */

  /**
   * Constructs a new ReqCaptureWideRawLiveStacking.
   * @exports ReqCaptureWideRawLiveStacking
   * @classdesc Represents a ReqCaptureWideRawLiveStacking.
   * @implements IReqCaptureWideRawLiveStacking
   * @constructor
   * @param {IReqCaptureWideRawLiveStacking=} [properties] Properties to set
   */
  function ReqCaptureWideRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCaptureWideRawLiveStacking forceStart.
   * @member {boolean} forceStart
   * @memberof ReqCaptureWideRawLiveStacking
   * @instance
   */
  ReqCaptureWideRawLiveStacking.prototype.forceStart = false;

  /**
   * Creates a new ReqCaptureWideRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {IReqCaptureWideRawLiveStacking=} [properties] Properties to set
   * @returns {ReqCaptureWideRawLiveStacking} ReqCaptureWideRawLiveStacking instance
   */
  ReqCaptureWideRawLiveStacking.create = function create(properties) {
    return new ReqCaptureWideRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqCaptureWideRawLiveStacking message. Does not implicitly {@link ReqCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {IReqCaptureWideRawLiveStacking} message ReqCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideRawLiveStacking.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.forceStart);
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureWideRawLiveStacking message, length delimited. Does not implicitly {@link ReqCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {IReqCaptureWideRawLiveStacking} message ReqCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideRawLiveStacking.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureWideRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureWideRawLiveStacking} ReqCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureWideRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.forceStart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureWideRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureWideRawLiveStacking} ReqCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideRawLiveStacking.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureWideRawLiveStacking message.
   * @function verify
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureWideRawLiveStacking.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      if (typeof message.forceStart !== "boolean")
        return "forceStart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqCaptureWideRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureWideRawLiveStacking} ReqCaptureWideRawLiveStacking
   */
  ReqCaptureWideRawLiveStacking.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureWideRawLiveStacking) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCaptureWideRawLiveStacking: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCaptureWideRawLiveStacking();
    if (object.forceStart != null)
      message.forceStart = Boolean(object.forceStart);
    return message;
  };

  /**
   * Creates a plain object from a ReqCaptureWideRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {ReqCaptureWideRawLiveStacking} message ReqCaptureWideRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureWideRawLiveStacking.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.forceStart = false;
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      object.forceStart = message.forceStart;
    return object;
  };

  /**
   * Converts this ReqCaptureWideRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqCaptureWideRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureWideRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureWideRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqCaptureWideRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureWideRawLiveStacking.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureWideRawLiveStacking";
  };

  return ReqCaptureWideRawLiveStacking;
})();

$root.ReqStopCaptureWideRawLiveStacking = (function () {
  /**
   * Properties of a ReqStopCaptureWideRawLiveStacking.
   * @exports IReqStopCaptureWideRawLiveStacking
   * @interface IReqStopCaptureWideRawLiveStacking
   */

  /**
   * Constructs a new ReqStopCaptureWideRawLiveStacking.
   * @exports ReqStopCaptureWideRawLiveStacking
   * @classdesc Represents a ReqStopCaptureWideRawLiveStacking.
   * @implements IReqStopCaptureWideRawLiveStacking
   * @constructor
   * @param {IReqStopCaptureWideRawLiveStacking=} [properties] Properties to set
   */
  function ReqStopCaptureWideRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCaptureWideRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqStopCaptureWideRawLiveStacking=} [properties] Properties to set
   * @returns {ReqStopCaptureWideRawLiveStacking} ReqStopCaptureWideRawLiveStacking instance
   */
  ReqStopCaptureWideRawLiveStacking.create = function create(properties) {
    return new ReqStopCaptureWideRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqStopCaptureWideRawLiveStacking message. Does not implicitly {@link ReqStopCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqStopCaptureWideRawLiveStacking} message ReqStopCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureWideRawLiveStacking.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCaptureWideRawLiveStacking message, length delimited. Does not implicitly {@link ReqStopCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqStopCaptureWideRawLiveStacking} message ReqStopCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureWideRawLiveStacking.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCaptureWideRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCaptureWideRawLiveStacking} ReqStopCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureWideRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCaptureWideRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCaptureWideRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCaptureWideRawLiveStacking} ReqStopCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureWideRawLiveStacking.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCaptureWideRawLiveStacking message.
   * @function verify
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCaptureWideRawLiveStacking.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCaptureWideRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCaptureWideRawLiveStacking} ReqStopCaptureWideRawLiveStacking
   */
  ReqStopCaptureWideRawLiveStacking.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqStopCaptureWideRawLiveStacking)
      return object;
    return new $root.ReqStopCaptureWideRawLiveStacking();
  };

  /**
   * Creates a plain object from a ReqStopCaptureWideRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {ReqStopCaptureWideRawLiveStacking} message ReqStopCaptureWideRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCaptureWideRawLiveStacking.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCaptureWideRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCaptureWideRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCaptureWideRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqStopCaptureWideRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCaptureWideRawLiveStacking.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCaptureWideRawLiveStacking";
  };

  return ReqStopCaptureWideRawLiveStacking;
})();

$root.ReqFastStopCaptureWideRawLiveStacking = (function () {
  /**
   * Properties of a ReqFastStopCaptureWideRawLiveStacking.
   * @exports IReqFastStopCaptureWideRawLiveStacking
   * @interface IReqFastStopCaptureWideRawLiveStacking
   */

  /**
   * Constructs a new ReqFastStopCaptureWideRawLiveStacking.
   * @exports ReqFastStopCaptureWideRawLiveStacking
   * @classdesc Represents a ReqFastStopCaptureWideRawLiveStacking.
   * @implements IReqFastStopCaptureWideRawLiveStacking
   * @constructor
   * @param {IReqFastStopCaptureWideRawLiveStacking=} [properties] Properties to set
   */
  function ReqFastStopCaptureWideRawLiveStacking(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqFastStopCaptureWideRawLiveStacking instance using the specified properties.
   * @function create
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureWideRawLiveStacking=} [properties] Properties to set
   * @returns {ReqFastStopCaptureWideRawLiveStacking} ReqFastStopCaptureWideRawLiveStacking instance
   */
  ReqFastStopCaptureWideRawLiveStacking.create = function create(properties) {
    return new ReqFastStopCaptureWideRawLiveStacking(properties);
  };

  /**
   * Encodes the specified ReqFastStopCaptureWideRawLiveStacking message. Does not implicitly {@link ReqFastStopCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encode
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureWideRawLiveStacking} message ReqFastStopCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFastStopCaptureWideRawLiveStacking.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqFastStopCaptureWideRawLiveStacking message, length delimited. Does not implicitly {@link ReqFastStopCaptureWideRawLiveStacking.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {IReqFastStopCaptureWideRawLiveStacking} message ReqFastStopCaptureWideRawLiveStacking message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFastStopCaptureWideRawLiveStacking.encodeDelimited =
    function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

  /**
   * Decodes a ReqFastStopCaptureWideRawLiveStacking message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFastStopCaptureWideRawLiveStacking} ReqFastStopCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFastStopCaptureWideRawLiveStacking.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFastStopCaptureWideRawLiveStacking();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFastStopCaptureWideRawLiveStacking message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFastStopCaptureWideRawLiveStacking} ReqFastStopCaptureWideRawLiveStacking
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFastStopCaptureWideRawLiveStacking.decodeDelimited =
    function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

  /**
   * Verifies a ReqFastStopCaptureWideRawLiveStacking message.
   * @function verify
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFastStopCaptureWideRawLiveStacking.verify = function verify(
    message,
    long,
  ) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqFastStopCaptureWideRawLiveStacking message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFastStopCaptureWideRawLiveStacking} ReqFastStopCaptureWideRawLiveStacking
   */
  ReqFastStopCaptureWideRawLiveStacking.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqFastStopCaptureWideRawLiveStacking)
      return object;
    return new $root.ReqFastStopCaptureWideRawLiveStacking();
  };

  /**
   * Creates a plain object from a ReqFastStopCaptureWideRawLiveStacking message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {ReqFastStopCaptureWideRawLiveStacking} message ReqFastStopCaptureWideRawLiveStacking
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFastStopCaptureWideRawLiveStacking.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqFastStopCaptureWideRawLiveStacking to JSON.
   * @function toJSON
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFastStopCaptureWideRawLiveStacking.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFastStopCaptureWideRawLiveStacking
   * @function getTypeUrl
   * @memberof ReqFastStopCaptureWideRawLiveStacking
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFastStopCaptureWideRawLiveStacking.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFastStopCaptureWideRawLiveStacking";
  };

  return ReqFastStopCaptureWideRawLiveStacking;
})();

$root.ReqStartEqSolving = (function () {
  /**
   * Properties of a ReqStartEqSolving.
   * @exports IReqStartEqSolving
   * @interface IReqStartEqSolving
   * @property {number|null} [lon] ReqStartEqSolving lon
   * @property {number|null} [lat] ReqStartEqSolving lat
   * @property {boolean|null} [useCalibration] ReqStartEqSolving useCalibration
   */

  /**
   * Constructs a new ReqStartEqSolving.
   * @exports ReqStartEqSolving
   * @classdesc Represents a ReqStartEqSolving.
   * @implements IReqStartEqSolving
   * @constructor
   * @param {IReqStartEqSolving=} [properties] Properties to set
   */
  function ReqStartEqSolving(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartEqSolving lon.
   * @member {number} lon
   * @memberof ReqStartEqSolving
   * @instance
   */
  ReqStartEqSolving.prototype.lon = 0;

  /**
   * ReqStartEqSolving lat.
   * @member {number} lat
   * @memberof ReqStartEqSolving
   * @instance
   */
  ReqStartEqSolving.prototype.lat = 0;

  /**
   * ReqStartEqSolving useCalibration.
   * @member {boolean|null|undefined} useCalibration
   * @memberof ReqStartEqSolving
   * @instance
   */
  ReqStartEqSolving.prototype.useCalibration = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqStartEqSolving.prototype, "_useCalibration", {
    get: $util.oneOfGetter(($oneOfFields = ["useCalibration"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqStartEqSolving instance using the specified properties.
   * @function create
   * @memberof ReqStartEqSolving
   * @static
   * @param {IReqStartEqSolving=} [properties] Properties to set
   * @returns {ReqStartEqSolving} ReqStartEqSolving instance
   */
  ReqStartEqSolving.create = function create(properties) {
    return new ReqStartEqSolving(properties);
  };

  /**
   * Encodes the specified ReqStartEqSolving message. Does not implicitly {@link ReqStartEqSolving.verify|verify} messages.
   * @function encode
   * @memberof ReqStartEqSolving
   * @static
   * @param {IReqStartEqSolving} message ReqStartEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartEqSolving.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lat);
    if (
      message.useCalibration != null &&
      Object.hasOwnProperty.call(message, "useCalibration")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.useCalibration);
    return writer;
  };

  /**
   * Encodes the specified ReqStartEqSolving message, length delimited. Does not implicitly {@link ReqStartEqSolving.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartEqSolving
   * @static
   * @param {IReqStartEqSolving} message ReqStartEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartEqSolving.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartEqSolving message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartEqSolving} ReqStartEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartEqSolving.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartEqSolving();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.lon = reader.double();
          break;
        }
        case 2: {
          message.lat = reader.double();
          break;
        }
        case 3: {
          message.useCalibration = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartEqSolving message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartEqSolving} ReqStartEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartEqSolving.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartEqSolving message.
   * @function verify
   * @memberof ReqStartEqSolving
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartEqSolving.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    if (
      message.useCalibration != null &&
      Object.hasOwnProperty.call(message, "useCalibration")
    ) {
      properties._useCalibration = 1;
      if (typeof message.useCalibration !== "boolean")
        return "useCalibration: boolean expected";
    }
    return null;
  };

  /**
   * Creates a ReqStartEqSolving message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartEqSolving
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartEqSolving} ReqStartEqSolving
   */
  ReqStartEqSolving.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartEqSolving) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartEqSolving: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartEqSolving();
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    if (object.useCalibration != null)
      message.useCalibration = Boolean(object.useCalibration);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartEqSolving message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartEqSolving
   * @static
   * @param {ReqStartEqSolving} message ReqStartEqSolving
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartEqSolving.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.lon = 0;
      object.lat = 0;
    }
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    if (
      message.useCalibration != null &&
      Object.hasOwnProperty.call(message, "useCalibration")
    ) {
      object.useCalibration = message.useCalibration;
      if (options.oneofs) object._useCalibration = "useCalibration";
    }
    return object;
  };

  /**
   * Converts this ReqStartEqSolving to JSON.
   * @function toJSON
   * @memberof ReqStartEqSolving
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartEqSolving.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartEqSolving
   * @function getTypeUrl
   * @memberof ReqStartEqSolving
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartEqSolving.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartEqSolving";
  };

  return ReqStartEqSolving;
})();

$root.ResStartEqSolving = (function () {
  /**
   * Properties of a ResStartEqSolving.
   * @exports IResStartEqSolving
   * @interface IResStartEqSolving
   * @property {number|null} [aziErr] ResStartEqSolving aziErr
   * @property {number|null} [altErr] ResStartEqSolving altErr
   * @property {number|null} [code] ResStartEqSolving code
   */

  /**
   * Constructs a new ResStartEqSolving.
   * @exports ResStartEqSolving
   * @classdesc Represents a ResStartEqSolving.
   * @implements IResStartEqSolving
   * @constructor
   * @param {IResStartEqSolving=} [properties] Properties to set
   */
  function ResStartEqSolving(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResStartEqSolving aziErr.
   * @member {number} aziErr
   * @memberof ResStartEqSolving
   * @instance
   */
  ResStartEqSolving.prototype.aziErr = 0;

  /**
   * ResStartEqSolving altErr.
   * @member {number} altErr
   * @memberof ResStartEqSolving
   * @instance
   */
  ResStartEqSolving.prototype.altErr = 0;

  /**
   * ResStartEqSolving code.
   * @member {number} code
   * @memberof ResStartEqSolving
   * @instance
   */
  ResStartEqSolving.prototype.code = 0;

  /**
   * Creates a new ResStartEqSolving instance using the specified properties.
   * @function create
   * @memberof ResStartEqSolving
   * @static
   * @param {IResStartEqSolving=} [properties] Properties to set
   * @returns {ResStartEqSolving} ResStartEqSolving instance
   */
  ResStartEqSolving.create = function create(properties) {
    return new ResStartEqSolving(properties);
  };

  /**
   * Encodes the specified ResStartEqSolving message. Does not implicitly {@link ResStartEqSolving.verify|verify} messages.
   * @function encode
   * @memberof ResStartEqSolving
   * @static
   * @param {IResStartEqSolving} message ResStartEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartEqSolving.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.aziErr != null && Object.hasOwnProperty.call(message, "aziErr"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.aziErr);
    if (message.altErr != null && Object.hasOwnProperty.call(message, "altErr"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.altErr);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResStartEqSolving message, length delimited. Does not implicitly {@link ResStartEqSolving.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResStartEqSolving
   * @static
   * @param {IResStartEqSolving} message ResStartEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartEqSolving.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResStartEqSolving message from the specified reader or buffer.
   * @function decode
   * @memberof ResStartEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResStartEqSolving} ResStartEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartEqSolving.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResStartEqSolving();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.aziErr = reader.double();
          break;
        }
        case 2: {
          message.altErr = reader.double();
          break;
        }
        case 3: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResStartEqSolving message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResStartEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResStartEqSolving} ResStartEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartEqSolving.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResStartEqSolving message.
   * @function verify
   * @memberof ResStartEqSolving
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResStartEqSolving.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.aziErr != null && Object.hasOwnProperty.call(message, "aziErr"))
      if (typeof message.aziErr !== "number") return "aziErr: number expected";
    if (message.altErr != null && Object.hasOwnProperty.call(message, "altErr"))
      if (typeof message.altErr !== "number") return "altErr: number expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResStartEqSolving message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResStartEqSolving
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResStartEqSolving} ResStartEqSolving
   */
  ResStartEqSolving.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResStartEqSolving) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResStartEqSolving: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResStartEqSolving();
    if (object.aziErr != null) message.aziErr = Number(object.aziErr);
    if (object.altErr != null) message.altErr = Number(object.altErr);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResStartEqSolving message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResStartEqSolving
   * @static
   * @param {ResStartEqSolving} message ResStartEqSolving
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResStartEqSolving.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.aziErr = 0;
      object.altErr = 0;
      object.code = 0;
    }
    if (message.aziErr != null && Object.hasOwnProperty.call(message, "aziErr"))
      object.aziErr =
        options.json && !isFinite(message.aziErr)
          ? String(message.aziErr)
          : message.aziErr;
    if (message.altErr != null && Object.hasOwnProperty.call(message, "altErr"))
      object.altErr =
        options.json && !isFinite(message.altErr)
          ? String(message.altErr)
          : message.altErr;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResStartEqSolving to JSON.
   * @function toJSON
   * @memberof ResStartEqSolving
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResStartEqSolving.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResStartEqSolving
   * @function getTypeUrl
   * @memberof ResStartEqSolving
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResStartEqSolving.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResStartEqSolving";
  };

  return ResStartEqSolving;
})();

$root.ReqStopEqSolving = (function () {
  /**
   * Properties of a ReqStopEqSolving.
   * @exports IReqStopEqSolving
   * @interface IReqStopEqSolving
   */

  /**
   * Constructs a new ReqStopEqSolving.
   * @exports ReqStopEqSolving
   * @classdesc Represents a ReqStopEqSolving.
   * @implements IReqStopEqSolving
   * @constructor
   * @param {IReqStopEqSolving=} [properties] Properties to set
   */
  function ReqStopEqSolving(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopEqSolving instance using the specified properties.
   * @function create
   * @memberof ReqStopEqSolving
   * @static
   * @param {IReqStopEqSolving=} [properties] Properties to set
   * @returns {ReqStopEqSolving} ReqStopEqSolving instance
   */
  ReqStopEqSolving.create = function create(properties) {
    return new ReqStopEqSolving(properties);
  };

  /**
   * Encodes the specified ReqStopEqSolving message. Does not implicitly {@link ReqStopEqSolving.verify|verify} messages.
   * @function encode
   * @memberof ReqStopEqSolving
   * @static
   * @param {IReqStopEqSolving} message ReqStopEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopEqSolving.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopEqSolving message, length delimited. Does not implicitly {@link ReqStopEqSolving.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopEqSolving
   * @static
   * @param {IReqStopEqSolving} message ReqStopEqSolving message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopEqSolving.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopEqSolving message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopEqSolving} ReqStopEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopEqSolving.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopEqSolving();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopEqSolving message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopEqSolving
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopEqSolving} ReqStopEqSolving
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopEqSolving.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopEqSolving message.
   * @function verify
   * @memberof ReqStopEqSolving
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopEqSolving.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopEqSolving message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopEqSolving
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopEqSolving} ReqStopEqSolving
   */
  ReqStopEqSolving.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopEqSolving) return object;
    return new $root.ReqStopEqSolving();
  };

  /**
   * Creates a plain object from a ReqStopEqSolving message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopEqSolving
   * @static
   * @param {ReqStopEqSolving} message ReqStopEqSolving
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopEqSolving.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopEqSolving to JSON.
   * @function toJSON
   * @memberof ReqStopEqSolving
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopEqSolving.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopEqSolving
   * @function getTypeUrl
   * @memberof ReqStopEqSolving
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopEqSolving.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopEqSolving";
  };

  return ReqStopEqSolving;
})();

$root.ReqStartAiEnhance = (function () {
  /**
   * Properties of a ReqStartAiEnhance.
   * @exports IReqStartAiEnhance
   * @interface IReqStartAiEnhance
   */

  /**
   * Constructs a new ReqStartAiEnhance.
   * @exports ReqStartAiEnhance
   * @classdesc Represents a ReqStartAiEnhance.
   * @implements IReqStartAiEnhance
   * @constructor
   * @param {IReqStartAiEnhance=} [properties] Properties to set
   */
  function ReqStartAiEnhance(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStartAiEnhance instance using the specified properties.
   * @function create
   * @memberof ReqStartAiEnhance
   * @static
   * @param {IReqStartAiEnhance=} [properties] Properties to set
   * @returns {ReqStartAiEnhance} ReqStartAiEnhance instance
   */
  ReqStartAiEnhance.create = function create(properties) {
    return new ReqStartAiEnhance(properties);
  };

  /**
   * Encodes the specified ReqStartAiEnhance message. Does not implicitly {@link ReqStartAiEnhance.verify|verify} messages.
   * @function encode
   * @memberof ReqStartAiEnhance
   * @static
   * @param {IReqStartAiEnhance} message ReqStartAiEnhance message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartAiEnhance.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStartAiEnhance message, length delimited. Does not implicitly {@link ReqStartAiEnhance.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartAiEnhance
   * @static
   * @param {IReqStartAiEnhance} message ReqStartAiEnhance message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartAiEnhance.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartAiEnhance message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartAiEnhance
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartAiEnhance} ReqStartAiEnhance
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartAiEnhance.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartAiEnhance();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartAiEnhance message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartAiEnhance
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartAiEnhance} ReqStartAiEnhance
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartAiEnhance.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartAiEnhance message.
   * @function verify
   * @memberof ReqStartAiEnhance
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartAiEnhance.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStartAiEnhance message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartAiEnhance
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartAiEnhance} ReqStartAiEnhance
   */
  ReqStartAiEnhance.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartAiEnhance) return object;
    return new $root.ReqStartAiEnhance();
  };

  /**
   * Creates a plain object from a ReqStartAiEnhance message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartAiEnhance
   * @static
   * @param {ReqStartAiEnhance} message ReqStartAiEnhance
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartAiEnhance.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStartAiEnhance to JSON.
   * @function toJSON
   * @memberof ReqStartAiEnhance
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartAiEnhance.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartAiEnhance
   * @function getTypeUrl
   * @memberof ReqStartAiEnhance
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartAiEnhance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartAiEnhance";
  };

  return ReqStartAiEnhance;
})();

$root.ReqStopAiEnhance = (function () {
  /**
   * Properties of a ReqStopAiEnhance.
   * @exports IReqStopAiEnhance
   * @interface IReqStopAiEnhance
   */

  /**
   * Constructs a new ReqStopAiEnhance.
   * @exports ReqStopAiEnhance
   * @classdesc Represents a ReqStopAiEnhance.
   * @implements IReqStopAiEnhance
   * @constructor
   * @param {IReqStopAiEnhance=} [properties] Properties to set
   */
  function ReqStopAiEnhance(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopAiEnhance instance using the specified properties.
   * @function create
   * @memberof ReqStopAiEnhance
   * @static
   * @param {IReqStopAiEnhance=} [properties] Properties to set
   * @returns {ReqStopAiEnhance} ReqStopAiEnhance instance
   */
  ReqStopAiEnhance.create = function create(properties) {
    return new ReqStopAiEnhance(properties);
  };

  /**
   * Encodes the specified ReqStopAiEnhance message. Does not implicitly {@link ReqStopAiEnhance.verify|verify} messages.
   * @function encode
   * @memberof ReqStopAiEnhance
   * @static
   * @param {IReqStopAiEnhance} message ReqStopAiEnhance message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopAiEnhance.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopAiEnhance message, length delimited. Does not implicitly {@link ReqStopAiEnhance.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopAiEnhance
   * @static
   * @param {IReqStopAiEnhance} message ReqStopAiEnhance message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopAiEnhance.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopAiEnhance message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopAiEnhance
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopAiEnhance} ReqStopAiEnhance
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopAiEnhance.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopAiEnhance();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopAiEnhance message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopAiEnhance
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopAiEnhance} ReqStopAiEnhance
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopAiEnhance.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopAiEnhance message.
   * @function verify
   * @memberof ReqStopAiEnhance
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopAiEnhance.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopAiEnhance message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopAiEnhance
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopAiEnhance} ReqStopAiEnhance
   */
  ReqStopAiEnhance.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopAiEnhance) return object;
    return new $root.ReqStopAiEnhance();
  };

  /**
   * Creates a plain object from a ReqStopAiEnhance message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopAiEnhance
   * @static
   * @param {ReqStopAiEnhance} message ReqStopAiEnhance
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopAiEnhance.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopAiEnhance to JSON.
   * @function toJSON
   * @memberof ReqStopAiEnhance
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopAiEnhance.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopAiEnhance
   * @function getTypeUrl
   * @memberof ReqStopAiEnhance
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopAiEnhance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopAiEnhance";
  };

  return ReqStopAiEnhance;
})();

$root.ReqStartMosaic = (function () {
  /**
   * Properties of a ReqStartMosaic.
   * @exports IReqStartMosaic
   * @interface IReqStartMosaic
   * @property {number|null} [horizontalScale] ReqStartMosaic horizontalScale
   * @property {number|null} [verticalScale] ReqStartMosaic verticalScale
   * @property {number|null} [rotation] ReqStartMosaic rotation
   * @property {number|null} [irIndex] ReqStartMosaic irIndex
   * @property {boolean|null} [forceStart] ReqStartMosaic forceStart
   */

  /**
   * Constructs a new ReqStartMosaic.
   * @exports ReqStartMosaic
   * @classdesc Represents a ReqStartMosaic.
   * @implements IReqStartMosaic
   * @constructor
   * @param {IReqStartMosaic=} [properties] Properties to set
   */
  function ReqStartMosaic(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartMosaic horizontalScale.
   * @member {number} horizontalScale
   * @memberof ReqStartMosaic
   * @instance
   */
  ReqStartMosaic.prototype.horizontalScale = 0;

  /**
   * ReqStartMosaic verticalScale.
   * @member {number} verticalScale
   * @memberof ReqStartMosaic
   * @instance
   */
  ReqStartMosaic.prototype.verticalScale = 0;

  /**
   * ReqStartMosaic rotation.
   * @member {number} rotation
   * @memberof ReqStartMosaic
   * @instance
   */
  ReqStartMosaic.prototype.rotation = 0;

  /**
   * ReqStartMosaic irIndex.
   * @member {number} irIndex
   * @memberof ReqStartMosaic
   * @instance
   */
  ReqStartMosaic.prototype.irIndex = 0;

  /**
   * ReqStartMosaic forceStart.
   * @member {boolean} forceStart
   * @memberof ReqStartMosaic
   * @instance
   */
  ReqStartMosaic.prototype.forceStart = false;

  /**
   * Creates a new ReqStartMosaic instance using the specified properties.
   * @function create
   * @memberof ReqStartMosaic
   * @static
   * @param {IReqStartMosaic=} [properties] Properties to set
   * @returns {ReqStartMosaic} ReqStartMosaic instance
   */
  ReqStartMosaic.create = function create(properties) {
    return new ReqStartMosaic(properties);
  };

  /**
   * Encodes the specified ReqStartMosaic message. Does not implicitly {@link ReqStartMosaic.verify|verify} messages.
   * @function encode
   * @memberof ReqStartMosaic
   * @static
   * @param {IReqStartMosaic} message ReqStartMosaic message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartMosaic.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.horizontalScale);
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.verticalScale);
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.rotation);
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.irIndex);
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).bool(message.forceStart);
    return writer;
  };

  /**
   * Encodes the specified ReqStartMosaic message, length delimited. Does not implicitly {@link ReqStartMosaic.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartMosaic
   * @static
   * @param {IReqStartMosaic} message ReqStartMosaic message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartMosaic.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartMosaic message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartMosaic
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartMosaic} ReqStartMosaic
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartMosaic.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartMosaic();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.horizontalScale = reader.int32();
          break;
        }
        case 2: {
          message.verticalScale = reader.int32();
          break;
        }
        case 3: {
          message.rotation = reader.int32();
          break;
        }
        case 4: {
          message.irIndex = reader.int32();
          break;
        }
        case 5: {
          message.forceStart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartMosaic message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartMosaic
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartMosaic} ReqStartMosaic
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartMosaic.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartMosaic message.
   * @function verify
   * @memberof ReqStartMosaic
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartMosaic.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      if (!$util.isInteger(message.horizontalScale))
        return "horizontalScale: integer expected";
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      if (!$util.isInteger(message.verticalScale))
        return "verticalScale: integer expected";
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      if (!$util.isInteger(message.rotation))
        return "rotation: integer expected";
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      if (!$util.isInteger(message.irIndex)) return "irIndex: integer expected";
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      if (typeof message.forceStart !== "boolean")
        return "forceStart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqStartMosaic message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartMosaic
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartMosaic} ReqStartMosaic
   */
  ReqStartMosaic.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartMosaic) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartMosaic: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartMosaic();
    if (object.horizontalScale != null)
      message.horizontalScale = object.horizontalScale | 0;
    if (object.verticalScale != null)
      message.verticalScale = object.verticalScale | 0;
    if (object.rotation != null) message.rotation = object.rotation | 0;
    if (object.irIndex != null) message.irIndex = object.irIndex | 0;
    if (object.forceStart != null)
      message.forceStart = Boolean(object.forceStart);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartMosaic message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartMosaic
   * @static
   * @param {ReqStartMosaic} message ReqStartMosaic
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartMosaic.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.horizontalScale = 0;
      object.verticalScale = 0;
      object.rotation = 0;
      object.irIndex = 0;
      object.forceStart = false;
    }
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      object.horizontalScale = message.horizontalScale;
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      object.verticalScale = message.verticalScale;
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      object.rotation = message.rotation;
    if (
      message.irIndex != null &&
      Object.hasOwnProperty.call(message, "irIndex")
    )
      object.irIndex = message.irIndex;
    if (
      message.forceStart != null &&
      Object.hasOwnProperty.call(message, "forceStart")
    )
      object.forceStart = message.forceStart;
    return object;
  };

  /**
   * Converts this ReqStartMosaic to JSON.
   * @function toJSON
   * @memberof ReqStartMosaic
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartMosaic.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartMosaic
   * @function getTypeUrl
   * @memberof ReqStartMosaic
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartMosaic.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartMosaic";
  };

  return ReqStartMosaic;
})();

$root.ReqStartMakeFitsThumb = (function () {
  /**
   * Properties of a ReqStartMakeFitsThumb.
   * @exports IReqStartMakeFitsThumb
   * @interface IReqStartMakeFitsThumb
   * @property {string|null} [srcDir] ReqStartMakeFitsThumb srcDir
   */

  /**
   * Constructs a new ReqStartMakeFitsThumb.
   * @exports ReqStartMakeFitsThumb
   * @classdesc Represents a ReqStartMakeFitsThumb.
   * @implements IReqStartMakeFitsThumb
   * @constructor
   * @param {IReqStartMakeFitsThumb=} [properties] Properties to set
   */
  function ReqStartMakeFitsThumb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartMakeFitsThumb srcDir.
   * @member {string} srcDir
   * @memberof ReqStartMakeFitsThumb
   * @instance
   */
  ReqStartMakeFitsThumb.prototype.srcDir = "";

  /**
   * Creates a new ReqStartMakeFitsThumb instance using the specified properties.
   * @function create
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {IReqStartMakeFitsThumb=} [properties] Properties to set
   * @returns {ReqStartMakeFitsThumb} ReqStartMakeFitsThumb instance
   */
  ReqStartMakeFitsThumb.create = function create(properties) {
    return new ReqStartMakeFitsThumb(properties);
  };

  /**
   * Encodes the specified ReqStartMakeFitsThumb message. Does not implicitly {@link ReqStartMakeFitsThumb.verify|verify} messages.
   * @function encode
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {IReqStartMakeFitsThumb} message ReqStartMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartMakeFitsThumb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.srcDir);
    return writer;
  };

  /**
   * Encodes the specified ReqStartMakeFitsThumb message, length delimited. Does not implicitly {@link ReqStartMakeFitsThumb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {IReqStartMakeFitsThumb} message ReqStartMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartMakeFitsThumb.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartMakeFitsThumb message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartMakeFitsThumb} ReqStartMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartMakeFitsThumb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartMakeFitsThumb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.srcDir = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartMakeFitsThumb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartMakeFitsThumb} ReqStartMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartMakeFitsThumb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartMakeFitsThumb message.
   * @function verify
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartMakeFitsThumb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      if (!$util.isString(message.srcDir)) return "srcDir: string expected";
    return null;
  };

  /**
   * Creates a ReqStartMakeFitsThumb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartMakeFitsThumb} ReqStartMakeFitsThumb
   */
  ReqStartMakeFitsThumb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartMakeFitsThumb) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartMakeFitsThumb: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartMakeFitsThumb();
    if (object.srcDir != null) message.srcDir = String(object.srcDir);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartMakeFitsThumb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {ReqStartMakeFitsThumb} message ReqStartMakeFitsThumb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartMakeFitsThumb.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.srcDir = "";
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      object.srcDir = message.srcDir;
    return object;
  };

  /**
   * Converts this ReqStartMakeFitsThumb to JSON.
   * @function toJSON
   * @memberof ReqStartMakeFitsThumb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartMakeFitsThumb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartMakeFitsThumb
   * @function getTypeUrl
   * @memberof ReqStartMakeFitsThumb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartMakeFitsThumb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartMakeFitsThumb";
  };

  return ReqStartMakeFitsThumb;
})();

$root.ReqStopMakeFitsThumb = (function () {
  /**
   * Properties of a ReqStopMakeFitsThumb.
   * @exports IReqStopMakeFitsThumb
   * @interface IReqStopMakeFitsThumb
   */

  /**
   * Constructs a new ReqStopMakeFitsThumb.
   * @exports ReqStopMakeFitsThumb
   * @classdesc Represents a ReqStopMakeFitsThumb.
   * @implements IReqStopMakeFitsThumb
   * @constructor
   * @param {IReqStopMakeFitsThumb=} [properties] Properties to set
   */
  function ReqStopMakeFitsThumb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopMakeFitsThumb instance using the specified properties.
   * @function create
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {IReqStopMakeFitsThumb=} [properties] Properties to set
   * @returns {ReqStopMakeFitsThumb} ReqStopMakeFitsThumb instance
   */
  ReqStopMakeFitsThumb.create = function create(properties) {
    return new ReqStopMakeFitsThumb(properties);
  };

  /**
   * Encodes the specified ReqStopMakeFitsThumb message. Does not implicitly {@link ReqStopMakeFitsThumb.verify|verify} messages.
   * @function encode
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {IReqStopMakeFitsThumb} message ReqStopMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopMakeFitsThumb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopMakeFitsThumb message, length delimited. Does not implicitly {@link ReqStopMakeFitsThumb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {IReqStopMakeFitsThumb} message ReqStopMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopMakeFitsThumb.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopMakeFitsThumb message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopMakeFitsThumb} ReqStopMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopMakeFitsThumb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopMakeFitsThumb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopMakeFitsThumb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopMakeFitsThumb} ReqStopMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopMakeFitsThumb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopMakeFitsThumb message.
   * @function verify
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopMakeFitsThumb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopMakeFitsThumb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopMakeFitsThumb} ReqStopMakeFitsThumb
   */
  ReqStopMakeFitsThumb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopMakeFitsThumb) return object;
    return new $root.ReqStopMakeFitsThumb();
  };

  /**
   * Creates a plain object from a ReqStopMakeFitsThumb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {ReqStopMakeFitsThumb} message ReqStopMakeFitsThumb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopMakeFitsThumb.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopMakeFitsThumb to JSON.
   * @function toJSON
   * @memberof ReqStopMakeFitsThumb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopMakeFitsThumb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopMakeFitsThumb
   * @function getTypeUrl
   * @memberof ReqStopMakeFitsThumb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopMakeFitsThumb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopMakeFitsThumb";
  };

  return ReqStopMakeFitsThumb;
})();

$root.ResMakeFitsThumb = (function () {
  /**
   * Properties of a ResMakeFitsThumb.
   * @exports IResMakeFitsThumb
   * @interface IResMakeFitsThumb
   * @property {number|null} [code] ResMakeFitsThumb code
   * @property {string|null} [srcDir] ResMakeFitsThumb srcDir
   */

  /**
   * Constructs a new ResMakeFitsThumb.
   * @exports ResMakeFitsThumb
   * @classdesc Represents a ResMakeFitsThumb.
   * @implements IResMakeFitsThumb
   * @constructor
   * @param {IResMakeFitsThumb=} [properties] Properties to set
   */
  function ResMakeFitsThumb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResMakeFitsThumb code.
   * @member {number} code
   * @memberof ResMakeFitsThumb
   * @instance
   */
  ResMakeFitsThumb.prototype.code = 0;

  /**
   * ResMakeFitsThumb srcDir.
   * @member {string} srcDir
   * @memberof ResMakeFitsThumb
   * @instance
   */
  ResMakeFitsThumb.prototype.srcDir = "";

  /**
   * Creates a new ResMakeFitsThumb instance using the specified properties.
   * @function create
   * @memberof ResMakeFitsThumb
   * @static
   * @param {IResMakeFitsThumb=} [properties] Properties to set
   * @returns {ResMakeFitsThumb} ResMakeFitsThumb instance
   */
  ResMakeFitsThumb.create = function create(properties) {
    return new ResMakeFitsThumb(properties);
  };

  /**
   * Encodes the specified ResMakeFitsThumb message. Does not implicitly {@link ResMakeFitsThumb.verify|verify} messages.
   * @function encode
   * @memberof ResMakeFitsThumb
   * @static
   * @param {IResMakeFitsThumb} message ResMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMakeFitsThumb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.srcDir);
    return writer;
  };

  /**
   * Encodes the specified ResMakeFitsThumb message, length delimited. Does not implicitly {@link ResMakeFitsThumb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResMakeFitsThumb
   * @static
   * @param {IResMakeFitsThumb} message ResMakeFitsThumb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMakeFitsThumb.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResMakeFitsThumb message from the specified reader or buffer.
   * @function decode
   * @memberof ResMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResMakeFitsThumb} ResMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMakeFitsThumb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResMakeFitsThumb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.srcDir = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResMakeFitsThumb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResMakeFitsThumb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResMakeFitsThumb} ResMakeFitsThumb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMakeFitsThumb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResMakeFitsThumb message.
   * @function verify
   * @memberof ResMakeFitsThumb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResMakeFitsThumb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      if (!$util.isString(message.srcDir)) return "srcDir: string expected";
    return null;
  };

  /**
   * Creates a ResMakeFitsThumb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResMakeFitsThumb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResMakeFitsThumb} ResMakeFitsThumb
   */
  ResMakeFitsThumb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResMakeFitsThumb) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResMakeFitsThumb: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResMakeFitsThumb();
    if (object.code != null) message.code = object.code | 0;
    if (object.srcDir != null) message.srcDir = String(object.srcDir);
    return message;
  };

  /**
   * Creates a plain object from a ResMakeFitsThumb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResMakeFitsThumb
   * @static
   * @param {ResMakeFitsThumb} message ResMakeFitsThumb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResMakeFitsThumb.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.srcDir = "";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      object.srcDir = message.srcDir;
    return object;
  };

  /**
   * Converts this ResMakeFitsThumb to JSON.
   * @function toJSON
   * @memberof ResMakeFitsThumb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResMakeFitsThumb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResMakeFitsThumb
   * @function getTypeUrl
   * @memberof ResMakeFitsThumb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResMakeFitsThumb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResMakeFitsThumb";
  };

  return ResMakeFitsThumb;
})();

$root.MakeFitsThumbTaskParam = (function () {
  /**
   * Properties of a MakeFitsThumbTaskParam.
   * @exports IMakeFitsThumbTaskParam
   * @interface IMakeFitsThumbTaskParam
   * @property {string|null} [srcDir] MakeFitsThumbTaskParam srcDir
   */

  /**
   * Constructs a new MakeFitsThumbTaskParam.
   * @exports MakeFitsThumbTaskParam
   * @classdesc Represents a MakeFitsThumbTaskParam.
   * @implements IMakeFitsThumbTaskParam
   * @constructor
   * @param {IMakeFitsThumbTaskParam=} [properties] Properties to set
   */
  function MakeFitsThumbTaskParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * MakeFitsThumbTaskParam srcDir.
   * @member {string} srcDir
   * @memberof MakeFitsThumbTaskParam
   * @instance
   */
  MakeFitsThumbTaskParam.prototype.srcDir = "";

  /**
   * Creates a new MakeFitsThumbTaskParam instance using the specified properties.
   * @function create
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {IMakeFitsThumbTaskParam=} [properties] Properties to set
   * @returns {MakeFitsThumbTaskParam} MakeFitsThumbTaskParam instance
   */
  MakeFitsThumbTaskParam.create = function create(properties) {
    return new MakeFitsThumbTaskParam(properties);
  };

  /**
   * Encodes the specified MakeFitsThumbTaskParam message. Does not implicitly {@link MakeFitsThumbTaskParam.verify|verify} messages.
   * @function encode
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {IMakeFitsThumbTaskParam} message MakeFitsThumbTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  MakeFitsThumbTaskParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.srcDir);
    return writer;
  };

  /**
   * Encodes the specified MakeFitsThumbTaskParam message, length delimited. Does not implicitly {@link MakeFitsThumbTaskParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {IMakeFitsThumbTaskParam} message MakeFitsThumbTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  MakeFitsThumbTaskParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a MakeFitsThumbTaskParam message from the specified reader or buffer.
   * @function decode
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {MakeFitsThumbTaskParam} MakeFitsThumbTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  MakeFitsThumbTaskParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.MakeFitsThumbTaskParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.srcDir = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a MakeFitsThumbTaskParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {MakeFitsThumbTaskParam} MakeFitsThumbTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  MakeFitsThumbTaskParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a MakeFitsThumbTaskParam message.
   * @function verify
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  MakeFitsThumbTaskParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      if (!$util.isString(message.srcDir)) return "srcDir: string expected";
    return null;
  };

  /**
   * Creates a MakeFitsThumbTaskParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {MakeFitsThumbTaskParam} MakeFitsThumbTaskParam
   */
  MakeFitsThumbTaskParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.MakeFitsThumbTaskParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".MakeFitsThumbTaskParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.MakeFitsThumbTaskParam();
    if (object.srcDir != null) message.srcDir = String(object.srcDir);
    return message;
  };

  /**
   * Creates a plain object from a MakeFitsThumbTaskParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {MakeFitsThumbTaskParam} message MakeFitsThumbTaskParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  MakeFitsThumbTaskParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.srcDir = "";
    if (message.srcDir != null && Object.hasOwnProperty.call(message, "srcDir"))
      object.srcDir = message.srcDir;
    return object;
  };

  /**
   * Converts this MakeFitsThumbTaskParam to JSON.
   * @function toJSON
   * @memberof MakeFitsThumbTaskParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  MakeFitsThumbTaskParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for MakeFitsThumbTaskParam
   * @function getTypeUrl
   * @memberof MakeFitsThumbTaskParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  MakeFitsThumbTaskParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/MakeFitsThumbTaskParam";
  };

  return MakeFitsThumbTaskParam;
})();

$root.ReqIsImageStackable = (function () {
  /**
   * Properties of a ReqIsImageStackable.
   * @exports IReqIsImageStackable
   * @interface IReqIsImageStackable
   * @property {Array.<string>|null} [srcDirs] ReqIsImageStackable srcDirs
   */

  /**
   * Constructs a new ReqIsImageStackable.
   * @exports ReqIsImageStackable
   * @classdesc Represents a ReqIsImageStackable.
   * @implements IReqIsImageStackable
   * @constructor
   * @param {IReqIsImageStackable=} [properties] Properties to set
   */
  function ReqIsImageStackable(properties) {
    this.srcDirs = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqIsImageStackable srcDirs.
   * @member {Array.<string>} srcDirs
   * @memberof ReqIsImageStackable
   * @instance
   */
  ReqIsImageStackable.prototype.srcDirs = $util.emptyArray;

  /**
   * Creates a new ReqIsImageStackable instance using the specified properties.
   * @function create
   * @memberof ReqIsImageStackable
   * @static
   * @param {IReqIsImageStackable=} [properties] Properties to set
   * @returns {ReqIsImageStackable} ReqIsImageStackable instance
   */
  ReqIsImageStackable.create = function create(properties) {
    return new ReqIsImageStackable(properties);
  };

  /**
   * Encodes the specified ReqIsImageStackable message. Does not implicitly {@link ReqIsImageStackable.verify|verify} messages.
   * @function encode
   * @memberof ReqIsImageStackable
   * @static
   * @param {IReqIsImageStackable} message ReqIsImageStackable message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqIsImageStackable.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.srcDirs != null && message.srcDirs.length)
      for (var i = 0; i < message.srcDirs.length; ++i)
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.srcDirs[i]);
    return writer;
  };

  /**
   * Encodes the specified ReqIsImageStackable message, length delimited. Does not implicitly {@link ReqIsImageStackable.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqIsImageStackable
   * @static
   * @param {IReqIsImageStackable} message ReqIsImageStackable message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqIsImageStackable.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqIsImageStackable message from the specified reader or buffer.
   * @function decode
   * @memberof ReqIsImageStackable
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqIsImageStackable} ReqIsImageStackable
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqIsImageStackable.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqIsImageStackable();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.srcDirs && message.srcDirs.length))
            message.srcDirs = [];
          message.srcDirs.push(reader.string());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqIsImageStackable message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqIsImageStackable
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqIsImageStackable} ReqIsImageStackable
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqIsImageStackable.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqIsImageStackable message.
   * @function verify
   * @memberof ReqIsImageStackable
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqIsImageStackable.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.srcDirs != null &&
      Object.hasOwnProperty.call(message, "srcDirs")
    ) {
      if (!Array.isArray(message.srcDirs)) return "srcDirs: array expected";
      for (var i = 0; i < message.srcDirs.length; ++i)
        if (!$util.isString(message.srcDirs[i]))
          return "srcDirs: string[] expected";
    }
    return null;
  };

  /**
   * Creates a ReqIsImageStackable message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqIsImageStackable
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqIsImageStackable} ReqIsImageStackable
   */
  ReqIsImageStackable.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqIsImageStackable) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqIsImageStackable: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqIsImageStackable();
    if (object.srcDirs) {
      if (!Array.isArray(object.srcDirs))
        throw TypeError(".ReqIsImageStackable.srcDirs: array expected");
      message.srcDirs = [];
      for (var i = 0; i < object.srcDirs.length; ++i)
        message.srcDirs[i] = String(object.srcDirs[i]);
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqIsImageStackable message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqIsImageStackable
   * @static
   * @param {ReqIsImageStackable} message ReqIsImageStackable
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqIsImageStackable.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.srcDirs = [];
    if (message.srcDirs && message.srcDirs.length) {
      object.srcDirs = [];
      for (var j = 0; j < message.srcDirs.length; ++j)
        object.srcDirs[j] = message.srcDirs[j];
    }
    return object;
  };

  /**
   * Converts this ReqIsImageStackable to JSON.
   * @function toJSON
   * @memberof ReqIsImageStackable
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqIsImageStackable.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqIsImageStackable
   * @function getTypeUrl
   * @memberof ReqIsImageStackable
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqIsImageStackable.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqIsImageStackable";
  };

  return ReqIsImageStackable;
})();

$root.ResIsImageStackable = (function () {
  /**
   * Properties of a ResIsImageStackable.
   * @exports IResIsImageStackable
   * @interface IResIsImageStackable
   * @property {number|null} [code] ResIsImageStackable code
   * @property {Array.<IResGetDarkFrameInfo>|null} [needDarkFrameInfo] ResIsImageStackable needDarkFrameInfo
   */

  /**
   * Constructs a new ResIsImageStackable.
   * @exports ResIsImageStackable
   * @classdesc Represents a ResIsImageStackable.
   * @implements IResIsImageStackable
   * @constructor
   * @param {IResIsImageStackable=} [properties] Properties to set
   */
  function ResIsImageStackable(properties) {
    this.needDarkFrameInfo = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResIsImageStackable code.
   * @member {number} code
   * @memberof ResIsImageStackable
   * @instance
   */
  ResIsImageStackable.prototype.code = 0;

  /**
   * ResIsImageStackable needDarkFrameInfo.
   * @member {Array.<IResGetDarkFrameInfo>} needDarkFrameInfo
   * @memberof ResIsImageStackable
   * @instance
   */
  ResIsImageStackable.prototype.needDarkFrameInfo = $util.emptyArray;

  /**
   * Creates a new ResIsImageStackable instance using the specified properties.
   * @function create
   * @memberof ResIsImageStackable
   * @static
   * @param {IResIsImageStackable=} [properties] Properties to set
   * @returns {ResIsImageStackable} ResIsImageStackable instance
   */
  ResIsImageStackable.create = function create(properties) {
    return new ResIsImageStackable(properties);
  };

  /**
   * Encodes the specified ResIsImageStackable message. Does not implicitly {@link ResIsImageStackable.verify|verify} messages.
   * @function encode
   * @memberof ResIsImageStackable
   * @static
   * @param {IResIsImageStackable} message ResIsImageStackable message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResIsImageStackable.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.needDarkFrameInfo != null && message.needDarkFrameInfo.length)
      for (var i = 0; i < message.needDarkFrameInfo.length; ++i)
        $root.ResGetDarkFrameInfo.encode(
          message.needDarkFrameInfo[i],
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResIsImageStackable message, length delimited. Does not implicitly {@link ResIsImageStackable.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResIsImageStackable
   * @static
   * @param {IResIsImageStackable} message ResIsImageStackable message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResIsImageStackable.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResIsImageStackable message from the specified reader or buffer.
   * @function decode
   * @memberof ResIsImageStackable
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResIsImageStackable} ResIsImageStackable
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResIsImageStackable.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResIsImageStackable();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          if (!(message.needDarkFrameInfo && message.needDarkFrameInfo.length))
            message.needDarkFrameInfo = [];
          message.needDarkFrameInfo.push(
            $root.ResGetDarkFrameInfo.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResIsImageStackable message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResIsImageStackable
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResIsImageStackable} ResIsImageStackable
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResIsImageStackable.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResIsImageStackable message.
   * @function verify
   * @memberof ResIsImageStackable
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResIsImageStackable.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.needDarkFrameInfo != null &&
      Object.hasOwnProperty.call(message, "needDarkFrameInfo")
    ) {
      if (!Array.isArray(message.needDarkFrameInfo))
        return "needDarkFrameInfo: array expected";
      for (var i = 0; i < message.needDarkFrameInfo.length; ++i) {
        var error = $root.ResGetDarkFrameInfo.verify(
          message.needDarkFrameInfo[i],
          long + 1,
        );
        if (error) return "needDarkFrameInfo." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ResIsImageStackable message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResIsImageStackable
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResIsImageStackable} ResIsImageStackable
   */
  ResIsImageStackable.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResIsImageStackable) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResIsImageStackable: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResIsImageStackable();
    if (object.code != null) message.code = object.code | 0;
    if (object.needDarkFrameInfo) {
      if (!Array.isArray(object.needDarkFrameInfo))
        throw TypeError(
          ".ResIsImageStackable.needDarkFrameInfo: array expected",
        );
      message.needDarkFrameInfo = [];
      for (var i = 0; i < object.needDarkFrameInfo.length; ++i) {
        if (!$util.isObject(object.needDarkFrameInfo[i]))
          throw TypeError(
            ".ResIsImageStackable.needDarkFrameInfo: object expected",
          );
        message.needDarkFrameInfo[i] = $root.ResGetDarkFrameInfo.fromObject(
          object.needDarkFrameInfo[i],
          long + 1,
        );
      }
    }
    return message;
  };

  /**
   * Creates a plain object from a ResIsImageStackable message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResIsImageStackable
   * @static
   * @param {ResIsImageStackable} message ResIsImageStackable
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResIsImageStackable.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.needDarkFrameInfo = [];
    if (options.defaults) object.code = 0;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.needDarkFrameInfo && message.needDarkFrameInfo.length) {
      object.needDarkFrameInfo = [];
      for (var j = 0; j < message.needDarkFrameInfo.length; ++j)
        object.needDarkFrameInfo[j] = $root.ResGetDarkFrameInfo.toObject(
          message.needDarkFrameInfo[j],
          options,
          q + 1,
        );
    }
    return object;
  };

  /**
   * Converts this ResIsImageStackable to JSON.
   * @function toJSON
   * @memberof ResIsImageStackable
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResIsImageStackable.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResIsImageStackable
   * @function getTypeUrl
   * @memberof ResIsImageStackable
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResIsImageStackable.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResIsImageStackable";
  };

  return ResIsImageStackable;
})();

$root.ReqStartRepostprocess = (function () {
  /**
   * Properties of a ReqStartRepostprocess.
   * @exports IReqStartRepostprocess
   * @interface IReqStartRepostprocess
   * @property {Array.<string>|null} [srcDirs] ReqStartRepostprocess srcDirs
   */

  /**
   * Constructs a new ReqStartRepostprocess.
   * @exports ReqStartRepostprocess
   * @classdesc Represents a ReqStartRepostprocess.
   * @implements IReqStartRepostprocess
   * @constructor
   * @param {IReqStartRepostprocess=} [properties] Properties to set
   */
  function ReqStartRepostprocess(properties) {
    this.srcDirs = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartRepostprocess srcDirs.
   * @member {Array.<string>} srcDirs
   * @memberof ReqStartRepostprocess
   * @instance
   */
  ReqStartRepostprocess.prototype.srcDirs = $util.emptyArray;

  /**
   * Creates a new ReqStartRepostprocess instance using the specified properties.
   * @function create
   * @memberof ReqStartRepostprocess
   * @static
   * @param {IReqStartRepostprocess=} [properties] Properties to set
   * @returns {ReqStartRepostprocess} ReqStartRepostprocess instance
   */
  ReqStartRepostprocess.create = function create(properties) {
    return new ReqStartRepostprocess(properties);
  };

  /**
   * Encodes the specified ReqStartRepostprocess message. Does not implicitly {@link ReqStartRepostprocess.verify|verify} messages.
   * @function encode
   * @memberof ReqStartRepostprocess
   * @static
   * @param {IReqStartRepostprocess} message ReqStartRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartRepostprocess.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.srcDirs != null && message.srcDirs.length)
      for (var i = 0; i < message.srcDirs.length; ++i)
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.srcDirs[i]);
    return writer;
  };

  /**
   * Encodes the specified ReqStartRepostprocess message, length delimited. Does not implicitly {@link ReqStartRepostprocess.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartRepostprocess
   * @static
   * @param {IReqStartRepostprocess} message ReqStartRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartRepostprocess.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartRepostprocess message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartRepostprocess} ReqStartRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartRepostprocess.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartRepostprocess();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.srcDirs && message.srcDirs.length))
            message.srcDirs = [];
          message.srcDirs.push(reader.string());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartRepostprocess message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartRepostprocess} ReqStartRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartRepostprocess.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartRepostprocess message.
   * @function verify
   * @memberof ReqStartRepostprocess
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartRepostprocess.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.srcDirs != null &&
      Object.hasOwnProperty.call(message, "srcDirs")
    ) {
      if (!Array.isArray(message.srcDirs)) return "srcDirs: array expected";
      for (var i = 0; i < message.srcDirs.length; ++i)
        if (!$util.isString(message.srcDirs[i]))
          return "srcDirs: string[] expected";
    }
    return null;
  };

  /**
   * Creates a ReqStartRepostprocess message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartRepostprocess
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartRepostprocess} ReqStartRepostprocess
   */
  ReqStartRepostprocess.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartRepostprocess) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartRepostprocess: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartRepostprocess();
    if (object.srcDirs) {
      if (!Array.isArray(object.srcDirs))
        throw TypeError(".ReqStartRepostprocess.srcDirs: array expected");
      message.srcDirs = [];
      for (var i = 0; i < object.srcDirs.length; ++i)
        message.srcDirs[i] = String(object.srcDirs[i]);
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqStartRepostprocess message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartRepostprocess
   * @static
   * @param {ReqStartRepostprocess} message ReqStartRepostprocess
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartRepostprocess.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.srcDirs = [];
    if (message.srcDirs && message.srcDirs.length) {
      object.srcDirs = [];
      for (var j = 0; j < message.srcDirs.length; ++j)
        object.srcDirs[j] = message.srcDirs[j];
    }
    return object;
  };

  /**
   * Converts this ReqStartRepostprocess to JSON.
   * @function toJSON
   * @memberof ReqStartRepostprocess
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartRepostprocess.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartRepostprocess
   * @function getTypeUrl
   * @memberof ReqStartRepostprocess
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartRepostprocess.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartRepostprocess";
  };

  return ReqStartRepostprocess;
})();

$root.ReqStopRepostprocess = (function () {
  /**
   * Properties of a ReqStopRepostprocess.
   * @exports IReqStopRepostprocess
   * @interface IReqStopRepostprocess
   */

  /**
   * Constructs a new ReqStopRepostprocess.
   * @exports ReqStopRepostprocess
   * @classdesc Represents a ReqStopRepostprocess.
   * @implements IReqStopRepostprocess
   * @constructor
   * @param {IReqStopRepostprocess=} [properties] Properties to set
   */
  function ReqStopRepostprocess(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopRepostprocess instance using the specified properties.
   * @function create
   * @memberof ReqStopRepostprocess
   * @static
   * @param {IReqStopRepostprocess=} [properties] Properties to set
   * @returns {ReqStopRepostprocess} ReqStopRepostprocess instance
   */
  ReqStopRepostprocess.create = function create(properties) {
    return new ReqStopRepostprocess(properties);
  };

  /**
   * Encodes the specified ReqStopRepostprocess message. Does not implicitly {@link ReqStopRepostprocess.verify|verify} messages.
   * @function encode
   * @memberof ReqStopRepostprocess
   * @static
   * @param {IReqStopRepostprocess} message ReqStopRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopRepostprocess.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopRepostprocess message, length delimited. Does not implicitly {@link ReqStopRepostprocess.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopRepostprocess
   * @static
   * @param {IReqStopRepostprocess} message ReqStopRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopRepostprocess.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopRepostprocess message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopRepostprocess} ReqStopRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopRepostprocess.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopRepostprocess();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopRepostprocess message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopRepostprocess} ReqStopRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopRepostprocess.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopRepostprocess message.
   * @function verify
   * @memberof ReqStopRepostprocess
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopRepostprocess.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopRepostprocess message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopRepostprocess
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopRepostprocess} ReqStopRepostprocess
   */
  ReqStopRepostprocess.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopRepostprocess) return object;
    return new $root.ReqStopRepostprocess();
  };

  /**
   * Creates a plain object from a ReqStopRepostprocess message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopRepostprocess
   * @static
   * @param {ReqStopRepostprocess} message ReqStopRepostprocess
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopRepostprocess.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopRepostprocess to JSON.
   * @function toJSON
   * @memberof ReqStopRepostprocess
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopRepostprocess.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopRepostprocess
   * @function getTypeUrl
   * @memberof ReqStopRepostprocess
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopRepostprocess.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopRepostprocess";
  };

  return ReqStopRepostprocess;
})();

$root.ResRepostprocess = (function () {
  /**
   * Properties of a ResRepostprocess.
   * @exports IResRepostprocess
   * @interface IResRepostprocess
   * @property {number|null} [code] ResRepostprocess code
   * @property {string|null} [resultDir] ResRepostprocess resultDir
   */

  /**
   * Constructs a new ResRepostprocess.
   * @exports ResRepostprocess
   * @classdesc Represents a ResRepostprocess.
   * @implements IResRepostprocess
   * @constructor
   * @param {IResRepostprocess=} [properties] Properties to set
   */
  function ResRepostprocess(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResRepostprocess code.
   * @member {number} code
   * @memberof ResRepostprocess
   * @instance
   */
  ResRepostprocess.prototype.code = 0;

  /**
   * ResRepostprocess resultDir.
   * @member {string} resultDir
   * @memberof ResRepostprocess
   * @instance
   */
  ResRepostprocess.prototype.resultDir = "";

  /**
   * Creates a new ResRepostprocess instance using the specified properties.
   * @function create
   * @memberof ResRepostprocess
   * @static
   * @param {IResRepostprocess=} [properties] Properties to set
   * @returns {ResRepostprocess} ResRepostprocess instance
   */
  ResRepostprocess.create = function create(properties) {
    return new ResRepostprocess(properties);
  };

  /**
   * Encodes the specified ResRepostprocess message. Does not implicitly {@link ResRepostprocess.verify|verify} messages.
   * @function encode
   * @memberof ResRepostprocess
   * @static
   * @param {IResRepostprocess} message ResRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResRepostprocess.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.resultDir);
    return writer;
  };

  /**
   * Encodes the specified ResRepostprocess message, length delimited. Does not implicitly {@link ResRepostprocess.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResRepostprocess
   * @static
   * @param {IResRepostprocess} message ResRepostprocess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResRepostprocess.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResRepostprocess message from the specified reader or buffer.
   * @function decode
   * @memberof ResRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResRepostprocess} ResRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResRepostprocess.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResRepostprocess();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.resultDir = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResRepostprocess message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResRepostprocess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResRepostprocess} ResRepostprocess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResRepostprocess.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResRepostprocess message.
   * @function verify
   * @memberof ResRepostprocess
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResRepostprocess.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      if (!$util.isString(message.resultDir))
        return "resultDir: string expected";
    return null;
  };

  /**
   * Creates a ResRepostprocess message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResRepostprocess
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResRepostprocess} ResRepostprocess
   */
  ResRepostprocess.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResRepostprocess) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResRepostprocess: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResRepostprocess();
    if (object.code != null) message.code = object.code | 0;
    if (object.resultDir != null) message.resultDir = String(object.resultDir);
    return message;
  };

  /**
   * Creates a plain object from a ResRepostprocess message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResRepostprocess
   * @static
   * @param {ResRepostprocess} message ResRepostprocess
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResRepostprocess.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.resultDir = "";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      object.resultDir = message.resultDir;
    return object;
  };

  /**
   * Converts this ResRepostprocess to JSON.
   * @function toJSON
   * @memberof ResRepostprocess
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResRepostprocess.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResRepostprocess
   * @function getTypeUrl
   * @memberof ResRepostprocess
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResRepostprocess.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResRepostprocess";
  };

  return ResRepostprocess;
})();

$root.RepostprocessTaskParam = (function () {
  /**
   * Properties of a RepostprocessTaskParam.
   * @exports IRepostprocessTaskParam
   * @interface IRepostprocessTaskParam
   * @property {string|null} [resultDir] RepostprocessTaskParam resultDir
   */

  /**
   * Constructs a new RepostprocessTaskParam.
   * @exports RepostprocessTaskParam
   * @classdesc Represents a RepostprocessTaskParam.
   * @implements IRepostprocessTaskParam
   * @constructor
   * @param {IRepostprocessTaskParam=} [properties] Properties to set
   */
  function RepostprocessTaskParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * RepostprocessTaskParam resultDir.
   * @member {string} resultDir
   * @memberof RepostprocessTaskParam
   * @instance
   */
  RepostprocessTaskParam.prototype.resultDir = "";

  /**
   * Creates a new RepostprocessTaskParam instance using the specified properties.
   * @function create
   * @memberof RepostprocessTaskParam
   * @static
   * @param {IRepostprocessTaskParam=} [properties] Properties to set
   * @returns {RepostprocessTaskParam} RepostprocessTaskParam instance
   */
  RepostprocessTaskParam.create = function create(properties) {
    return new RepostprocessTaskParam(properties);
  };

  /**
   * Encodes the specified RepostprocessTaskParam message. Does not implicitly {@link RepostprocessTaskParam.verify|verify} messages.
   * @function encode
   * @memberof RepostprocessTaskParam
   * @static
   * @param {IRepostprocessTaskParam} message RepostprocessTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  RepostprocessTaskParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.resultDir);
    return writer;
  };

  /**
   * Encodes the specified RepostprocessTaskParam message, length delimited. Does not implicitly {@link RepostprocessTaskParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof RepostprocessTaskParam
   * @static
   * @param {IRepostprocessTaskParam} message RepostprocessTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  RepostprocessTaskParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a RepostprocessTaskParam message from the specified reader or buffer.
   * @function decode
   * @memberof RepostprocessTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {RepostprocessTaskParam} RepostprocessTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  RepostprocessTaskParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.RepostprocessTaskParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.resultDir = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a RepostprocessTaskParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof RepostprocessTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {RepostprocessTaskParam} RepostprocessTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  RepostprocessTaskParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a RepostprocessTaskParam message.
   * @function verify
   * @memberof RepostprocessTaskParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  RepostprocessTaskParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      if (!$util.isString(message.resultDir))
        return "resultDir: string expected";
    return null;
  };

  /**
   * Creates a RepostprocessTaskParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof RepostprocessTaskParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {RepostprocessTaskParam} RepostprocessTaskParam
   */
  RepostprocessTaskParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.RepostprocessTaskParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".RepostprocessTaskParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.RepostprocessTaskParam();
    if (object.resultDir != null) message.resultDir = String(object.resultDir);
    return message;
  };

  /**
   * Creates a plain object from a RepostprocessTaskParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof RepostprocessTaskParam
   * @static
   * @param {RepostprocessTaskParam} message RepostprocessTaskParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  RepostprocessTaskParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.resultDir = "";
    if (
      message.resultDir != null &&
      Object.hasOwnProperty.call(message, "resultDir")
    )
      object.resultDir = message.resultDir;
    return object;
  };

  /**
   * Converts this RepostprocessTaskParam to JSON.
   * @function toJSON
   * @memberof RepostprocessTaskParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  RepostprocessTaskParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for RepostprocessTaskParam
   * @function getTypeUrl
   * @memberof RepostprocessTaskParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  RepostprocessTaskParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/RepostprocessTaskParam";
  };

  return RepostprocessTaskParam;
})();

$root.ReqGetAstroShootingTime = (function () {
  /**
   * Properties of a ReqGetAstroShootingTime.
   * @exports IReqGetAstroShootingTime
   * @interface IReqGetAstroShootingTime
   * @property {number|null} [expIndex] ReqGetAstroShootingTime expIndex
   * @property {number|null} [horizontalScale] ReqGetAstroShootingTime horizontalScale
   * @property {number|null} [verticalScale] ReqGetAstroShootingTime verticalScale
   * @property {number|null} [rotation] ReqGetAstroShootingTime rotation
   * @property {number|null} [camId] ReqGetAstroShootingTime camId
   * @property {number|null} [shootingMode] ReqGetAstroShootingTime shootingMode
   */

  /**
   * Constructs a new ReqGetAstroShootingTime.
   * @exports ReqGetAstroShootingTime
   * @classdesc Represents a ReqGetAstroShootingTime.
   * @implements IReqGetAstroShootingTime
   * @constructor
   * @param {IReqGetAstroShootingTime=} [properties] Properties to set
   */
  function ReqGetAstroShootingTime(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetAstroShootingTime expIndex.
   * @member {number} expIndex
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.expIndex = 0;

  /**
   * ReqGetAstroShootingTime horizontalScale.
   * @member {number} horizontalScale
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.horizontalScale = 0;

  /**
   * ReqGetAstroShootingTime verticalScale.
   * @member {number} verticalScale
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.verticalScale = 0;

  /**
   * ReqGetAstroShootingTime rotation.
   * @member {number} rotation
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.rotation = 0;

  /**
   * ReqGetAstroShootingTime camId.
   * @member {number} camId
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.camId = 0;

  /**
   * ReqGetAstroShootingTime shootingMode.
   * @member {number} shootingMode
   * @memberof ReqGetAstroShootingTime
   * @instance
   */
  ReqGetAstroShootingTime.prototype.shootingMode = 0;

  /**
   * Creates a new ReqGetAstroShootingTime instance using the specified properties.
   * @function create
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {IReqGetAstroShootingTime=} [properties] Properties to set
   * @returns {ReqGetAstroShootingTime} ReqGetAstroShootingTime instance
   */
  ReqGetAstroShootingTime.create = function create(properties) {
    return new ReqGetAstroShootingTime(properties);
  };

  /**
   * Encodes the specified ReqGetAstroShootingTime message. Does not implicitly {@link ReqGetAstroShootingTime.verify|verify} messages.
   * @function encode
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {IReqGetAstroShootingTime} message ReqGetAstroShootingTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAstroShootingTime.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expIndex);
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.horizontalScale);
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.verticalScale);
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.rotation);
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.camId);
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.shootingMode);
    return writer;
  };

  /**
   * Encodes the specified ReqGetAstroShootingTime message, length delimited. Does not implicitly {@link ReqGetAstroShootingTime.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {IReqGetAstroShootingTime} message ReqGetAstroShootingTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAstroShootingTime.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetAstroShootingTime message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetAstroShootingTime} ReqGetAstroShootingTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAstroShootingTime.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetAstroShootingTime();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expIndex = reader.int32();
          break;
        }
        case 2: {
          message.horizontalScale = reader.int32();
          break;
        }
        case 3: {
          message.verticalScale = reader.int32();
          break;
        }
        case 4: {
          message.rotation = reader.int32();
          break;
        }
        case 5: {
          message.camId = reader.int32();
          break;
        }
        case 6: {
          message.shootingMode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetAstroShootingTime message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetAstroShootingTime} ReqGetAstroShootingTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAstroShootingTime.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetAstroShootingTime message.
   * @function verify
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetAstroShootingTime.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      if (!$util.isInteger(message.horizontalScale))
        return "horizontalScale: integer expected";
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      if (!$util.isInteger(message.verticalScale))
        return "verticalScale: integer expected";
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      if (!$util.isInteger(message.rotation))
        return "rotation: integer expected";
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      if (!$util.isInteger(message.camId)) return "camId: integer expected";
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    return null;
  };

  /**
   * Creates a ReqGetAstroShootingTime message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetAstroShootingTime} ReqGetAstroShootingTime
   */
  ReqGetAstroShootingTime.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetAstroShootingTime) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetAstroShootingTime: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetAstroShootingTime();
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.horizontalScale != null)
      message.horizontalScale = object.horizontalScale | 0;
    if (object.verticalScale != null)
      message.verticalScale = object.verticalScale | 0;
    if (object.rotation != null) message.rotation = object.rotation | 0;
    if (object.camId != null) message.camId = object.camId | 0;
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqGetAstroShootingTime message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {ReqGetAstroShootingTime} message ReqGetAstroShootingTime
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetAstroShootingTime.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expIndex = 0;
      object.horizontalScale = 0;
      object.verticalScale = 0;
      object.rotation = 0;
      object.camId = 0;
      object.shootingMode = 0;
    }
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      object.horizontalScale = message.horizontalScale;
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      object.verticalScale = message.verticalScale;
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      object.rotation = message.rotation;
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      object.camId = message.camId;
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    return object;
  };

  /**
   * Converts this ReqGetAstroShootingTime to JSON.
   * @function toJSON
   * @memberof ReqGetAstroShootingTime
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetAstroShootingTime.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetAstroShootingTime
   * @function getTypeUrl
   * @memberof ReqGetAstroShootingTime
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetAstroShootingTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetAstroShootingTime";
  };

  return ReqGetAstroShootingTime;
})();

$root.ResGetAstroShootingTime = (function () {
  /**
   * Properties of a ResGetAstroShootingTime.
   * @exports IResGetAstroShootingTime
   * @interface IResGetAstroShootingTime
   * @property {number|null} [code] ResGetAstroShootingTime code
   * @property {number|null} [shootingTime] ResGetAstroShootingTime shootingTime
   * @property {number|null} [camId] ResGetAstroShootingTime camId
   * @property {ResGetAstroShootingTime.AstroMode|null} [astroMode] ResGetAstroShootingTime astroMode
   * @property {number|null} [shootingMode] ResGetAstroShootingTime shootingMode
   */

  /**
   * Constructs a new ResGetAstroShootingTime.
   * @exports ResGetAstroShootingTime
   * @classdesc Represents a ResGetAstroShootingTime.
   * @implements IResGetAstroShootingTime
   * @constructor
   * @param {IResGetAstroShootingTime=} [properties] Properties to set
   */
  function ResGetAstroShootingTime(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetAstroShootingTime code.
   * @member {number} code
   * @memberof ResGetAstroShootingTime
   * @instance
   */
  ResGetAstroShootingTime.prototype.code = 0;

  /**
   * ResGetAstroShootingTime shootingTime.
   * @member {number} shootingTime
   * @memberof ResGetAstroShootingTime
   * @instance
   */
  ResGetAstroShootingTime.prototype.shootingTime = 0;

  /**
   * ResGetAstroShootingTime camId.
   * @member {number} camId
   * @memberof ResGetAstroShootingTime
   * @instance
   */
  ResGetAstroShootingTime.prototype.camId = 0;

  /**
   * ResGetAstroShootingTime astroMode.
   * @member {ResGetAstroShootingTime.AstroMode} astroMode
   * @memberof ResGetAstroShootingTime
   * @instance
   */
  ResGetAstroShootingTime.prototype.astroMode = 0;

  /**
   * ResGetAstroShootingTime shootingMode.
   * @member {number} shootingMode
   * @memberof ResGetAstroShootingTime
   * @instance
   */
  ResGetAstroShootingTime.prototype.shootingMode = 0;

  /**
   * Creates a new ResGetAstroShootingTime instance using the specified properties.
   * @function create
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {IResGetAstroShootingTime=} [properties] Properties to set
   * @returns {ResGetAstroShootingTime} ResGetAstroShootingTime instance
   */
  ResGetAstroShootingTime.create = function create(properties) {
    return new ResGetAstroShootingTime(properties);
  };

  /**
   * Encodes the specified ResGetAstroShootingTime message. Does not implicitly {@link ResGetAstroShootingTime.verify|verify} messages.
   * @function encode
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {IResGetAstroShootingTime} message ResGetAstroShootingTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAstroShootingTime.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.shootingTime != null &&
      Object.hasOwnProperty.call(message, "shootingTime")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.shootingTime);
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.camId);
    if (
      message.astroMode != null &&
      Object.hasOwnProperty.call(message, "astroMode")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.astroMode);
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.shootingMode);
    return writer;
  };

  /**
   * Encodes the specified ResGetAstroShootingTime message, length delimited. Does not implicitly {@link ResGetAstroShootingTime.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {IResGetAstroShootingTime} message ResGetAstroShootingTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAstroShootingTime.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetAstroShootingTime message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetAstroShootingTime} ResGetAstroShootingTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAstroShootingTime.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetAstroShootingTime();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.shootingTime = reader.int32();
          break;
        }
        case 3: {
          message.camId = reader.int32();
          break;
        }
        case 4: {
          message.astroMode = reader.int32();
          break;
        }
        case 5: {
          message.shootingMode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetAstroShootingTime message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetAstroShootingTime} ResGetAstroShootingTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAstroShootingTime.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetAstroShootingTime message.
   * @function verify
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetAstroShootingTime.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.shootingTime != null &&
      Object.hasOwnProperty.call(message, "shootingTime")
    )
      if (!$util.isInteger(message.shootingTime))
        return "shootingTime: integer expected";
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      if (!$util.isInteger(message.camId)) return "camId: integer expected";
    if (
      message.astroMode != null &&
      Object.hasOwnProperty.call(message, "astroMode")
    )
      switch (message.astroMode) {
        default:
          return "astroMode: enum value expected";
        case 0:
        case 1:
          break;
      }
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    return null;
  };

  /**
   * Creates a ResGetAstroShootingTime message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetAstroShootingTime} ResGetAstroShootingTime
   */
  ResGetAstroShootingTime.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetAstroShootingTime) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetAstroShootingTime: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetAstroShootingTime();
    if (object.code != null) message.code = object.code | 0;
    if (object.shootingTime != null)
      message.shootingTime = object.shootingTime | 0;
    if (object.camId != null) message.camId = object.camId | 0;
    switch (object.astroMode) {
      default:
        if (typeof object.astroMode === "number") {
          message.astroMode = object.astroMode;
          break;
        }
        break;
      case "NORMAL":
      case 0:
        message.astroMode = 0;
        break;
      case "MOSAIC":
      case 1:
        message.astroMode = 1;
        break;
    }
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetAstroShootingTime message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {ResGetAstroShootingTime} message ResGetAstroShootingTime
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetAstroShootingTime.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.shootingTime = 0;
      object.camId = 0;
      object.astroMode = options.enums === String ? "NORMAL" : 0;
      object.shootingMode = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.shootingTime != null &&
      Object.hasOwnProperty.call(message, "shootingTime")
    )
      object.shootingTime = message.shootingTime;
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      object.camId = message.camId;
    if (
      message.astroMode != null &&
      Object.hasOwnProperty.call(message, "astroMode")
    )
      object.astroMode =
        options.enums === String
          ? $root.ResGetAstroShootingTime.AstroMode[message.astroMode] ===
            undefined
            ? message.astroMode
            : $root.ResGetAstroShootingTime.AstroMode[message.astroMode]
          : message.astroMode;
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    return object;
  };

  /**
   * Converts this ResGetAstroShootingTime to JSON.
   * @function toJSON
   * @memberof ResGetAstroShootingTime
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetAstroShootingTime.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetAstroShootingTime
   * @function getTypeUrl
   * @memberof ResGetAstroShootingTime
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetAstroShootingTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetAstroShootingTime";
  };

  /**
   * AstroMode enum.
   * @name ResGetAstroShootingTime.AstroMode
   * @enum {number}
   * @property {number} NORMAL=0 NORMAL value
   * @property {number} MOSAIC=1 MOSAIC value
   */
  ResGetAstroShootingTime.AstroMode = (function () {
    var valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = "NORMAL")] = 0;
    values[(valuesById[1] = "MOSAIC")] = 1;
    return values;
  })();

  return ResGetAstroShootingTime;
})();

$root.ReqGetCaliFrameList = (function () {
  /**
   * Properties of a ReqGetCaliFrameList.
   * @exports IReqGetCaliFrameList
   * @interface IReqGetCaliFrameList
   * @property {number|null} [cameraType] ReqGetCaliFrameList cameraType
   * @property {number|null} [caliFrameType] ReqGetCaliFrameList caliFrameType
   */

  /**
   * Constructs a new ReqGetCaliFrameList.
   * @exports ReqGetCaliFrameList
   * @classdesc Represents a ReqGetCaliFrameList.
   * @implements IReqGetCaliFrameList
   * @constructor
   * @param {IReqGetCaliFrameList=} [properties] Properties to set
   */
  function ReqGetCaliFrameList(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetCaliFrameList cameraType.
   * @member {number} cameraType
   * @memberof ReqGetCaliFrameList
   * @instance
   */
  ReqGetCaliFrameList.prototype.cameraType = 0;

  /**
   * ReqGetCaliFrameList caliFrameType.
   * @member {number} caliFrameType
   * @memberof ReqGetCaliFrameList
   * @instance
   */
  ReqGetCaliFrameList.prototype.caliFrameType = 0;

  /**
   * Creates a new ReqGetCaliFrameList instance using the specified properties.
   * @function create
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {IReqGetCaliFrameList=} [properties] Properties to set
   * @returns {ReqGetCaliFrameList} ReqGetCaliFrameList instance
   */
  ReqGetCaliFrameList.create = function create(properties) {
    return new ReqGetCaliFrameList(properties);
  };

  /**
   * Encodes the specified ReqGetCaliFrameList message. Does not implicitly {@link ReqGetCaliFrameList.verify|verify} messages.
   * @function encode
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {IReqGetCaliFrameList} message ReqGetCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetCaliFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cameraType);
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.caliFrameType);
    return writer;
  };

  /**
   * Encodes the specified ReqGetCaliFrameList message, length delimited. Does not implicitly {@link ReqGetCaliFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {IReqGetCaliFrameList} message ReqGetCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetCaliFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetCaliFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetCaliFrameList} ReqGetCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetCaliFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetCaliFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cameraType = reader.int32();
          break;
        }
        case 2: {
          message.caliFrameType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetCaliFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetCaliFrameList} ReqGetCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetCaliFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetCaliFrameList message.
   * @function verify
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetCaliFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      if (!$util.isInteger(message.caliFrameType))
        return "caliFrameType: integer expected";
    return null;
  };

  /**
   * Creates a ReqGetCaliFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetCaliFrameList} ReqGetCaliFrameList
   */
  ReqGetCaliFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetCaliFrameList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetCaliFrameList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetCaliFrameList();
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.caliFrameType != null)
      message.caliFrameType = object.caliFrameType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqGetCaliFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {ReqGetCaliFrameList} message ReqGetCaliFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetCaliFrameList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cameraType = 0;
      object.caliFrameType = 0;
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      object.caliFrameType = message.caliFrameType;
    return object;
  };

  /**
   * Converts this ReqGetCaliFrameList to JSON.
   * @function toJSON
   * @memberof ReqGetCaliFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetCaliFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetCaliFrameList
   * @function getTypeUrl
   * @memberof ReqGetCaliFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetCaliFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetCaliFrameList";
  };

  return ReqGetCaliFrameList;
})();

$root.CaliFrameInfo = (function () {
  /**
   * Properties of a CaliFrameInfo.
   * @exports ICaliFrameInfo
   * @interface ICaliFrameInfo
   * @property {string|null} [expName] CaliFrameInfo expName
   * @property {number|null} [gain] CaliFrameInfo gain
   * @property {number|null} [resolution] CaliFrameInfo resolution
   * @property {number|null} [cameraType] CaliFrameInfo cameraType
   * @property {number|null} [progress] CaliFrameInfo progress
   * @property {number|null} [caliFrameType] CaliFrameInfo caliFrameType
   * @property {number|null} [filterType] CaliFrameInfo filterType
   * @property {number|null} [infoId] CaliFrameInfo infoId
   * @property {number|null} [tempValue] CaliFrameInfo tempValue
   */

  /**
   * Constructs a new CaliFrameInfo.
   * @exports CaliFrameInfo
   * @classdesc Represents a CaliFrameInfo.
   * @implements ICaliFrameInfo
   * @constructor
   * @param {ICaliFrameInfo=} [properties] Properties to set
   */
  function CaliFrameInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * CaliFrameInfo expName.
   * @member {string} expName
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.expName = "";

  /**
   * CaliFrameInfo gain.
   * @member {number} gain
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.gain = 0;

  /**
   * CaliFrameInfo resolution.
   * @member {number} resolution
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.resolution = 0;

  /**
   * CaliFrameInfo cameraType.
   * @member {number} cameraType
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.cameraType = 0;

  /**
   * CaliFrameInfo progress.
   * @member {number} progress
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.progress = 0;

  /**
   * CaliFrameInfo caliFrameType.
   * @member {number} caliFrameType
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.caliFrameType = 0;

  /**
   * CaliFrameInfo filterType.
   * @member {number|null|undefined} filterType
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.filterType = null;

  /**
   * CaliFrameInfo infoId.
   * @member {number|null|undefined} infoId
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.infoId = null;

  /**
   * CaliFrameInfo tempValue.
   * @member {number|null|undefined} tempValue
   * @memberof CaliFrameInfo
   * @instance
   */
  CaliFrameInfo.prototype.tempValue = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(CaliFrameInfo.prototype, "_filterType", {
    get: $util.oneOfGetter(($oneOfFields = ["filterType"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(CaliFrameInfo.prototype, "_infoId", {
    get: $util.oneOfGetter(($oneOfFields = ["infoId"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(CaliFrameInfo.prototype, "_tempValue", {
    get: $util.oneOfGetter(($oneOfFields = ["tempValue"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new CaliFrameInfo instance using the specified properties.
   * @function create
   * @memberof CaliFrameInfo
   * @static
   * @param {ICaliFrameInfo=} [properties] Properties to set
   * @returns {CaliFrameInfo} CaliFrameInfo instance
   */
  CaliFrameInfo.create = function create(properties) {
    return new CaliFrameInfo(properties);
  };

  /**
   * Encodes the specified CaliFrameInfo message. Does not implicitly {@link CaliFrameInfo.verify|verify} messages.
   * @function encode
   * @memberof CaliFrameInfo
   * @static
   * @param {ICaliFrameInfo} message CaliFrameInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CaliFrameInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.expName);
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.gain);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.resolution);
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.cameraType);
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.progress);
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.caliFrameType);
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.filterType);
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.infoId);
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.tempValue);
    return writer;
  };

  /**
   * Encodes the specified CaliFrameInfo message, length delimited. Does not implicitly {@link CaliFrameInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof CaliFrameInfo
   * @static
   * @param {ICaliFrameInfo} message CaliFrameInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CaliFrameInfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a CaliFrameInfo message from the specified reader or buffer.
   * @function decode
   * @memberof CaliFrameInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {CaliFrameInfo} CaliFrameInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CaliFrameInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.CaliFrameInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expName = reader.string();
          break;
        }
        case 2: {
          message.gain = reader.int32();
          break;
        }
        case 3: {
          message.resolution = reader.int32();
          break;
        }
        case 4: {
          message.cameraType = reader.int32();
          break;
        }
        case 5: {
          message.progress = reader.int32();
          break;
        }
        case 6: {
          message.caliFrameType = reader.int32();
          break;
        }
        case 7: {
          message.filterType = reader.int32();
          break;
        }
        case 8: {
          message.infoId = reader.int32();
          break;
        }
        case 9: {
          message.tempValue = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a CaliFrameInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof CaliFrameInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {CaliFrameInfo} CaliFrameInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CaliFrameInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a CaliFrameInfo message.
   * @function verify
   * @memberof CaliFrameInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  CaliFrameInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      if (!$util.isString(message.expName)) return "expName: string expected";
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      if (!$util.isInteger(message.gain)) return "gain: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      if (!$util.isInteger(message.progress))
        return "progress: integer expected";
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      if (!$util.isInteger(message.caliFrameType))
        return "caliFrameType: integer expected";
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      properties._filterType = 1;
      if (!$util.isInteger(message.filterType))
        return "filterType: integer expected";
    }
    if (
      message.infoId != null &&
      Object.hasOwnProperty.call(message, "infoId")
    ) {
      properties._infoId = 1;
      if (!$util.isInteger(message.infoId)) return "infoId: integer expected";
    }
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    ) {
      properties._tempValue = 1;
      if (!$util.isInteger(message.tempValue))
        return "tempValue: integer expected";
    }
    return null;
  };

  /**
   * Creates a CaliFrameInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof CaliFrameInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {CaliFrameInfo} CaliFrameInfo
   */
  CaliFrameInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.CaliFrameInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".CaliFrameInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.CaliFrameInfo();
    if (object.expName != null) message.expName = String(object.expName);
    if (object.gain != null) message.gain = object.gain | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.progress != null) message.progress = object.progress | 0;
    if (object.caliFrameType != null)
      message.caliFrameType = object.caliFrameType | 0;
    if (object.filterType != null) message.filterType = object.filterType | 0;
    if (object.infoId != null) message.infoId = object.infoId | 0;
    if (object.tempValue != null) message.tempValue = object.tempValue | 0;
    return message;
  };

  /**
   * Creates a plain object from a CaliFrameInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof CaliFrameInfo
   * @static
   * @param {CaliFrameInfo} message CaliFrameInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  CaliFrameInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expName = "";
      object.gain = 0;
      object.resolution = 0;
      object.cameraType = 0;
      object.progress = 0;
      object.caliFrameType = 0;
    }
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      object.expName = message.expName;
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      object.gain = message.gain;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.progress != null &&
      Object.hasOwnProperty.call(message, "progress")
    )
      object.progress = message.progress;
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      object.caliFrameType = message.caliFrameType;
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      object.filterType = message.filterType;
      if (options.oneofs) object._filterType = "filterType";
    }
    if (
      message.infoId != null &&
      Object.hasOwnProperty.call(message, "infoId")
    ) {
      object.infoId = message.infoId;
      if (options.oneofs) object._infoId = "infoId";
    }
    if (
      message.tempValue != null &&
      Object.hasOwnProperty.call(message, "tempValue")
    ) {
      object.tempValue = message.tempValue;
      if (options.oneofs) object._tempValue = "tempValue";
    }
    return object;
  };

  /**
   * Converts this CaliFrameInfo to JSON.
   * @function toJSON
   * @memberof CaliFrameInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  CaliFrameInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for CaliFrameInfo
   * @function getTypeUrl
   * @memberof CaliFrameInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  CaliFrameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/CaliFrameInfo";
  };

  return CaliFrameInfo;
})();

$root.ResGetCaliFrameList = (function () {
  /**
   * Properties of a ResGetCaliFrameList.
   * @exports IResGetCaliFrameList
   * @interface IResGetCaliFrameList
   * @property {number|null} [code] ResGetCaliFrameList code
   * @property {Array.<ICaliFrameInfo>|null} [list] ResGetCaliFrameList list
   * @property {number|null} [cameraType] ResGetCaliFrameList cameraType
   * @property {number|null} [caliFrameType] ResGetCaliFrameList caliFrameType
   */

  /**
   * Constructs a new ResGetCaliFrameList.
   * @exports ResGetCaliFrameList
   * @classdesc Represents a ResGetCaliFrameList.
   * @implements IResGetCaliFrameList
   * @constructor
   * @param {IResGetCaliFrameList=} [properties] Properties to set
   */
  function ResGetCaliFrameList(properties) {
    this.list = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetCaliFrameList code.
   * @member {number} code
   * @memberof ResGetCaliFrameList
   * @instance
   */
  ResGetCaliFrameList.prototype.code = 0;

  /**
   * ResGetCaliFrameList list.
   * @member {Array.<ICaliFrameInfo>} list
   * @memberof ResGetCaliFrameList
   * @instance
   */
  ResGetCaliFrameList.prototype.list = $util.emptyArray;

  /**
   * ResGetCaliFrameList cameraType.
   * @member {number} cameraType
   * @memberof ResGetCaliFrameList
   * @instance
   */
  ResGetCaliFrameList.prototype.cameraType = 0;

  /**
   * ResGetCaliFrameList caliFrameType.
   * @member {number} caliFrameType
   * @memberof ResGetCaliFrameList
   * @instance
   */
  ResGetCaliFrameList.prototype.caliFrameType = 0;

  /**
   * Creates a new ResGetCaliFrameList instance using the specified properties.
   * @function create
   * @memberof ResGetCaliFrameList
   * @static
   * @param {IResGetCaliFrameList=} [properties] Properties to set
   * @returns {ResGetCaliFrameList} ResGetCaliFrameList instance
   */
  ResGetCaliFrameList.create = function create(properties) {
    return new ResGetCaliFrameList(properties);
  };

  /**
   * Encodes the specified ResGetCaliFrameList message. Does not implicitly {@link ResGetCaliFrameList.verify|verify} messages.
   * @function encode
   * @memberof ResGetCaliFrameList
   * @static
   * @param {IResGetCaliFrameList} message ResGetCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetCaliFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.list != null && message.list.length)
      for (var i = 0; i < message.list.length; ++i)
        $root.CaliFrameInfo.encode(
          message.list[i],
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.cameraType);
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.caliFrameType);
    return writer;
  };

  /**
   * Encodes the specified ResGetCaliFrameList message, length delimited. Does not implicitly {@link ResGetCaliFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetCaliFrameList
   * @static
   * @param {IResGetCaliFrameList} message ResGetCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetCaliFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetCaliFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetCaliFrameList} ResGetCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetCaliFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetCaliFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          if (!(message.list && message.list.length)) message.list = [];
          message.list.push(
            $root.CaliFrameInfo.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 3: {
          message.cameraType = reader.int32();
          break;
        }
        case 4: {
          message.caliFrameType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetCaliFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetCaliFrameList} ResGetCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetCaliFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetCaliFrameList message.
   * @function verify
   * @memberof ResGetCaliFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetCaliFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.list != null && Object.hasOwnProperty.call(message, "list")) {
      if (!Array.isArray(message.list)) return "list: array expected";
      for (var i = 0; i < message.list.length; ++i) {
        var error = $root.CaliFrameInfo.verify(message.list[i], long + 1);
        if (error) return "list." + error;
      }
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      if (!$util.isInteger(message.caliFrameType))
        return "caliFrameType: integer expected";
    return null;
  };

  /**
   * Creates a ResGetCaliFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetCaliFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetCaliFrameList} ResGetCaliFrameList
   */
  ResGetCaliFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetCaliFrameList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetCaliFrameList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetCaliFrameList();
    if (object.code != null) message.code = object.code | 0;
    if (object.list) {
      if (!Array.isArray(object.list))
        throw TypeError(".ResGetCaliFrameList.list: array expected");
      message.list = [];
      for (var i = 0; i < object.list.length; ++i) {
        if (!$util.isObject(object.list[i]))
          throw TypeError(".ResGetCaliFrameList.list: object expected");
        message.list[i] = $root.CaliFrameInfo.fromObject(
          object.list[i],
          long + 1,
        );
      }
    }
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.caliFrameType != null)
      message.caliFrameType = object.caliFrameType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetCaliFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetCaliFrameList
   * @static
   * @param {ResGetCaliFrameList} message ResGetCaliFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetCaliFrameList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.list = [];
    if (options.defaults) {
      object.code = 0;
      object.cameraType = 0;
      object.caliFrameType = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.list && message.list.length) {
      object.list = [];
      for (var j = 0; j < message.list.length; ++j)
        object.list[j] = $root.CaliFrameInfo.toObject(
          message.list[j],
          options,
          q + 1,
        );
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      object.caliFrameType = message.caliFrameType;
    return object;
  };

  /**
   * Converts this ResGetCaliFrameList to JSON.
   * @function toJSON
   * @memberof ResGetCaliFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetCaliFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetCaliFrameList
   * @function getTypeUrl
   * @memberof ResGetCaliFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetCaliFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetCaliFrameList";
  };

  return ResGetCaliFrameList;
})();

$root.ReqDelCaliFrameList = (function () {
  /**
   * Properties of a ReqDelCaliFrameList.
   * @exports IReqDelCaliFrameList
   * @interface IReqDelCaliFrameList
   * @property {Array.<number>|null} [infoIds] ReqDelCaliFrameList infoIds
   */

  /**
   * Constructs a new ReqDelCaliFrameList.
   * @exports ReqDelCaliFrameList
   * @classdesc Represents a ReqDelCaliFrameList.
   * @implements IReqDelCaliFrameList
   * @constructor
   * @param {IReqDelCaliFrameList=} [properties] Properties to set
   */
  function ReqDelCaliFrameList(properties) {
    this.infoIds = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqDelCaliFrameList infoIds.
   * @member {Array.<number>} infoIds
   * @memberof ReqDelCaliFrameList
   * @instance
   */
  ReqDelCaliFrameList.prototype.infoIds = $util.emptyArray;

  /**
   * Creates a new ReqDelCaliFrameList instance using the specified properties.
   * @function create
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {IReqDelCaliFrameList=} [properties] Properties to set
   * @returns {ReqDelCaliFrameList} ReqDelCaliFrameList instance
   */
  ReqDelCaliFrameList.create = function create(properties) {
    return new ReqDelCaliFrameList(properties);
  };

  /**
   * Encodes the specified ReqDelCaliFrameList message. Does not implicitly {@link ReqDelCaliFrameList.verify|verify} messages.
   * @function encode
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {IReqDelCaliFrameList} message ReqDelCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelCaliFrameList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.infoIds != null && message.infoIds.length) {
      writer.uint32(/* id 1, wireType 2 =*/ 10).fork();
      for (var i = 0; i < message.infoIds.length; ++i)
        writer.int32(message.infoIds[i]);
      writer.ldelim();
    }
    return writer;
  };

  /**
   * Encodes the specified ReqDelCaliFrameList message, length delimited. Does not implicitly {@link ReqDelCaliFrameList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {IReqDelCaliFrameList} message ReqDelCaliFrameList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDelCaliFrameList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDelCaliFrameList message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDelCaliFrameList} ReqDelCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelCaliFrameList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDelCaliFrameList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.infoIds && message.infoIds.length))
            message.infoIds = [];
          if ((tag & 7) === 2) {
            var end2 = reader.uint32() + reader.pos;
            if (end2 > reader.len) throw RangeError("index out of range");
            reader.len = end2;
            while (reader.pos < end2) message.infoIds.push(reader.int32());
            if (reader.pos !== end2) throw RangeError("index out of range");
            reader.len = end;
          } else message.infoIds.push(reader.int32());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDelCaliFrameList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDelCaliFrameList} ReqDelCaliFrameList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDelCaliFrameList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDelCaliFrameList message.
   * @function verify
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDelCaliFrameList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.infoIds != null &&
      Object.hasOwnProperty.call(message, "infoIds")
    ) {
      if (!Array.isArray(message.infoIds)) return "infoIds: array expected";
      for (var i = 0; i < message.infoIds.length; ++i)
        if (!$util.isInteger(message.infoIds[i]))
          return "infoIds: integer[] expected";
    }
    return null;
  };

  /**
   * Creates a ReqDelCaliFrameList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDelCaliFrameList} ReqDelCaliFrameList
   */
  ReqDelCaliFrameList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDelCaliFrameList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqDelCaliFrameList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqDelCaliFrameList();
    if (object.infoIds) {
      if (!Array.isArray(object.infoIds))
        throw TypeError(".ReqDelCaliFrameList.infoIds: array expected");
      message.infoIds = [];
      for (var i = 0; i < object.infoIds.length; ++i)
        message.infoIds[i] = object.infoIds[i] | 0;
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqDelCaliFrameList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {ReqDelCaliFrameList} message ReqDelCaliFrameList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDelCaliFrameList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.infoIds = [];
    if (message.infoIds && message.infoIds.length) {
      object.infoIds = [];
      for (var j = 0; j < message.infoIds.length; ++j)
        object.infoIds[j] = message.infoIds[j];
    }
    return object;
  };

  /**
   * Converts this ReqDelCaliFrameList to JSON.
   * @function toJSON
   * @memberof ReqDelCaliFrameList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDelCaliFrameList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDelCaliFrameList
   * @function getTypeUrl
   * @memberof ReqDelCaliFrameList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDelCaliFrameList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDelCaliFrameList";
  };

  return ReqDelCaliFrameList;
})();

$root.ReqCaptureCaliFrame = (function () {
  /**
   * Properties of a ReqCaptureCaliFrame.
   * @exports IReqCaptureCaliFrame
   * @interface IReqCaptureCaliFrame
   * @property {number|null} [expIndex] ReqCaptureCaliFrame expIndex
   * @property {number|null} [gain] ReqCaptureCaliFrame gain
   * @property {number|null} [resolution] ReqCaptureCaliFrame resolution
   * @property {number|null} [capSize] ReqCaptureCaliFrame capSize
   * @property {number|null} [cameraType] ReqCaptureCaliFrame cameraType
   * @property {number|null} [caliFrameType] ReqCaptureCaliFrame caliFrameType
   * @property {number|null} [filterType] ReqCaptureCaliFrame filterType
   * @property {number|null} [sceneType] ReqCaptureCaliFrame sceneType
   */

  /**
   * Constructs a new ReqCaptureCaliFrame.
   * @exports ReqCaptureCaliFrame
   * @classdesc Represents a ReqCaptureCaliFrame.
   * @implements IReqCaptureCaliFrame
   * @constructor
   * @param {IReqCaptureCaliFrame=} [properties] Properties to set
   */
  function ReqCaptureCaliFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCaptureCaliFrame expIndex.
   * @member {number} expIndex
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.expIndex = 0;

  /**
   * ReqCaptureCaliFrame gain.
   * @member {number} gain
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.gain = 0;

  /**
   * ReqCaptureCaliFrame resolution.
   * @member {number} resolution
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.resolution = 0;

  /**
   * ReqCaptureCaliFrame capSize.
   * @member {number} capSize
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.capSize = 0;

  /**
   * ReqCaptureCaliFrame cameraType.
   * @member {number} cameraType
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.cameraType = 0;

  /**
   * ReqCaptureCaliFrame caliFrameType.
   * @member {number} caliFrameType
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.caliFrameType = 0;

  /**
   * ReqCaptureCaliFrame filterType.
   * @member {number|null|undefined} filterType
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.filterType = null;

  /**
   * ReqCaptureCaliFrame sceneType.
   * @member {number} sceneType
   * @memberof ReqCaptureCaliFrame
   * @instance
   */
  ReqCaptureCaliFrame.prototype.sceneType = 0;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqCaptureCaliFrame.prototype, "_filterType", {
    get: $util.oneOfGetter(($oneOfFields = ["filterType"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqCaptureCaliFrame instance using the specified properties.
   * @function create
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {IReqCaptureCaliFrame=} [properties] Properties to set
   * @returns {ReqCaptureCaliFrame} ReqCaptureCaliFrame instance
   */
  ReqCaptureCaliFrame.create = function create(properties) {
    return new ReqCaptureCaliFrame(properties);
  };

  /**
   * Encodes the specified ReqCaptureCaliFrame message. Does not implicitly {@link ReqCaptureCaliFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {IReqCaptureCaliFrame} message ReqCaptureCaliFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureCaliFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expIndex);
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.gain);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.resolution);
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.capSize);
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.cameraType);
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.caliFrameType);
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.filterType);
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.sceneType);
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureCaliFrame message, length delimited. Does not implicitly {@link ReqCaptureCaliFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {IReqCaptureCaliFrame} message ReqCaptureCaliFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureCaliFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureCaliFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureCaliFrame} ReqCaptureCaliFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureCaliFrame.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureCaliFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expIndex = reader.int32();
          break;
        }
        case 2: {
          message.gain = reader.int32();
          break;
        }
        case 3: {
          message.resolution = reader.int32();
          break;
        }
        case 4: {
          message.capSize = reader.int32();
          break;
        }
        case 5: {
          message.cameraType = reader.int32();
          break;
        }
        case 6: {
          message.caliFrameType = reader.int32();
          break;
        }
        case 7: {
          message.filterType = reader.int32();
          break;
        }
        case 8: {
          message.sceneType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureCaliFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureCaliFrame} ReqCaptureCaliFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureCaliFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureCaliFrame message.
   * @function verify
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureCaliFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      if (!$util.isInteger(message.gain)) return "gain: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      if (!$util.isInteger(message.capSize)) return "capSize: integer expected";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      if (!$util.isInteger(message.caliFrameType))
        return "caliFrameType: integer expected";
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      properties._filterType = 1;
      if (!$util.isInteger(message.filterType))
        return "filterType: integer expected";
    }
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      if (!$util.isInteger(message.sceneType))
        return "sceneType: integer expected";
    return null;
  };

  /**
   * Creates a ReqCaptureCaliFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureCaliFrame} ReqCaptureCaliFrame
   */
  ReqCaptureCaliFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureCaliFrame) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCaptureCaliFrame: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCaptureCaliFrame();
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gain != null) message.gain = object.gain | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    if (object.capSize != null) message.capSize = object.capSize | 0;
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.caliFrameType != null)
      message.caliFrameType = object.caliFrameType | 0;
    if (object.filterType != null) message.filterType = object.filterType | 0;
    if (object.sceneType != null) message.sceneType = object.sceneType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqCaptureCaliFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {ReqCaptureCaliFrame} message ReqCaptureCaliFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureCaliFrame.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expIndex = 0;
      object.gain = 0;
      object.resolution = 0;
      object.capSize = 0;
      object.cameraType = 0;
      object.caliFrameType = 0;
      object.sceneType = 0;
    }
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      object.gain = message.gain;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      object.capSize = message.capSize;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      object.caliFrameType = message.caliFrameType;
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      object.filterType = message.filterType;
      if (options.oneofs) object._filterType = "filterType";
    }
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      object.sceneType = message.sceneType;
    return object;
  };

  /**
   * Converts this ReqCaptureCaliFrame to JSON.
   * @function toJSON
   * @memberof ReqCaptureCaliFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureCaliFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureCaliFrame
   * @function getTypeUrl
   * @memberof ReqCaptureCaliFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureCaliFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureCaliFrame";
  };

  return ReqCaptureCaliFrame;
})();

$root.ReqStopCaptureCaliFrame = (function () {
  /**
   * Properties of a ReqStopCaptureCaliFrame.
   * @exports IReqStopCaptureCaliFrame
   * @interface IReqStopCaptureCaliFrame
   * @property {number|null} [cameraType] ReqStopCaptureCaliFrame cameraType
   */

  /**
   * Constructs a new ReqStopCaptureCaliFrame.
   * @exports ReqStopCaptureCaliFrame
   * @classdesc Represents a ReqStopCaptureCaliFrame.
   * @implements IReqStopCaptureCaliFrame
   * @constructor
   * @param {IReqStopCaptureCaliFrame=} [properties] Properties to set
   */
  function ReqStopCaptureCaliFrame(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStopCaptureCaliFrame cameraType.
   * @member {number} cameraType
   * @memberof ReqStopCaptureCaliFrame
   * @instance
   */
  ReqStopCaptureCaliFrame.prototype.cameraType = 0;

  /**
   * Creates a new ReqStopCaptureCaliFrame instance using the specified properties.
   * @function create
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {IReqStopCaptureCaliFrame=} [properties] Properties to set
   * @returns {ReqStopCaptureCaliFrame} ReqStopCaptureCaliFrame instance
   */
  ReqStopCaptureCaliFrame.create = function create(properties) {
    return new ReqStopCaptureCaliFrame(properties);
  };

  /**
   * Encodes the specified ReqStopCaptureCaliFrame message. Does not implicitly {@link ReqStopCaptureCaliFrame.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {IReqStopCaptureCaliFrame} message ReqStopCaptureCaliFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureCaliFrame.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cameraType);
    return writer;
  };

  /**
   * Encodes the specified ReqStopCaptureCaliFrame message, length delimited. Does not implicitly {@link ReqStopCaptureCaliFrame.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {IReqStopCaptureCaliFrame} message ReqStopCaptureCaliFrame message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCaptureCaliFrame.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCaptureCaliFrame message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCaptureCaliFrame} ReqStopCaptureCaliFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureCaliFrame.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCaptureCaliFrame();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cameraType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCaptureCaliFrame message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCaptureCaliFrame} ReqStopCaptureCaliFrame
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCaptureCaliFrame.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCaptureCaliFrame message.
   * @function verify
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCaptureCaliFrame.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    return null;
  };

  /**
   * Creates a ReqStopCaptureCaliFrame message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCaptureCaliFrame} ReqStopCaptureCaliFrame
   */
  ReqStopCaptureCaliFrame.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopCaptureCaliFrame) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStopCaptureCaliFrame: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStopCaptureCaliFrame();
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStopCaptureCaliFrame message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {ReqStopCaptureCaliFrame} message ReqStopCaptureCaliFrame
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCaptureCaliFrame.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.cameraType = 0;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    return object;
  };

  /**
   * Converts this ReqStopCaptureCaliFrame to JSON.
   * @function toJSON
   * @memberof ReqStopCaptureCaliFrame
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCaptureCaliFrame.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCaptureCaliFrame
   * @function getTypeUrl
   * @memberof ReqStopCaptureCaliFrame
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCaptureCaliFrame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCaptureCaliFrame";
  };

  return ReqStopCaptureCaliFrame;
})();

$root.CaptureCaliFrameTaskParam = (function () {
  /**
   * Properties of a CaptureCaliFrameTaskParam.
   * @exports ICaptureCaliFrameTaskParam
   * @interface ICaptureCaliFrameTaskParam
   * @property {number|null} [cameraType] CaptureCaliFrameTaskParam cameraType
   * @property {number|null} [caliFrameType] CaptureCaliFrameTaskParam caliFrameType
   */

  /**
   * Constructs a new CaptureCaliFrameTaskParam.
   * @exports CaptureCaliFrameTaskParam
   * @classdesc Represents a CaptureCaliFrameTaskParam.
   * @implements ICaptureCaliFrameTaskParam
   * @constructor
   * @param {ICaptureCaliFrameTaskParam=} [properties] Properties to set
   */
  function CaptureCaliFrameTaskParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * CaptureCaliFrameTaskParam cameraType.
   * @member {number} cameraType
   * @memberof CaptureCaliFrameTaskParam
   * @instance
   */
  CaptureCaliFrameTaskParam.prototype.cameraType = 0;

  /**
   * CaptureCaliFrameTaskParam caliFrameType.
   * @member {number} caliFrameType
   * @memberof CaptureCaliFrameTaskParam
   * @instance
   */
  CaptureCaliFrameTaskParam.prototype.caliFrameType = 0;

  /**
   * Creates a new CaptureCaliFrameTaskParam instance using the specified properties.
   * @function create
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {ICaptureCaliFrameTaskParam=} [properties] Properties to set
   * @returns {CaptureCaliFrameTaskParam} CaptureCaliFrameTaskParam instance
   */
  CaptureCaliFrameTaskParam.create = function create(properties) {
    return new CaptureCaliFrameTaskParam(properties);
  };

  /**
   * Encodes the specified CaptureCaliFrameTaskParam message. Does not implicitly {@link CaptureCaliFrameTaskParam.verify|verify} messages.
   * @function encode
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {ICaptureCaliFrameTaskParam} message CaptureCaliFrameTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CaptureCaliFrameTaskParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cameraType);
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.caliFrameType);
    return writer;
  };

  /**
   * Encodes the specified CaptureCaliFrameTaskParam message, length delimited. Does not implicitly {@link CaptureCaliFrameTaskParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {ICaptureCaliFrameTaskParam} message CaptureCaliFrameTaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CaptureCaliFrameTaskParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a CaptureCaliFrameTaskParam message from the specified reader or buffer.
   * @function decode
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {CaptureCaliFrameTaskParam} CaptureCaliFrameTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CaptureCaliFrameTaskParam.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.CaptureCaliFrameTaskParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cameraType = reader.int32();
          break;
        }
        case 2: {
          message.caliFrameType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a CaptureCaliFrameTaskParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {CaptureCaliFrameTaskParam} CaptureCaliFrameTaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CaptureCaliFrameTaskParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a CaptureCaliFrameTaskParam message.
   * @function verify
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  CaptureCaliFrameTaskParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      if (!$util.isInteger(message.caliFrameType))
        return "caliFrameType: integer expected";
    return null;
  };

  /**
   * Creates a CaptureCaliFrameTaskParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {CaptureCaliFrameTaskParam} CaptureCaliFrameTaskParam
   */
  CaptureCaliFrameTaskParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.CaptureCaliFrameTaskParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".CaptureCaliFrameTaskParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.CaptureCaliFrameTaskParam();
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.caliFrameType != null)
      message.caliFrameType = object.caliFrameType | 0;
    return message;
  };

  /**
   * Creates a plain object from a CaptureCaliFrameTaskParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {CaptureCaliFrameTaskParam} message CaptureCaliFrameTaskParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  CaptureCaliFrameTaskParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cameraType = 0;
      object.caliFrameType = 0;
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.caliFrameType != null &&
      Object.hasOwnProperty.call(message, "caliFrameType")
    )
      object.caliFrameType = message.caliFrameType;
    return object;
  };

  /**
   * Converts this CaptureCaliFrameTaskParam to JSON.
   * @function toJSON
   * @memberof CaptureCaliFrameTaskParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  CaptureCaliFrameTaskParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for CaptureCaliFrameTaskParam
   * @function getTypeUrl
   * @memberof CaptureCaliFrameTaskParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  CaptureCaliFrameTaskParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/CaptureCaliFrameTaskParam";
  };

  return CaptureCaliFrameTaskParam;
})();

$root.ReqGetQuickSetList = (function () {
  /**
   * Properties of a ReqGetQuickSetList.
   * @exports IReqGetQuickSetList
   * @interface IReqGetQuickSetList
   * @property {number|null} [cameraType] ReqGetQuickSetList cameraType
   */

  /**
   * Constructs a new ReqGetQuickSetList.
   * @exports ReqGetQuickSetList
   * @classdesc Represents a ReqGetQuickSetList.
   * @implements IReqGetQuickSetList
   * @constructor
   * @param {IReqGetQuickSetList=} [properties] Properties to set
   */
  function ReqGetQuickSetList(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetQuickSetList cameraType.
   * @member {number} cameraType
   * @memberof ReqGetQuickSetList
   * @instance
   */
  ReqGetQuickSetList.prototype.cameraType = 0;

  /**
   * Creates a new ReqGetQuickSetList instance using the specified properties.
   * @function create
   * @memberof ReqGetQuickSetList
   * @static
   * @param {IReqGetQuickSetList=} [properties] Properties to set
   * @returns {ReqGetQuickSetList} ReqGetQuickSetList instance
   */
  ReqGetQuickSetList.create = function create(properties) {
    return new ReqGetQuickSetList(properties);
  };

  /**
   * Encodes the specified ReqGetQuickSetList message. Does not implicitly {@link ReqGetQuickSetList.verify|verify} messages.
   * @function encode
   * @memberof ReqGetQuickSetList
   * @static
   * @param {IReqGetQuickSetList} message ReqGetQuickSetList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetQuickSetList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cameraType);
    return writer;
  };

  /**
   * Encodes the specified ReqGetQuickSetList message, length delimited. Does not implicitly {@link ReqGetQuickSetList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetQuickSetList
   * @static
   * @param {IReqGetQuickSetList} message ReqGetQuickSetList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetQuickSetList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetQuickSetList message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetQuickSetList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetQuickSetList} ReqGetQuickSetList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetQuickSetList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetQuickSetList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cameraType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetQuickSetList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetQuickSetList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetQuickSetList} ReqGetQuickSetList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetQuickSetList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetQuickSetList message.
   * @function verify
   * @memberof ReqGetQuickSetList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetQuickSetList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    return null;
  };

  /**
   * Creates a ReqGetQuickSetList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetQuickSetList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetQuickSetList} ReqGetQuickSetList
   */
  ReqGetQuickSetList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetQuickSetList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetQuickSetList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetQuickSetList();
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqGetQuickSetList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetQuickSetList
   * @static
   * @param {ReqGetQuickSetList} message ReqGetQuickSetList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetQuickSetList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.cameraType = 0;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    return object;
  };

  /**
   * Converts this ReqGetQuickSetList to JSON.
   * @function toJSON
   * @memberof ReqGetQuickSetList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetQuickSetList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetQuickSetList
   * @function getTypeUrl
   * @memberof ReqGetQuickSetList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetQuickSetList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetQuickSetList";
  };

  return ReqGetQuickSetList;
})();

$root.QuikSetInfo = (function () {
  /**
   * Properties of a QuikSetInfo.
   * @exports IQuikSetInfo
   * @interface IQuikSetInfo
   * @property {string|null} [expName] QuikSetInfo expName
   * @property {number|null} [expIndex] QuikSetInfo expIndex
   * @property {number|null} [gain] QuikSetInfo gain
   * @property {number|null} [resolution] QuikSetInfo resolution
   * @property {number|null} [cameraType] QuikSetInfo cameraType
   * @property {string|null} [infoId] QuikSetInfo infoId
   */

  /**
   * Constructs a new QuikSetInfo.
   * @exports QuikSetInfo
   * @classdesc Represents a QuikSetInfo.
   * @implements IQuikSetInfo
   * @constructor
   * @param {IQuikSetInfo=} [properties] Properties to set
   */
  function QuikSetInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * QuikSetInfo expName.
   * @member {string} expName
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.expName = "";

  /**
   * QuikSetInfo expIndex.
   * @member {number} expIndex
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.expIndex = 0;

  /**
   * QuikSetInfo gain.
   * @member {number} gain
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.gain = 0;

  /**
   * QuikSetInfo resolution.
   * @member {number} resolution
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.resolution = 0;

  /**
   * QuikSetInfo cameraType.
   * @member {number} cameraType
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.cameraType = 0;

  /**
   * QuikSetInfo infoId.
   * @member {string} infoId
   * @memberof QuikSetInfo
   * @instance
   */
  QuikSetInfo.prototype.infoId = "";

  /**
   * Creates a new QuikSetInfo instance using the specified properties.
   * @function create
   * @memberof QuikSetInfo
   * @static
   * @param {IQuikSetInfo=} [properties] Properties to set
   * @returns {QuikSetInfo} QuikSetInfo instance
   */
  QuikSetInfo.create = function create(properties) {
    return new QuikSetInfo(properties);
  };

  /**
   * Encodes the specified QuikSetInfo message. Does not implicitly {@link QuikSetInfo.verify|verify} messages.
   * @function encode
   * @memberof QuikSetInfo
   * @static
   * @param {IQuikSetInfo} message QuikSetInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  QuikSetInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.expName);
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.expIndex);
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.gain);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.resolution);
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.cameraType);
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.infoId);
    return writer;
  };

  /**
   * Encodes the specified QuikSetInfo message, length delimited. Does not implicitly {@link QuikSetInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof QuikSetInfo
   * @static
   * @param {IQuikSetInfo} message QuikSetInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  QuikSetInfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a QuikSetInfo message from the specified reader or buffer.
   * @function decode
   * @memberof QuikSetInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {QuikSetInfo} QuikSetInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  QuikSetInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.QuikSetInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expName = reader.string();
          break;
        }
        case 2: {
          message.expIndex = reader.int32();
          break;
        }
        case 3: {
          message.gain = reader.int32();
          break;
        }
        case 4: {
          message.resolution = reader.int32();
          break;
        }
        case 5: {
          message.cameraType = reader.int32();
          break;
        }
        case 6: {
          message.infoId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a QuikSetInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof QuikSetInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {QuikSetInfo} QuikSetInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  QuikSetInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a QuikSetInfo message.
   * @function verify
   * @memberof QuikSetInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  QuikSetInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      if (!$util.isString(message.expName)) return "expName: string expected";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      if (!$util.isInteger(message.gain)) return "gain: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      if (!$util.isString(message.infoId)) return "infoId: string expected";
    return null;
  };

  /**
   * Creates a QuikSetInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof QuikSetInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {QuikSetInfo} QuikSetInfo
   */
  QuikSetInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.QuikSetInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".QuikSetInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.QuikSetInfo();
    if (object.expName != null) message.expName = String(object.expName);
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gain != null) message.gain = object.gain | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.infoId != null) message.infoId = String(object.infoId);
    return message;
  };

  /**
   * Creates a plain object from a QuikSetInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof QuikSetInfo
   * @static
   * @param {QuikSetInfo} message QuikSetInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  QuikSetInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expName = "";
      object.expIndex = 0;
      object.gain = 0;
      object.resolution = 0;
      object.cameraType = 0;
      object.infoId = "";
    }
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      object.expName = message.expName;
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      object.gain = message.gain;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      object.infoId = message.infoId;
    return object;
  };

  /**
   * Converts this QuikSetInfo to JSON.
   * @function toJSON
   * @memberof QuikSetInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  QuikSetInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for QuikSetInfo
   * @function getTypeUrl
   * @memberof QuikSetInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  QuikSetInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/QuikSetInfo";
  };

  return QuikSetInfo;
})();

$root.ResGetQuickSetList = (function () {
  /**
   * Properties of a ResGetQuickSetList.
   * @exports IResGetQuickSetList
   * @interface IResGetQuickSetList
   * @property {number|null} [code] ResGetQuickSetList code
   * @property {Array.<IQuikSetInfo>|null} [quickSetList] ResGetQuickSetList quickSetList
   * @property {number|null} [cameraType] ResGetQuickSetList cameraType
   */

  /**
   * Constructs a new ResGetQuickSetList.
   * @exports ResGetQuickSetList
   * @classdesc Represents a ResGetQuickSetList.
   * @implements IResGetQuickSetList
   * @constructor
   * @param {IResGetQuickSetList=} [properties] Properties to set
   */
  function ResGetQuickSetList(properties) {
    this.quickSetList = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetQuickSetList code.
   * @member {number} code
   * @memberof ResGetQuickSetList
   * @instance
   */
  ResGetQuickSetList.prototype.code = 0;

  /**
   * ResGetQuickSetList quickSetList.
   * @member {Array.<IQuikSetInfo>} quickSetList
   * @memberof ResGetQuickSetList
   * @instance
   */
  ResGetQuickSetList.prototype.quickSetList = $util.emptyArray;

  /**
   * ResGetQuickSetList cameraType.
   * @member {number} cameraType
   * @memberof ResGetQuickSetList
   * @instance
   */
  ResGetQuickSetList.prototype.cameraType = 0;

  /**
   * Creates a new ResGetQuickSetList instance using the specified properties.
   * @function create
   * @memberof ResGetQuickSetList
   * @static
   * @param {IResGetQuickSetList=} [properties] Properties to set
   * @returns {ResGetQuickSetList} ResGetQuickSetList instance
   */
  ResGetQuickSetList.create = function create(properties) {
    return new ResGetQuickSetList(properties);
  };

  /**
   * Encodes the specified ResGetQuickSetList message. Does not implicitly {@link ResGetQuickSetList.verify|verify} messages.
   * @function encode
   * @memberof ResGetQuickSetList
   * @static
   * @param {IResGetQuickSetList} message ResGetQuickSetList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetQuickSetList.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.quickSetList != null && message.quickSetList.length)
      for (var i = 0; i < message.quickSetList.length; ++i)
        $root.QuikSetInfo.encode(
          message.quickSetList[i],
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.cameraType);
    return writer;
  };

  /**
   * Encodes the specified ResGetQuickSetList message, length delimited. Does not implicitly {@link ResGetQuickSetList.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetQuickSetList
   * @static
   * @param {IResGetQuickSetList} message ResGetQuickSetList message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetQuickSetList.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetQuickSetList message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetQuickSetList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetQuickSetList} ResGetQuickSetList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetQuickSetList.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetQuickSetList();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          if (!(message.quickSetList && message.quickSetList.length))
            message.quickSetList = [];
          message.quickSetList.push(
            $root.QuikSetInfo.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 3: {
          message.cameraType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetQuickSetList message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetQuickSetList
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetQuickSetList} ResGetQuickSetList
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetQuickSetList.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetQuickSetList message.
   * @function verify
   * @memberof ResGetQuickSetList
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetQuickSetList.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.quickSetList != null &&
      Object.hasOwnProperty.call(message, "quickSetList")
    ) {
      if (!Array.isArray(message.quickSetList))
        return "quickSetList: array expected";
      for (var i = 0; i < message.quickSetList.length; ++i) {
        var error = $root.QuikSetInfo.verify(message.quickSetList[i], long + 1);
        if (error) return "quickSetList." + error;
      }
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    return null;
  };

  /**
   * Creates a ResGetQuickSetList message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetQuickSetList
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetQuickSetList} ResGetQuickSetList
   */
  ResGetQuickSetList.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetQuickSetList) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetQuickSetList: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetQuickSetList();
    if (object.code != null) message.code = object.code | 0;
    if (object.quickSetList) {
      if (!Array.isArray(object.quickSetList))
        throw TypeError(".ResGetQuickSetList.quickSetList: array expected");
      message.quickSetList = [];
      for (var i = 0; i < object.quickSetList.length; ++i) {
        if (!$util.isObject(object.quickSetList[i]))
          throw TypeError(".ResGetQuickSetList.quickSetList: object expected");
        message.quickSetList[i] = $root.QuikSetInfo.fromObject(
          object.quickSetList[i],
          long + 1,
        );
      }
    }
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetQuickSetList message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetQuickSetList
   * @static
   * @param {ResGetQuickSetList} message ResGetQuickSetList
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetQuickSetList.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.quickSetList = [];
    if (options.defaults) {
      object.code = 0;
      object.cameraType = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.quickSetList && message.quickSetList.length) {
      object.quickSetList = [];
      for (var j = 0; j < message.quickSetList.length; ++j)
        object.quickSetList[j] = $root.QuikSetInfo.toObject(
          message.quickSetList[j],
          options,
          q + 1,
        );
    }
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    return object;
  };

  /**
   * Converts this ResGetQuickSetList to JSON.
   * @function toJSON
   * @memberof ResGetQuickSetList
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetQuickSetList.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetQuickSetList
   * @function getTypeUrl
   * @memberof ResGetQuickSetList
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetQuickSetList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetQuickSetList";
  };

  return ResGetQuickSetList;
})();

$root.ReqSetQuickSet = (function () {
  /**
   * Properties of a ReqSetQuickSet.
   * @exports IReqSetQuickSet
   * @interface IReqSetQuickSet
   * @property {string|null} [infoId] ReqSetQuickSet infoId
   */

  /**
   * Constructs a new ReqSetQuickSet.
   * @exports ReqSetQuickSet
   * @classdesc Represents a ReqSetQuickSet.
   * @implements IReqSetQuickSet
   * @constructor
   * @param {IReqSetQuickSet=} [properties] Properties to set
   */
  function ReqSetQuickSet(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetQuickSet infoId.
   * @member {string} infoId
   * @memberof ReqSetQuickSet
   * @instance
   */
  ReqSetQuickSet.prototype.infoId = "";

  /**
   * Creates a new ReqSetQuickSet instance using the specified properties.
   * @function create
   * @memberof ReqSetQuickSet
   * @static
   * @param {IReqSetQuickSet=} [properties] Properties to set
   * @returns {ReqSetQuickSet} ReqSetQuickSet instance
   */
  ReqSetQuickSet.create = function create(properties) {
    return new ReqSetQuickSet(properties);
  };

  /**
   * Encodes the specified ReqSetQuickSet message. Does not implicitly {@link ReqSetQuickSet.verify|verify} messages.
   * @function encode
   * @memberof ReqSetQuickSet
   * @static
   * @param {IReqSetQuickSet} message ReqSetQuickSet message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetQuickSet.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.infoId);
    return writer;
  };

  /**
   * Encodes the specified ReqSetQuickSet message, length delimited. Does not implicitly {@link ReqSetQuickSet.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetQuickSet
   * @static
   * @param {IReqSetQuickSet} message ReqSetQuickSet message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetQuickSet.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetQuickSet message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetQuickSet
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetQuickSet} ReqSetQuickSet
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetQuickSet.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetQuickSet();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.infoId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetQuickSet message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetQuickSet
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetQuickSet} ReqSetQuickSet
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetQuickSet.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetQuickSet message.
   * @function verify
   * @memberof ReqSetQuickSet
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetQuickSet.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      if (!$util.isString(message.infoId)) return "infoId: string expected";
    return null;
  };

  /**
   * Creates a ReqSetQuickSet message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetQuickSet
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetQuickSet} ReqSetQuickSet
   */
  ReqSetQuickSet.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetQuickSet) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetQuickSet: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetQuickSet();
    if (object.infoId != null) message.infoId = String(object.infoId);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetQuickSet message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetQuickSet
   * @static
   * @param {ReqSetQuickSet} message ReqSetQuickSet
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetQuickSet.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.infoId = "";
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      object.infoId = message.infoId;
    return object;
  };

  /**
   * Converts this ReqSetQuickSet to JSON.
   * @function toJSON
   * @memberof ReqSetQuickSet
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetQuickSet.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetQuickSet
   * @function getTypeUrl
   * @memberof ReqSetQuickSet
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetQuickSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetQuickSet";
  };

  return ReqSetQuickSet;
})();

$root.ResSetQuickSet = (function () {
  /**
   * Properties of a ResSetQuickSet.
   * @exports IResSetQuickSet
   * @interface IResSetQuickSet
   * @property {number|null} [code] ResSetQuickSet code
   * @property {string|null} [infoId] ResSetQuickSet infoId
   */

  /**
   * Constructs a new ResSetQuickSet.
   * @exports ResSetQuickSet
   * @classdesc Represents a ResSetQuickSet.
   * @implements IResSetQuickSet
   * @constructor
   * @param {IResSetQuickSet=} [properties] Properties to set
   */
  function ResSetQuickSet(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSetQuickSet code.
   * @member {number} code
   * @memberof ResSetQuickSet
   * @instance
   */
  ResSetQuickSet.prototype.code = 0;

  /**
   * ResSetQuickSet infoId.
   * @member {string} infoId
   * @memberof ResSetQuickSet
   * @instance
   */
  ResSetQuickSet.prototype.infoId = "";

  /**
   * Creates a new ResSetQuickSet instance using the specified properties.
   * @function create
   * @memberof ResSetQuickSet
   * @static
   * @param {IResSetQuickSet=} [properties] Properties to set
   * @returns {ResSetQuickSet} ResSetQuickSet instance
   */
  ResSetQuickSet.create = function create(properties) {
    return new ResSetQuickSet(properties);
  };

  /**
   * Encodes the specified ResSetQuickSet message. Does not implicitly {@link ResSetQuickSet.verify|verify} messages.
   * @function encode
   * @memberof ResSetQuickSet
   * @static
   * @param {IResSetQuickSet} message ResSetQuickSet message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSetQuickSet.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.infoId);
    return writer;
  };

  /**
   * Encodes the specified ResSetQuickSet message, length delimited. Does not implicitly {@link ResSetQuickSet.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSetQuickSet
   * @static
   * @param {IResSetQuickSet} message ResSetQuickSet message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSetQuickSet.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSetQuickSet message from the specified reader or buffer.
   * @function decode
   * @memberof ResSetQuickSet
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSetQuickSet} ResSetQuickSet
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSetQuickSet.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSetQuickSet();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.infoId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSetQuickSet message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSetQuickSet
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSetQuickSet} ResSetQuickSet
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSetQuickSet.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSetQuickSet message.
   * @function verify
   * @memberof ResSetQuickSet
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSetQuickSet.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      if (!$util.isString(message.infoId)) return "infoId: string expected";
    return null;
  };

  /**
   * Creates a ResSetQuickSet message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSetQuickSet
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSetQuickSet} ResSetQuickSet
   */
  ResSetQuickSet.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSetQuickSet) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResSetQuickSet: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSetQuickSet();
    if (object.code != null) message.code = object.code | 0;
    if (object.infoId != null) message.infoId = String(object.infoId);
    return message;
  };

  /**
   * Creates a plain object from a ResSetQuickSet message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSetQuickSet
   * @static
   * @param {ResSetQuickSet} message ResSetQuickSet
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSetQuickSet.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.infoId = "";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.infoId != null && Object.hasOwnProperty.call(message, "infoId"))
      object.infoId = message.infoId;
    return object;
  };

  /**
   * Converts this ResSetQuickSet to JSON.
   * @function toJSON
   * @memberof ResSetQuickSet
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSetQuickSet.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSetQuickSet
   * @function getTypeUrl
   * @memberof ResSetQuickSet
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSetQuickSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSetQuickSet";
  };

  return ResSetQuickSet;
})();

$root.ReqOneClickShootingParam = (function () {
  /**
   * Properties of a ReqOneClickShootingParam.
   * @exports IReqOneClickShootingParam
   * @interface IReqOneClickShootingParam
   * @property {number|null} [horizontalScale] ReqOneClickShootingParam horizontalScale
   * @property {number|null} [verticalScale] ReqOneClickShootingParam verticalScale
   * @property {number|null} [rotation] ReqOneClickShootingParam rotation
   * @property {number|null} [cameraType] ReqOneClickShootingParam cameraType
   * @property {number|null} [expIndex] ReqOneClickShootingParam expIndex
   * @property {number|null} [gain] ReqOneClickShootingParam gain
   * @property {number|null} [filterType] ReqOneClickShootingParam filterType
   * @property {number|null} [capSize] ReqOneClickShootingParam capSize
   * @property {number|null} [resolution] ReqOneClickShootingParam resolution
   */

  /**
   * Constructs a new ReqOneClickShootingParam.
   * @exports ReqOneClickShootingParam
   * @classdesc Represents a ReqOneClickShootingParam.
   * @implements IReqOneClickShootingParam
   * @constructor
   * @param {IReqOneClickShootingParam=} [properties] Properties to set
   */
  function ReqOneClickShootingParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqOneClickShootingParam horizontalScale.
   * @member {number} horizontalScale
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.horizontalScale = 0;

  /**
   * ReqOneClickShootingParam verticalScale.
   * @member {number} verticalScale
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.verticalScale = 0;

  /**
   * ReqOneClickShootingParam rotation.
   * @member {number} rotation
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.rotation = 0;

  /**
   * ReqOneClickShootingParam cameraType.
   * @member {number} cameraType
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.cameraType = 0;

  /**
   * ReqOneClickShootingParam expIndex.
   * @member {number} expIndex
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.expIndex = 0;

  /**
   * ReqOneClickShootingParam gain.
   * @member {number} gain
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.gain = 0;

  /**
   * ReqOneClickShootingParam filterType.
   * @member {number} filterType
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.filterType = 0;

  /**
   * ReqOneClickShootingParam capSize.
   * @member {number} capSize
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.capSize = 0;

  /**
   * ReqOneClickShootingParam resolution.
   * @member {number} resolution
   * @memberof ReqOneClickShootingParam
   * @instance
   */
  ReqOneClickShootingParam.prototype.resolution = 0;

  /**
   * Creates a new ReqOneClickShootingParam instance using the specified properties.
   * @function create
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {IReqOneClickShootingParam=} [properties] Properties to set
   * @returns {ReqOneClickShootingParam} ReqOneClickShootingParam instance
   */
  ReqOneClickShootingParam.create = function create(properties) {
    return new ReqOneClickShootingParam(properties);
  };

  /**
   * Encodes the specified ReqOneClickShootingParam message. Does not implicitly {@link ReqOneClickShootingParam.verify|verify} messages.
   * @function encode
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {IReqOneClickShootingParam} message ReqOneClickShootingParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickShootingParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.horizontalScale);
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.verticalScale);
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.rotation);
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.cameraType);
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.expIndex);
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.gain);
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.filterType);
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.capSize);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.resolution);
    return writer;
  };

  /**
   * Encodes the specified ReqOneClickShootingParam message, length delimited. Does not implicitly {@link ReqOneClickShootingParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {IReqOneClickShootingParam} message ReqOneClickShootingParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickShootingParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOneClickShootingParam message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOneClickShootingParam} ReqOneClickShootingParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickShootingParam.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOneClickShootingParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.horizontalScale = reader.int32();
          break;
        }
        case 2: {
          message.verticalScale = reader.int32();
          break;
        }
        case 3: {
          message.rotation = reader.int32();
          break;
        }
        case 4: {
          message.cameraType = reader.int32();
          break;
        }
        case 5: {
          message.expIndex = reader.int32();
          break;
        }
        case 6: {
          message.gain = reader.int32();
          break;
        }
        case 7: {
          message.filterType = reader.int32();
          break;
        }
        case 8: {
          message.capSize = reader.int32();
          break;
        }
        case 9: {
          message.resolution = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOneClickShootingParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOneClickShootingParam} ReqOneClickShootingParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickShootingParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOneClickShootingParam message.
   * @function verify
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOneClickShootingParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      if (!$util.isInteger(message.horizontalScale))
        return "horizontalScale: integer expected";
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      if (!$util.isInteger(message.verticalScale))
        return "verticalScale: integer expected";
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      if (!$util.isInteger(message.rotation))
        return "rotation: integer expected";
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      if (!$util.isInteger(message.cameraType))
        return "cameraType: integer expected";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      if (!$util.isInteger(message.gain)) return "gain: integer expected";
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      if (!$util.isInteger(message.filterType))
        return "filterType: integer expected";
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      if (!$util.isInteger(message.capSize)) return "capSize: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    return null;
  };

  /**
   * Creates a ReqOneClickShootingParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOneClickShootingParam} ReqOneClickShootingParam
   */
  ReqOneClickShootingParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOneClickShootingParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqOneClickShootingParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqOneClickShootingParam();
    if (object.horizontalScale != null)
      message.horizontalScale = object.horizontalScale | 0;
    if (object.verticalScale != null)
      message.verticalScale = object.verticalScale | 0;
    if (object.rotation != null) message.rotation = object.rotation | 0;
    if (object.cameraType != null) message.cameraType = object.cameraType | 0;
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gain != null) message.gain = object.gain | 0;
    if (object.filterType != null) message.filterType = object.filterType | 0;
    if (object.capSize != null) message.capSize = object.capSize | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqOneClickShootingParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {ReqOneClickShootingParam} message ReqOneClickShootingParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOneClickShootingParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.horizontalScale = 0;
      object.verticalScale = 0;
      object.rotation = 0;
      object.cameraType = 0;
      object.expIndex = 0;
      object.gain = 0;
      object.filterType = 0;
      object.capSize = 0;
      object.resolution = 0;
    }
    if (
      message.horizontalScale != null &&
      Object.hasOwnProperty.call(message, "horizontalScale")
    )
      object.horizontalScale = message.horizontalScale;
    if (
      message.verticalScale != null &&
      Object.hasOwnProperty.call(message, "verticalScale")
    )
      object.verticalScale = message.verticalScale;
    if (
      message.rotation != null &&
      Object.hasOwnProperty.call(message, "rotation")
    )
      object.rotation = message.rotation;
    if (
      message.cameraType != null &&
      Object.hasOwnProperty.call(message, "cameraType")
    )
      object.cameraType = message.cameraType;
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      object.gain = message.gain;
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      object.filterType = message.filterType;
    if (
      message.capSize != null &&
      Object.hasOwnProperty.call(message, "capSize")
    )
      object.capSize = message.capSize;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    return object;
  };

  /**
   * Converts this ReqOneClickShootingParam to JSON.
   * @function toJSON
   * @memberof ReqOneClickShootingParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOneClickShootingParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOneClickShootingParam
   * @function getTypeUrl
   * @memberof ReqOneClickShootingParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOneClickShootingParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOneClickShootingParam";
  };

  return ReqOneClickShootingParam;
})();

$root.ReqOneClickShooting = (function () {
  /**
   * Properties of a ReqOneClickShooting.
   * @exports IReqOneClickShooting
   * @interface IReqOneClickShooting
   * @property {IReqOneClickGotoDSO|null} [gotoDso] ReqOneClickShooting gotoDso
   * @property {IReqOneClickShootingParam|null} [shootingParam] ReqOneClickShooting shootingParam
   */

  /**
   * Constructs a new ReqOneClickShooting.
   * @exports ReqOneClickShooting
   * @classdesc Represents a ReqOneClickShooting.
   * @implements IReqOneClickShooting
   * @constructor
   * @param {IReqOneClickShooting=} [properties] Properties to set
   */
  function ReqOneClickShooting(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqOneClickShooting gotoDso.
   * @member {IReqOneClickGotoDSO|null|undefined} gotoDso
   * @memberof ReqOneClickShooting
   * @instance
   */
  ReqOneClickShooting.prototype.gotoDso = null;

  /**
   * ReqOneClickShooting shootingParam.
   * @member {IReqOneClickShootingParam|null|undefined} shootingParam
   * @memberof ReqOneClickShooting
   * @instance
   */
  ReqOneClickShooting.prototype.shootingParam = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqOneClickShooting.prototype, "_gotoDso", {
    get: $util.oneOfGetter(($oneOfFields = ["gotoDso"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqOneClickShooting.prototype, "_shootingParam", {
    get: $util.oneOfGetter(($oneOfFields = ["shootingParam"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqOneClickShooting instance using the specified properties.
   * @function create
   * @memberof ReqOneClickShooting
   * @static
   * @param {IReqOneClickShooting=} [properties] Properties to set
   * @returns {ReqOneClickShooting} ReqOneClickShooting instance
   */
  ReqOneClickShooting.create = function create(properties) {
    return new ReqOneClickShooting(properties);
  };

  /**
   * Encodes the specified ReqOneClickShooting message. Does not implicitly {@link ReqOneClickShooting.verify|verify} messages.
   * @function encode
   * @memberof ReqOneClickShooting
   * @static
   * @param {IReqOneClickShooting} message ReqOneClickShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickShooting.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.gotoDso != null &&
      Object.hasOwnProperty.call(message, "gotoDso")
    )
      $root.ReqOneClickGotoDSO.encode(
        message.gotoDso,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.shootingParam != null &&
      Object.hasOwnProperty.call(message, "shootingParam")
    )
      $root.ReqOneClickShootingParam.encode(
        message.shootingParam,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqOneClickShooting message, length delimited. Does not implicitly {@link ReqOneClickShooting.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOneClickShooting
   * @static
   * @param {IReqOneClickShooting} message ReqOneClickShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOneClickShooting.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOneClickShooting message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOneClickShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOneClickShooting} ReqOneClickShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickShooting.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOneClickShooting();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.gotoDso = $root.ReqOneClickGotoDSO.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.shootingParam = $root.ReqOneClickShootingParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOneClickShooting message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOneClickShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOneClickShooting} ReqOneClickShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOneClickShooting.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOneClickShooting message.
   * @function verify
   * @memberof ReqOneClickShooting
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOneClickShooting.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.gotoDso != null &&
      Object.hasOwnProperty.call(message, "gotoDso")
    ) {
      properties._gotoDso = 1;
      {
        var error = $root.ReqOneClickGotoDSO.verify(message.gotoDso, long + 1);
        if (error) return "gotoDso." + error;
      }
    }
    if (
      message.shootingParam != null &&
      Object.hasOwnProperty.call(message, "shootingParam")
    ) {
      properties._shootingParam = 1;
      {
        var error = $root.ReqOneClickShootingParam.verify(
          message.shootingParam,
          long + 1,
        );
        if (error) return "shootingParam." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ReqOneClickShooting message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOneClickShooting
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOneClickShooting} ReqOneClickShooting
   */
  ReqOneClickShooting.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOneClickShooting) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqOneClickShooting: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqOneClickShooting();
    if (object.gotoDso != null) {
      if (!$util.isObject(object.gotoDso))
        throw TypeError(".ReqOneClickShooting.gotoDso: object expected");
      message.gotoDso = $root.ReqOneClickGotoDSO.fromObject(
        object.gotoDso,
        long + 1,
      );
    }
    if (object.shootingParam != null) {
      if (!$util.isObject(object.shootingParam))
        throw TypeError(".ReqOneClickShooting.shootingParam: object expected");
      message.shootingParam = $root.ReqOneClickShootingParam.fromObject(
        object.shootingParam,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqOneClickShooting message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOneClickShooting
   * @static
   * @param {ReqOneClickShooting} message ReqOneClickShooting
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOneClickShooting.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.gotoDso != null &&
      Object.hasOwnProperty.call(message, "gotoDso")
    ) {
      object.gotoDso = $root.ReqOneClickGotoDSO.toObject(
        message.gotoDso,
        options,
        q + 1,
      );
      if (options.oneofs) object._gotoDso = "gotoDso";
    }
    if (
      message.shootingParam != null &&
      Object.hasOwnProperty.call(message, "shootingParam")
    ) {
      object.shootingParam = $root.ReqOneClickShootingParam.toObject(
        message.shootingParam,
        options,
        q + 1,
      );
      if (options.oneofs) object._shootingParam = "shootingParam";
    }
    return object;
  };

  /**
   * Converts this ReqOneClickShooting to JSON.
   * @function toJSON
   * @memberof ReqOneClickShooting
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOneClickShooting.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOneClickShooting
   * @function getTypeUrl
   * @memberof ReqOneClickShooting
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOneClickShooting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOneClickShooting";
  };

  return ReqOneClickShooting;
})();

$root.ResAstroShooting = (function () {
  /**
   * Properties of a ResAstroShooting.
   * @exports IResAstroShooting
   * @interface IResAstroShooting
   * @property {number|null} [code] ResAstroShooting code
   * @property {string|null} [expName] ResAstroShooting expName
   * @property {number|null} [gain] ResAstroShooting gain
   * @property {number|null} [resolution] ResAstroShooting resolution
   * @property {number|null} [filterType] ResAstroShooting filterType
   * @property {number|null} [tempThreshold] ResAstroShooting tempThreshold
   */

  /**
   * Constructs a new ResAstroShooting.
   * @exports ResAstroShooting
   * @classdesc Represents a ResAstroShooting.
   * @implements IResAstroShooting
   * @constructor
   * @param {IResAstroShooting=} [properties] Properties to set
   */
  function ResAstroShooting(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResAstroShooting code.
   * @member {number} code
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.code = 0;

  /**
   * ResAstroShooting expName.
   * @member {string|null|undefined} expName
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.expName = null;

  /**
   * ResAstroShooting gain.
   * @member {number|null|undefined} gain
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.gain = null;

  /**
   * ResAstroShooting resolution.
   * @member {number|null|undefined} resolution
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.resolution = null;

  /**
   * ResAstroShooting filterType.
   * @member {number|null|undefined} filterType
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.filterType = null;

  /**
   * ResAstroShooting tempThreshold.
   * @member {number|null|undefined} tempThreshold
   * @memberof ResAstroShooting
   * @instance
   */
  ResAstroShooting.prototype.tempThreshold = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResAstroShooting.prototype, "_expName", {
    get: $util.oneOfGetter(($oneOfFields = ["expName"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResAstroShooting.prototype, "_gain", {
    get: $util.oneOfGetter(($oneOfFields = ["gain"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResAstroShooting.prototype, "_resolution", {
    get: $util.oneOfGetter(($oneOfFields = ["resolution"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResAstroShooting.prototype, "_filterType", {
    get: $util.oneOfGetter(($oneOfFields = ["filterType"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ResAstroShooting.prototype, "_tempThreshold", {
    get: $util.oneOfGetter(($oneOfFields = ["tempThreshold"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ResAstroShooting instance using the specified properties.
   * @function create
   * @memberof ResAstroShooting
   * @static
   * @param {IResAstroShooting=} [properties] Properties to set
   * @returns {ResAstroShooting} ResAstroShooting instance
   */
  ResAstroShooting.create = function create(properties) {
    return new ResAstroShooting(properties);
  };

  /**
   * Encodes the specified ResAstroShooting message. Does not implicitly {@link ResAstroShooting.verify|verify} messages.
   * @function encode
   * @memberof ResAstroShooting
   * @static
   * @param {IResAstroShooting} message ResAstroShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAstroShooting.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.expName);
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.gain);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.resolution);
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.filterType);
    if (
      message.tempThreshold != null &&
      Object.hasOwnProperty.call(message, "tempThreshold")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.tempThreshold);
    return writer;
  };

  /**
   * Encodes the specified ResAstroShooting message, length delimited. Does not implicitly {@link ResAstroShooting.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResAstroShooting
   * @static
   * @param {IResAstroShooting} message ResAstroShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAstroShooting.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResAstroShooting message from the specified reader or buffer.
   * @function decode
   * @memberof ResAstroShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResAstroShooting} ResAstroShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAstroShooting.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResAstroShooting();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.expName = reader.string();
          break;
        }
        case 3: {
          message.gain = reader.int32();
          break;
        }
        case 4: {
          message.resolution = reader.int32();
          break;
        }
        case 5: {
          message.filterType = reader.int32();
          break;
        }
        case 6: {
          message.tempThreshold = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResAstroShooting message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResAstroShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResAstroShooting} ResAstroShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAstroShooting.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResAstroShooting message.
   * @function verify
   * @memberof ResAstroShooting
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResAstroShooting.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    ) {
      properties._expName = 1;
      if (!$util.isString(message.expName)) return "expName: string expected";
    }
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain")) {
      properties._gain = 1;
      if (!$util.isInteger(message.gain)) return "gain: integer expected";
    }
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    ) {
      properties._resolution = 1;
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    }
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      properties._filterType = 1;
      if (!$util.isInteger(message.filterType))
        return "filterType: integer expected";
    }
    if (
      message.tempThreshold != null &&
      Object.hasOwnProperty.call(message, "tempThreshold")
    ) {
      properties._tempThreshold = 1;
      if (!$util.isInteger(message.tempThreshold))
        return "tempThreshold: integer expected";
    }
    return null;
  };

  /**
   * Creates a ResAstroShooting message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResAstroShooting
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResAstroShooting} ResAstroShooting
   */
  ResAstroShooting.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResAstroShooting) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResAstroShooting: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResAstroShooting();
    if (object.code != null) message.code = object.code | 0;
    if (object.expName != null) message.expName = String(object.expName);
    if (object.gain != null) message.gain = object.gain | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    if (object.filterType != null) message.filterType = object.filterType | 0;
    if (object.tempThreshold != null)
      message.tempThreshold = object.tempThreshold | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResAstroShooting message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResAstroShooting
   * @static
   * @param {ResAstroShooting} message ResAstroShooting
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResAstroShooting.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.code = 0;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.expName != null &&
      Object.hasOwnProperty.call(message, "expName")
    ) {
      object.expName = message.expName;
      if (options.oneofs) object._expName = "expName";
    }
    if (message.gain != null && Object.hasOwnProperty.call(message, "gain")) {
      object.gain = message.gain;
      if (options.oneofs) object._gain = "gain";
    }
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    ) {
      object.resolution = message.resolution;
      if (options.oneofs) object._resolution = "resolution";
    }
    if (
      message.filterType != null &&
      Object.hasOwnProperty.call(message, "filterType")
    ) {
      object.filterType = message.filterType;
      if (options.oneofs) object._filterType = "filterType";
    }
    if (
      message.tempThreshold != null &&
      Object.hasOwnProperty.call(message, "tempThreshold")
    ) {
      object.tempThreshold = message.tempThreshold;
      if (options.oneofs) object._tempThreshold = "tempThreshold";
    }
    return object;
  };

  /**
   * Converts this ResAstroShooting to JSON.
   * @function toJSON
   * @memberof ResAstroShooting
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResAstroShooting.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResAstroShooting
   * @function getTypeUrl
   * @memberof ResAstroShooting
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResAstroShooting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResAstroShooting";
  };

  return ResAstroShooting;
})();

$root.ReqStartSkyTargetFinder = (function () {
  /**
   * Properties of a ReqStartSkyTargetFinder.
   * @exports IReqStartSkyTargetFinder
   * @interface IReqStartSkyTargetFinder
   * @property {number|null} [lon] ReqStartSkyTargetFinder lon
   * @property {number|null} [lat] ReqStartSkyTargetFinder lat
   * @property {boolean|null} [forceRestart] ReqStartSkyTargetFinder forceRestart
   * @property {number|null} [sceneType] ReqStartSkyTargetFinder sceneType
   */

  /**
   * Constructs a new ReqStartSkyTargetFinder.
   * @exports ReqStartSkyTargetFinder
   * @classdesc Represents a ReqStartSkyTargetFinder.
   * @implements IReqStartSkyTargetFinder
   * @constructor
   * @param {IReqStartSkyTargetFinder=} [properties] Properties to set
   */
  function ReqStartSkyTargetFinder(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartSkyTargetFinder lon.
   * @member {number} lon
   * @memberof ReqStartSkyTargetFinder
   * @instance
   */
  ReqStartSkyTargetFinder.prototype.lon = 0;

  /**
   * ReqStartSkyTargetFinder lat.
   * @member {number} lat
   * @memberof ReqStartSkyTargetFinder
   * @instance
   */
  ReqStartSkyTargetFinder.prototype.lat = 0;

  /**
   * ReqStartSkyTargetFinder forceRestart.
   * @member {boolean} forceRestart
   * @memberof ReqStartSkyTargetFinder
   * @instance
   */
  ReqStartSkyTargetFinder.prototype.forceRestart = false;

  /**
   * ReqStartSkyTargetFinder sceneType.
   * @member {number} sceneType
   * @memberof ReqStartSkyTargetFinder
   * @instance
   */
  ReqStartSkyTargetFinder.prototype.sceneType = 0;

  /**
   * Creates a new ReqStartSkyTargetFinder instance using the specified properties.
   * @function create
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {IReqStartSkyTargetFinder=} [properties] Properties to set
   * @returns {ReqStartSkyTargetFinder} ReqStartSkyTargetFinder instance
   */
  ReqStartSkyTargetFinder.create = function create(properties) {
    return new ReqStartSkyTargetFinder(properties);
  };

  /**
   * Encodes the specified ReqStartSkyTargetFinder message. Does not implicitly {@link ReqStartSkyTargetFinder.verify|verify} messages.
   * @function encode
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {IReqStartSkyTargetFinder} message ReqStartSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartSkyTargetFinder.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.lon);
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.lat);
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.forceRestart);
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.sceneType);
    return writer;
  };

  /**
   * Encodes the specified ReqStartSkyTargetFinder message, length delimited. Does not implicitly {@link ReqStartSkyTargetFinder.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {IReqStartSkyTargetFinder} message ReqStartSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartSkyTargetFinder.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartSkyTargetFinder message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartSkyTargetFinder} ReqStartSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartSkyTargetFinder.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartSkyTargetFinder();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.lon = reader.double();
          break;
        }
        case 2: {
          message.lat = reader.double();
          break;
        }
        case 3: {
          message.forceRestart = reader.bool();
          break;
        }
        case 4: {
          message.sceneType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartSkyTargetFinder message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartSkyTargetFinder} ReqStartSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartSkyTargetFinder.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartSkyTargetFinder message.
   * @function verify
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartSkyTargetFinder.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      if (typeof message.lon !== "number") return "lon: number expected";
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      if (typeof message.lat !== "number") return "lat: number expected";
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      if (typeof message.forceRestart !== "boolean")
        return "forceRestart: boolean expected";
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      if (!$util.isInteger(message.sceneType))
        return "sceneType: integer expected";
    return null;
  };

  /**
   * Creates a ReqStartSkyTargetFinder message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartSkyTargetFinder} ReqStartSkyTargetFinder
   */
  ReqStartSkyTargetFinder.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartSkyTargetFinder) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartSkyTargetFinder: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartSkyTargetFinder();
    if (object.lon != null) message.lon = Number(object.lon);
    if (object.lat != null) message.lat = Number(object.lat);
    if (object.forceRestart != null)
      message.forceRestart = Boolean(object.forceRestart);
    if (object.sceneType != null) message.sceneType = object.sceneType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStartSkyTargetFinder message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {ReqStartSkyTargetFinder} message ReqStartSkyTargetFinder
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartSkyTargetFinder.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.lon = 0;
      object.lat = 0;
      object.forceRestart = false;
      object.sceneType = 0;
    }
    if (message.lon != null && Object.hasOwnProperty.call(message, "lon"))
      object.lon =
        options.json && !isFinite(message.lon)
          ? String(message.lon)
          : message.lon;
    if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
      object.lat =
        options.json && !isFinite(message.lat)
          ? String(message.lat)
          : message.lat;
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      object.forceRestart = message.forceRestart;
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      object.sceneType = message.sceneType;
    return object;
  };

  /**
   * Converts this ReqStartSkyTargetFinder to JSON.
   * @function toJSON
   * @memberof ReqStartSkyTargetFinder
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartSkyTargetFinder.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartSkyTargetFinder
   * @function getTypeUrl
   * @memberof ReqStartSkyTargetFinder
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartSkyTargetFinder.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartSkyTargetFinder";
  };

  return ReqStartSkyTargetFinder;
})();

$root.ResStartSkyTargetFinder = (function () {
  /**
   * Properties of a ResStartSkyTargetFinder.
   * @exports IResStartSkyTargetFinder
   * @interface IResStartSkyTargetFinder
   * @property {number|null} [code] ResStartSkyTargetFinder code
   * @property {number|null} [zenithAzi] ResStartSkyTargetFinder zenithAzi
   * @property {number|null} [zenithAlt] ResStartSkyTargetFinder zenithAlt
   * @property {number|null} [centerAzi] ResStartSkyTargetFinder centerAzi
   * @property {number|null} [centerAlt] ResStartSkyTargetFinder centerAlt
   * @property {number|null} [centerRoll] ResStartSkyTargetFinder centerRoll
   * @property {number|null} [sceneType] ResStartSkyTargetFinder sceneType
   */

  /**
   * Constructs a new ResStartSkyTargetFinder.
   * @exports ResStartSkyTargetFinder
   * @classdesc Represents a ResStartSkyTargetFinder.
   * @implements IResStartSkyTargetFinder
   * @constructor
   * @param {IResStartSkyTargetFinder=} [properties] Properties to set
   */
  function ResStartSkyTargetFinder(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResStartSkyTargetFinder code.
   * @member {number} code
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.code = 0;

  /**
   * ResStartSkyTargetFinder zenithAzi.
   * @member {number} zenithAzi
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.zenithAzi = 0;

  /**
   * ResStartSkyTargetFinder zenithAlt.
   * @member {number} zenithAlt
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.zenithAlt = 0;

  /**
   * ResStartSkyTargetFinder centerAzi.
   * @member {number} centerAzi
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.centerAzi = 0;

  /**
   * ResStartSkyTargetFinder centerAlt.
   * @member {number} centerAlt
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.centerAlt = 0;

  /**
   * ResStartSkyTargetFinder centerRoll.
   * @member {number} centerRoll
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.centerRoll = 0;

  /**
   * ResStartSkyTargetFinder sceneType.
   * @member {number} sceneType
   * @memberof ResStartSkyTargetFinder
   * @instance
   */
  ResStartSkyTargetFinder.prototype.sceneType = 0;

  /**
   * Creates a new ResStartSkyTargetFinder instance using the specified properties.
   * @function create
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {IResStartSkyTargetFinder=} [properties] Properties to set
   * @returns {ResStartSkyTargetFinder} ResStartSkyTargetFinder instance
   */
  ResStartSkyTargetFinder.create = function create(properties) {
    return new ResStartSkyTargetFinder(properties);
  };

  /**
   * Encodes the specified ResStartSkyTargetFinder message. Does not implicitly {@link ResStartSkyTargetFinder.verify|verify} messages.
   * @function encode
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {IResStartSkyTargetFinder} message ResStartSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartSkyTargetFinder.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.zenithAzi != null &&
      Object.hasOwnProperty.call(message, "zenithAzi")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.zenithAzi);
    if (
      message.zenithAlt != null &&
      Object.hasOwnProperty.call(message, "zenithAlt")
    )
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.zenithAlt);
    if (
      message.centerAzi != null &&
      Object.hasOwnProperty.call(message, "centerAzi")
    )
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.centerAzi);
    if (
      message.centerAlt != null &&
      Object.hasOwnProperty.call(message, "centerAlt")
    )
      writer.uint32(/* id 5, wireType 1 =*/ 41).double(message.centerAlt);
    if (
      message.centerRoll != null &&
      Object.hasOwnProperty.call(message, "centerRoll")
    )
      writer.uint32(/* id 6, wireType 1 =*/ 49).double(message.centerRoll);
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.sceneType);
    return writer;
  };

  /**
   * Encodes the specified ResStartSkyTargetFinder message, length delimited. Does not implicitly {@link ResStartSkyTargetFinder.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {IResStartSkyTargetFinder} message ResStartSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartSkyTargetFinder.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResStartSkyTargetFinder message from the specified reader or buffer.
   * @function decode
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResStartSkyTargetFinder} ResStartSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartSkyTargetFinder.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResStartSkyTargetFinder();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.zenithAzi = reader.double();
          break;
        }
        case 3: {
          message.zenithAlt = reader.double();
          break;
        }
        case 4: {
          message.centerAzi = reader.double();
          break;
        }
        case 5: {
          message.centerAlt = reader.double();
          break;
        }
        case 6: {
          message.centerRoll = reader.double();
          break;
        }
        case 7: {
          message.sceneType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResStartSkyTargetFinder message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResStartSkyTargetFinder} ResStartSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartSkyTargetFinder.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResStartSkyTargetFinder message.
   * @function verify
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResStartSkyTargetFinder.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.zenithAzi != null &&
      Object.hasOwnProperty.call(message, "zenithAzi")
    )
      if (typeof message.zenithAzi !== "number")
        return "zenithAzi: number expected";
    if (
      message.zenithAlt != null &&
      Object.hasOwnProperty.call(message, "zenithAlt")
    )
      if (typeof message.zenithAlt !== "number")
        return "zenithAlt: number expected";
    if (
      message.centerAzi != null &&
      Object.hasOwnProperty.call(message, "centerAzi")
    )
      if (typeof message.centerAzi !== "number")
        return "centerAzi: number expected";
    if (
      message.centerAlt != null &&
      Object.hasOwnProperty.call(message, "centerAlt")
    )
      if (typeof message.centerAlt !== "number")
        return "centerAlt: number expected";
    if (
      message.centerRoll != null &&
      Object.hasOwnProperty.call(message, "centerRoll")
    )
      if (typeof message.centerRoll !== "number")
        return "centerRoll: number expected";
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      if (!$util.isInteger(message.sceneType))
        return "sceneType: integer expected";
    return null;
  };

  /**
   * Creates a ResStartSkyTargetFinder message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResStartSkyTargetFinder} ResStartSkyTargetFinder
   */
  ResStartSkyTargetFinder.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResStartSkyTargetFinder) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResStartSkyTargetFinder: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResStartSkyTargetFinder();
    if (object.code != null) message.code = object.code | 0;
    if (object.zenithAzi != null) message.zenithAzi = Number(object.zenithAzi);
    if (object.zenithAlt != null) message.zenithAlt = Number(object.zenithAlt);
    if (object.centerAzi != null) message.centerAzi = Number(object.centerAzi);
    if (object.centerAlt != null) message.centerAlt = Number(object.centerAlt);
    if (object.centerRoll != null)
      message.centerRoll = Number(object.centerRoll);
    if (object.sceneType != null) message.sceneType = object.sceneType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResStartSkyTargetFinder message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {ResStartSkyTargetFinder} message ResStartSkyTargetFinder
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResStartSkyTargetFinder.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.zenithAzi = 0;
      object.zenithAlt = 0;
      object.centerAzi = 0;
      object.centerAlt = 0;
      object.centerRoll = 0;
      object.sceneType = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.zenithAzi != null &&
      Object.hasOwnProperty.call(message, "zenithAzi")
    )
      object.zenithAzi =
        options.json && !isFinite(message.zenithAzi)
          ? String(message.zenithAzi)
          : message.zenithAzi;
    if (
      message.zenithAlt != null &&
      Object.hasOwnProperty.call(message, "zenithAlt")
    )
      object.zenithAlt =
        options.json && !isFinite(message.zenithAlt)
          ? String(message.zenithAlt)
          : message.zenithAlt;
    if (
      message.centerAzi != null &&
      Object.hasOwnProperty.call(message, "centerAzi")
    )
      object.centerAzi =
        options.json && !isFinite(message.centerAzi)
          ? String(message.centerAzi)
          : message.centerAzi;
    if (
      message.centerAlt != null &&
      Object.hasOwnProperty.call(message, "centerAlt")
    )
      object.centerAlt =
        options.json && !isFinite(message.centerAlt)
          ? String(message.centerAlt)
          : message.centerAlt;
    if (
      message.centerRoll != null &&
      Object.hasOwnProperty.call(message, "centerRoll")
    )
      object.centerRoll =
        options.json && !isFinite(message.centerRoll)
          ? String(message.centerRoll)
          : message.centerRoll;
    if (
      message.sceneType != null &&
      Object.hasOwnProperty.call(message, "sceneType")
    )
      object.sceneType = message.sceneType;
    return object;
  };

  /**
   * Converts this ResStartSkyTargetFinder to JSON.
   * @function toJSON
   * @memberof ResStartSkyTargetFinder
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResStartSkyTargetFinder.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResStartSkyTargetFinder
   * @function getTypeUrl
   * @memberof ResStartSkyTargetFinder
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResStartSkyTargetFinder.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResStartSkyTargetFinder";
  };

  return ResStartSkyTargetFinder;
})();

$root.ReqStopSkyTargetFinder = (function () {
  /**
   * Properties of a ReqStopSkyTargetFinder.
   * @exports IReqStopSkyTargetFinder
   * @interface IReqStopSkyTargetFinder
   */

  /**
   * Constructs a new ReqStopSkyTargetFinder.
   * @exports ReqStopSkyTargetFinder
   * @classdesc Represents a ReqStopSkyTargetFinder.
   * @implements IReqStopSkyTargetFinder
   * @constructor
   * @param {IReqStopSkyTargetFinder=} [properties] Properties to set
   */
  function ReqStopSkyTargetFinder(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopSkyTargetFinder instance using the specified properties.
   * @function create
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {IReqStopSkyTargetFinder=} [properties] Properties to set
   * @returns {ReqStopSkyTargetFinder} ReqStopSkyTargetFinder instance
   */
  ReqStopSkyTargetFinder.create = function create(properties) {
    return new ReqStopSkyTargetFinder(properties);
  };

  /**
   * Encodes the specified ReqStopSkyTargetFinder message. Does not implicitly {@link ReqStopSkyTargetFinder.verify|verify} messages.
   * @function encode
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {IReqStopSkyTargetFinder} message ReqStopSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopSkyTargetFinder.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopSkyTargetFinder message, length delimited. Does not implicitly {@link ReqStopSkyTargetFinder.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {IReqStopSkyTargetFinder} message ReqStopSkyTargetFinder message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopSkyTargetFinder.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopSkyTargetFinder message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopSkyTargetFinder} ReqStopSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopSkyTargetFinder.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopSkyTargetFinder();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopSkyTargetFinder message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopSkyTargetFinder} ReqStopSkyTargetFinder
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopSkyTargetFinder.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopSkyTargetFinder message.
   * @function verify
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopSkyTargetFinder.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopSkyTargetFinder message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopSkyTargetFinder} ReqStopSkyTargetFinder
   */
  ReqStopSkyTargetFinder.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopSkyTargetFinder) return object;
    return new $root.ReqStopSkyTargetFinder();
  };

  /**
   * Creates a plain object from a ReqStopSkyTargetFinder message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {ReqStopSkyTargetFinder} message ReqStopSkyTargetFinder
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopSkyTargetFinder.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopSkyTargetFinder to JSON.
   * @function toJSON
   * @memberof ReqStopSkyTargetFinder
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopSkyTargetFinder.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopSkyTargetFinder
   * @function getTypeUrl
   * @memberof ReqStopSkyTargetFinder
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopSkyTargetFinder.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopSkyTargetFinder";
  };

  return ReqStopSkyTargetFinder;
})();

$root.ReqGetGyroAttitude = (function () {
  /**
   * Properties of a ReqGetGyroAttitude.
   * @exports IReqGetGyroAttitude
   * @interface IReqGetGyroAttitude
   * @property {number|null} [durationMs] ReqGetGyroAttitude durationMs
   */

  /**
   * Constructs a new ReqGetGyroAttitude.
   * @exports ReqGetGyroAttitude
   * @classdesc Represents a ReqGetGyroAttitude.
   * @implements IReqGetGyroAttitude
   * @constructor
   * @param {IReqGetGyroAttitude=} [properties] Properties to set
   */
  function ReqGetGyroAttitude(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetGyroAttitude durationMs.
   * @member {number} durationMs
   * @memberof ReqGetGyroAttitude
   * @instance
   */
  ReqGetGyroAttitude.prototype.durationMs = 0;

  /**
   * Creates a new ReqGetGyroAttitude instance using the specified properties.
   * @function create
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {IReqGetGyroAttitude=} [properties] Properties to set
   * @returns {ReqGetGyroAttitude} ReqGetGyroAttitude instance
   */
  ReqGetGyroAttitude.create = function create(properties) {
    return new ReqGetGyroAttitude(properties);
  };

  /**
   * Encodes the specified ReqGetGyroAttitude message. Does not implicitly {@link ReqGetGyroAttitude.verify|verify} messages.
   * @function encode
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {IReqGetGyroAttitude} message ReqGetGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGyroAttitude.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.durationMs != null &&
      Object.hasOwnProperty.call(message, "durationMs")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.durationMs);
    return writer;
  };

  /**
   * Encodes the specified ReqGetGyroAttitude message, length delimited. Does not implicitly {@link ReqGetGyroAttitude.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {IReqGetGyroAttitude} message ReqGetGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGyroAttitude.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetGyroAttitude message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetGyroAttitude} ReqGetGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGyroAttitude.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetGyroAttitude();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.durationMs = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetGyroAttitude message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetGyroAttitude} ReqGetGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGyroAttitude.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetGyroAttitude message.
   * @function verify
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetGyroAttitude.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.durationMs != null &&
      Object.hasOwnProperty.call(message, "durationMs")
    )
      if (!$util.isInteger(message.durationMs))
        return "durationMs: integer expected";
    return null;
  };

  /**
   * Creates a ReqGetGyroAttitude message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetGyroAttitude} ReqGetGyroAttitude
   */
  ReqGetGyroAttitude.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetGyroAttitude) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetGyroAttitude: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetGyroAttitude();
    if (object.durationMs != null) message.durationMs = object.durationMs | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqGetGyroAttitude message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {ReqGetGyroAttitude} message ReqGetGyroAttitude
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetGyroAttitude.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.durationMs = 0;
    if (
      message.durationMs != null &&
      Object.hasOwnProperty.call(message, "durationMs")
    )
      object.durationMs = message.durationMs;
    return object;
  };

  /**
   * Converts this ReqGetGyroAttitude to JSON.
   * @function toJSON
   * @memberof ReqGetGyroAttitude
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetGyroAttitude.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetGyroAttitude
   * @function getTypeUrl
   * @memberof ReqGetGyroAttitude
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetGyroAttitude.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetGyroAttitude";
  };

  return ReqGetGyroAttitude;
})();

$root.ResGetGyroAttitude = (function () {
  /**
   * Properties of a ResGetGyroAttitude.
   * @exports IResGetGyroAttitude
   * @interface IResGetGyroAttitude
   * @property {number|null} [code] ResGetGyroAttitude code
   * @property {number|null} [roll] ResGetGyroAttitude roll
   * @property {number|null} [pitch] ResGetGyroAttitude pitch
   * @property {number|null} [yaw] ResGetGyroAttitude yaw
   * @property {number|null} [azi] ResGetGyroAttitude azi
   * @property {number|null} [alt] ResGetGyroAttitude alt
   */

  /**
   * Constructs a new ResGetGyroAttitude.
   * @exports ResGetGyroAttitude
   * @classdesc Represents a ResGetGyroAttitude.
   * @implements IResGetGyroAttitude
   * @constructor
   * @param {IResGetGyroAttitude=} [properties] Properties to set
   */
  function ResGetGyroAttitude(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetGyroAttitude code.
   * @member {number} code
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.code = 0;

  /**
   * ResGetGyroAttitude roll.
   * @member {number} roll
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.roll = 0;

  /**
   * ResGetGyroAttitude pitch.
   * @member {number} pitch
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.pitch = 0;

  /**
   * ResGetGyroAttitude yaw.
   * @member {number} yaw
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.yaw = 0;

  /**
   * ResGetGyroAttitude azi.
   * @member {number} azi
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.azi = 0;

  /**
   * ResGetGyroAttitude alt.
   * @member {number} alt
   * @memberof ResGetGyroAttitude
   * @instance
   */
  ResGetGyroAttitude.prototype.alt = 0;

  /**
   * Creates a new ResGetGyroAttitude instance using the specified properties.
   * @function create
   * @memberof ResGetGyroAttitude
   * @static
   * @param {IResGetGyroAttitude=} [properties] Properties to set
   * @returns {ResGetGyroAttitude} ResGetGyroAttitude instance
   */
  ResGetGyroAttitude.create = function create(properties) {
    return new ResGetGyroAttitude(properties);
  };

  /**
   * Encodes the specified ResGetGyroAttitude message. Does not implicitly {@link ResGetGyroAttitude.verify|verify} messages.
   * @function encode
   * @memberof ResGetGyroAttitude
   * @static
   * @param {IResGetGyroAttitude} message ResGetGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetGyroAttitude.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.roll);
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.pitch);
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.yaw);
    if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
      writer.uint32(/* id 5, wireType 1 =*/ 41).double(message.azi);
    if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
      writer.uint32(/* id 6, wireType 1 =*/ 49).double(message.alt);
    return writer;
  };

  /**
   * Encodes the specified ResGetGyroAttitude message, length delimited. Does not implicitly {@link ResGetGyroAttitude.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetGyroAttitude
   * @static
   * @param {IResGetGyroAttitude} message ResGetGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetGyroAttitude.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetGyroAttitude message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetGyroAttitude} ResGetGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetGyroAttitude.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetGyroAttitude();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.roll = reader.double();
          break;
        }
        case 3: {
          message.pitch = reader.double();
          break;
        }
        case 4: {
          message.yaw = reader.double();
          break;
        }
        case 5: {
          message.azi = reader.double();
          break;
        }
        case 6: {
          message.alt = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetGyroAttitude message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetGyroAttitude} ResGetGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetGyroAttitude.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetGyroAttitude message.
   * @function verify
   * @memberof ResGetGyroAttitude
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetGyroAttitude.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      if (typeof message.roll !== "number") return "roll: number expected";
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      if (typeof message.pitch !== "number") return "pitch: number expected";
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      if (typeof message.yaw !== "number") return "yaw: number expected";
    if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
      if (typeof message.azi !== "number") return "azi: number expected";
    if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
      if (typeof message.alt !== "number") return "alt: number expected";
    return null;
  };

  /**
   * Creates a ResGetGyroAttitude message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetGyroAttitude
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetGyroAttitude} ResGetGyroAttitude
   */
  ResGetGyroAttitude.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetGyroAttitude) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetGyroAttitude: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetGyroAttitude();
    if (object.code != null) message.code = object.code | 0;
    if (object.roll != null) message.roll = Number(object.roll);
    if (object.pitch != null) message.pitch = Number(object.pitch);
    if (object.yaw != null) message.yaw = Number(object.yaw);
    if (object.azi != null) message.azi = Number(object.azi);
    if (object.alt != null) message.alt = Number(object.alt);
    return message;
  };

  /**
   * Creates a plain object from a ResGetGyroAttitude message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetGyroAttitude
   * @static
   * @param {ResGetGyroAttitude} message ResGetGyroAttitude
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetGyroAttitude.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.roll = 0;
      object.pitch = 0;
      object.yaw = 0;
      object.azi = 0;
      object.alt = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      object.roll =
        options.json && !isFinite(message.roll)
          ? String(message.roll)
          : message.roll;
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      object.pitch =
        options.json && !isFinite(message.pitch)
          ? String(message.pitch)
          : message.pitch;
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      object.yaw =
        options.json && !isFinite(message.yaw)
          ? String(message.yaw)
          : message.yaw;
    if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
      object.azi =
        options.json && !isFinite(message.azi)
          ? String(message.azi)
          : message.azi;
    if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
      object.alt =
        options.json && !isFinite(message.alt)
          ? String(message.alt)
          : message.alt;
    return object;
  };

  /**
   * Converts this ResGetGyroAttitude to JSON.
   * @function toJSON
   * @memberof ResGetGyroAttitude
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetGyroAttitude.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetGyroAttitude
   * @function getTypeUrl
   * @memberof ResGetGyroAttitude
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetGyroAttitude.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetGyroAttitude";
  };

  return ResGetGyroAttitude;
})();

$root.ReqContinueShooting = (function () {
  /**
   * Properties of a ReqContinueShooting.
   * @exports IReqContinueShooting
   * @interface IReqContinueShooting
   */

  /**
   * Constructs a new ReqContinueShooting.
   * @exports ReqContinueShooting
   * @classdesc Represents a ReqContinueShooting.
   * @implements IReqContinueShooting
   * @constructor
   * @param {IReqContinueShooting=} [properties] Properties to set
   */
  function ReqContinueShooting(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqContinueShooting instance using the specified properties.
   * @function create
   * @memberof ReqContinueShooting
   * @static
   * @param {IReqContinueShooting=} [properties] Properties to set
   * @returns {ReqContinueShooting} ReqContinueShooting instance
   */
  ReqContinueShooting.create = function create(properties) {
    return new ReqContinueShooting(properties);
  };

  /**
   * Encodes the specified ReqContinueShooting message. Does not implicitly {@link ReqContinueShooting.verify|verify} messages.
   * @function encode
   * @memberof ReqContinueShooting
   * @static
   * @param {IReqContinueShooting} message ReqContinueShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqContinueShooting.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqContinueShooting message, length delimited. Does not implicitly {@link ReqContinueShooting.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqContinueShooting
   * @static
   * @param {IReqContinueShooting} message ReqContinueShooting message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqContinueShooting.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqContinueShooting message from the specified reader or buffer.
   * @function decode
   * @memberof ReqContinueShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqContinueShooting} ReqContinueShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqContinueShooting.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqContinueShooting();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqContinueShooting message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqContinueShooting
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqContinueShooting} ReqContinueShooting
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqContinueShooting.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqContinueShooting message.
   * @function verify
   * @memberof ReqContinueShooting
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqContinueShooting.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqContinueShooting message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqContinueShooting
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqContinueShooting} ReqContinueShooting
   */
  ReqContinueShooting.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqContinueShooting) return object;
    return new $root.ReqContinueShooting();
  };

  /**
   * Creates a plain object from a ReqContinueShooting message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqContinueShooting
   * @static
   * @param {ReqContinueShooting} message ReqContinueShooting
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqContinueShooting.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqContinueShooting to JSON.
   * @function toJSON
   * @memberof ReqContinueShooting
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqContinueShooting.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqContinueShooting
   * @function getTypeUrl
   * @memberof ReqContinueShooting
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqContinueShooting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqContinueShooting";
  };

  return ReqContinueShooting;
})();

/**
 * WsMajorVersion enum.
 * @exports WsMajorVersion
 * @enum {number}
 * @property {number} WS_MAJOR_VERSION_UNKNOWN=0 WS_MAJOR_VERSION_UNKNOWN value
 * @property {number} WS_MAJOR_VERSION_NUMBER=1 WS_MAJOR_VERSION_NUMBER value
 */
$root.WsMajorVersion = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "WS_MAJOR_VERSION_UNKNOWN")] = 0;
  values[(valuesById[1] = "WS_MAJOR_VERSION_NUMBER")] = 1;
  return values;
})();

/**
 * WsMinorVersion enum.
 * @exports WsMinorVersion
 * @enum {number}
 * @property {number} WS_MINOR_VERSION_UNKNOWN=0 WS_MINOR_VERSION_UNKNOWN value
 * @property {number} WS_MINOR_VERSION_NUMBER=9 WS_MINOR_VERSION_NUMBER value
 */
$root.WsMinorVersion = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "WS_MINOR_VERSION_UNKNOWN")] = 0;
  values[(valuesById[9] = "WS_MINOR_VERSION_NUMBER")] = 9;
  return values;
})();

$root.WsPacket = (function () {
  /**
   * Properties of a WsPacket.
   * @exports IWsPacket
   * @interface IWsPacket
   * @property {number|null} [majorVersion] WsPacket majorVersion
   * @property {number|null} [minorVersion] WsPacket minorVersion
   * @property {number|null} [deviceId] WsPacket deviceId
   * @property {number|null} [moduleId] WsPacket moduleId
   * @property {number|null} [cmd] WsPacket cmd
   * @property {number|null} [type] WsPacket type
   * @property {Uint8Array|null} [data] WsPacket data
   * @property {string|null} [clientId] WsPacket clientId
   */

  /**
   * Constructs a new WsPacket.
   * @exports WsPacket
   * @classdesc Represents a WsPacket.
   * @implements IWsPacket
   * @constructor
   * @param {IWsPacket=} [properties] Properties to set
   */
  function WsPacket(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * WsPacket majorVersion.
   * @member {number} majorVersion
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.majorVersion = 0;

  /**
   * WsPacket minorVersion.
   * @member {number} minorVersion
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.minorVersion = 0;

  /**
   * WsPacket deviceId.
   * @member {number} deviceId
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.deviceId = 0;

  /**
   * WsPacket moduleId.
   * @member {number} moduleId
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.moduleId = 0;

  /**
   * WsPacket cmd.
   * @member {number} cmd
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.cmd = 0;

  /**
   * WsPacket type.
   * @member {number} type
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.type = 0;

  /**
   * WsPacket data.
   * @member {Uint8Array} data
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.data = $util.newBuffer([]);

  /**
   * WsPacket clientId.
   * @member {string} clientId
   * @memberof WsPacket
   * @instance
   */
  WsPacket.prototype.clientId = "";

  /**
   * Creates a new WsPacket instance using the specified properties.
   * @function create
   * @memberof WsPacket
   * @static
   * @param {IWsPacket=} [properties] Properties to set
   * @returns {WsPacket} WsPacket instance
   */
  WsPacket.create = function create(properties) {
    return new WsPacket(properties);
  };

  /**
   * Encodes the specified WsPacket message. Does not implicitly {@link WsPacket.verify|verify} messages.
   * @function encode
   * @memberof WsPacket
   * @static
   * @param {IWsPacket} message WsPacket message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WsPacket.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.majorVersion != null &&
      Object.hasOwnProperty.call(message, "majorVersion")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.majorVersion);
    if (
      message.minorVersion != null &&
      Object.hasOwnProperty.call(message, "minorVersion")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.minorVersion);
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.deviceId);
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).uint32(message.moduleId);
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 5, wireType 0 =*/ 40).uint32(message.cmd);
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      writer.uint32(/* id 6, wireType 0 =*/ 48).uint32(message.type);
    if (message.data != null && Object.hasOwnProperty.call(message, "data"))
      writer.uint32(/* id 7, wireType 2 =*/ 58).bytes(message.data);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 8, wireType 2 =*/ 66).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified WsPacket message, length delimited. Does not implicitly {@link WsPacket.verify|verify} messages.
   * @function encodeDelimited
   * @memberof WsPacket
   * @static
   * @param {IWsPacket} message WsPacket message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WsPacket.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a WsPacket message from the specified reader or buffer.
   * @function decode
   * @memberof WsPacket
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {WsPacket} WsPacket
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WsPacket.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.WsPacket();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.majorVersion = reader.uint32();
          break;
        }
        case 2: {
          message.minorVersion = reader.uint32();
          break;
        }
        case 3: {
          message.deviceId = reader.uint32();
          break;
        }
        case 4: {
          message.moduleId = reader.uint32();
          break;
        }
        case 5: {
          message.cmd = reader.uint32();
          break;
        }
        case 6: {
          message.type = reader.uint32();
          break;
        }
        case 7: {
          message.data = reader.bytes();
          break;
        }
        case 8: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a WsPacket message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof WsPacket
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {WsPacket} WsPacket
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WsPacket.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a WsPacket message.
   * @function verify
   * @memberof WsPacket
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  WsPacket.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.majorVersion != null &&
      Object.hasOwnProperty.call(message, "majorVersion")
    )
      if (!$util.isInteger(message.majorVersion))
        return "majorVersion: integer expected";
    if (
      message.minorVersion != null &&
      Object.hasOwnProperty.call(message, "minorVersion")
    )
      if (!$util.isInteger(message.minorVersion))
        return "minorVersion: integer expected";
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      if (!$util.isInteger(message.deviceId))
        return "deviceId: integer expected";
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      if (!$util.isInteger(message.moduleId))
        return "moduleId: integer expected";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      if (!$util.isInteger(message.type)) return "type: integer expected";
    if (message.data != null && Object.hasOwnProperty.call(message, "data"))
      if (!(
        (message.data && typeof message.data.length === "number") ||
        $util.isString(message.data)
      ))
        return "data: buffer expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a WsPacket message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof WsPacket
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {WsPacket} WsPacket
   */
  WsPacket.fromObject = function fromObject(object, long) {
    if (object instanceof $root.WsPacket) return object;
    if (!$util.isObject(object)) throw TypeError(".WsPacket: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.WsPacket();
    if (object.majorVersion != null)
      message.majorVersion = object.majorVersion >>> 0;
    if (object.minorVersion != null)
      message.minorVersion = object.minorVersion >>> 0;
    if (object.deviceId != null) message.deviceId = object.deviceId >>> 0;
    if (object.moduleId != null) message.moduleId = object.moduleId >>> 0;
    if (object.cmd != null) message.cmd = object.cmd >>> 0;
    if (object.type != null) message.type = object.type >>> 0;
    if (object.data != null)
      if (typeof object.data === "string")
        $util.base64.decode(
          object.data,
          (message.data = $util.newBuffer($util.base64.length(object.data))),
          0,
        );
      else if (object.data.length >= 0) message.data = object.data;
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a WsPacket message. Also converts values to other types if specified.
   * @function toObject
   * @memberof WsPacket
   * @static
   * @param {WsPacket} message WsPacket
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  WsPacket.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.majorVersion = 0;
      object.minorVersion = 0;
      object.deviceId = 0;
      object.moduleId = 0;
      object.cmd = 0;
      object.type = 0;
      if (options.bytes === String) object.data = "";
      else {
        object.data = [];
        if (options.bytes !== Array) object.data = $util.newBuffer(object.data);
      }
      object.clientId = "";
    }
    if (
      message.majorVersion != null &&
      Object.hasOwnProperty.call(message, "majorVersion")
    )
      object.majorVersion = message.majorVersion;
    if (
      message.minorVersion != null &&
      Object.hasOwnProperty.call(message, "minorVersion")
    )
      object.minorVersion = message.minorVersion;
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      object.deviceId = message.deviceId;
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      object.moduleId = message.moduleId;
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      object.type = message.type;
    if (message.data != null && Object.hasOwnProperty.call(message, "data"))
      object.data =
        options.bytes === String
          ? $util.base64.encode(message.data, 0, message.data.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.data)
            : message.data;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this WsPacket to JSON.
   * @function toJSON
   * @memberof WsPacket
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  WsPacket.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for WsPacket
   * @function getTypeUrl
   * @memberof WsPacket
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  WsPacket.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/WsPacket";
  };

  return WsPacket;
})();

$root.ComResponse = (function () {
  /**
   * Properties of a ComResponse.
   * @exports IComResponse
   * @interface IComResponse
   * @property {number|null} [code] ComResponse code
   */

  /**
   * Constructs a new ComResponse.
   * @exports ComResponse
   * @classdesc Represents a ComResponse.
   * @implements IComResponse
   * @constructor
   * @param {IComResponse=} [properties] Properties to set
   */
  function ComResponse(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ComResponse code.
   * @member {number} code
   * @memberof ComResponse
   * @instance
   */
  ComResponse.prototype.code = 0;

  /**
   * Creates a new ComResponse instance using the specified properties.
   * @function create
   * @memberof ComResponse
   * @static
   * @param {IComResponse=} [properties] Properties to set
   * @returns {ComResponse} ComResponse instance
   */
  ComResponse.create = function create(properties) {
    return new ComResponse(properties);
  };

  /**
   * Encodes the specified ComResponse message. Does not implicitly {@link ComResponse.verify|verify} messages.
   * @function encode
   * @memberof ComResponse
   * @static
   * @param {IComResponse} message ComResponse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResponse.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ComResponse message, length delimited. Does not implicitly {@link ComResponse.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ComResponse
   * @static
   * @param {IComResponse} message ComResponse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResponse.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ComResponse message from the specified reader or buffer.
   * @function decode
   * @memberof ComResponse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ComResponse} ComResponse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResponse.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ComResponse();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ComResponse message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ComResponse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ComResponse} ComResponse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResponse.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ComResponse message.
   * @function verify
   * @memberof ComResponse
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ComResponse.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ComResponse message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ComResponse
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ComResponse} ComResponse
   */
  ComResponse.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ComResponse) return object;
    if (!$util.isObject(object))
      throw TypeError(".ComResponse: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ComResponse();
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ComResponse message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ComResponse
   * @static
   * @param {ComResponse} message ComResponse
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ComResponse.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.code = 0;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ComResponse to JSON.
   * @function toJSON
   * @memberof ComResponse
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ComResponse.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ComResponse
   * @function getTypeUrl
   * @memberof ComResponse
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ComResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ComResponse";
  };

  return ComResponse;
})();

$root.ComResWithInt = (function () {
  /**
   * Properties of a ComResWithInt.
   * @exports IComResWithInt
   * @interface IComResWithInt
   * @property {number|null} [value] ComResWithInt value
   * @property {number|null} [code] ComResWithInt code
   */

  /**
   * Constructs a new ComResWithInt.
   * @exports ComResWithInt
   * @classdesc Represents a ComResWithInt.
   * @implements IComResWithInt
   * @constructor
   * @param {IComResWithInt=} [properties] Properties to set
   */
  function ComResWithInt(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ComResWithInt value.
   * @member {number} value
   * @memberof ComResWithInt
   * @instance
   */
  ComResWithInt.prototype.value = 0;

  /**
   * ComResWithInt code.
   * @member {number} code
   * @memberof ComResWithInt
   * @instance
   */
  ComResWithInt.prototype.code = 0;

  /**
   * Creates a new ComResWithInt instance using the specified properties.
   * @function create
   * @memberof ComResWithInt
   * @static
   * @param {IComResWithInt=} [properties] Properties to set
   * @returns {ComResWithInt} ComResWithInt instance
   */
  ComResWithInt.create = function create(properties) {
    return new ComResWithInt(properties);
  };

  /**
   * Encodes the specified ComResWithInt message. Does not implicitly {@link ComResWithInt.verify|verify} messages.
   * @function encode
   * @memberof ComResWithInt
   * @static
   * @param {IComResWithInt} message ComResWithInt message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithInt.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ComResWithInt message, length delimited. Does not implicitly {@link ComResWithInt.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ComResWithInt
   * @static
   * @param {IComResWithInt} message ComResWithInt message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithInt.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ComResWithInt message from the specified reader or buffer.
   * @function decode
   * @memberof ComResWithInt
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ComResWithInt} ComResWithInt
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithInt.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ComResWithInt();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ComResWithInt message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ComResWithInt
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ComResWithInt} ComResWithInt
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithInt.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ComResWithInt message.
   * @function verify
   * @memberof ComResWithInt
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ComResWithInt.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ComResWithInt message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ComResWithInt
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ComResWithInt} ComResWithInt
   */
  ComResWithInt.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ComResWithInt) return object;
    if (!$util.isObject(object))
      throw TypeError(".ComResWithInt: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ComResWithInt();
    if (object.value != null) message.value = object.value | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ComResWithInt message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ComResWithInt
   * @static
   * @param {ComResWithInt} message ComResWithInt
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ComResWithInt.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.value = 0;
      object.code = 0;
    }
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ComResWithInt to JSON.
   * @function toJSON
   * @memberof ComResWithInt
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ComResWithInt.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ComResWithInt
   * @function getTypeUrl
   * @memberof ComResWithInt
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ComResWithInt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ComResWithInt";
  };

  return ComResWithInt;
})();

$root.ComResWithDouble = (function () {
  /**
   * Properties of a ComResWithDouble.
   * @exports IComResWithDouble
   * @interface IComResWithDouble
   * @property {number|null} [value] ComResWithDouble value
   * @property {number|null} [code] ComResWithDouble code
   */

  /**
   * Constructs a new ComResWithDouble.
   * @exports ComResWithDouble
   * @classdesc Represents a ComResWithDouble.
   * @implements IComResWithDouble
   * @constructor
   * @param {IComResWithDouble=} [properties] Properties to set
   */
  function ComResWithDouble(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ComResWithDouble value.
   * @member {number} value
   * @memberof ComResWithDouble
   * @instance
   */
  ComResWithDouble.prototype.value = 0;

  /**
   * ComResWithDouble code.
   * @member {number} code
   * @memberof ComResWithDouble
   * @instance
   */
  ComResWithDouble.prototype.code = 0;

  /**
   * Creates a new ComResWithDouble instance using the specified properties.
   * @function create
   * @memberof ComResWithDouble
   * @static
   * @param {IComResWithDouble=} [properties] Properties to set
   * @returns {ComResWithDouble} ComResWithDouble instance
   */
  ComResWithDouble.create = function create(properties) {
    return new ComResWithDouble(properties);
  };

  /**
   * Encodes the specified ComResWithDouble message. Does not implicitly {@link ComResWithDouble.verify|verify} messages.
   * @function encode
   * @memberof ComResWithDouble
   * @static
   * @param {IComResWithDouble} message ComResWithDouble message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithDouble.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.value);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ComResWithDouble message, length delimited. Does not implicitly {@link ComResWithDouble.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ComResWithDouble
   * @static
   * @param {IComResWithDouble} message ComResWithDouble message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithDouble.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ComResWithDouble message from the specified reader or buffer.
   * @function decode
   * @memberof ComResWithDouble
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ComResWithDouble} ComResWithDouble
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithDouble.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ComResWithDouble();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.double();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ComResWithDouble message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ComResWithDouble
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ComResWithDouble} ComResWithDouble
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithDouble.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ComResWithDouble message.
   * @function verify
   * @memberof ComResWithDouble
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ComResWithDouble.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (typeof message.value !== "number") return "value: number expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ComResWithDouble message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ComResWithDouble
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ComResWithDouble} ComResWithDouble
   */
  ComResWithDouble.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ComResWithDouble) return object;
    if (!$util.isObject(object))
      throw TypeError(".ComResWithDouble: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ComResWithDouble();
    if (object.value != null) message.value = Number(object.value);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ComResWithDouble message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ComResWithDouble
   * @static
   * @param {ComResWithDouble} message ComResWithDouble
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ComResWithDouble.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.value = 0;
      object.code = 0;
    }
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value =
        options.json && !isFinite(message.value)
          ? String(message.value)
          : message.value;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ComResWithDouble to JSON.
   * @function toJSON
   * @memberof ComResWithDouble
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ComResWithDouble.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ComResWithDouble
   * @function getTypeUrl
   * @memberof ComResWithDouble
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ComResWithDouble.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ComResWithDouble";
  };

  return ComResWithDouble;
})();

$root.ComResWithString = (function () {
  /**
   * Properties of a ComResWithString.
   * @exports IComResWithString
   * @interface IComResWithString
   * @property {string|null} [str] ComResWithString str
   * @property {number|null} [code] ComResWithString code
   */

  /**
   * Constructs a new ComResWithString.
   * @exports ComResWithString
   * @classdesc Represents a ComResWithString.
   * @implements IComResWithString
   * @constructor
   * @param {IComResWithString=} [properties] Properties to set
   */
  function ComResWithString(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ComResWithString str.
   * @member {string} str
   * @memberof ComResWithString
   * @instance
   */
  ComResWithString.prototype.str = "";

  /**
   * ComResWithString code.
   * @member {number} code
   * @memberof ComResWithString
   * @instance
   */
  ComResWithString.prototype.code = 0;

  /**
   * Creates a new ComResWithString instance using the specified properties.
   * @function create
   * @memberof ComResWithString
   * @static
   * @param {IComResWithString=} [properties] Properties to set
   * @returns {ComResWithString} ComResWithString instance
   */
  ComResWithString.create = function create(properties) {
    return new ComResWithString(properties);
  };

  /**
   * Encodes the specified ComResWithString message. Does not implicitly {@link ComResWithString.verify|verify} messages.
   * @function encode
   * @memberof ComResWithString
   * @static
   * @param {IComResWithString} message ComResWithString message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithString.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.str != null && Object.hasOwnProperty.call(message, "str"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.str);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ComResWithString message, length delimited. Does not implicitly {@link ComResWithString.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ComResWithString
   * @static
   * @param {IComResWithString} message ComResWithString message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComResWithString.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ComResWithString message from the specified reader or buffer.
   * @function decode
   * @memberof ComResWithString
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ComResWithString} ComResWithString
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithString.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ComResWithString();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.str = reader.string();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ComResWithString message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ComResWithString
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ComResWithString} ComResWithString
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComResWithString.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ComResWithString message.
   * @function verify
   * @memberof ComResWithString
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ComResWithString.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.str != null && Object.hasOwnProperty.call(message, "str"))
      if (!$util.isString(message.str)) return "str: string expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ComResWithString message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ComResWithString
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ComResWithString} ComResWithString
   */
  ComResWithString.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ComResWithString) return object;
    if (!$util.isObject(object))
      throw TypeError(".ComResWithString: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ComResWithString();
    if (object.str != null) message.str = String(object.str);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ComResWithString message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ComResWithString
   * @static
   * @param {ComResWithString} message ComResWithString
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ComResWithString.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.str = "";
      object.code = 0;
    }
    if (message.str != null && Object.hasOwnProperty.call(message, "str"))
      object.str = message.str;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ComResWithString to JSON.
   * @function toJSON
   * @memberof ComResWithString
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ComResWithString.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ComResWithString
   * @function getTypeUrl
   * @memberof ComResWithString
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ComResWithString.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ComResWithString";
  };

  return ComResWithString;
})();

$root.CommonParam = (function () {
  /**
   * Properties of a CommonParam.
   * @exports ICommonParam
   * @interface ICommonParam
   * @property {boolean|null} [hasAuto] CommonParam hasAuto
   * @property {number|null} [autoMode] CommonParam autoMode
   * @property {number|null} [id] CommonParam id
   * @property {number|null} [modeIndex] CommonParam modeIndex
   * @property {number|null} [index] CommonParam index
   * @property {number|null} [continueValue] CommonParam continueValue
   */

  /**
   * Constructs a new CommonParam.
   * @exports CommonParam
   * @classdesc Represents a CommonParam.
   * @implements ICommonParam
   * @constructor
   * @param {ICommonParam=} [properties] Properties to set
   */
  function CommonParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * CommonParam hasAuto.
   * @member {boolean} hasAuto
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.hasAuto = false;

  /**
   * CommonParam autoMode.
   * @member {number} autoMode
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.autoMode = 0;

  /**
   * CommonParam id.
   * @member {number} id
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.id = 0;

  /**
   * CommonParam modeIndex.
   * @member {number} modeIndex
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.modeIndex = 0;

  /**
   * CommonParam index.
   * @member {number} index
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.index = 0;

  /**
   * CommonParam continueValue.
   * @member {number} continueValue
   * @memberof CommonParam
   * @instance
   */
  CommonParam.prototype.continueValue = 0;

  /**
   * Creates a new CommonParam instance using the specified properties.
   * @function create
   * @memberof CommonParam
   * @static
   * @param {ICommonParam=} [properties] Properties to set
   * @returns {CommonParam} CommonParam instance
   */
  CommonParam.create = function create(properties) {
    return new CommonParam(properties);
  };

  /**
   * Encodes the specified CommonParam message. Does not implicitly {@link CommonParam.verify|verify} messages.
   * @function encode
   * @memberof CommonParam
   * @static
   * @param {ICommonParam} message CommonParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CommonParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.hasAuto != null &&
      Object.hasOwnProperty.call(message, "hasAuto")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.hasAuto);
    if (
      message.autoMode != null &&
      Object.hasOwnProperty.call(message, "autoMode")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.autoMode);
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.id);
    if (
      message.modeIndex != null &&
      Object.hasOwnProperty.call(message, "modeIndex")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.modeIndex);
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.index);
    if (
      message.continueValue != null &&
      Object.hasOwnProperty.call(message, "continueValue")
    )
      writer.uint32(/* id 6, wireType 1 =*/ 49).double(message.continueValue);
    return writer;
  };

  /**
   * Encodes the specified CommonParam message, length delimited. Does not implicitly {@link CommonParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof CommonParam
   * @static
   * @param {ICommonParam} message CommonParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  CommonParam.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a CommonParam message from the specified reader or buffer.
   * @function decode
   * @memberof CommonParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {CommonParam} CommonParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CommonParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.CommonParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.hasAuto = reader.bool();
          break;
        }
        case 2: {
          message.autoMode = reader.int32();
          break;
        }
        case 3: {
          message.id = reader.int32();
          break;
        }
        case 4: {
          message.modeIndex = reader.int32();
          break;
        }
        case 5: {
          message.index = reader.int32();
          break;
        }
        case 6: {
          message.continueValue = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a CommonParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof CommonParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {CommonParam} CommonParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  CommonParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a CommonParam message.
   * @function verify
   * @memberof CommonParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  CommonParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.hasAuto != null &&
      Object.hasOwnProperty.call(message, "hasAuto")
    )
      if (typeof message.hasAuto !== "boolean")
        return "hasAuto: boolean expected";
    if (
      message.autoMode != null &&
      Object.hasOwnProperty.call(message, "autoMode")
    )
      if (!$util.isInteger(message.autoMode))
        return "autoMode: integer expected";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (
      message.modeIndex != null &&
      Object.hasOwnProperty.call(message, "modeIndex")
    )
      if (!$util.isInteger(message.modeIndex))
        return "modeIndex: integer expected";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    if (
      message.continueValue != null &&
      Object.hasOwnProperty.call(message, "continueValue")
    )
      if (typeof message.continueValue !== "number")
        return "continueValue: number expected";
    return null;
  };

  /**
   * Creates a CommonParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof CommonParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {CommonParam} CommonParam
   */
  CommonParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.CommonParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".CommonParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.CommonParam();
    if (object.hasAuto != null) message.hasAuto = Boolean(object.hasAuto);
    if (object.autoMode != null) message.autoMode = object.autoMode | 0;
    if (object.id != null) message.id = object.id | 0;
    if (object.modeIndex != null) message.modeIndex = object.modeIndex | 0;
    if (object.index != null) message.index = object.index | 0;
    if (object.continueValue != null)
      message.continueValue = Number(object.continueValue);
    return message;
  };

  /**
   * Creates a plain object from a CommonParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof CommonParam
   * @static
   * @param {CommonParam} message CommonParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  CommonParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.hasAuto = false;
      object.autoMode = 0;
      object.id = 0;
      object.modeIndex = 0;
      object.index = 0;
      object.continueValue = 0;
    }
    if (
      message.hasAuto != null &&
      Object.hasOwnProperty.call(message, "hasAuto")
    )
      object.hasAuto = message.hasAuto;
    if (
      message.autoMode != null &&
      Object.hasOwnProperty.call(message, "autoMode")
    )
      object.autoMode = message.autoMode;
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.modeIndex != null &&
      Object.hasOwnProperty.call(message, "modeIndex")
    )
      object.modeIndex = message.modeIndex;
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    if (
      message.continueValue != null &&
      Object.hasOwnProperty.call(message, "continueValue")
    )
      object.continueValue =
        options.json && !isFinite(message.continueValue)
          ? String(message.continueValue)
          : message.continueValue;
    return object;
  };

  /**
   * Converts this CommonParam to JSON.
   * @function toJSON
   * @memberof CommonParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  CommonParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for CommonParam
   * @function getTypeUrl
   * @memberof CommonParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  CommonParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/CommonParam";
  };

  return CommonParam;
})();

/**
 * VocalType enum.
 * @exports VocalType
 * @enum {number}
 * @property {number} VT_UNKNOWN=0 VT_UNKNOWN value
 * @property {number} VT_PING=1 VT_PING value
 * @property {number} VT_ECHO=2 VT_ECHO value
 */
$root.VocalType = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "VT_UNKNOWN")] = 0;
  values[(valuesById[1] = "VT_PING")] = 1;
  values[(valuesById[2] = "VT_ECHO")] = 2;
  return values;
})();

$root.ReqGetconfig = (function () {
  /**
   * Properties of a ReqGetconfig.
   * @exports IReqGetconfig
   * @interface IReqGetconfig
   * @property {number|null} [cmd] ReqGetconfig cmd
   * @property {string|null} [blePsd] ReqGetconfig blePsd
   * @property {string|null} [clientId] ReqGetconfig clientId
   */

  /**
   * Constructs a new ReqGetconfig.
   * @exports ReqGetconfig
   * @classdesc Represents a ReqGetconfig.
   * @implements IReqGetconfig
   * @constructor
   * @param {IReqGetconfig=} [properties] Properties to set
   */
  function ReqGetconfig(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetconfig cmd.
   * @member {number} cmd
   * @memberof ReqGetconfig
   * @instance
   */
  ReqGetconfig.prototype.cmd = 0;

  /**
   * ReqGetconfig blePsd.
   * @member {string} blePsd
   * @memberof ReqGetconfig
   * @instance
   */
  ReqGetconfig.prototype.blePsd = "";

  /**
   * ReqGetconfig clientId.
   * @member {string} clientId
   * @memberof ReqGetconfig
   * @instance
   */
  ReqGetconfig.prototype.clientId = "";

  /**
   * Creates a new ReqGetconfig instance using the specified properties.
   * @function create
   * @memberof ReqGetconfig
   * @static
   * @param {IReqGetconfig=} [properties] Properties to set
   * @returns {ReqGetconfig} ReqGetconfig instance
   */
  ReqGetconfig.create = function create(properties) {
    return new ReqGetconfig(properties);
  };

  /**
   * Encodes the specified ReqGetconfig message. Does not implicitly {@link ReqGetconfig.verify|verify} messages.
   * @function encode
   * @memberof ReqGetconfig
   * @static
   * @param {IReqGetconfig} message ReqGetconfig message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetconfig.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.blePsd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqGetconfig message, length delimited. Does not implicitly {@link ReqGetconfig.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetconfig
   * @static
   * @param {IReqGetconfig} message ReqGetconfig message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetconfig.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetconfig message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetconfig
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetconfig} ReqGetconfig
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetconfig.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetconfig();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.blePsd = reader.string();
          break;
        }
        case 3: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetconfig message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetconfig
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetconfig} ReqGetconfig
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetconfig.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetconfig message.
   * @function verify
   * @memberof ReqGetconfig
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetconfig.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      if (!$util.isString(message.blePsd)) return "blePsd: string expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqGetconfig message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetconfig
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetconfig} ReqGetconfig
   */
  ReqGetconfig.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetconfig) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetconfig: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetconfig();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.blePsd != null) message.blePsd = String(object.blePsd);
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetconfig message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetconfig
   * @static
   * @param {ReqGetconfig} message ReqGetconfig
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetconfig.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.blePsd = "";
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      object.blePsd = message.blePsd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqGetconfig to JSON.
   * @function toJSON
   * @memberof ReqGetconfig
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetconfig.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetconfig
   * @function getTypeUrl
   * @memberof ReqGetconfig
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetconfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetconfig";
  };

  return ReqGetconfig;
})();

$root.ReqAp = (function () {
  /**
   * Properties of a ReqAp.
   * @exports IReqAp
   * @interface IReqAp
   * @property {number|null} [cmd] ReqAp cmd
   * @property {number|null} [wifiType] ReqAp wifiType
   * @property {number|null} [autoStart] ReqAp autoStart
   * @property {number|null} [countryList] ReqAp countryList
   * @property {string|null} [country] ReqAp country
   * @property {string|null} [blePsd] ReqAp blePsd
   * @property {string|null} [clientId] ReqAp clientId
   * @property {boolean|null} [forceRestart] ReqAp forceRestart
   */

  /**
   * Constructs a new ReqAp.
   * @exports ReqAp
   * @classdesc Represents a ReqAp.
   * @implements IReqAp
   * @constructor
   * @param {IReqAp=} [properties] Properties to set
   */
  function ReqAp(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqAp cmd.
   * @member {number} cmd
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.cmd = 0;

  /**
   * ReqAp wifiType.
   * @member {number} wifiType
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.wifiType = 0;

  /**
   * ReqAp autoStart.
   * @member {number} autoStart
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.autoStart = 0;

  /**
   * ReqAp countryList.
   * @member {number} countryList
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.countryList = 0;

  /**
   * ReqAp country.
   * @member {string} country
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.country = "";

  /**
   * ReqAp blePsd.
   * @member {string} blePsd
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.blePsd = "";

  /**
   * ReqAp clientId.
   * @member {string} clientId
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.clientId = "";

  /**
   * ReqAp forceRestart.
   * @member {boolean} forceRestart
   * @memberof ReqAp
   * @instance
   */
  ReqAp.prototype.forceRestart = false;

  /**
   * Creates a new ReqAp instance using the specified properties.
   * @function create
   * @memberof ReqAp
   * @static
   * @param {IReqAp=} [properties] Properties to set
   * @returns {ReqAp} ReqAp instance
   */
  ReqAp.create = function create(properties) {
    return new ReqAp(properties);
  };

  /**
   * Encodes the specified ReqAp message. Does not implicitly {@link ReqAp.verify|verify} messages.
   * @function encode
   * @memberof ReqAp
   * @static
   * @param {IReqAp} message ReqAp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAp.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.wifiType != null &&
      Object.hasOwnProperty.call(message, "wifiType")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.wifiType);
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.autoStart);
    if (
      message.countryList != null &&
      Object.hasOwnProperty.call(message, "countryList")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.countryList);
    if (
      message.country != null &&
      Object.hasOwnProperty.call(message, "country")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.country);
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.blePsd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.clientId);
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).bool(message.forceRestart);
    return writer;
  };

  /**
   * Encodes the specified ReqAp message, length delimited. Does not implicitly {@link ReqAp.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqAp
   * @static
   * @param {IReqAp} message ReqAp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAp.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqAp message from the specified reader or buffer.
   * @function decode
   * @memberof ReqAp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqAp} ReqAp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAp.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqAp();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.wifiType = reader.int32();
          break;
        }
        case 3: {
          message.autoStart = reader.int32();
          break;
        }
        case 4: {
          message.countryList = reader.int32();
          break;
        }
        case 5: {
          message.country = reader.string();
          break;
        }
        case 6: {
          message.blePsd = reader.string();
          break;
        }
        case 7: {
          message.clientId = reader.string();
          break;
        }
        case 8: {
          message.forceRestart = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqAp message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqAp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqAp} ReqAp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAp.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqAp message.
   * @function verify
   * @memberof ReqAp
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqAp.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.wifiType != null &&
      Object.hasOwnProperty.call(message, "wifiType")
    )
      if (!$util.isInteger(message.wifiType))
        return "wifiType: integer expected";
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      if (!$util.isInteger(message.autoStart))
        return "autoStart: integer expected";
    if (
      message.countryList != null &&
      Object.hasOwnProperty.call(message, "countryList")
    )
      if (!$util.isInteger(message.countryList))
        return "countryList: integer expected";
    if (
      message.country != null &&
      Object.hasOwnProperty.call(message, "country")
    )
      if (!$util.isString(message.country)) return "country: string expected";
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      if (!$util.isString(message.blePsd)) return "blePsd: string expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      if (typeof message.forceRestart !== "boolean")
        return "forceRestart: boolean expected";
    return null;
  };

  /**
   * Creates a ReqAp message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqAp
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqAp} ReqAp
   */
  ReqAp.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqAp) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqAp: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqAp();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.wifiType != null) message.wifiType = object.wifiType | 0;
    if (object.autoStart != null) message.autoStart = object.autoStart | 0;
    if (object.countryList != null)
      message.countryList = object.countryList | 0;
    if (object.country != null) message.country = String(object.country);
    if (object.blePsd != null) message.blePsd = String(object.blePsd);
    if (object.clientId != null) message.clientId = String(object.clientId);
    if (object.forceRestart != null)
      message.forceRestart = Boolean(object.forceRestart);
    return message;
  };

  /**
   * Creates a plain object from a ReqAp message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqAp
   * @static
   * @param {ReqAp} message ReqAp
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqAp.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.wifiType = 0;
      object.autoStart = 0;
      object.countryList = 0;
      object.country = "";
      object.blePsd = "";
      object.clientId = "";
      object.forceRestart = false;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.wifiType != null &&
      Object.hasOwnProperty.call(message, "wifiType")
    )
      object.wifiType = message.wifiType;
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      object.autoStart = message.autoStart;
    if (
      message.countryList != null &&
      Object.hasOwnProperty.call(message, "countryList")
    )
      object.countryList = message.countryList;
    if (
      message.country != null &&
      Object.hasOwnProperty.call(message, "country")
    )
      object.country = message.country;
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      object.blePsd = message.blePsd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    if (
      message.forceRestart != null &&
      Object.hasOwnProperty.call(message, "forceRestart")
    )
      object.forceRestart = message.forceRestart;
    return object;
  };

  /**
   * Converts this ReqAp to JSON.
   * @function toJSON
   * @memberof ReqAp
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqAp.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqAp
   * @function getTypeUrl
   * @memberof ReqAp
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqAp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqAp";
  };

  return ReqAp;
})();

$root.ReqSta = (function () {
  /**
   * Properties of a ReqSta.
   * @exports IReqSta
   * @interface IReqSta
   * @property {number|null} [cmd] ReqSta cmd
   * @property {number|null} [autoStart] ReqSta autoStart
   * @property {string|null} [blePsd] ReqSta blePsd
   * @property {string|null} [ssid] ReqSta ssid
   * @property {string|null} [psd] ReqSta psd
   * @property {string|null} [clientId] ReqSta clientId
   */

  /**
   * Constructs a new ReqSta.
   * @exports ReqSta
   * @classdesc Represents a ReqSta.
   * @implements IReqSta
   * @constructor
   * @param {IReqSta=} [properties] Properties to set
   */
  function ReqSta(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSta cmd.
   * @member {number} cmd
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.cmd = 0;

  /**
   * ReqSta autoStart.
   * @member {number} autoStart
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.autoStart = 0;

  /**
   * ReqSta blePsd.
   * @member {string} blePsd
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.blePsd = "";

  /**
   * ReqSta ssid.
   * @member {string} ssid
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.ssid = "";

  /**
   * ReqSta psd.
   * @member {string} psd
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.psd = "";

  /**
   * ReqSta clientId.
   * @member {string} clientId
   * @memberof ReqSta
   * @instance
   */
  ReqSta.prototype.clientId = "";

  /**
   * Creates a new ReqSta instance using the specified properties.
   * @function create
   * @memberof ReqSta
   * @static
   * @param {IReqSta=} [properties] Properties to set
   * @returns {ReqSta} ReqSta instance
   */
  ReqSta.create = function create(properties) {
    return new ReqSta(properties);
  };

  /**
   * Encodes the specified ReqSta message. Does not implicitly {@link ReqSta.verify|verify} messages.
   * @function encode
   * @memberof ReqSta
   * @static
   * @param {IReqSta} message ReqSta message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSta.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.autoStart);
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.blePsd);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.ssid);
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.psd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqSta message, length delimited. Does not implicitly {@link ReqSta.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSta
   * @static
   * @param {IReqSta} message ReqSta message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSta.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSta message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSta
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSta} ReqSta
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSta.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSta();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.autoStart = reader.int32();
          break;
        }
        case 3: {
          message.blePsd = reader.string();
          break;
        }
        case 4: {
          message.ssid = reader.string();
          break;
        }
        case 5: {
          message.psd = reader.string();
          break;
        }
        case 6: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSta message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSta
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSta} ReqSta
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSta.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSta message.
   * @function verify
   * @memberof ReqSta
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSta.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      if (!$util.isInteger(message.autoStart))
        return "autoStart: integer expected";
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      if (!$util.isString(message.blePsd)) return "blePsd: string expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      if (!$util.isString(message.psd)) return "psd: string expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqSta message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSta
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSta} ReqSta
   */
  ReqSta.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSta) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqSta: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSta();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.autoStart != null) message.autoStart = object.autoStart | 0;
    if (object.blePsd != null) message.blePsd = String(object.blePsd);
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.psd != null) message.psd = String(object.psd);
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqSta message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSta
   * @static
   * @param {ReqSta} message ReqSta
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSta.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.autoStart = 0;
      object.blePsd = "";
      object.ssid = "";
      object.psd = "";
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      object.autoStart = message.autoStart;
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      object.blePsd = message.blePsd;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      object.psd = message.psd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqSta to JSON.
   * @function toJSON
   * @memberof ReqSta
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSta.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSta
   * @function getTypeUrl
   * @memberof ReqSta
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSta.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSta";
  };

  return ReqSta;
})();

$root.ReqSetblewifi = (function () {
  /**
   * Properties of a ReqSetblewifi.
   * @exports IReqSetblewifi
   * @interface IReqSetblewifi
   * @property {number|null} [cmd] ReqSetblewifi cmd
   * @property {number|null} [mode] ReqSetblewifi mode
   * @property {string|null} [blePsd] ReqSetblewifi blePsd
   * @property {string|null} [value] ReqSetblewifi value
   * @property {string|null} [clientId] ReqSetblewifi clientId
   */

  /**
   * Constructs a new ReqSetblewifi.
   * @exports ReqSetblewifi
   * @classdesc Represents a ReqSetblewifi.
   * @implements IReqSetblewifi
   * @constructor
   * @param {IReqSetblewifi=} [properties] Properties to set
   */
  function ReqSetblewifi(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetblewifi cmd.
   * @member {number} cmd
   * @memberof ReqSetblewifi
   * @instance
   */
  ReqSetblewifi.prototype.cmd = 0;

  /**
   * ReqSetblewifi mode.
   * @member {number} mode
   * @memberof ReqSetblewifi
   * @instance
   */
  ReqSetblewifi.prototype.mode = 0;

  /**
   * ReqSetblewifi blePsd.
   * @member {string} blePsd
   * @memberof ReqSetblewifi
   * @instance
   */
  ReqSetblewifi.prototype.blePsd = "";

  /**
   * ReqSetblewifi value.
   * @member {string} value
   * @memberof ReqSetblewifi
   * @instance
   */
  ReqSetblewifi.prototype.value = "";

  /**
   * ReqSetblewifi clientId.
   * @member {string} clientId
   * @memberof ReqSetblewifi
   * @instance
   */
  ReqSetblewifi.prototype.clientId = "";

  /**
   * Creates a new ReqSetblewifi instance using the specified properties.
   * @function create
   * @memberof ReqSetblewifi
   * @static
   * @param {IReqSetblewifi=} [properties] Properties to set
   * @returns {ReqSetblewifi} ReqSetblewifi instance
   */
  ReqSetblewifi.create = function create(properties) {
    return new ReqSetblewifi(properties);
  };

  /**
   * Encodes the specified ReqSetblewifi message. Does not implicitly {@link ReqSetblewifi.verify|verify} messages.
   * @function encode
   * @memberof ReqSetblewifi
   * @static
   * @param {IReqSetblewifi} message ReqSetblewifi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetblewifi.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.blePsd);
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.value);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqSetblewifi message, length delimited. Does not implicitly {@link ReqSetblewifi.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetblewifi
   * @static
   * @param {IReqSetblewifi} message ReqSetblewifi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetblewifi.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetblewifi message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetblewifi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetblewifi} ReqSetblewifi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetblewifi.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetblewifi();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.mode = reader.int32();
          break;
        }
        case 3: {
          message.blePsd = reader.string();
          break;
        }
        case 4: {
          message.value = reader.string();
          break;
        }
        case 5: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetblewifi message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetblewifi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetblewifi} ReqSetblewifi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetblewifi.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetblewifi message.
   * @function verify
   * @memberof ReqSetblewifi
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetblewifi.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      if (!$util.isString(message.blePsd)) return "blePsd: string expected";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isString(message.value)) return "value: string expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqSetblewifi message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetblewifi
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetblewifi} ReqSetblewifi
   */
  ReqSetblewifi.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetblewifi) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetblewifi: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetblewifi();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.mode != null) message.mode = object.mode | 0;
    if (object.blePsd != null) message.blePsd = String(object.blePsd);
    if (object.value != null) message.value = String(object.value);
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetblewifi message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetblewifi
   * @static
   * @param {ReqSetblewifi} message ReqSetblewifi
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetblewifi.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.mode = 0;
      object.blePsd = "";
      object.value = "";
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (message.blePsd != null && Object.hasOwnProperty.call(message, "blePsd"))
      object.blePsd = message.blePsd;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqSetblewifi to JSON.
   * @function toJSON
   * @memberof ReqSetblewifi
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetblewifi.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetblewifi
   * @function getTypeUrl
   * @memberof ReqSetblewifi
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetblewifi.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetblewifi";
  };

  return ReqSetblewifi;
})();

$root.ReqReset = (function () {
  /**
   * Properties of a ReqReset.
   * @exports IReqReset
   * @interface IReqReset
   * @property {number|null} [cmd] ReqReset cmd
   * @property {string|null} [clientId] ReqReset clientId
   */

  /**
   * Constructs a new ReqReset.
   * @exports ReqReset
   * @classdesc Represents a ReqReset.
   * @implements IReqReset
   * @constructor
   * @param {IReqReset=} [properties] Properties to set
   */
  function ReqReset(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqReset cmd.
   * @member {number} cmd
   * @memberof ReqReset
   * @instance
   */
  ReqReset.prototype.cmd = 0;

  /**
   * ReqReset clientId.
   * @member {string} clientId
   * @memberof ReqReset
   * @instance
   */
  ReqReset.prototype.clientId = "";

  /**
   * Creates a new ReqReset instance using the specified properties.
   * @function create
   * @memberof ReqReset
   * @static
   * @param {IReqReset=} [properties] Properties to set
   * @returns {ReqReset} ReqReset instance
   */
  ReqReset.create = function create(properties) {
    return new ReqReset(properties);
  };

  /**
   * Encodes the specified ReqReset message. Does not implicitly {@link ReqReset.verify|verify} messages.
   * @function encode
   * @memberof ReqReset
   * @static
   * @param {IReqReset} message ReqReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReset.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqReset message, length delimited. Does not implicitly {@link ReqReset.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqReset
   * @static
   * @param {IReqReset} message ReqReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReset.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqReset message from the specified reader or buffer.
   * @function decode
   * @memberof ReqReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqReset} ReqReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReset.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqReset();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqReset message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqReset} ReqReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReset.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqReset message.
   * @function verify
   * @memberof ReqReset
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqReset.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqReset message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqReset
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqReset} ReqReset
   */
  ReqReset.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqReset) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqReset: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqReset();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqReset message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqReset
   * @static
   * @param {ReqReset} message ReqReset
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqReset.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqReset to JSON.
   * @function toJSON
   * @memberof ReqReset
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqReset.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqReset
   * @function getTypeUrl
   * @memberof ReqReset
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqReset.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqReset";
  };

  return ReqReset;
})();

$root.ReqGetwifilist = (function () {
  /**
   * Properties of a ReqGetwifilist.
   * @exports IReqGetwifilist
   * @interface IReqGetwifilist
   * @property {number|null} [cmd] ReqGetwifilist cmd
   * @property {string|null} [clientId] ReqGetwifilist clientId
   */

  /**
   * Constructs a new ReqGetwifilist.
   * @exports ReqGetwifilist
   * @classdesc Represents a ReqGetwifilist.
   * @implements IReqGetwifilist
   * @constructor
   * @param {IReqGetwifilist=} [properties] Properties to set
   */
  function ReqGetwifilist(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetwifilist cmd.
   * @member {number} cmd
   * @memberof ReqGetwifilist
   * @instance
   */
  ReqGetwifilist.prototype.cmd = 0;

  /**
   * ReqGetwifilist clientId.
   * @member {string} clientId
   * @memberof ReqGetwifilist
   * @instance
   */
  ReqGetwifilist.prototype.clientId = "";

  /**
   * Creates a new ReqGetwifilist instance using the specified properties.
   * @function create
   * @memberof ReqGetwifilist
   * @static
   * @param {IReqGetwifilist=} [properties] Properties to set
   * @returns {ReqGetwifilist} ReqGetwifilist instance
   */
  ReqGetwifilist.create = function create(properties) {
    return new ReqGetwifilist(properties);
  };

  /**
   * Encodes the specified ReqGetwifilist message. Does not implicitly {@link ReqGetwifilist.verify|verify} messages.
   * @function encode
   * @memberof ReqGetwifilist
   * @static
   * @param {IReqGetwifilist} message ReqGetwifilist message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetwifilist.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqGetwifilist message, length delimited. Does not implicitly {@link ReqGetwifilist.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetwifilist
   * @static
   * @param {IReqGetwifilist} message ReqGetwifilist message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetwifilist.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetwifilist message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetwifilist
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetwifilist} ReqGetwifilist
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetwifilist.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetwifilist();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetwifilist message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetwifilist
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetwifilist} ReqGetwifilist
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetwifilist.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetwifilist message.
   * @function verify
   * @memberof ReqGetwifilist
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetwifilist.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqGetwifilist message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetwifilist
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetwifilist} ReqGetwifilist
   */
  ReqGetwifilist.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetwifilist) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetwifilist: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetwifilist();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetwifilist message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetwifilist
   * @static
   * @param {ReqGetwifilist} message ReqGetwifilist
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetwifilist.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqGetwifilist to JSON.
   * @function toJSON
   * @memberof ReqGetwifilist
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetwifilist.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetwifilist
   * @function getTypeUrl
   * @memberof ReqGetwifilist
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetwifilist.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetwifilist";
  };

  return ReqGetwifilist;
})();

$root.ReqGetsysteminfo = (function () {
  /**
   * Properties of a ReqGetsysteminfo.
   * @exports IReqGetsysteminfo
   * @interface IReqGetsysteminfo
   * @property {number|null} [cmd] ReqGetsysteminfo cmd
   * @property {string|null} [clientId] ReqGetsysteminfo clientId
   */

  /**
   * Constructs a new ReqGetsysteminfo.
   * @exports ReqGetsysteminfo
   * @classdesc Represents a ReqGetsysteminfo.
   * @implements IReqGetsysteminfo
   * @constructor
   * @param {IReqGetsysteminfo=} [properties] Properties to set
   */
  function ReqGetsysteminfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetsysteminfo cmd.
   * @member {number} cmd
   * @memberof ReqGetsysteminfo
   * @instance
   */
  ReqGetsysteminfo.prototype.cmd = 0;

  /**
   * ReqGetsysteminfo clientId.
   * @member {string} clientId
   * @memberof ReqGetsysteminfo
   * @instance
   */
  ReqGetsysteminfo.prototype.clientId = "";

  /**
   * Creates a new ReqGetsysteminfo instance using the specified properties.
   * @function create
   * @memberof ReqGetsysteminfo
   * @static
   * @param {IReqGetsysteminfo=} [properties] Properties to set
   * @returns {ReqGetsysteminfo} ReqGetsysteminfo instance
   */
  ReqGetsysteminfo.create = function create(properties) {
    return new ReqGetsysteminfo(properties);
  };

  /**
   * Encodes the specified ReqGetsysteminfo message. Does not implicitly {@link ReqGetsysteminfo.verify|verify} messages.
   * @function encode
   * @memberof ReqGetsysteminfo
   * @static
   * @param {IReqGetsysteminfo} message ReqGetsysteminfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetsysteminfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.clientId);
    return writer;
  };

  /**
   * Encodes the specified ReqGetsysteminfo message, length delimited. Does not implicitly {@link ReqGetsysteminfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetsysteminfo
   * @static
   * @param {IReqGetsysteminfo} message ReqGetsysteminfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetsysteminfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetsysteminfo message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetsysteminfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetsysteminfo} ReqGetsysteminfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetsysteminfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetsysteminfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.clientId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetsysteminfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetsysteminfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetsysteminfo} ReqGetsysteminfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetsysteminfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetsysteminfo message.
   * @function verify
   * @memberof ReqGetsysteminfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetsysteminfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      if (!$util.isString(message.clientId)) return "clientId: string expected";
    return null;
  };

  /**
   * Creates a ReqGetsysteminfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetsysteminfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetsysteminfo} ReqGetsysteminfo
   */
  ReqGetsysteminfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetsysteminfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetsysteminfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetsysteminfo();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.clientId != null) message.clientId = String(object.clientId);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetsysteminfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetsysteminfo
   * @static
   * @param {ReqGetsysteminfo} message ReqGetsysteminfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetsysteminfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.clientId = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.clientId != null &&
      Object.hasOwnProperty.call(message, "clientId")
    )
      object.clientId = message.clientId;
    return object;
  };

  /**
   * Converts this ReqGetsysteminfo to JSON.
   * @function toJSON
   * @memberof ReqGetsysteminfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetsysteminfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetsysteminfo
   * @function getTypeUrl
   * @memberof ReqGetsysteminfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetsysteminfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetsysteminfo";
  };

  return ReqGetsysteminfo;
})();

$root.ReqCheckFile = (function () {
  /**
   * Properties of a ReqCheckFile.
   * @exports IReqCheckFile
   * @interface IReqCheckFile
   * @property {number|null} [cmd] ReqCheckFile cmd
   * @property {string|null} [filePath] ReqCheckFile filePath
   * @property {string|null} [md5] ReqCheckFile md5
   */

  /**
   * Constructs a new ReqCheckFile.
   * @exports ReqCheckFile
   * @classdesc Represents a ReqCheckFile.
   * @implements IReqCheckFile
   * @constructor
   * @param {IReqCheckFile=} [properties] Properties to set
   */
  function ReqCheckFile(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCheckFile cmd.
   * @member {number} cmd
   * @memberof ReqCheckFile
   * @instance
   */
  ReqCheckFile.prototype.cmd = 0;

  /**
   * ReqCheckFile filePath.
   * @member {string} filePath
   * @memberof ReqCheckFile
   * @instance
   */
  ReqCheckFile.prototype.filePath = "";

  /**
   * ReqCheckFile md5.
   * @member {string} md5
   * @memberof ReqCheckFile
   * @instance
   */
  ReqCheckFile.prototype.md5 = "";

  /**
   * Creates a new ReqCheckFile instance using the specified properties.
   * @function create
   * @memberof ReqCheckFile
   * @static
   * @param {IReqCheckFile=} [properties] Properties to set
   * @returns {ReqCheckFile} ReqCheckFile instance
   */
  ReqCheckFile.create = function create(properties) {
    return new ReqCheckFile(properties);
  };

  /**
   * Encodes the specified ReqCheckFile message. Does not implicitly {@link ReqCheckFile.verify|verify} messages.
   * @function encode
   * @memberof ReqCheckFile
   * @static
   * @param {IReqCheckFile} message ReqCheckFile message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCheckFile.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (
      message.filePath != null &&
      Object.hasOwnProperty.call(message, "filePath")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.filePath);
    if (message.md5 != null && Object.hasOwnProperty.call(message, "md5"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.md5);
    return writer;
  };

  /**
   * Encodes the specified ReqCheckFile message, length delimited. Does not implicitly {@link ReqCheckFile.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCheckFile
   * @static
   * @param {IReqCheckFile} message ReqCheckFile message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCheckFile.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCheckFile message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCheckFile
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCheckFile} ReqCheckFile
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCheckFile.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCheckFile();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.filePath = reader.string();
          break;
        }
        case 3: {
          message.md5 = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCheckFile message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCheckFile
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCheckFile} ReqCheckFile
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCheckFile.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCheckFile message.
   * @function verify
   * @memberof ReqCheckFile
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCheckFile.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (
      message.filePath != null &&
      Object.hasOwnProperty.call(message, "filePath")
    )
      if (!$util.isString(message.filePath)) return "filePath: string expected";
    if (message.md5 != null && Object.hasOwnProperty.call(message, "md5"))
      if (!$util.isString(message.md5)) return "md5: string expected";
    return null;
  };

  /**
   * Creates a ReqCheckFile message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCheckFile
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCheckFile} ReqCheckFile
   */
  ReqCheckFile.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCheckFile) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCheckFile: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCheckFile();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.filePath != null) message.filePath = String(object.filePath);
    if (object.md5 != null) message.md5 = String(object.md5);
    return message;
  };

  /**
   * Creates a plain object from a ReqCheckFile message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCheckFile
   * @static
   * @param {ReqCheckFile} message ReqCheckFile
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCheckFile.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.filePath = "";
      object.md5 = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (
      message.filePath != null &&
      Object.hasOwnProperty.call(message, "filePath")
    )
      object.filePath = message.filePath;
    if (message.md5 != null && Object.hasOwnProperty.call(message, "md5"))
      object.md5 = message.md5;
    return object;
  };

  /**
   * Converts this ReqCheckFile to JSON.
   * @function toJSON
   * @memberof ReqCheckFile
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCheckFile.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCheckFile
   * @function getTypeUrl
   * @memberof ReqCheckFile
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCheckFile.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCheckFile";
  };

  return ReqCheckFile;
})();

$root.ResCommon = (function () {
  /**
   * Properties of a ResCommon.
   * @exports IResCommon
   * @interface IResCommon
   * @property {number|null} [cmd] ResCommon cmd
   * @property {number|null} [code] ResCommon code
   */

  /**
   * Constructs a new ResCommon.
   * @exports ResCommon
   * @classdesc Represents a ResCommon.
   * @implements IResCommon
   * @constructor
   * @param {IResCommon=} [properties] Properties to set
   */
  function ResCommon(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResCommon cmd.
   * @member {number} cmd
   * @memberof ResCommon
   * @instance
   */
  ResCommon.prototype.cmd = 0;

  /**
   * ResCommon code.
   * @member {number} code
   * @memberof ResCommon
   * @instance
   */
  ResCommon.prototype.code = 0;

  /**
   * Creates a new ResCommon instance using the specified properties.
   * @function create
   * @memberof ResCommon
   * @static
   * @param {IResCommon=} [properties] Properties to set
   * @returns {ResCommon} ResCommon instance
   */
  ResCommon.create = function create(properties) {
    return new ResCommon(properties);
  };

  /**
   * Encodes the specified ResCommon message. Does not implicitly {@link ResCommon.verify|verify} messages.
   * @function encode
   * @memberof ResCommon
   * @static
   * @param {IResCommon} message ResCommon message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCommon.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResCommon message, length delimited. Does not implicitly {@link ResCommon.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResCommon
   * @static
   * @param {IResCommon} message ResCommon message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCommon.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResCommon message from the specified reader or buffer.
   * @function decode
   * @memberof ResCommon
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResCommon} ResCommon
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCommon.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResCommon();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResCommon message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResCommon
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResCommon} ResCommon
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCommon.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResCommon message.
   * @function verify
   * @memberof ResCommon
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResCommon.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResCommon message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResCommon
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResCommon} ResCommon
   */
  ResCommon.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResCommon) return object;
    if (!$util.isObject(object)) throw TypeError(".ResCommon: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResCommon();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResCommon message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResCommon
   * @static
   * @param {ResCommon} message ResCommon
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResCommon.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResCommon to JSON.
   * @function toJSON
   * @memberof ResCommon
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResCommon.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResCommon
   * @function getTypeUrl
   * @memberof ResCommon
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResCommon.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResCommon";
  };

  return ResCommon;
})();

$root.ResGetconfig = (function () {
  /**
   * Properties of a ResGetconfig.
   * @exports IResGetconfig
   * @interface IResGetconfig
   * @property {number|null} [cmd] ResGetconfig cmd
   * @property {number|null} [code] ResGetconfig code
   * @property {number|null} [state] ResGetconfig state
   * @property {number|null} [wifiMode] ResGetconfig wifiMode
   * @property {number|null} [apMode] ResGetconfig apMode
   * @property {number|null} [autoStart] ResGetconfig autoStart
   * @property {number|null} [apCountryList] ResGetconfig apCountryList
   * @property {string|null} [ssid] ResGetconfig ssid
   * @property {string|null} [psd] ResGetconfig psd
   * @property {string|null} [ip] ResGetconfig ip
   * @property {string|null} [apCountry] ResGetconfig apCountry
   */

  /**
   * Constructs a new ResGetconfig.
   * @exports ResGetconfig
   * @classdesc Represents a ResGetconfig.
   * @implements IResGetconfig
   * @constructor
   * @param {IResGetconfig=} [properties] Properties to set
   */
  function ResGetconfig(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetconfig cmd.
   * @member {number} cmd
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.cmd = 0;

  /**
   * ResGetconfig code.
   * @member {number} code
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.code = 0;

  /**
   * ResGetconfig state.
   * @member {number} state
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.state = 0;

  /**
   * ResGetconfig wifiMode.
   * @member {number} wifiMode
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.wifiMode = 0;

  /**
   * ResGetconfig apMode.
   * @member {number} apMode
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.apMode = 0;

  /**
   * ResGetconfig autoStart.
   * @member {number} autoStart
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.autoStart = 0;

  /**
   * ResGetconfig apCountryList.
   * @member {number} apCountryList
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.apCountryList = 0;

  /**
   * ResGetconfig ssid.
   * @member {string} ssid
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.ssid = "";

  /**
   * ResGetconfig psd.
   * @member {string} psd
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.psd = "";

  /**
   * ResGetconfig ip.
   * @member {string} ip
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.ip = "";

  /**
   * ResGetconfig apCountry.
   * @member {string} apCountry
   * @memberof ResGetconfig
   * @instance
   */
  ResGetconfig.prototype.apCountry = "";

  /**
   * Creates a new ResGetconfig instance using the specified properties.
   * @function create
   * @memberof ResGetconfig
   * @static
   * @param {IResGetconfig=} [properties] Properties to set
   * @returns {ResGetconfig} ResGetconfig instance
   */
  ResGetconfig.create = function create(properties) {
    return new ResGetconfig(properties);
  };

  /**
   * Encodes the specified ResGetconfig message. Does not implicitly {@link ResGetconfig.verify|verify} messages.
   * @function encode
   * @memberof ResGetconfig
   * @static
   * @param {IResGetconfig} message ResGetconfig message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetconfig.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.state);
    if (
      message.wifiMode != null &&
      Object.hasOwnProperty.call(message, "wifiMode")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.wifiMode);
    if (message.apMode != null && Object.hasOwnProperty.call(message, "apMode"))
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.apMode);
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.autoStart);
    if (
      message.apCountryList != null &&
      Object.hasOwnProperty.call(message, "apCountryList")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.apCountryList);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 8, wireType 2 =*/ 66).string(message.ssid);
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      writer.uint32(/* id 9, wireType 2 =*/ 74).string(message.psd);
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      writer.uint32(/* id 10, wireType 2 =*/ 82).string(message.ip);
    if (
      message.apCountry != null &&
      Object.hasOwnProperty.call(message, "apCountry")
    )
      writer.uint32(/* id 11, wireType 2 =*/ 90).string(message.apCountry);
    return writer;
  };

  /**
   * Encodes the specified ResGetconfig message, length delimited. Does not implicitly {@link ResGetconfig.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetconfig
   * @static
   * @param {IResGetconfig} message ResGetconfig message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetconfig.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetconfig message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetconfig
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetconfig} ResGetconfig
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetconfig.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetconfig();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.state = reader.int32();
          break;
        }
        case 4: {
          message.wifiMode = reader.int32();
          break;
        }
        case 5: {
          message.apMode = reader.int32();
          break;
        }
        case 6: {
          message.autoStart = reader.int32();
          break;
        }
        case 7: {
          message.apCountryList = reader.int32();
          break;
        }
        case 8: {
          message.ssid = reader.string();
          break;
        }
        case 9: {
          message.psd = reader.string();
          break;
        }
        case 10: {
          message.ip = reader.string();
          break;
        }
        case 11: {
          message.apCountry = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetconfig message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetconfig
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetconfig} ResGetconfig
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetconfig.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetconfig message.
   * @function verify
   * @memberof ResGetconfig
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetconfig.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (!$util.isInteger(message.state)) return "state: integer expected";
    if (
      message.wifiMode != null &&
      Object.hasOwnProperty.call(message, "wifiMode")
    )
      if (!$util.isInteger(message.wifiMode))
        return "wifiMode: integer expected";
    if (message.apMode != null && Object.hasOwnProperty.call(message, "apMode"))
      if (!$util.isInteger(message.apMode)) return "apMode: integer expected";
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      if (!$util.isInteger(message.autoStart))
        return "autoStart: integer expected";
    if (
      message.apCountryList != null &&
      Object.hasOwnProperty.call(message, "apCountryList")
    )
      if (!$util.isInteger(message.apCountryList))
        return "apCountryList: integer expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      if (!$util.isString(message.psd)) return "psd: string expected";
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      if (!$util.isString(message.ip)) return "ip: string expected";
    if (
      message.apCountry != null &&
      Object.hasOwnProperty.call(message, "apCountry")
    )
      if (!$util.isString(message.apCountry))
        return "apCountry: string expected";
    return null;
  };

  /**
   * Creates a ResGetconfig message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetconfig
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetconfig} ResGetconfig
   */
  ResGetconfig.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetconfig) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetconfig: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetconfig();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.state != null) message.state = object.state | 0;
    if (object.wifiMode != null) message.wifiMode = object.wifiMode | 0;
    if (object.apMode != null) message.apMode = object.apMode | 0;
    if (object.autoStart != null) message.autoStart = object.autoStart | 0;
    if (object.apCountryList != null)
      message.apCountryList = object.apCountryList | 0;
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.psd != null) message.psd = String(object.psd);
    if (object.ip != null) message.ip = String(object.ip);
    if (object.apCountry != null) message.apCountry = String(object.apCountry);
    return message;
  };

  /**
   * Creates a plain object from a ResGetconfig message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetconfig
   * @static
   * @param {ResGetconfig} message ResGetconfig
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetconfig.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.state = 0;
      object.wifiMode = 0;
      object.apMode = 0;
      object.autoStart = 0;
      object.apCountryList = 0;
      object.ssid = "";
      object.psd = "";
      object.ip = "";
      object.apCountry = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    if (
      message.wifiMode != null &&
      Object.hasOwnProperty.call(message, "wifiMode")
    )
      object.wifiMode = message.wifiMode;
    if (message.apMode != null && Object.hasOwnProperty.call(message, "apMode"))
      object.apMode = message.apMode;
    if (
      message.autoStart != null &&
      Object.hasOwnProperty.call(message, "autoStart")
    )
      object.autoStart = message.autoStart;
    if (
      message.apCountryList != null &&
      Object.hasOwnProperty.call(message, "apCountryList")
    )
      object.apCountryList = message.apCountryList;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      object.psd = message.psd;
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      object.ip = message.ip;
    if (
      message.apCountry != null &&
      Object.hasOwnProperty.call(message, "apCountry")
    )
      object.apCountry = message.apCountry;
    return object;
  };

  /**
   * Converts this ResGetconfig to JSON.
   * @function toJSON
   * @memberof ResGetconfig
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetconfig.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetconfig
   * @function getTypeUrl
   * @memberof ResGetconfig
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetconfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetconfig";
  };

  return ResGetconfig;
})();

$root.ResAp = (function () {
  /**
   * Properties of a ResAp.
   * @exports IResAp
   * @interface IResAp
   * @property {number|null} [cmd] ResAp cmd
   * @property {number|null} [code] ResAp code
   * @property {number|null} [mode] ResAp mode
   * @property {string|null} [ssid] ResAp ssid
   * @property {string|null} [psd] ResAp psd
   */

  /**
   * Constructs a new ResAp.
   * @exports ResAp
   * @classdesc Represents a ResAp.
   * @implements IResAp
   * @constructor
   * @param {IResAp=} [properties] Properties to set
   */
  function ResAp(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResAp cmd.
   * @member {number} cmd
   * @memberof ResAp
   * @instance
   */
  ResAp.prototype.cmd = 0;

  /**
   * ResAp code.
   * @member {number} code
   * @memberof ResAp
   * @instance
   */
  ResAp.prototype.code = 0;

  /**
   * ResAp mode.
   * @member {number} mode
   * @memberof ResAp
   * @instance
   */
  ResAp.prototype.mode = 0;

  /**
   * ResAp ssid.
   * @member {string} ssid
   * @memberof ResAp
   * @instance
   */
  ResAp.prototype.ssid = "";

  /**
   * ResAp psd.
   * @member {string} psd
   * @memberof ResAp
   * @instance
   */
  ResAp.prototype.psd = "";

  /**
   * Creates a new ResAp instance using the specified properties.
   * @function create
   * @memberof ResAp
   * @static
   * @param {IResAp=} [properties] Properties to set
   * @returns {ResAp} ResAp instance
   */
  ResAp.create = function create(properties) {
    return new ResAp(properties);
  };

  /**
   * Encodes the specified ResAp message. Does not implicitly {@link ResAp.verify|verify} messages.
   * @function encode
   * @memberof ResAp
   * @static
   * @param {IResAp} message ResAp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAp.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.mode);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.ssid);
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.psd);
    return writer;
  };

  /**
   * Encodes the specified ResAp message, length delimited. Does not implicitly {@link ResAp.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResAp
   * @static
   * @param {IResAp} message ResAp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAp.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResAp message from the specified reader or buffer.
   * @function decode
   * @memberof ResAp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResAp} ResAp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAp.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResAp();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.mode = reader.int32();
          break;
        }
        case 4: {
          message.ssid = reader.string();
          break;
        }
        case 5: {
          message.psd = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResAp message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResAp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResAp} ResAp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAp.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResAp message.
   * @function verify
   * @memberof ResAp
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResAp.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      if (!$util.isString(message.psd)) return "psd: string expected";
    return null;
  };

  /**
   * Creates a ResAp message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResAp
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResAp} ResAp
   */
  ResAp.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResAp) return object;
    if (!$util.isObject(object)) throw TypeError(".ResAp: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResAp();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.mode != null) message.mode = object.mode | 0;
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.psd != null) message.psd = String(object.psd);
    return message;
  };

  /**
   * Creates a plain object from a ResAp message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResAp
   * @static
   * @param {ResAp} message ResAp
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResAp.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.mode = 0;
      object.ssid = "";
      object.psd = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      object.psd = message.psd;
    return object;
  };

  /**
   * Converts this ResAp to JSON.
   * @function toJSON
   * @memberof ResAp
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResAp.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResAp
   * @function getTypeUrl
   * @memberof ResAp
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResAp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResAp";
  };

  return ResAp;
})();

$root.ResSta = (function () {
  /**
   * Properties of a ResSta.
   * @exports IResSta
   * @interface IResSta
   * @property {number|null} [cmd] ResSta cmd
   * @property {number|null} [code] ResSta code
   * @property {string|null} [ssid] ResSta ssid
   * @property {string|null} [psd] ResSta psd
   * @property {string|null} [ip] ResSta ip
   */

  /**
   * Constructs a new ResSta.
   * @exports ResSta
   * @classdesc Represents a ResSta.
   * @implements IResSta
   * @constructor
   * @param {IResSta=} [properties] Properties to set
   */
  function ResSta(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSta cmd.
   * @member {number} cmd
   * @memberof ResSta
   * @instance
   */
  ResSta.prototype.cmd = 0;

  /**
   * ResSta code.
   * @member {number} code
   * @memberof ResSta
   * @instance
   */
  ResSta.prototype.code = 0;

  /**
   * ResSta ssid.
   * @member {string} ssid
   * @memberof ResSta
   * @instance
   */
  ResSta.prototype.ssid = "";

  /**
   * ResSta psd.
   * @member {string} psd
   * @memberof ResSta
   * @instance
   */
  ResSta.prototype.psd = "";

  /**
   * ResSta ip.
   * @member {string} ip
   * @memberof ResSta
   * @instance
   */
  ResSta.prototype.ip = "";

  /**
   * Creates a new ResSta instance using the specified properties.
   * @function create
   * @memberof ResSta
   * @static
   * @param {IResSta=} [properties] Properties to set
   * @returns {ResSta} ResSta instance
   */
  ResSta.create = function create(properties) {
    return new ResSta(properties);
  };

  /**
   * Encodes the specified ResSta message. Does not implicitly {@link ResSta.verify|verify} messages.
   * @function encode
   * @memberof ResSta
   * @static
   * @param {IResSta} message ResSta message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSta.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.ssid);
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.psd);
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.ip);
    return writer;
  };

  /**
   * Encodes the specified ResSta message, length delimited. Does not implicitly {@link ResSta.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSta
   * @static
   * @param {IResSta} message ResSta message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSta.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSta message from the specified reader or buffer.
   * @function decode
   * @memberof ResSta
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSta} ResSta
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSta.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSta();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.ssid = reader.string();
          break;
        }
        case 4: {
          message.psd = reader.string();
          break;
        }
        case 5: {
          message.ip = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSta message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSta
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSta} ResSta
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSta.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSta message.
   * @function verify
   * @memberof ResSta
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSta.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      if (!$util.isString(message.psd)) return "psd: string expected";
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      if (!$util.isString(message.ip)) return "ip: string expected";
    return null;
  };

  /**
   * Creates a ResSta message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSta
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSta} ResSta
   */
  ResSta.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSta) return object;
    if (!$util.isObject(object)) throw TypeError(".ResSta: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSta();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.psd != null) message.psd = String(object.psd);
    if (object.ip != null) message.ip = String(object.ip);
    return message;
  };

  /**
   * Creates a plain object from a ResSta message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSta
   * @static
   * @param {ResSta} message ResSta
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSta.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.ssid = "";
      object.psd = "";
      object.ip = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (message.psd != null && Object.hasOwnProperty.call(message, "psd"))
      object.psd = message.psd;
    if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
      object.ip = message.ip;
    return object;
  };

  /**
   * Converts this ResSta to JSON.
   * @function toJSON
   * @memberof ResSta
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSta.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSta
   * @function getTypeUrl
   * @memberof ResSta
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSta.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSta";
  };

  return ResSta;
})();

$root.ResSetblewifi = (function () {
  /**
   * Properties of a ResSetblewifi.
   * @exports IResSetblewifi
   * @interface IResSetblewifi
   * @property {number|null} [cmd] ResSetblewifi cmd
   * @property {number|null} [code] ResSetblewifi code
   * @property {number|null} [mode] ResSetblewifi mode
   * @property {string|null} [value] ResSetblewifi value
   */

  /**
   * Constructs a new ResSetblewifi.
   * @exports ResSetblewifi
   * @classdesc Represents a ResSetblewifi.
   * @implements IResSetblewifi
   * @constructor
   * @param {IResSetblewifi=} [properties] Properties to set
   */
  function ResSetblewifi(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSetblewifi cmd.
   * @member {number} cmd
   * @memberof ResSetblewifi
   * @instance
   */
  ResSetblewifi.prototype.cmd = 0;

  /**
   * ResSetblewifi code.
   * @member {number} code
   * @memberof ResSetblewifi
   * @instance
   */
  ResSetblewifi.prototype.code = 0;

  /**
   * ResSetblewifi mode.
   * @member {number} mode
   * @memberof ResSetblewifi
   * @instance
   */
  ResSetblewifi.prototype.mode = 0;

  /**
   * ResSetblewifi value.
   * @member {string} value
   * @memberof ResSetblewifi
   * @instance
   */
  ResSetblewifi.prototype.value = "";

  /**
   * Creates a new ResSetblewifi instance using the specified properties.
   * @function create
   * @memberof ResSetblewifi
   * @static
   * @param {IResSetblewifi=} [properties] Properties to set
   * @returns {ResSetblewifi} ResSetblewifi instance
   */
  ResSetblewifi.create = function create(properties) {
    return new ResSetblewifi(properties);
  };

  /**
   * Encodes the specified ResSetblewifi message. Does not implicitly {@link ResSetblewifi.verify|verify} messages.
   * @function encode
   * @memberof ResSetblewifi
   * @static
   * @param {IResSetblewifi} message ResSetblewifi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSetblewifi.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.mode);
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.value);
    return writer;
  };

  /**
   * Encodes the specified ResSetblewifi message, length delimited. Does not implicitly {@link ResSetblewifi.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSetblewifi
   * @static
   * @param {IResSetblewifi} message ResSetblewifi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSetblewifi.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSetblewifi message from the specified reader or buffer.
   * @function decode
   * @memberof ResSetblewifi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSetblewifi} ResSetblewifi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSetblewifi.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSetblewifi();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.mode = reader.int32();
          break;
        }
        case 4: {
          message.value = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSetblewifi message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSetblewifi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSetblewifi} ResSetblewifi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSetblewifi.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSetblewifi message.
   * @function verify
   * @memberof ResSetblewifi
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSetblewifi.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isString(message.value)) return "value: string expected";
    return null;
  };

  /**
   * Creates a ResSetblewifi message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSetblewifi
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSetblewifi} ResSetblewifi
   */
  ResSetblewifi.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSetblewifi) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResSetblewifi: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSetblewifi();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.mode != null) message.mode = object.mode | 0;
    if (object.value != null) message.value = String(object.value);
    return message;
  };

  /**
   * Creates a plain object from a ResSetblewifi message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSetblewifi
   * @static
   * @param {ResSetblewifi} message ResSetblewifi
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSetblewifi.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.mode = 0;
      object.value = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ResSetblewifi to JSON.
   * @function toJSON
   * @memberof ResSetblewifi
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSetblewifi.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSetblewifi
   * @function getTypeUrl
   * @memberof ResSetblewifi
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSetblewifi.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSetblewifi";
  };

  return ResSetblewifi;
})();

$root.ResReset = (function () {
  /**
   * Properties of a ResReset.
   * @exports IResReset
   * @interface IResReset
   * @property {number|null} [cmd] ResReset cmd
   * @property {number|null} [code] ResReset code
   */

  /**
   * Constructs a new ResReset.
   * @exports ResReset
   * @classdesc Represents a ResReset.
   * @implements IResReset
   * @constructor
   * @param {IResReset=} [properties] Properties to set
   */
  function ResReset(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResReset cmd.
   * @member {number} cmd
   * @memberof ResReset
   * @instance
   */
  ResReset.prototype.cmd = 0;

  /**
   * ResReset code.
   * @member {number} code
   * @memberof ResReset
   * @instance
   */
  ResReset.prototype.code = 0;

  /**
   * Creates a new ResReset instance using the specified properties.
   * @function create
   * @memberof ResReset
   * @static
   * @param {IResReset=} [properties] Properties to set
   * @returns {ResReset} ResReset instance
   */
  ResReset.create = function create(properties) {
    return new ResReset(properties);
  };

  /**
   * Encodes the specified ResReset message. Does not implicitly {@link ResReset.verify|verify} messages.
   * @function encode
   * @memberof ResReset
   * @static
   * @param {IResReset} message ResReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReset.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResReset message, length delimited. Does not implicitly {@link ResReset.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResReset
   * @static
   * @param {IResReset} message ResReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReset.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResReset message from the specified reader or buffer.
   * @function decode
   * @memberof ResReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResReset} ResReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReset.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResReset();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResReset message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResReset} ResReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReset.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResReset message.
   * @function verify
   * @memberof ResReset
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResReset.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResReset message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResReset
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResReset} ResReset
   */
  ResReset.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResReset) return object;
    if (!$util.isObject(object)) throw TypeError(".ResReset: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResReset();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResReset message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResReset
   * @static
   * @param {ResReset} message ResReset
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResReset.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResReset to JSON.
   * @function toJSON
   * @memberof ResReset
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResReset.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResReset
   * @function getTypeUrl
   * @memberof ResReset
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResReset.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResReset";
  };

  return ResReset;
})();

$root.WifiInfo = (function () {
  /**
   * Properties of a WifiInfo.
   * @exports IWifiInfo
   * @interface IWifiInfo
   * @property {number|null} [signalLevel] WifiInfo signalLevel
   * @property {string|null} [ssid] WifiInfo ssid
   * @property {string|null} [securityCapability] WifiInfo securityCapability
   */

  /**
   * Constructs a new WifiInfo.
   * @exports WifiInfo
   * @classdesc Represents a WifiInfo.
   * @implements IWifiInfo
   * @constructor
   * @param {IWifiInfo=} [properties] Properties to set
   */
  function WifiInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * WifiInfo signalLevel.
   * @member {number} signalLevel
   * @memberof WifiInfo
   * @instance
   */
  WifiInfo.prototype.signalLevel = 0;

  /**
   * WifiInfo ssid.
   * @member {string} ssid
   * @memberof WifiInfo
   * @instance
   */
  WifiInfo.prototype.ssid = "";

  /**
   * WifiInfo securityCapability.
   * @member {string} securityCapability
   * @memberof WifiInfo
   * @instance
   */
  WifiInfo.prototype.securityCapability = "";

  /**
   * Creates a new WifiInfo instance using the specified properties.
   * @function create
   * @memberof WifiInfo
   * @static
   * @param {IWifiInfo=} [properties] Properties to set
   * @returns {WifiInfo} WifiInfo instance
   */
  WifiInfo.create = function create(properties) {
    return new WifiInfo(properties);
  };

  /**
   * Encodes the specified WifiInfo message. Does not implicitly {@link WifiInfo.verify|verify} messages.
   * @function encode
   * @memberof WifiInfo
   * @static
   * @param {IWifiInfo} message WifiInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WifiInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.signalLevel != null &&
      Object.hasOwnProperty.call(message, "signalLevel")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.signalLevel);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.ssid);
    if (
      message.securityCapability != null &&
      Object.hasOwnProperty.call(message, "securityCapability")
    )
      writer
        .uint32(/* id 3, wireType 2 =*/ 26)
        .string(message.securityCapability);
    return writer;
  };

  /**
   * Encodes the specified WifiInfo message, length delimited. Does not implicitly {@link WifiInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof WifiInfo
   * @static
   * @param {IWifiInfo} message WifiInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WifiInfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a WifiInfo message from the specified reader or buffer.
   * @function decode
   * @memberof WifiInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {WifiInfo} WifiInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WifiInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.WifiInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.signalLevel = reader.int32();
          break;
        }
        case 2: {
          message.ssid = reader.string();
          break;
        }
        case 3: {
          message.securityCapability = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a WifiInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof WifiInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {WifiInfo} WifiInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WifiInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a WifiInfo message.
   * @function verify
   * @memberof WifiInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  WifiInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.signalLevel != null &&
      Object.hasOwnProperty.call(message, "signalLevel")
    )
      if (!$util.isInteger(message.signalLevel))
        return "signalLevel: integer expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (
      message.securityCapability != null &&
      Object.hasOwnProperty.call(message, "securityCapability")
    )
      if (!$util.isString(message.securityCapability))
        return "securityCapability: string expected";
    return null;
  };

  /**
   * Creates a WifiInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof WifiInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {WifiInfo} WifiInfo
   */
  WifiInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.WifiInfo) return object;
    if (!$util.isObject(object)) throw TypeError(".WifiInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.WifiInfo();
    if (object.signalLevel != null)
      message.signalLevel = object.signalLevel | 0;
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.securityCapability != null)
      message.securityCapability = String(object.securityCapability);
    return message;
  };

  /**
   * Creates a plain object from a WifiInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof WifiInfo
   * @static
   * @param {WifiInfo} message WifiInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  WifiInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.signalLevel = 0;
      object.ssid = "";
      object.securityCapability = "";
    }
    if (
      message.signalLevel != null &&
      Object.hasOwnProperty.call(message, "signalLevel")
    )
      object.signalLevel = message.signalLevel;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (
      message.securityCapability != null &&
      Object.hasOwnProperty.call(message, "securityCapability")
    )
      object.securityCapability = message.securityCapability;
    return object;
  };

  /**
   * Converts this WifiInfo to JSON.
   * @function toJSON
   * @memberof WifiInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  WifiInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for WifiInfo
   * @function getTypeUrl
   * @memberof WifiInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  WifiInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/WifiInfo";
  };

  return WifiInfo;
})();

$root.ResWifilist = (function () {
  /**
   * Properties of a ResWifilist.
   * @exports IResWifilist
   * @interface IResWifilist
   * @property {number|null} [cmd] ResWifilist cmd
   * @property {number|null} [code] ResWifilist code
   * @property {Array.<string>|null} [ssid] ResWifilist ssid
   * @property {Array.<IWifiInfo>|null} [wifiInfoList] ResWifilist wifiInfoList
   */

  /**
   * Constructs a new ResWifilist.
   * @exports ResWifilist
   * @classdesc Represents a ResWifilist.
   * @implements IResWifilist
   * @constructor
   * @param {IResWifilist=} [properties] Properties to set
   */
  function ResWifilist(properties) {
    this.ssid = [];
    this.wifiInfoList = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResWifilist cmd.
   * @member {number} cmd
   * @memberof ResWifilist
   * @instance
   */
  ResWifilist.prototype.cmd = 0;

  /**
   * ResWifilist code.
   * @member {number} code
   * @memberof ResWifilist
   * @instance
   */
  ResWifilist.prototype.code = 0;

  /**
   * ResWifilist ssid.
   * @member {Array.<string>} ssid
   * @memberof ResWifilist
   * @instance
   */
  ResWifilist.prototype.ssid = $util.emptyArray;

  /**
   * ResWifilist wifiInfoList.
   * @member {Array.<IWifiInfo>} wifiInfoList
   * @memberof ResWifilist
   * @instance
   */
  ResWifilist.prototype.wifiInfoList = $util.emptyArray;

  /**
   * Creates a new ResWifilist instance using the specified properties.
   * @function create
   * @memberof ResWifilist
   * @static
   * @param {IResWifilist=} [properties] Properties to set
   * @returns {ResWifilist} ResWifilist instance
   */
  ResWifilist.create = function create(properties) {
    return new ResWifilist(properties);
  };

  /**
   * Encodes the specified ResWifilist message. Does not implicitly {@link ResWifilist.verify|verify} messages.
   * @function encode
   * @memberof ResWifilist
   * @static
   * @param {IResWifilist} message ResWifilist message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResWifilist.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.ssid != null && message.ssid.length)
      for (var i = 0; i < message.ssid.length; ++i)
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.ssid[i]);
    if (message.wifiInfoList != null && message.wifiInfoList.length)
      for (var i = 0; i < message.wifiInfoList.length; ++i)
        $root.WifiInfo.encode(
          message.wifiInfoList[i],
          writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
          q + 1,
        ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResWifilist message, length delimited. Does not implicitly {@link ResWifilist.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResWifilist
   * @static
   * @param {IResWifilist} message ResWifilist message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResWifilist.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResWifilist message from the specified reader or buffer.
   * @function decode
   * @memberof ResWifilist
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResWifilist} ResWifilist
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResWifilist.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResWifilist();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 4: {
          if (!(message.ssid && message.ssid.length)) message.ssid = [];
          message.ssid.push(reader.string());
          break;
        }
        case 5: {
          if (!(message.wifiInfoList && message.wifiInfoList.length))
            message.wifiInfoList = [];
          message.wifiInfoList.push(
            $root.WifiInfo.decode(reader, reader.uint32(), undefined, long + 1),
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResWifilist message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResWifilist
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResWifilist} ResWifilist
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResWifilist.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResWifilist message.
   * @function verify
   * @memberof ResWifilist
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResWifilist.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid")) {
      if (!Array.isArray(message.ssid)) return "ssid: array expected";
      for (var i = 0; i < message.ssid.length; ++i)
        if (!$util.isString(message.ssid[i])) return "ssid: string[] expected";
    }
    if (
      message.wifiInfoList != null &&
      Object.hasOwnProperty.call(message, "wifiInfoList")
    ) {
      if (!Array.isArray(message.wifiInfoList))
        return "wifiInfoList: array expected";
      for (var i = 0; i < message.wifiInfoList.length; ++i) {
        var error = $root.WifiInfo.verify(message.wifiInfoList[i], long + 1);
        if (error) return "wifiInfoList." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ResWifilist message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResWifilist
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResWifilist} ResWifilist
   */
  ResWifilist.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResWifilist) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResWifilist: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResWifilist();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.ssid) {
      if (!Array.isArray(object.ssid))
        throw TypeError(".ResWifilist.ssid: array expected");
      message.ssid = [];
      for (var i = 0; i < object.ssid.length; ++i)
        message.ssid[i] = String(object.ssid[i]);
    }
    if (object.wifiInfoList) {
      if (!Array.isArray(object.wifiInfoList))
        throw TypeError(".ResWifilist.wifiInfoList: array expected");
      message.wifiInfoList = [];
      for (var i = 0; i < object.wifiInfoList.length; ++i) {
        if (!$util.isObject(object.wifiInfoList[i]))
          throw TypeError(".ResWifilist.wifiInfoList: object expected");
        message.wifiInfoList[i] = $root.WifiInfo.fromObject(
          object.wifiInfoList[i],
          long + 1,
        );
      }
    }
    return message;
  };

  /**
   * Creates a plain object from a ResWifilist message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResWifilist
   * @static
   * @param {ResWifilist} message ResWifilist
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResWifilist.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) {
      object.ssid = [];
      object.wifiInfoList = [];
    }
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.ssid && message.ssid.length) {
      object.ssid = [];
      for (var j = 0; j < message.ssid.length; ++j)
        object.ssid[j] = message.ssid[j];
    }
    if (message.wifiInfoList && message.wifiInfoList.length) {
      object.wifiInfoList = [];
      for (var j = 0; j < message.wifiInfoList.length; ++j)
        object.wifiInfoList[j] = $root.WifiInfo.toObject(
          message.wifiInfoList[j],
          options,
          q + 1,
        );
    }
    return object;
  };

  /**
   * Converts this ResWifilist to JSON.
   * @function toJSON
   * @memberof ResWifilist
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResWifilist.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResWifilist
   * @function getTypeUrl
   * @memberof ResWifilist
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResWifilist.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResWifilist";
  };

  return ResWifilist;
})();

$root.ResGetsysteminfo = (function () {
  /**
   * Properties of a ResGetsysteminfo.
   * @exports IResGetsysteminfo
   * @interface IResGetsysteminfo
   * @property {number|null} [cmd] ResGetsysteminfo cmd
   * @property {number|null} [code] ResGetsysteminfo code
   * @property {number|null} [protocolVersion] ResGetsysteminfo protocolVersion
   * @property {string|null} [device] ResGetsysteminfo device
   * @property {string|null} [macAddress] ResGetsysteminfo macAddress
   * @property {string|null} [dwarfOtaVersion] ResGetsysteminfo dwarfOtaVersion
   */

  /**
   * Constructs a new ResGetsysteminfo.
   * @exports ResGetsysteminfo
   * @classdesc Represents a ResGetsysteminfo.
   * @implements IResGetsysteminfo
   * @constructor
   * @param {IResGetsysteminfo=} [properties] Properties to set
   */
  function ResGetsysteminfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetsysteminfo cmd.
   * @member {number} cmd
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.cmd = 0;

  /**
   * ResGetsysteminfo code.
   * @member {number} code
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.code = 0;

  /**
   * ResGetsysteminfo protocolVersion.
   * @member {number} protocolVersion
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.protocolVersion = 0;

  /**
   * ResGetsysteminfo device.
   * @member {string} device
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.device = "";

  /**
   * ResGetsysteminfo macAddress.
   * @member {string} macAddress
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.macAddress = "";

  /**
   * ResGetsysteminfo dwarfOtaVersion.
   * @member {string} dwarfOtaVersion
   * @memberof ResGetsysteminfo
   * @instance
   */
  ResGetsysteminfo.prototype.dwarfOtaVersion = "";

  /**
   * Creates a new ResGetsysteminfo instance using the specified properties.
   * @function create
   * @memberof ResGetsysteminfo
   * @static
   * @param {IResGetsysteminfo=} [properties] Properties to set
   * @returns {ResGetsysteminfo} ResGetsysteminfo instance
   */
  ResGetsysteminfo.create = function create(properties) {
    return new ResGetsysteminfo(properties);
  };

  /**
   * Encodes the specified ResGetsysteminfo message. Does not implicitly {@link ResGetsysteminfo.verify|verify} messages.
   * @function encode
   * @memberof ResGetsysteminfo
   * @static
   * @param {IResGetsysteminfo} message ResGetsysteminfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetsysteminfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (
      message.protocolVersion != null &&
      Object.hasOwnProperty.call(message, "protocolVersion")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.protocolVersion);
    if (message.device != null && Object.hasOwnProperty.call(message, "device"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.device);
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.macAddress);
    if (
      message.dwarfOtaVersion != null &&
      Object.hasOwnProperty.call(message, "dwarfOtaVersion")
    )
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.dwarfOtaVersion);
    return writer;
  };

  /**
   * Encodes the specified ResGetsysteminfo message, length delimited. Does not implicitly {@link ResGetsysteminfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetsysteminfo
   * @static
   * @param {IResGetsysteminfo} message ResGetsysteminfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetsysteminfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetsysteminfo message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetsysteminfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetsysteminfo} ResGetsysteminfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetsysteminfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetsysteminfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.protocolVersion = reader.int32();
          break;
        }
        case 4: {
          message.device = reader.string();
          break;
        }
        case 5: {
          message.macAddress = reader.string();
          break;
        }
        case 6: {
          message.dwarfOtaVersion = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetsysteminfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetsysteminfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetsysteminfo} ResGetsysteminfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetsysteminfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetsysteminfo message.
   * @function verify
   * @memberof ResGetsysteminfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetsysteminfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.protocolVersion != null &&
      Object.hasOwnProperty.call(message, "protocolVersion")
    )
      if (!$util.isInteger(message.protocolVersion))
        return "protocolVersion: integer expected";
    if (message.device != null && Object.hasOwnProperty.call(message, "device"))
      if (!$util.isString(message.device)) return "device: string expected";
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      if (!$util.isString(message.macAddress))
        return "macAddress: string expected";
    if (
      message.dwarfOtaVersion != null &&
      Object.hasOwnProperty.call(message, "dwarfOtaVersion")
    )
      if (!$util.isString(message.dwarfOtaVersion))
        return "dwarfOtaVersion: string expected";
    return null;
  };

  /**
   * Creates a ResGetsysteminfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetsysteminfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetsysteminfo} ResGetsysteminfo
   */
  ResGetsysteminfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetsysteminfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetsysteminfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetsysteminfo();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.protocolVersion != null)
      message.protocolVersion = object.protocolVersion | 0;
    if (object.device != null) message.device = String(object.device);
    if (object.macAddress != null)
      message.macAddress = String(object.macAddress);
    if (object.dwarfOtaVersion != null)
      message.dwarfOtaVersion = String(object.dwarfOtaVersion);
    return message;
  };

  /**
   * Creates a plain object from a ResGetsysteminfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetsysteminfo
   * @static
   * @param {ResGetsysteminfo} message ResGetsysteminfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetsysteminfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.protocolVersion = 0;
      object.device = "";
      object.macAddress = "";
      object.dwarfOtaVersion = "";
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.protocolVersion != null &&
      Object.hasOwnProperty.call(message, "protocolVersion")
    )
      object.protocolVersion = message.protocolVersion;
    if (message.device != null && Object.hasOwnProperty.call(message, "device"))
      object.device = message.device;
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      object.macAddress = message.macAddress;
    if (
      message.dwarfOtaVersion != null &&
      Object.hasOwnProperty.call(message, "dwarfOtaVersion")
    )
      object.dwarfOtaVersion = message.dwarfOtaVersion;
    return object;
  };

  /**
   * Converts this ResGetsysteminfo to JSON.
   * @function toJSON
   * @memberof ResGetsysteminfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetsysteminfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetsysteminfo
   * @function getTypeUrl
   * @memberof ResGetsysteminfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetsysteminfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetsysteminfo";
  };

  return ResGetsysteminfo;
})();

$root.ResReceiveDataError = (function () {
  /**
   * Properties of a ResReceiveDataError.
   * @exports IResReceiveDataError
   * @interface IResReceiveDataError
   * @property {number|null} [cmd] ResReceiveDataError cmd
   * @property {number|null} [code] ResReceiveDataError code
   */

  /**
   * Constructs a new ResReceiveDataError.
   * @exports ResReceiveDataError
   * @classdesc Represents a ResReceiveDataError.
   * @implements IResReceiveDataError
   * @constructor
   * @param {IResReceiveDataError=} [properties] Properties to set
   */
  function ResReceiveDataError(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResReceiveDataError cmd.
   * @member {number} cmd
   * @memberof ResReceiveDataError
   * @instance
   */
  ResReceiveDataError.prototype.cmd = 0;

  /**
   * ResReceiveDataError code.
   * @member {number} code
   * @memberof ResReceiveDataError
   * @instance
   */
  ResReceiveDataError.prototype.code = 0;

  /**
   * Creates a new ResReceiveDataError instance using the specified properties.
   * @function create
   * @memberof ResReceiveDataError
   * @static
   * @param {IResReceiveDataError=} [properties] Properties to set
   * @returns {ResReceiveDataError} ResReceiveDataError instance
   */
  ResReceiveDataError.create = function create(properties) {
    return new ResReceiveDataError(properties);
  };

  /**
   * Encodes the specified ResReceiveDataError message. Does not implicitly {@link ResReceiveDataError.verify|verify} messages.
   * @function encode
   * @memberof ResReceiveDataError
   * @static
   * @param {IResReceiveDataError} message ResReceiveDataError message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReceiveDataError.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResReceiveDataError message, length delimited. Does not implicitly {@link ResReceiveDataError.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResReceiveDataError
   * @static
   * @param {IResReceiveDataError} message ResReceiveDataError message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReceiveDataError.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResReceiveDataError message from the specified reader or buffer.
   * @function decode
   * @memberof ResReceiveDataError
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResReceiveDataError} ResReceiveDataError
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReceiveDataError.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResReceiveDataError();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResReceiveDataError message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResReceiveDataError
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResReceiveDataError} ResReceiveDataError
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReceiveDataError.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResReceiveDataError message.
   * @function verify
   * @memberof ResReceiveDataError
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResReceiveDataError.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResReceiveDataError message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResReceiveDataError
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResReceiveDataError} ResReceiveDataError
   */
  ResReceiveDataError.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResReceiveDataError) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResReceiveDataError: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResReceiveDataError();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResReceiveDataError message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResReceiveDataError
   * @static
   * @param {ResReceiveDataError} message ResReceiveDataError
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResReceiveDataError.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResReceiveDataError to JSON.
   * @function toJSON
   * @memberof ResReceiveDataError
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResReceiveDataError.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResReceiveDataError
   * @function getTypeUrl
   * @memberof ResReceiveDataError
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResReceiveDataError.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResReceiveDataError";
  };

  return ResReceiveDataError;
})();

$root.ResCheckFile = (function () {
  /**
   * Properties of a ResCheckFile.
   * @exports IResCheckFile
   * @interface IResCheckFile
   * @property {number|null} [cmd] ResCheckFile cmd
   * @property {number|null} [code] ResCheckFile code
   */

  /**
   * Constructs a new ResCheckFile.
   * @exports ResCheckFile
   * @classdesc Represents a ResCheckFile.
   * @implements IResCheckFile
   * @constructor
   * @param {IResCheckFile=} [properties] Properties to set
   */
  function ResCheckFile(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResCheckFile cmd.
   * @member {number} cmd
   * @memberof ResCheckFile
   * @instance
   */
  ResCheckFile.prototype.cmd = 0;

  /**
   * ResCheckFile code.
   * @member {number} code
   * @memberof ResCheckFile
   * @instance
   */
  ResCheckFile.prototype.code = 0;

  /**
   * Creates a new ResCheckFile instance using the specified properties.
   * @function create
   * @memberof ResCheckFile
   * @static
   * @param {IResCheckFile=} [properties] Properties to set
   * @returns {ResCheckFile} ResCheckFile instance
   */
  ResCheckFile.create = function create(properties) {
    return new ResCheckFile(properties);
  };

  /**
   * Encodes the specified ResCheckFile message. Does not implicitly {@link ResCheckFile.verify|verify} messages.
   * @function encode
   * @memberof ResCheckFile
   * @static
   * @param {IResCheckFile} message ResCheckFile message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCheckFile.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResCheckFile message, length delimited. Does not implicitly {@link ResCheckFile.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResCheckFile
   * @static
   * @param {IResCheckFile} message ResCheckFile message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCheckFile.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResCheckFile message from the specified reader or buffer.
   * @function decode
   * @memberof ResCheckFile
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResCheckFile} ResCheckFile
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCheckFile.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResCheckFile();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResCheckFile message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResCheckFile
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResCheckFile} ResCheckFile
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCheckFile.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResCheckFile message.
   * @function verify
   * @memberof ResCheckFile
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResCheckFile.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResCheckFile message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResCheckFile
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResCheckFile} ResCheckFile
   */
  ResCheckFile.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResCheckFile) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResCheckFile: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResCheckFile();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResCheckFile message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResCheckFile
   * @static
   * @param {ResCheckFile} message ResCheckFile
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResCheckFile.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResCheckFile to JSON.
   * @function toJSON
   * @memberof ResCheckFile
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResCheckFile.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResCheckFile
   * @function getTypeUrl
   * @memberof ResCheckFile
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResCheckFile.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResCheckFile";
  };

  return ResCheckFile;
})();

$root.ComDwarfMsg = (function () {
  /**
   * Properties of a ComDwarfMsg.
   * @exports IComDwarfMsg
   * @interface IComDwarfMsg
   * @property {VocalType|null} [vocaltype] ComDwarfMsg vocaltype
   */

  /**
   * Constructs a new ComDwarfMsg.
   * @exports ComDwarfMsg
   * @classdesc Represents a ComDwarfMsg.
   * @implements IComDwarfMsg
   * @constructor
   * @param {IComDwarfMsg=} [properties] Properties to set
   */
  function ComDwarfMsg(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ComDwarfMsg vocaltype.
   * @member {VocalType} vocaltype
   * @memberof ComDwarfMsg
   * @instance
   */
  ComDwarfMsg.prototype.vocaltype = 0;

  /**
   * Creates a new ComDwarfMsg instance using the specified properties.
   * @function create
   * @memberof ComDwarfMsg
   * @static
   * @param {IComDwarfMsg=} [properties] Properties to set
   * @returns {ComDwarfMsg} ComDwarfMsg instance
   */
  ComDwarfMsg.create = function create(properties) {
    return new ComDwarfMsg(properties);
  };

  /**
   * Encodes the specified ComDwarfMsg message. Does not implicitly {@link ComDwarfMsg.verify|verify} messages.
   * @function encode
   * @memberof ComDwarfMsg
   * @static
   * @param {IComDwarfMsg} message ComDwarfMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComDwarfMsg.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.vocaltype);
    return writer;
  };

  /**
   * Encodes the specified ComDwarfMsg message, length delimited. Does not implicitly {@link ComDwarfMsg.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ComDwarfMsg
   * @static
   * @param {IComDwarfMsg} message ComDwarfMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ComDwarfMsg.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ComDwarfMsg message from the specified reader or buffer.
   * @function decode
   * @memberof ComDwarfMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ComDwarfMsg} ComDwarfMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComDwarfMsg.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ComDwarfMsg();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.vocaltype = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ComDwarfMsg message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ComDwarfMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ComDwarfMsg} ComDwarfMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ComDwarfMsg.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ComDwarfMsg message.
   * @function verify
   * @memberof ComDwarfMsg
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ComDwarfMsg.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      switch (message.vocaltype) {
        default:
          return "vocaltype: enum value expected";
        case 0:
        case 1:
        case 2:
          break;
      }
    return null;
  };

  /**
   * Creates a ComDwarfMsg message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ComDwarfMsg
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ComDwarfMsg} ComDwarfMsg
   */
  ComDwarfMsg.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ComDwarfMsg) return object;
    if (!$util.isObject(object))
      throw TypeError(".ComDwarfMsg: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ComDwarfMsg();
    switch (object.vocaltype) {
      default:
        if (typeof object.vocaltype === "number") {
          message.vocaltype = object.vocaltype;
          break;
        }
        break;
      case "VT_UNKNOWN":
      case 0:
        message.vocaltype = 0;
        break;
      case "VT_PING":
      case 1:
        message.vocaltype = 1;
        break;
      case "VT_ECHO":
      case 2:
        message.vocaltype = 2;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a ComDwarfMsg message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ComDwarfMsg
   * @static
   * @param {ComDwarfMsg} message ComDwarfMsg
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ComDwarfMsg.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults)
      object.vocaltype = options.enums === String ? "VT_UNKNOWN" : 0;
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      object.vocaltype =
        options.enums === String
          ? $root.VocalType[message.vocaltype] === undefined
            ? message.vocaltype
            : $root.VocalType[message.vocaltype]
          : message.vocaltype;
    return object;
  };

  /**
   * Converts this ComDwarfMsg to JSON.
   * @function toJSON
   * @memberof ComDwarfMsg
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ComDwarfMsg.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ComDwarfMsg
   * @function getTypeUrl
   * @memberof ComDwarfMsg
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ComDwarfMsg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ComDwarfMsg";
  };

  return ComDwarfMsg;
})();

$root.DwarfPing = (function () {
  /**
   * Properties of a DwarfPing.
   * @exports IDwarfPing
   * @interface IDwarfPing
   * @property {VocalType|null} [vocaltype] DwarfPing vocaltype
   * @property {number|Long|null} [timestamp] DwarfPing timestamp
   * @property {Uint8Array|null} [magic] DwarfPing magic
   * @property {Array.<Uint8Array>|null} [vocals] DwarfPing vocals
   * @property {Array.<Uint8Array>|null} [mutes] DwarfPing mutes
   */

  /**
   * Constructs a new DwarfPing.
   * @exports DwarfPing
   * @classdesc Represents a DwarfPing.
   * @implements IDwarfPing
   * @constructor
   * @param {IDwarfPing=} [properties] Properties to set
   */
  function DwarfPing(properties) {
    this.vocals = [];
    this.mutes = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * DwarfPing vocaltype.
   * @member {VocalType} vocaltype
   * @memberof DwarfPing
   * @instance
   */
  DwarfPing.prototype.vocaltype = 0;

  /**
   * DwarfPing timestamp.
   * @member {number|Long} timestamp
   * @memberof DwarfPing
   * @instance
   */
  DwarfPing.prototype.timestamp = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * DwarfPing magic.
   * @member {Uint8Array} magic
   * @memberof DwarfPing
   * @instance
   */
  DwarfPing.prototype.magic = $util.newBuffer([]);

  /**
   * DwarfPing vocals.
   * @member {Array.<Uint8Array>} vocals
   * @memberof DwarfPing
   * @instance
   */
  DwarfPing.prototype.vocals = $util.emptyArray;

  /**
   * DwarfPing mutes.
   * @member {Array.<Uint8Array>} mutes
   * @memberof DwarfPing
   * @instance
   */
  DwarfPing.prototype.mutes = $util.emptyArray;

  /**
   * Creates a new DwarfPing instance using the specified properties.
   * @function create
   * @memberof DwarfPing
   * @static
   * @param {IDwarfPing=} [properties] Properties to set
   * @returns {DwarfPing} DwarfPing instance
   */
  DwarfPing.create = function create(properties) {
    return new DwarfPing(properties);
  };

  /**
   * Encodes the specified DwarfPing message. Does not implicitly {@link DwarfPing.verify|verify} messages.
   * @function encode
   * @memberof DwarfPing
   * @static
   * @param {IDwarfPing} message DwarfPing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DwarfPing.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.vocaltype);
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.timestamp);
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.magic);
    if (message.vocals != null && message.vocals.length)
      for (var i = 0; i < message.vocals.length; ++i)
        writer.uint32(/* id 4, wireType 2 =*/ 34).bytes(message.vocals[i]);
    if (message.mutes != null && message.mutes.length)
      for (var i = 0; i < message.mutes.length; ++i)
        writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.mutes[i]);
    return writer;
  };

  /**
   * Encodes the specified DwarfPing message, length delimited. Does not implicitly {@link DwarfPing.verify|verify} messages.
   * @function encodeDelimited
   * @memberof DwarfPing
   * @static
   * @param {IDwarfPing} message DwarfPing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DwarfPing.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a DwarfPing message from the specified reader or buffer.
   * @function decode
   * @memberof DwarfPing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {DwarfPing} DwarfPing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DwarfPing.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.DwarfPing();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.vocaltype = reader.int32();
          break;
        }
        case 2: {
          message.timestamp = reader.uint64();
          break;
        }
        case 3: {
          message.magic = reader.bytes();
          break;
        }
        case 4: {
          if (!(message.vocals && message.vocals.length)) message.vocals = [];
          message.vocals.push(reader.bytes());
          break;
        }
        case 5: {
          if (!(message.mutes && message.mutes.length)) message.mutes = [];
          message.mutes.push(reader.bytes());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a DwarfPing message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof DwarfPing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {DwarfPing} DwarfPing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DwarfPing.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a DwarfPing message.
   * @function verify
   * @memberof DwarfPing
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  DwarfPing.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      switch (message.vocaltype) {
        default:
          return "vocaltype: enum value expected";
        case 0:
        case 1:
        case 2:
          break;
      }
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (
        !$util.isInteger(message.timestamp) &&
        !(
          message.timestamp &&
          $util.isInteger(message.timestamp.low) &&
          $util.isInteger(message.timestamp.high)
        )
      )
        return "timestamp: integer|Long expected";
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      if (!(
        (message.magic && typeof message.magic.length === "number") ||
        $util.isString(message.magic)
      ))
        return "magic: buffer expected";
    if (
      message.vocals != null &&
      Object.hasOwnProperty.call(message, "vocals")
    ) {
      if (!Array.isArray(message.vocals)) return "vocals: array expected";
      for (var i = 0; i < message.vocals.length; ++i)
        if (!(
          (message.vocals[i] && typeof message.vocals[i].length === "number") ||
          $util.isString(message.vocals[i])
        ))
          return "vocals: buffer[] expected";
    }
    if (message.mutes != null && Object.hasOwnProperty.call(message, "mutes")) {
      if (!Array.isArray(message.mutes)) return "mutes: array expected";
      for (var i = 0; i < message.mutes.length; ++i)
        if (!(
          (message.mutes[i] && typeof message.mutes[i].length === "number") ||
          $util.isString(message.mutes[i])
        ))
          return "mutes: buffer[] expected";
    }
    return null;
  };

  /**
   * Creates a DwarfPing message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof DwarfPing
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {DwarfPing} DwarfPing
   */
  DwarfPing.fromObject = function fromObject(object, long) {
    if (object instanceof $root.DwarfPing) return object;
    if (!$util.isObject(object)) throw TypeError(".DwarfPing: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.DwarfPing();
    switch (object.vocaltype) {
      default:
        if (typeof object.vocaltype === "number") {
          message.vocaltype = object.vocaltype;
          break;
        }
        break;
      case "VT_UNKNOWN":
      case 0:
        message.vocaltype = 0;
        break;
      case "VT_PING":
      case 1:
        message.vocaltype = 1;
        break;
      case "VT_ECHO":
      case 2:
        message.vocaltype = 2;
        break;
    }
    if (object.timestamp != null)
      if ($util.Long)
        message.timestamp = $util.Long.fromValue(object.timestamp, true);
      else if (typeof object.timestamp === "string")
        message.timestamp = parseInt(object.timestamp, 10);
      else if (typeof object.timestamp === "number")
        message.timestamp = object.timestamp;
      else if (typeof object.timestamp === "object")
        message.timestamp = new $util.LongBits(
          object.timestamp.low >>> 0,
          object.timestamp.high >>> 0,
        ).toNumber(true);
    if (object.magic != null)
      if (typeof object.magic === "string")
        $util.base64.decode(
          object.magic,
          (message.magic = $util.newBuffer($util.base64.length(object.magic))),
          0,
        );
      else if (object.magic.length >= 0) message.magic = object.magic;
    if (object.vocals) {
      if (!Array.isArray(object.vocals))
        throw TypeError(".DwarfPing.vocals: array expected");
      message.vocals = [];
      for (var i = 0; i < object.vocals.length; ++i)
        if (typeof object.vocals[i] === "string")
          $util.base64.decode(
            object.vocals[i],
            (message.vocals[i] = $util.newBuffer(
              $util.base64.length(object.vocals[i]),
            )),
            0,
          );
        else if (object.vocals[i].length >= 0)
          message.vocals[i] = object.vocals[i];
    }
    if (object.mutes) {
      if (!Array.isArray(object.mutes))
        throw TypeError(".DwarfPing.mutes: array expected");
      message.mutes = [];
      for (var i = 0; i < object.mutes.length; ++i)
        if (typeof object.mutes[i] === "string")
          $util.base64.decode(
            object.mutes[i],
            (message.mutes[i] = $util.newBuffer(
              $util.base64.length(object.mutes[i]),
            )),
            0,
          );
        else if (object.mutes[i].length >= 0)
          message.mutes[i] = object.mutes[i];
    }
    return message;
  };

  /**
   * Creates a plain object from a DwarfPing message. Also converts values to other types if specified.
   * @function toObject
   * @memberof DwarfPing
   * @static
   * @param {DwarfPing} message DwarfPing
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  DwarfPing.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) {
      object.vocals = [];
      object.mutes = [];
    }
    if (options.defaults) {
      object.vocaltype = options.enums === String ? "VT_UNKNOWN" : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.timestamp =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.timestamp =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if (options.bytes === String) object.magic = "";
      else {
        object.magic = [];
        if (options.bytes !== Array)
          object.magic = $util.newBuffer(object.magic);
      }
    }
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      object.vocaltype =
        options.enums === String
          ? $root.VocalType[message.vocaltype] === undefined
            ? message.vocaltype
            : $root.VocalType[message.vocaltype]
          : message.vocaltype;
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.timestamp =
          typeof message.timestamp === "number"
            ? BigInt(message.timestamp)
            : $util.Long.fromBits(
                message.timestamp.low >>> 0,
                message.timestamp.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.timestamp === "number")
        object.timestamp =
          options.longs === String
            ? String(message.timestamp)
            : message.timestamp;
      else
        object.timestamp =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.timestamp)
            : options.longs === Number
              ? new $util.LongBits(
                  message.timestamp.low >>> 0,
                  message.timestamp.high >>> 0,
                ).toNumber(true)
              : message.timestamp;
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      object.magic =
        options.bytes === String
          ? $util.base64.encode(message.magic, 0, message.magic.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.magic)
            : message.magic;
    if (message.vocals && message.vocals.length) {
      object.vocals = [];
      for (var j = 0; j < message.vocals.length; ++j)
        object.vocals[j] =
          options.bytes === String
            ? $util.base64.encode(
                message.vocals[j],
                0,
                message.vocals[j].length,
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.vocals[j])
              : message.vocals[j];
    }
    if (message.mutes && message.mutes.length) {
      object.mutes = [];
      for (var j = 0; j < message.mutes.length; ++j)
        object.mutes[j] =
          options.bytes === String
            ? $util.base64.encode(message.mutes[j], 0, message.mutes[j].length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.mutes[j])
              : message.mutes[j];
    }
    return object;
  };

  /**
   * Converts this DwarfPing to JSON.
   * @function toJSON
   * @memberof DwarfPing
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  DwarfPing.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for DwarfPing
   * @function getTypeUrl
   * @memberof DwarfPing
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  DwarfPing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/DwarfPing";
  };

  return DwarfPing;
})();

$root.StationModel = (function () {
  /**
   * Properties of a StationModel.
   * @exports IStationModel
   * @interface IStationModel
   * @property {number|null} [family] StationModel family
   * @property {number|null} [revision] StationModel revision
   */

  /**
   * Constructs a new StationModel.
   * @exports StationModel
   * @classdesc Represents a StationModel.
   * @implements IStationModel
   * @constructor
   * @param {IStationModel=} [properties] Properties to set
   */
  function StationModel(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * StationModel family.
   * @member {number} family
   * @memberof StationModel
   * @instance
   */
  StationModel.prototype.family = 0;

  /**
   * StationModel revision.
   * @member {number} revision
   * @memberof StationModel
   * @instance
   */
  StationModel.prototype.revision = 0;

  /**
   * Creates a new StationModel instance using the specified properties.
   * @function create
   * @memberof StationModel
   * @static
   * @param {IStationModel=} [properties] Properties to set
   * @returns {StationModel} StationModel instance
   */
  StationModel.create = function create(properties) {
    return new StationModel(properties);
  };

  /**
   * Encodes the specified StationModel message. Does not implicitly {@link StationModel.verify|verify} messages.
   * @function encode
   * @memberof StationModel
   * @static
   * @param {IStationModel} message StationModel message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  StationModel.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.family != null && Object.hasOwnProperty.call(message, "family"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.family);
    if (
      message.revision != null &&
      Object.hasOwnProperty.call(message, "revision")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.revision);
    return writer;
  };

  /**
   * Encodes the specified StationModel message, length delimited. Does not implicitly {@link StationModel.verify|verify} messages.
   * @function encodeDelimited
   * @memberof StationModel
   * @static
   * @param {IStationModel} message StationModel message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  StationModel.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a StationModel message from the specified reader or buffer.
   * @function decode
   * @memberof StationModel
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {StationModel} StationModel
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  StationModel.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.StationModel();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.family = reader.uint32();
          break;
        }
        case 2: {
          message.revision = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a StationModel message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof StationModel
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {StationModel} StationModel
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  StationModel.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a StationModel message.
   * @function verify
   * @memberof StationModel
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  StationModel.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.family != null && Object.hasOwnProperty.call(message, "family"))
      if (!$util.isInteger(message.family)) return "family: integer expected";
    if (
      message.revision != null &&
      Object.hasOwnProperty.call(message, "revision")
    )
      if (!$util.isInteger(message.revision))
        return "revision: integer expected";
    return null;
  };

  /**
   * Creates a StationModel message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof StationModel
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {StationModel} StationModel
   */
  StationModel.fromObject = function fromObject(object, long) {
    if (object instanceof $root.StationModel) return object;
    if (!$util.isObject(object))
      throw TypeError(".StationModel: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.StationModel();
    if (object.family != null) message.family = object.family >>> 0;
    if (object.revision != null) message.revision = object.revision >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a StationModel message. Also converts values to other types if specified.
   * @function toObject
   * @memberof StationModel
   * @static
   * @param {StationModel} message StationModel
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  StationModel.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.family = 0;
      object.revision = 0;
    }
    if (message.family != null && Object.hasOwnProperty.call(message, "family"))
      object.family = message.family;
    if (
      message.revision != null &&
      Object.hasOwnProperty.call(message, "revision")
    )
      object.revision = message.revision;
    return object;
  };

  /**
   * Converts this StationModel to JSON.
   * @function toJSON
   * @memberof StationModel
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  StationModel.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for StationModel
   * @function getTypeUrl
   * @memberof StationModel
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  StationModel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/StationModel";
  };

  return StationModel;
})();

$root.NifAP = (function () {
  /**
   * Properties of a NifAP.
   * @exports INifAP
   * @interface INifAP
   * @property {string|null} [ifname] NifAP ifname
   * @property {number|null} [mode] NifAP mode
   * @property {string|null} [countryCode] NifAP countryCode
   * @property {string|null} [ssid] NifAP ssid
   * @property {string|null} [sec] NifAP sec
   * @property {string|null} [psw] NifAP psw
   * @property {Uint8Array|null} [ipv4] NifAP ipv4
   * @property {Uint8Array|null} [ipv6] NifAP ipv6
   */

  /**
   * Constructs a new NifAP.
   * @exports NifAP
   * @classdesc Represents a NifAP.
   * @implements INifAP
   * @constructor
   * @param {INifAP=} [properties] Properties to set
   */
  function NifAP(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * NifAP ifname.
   * @member {string} ifname
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.ifname = "";

  /**
   * NifAP mode.
   * @member {number} mode
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.mode = 0;

  /**
   * NifAP countryCode.
   * @member {string} countryCode
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.countryCode = "";

  /**
   * NifAP ssid.
   * @member {string} ssid
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.ssid = "";

  /**
   * NifAP sec.
   * @member {string} sec
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.sec = "";

  /**
   * NifAP psw.
   * @member {string} psw
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.psw = "";

  /**
   * NifAP ipv4.
   * @member {Uint8Array} ipv4
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.ipv4 = $util.newBuffer([]);

  /**
   * NifAP ipv6.
   * @member {Uint8Array|null|undefined} ipv6
   * @memberof NifAP
   * @instance
   */
  NifAP.prototype.ipv6 = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifAP.prototype, "_ipv6", {
    get: $util.oneOfGetter(($oneOfFields = ["ipv6"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new NifAP instance using the specified properties.
   * @function create
   * @memberof NifAP
   * @static
   * @param {INifAP=} [properties] Properties to set
   * @returns {NifAP} NifAP instance
   */
  NifAP.create = function create(properties) {
    return new NifAP(properties);
  };

  /**
   * Encodes the specified NifAP message. Does not implicitly {@link NifAP.verify|verify} messages.
   * @function encode
   * @memberof NifAP
   * @static
   * @param {INifAP} message NifAP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  NifAP.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.ifname);
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
    if (
      message.countryCode != null &&
      Object.hasOwnProperty.call(message, "countryCode")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.countryCode);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.ssid);
    if (message.sec != null && Object.hasOwnProperty.call(message, "sec"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.sec);
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.psw);
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4"))
      writer.uint32(/* id 7, wireType 2 =*/ 58).bytes(message.ipv4);
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6"))
      writer.uint32(/* id 8, wireType 2 =*/ 66).bytes(message.ipv6);
    return writer;
  };

  /**
   * Encodes the specified NifAP message, length delimited. Does not implicitly {@link NifAP.verify|verify} messages.
   * @function encodeDelimited
   * @memberof NifAP
   * @static
   * @param {INifAP} message NifAP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  NifAP.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a NifAP message from the specified reader or buffer.
   * @function decode
   * @memberof NifAP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {NifAP} NifAP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  NifAP.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.NifAP();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.ifname = reader.string();
          break;
        }
        case 2: {
          message.mode = reader.int32();
          break;
        }
        case 3: {
          message.countryCode = reader.string();
          break;
        }
        case 4: {
          message.ssid = reader.string();
          break;
        }
        case 5: {
          message.sec = reader.string();
          break;
        }
        case 6: {
          message.psw = reader.string();
          break;
        }
        case 7: {
          message.ipv4 = reader.bytes();
          break;
        }
        case 8: {
          message.ipv6 = reader.bytes();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a NifAP message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof NifAP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {NifAP} NifAP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  NifAP.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a NifAP message.
   * @function verify
   * @memberof NifAP
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  NifAP.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      if (!$util.isString(message.ifname)) return "ifname: string expected";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (
      message.countryCode != null &&
      Object.hasOwnProperty.call(message, "countryCode")
    )
      if (!$util.isString(message.countryCode))
        return "countryCode: string expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    if (message.sec != null && Object.hasOwnProperty.call(message, "sec"))
      if (!$util.isString(message.sec)) return "sec: string expected";
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      if (!$util.isString(message.psw)) return "psw: string expected";
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4"))
      if (!(
        (message.ipv4 && typeof message.ipv4.length === "number") ||
        $util.isString(message.ipv4)
      ))
        return "ipv4: buffer expected";
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6")) {
      properties._ipv6 = 1;
      if (!(
        (message.ipv6 && typeof message.ipv6.length === "number") ||
        $util.isString(message.ipv6)
      ))
        return "ipv6: buffer expected";
    }
    return null;
  };

  /**
   * Creates a NifAP message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof NifAP
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {NifAP} NifAP
   */
  NifAP.fromObject = function fromObject(object, long) {
    if (object instanceof $root.NifAP) return object;
    if (!$util.isObject(object)) throw TypeError(".NifAP: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.NifAP();
    if (object.ifname != null) message.ifname = String(object.ifname);
    if (object.mode != null) message.mode = object.mode | 0;
    if (object.countryCode != null)
      message.countryCode = String(object.countryCode);
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.sec != null) message.sec = String(object.sec);
    if (object.psw != null) message.psw = String(object.psw);
    if (object.ipv4 != null)
      if (typeof object.ipv4 === "string")
        $util.base64.decode(
          object.ipv4,
          (message.ipv4 = $util.newBuffer($util.base64.length(object.ipv4))),
          0,
        );
      else if (object.ipv4.length >= 0) message.ipv4 = object.ipv4;
    if (object.ipv6 != null)
      if (typeof object.ipv6 === "string")
        $util.base64.decode(
          object.ipv6,
          (message.ipv6 = $util.newBuffer($util.base64.length(object.ipv6))),
          0,
        );
      else if (object.ipv6.length >= 0) message.ipv6 = object.ipv6;
    return message;
  };

  /**
   * Creates a plain object from a NifAP message. Also converts values to other types if specified.
   * @function toObject
   * @memberof NifAP
   * @static
   * @param {NifAP} message NifAP
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  NifAP.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.ifname = "";
      object.mode = 0;
      object.countryCode = "";
      object.ssid = "";
      object.sec = "";
      object.psw = "";
      if (options.bytes === String) object.ipv4 = "";
      else {
        object.ipv4 = [];
        if (options.bytes !== Array) object.ipv4 = $util.newBuffer(object.ipv4);
      }
    }
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      object.ifname = message.ifname;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (
      message.countryCode != null &&
      Object.hasOwnProperty.call(message, "countryCode")
    )
      object.countryCode = message.countryCode;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      object.ssid = message.ssid;
    if (message.sec != null && Object.hasOwnProperty.call(message, "sec"))
      object.sec = message.sec;
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      object.psw = message.psw;
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4"))
      object.ipv4 =
        options.bytes === String
          ? $util.base64.encode(message.ipv4, 0, message.ipv4.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.ipv4)
            : message.ipv4;
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6")) {
      object.ipv6 =
        options.bytes === String
          ? $util.base64.encode(message.ipv6, 0, message.ipv6.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.ipv6)
            : message.ipv6;
      if (options.oneofs) object._ipv6 = "ipv6";
    }
    return object;
  };

  /**
   * Converts this NifAP to JSON.
   * @function toJSON
   * @memberof NifAP
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  NifAP.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for NifAP
   * @function getTypeUrl
   * @memberof NifAP
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  NifAP.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/NifAP";
  };

  return NifAP;
})();

$root.NifSTA = (function () {
  /**
   * Properties of a NifSTA.
   * @exports INifSTA
   * @interface INifSTA
   * @property {string|null} [ifname] NifSTA ifname
   * @property {string|null} [ssid] NifSTA ssid
   * @property {string|null} [psw] NifSTA psw
   * @property {number|null} [rssi] NifSTA rssi
   * @property {Uint8Array|null} [ipv4] NifSTA ipv4
   * @property {Uint8Array|null} [ipv6] NifSTA ipv6
   */

  /**
   * Constructs a new NifSTA.
   * @exports NifSTA
   * @classdesc Represents a NifSTA.
   * @implements INifSTA
   * @constructor
   * @param {INifSTA=} [properties] Properties to set
   */
  function NifSTA(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * NifSTA ifname.
   * @member {string} ifname
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.ifname = "";

  /**
   * NifSTA ssid.
   * @member {string|null|undefined} ssid
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.ssid = null;

  /**
   * NifSTA psw.
   * @member {string|null|undefined} psw
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.psw = null;

  /**
   * NifSTA rssi.
   * @member {number|null|undefined} rssi
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.rssi = null;

  /**
   * NifSTA ipv4.
   * @member {Uint8Array|null|undefined} ipv4
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.ipv4 = null;

  /**
   * NifSTA ipv6.
   * @member {Uint8Array|null|undefined} ipv6
   * @memberof NifSTA
   * @instance
   */
  NifSTA.prototype.ipv6 = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifSTA.prototype, "_ssid", {
    get: $util.oneOfGetter(($oneOfFields = ["ssid"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifSTA.prototype, "_psw", {
    get: $util.oneOfGetter(($oneOfFields = ["psw"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifSTA.prototype, "_rssi", {
    get: $util.oneOfGetter(($oneOfFields = ["rssi"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifSTA.prototype, "_ipv4", {
    get: $util.oneOfGetter(($oneOfFields = ["ipv4"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(NifSTA.prototype, "_ipv6", {
    get: $util.oneOfGetter(($oneOfFields = ["ipv6"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new NifSTA instance using the specified properties.
   * @function create
   * @memberof NifSTA
   * @static
   * @param {INifSTA=} [properties] Properties to set
   * @returns {NifSTA} NifSTA instance
   */
  NifSTA.create = function create(properties) {
    return new NifSTA(properties);
  };

  /**
   * Encodes the specified NifSTA message. Does not implicitly {@link NifSTA.verify|verify} messages.
   * @function encode
   * @memberof NifSTA
   * @static
   * @param {INifSTA} message NifSTA message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  NifSTA.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.ifname);
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.ssid);
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.psw);
    if (message.rssi != null && Object.hasOwnProperty.call(message, "rssi"))
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.rssi);
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.ipv4);
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).bytes(message.ipv6);
    return writer;
  };

  /**
   * Encodes the specified NifSTA message, length delimited. Does not implicitly {@link NifSTA.verify|verify} messages.
   * @function encodeDelimited
   * @memberof NifSTA
   * @static
   * @param {INifSTA} message NifSTA message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  NifSTA.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a NifSTA message from the specified reader or buffer.
   * @function decode
   * @memberof NifSTA
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {NifSTA} NifSTA
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  NifSTA.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.NifSTA();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.ifname = reader.string();
          break;
        }
        case 2: {
          message.ssid = reader.string();
          break;
        }
        case 3: {
          message.psw = reader.string();
          break;
        }
        case 4: {
          message.rssi = reader.int32();
          break;
        }
        case 5: {
          message.ipv4 = reader.bytes();
          break;
        }
        case 6: {
          message.ipv6 = reader.bytes();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a NifSTA message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof NifSTA
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {NifSTA} NifSTA
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  NifSTA.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a NifSTA message.
   * @function verify
   * @memberof NifSTA
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  NifSTA.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      if (!$util.isString(message.ifname)) return "ifname: string expected";
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid")) {
      properties._ssid = 1;
      if (!$util.isString(message.ssid)) return "ssid: string expected";
    }
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw")) {
      properties._psw = 1;
      if (!$util.isString(message.psw)) return "psw: string expected";
    }
    if (message.rssi != null && Object.hasOwnProperty.call(message, "rssi")) {
      properties._rssi = 1;
      if (!$util.isInteger(message.rssi)) return "rssi: integer expected";
    }
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4")) {
      properties._ipv4 = 1;
      if (!(
        (message.ipv4 && typeof message.ipv4.length === "number") ||
        $util.isString(message.ipv4)
      ))
        return "ipv4: buffer expected";
    }
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6")) {
      properties._ipv6 = 1;
      if (!(
        (message.ipv6 && typeof message.ipv6.length === "number") ||
        $util.isString(message.ipv6)
      ))
        return "ipv6: buffer expected";
    }
    return null;
  };

  /**
   * Creates a NifSTA message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof NifSTA
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {NifSTA} NifSTA
   */
  NifSTA.fromObject = function fromObject(object, long) {
    if (object instanceof $root.NifSTA) return object;
    if (!$util.isObject(object)) throw TypeError(".NifSTA: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.NifSTA();
    if (object.ifname != null) message.ifname = String(object.ifname);
    if (object.ssid != null) message.ssid = String(object.ssid);
    if (object.psw != null) message.psw = String(object.psw);
    if (object.rssi != null) message.rssi = object.rssi | 0;
    if (object.ipv4 != null)
      if (typeof object.ipv4 === "string")
        $util.base64.decode(
          object.ipv4,
          (message.ipv4 = $util.newBuffer($util.base64.length(object.ipv4))),
          0,
        );
      else if (object.ipv4.length >= 0) message.ipv4 = object.ipv4;
    if (object.ipv6 != null)
      if (typeof object.ipv6 === "string")
        $util.base64.decode(
          object.ipv6,
          (message.ipv6 = $util.newBuffer($util.base64.length(object.ipv6))),
          0,
        );
      else if (object.ipv6.length >= 0) message.ipv6 = object.ipv6;
    return message;
  };

  /**
   * Creates a plain object from a NifSTA message. Also converts values to other types if specified.
   * @function toObject
   * @memberof NifSTA
   * @static
   * @param {NifSTA} message NifSTA
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  NifSTA.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.ifname = "";
    if (message.ifname != null && Object.hasOwnProperty.call(message, "ifname"))
      object.ifname = message.ifname;
    if (message.ssid != null && Object.hasOwnProperty.call(message, "ssid")) {
      object.ssid = message.ssid;
      if (options.oneofs) object._ssid = "ssid";
    }
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw")) {
      object.psw = message.psw;
      if (options.oneofs) object._psw = "psw";
    }
    if (message.rssi != null && Object.hasOwnProperty.call(message, "rssi")) {
      object.rssi = message.rssi;
      if (options.oneofs) object._rssi = "rssi";
    }
    if (message.ipv4 != null && Object.hasOwnProperty.call(message, "ipv4")) {
      object.ipv4 =
        options.bytes === String
          ? $util.base64.encode(message.ipv4, 0, message.ipv4.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.ipv4)
            : message.ipv4;
      if (options.oneofs) object._ipv4 = "ipv4";
    }
    if (message.ipv6 != null && Object.hasOwnProperty.call(message, "ipv6")) {
      object.ipv6 =
        options.bytes === String
          ? $util.base64.encode(message.ipv6, 0, message.ipv6.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.ipv6)
            : message.ipv6;
      if (options.oneofs) object._ipv6 = "ipv6";
    }
    return object;
  };

  /**
   * Converts this NifSTA to JSON.
   * @function toJSON
   * @memberof NifSTA
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  NifSTA.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for NifSTA
   * @function getTypeUrl
   * @memberof NifSTA
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  NifSTA.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/NifSTA";
  };

  return NifSTA;
})();

$root.DwarfEcho = (function () {
  /**
   * Properties of a DwarfEcho.
   * @exports IDwarfEcho
   * @interface IDwarfEcho
   * @property {VocalType|null} [vocaltype] DwarfEcho vocaltype
   * @property {number|Long|null} [timestamp] DwarfEcho timestamp
   * @property {Uint8Array|null} [magic] DwarfEcho magic
   * @property {number|Long|null} [tsPing] DwarfEcho tsPing
   * @property {Uint8Array|null} [macAddress] DwarfEcho macAddress
   * @property {IStationModel|null} [model] DwarfEcho model
   * @property {string|null} [sn] DwarfEcho sn
   * @property {string|null} [name] DwarfEcho name
   * @property {string|null} [psw] DwarfEcho psw
   * @property {string|null} [fwVersion] DwarfEcho fwVersion
   * @property {string|null} [wsScheme] DwarfEcho wsScheme
   * @property {number|null} [session] DwarfEcho session
   * @property {INifAP|null} [ap] DwarfEcho ap
   * @property {INifSTA|null} [sta] DwarfEcho sta
   */

  /**
   * Constructs a new DwarfEcho.
   * @exports DwarfEcho
   * @classdesc Represents a DwarfEcho.
   * @implements IDwarfEcho
   * @constructor
   * @param {IDwarfEcho=} [properties] Properties to set
   */
  function DwarfEcho(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * DwarfEcho vocaltype.
   * @member {VocalType} vocaltype
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.vocaltype = 0;

  /**
   * DwarfEcho timestamp.
   * @member {number|Long} timestamp
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.timestamp = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * DwarfEcho magic.
   * @member {Uint8Array} magic
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.magic = $util.newBuffer([]);

  /**
   * DwarfEcho tsPing.
   * @member {number|Long} tsPing
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.tsPing = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;

  /**
   * DwarfEcho macAddress.
   * @member {Uint8Array} macAddress
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.macAddress = $util.newBuffer([]);

  /**
   * DwarfEcho model.
   * @member {IStationModel|null|undefined} model
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.model = null;

  /**
   * DwarfEcho sn.
   * @member {string} sn
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.sn = "";

  /**
   * DwarfEcho name.
   * @member {string} name
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.name = "";

  /**
   * DwarfEcho psw.
   * @member {string} psw
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.psw = "";

  /**
   * DwarfEcho fwVersion.
   * @member {string} fwVersion
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.fwVersion = "";

  /**
   * DwarfEcho wsScheme.
   * @member {string} wsScheme
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.wsScheme = "";

  /**
   * DwarfEcho session.
   * @member {number} session
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.session = 0;

  /**
   * DwarfEcho ap.
   * @member {INifAP|null|undefined} ap
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.ap = null;

  /**
   * DwarfEcho sta.
   * @member {INifSTA|null|undefined} sta
   * @memberof DwarfEcho
   * @instance
   */
  DwarfEcho.prototype.sta = null;

  /**
   * Creates a new DwarfEcho instance using the specified properties.
   * @function create
   * @memberof DwarfEcho
   * @static
   * @param {IDwarfEcho=} [properties] Properties to set
   * @returns {DwarfEcho} DwarfEcho instance
   */
  DwarfEcho.create = function create(properties) {
    return new DwarfEcho(properties);
  };

  /**
   * Encodes the specified DwarfEcho message. Does not implicitly {@link DwarfEcho.verify|verify} messages.
   * @function encode
   * @memberof DwarfEcho
   * @static
   * @param {IDwarfEcho} message DwarfEcho message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DwarfEcho.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.vocaltype);
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.timestamp);
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.magic);
    if (message.tsPing != null && Object.hasOwnProperty.call(message, "tsPing"))
      writer.uint32(/* id 4, wireType 0 =*/ 32).uint64(message.tsPing);
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.macAddress);
    if (message.model != null && Object.hasOwnProperty.call(message, "model"))
      $root.StationModel.encode(
        message.model,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.sn);
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      writer.uint32(/* id 8, wireType 2 =*/ 66).string(message.name);
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      writer.uint32(/* id 9, wireType 2 =*/ 74).string(message.psw);
    if (
      message.fwVersion != null &&
      Object.hasOwnProperty.call(message, "fwVersion")
    )
      writer.uint32(/* id 10, wireType 2 =*/ 82).string(message.fwVersion);
    if (
      message.wsScheme != null &&
      Object.hasOwnProperty.call(message, "wsScheme")
    )
      writer.uint32(/* id 11, wireType 2 =*/ 90).string(message.wsScheme);
    if (
      message.session != null &&
      Object.hasOwnProperty.call(message, "session")
    )
      writer.uint32(/* id 12, wireType 0 =*/ 96).uint32(message.session);
    if (message.ap != null && Object.hasOwnProperty.call(message, "ap"))
      $root.NifAP.encode(
        message.ap,
        writer.uint32(/* id 13, wireType 2 =*/ 106).fork(),
        q + 1,
      ).ldelim();
    if (message.sta != null && Object.hasOwnProperty.call(message, "sta"))
      $root.NifSTA.encode(
        message.sta,
        writer.uint32(/* id 14, wireType 2 =*/ 114).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified DwarfEcho message, length delimited. Does not implicitly {@link DwarfEcho.verify|verify} messages.
   * @function encodeDelimited
   * @memberof DwarfEcho
   * @static
   * @param {IDwarfEcho} message DwarfEcho message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DwarfEcho.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a DwarfEcho message from the specified reader or buffer.
   * @function decode
   * @memberof DwarfEcho
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {DwarfEcho} DwarfEcho
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DwarfEcho.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.DwarfEcho();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.vocaltype = reader.int32();
          break;
        }
        case 2: {
          message.timestamp = reader.uint64();
          break;
        }
        case 3: {
          message.magic = reader.bytes();
          break;
        }
        case 4: {
          message.tsPing = reader.uint64();
          break;
        }
        case 5: {
          message.macAddress = reader.bytes();
          break;
        }
        case 6: {
          message.model = $root.StationModel.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 7: {
          message.sn = reader.string();
          break;
        }
        case 8: {
          message.name = reader.string();
          break;
        }
        case 9: {
          message.psw = reader.string();
          break;
        }
        case 10: {
          message.fwVersion = reader.string();
          break;
        }
        case 11: {
          message.wsScheme = reader.string();
          break;
        }
        case 12: {
          message.session = reader.uint32();
          break;
        }
        case 13: {
          message.ap = $root.NifAP.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 14: {
          message.sta = $root.NifSTA.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a DwarfEcho message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof DwarfEcho
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {DwarfEcho} DwarfEcho
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DwarfEcho.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a DwarfEcho message.
   * @function verify
   * @memberof DwarfEcho
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  DwarfEcho.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      switch (message.vocaltype) {
        default:
          return "vocaltype: enum value expected";
        case 0:
        case 1:
        case 2:
          break;
      }
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (
        !$util.isInteger(message.timestamp) &&
        !(
          message.timestamp &&
          $util.isInteger(message.timestamp.low) &&
          $util.isInteger(message.timestamp.high)
        )
      )
        return "timestamp: integer|Long expected";
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      if (!(
        (message.magic && typeof message.magic.length === "number") ||
        $util.isString(message.magic)
      ))
        return "magic: buffer expected";
    if (message.tsPing != null && Object.hasOwnProperty.call(message, "tsPing"))
      if (
        !$util.isInteger(message.tsPing) &&
        !(
          message.tsPing &&
          $util.isInteger(message.tsPing.low) &&
          $util.isInteger(message.tsPing.high)
        )
      )
        return "tsPing: integer|Long expected";
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      if (!(
        (message.macAddress && typeof message.macAddress.length === "number") ||
        $util.isString(message.macAddress)
      ))
        return "macAddress: buffer expected";
    if (message.model != null && Object.hasOwnProperty.call(message, "model")) {
      var error = $root.StationModel.verify(message.model, long + 1);
      if (error) return "model." + error;
    }
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      if (!$util.isString(message.sn)) return "sn: string expected";
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      if (!$util.isString(message.name)) return "name: string expected";
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      if (!$util.isString(message.psw)) return "psw: string expected";
    if (
      message.fwVersion != null &&
      Object.hasOwnProperty.call(message, "fwVersion")
    )
      if (!$util.isString(message.fwVersion))
        return "fwVersion: string expected";
    if (
      message.wsScheme != null &&
      Object.hasOwnProperty.call(message, "wsScheme")
    )
      if (!$util.isString(message.wsScheme)) return "wsScheme: string expected";
    if (
      message.session != null &&
      Object.hasOwnProperty.call(message, "session")
    )
      if (!$util.isInteger(message.session)) return "session: integer expected";
    if (message.ap != null && Object.hasOwnProperty.call(message, "ap")) {
      var error = $root.NifAP.verify(message.ap, long + 1);
      if (error) return "ap." + error;
    }
    if (message.sta != null && Object.hasOwnProperty.call(message, "sta")) {
      var error = $root.NifSTA.verify(message.sta, long + 1);
      if (error) return "sta." + error;
    }
    return null;
  };

  /**
   * Creates a DwarfEcho message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof DwarfEcho
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {DwarfEcho} DwarfEcho
   */
  DwarfEcho.fromObject = function fromObject(object, long) {
    if (object instanceof $root.DwarfEcho) return object;
    if (!$util.isObject(object)) throw TypeError(".DwarfEcho: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.DwarfEcho();
    switch (object.vocaltype) {
      default:
        if (typeof object.vocaltype === "number") {
          message.vocaltype = object.vocaltype;
          break;
        }
        break;
      case "VT_UNKNOWN":
      case 0:
        message.vocaltype = 0;
        break;
      case "VT_PING":
      case 1:
        message.vocaltype = 1;
        break;
      case "VT_ECHO":
      case 2:
        message.vocaltype = 2;
        break;
    }
    if (object.timestamp != null)
      if ($util.Long)
        message.timestamp = $util.Long.fromValue(object.timestamp, true);
      else if (typeof object.timestamp === "string")
        message.timestamp = parseInt(object.timestamp, 10);
      else if (typeof object.timestamp === "number")
        message.timestamp = object.timestamp;
      else if (typeof object.timestamp === "object")
        message.timestamp = new $util.LongBits(
          object.timestamp.low >>> 0,
          object.timestamp.high >>> 0,
        ).toNumber(true);
    if (object.magic != null)
      if (typeof object.magic === "string")
        $util.base64.decode(
          object.magic,
          (message.magic = $util.newBuffer($util.base64.length(object.magic))),
          0,
        );
      else if (object.magic.length >= 0) message.magic = object.magic;
    if (object.tsPing != null)
      if ($util.Long)
        message.tsPing = $util.Long.fromValue(object.tsPing, true);
      else if (typeof object.tsPing === "string")
        message.tsPing = parseInt(object.tsPing, 10);
      else if (typeof object.tsPing === "number")
        message.tsPing = object.tsPing;
      else if (typeof object.tsPing === "object")
        message.tsPing = new $util.LongBits(
          object.tsPing.low >>> 0,
          object.tsPing.high >>> 0,
        ).toNumber(true);
    if (object.macAddress != null)
      if (typeof object.macAddress === "string")
        $util.base64.decode(
          object.macAddress,
          (message.macAddress = $util.newBuffer(
            $util.base64.length(object.macAddress),
          )),
          0,
        );
      else if (object.macAddress.length >= 0)
        message.macAddress = object.macAddress;
    if (object.model != null) {
      if (!$util.isObject(object.model))
        throw TypeError(".DwarfEcho.model: object expected");
      message.model = $root.StationModel.fromObject(object.model, long + 1);
    }
    if (object.sn != null) message.sn = String(object.sn);
    if (object.name != null) message.name = String(object.name);
    if (object.psw != null) message.psw = String(object.psw);
    if (object.fwVersion != null) message.fwVersion = String(object.fwVersion);
    if (object.wsScheme != null) message.wsScheme = String(object.wsScheme);
    if (object.session != null) message.session = object.session >>> 0;
    if (object.ap != null) {
      if (!$util.isObject(object.ap))
        throw TypeError(".DwarfEcho.ap: object expected");
      message.ap = $root.NifAP.fromObject(object.ap, long + 1);
    }
    if (object.sta != null) {
      if (!$util.isObject(object.sta))
        throw TypeError(".DwarfEcho.sta: object expected");
      message.sta = $root.NifSTA.fromObject(object.sta, long + 1);
    }
    return message;
  };

  /**
   * Creates a plain object from a DwarfEcho message. Also converts values to other types if specified.
   * @function toObject
   * @memberof DwarfEcho
   * @static
   * @param {DwarfEcho} message DwarfEcho
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  DwarfEcho.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.vocaltype = options.enums === String ? "VT_UNKNOWN" : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.timestamp =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.timestamp =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if (options.bytes === String) object.magic = "";
      else {
        object.magic = [];
        if (options.bytes !== Array)
          object.magic = $util.newBuffer(object.magic);
      }
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.tsPing =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.tsPing =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if (options.bytes === String) object.macAddress = "";
      else {
        object.macAddress = [];
        if (options.bytes !== Array)
          object.macAddress = $util.newBuffer(object.macAddress);
      }
      object.model = null;
      object.sn = "";
      object.name = "";
      object.psw = "";
      object.fwVersion = "";
      object.wsScheme = "";
      object.session = 0;
      object.ap = null;
      object.sta = null;
    }
    if (
      message.vocaltype != null &&
      Object.hasOwnProperty.call(message, "vocaltype")
    )
      object.vocaltype =
        options.enums === String
          ? $root.VocalType[message.vocaltype] === undefined
            ? message.vocaltype
            : $root.VocalType[message.vocaltype]
          : message.vocaltype;
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.timestamp =
          typeof message.timestamp === "number"
            ? BigInt(message.timestamp)
            : $util.Long.fromBits(
                message.timestamp.low >>> 0,
                message.timestamp.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.timestamp === "number")
        object.timestamp =
          options.longs === String
            ? String(message.timestamp)
            : message.timestamp;
      else
        object.timestamp =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.timestamp)
            : options.longs === Number
              ? new $util.LongBits(
                  message.timestamp.low >>> 0,
                  message.timestamp.high >>> 0,
                ).toNumber(true)
              : message.timestamp;
    if (message.magic != null && Object.hasOwnProperty.call(message, "magic"))
      object.magic =
        options.bytes === String
          ? $util.base64.encode(message.magic, 0, message.magic.length)
          : options.bytes === Array
            ? Array.prototype.slice.call(message.magic)
            : message.magic;
    if (message.tsPing != null && Object.hasOwnProperty.call(message, "tsPing"))
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.tsPing =
          typeof message.tsPing === "number"
            ? BigInt(message.tsPing)
            : $util.Long.fromBits(
                message.tsPing.low >>> 0,
                message.tsPing.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.tsPing === "number")
        object.tsPing =
          options.longs === String ? String(message.tsPing) : message.tsPing;
      else
        object.tsPing =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.tsPing)
            : options.longs === Number
              ? new $util.LongBits(
                  message.tsPing.low >>> 0,
                  message.tsPing.high >>> 0,
                ).toNumber(true)
              : message.tsPing;
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      object.macAddress =
        options.bytes === String
          ? $util.base64.encode(
              message.macAddress,
              0,
              message.macAddress.length,
            )
          : options.bytes === Array
            ? Array.prototype.slice.call(message.macAddress)
            : message.macAddress;
    if (message.model != null && Object.hasOwnProperty.call(message, "model"))
      object.model = $root.StationModel.toObject(message.model, options, q + 1);
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      object.sn = message.sn;
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      object.name = message.name;
    if (message.psw != null && Object.hasOwnProperty.call(message, "psw"))
      object.psw = message.psw;
    if (
      message.fwVersion != null &&
      Object.hasOwnProperty.call(message, "fwVersion")
    )
      object.fwVersion = message.fwVersion;
    if (
      message.wsScheme != null &&
      Object.hasOwnProperty.call(message, "wsScheme")
    )
      object.wsScheme = message.wsScheme;
    if (
      message.session != null &&
      Object.hasOwnProperty.call(message, "session")
    )
      object.session = message.session;
    if (message.ap != null && Object.hasOwnProperty.call(message, "ap"))
      object.ap = $root.NifAP.toObject(message.ap, options, q + 1);
    if (message.sta != null && Object.hasOwnProperty.call(message, "sta"))
      object.sta = $root.NifSTA.toObject(message.sta, options, q + 1);
    return object;
  };

  /**
   * Converts this DwarfEcho to JSON.
   * @function toJSON
   * @memberof DwarfEcho
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  DwarfEcho.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for DwarfEcho
   * @function getTypeUrl
   * @memberof DwarfEcho
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  DwarfEcho.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/DwarfEcho";
  };

  return DwarfEcho;
})();

$root.ReqOpenCamera = (function () {
  /**
   * Properties of a ReqOpenCamera.
   * @exports IReqOpenCamera
   * @interface IReqOpenCamera
   * @property {boolean|null} [binning] ReqOpenCamera binning
   * @property {number|null} [rtspEncodeType] ReqOpenCamera rtspEncodeType
   */

  /**
   * Constructs a new ReqOpenCamera.
   * @exports ReqOpenCamera
   * @classdesc Represents a ReqOpenCamera.
   * @implements IReqOpenCamera
   * @constructor
   * @param {IReqOpenCamera=} [properties] Properties to set
   */
  function ReqOpenCamera(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqOpenCamera binning.
   * @member {boolean} binning
   * @memberof ReqOpenCamera
   * @instance
   */
  ReqOpenCamera.prototype.binning = false;

  /**
   * ReqOpenCamera rtspEncodeType.
   * @member {number} rtspEncodeType
   * @memberof ReqOpenCamera
   * @instance
   */
  ReqOpenCamera.prototype.rtspEncodeType = 0;

  /**
   * Creates a new ReqOpenCamera instance using the specified properties.
   * @function create
   * @memberof ReqOpenCamera
   * @static
   * @param {IReqOpenCamera=} [properties] Properties to set
   * @returns {ReqOpenCamera} ReqOpenCamera instance
   */
  ReqOpenCamera.create = function create(properties) {
    return new ReqOpenCamera(properties);
  };

  /**
   * Encodes the specified ReqOpenCamera message. Does not implicitly {@link ReqOpenCamera.verify|verify} messages.
   * @function encode
   * @memberof ReqOpenCamera
   * @static
   * @param {IReqOpenCamera} message ReqOpenCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenCamera.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.binning != null &&
      Object.hasOwnProperty.call(message, "binning")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.binning);
    if (
      message.rtspEncodeType != null &&
      Object.hasOwnProperty.call(message, "rtspEncodeType")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.rtspEncodeType);
    return writer;
  };

  /**
   * Encodes the specified ReqOpenCamera message, length delimited. Does not implicitly {@link ReqOpenCamera.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOpenCamera
   * @static
   * @param {IReqOpenCamera} message ReqOpenCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenCamera.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOpenCamera message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOpenCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOpenCamera} ReqOpenCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenCamera.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOpenCamera();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.binning = reader.bool();
          break;
        }
        case 2: {
          message.rtspEncodeType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOpenCamera message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOpenCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOpenCamera} ReqOpenCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenCamera.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOpenCamera message.
   * @function verify
   * @memberof ReqOpenCamera
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOpenCamera.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.binning != null &&
      Object.hasOwnProperty.call(message, "binning")
    )
      if (typeof message.binning !== "boolean")
        return "binning: boolean expected";
    if (
      message.rtspEncodeType != null &&
      Object.hasOwnProperty.call(message, "rtspEncodeType")
    )
      if (!$util.isInteger(message.rtspEncodeType))
        return "rtspEncodeType: integer expected";
    return null;
  };

  /**
   * Creates a ReqOpenCamera message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOpenCamera
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOpenCamera} ReqOpenCamera
   */
  ReqOpenCamera.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOpenCamera) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqOpenCamera: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqOpenCamera();
    if (object.binning != null) message.binning = Boolean(object.binning);
    if (object.rtspEncodeType != null)
      message.rtspEncodeType = object.rtspEncodeType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqOpenCamera message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOpenCamera
   * @static
   * @param {ReqOpenCamera} message ReqOpenCamera
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOpenCamera.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.binning = false;
      object.rtspEncodeType = 0;
    }
    if (
      message.binning != null &&
      Object.hasOwnProperty.call(message, "binning")
    )
      object.binning = message.binning;
    if (
      message.rtspEncodeType != null &&
      Object.hasOwnProperty.call(message, "rtspEncodeType")
    )
      object.rtspEncodeType = message.rtspEncodeType;
    return object;
  };

  /**
   * Converts this ReqOpenCamera to JSON.
   * @function toJSON
   * @memberof ReqOpenCamera
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOpenCamera.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOpenCamera
   * @function getTypeUrl
   * @memberof ReqOpenCamera
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOpenCamera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOpenCamera";
  };

  return ReqOpenCamera;
})();

$root.ReqCloseCamera = (function () {
  /**
   * Properties of a ReqCloseCamera.
   * @exports IReqCloseCamera
   * @interface IReqCloseCamera
   */

  /**
   * Constructs a new ReqCloseCamera.
   * @exports ReqCloseCamera
   * @classdesc Represents a ReqCloseCamera.
   * @implements IReqCloseCamera
   * @constructor
   * @param {IReqCloseCamera=} [properties] Properties to set
   */
  function ReqCloseCamera(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCloseCamera instance using the specified properties.
   * @function create
   * @memberof ReqCloseCamera
   * @static
   * @param {IReqCloseCamera=} [properties] Properties to set
   * @returns {ReqCloseCamera} ReqCloseCamera instance
   */
  ReqCloseCamera.create = function create(properties) {
    return new ReqCloseCamera(properties);
  };

  /**
   * Encodes the specified ReqCloseCamera message. Does not implicitly {@link ReqCloseCamera.verify|verify} messages.
   * @function encode
   * @memberof ReqCloseCamera
   * @static
   * @param {IReqCloseCamera} message ReqCloseCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCloseCamera.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCloseCamera message, length delimited. Does not implicitly {@link ReqCloseCamera.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCloseCamera
   * @static
   * @param {IReqCloseCamera} message ReqCloseCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCloseCamera.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCloseCamera message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCloseCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCloseCamera} ReqCloseCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCloseCamera.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCloseCamera();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCloseCamera message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCloseCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCloseCamera} ReqCloseCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCloseCamera.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCloseCamera message.
   * @function verify
   * @memberof ReqCloseCamera
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCloseCamera.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCloseCamera message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCloseCamera
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCloseCamera} ReqCloseCamera
   */
  ReqCloseCamera.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCloseCamera) return object;
    return new $root.ReqCloseCamera();
  };

  /**
   * Creates a plain object from a ReqCloseCamera message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCloseCamera
   * @static
   * @param {ReqCloseCamera} message ReqCloseCamera
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCloseCamera.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCloseCamera to JSON.
   * @function toJSON
   * @memberof ReqCloseCamera
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCloseCamera.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCloseCamera
   * @function getTypeUrl
   * @memberof ReqCloseCamera
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCloseCamera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCloseCamera";
  };

  return ReqCloseCamera;
})();

$root.ReqPhoto = (function () {
  /**
   * Properties of a ReqPhoto.
   * @exports IReqPhoto
   * @interface IReqPhoto
   */

  /**
   * Constructs a new ReqPhoto.
   * @exports ReqPhoto
   * @classdesc Represents a ReqPhoto.
   * @implements IReqPhoto
   * @constructor
   * @param {IReqPhoto=} [properties] Properties to set
   */
  function ReqPhoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPhoto instance using the specified properties.
   * @function create
   * @memberof ReqPhoto
   * @static
   * @param {IReqPhoto=} [properties] Properties to set
   * @returns {ReqPhoto} ReqPhoto instance
   */
  ReqPhoto.create = function create(properties) {
    return new ReqPhoto(properties);
  };

  /**
   * Encodes the specified ReqPhoto message. Does not implicitly {@link ReqPhoto.verify|verify} messages.
   * @function encode
   * @memberof ReqPhoto
   * @static
   * @param {IReqPhoto} message ReqPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPhoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPhoto message, length delimited. Does not implicitly {@link ReqPhoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPhoto
   * @static
   * @param {IReqPhoto} message ReqPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPhoto.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPhoto message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPhoto} ReqPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPhoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPhoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPhoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPhoto} ReqPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPhoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPhoto message.
   * @function verify
   * @memberof ReqPhoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPhoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPhoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPhoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPhoto} ReqPhoto
   */
  ReqPhoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPhoto) return object;
    return new $root.ReqPhoto();
  };

  /**
   * Creates a plain object from a ReqPhoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPhoto
   * @static
   * @param {ReqPhoto} message ReqPhoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPhoto.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPhoto to JSON.
   * @function toJSON
   * @memberof ReqPhoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPhoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPhoto
   * @function getTypeUrl
   * @memberof ReqPhoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPhoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPhoto";
  };

  return ReqPhoto;
})();

$root.ReqBurstPhoto = (function () {
  /**
   * Properties of a ReqBurstPhoto.
   * @exports IReqBurstPhoto
   * @interface IReqBurstPhoto
   * @property {number|null} [count] ReqBurstPhoto count
   */

  /**
   * Constructs a new ReqBurstPhoto.
   * @exports ReqBurstPhoto
   * @classdesc Represents a ReqBurstPhoto.
   * @implements IReqBurstPhoto
   * @constructor
   * @param {IReqBurstPhoto=} [properties] Properties to set
   */
  function ReqBurstPhoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqBurstPhoto count.
   * @member {number} count
   * @memberof ReqBurstPhoto
   * @instance
   */
  ReqBurstPhoto.prototype.count = 0;

  /**
   * Creates a new ReqBurstPhoto instance using the specified properties.
   * @function create
   * @memberof ReqBurstPhoto
   * @static
   * @param {IReqBurstPhoto=} [properties] Properties to set
   * @returns {ReqBurstPhoto} ReqBurstPhoto instance
   */
  ReqBurstPhoto.create = function create(properties) {
    return new ReqBurstPhoto(properties);
  };

  /**
   * Encodes the specified ReqBurstPhoto message. Does not implicitly {@link ReqBurstPhoto.verify|verify} messages.
   * @function encode
   * @memberof ReqBurstPhoto
   * @static
   * @param {IReqBurstPhoto} message ReqBurstPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqBurstPhoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.count);
    return writer;
  };

  /**
   * Encodes the specified ReqBurstPhoto message, length delimited. Does not implicitly {@link ReqBurstPhoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqBurstPhoto
   * @static
   * @param {IReqBurstPhoto} message ReqBurstPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqBurstPhoto.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqBurstPhoto message from the specified reader or buffer.
   * @function decode
   * @memberof ReqBurstPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqBurstPhoto} ReqBurstPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqBurstPhoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqBurstPhoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.count = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqBurstPhoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqBurstPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqBurstPhoto} ReqBurstPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqBurstPhoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqBurstPhoto message.
   * @function verify
   * @memberof ReqBurstPhoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqBurstPhoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      if (!$util.isInteger(message.count)) return "count: integer expected";
    return null;
  };

  /**
   * Creates a ReqBurstPhoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqBurstPhoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqBurstPhoto} ReqBurstPhoto
   */
  ReqBurstPhoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqBurstPhoto) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqBurstPhoto: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqBurstPhoto();
    if (object.count != null) message.count = object.count | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqBurstPhoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqBurstPhoto
   * @static
   * @param {ReqBurstPhoto} message ReqBurstPhoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqBurstPhoto.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.count = 0;
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      object.count = message.count;
    return object;
  };

  /**
   * Converts this ReqBurstPhoto to JSON.
   * @function toJSON
   * @memberof ReqBurstPhoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqBurstPhoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqBurstPhoto
   * @function getTypeUrl
   * @memberof ReqBurstPhoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqBurstPhoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqBurstPhoto";
  };

  return ReqBurstPhoto;
})();

$root.ReqStopBurstPhoto = (function () {
  /**
   * Properties of a ReqStopBurstPhoto.
   * @exports IReqStopBurstPhoto
   * @interface IReqStopBurstPhoto
   */

  /**
   * Constructs a new ReqStopBurstPhoto.
   * @exports ReqStopBurstPhoto
   * @classdesc Represents a ReqStopBurstPhoto.
   * @implements IReqStopBurstPhoto
   * @constructor
   * @param {IReqStopBurstPhoto=} [properties] Properties to set
   */
  function ReqStopBurstPhoto(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopBurstPhoto instance using the specified properties.
   * @function create
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {IReqStopBurstPhoto=} [properties] Properties to set
   * @returns {ReqStopBurstPhoto} ReqStopBurstPhoto instance
   */
  ReqStopBurstPhoto.create = function create(properties) {
    return new ReqStopBurstPhoto(properties);
  };

  /**
   * Encodes the specified ReqStopBurstPhoto message. Does not implicitly {@link ReqStopBurstPhoto.verify|verify} messages.
   * @function encode
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {IReqStopBurstPhoto} message ReqStopBurstPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopBurstPhoto.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopBurstPhoto message, length delimited. Does not implicitly {@link ReqStopBurstPhoto.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {IReqStopBurstPhoto} message ReqStopBurstPhoto message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopBurstPhoto.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopBurstPhoto message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopBurstPhoto} ReqStopBurstPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopBurstPhoto.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopBurstPhoto();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopBurstPhoto message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopBurstPhoto} ReqStopBurstPhoto
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopBurstPhoto.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopBurstPhoto message.
   * @function verify
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopBurstPhoto.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopBurstPhoto message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopBurstPhoto} ReqStopBurstPhoto
   */
  ReqStopBurstPhoto.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopBurstPhoto) return object;
    return new $root.ReqStopBurstPhoto();
  };

  /**
   * Creates a plain object from a ReqStopBurstPhoto message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {ReqStopBurstPhoto} message ReqStopBurstPhoto
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopBurstPhoto.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopBurstPhoto to JSON.
   * @function toJSON
   * @memberof ReqStopBurstPhoto
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopBurstPhoto.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopBurstPhoto
   * @function getTypeUrl
   * @memberof ReqStopBurstPhoto
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopBurstPhoto.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopBurstPhoto";
  };

  return ReqStopBurstPhoto;
})();

$root.ReqStartRecord = (function () {
  /**
   * Properties of a ReqStartRecord.
   * @exports IReqStartRecord
   * @interface IReqStartRecord
   * @property {number|null} [encodeType] ReqStartRecord encodeType
   */

  /**
   * Constructs a new ReqStartRecord.
   * @exports ReqStartRecord
   * @classdesc Represents a ReqStartRecord.
   * @implements IReqStartRecord
   * @constructor
   * @param {IReqStartRecord=} [properties] Properties to set
   */
  function ReqStartRecord(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartRecord encodeType.
   * @member {number} encodeType
   * @memberof ReqStartRecord
   * @instance
   */
  ReqStartRecord.prototype.encodeType = 0;

  /**
   * Creates a new ReqStartRecord instance using the specified properties.
   * @function create
   * @memberof ReqStartRecord
   * @static
   * @param {IReqStartRecord=} [properties] Properties to set
   * @returns {ReqStartRecord} ReqStartRecord instance
   */
  ReqStartRecord.create = function create(properties) {
    return new ReqStartRecord(properties);
  };

  /**
   * Encodes the specified ReqStartRecord message. Does not implicitly {@link ReqStartRecord.verify|verify} messages.
   * @function encode
   * @memberof ReqStartRecord
   * @static
   * @param {IReqStartRecord} message ReqStartRecord message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartRecord.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.encodeType);
    return writer;
  };

  /**
   * Encodes the specified ReqStartRecord message, length delimited. Does not implicitly {@link ReqStartRecord.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartRecord
   * @static
   * @param {IReqStartRecord} message ReqStartRecord message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartRecord.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartRecord message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartRecord
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartRecord} ReqStartRecord
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartRecord.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartRecord();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.encodeType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartRecord message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartRecord
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartRecord} ReqStartRecord
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartRecord.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartRecord message.
   * @function verify
   * @memberof ReqStartRecord
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartRecord.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      if (!$util.isInteger(message.encodeType))
        return "encodeType: integer expected";
    return null;
  };

  /**
   * Creates a ReqStartRecord message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartRecord
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartRecord} ReqStartRecord
   */
  ReqStartRecord.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartRecord) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartRecord: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartRecord();
    if (object.encodeType != null) message.encodeType = object.encodeType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStartRecord message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartRecord
   * @static
   * @param {ReqStartRecord} message ReqStartRecord
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartRecord.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.encodeType = 0;
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      object.encodeType = message.encodeType;
    return object;
  };

  /**
   * Converts this ReqStartRecord to JSON.
   * @function toJSON
   * @memberof ReqStartRecord
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartRecord.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartRecord
   * @function getTypeUrl
   * @memberof ReqStartRecord
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartRecord";
  };

  return ReqStartRecord;
})();

$root.ReqStopRecord = (function () {
  /**
   * Properties of a ReqStopRecord.
   * @exports IReqStopRecord
   * @interface IReqStopRecord
   */

  /**
   * Constructs a new ReqStopRecord.
   * @exports ReqStopRecord
   * @classdesc Represents a ReqStopRecord.
   * @implements IReqStopRecord
   * @constructor
   * @param {IReqStopRecord=} [properties] Properties to set
   */
  function ReqStopRecord(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopRecord instance using the specified properties.
   * @function create
   * @memberof ReqStopRecord
   * @static
   * @param {IReqStopRecord=} [properties] Properties to set
   * @returns {ReqStopRecord} ReqStopRecord instance
   */
  ReqStopRecord.create = function create(properties) {
    return new ReqStopRecord(properties);
  };

  /**
   * Encodes the specified ReqStopRecord message. Does not implicitly {@link ReqStopRecord.verify|verify} messages.
   * @function encode
   * @memberof ReqStopRecord
   * @static
   * @param {IReqStopRecord} message ReqStopRecord message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopRecord.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopRecord message, length delimited. Does not implicitly {@link ReqStopRecord.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopRecord
   * @static
   * @param {IReqStopRecord} message ReqStopRecord message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopRecord.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopRecord message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopRecord
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopRecord} ReqStopRecord
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopRecord.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopRecord();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopRecord message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopRecord
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopRecord} ReqStopRecord
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopRecord.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopRecord message.
   * @function verify
   * @memberof ReqStopRecord
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopRecord.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopRecord message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopRecord
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopRecord} ReqStopRecord
   */
  ReqStopRecord.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopRecord) return object;
    return new $root.ReqStopRecord();
  };

  /**
   * Creates a plain object from a ReqStopRecord message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopRecord
   * @static
   * @param {ReqStopRecord} message ReqStopRecord
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopRecord.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopRecord to JSON.
   * @function toJSON
   * @memberof ReqStopRecord
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopRecord.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopRecord
   * @function getTypeUrl
   * @memberof ReqStopRecord
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopRecord";
  };

  return ReqStopRecord;
})();

$root.ReqSetExpMode = (function () {
  /**
   * Properties of a ReqSetExpMode.
   * @exports IReqSetExpMode
   * @interface IReqSetExpMode
   * @property {number|null} [mode] ReqSetExpMode mode
   */

  /**
   * Constructs a new ReqSetExpMode.
   * @exports ReqSetExpMode
   * @classdesc Represents a ReqSetExpMode.
   * @implements IReqSetExpMode
   * @constructor
   * @param {IReqSetExpMode=} [properties] Properties to set
   */
  function ReqSetExpMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetExpMode mode.
   * @member {number} mode
   * @memberof ReqSetExpMode
   * @instance
   */
  ReqSetExpMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetExpMode instance using the specified properties.
   * @function create
   * @memberof ReqSetExpMode
   * @static
   * @param {IReqSetExpMode=} [properties] Properties to set
   * @returns {ReqSetExpMode} ReqSetExpMode instance
   */
  ReqSetExpMode.create = function create(properties) {
    return new ReqSetExpMode(properties);
  };

  /**
   * Encodes the specified ReqSetExpMode message. Does not implicitly {@link ReqSetExpMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetExpMode
   * @static
   * @param {IReqSetExpMode} message ReqSetExpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetExpMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetExpMode message, length delimited. Does not implicitly {@link ReqSetExpMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetExpMode
   * @static
   * @param {IReqSetExpMode} message ReqSetExpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetExpMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetExpMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetExpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetExpMode} ReqSetExpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetExpMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetExpMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetExpMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetExpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetExpMode} ReqSetExpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetExpMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetExpMode message.
   * @function verify
   * @memberof ReqSetExpMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetExpMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetExpMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetExpMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetExpMode} ReqSetExpMode
   */
  ReqSetExpMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetExpMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetExpMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetExpMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetExpMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetExpMode
   * @static
   * @param {ReqSetExpMode} message ReqSetExpMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetExpMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetExpMode to JSON.
   * @function toJSON
   * @memberof ReqSetExpMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetExpMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetExpMode
   * @function getTypeUrl
   * @memberof ReqSetExpMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetExpMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetExpMode";
  };

  return ReqSetExpMode;
})();

$root.ReqGetExpMode = (function () {
  /**
   * Properties of a ReqGetExpMode.
   * @exports IReqGetExpMode
   * @interface IReqGetExpMode
   */

  /**
   * Constructs a new ReqGetExpMode.
   * @exports ReqGetExpMode
   * @classdesc Represents a ReqGetExpMode.
   * @implements IReqGetExpMode
   * @constructor
   * @param {IReqGetExpMode=} [properties] Properties to set
   */
  function ReqGetExpMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetExpMode instance using the specified properties.
   * @function create
   * @memberof ReqGetExpMode
   * @static
   * @param {IReqGetExpMode=} [properties] Properties to set
   * @returns {ReqGetExpMode} ReqGetExpMode instance
   */
  ReqGetExpMode.create = function create(properties) {
    return new ReqGetExpMode(properties);
  };

  /**
   * Encodes the specified ReqGetExpMode message. Does not implicitly {@link ReqGetExpMode.verify|verify} messages.
   * @function encode
   * @memberof ReqGetExpMode
   * @static
   * @param {IReqGetExpMode} message ReqGetExpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetExpMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetExpMode message, length delimited. Does not implicitly {@link ReqGetExpMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetExpMode
   * @static
   * @param {IReqGetExpMode} message ReqGetExpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetExpMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetExpMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetExpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetExpMode} ReqGetExpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetExpMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetExpMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetExpMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetExpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetExpMode} ReqGetExpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetExpMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetExpMode message.
   * @function verify
   * @memberof ReqGetExpMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetExpMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetExpMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetExpMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetExpMode} ReqGetExpMode
   */
  ReqGetExpMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetExpMode) return object;
    return new $root.ReqGetExpMode();
  };

  /**
   * Creates a plain object from a ReqGetExpMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetExpMode
   * @static
   * @param {ReqGetExpMode} message ReqGetExpMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetExpMode.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetExpMode to JSON.
   * @function toJSON
   * @memberof ReqGetExpMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetExpMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetExpMode
   * @function getTypeUrl
   * @memberof ReqGetExpMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetExpMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetExpMode";
  };

  return ReqGetExpMode;
})();

$root.ReqSetExp = (function () {
  /**
   * Properties of a ReqSetExp.
   * @exports IReqSetExp
   * @interface IReqSetExp
   * @property {number|null} [index] ReqSetExp index
   */

  /**
   * Constructs a new ReqSetExp.
   * @exports ReqSetExp
   * @classdesc Represents a ReqSetExp.
   * @implements IReqSetExp
   * @constructor
   * @param {IReqSetExp=} [properties] Properties to set
   */
  function ReqSetExp(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetExp index.
   * @member {number} index
   * @memberof ReqSetExp
   * @instance
   */
  ReqSetExp.prototype.index = 0;

  /**
   * Creates a new ReqSetExp instance using the specified properties.
   * @function create
   * @memberof ReqSetExp
   * @static
   * @param {IReqSetExp=} [properties] Properties to set
   * @returns {ReqSetExp} ReqSetExp instance
   */
  ReqSetExp.create = function create(properties) {
    return new ReqSetExp(properties);
  };

  /**
   * Encodes the specified ReqSetExp message. Does not implicitly {@link ReqSetExp.verify|verify} messages.
   * @function encode
   * @memberof ReqSetExp
   * @static
   * @param {IReqSetExp} message ReqSetExp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetExp.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    return writer;
  };

  /**
   * Encodes the specified ReqSetExp message, length delimited. Does not implicitly {@link ReqSetExp.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetExp
   * @static
   * @param {IReqSetExp} message ReqSetExp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetExp.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetExp message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetExp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetExp} ReqSetExp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetExp.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetExp();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetExp message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetExp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetExp} ReqSetExp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetExp.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetExp message.
   * @function verify
   * @memberof ReqSetExp
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetExp.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetExp message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetExp
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetExp} ReqSetExp
   */
  ReqSetExp.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetExp) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqSetExp: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetExp();
    if (object.index != null) message.index = object.index | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetExp message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetExp
   * @static
   * @param {ReqSetExp} message ReqSetExp
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetExp.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.index = 0;
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    return object;
  };

  /**
   * Converts this ReqSetExp to JSON.
   * @function toJSON
   * @memberof ReqSetExp
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetExp.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetExp
   * @function getTypeUrl
   * @memberof ReqSetExp
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetExp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetExp";
  };

  return ReqSetExp;
})();

$root.ReqGetExp = (function () {
  /**
   * Properties of a ReqGetExp.
   * @exports IReqGetExp
   * @interface IReqGetExp
   */

  /**
   * Constructs a new ReqGetExp.
   * @exports ReqGetExp
   * @classdesc Represents a ReqGetExp.
   * @implements IReqGetExp
   * @constructor
   * @param {IReqGetExp=} [properties] Properties to set
   */
  function ReqGetExp(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetExp instance using the specified properties.
   * @function create
   * @memberof ReqGetExp
   * @static
   * @param {IReqGetExp=} [properties] Properties to set
   * @returns {ReqGetExp} ReqGetExp instance
   */
  ReqGetExp.create = function create(properties) {
    return new ReqGetExp(properties);
  };

  /**
   * Encodes the specified ReqGetExp message. Does not implicitly {@link ReqGetExp.verify|verify} messages.
   * @function encode
   * @memberof ReqGetExp
   * @static
   * @param {IReqGetExp} message ReqGetExp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetExp.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetExp message, length delimited. Does not implicitly {@link ReqGetExp.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetExp
   * @static
   * @param {IReqGetExp} message ReqGetExp message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetExp.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetExp message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetExp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetExp} ReqGetExp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetExp.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetExp();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetExp message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetExp
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetExp} ReqGetExp
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetExp.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetExp message.
   * @function verify
   * @memberof ReqGetExp
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetExp.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetExp message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetExp
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetExp} ReqGetExp
   */
  ReqGetExp.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetExp) return object;
    return new $root.ReqGetExp();
  };

  /**
   * Creates a plain object from a ReqGetExp message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetExp
   * @static
   * @param {ReqGetExp} message ReqGetExp
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetExp.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetExp to JSON.
   * @function toJSON
   * @memberof ReqGetExp
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetExp.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetExp
   * @function getTypeUrl
   * @memberof ReqGetExp
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetExp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetExp";
  };

  return ReqGetExp;
})();

$root.ReqSetGainMode = (function () {
  /**
   * Properties of a ReqSetGainMode.
   * @exports IReqSetGainMode
   * @interface IReqSetGainMode
   * @property {number|null} [mode] ReqSetGainMode mode
   */

  /**
   * Constructs a new ReqSetGainMode.
   * @exports ReqSetGainMode
   * @classdesc Represents a ReqSetGainMode.
   * @implements IReqSetGainMode
   * @constructor
   * @param {IReqSetGainMode=} [properties] Properties to set
   */
  function ReqSetGainMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetGainMode mode.
   * @member {number} mode
   * @memberof ReqSetGainMode
   * @instance
   */
  ReqSetGainMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetGainMode instance using the specified properties.
   * @function create
   * @memberof ReqSetGainMode
   * @static
   * @param {IReqSetGainMode=} [properties] Properties to set
   * @returns {ReqSetGainMode} ReqSetGainMode instance
   */
  ReqSetGainMode.create = function create(properties) {
    return new ReqSetGainMode(properties);
  };

  /**
   * Encodes the specified ReqSetGainMode message. Does not implicitly {@link ReqSetGainMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetGainMode
   * @static
   * @param {IReqSetGainMode} message ReqSetGainMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGainMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetGainMode message, length delimited. Does not implicitly {@link ReqSetGainMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetGainMode
   * @static
   * @param {IReqSetGainMode} message ReqSetGainMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGainMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetGainMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetGainMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetGainMode} ReqSetGainMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGainMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetGainMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetGainMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetGainMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetGainMode} ReqSetGainMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGainMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetGainMode message.
   * @function verify
   * @memberof ReqSetGainMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetGainMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetGainMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetGainMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetGainMode} ReqSetGainMode
   */
  ReqSetGainMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetGainMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetGainMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetGainMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetGainMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetGainMode
   * @static
   * @param {ReqSetGainMode} message ReqSetGainMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetGainMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetGainMode to JSON.
   * @function toJSON
   * @memberof ReqSetGainMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetGainMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetGainMode
   * @function getTypeUrl
   * @memberof ReqSetGainMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetGainMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetGainMode";
  };

  return ReqSetGainMode;
})();

$root.ReqGetGainMode = (function () {
  /**
   * Properties of a ReqGetGainMode.
   * @exports IReqGetGainMode
   * @interface IReqGetGainMode
   */

  /**
   * Constructs a new ReqGetGainMode.
   * @exports ReqGetGainMode
   * @classdesc Represents a ReqGetGainMode.
   * @implements IReqGetGainMode
   * @constructor
   * @param {IReqGetGainMode=} [properties] Properties to set
   */
  function ReqGetGainMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetGainMode instance using the specified properties.
   * @function create
   * @memberof ReqGetGainMode
   * @static
   * @param {IReqGetGainMode=} [properties] Properties to set
   * @returns {ReqGetGainMode} ReqGetGainMode instance
   */
  ReqGetGainMode.create = function create(properties) {
    return new ReqGetGainMode(properties);
  };

  /**
   * Encodes the specified ReqGetGainMode message. Does not implicitly {@link ReqGetGainMode.verify|verify} messages.
   * @function encode
   * @memberof ReqGetGainMode
   * @static
   * @param {IReqGetGainMode} message ReqGetGainMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGainMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetGainMode message, length delimited. Does not implicitly {@link ReqGetGainMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetGainMode
   * @static
   * @param {IReqGetGainMode} message ReqGetGainMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGainMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetGainMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetGainMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetGainMode} ReqGetGainMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGainMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetGainMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetGainMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetGainMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetGainMode} ReqGetGainMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGainMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetGainMode message.
   * @function verify
   * @memberof ReqGetGainMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetGainMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetGainMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetGainMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetGainMode} ReqGetGainMode
   */
  ReqGetGainMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetGainMode) return object;
    return new $root.ReqGetGainMode();
  };

  /**
   * Creates a plain object from a ReqGetGainMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetGainMode
   * @static
   * @param {ReqGetGainMode} message ReqGetGainMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetGainMode.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetGainMode to JSON.
   * @function toJSON
   * @memberof ReqGetGainMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetGainMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetGainMode
   * @function getTypeUrl
   * @memberof ReqGetGainMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetGainMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetGainMode";
  };

  return ReqGetGainMode;
})();

$root.ReqSetGain = (function () {
  /**
   * Properties of a ReqSetGain.
   * @exports IReqSetGain
   * @interface IReqSetGain
   * @property {number|null} [index] ReqSetGain index
   */

  /**
   * Constructs a new ReqSetGain.
   * @exports ReqSetGain
   * @classdesc Represents a ReqSetGain.
   * @implements IReqSetGain
   * @constructor
   * @param {IReqSetGain=} [properties] Properties to set
   */
  function ReqSetGain(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetGain index.
   * @member {number} index
   * @memberof ReqSetGain
   * @instance
   */
  ReqSetGain.prototype.index = 0;

  /**
   * Creates a new ReqSetGain instance using the specified properties.
   * @function create
   * @memberof ReqSetGain
   * @static
   * @param {IReqSetGain=} [properties] Properties to set
   * @returns {ReqSetGain} ReqSetGain instance
   */
  ReqSetGain.create = function create(properties) {
    return new ReqSetGain(properties);
  };

  /**
   * Encodes the specified ReqSetGain message. Does not implicitly {@link ReqSetGain.verify|verify} messages.
   * @function encode
   * @memberof ReqSetGain
   * @static
   * @param {IReqSetGain} message ReqSetGain message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGain.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    return writer;
  };

  /**
   * Encodes the specified ReqSetGain message, length delimited. Does not implicitly {@link ReqSetGain.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetGain
   * @static
   * @param {IReqSetGain} message ReqSetGain message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGain.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetGain message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetGain
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetGain} ReqSetGain
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGain.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetGain();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetGain message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetGain
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetGain} ReqSetGain
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGain.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetGain message.
   * @function verify
   * @memberof ReqSetGain
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetGain.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetGain message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetGain
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetGain} ReqSetGain
   */
  ReqSetGain.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetGain) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetGain: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetGain();
    if (object.index != null) message.index = object.index | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetGain message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetGain
   * @static
   * @param {ReqSetGain} message ReqSetGain
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetGain.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.index = 0;
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    return object;
  };

  /**
   * Converts this ReqSetGain to JSON.
   * @function toJSON
   * @memberof ReqSetGain
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetGain.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetGain
   * @function getTypeUrl
   * @memberof ReqSetGain
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetGain.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetGain";
  };

  return ReqSetGain;
})();

$root.ReqGetGain = (function () {
  /**
   * Properties of a ReqGetGain.
   * @exports IReqGetGain
   * @interface IReqGetGain
   */

  /**
   * Constructs a new ReqGetGain.
   * @exports ReqGetGain
   * @classdesc Represents a ReqGetGain.
   * @implements IReqGetGain
   * @constructor
   * @param {IReqGetGain=} [properties] Properties to set
   */
  function ReqGetGain(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetGain instance using the specified properties.
   * @function create
   * @memberof ReqGetGain
   * @static
   * @param {IReqGetGain=} [properties] Properties to set
   * @returns {ReqGetGain} ReqGetGain instance
   */
  ReqGetGain.create = function create(properties) {
    return new ReqGetGain(properties);
  };

  /**
   * Encodes the specified ReqGetGain message. Does not implicitly {@link ReqGetGain.verify|verify} messages.
   * @function encode
   * @memberof ReqGetGain
   * @static
   * @param {IReqGetGain} message ReqGetGain message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGain.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetGain message, length delimited. Does not implicitly {@link ReqGetGain.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetGain
   * @static
   * @param {IReqGetGain} message ReqGetGain message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetGain.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetGain message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetGain
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetGain} ReqGetGain
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGain.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetGain();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetGain message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetGain
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetGain} ReqGetGain
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetGain.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetGain message.
   * @function verify
   * @memberof ReqGetGain
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetGain.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetGain message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetGain
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetGain} ReqGetGain
   */
  ReqGetGain.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetGain) return object;
    return new $root.ReqGetGain();
  };

  /**
   * Creates a plain object from a ReqGetGain message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetGain
   * @static
   * @param {ReqGetGain} message ReqGetGain
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetGain.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetGain to JSON.
   * @function toJSON
   * @memberof ReqGetGain
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetGain.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetGain
   * @function getTypeUrl
   * @memberof ReqGetGain
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetGain.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetGain";
  };

  return ReqGetGain;
})();

$root.ReqSetBrightness = (function () {
  /**
   * Properties of a ReqSetBrightness.
   * @exports IReqSetBrightness
   * @interface IReqSetBrightness
   * @property {number|null} [value] ReqSetBrightness value
   */

  /**
   * Constructs a new ReqSetBrightness.
   * @exports ReqSetBrightness
   * @classdesc Represents a ReqSetBrightness.
   * @implements IReqSetBrightness
   * @constructor
   * @param {IReqSetBrightness=} [properties] Properties to set
   */
  function ReqSetBrightness(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetBrightness value.
   * @member {number} value
   * @memberof ReqSetBrightness
   * @instance
   */
  ReqSetBrightness.prototype.value = 0;

  /**
   * Creates a new ReqSetBrightness instance using the specified properties.
   * @function create
   * @memberof ReqSetBrightness
   * @static
   * @param {IReqSetBrightness=} [properties] Properties to set
   * @returns {ReqSetBrightness} ReqSetBrightness instance
   */
  ReqSetBrightness.create = function create(properties) {
    return new ReqSetBrightness(properties);
  };

  /**
   * Encodes the specified ReqSetBrightness message. Does not implicitly {@link ReqSetBrightness.verify|verify} messages.
   * @function encode
   * @memberof ReqSetBrightness
   * @static
   * @param {IReqSetBrightness} message ReqSetBrightness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetBrightness.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetBrightness message, length delimited. Does not implicitly {@link ReqSetBrightness.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetBrightness
   * @static
   * @param {IReqSetBrightness} message ReqSetBrightness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetBrightness.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetBrightness message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetBrightness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetBrightness} ReqSetBrightness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetBrightness.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetBrightness();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetBrightness message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetBrightness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetBrightness} ReqSetBrightness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetBrightness.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetBrightness message.
   * @function verify
   * @memberof ReqSetBrightness
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetBrightness.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetBrightness message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetBrightness
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetBrightness} ReqSetBrightness
   */
  ReqSetBrightness.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetBrightness) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetBrightness: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetBrightness();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetBrightness message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetBrightness
   * @static
   * @param {ReqSetBrightness} message ReqSetBrightness
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetBrightness.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetBrightness to JSON.
   * @function toJSON
   * @memberof ReqSetBrightness
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetBrightness.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetBrightness
   * @function getTypeUrl
   * @memberof ReqSetBrightness
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetBrightness.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetBrightness";
  };

  return ReqSetBrightness;
})();

$root.ReqGetBrightness = (function () {
  /**
   * Properties of a ReqGetBrightness.
   * @exports IReqGetBrightness
   * @interface IReqGetBrightness
   */

  /**
   * Constructs a new ReqGetBrightness.
   * @exports ReqGetBrightness
   * @classdesc Represents a ReqGetBrightness.
   * @implements IReqGetBrightness
   * @constructor
   * @param {IReqGetBrightness=} [properties] Properties to set
   */
  function ReqGetBrightness(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetBrightness instance using the specified properties.
   * @function create
   * @memberof ReqGetBrightness
   * @static
   * @param {IReqGetBrightness=} [properties] Properties to set
   * @returns {ReqGetBrightness} ReqGetBrightness instance
   */
  ReqGetBrightness.create = function create(properties) {
    return new ReqGetBrightness(properties);
  };

  /**
   * Encodes the specified ReqGetBrightness message. Does not implicitly {@link ReqGetBrightness.verify|verify} messages.
   * @function encode
   * @memberof ReqGetBrightness
   * @static
   * @param {IReqGetBrightness} message ReqGetBrightness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetBrightness.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetBrightness message, length delimited. Does not implicitly {@link ReqGetBrightness.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetBrightness
   * @static
   * @param {IReqGetBrightness} message ReqGetBrightness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetBrightness.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetBrightness message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetBrightness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetBrightness} ReqGetBrightness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetBrightness.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetBrightness();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetBrightness message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetBrightness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetBrightness} ReqGetBrightness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetBrightness.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetBrightness message.
   * @function verify
   * @memberof ReqGetBrightness
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetBrightness.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetBrightness message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetBrightness
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetBrightness} ReqGetBrightness
   */
  ReqGetBrightness.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetBrightness) return object;
    return new $root.ReqGetBrightness();
  };

  /**
   * Creates a plain object from a ReqGetBrightness message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetBrightness
   * @static
   * @param {ReqGetBrightness} message ReqGetBrightness
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetBrightness.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetBrightness to JSON.
   * @function toJSON
   * @memberof ReqGetBrightness
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetBrightness.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetBrightness
   * @function getTypeUrl
   * @memberof ReqGetBrightness
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetBrightness.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetBrightness";
  };

  return ReqGetBrightness;
})();

$root.ReqSetContrast = (function () {
  /**
   * Properties of a ReqSetContrast.
   * @exports IReqSetContrast
   * @interface IReqSetContrast
   * @property {number|null} [value] ReqSetContrast value
   */

  /**
   * Constructs a new ReqSetContrast.
   * @exports ReqSetContrast
   * @classdesc Represents a ReqSetContrast.
   * @implements IReqSetContrast
   * @constructor
   * @param {IReqSetContrast=} [properties] Properties to set
   */
  function ReqSetContrast(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetContrast value.
   * @member {number} value
   * @memberof ReqSetContrast
   * @instance
   */
  ReqSetContrast.prototype.value = 0;

  /**
   * Creates a new ReqSetContrast instance using the specified properties.
   * @function create
   * @memberof ReqSetContrast
   * @static
   * @param {IReqSetContrast=} [properties] Properties to set
   * @returns {ReqSetContrast} ReqSetContrast instance
   */
  ReqSetContrast.create = function create(properties) {
    return new ReqSetContrast(properties);
  };

  /**
   * Encodes the specified ReqSetContrast message. Does not implicitly {@link ReqSetContrast.verify|verify} messages.
   * @function encode
   * @memberof ReqSetContrast
   * @static
   * @param {IReqSetContrast} message ReqSetContrast message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetContrast.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetContrast message, length delimited. Does not implicitly {@link ReqSetContrast.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetContrast
   * @static
   * @param {IReqSetContrast} message ReqSetContrast message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetContrast.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetContrast message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetContrast
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetContrast} ReqSetContrast
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetContrast.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetContrast();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetContrast message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetContrast
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetContrast} ReqSetContrast
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetContrast.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetContrast message.
   * @function verify
   * @memberof ReqSetContrast
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetContrast.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetContrast message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetContrast
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetContrast} ReqSetContrast
   */
  ReqSetContrast.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetContrast) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetContrast: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetContrast();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetContrast message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetContrast
   * @static
   * @param {ReqSetContrast} message ReqSetContrast
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetContrast.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetContrast to JSON.
   * @function toJSON
   * @memberof ReqSetContrast
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetContrast.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetContrast
   * @function getTypeUrl
   * @memberof ReqSetContrast
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetContrast.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetContrast";
  };

  return ReqSetContrast;
})();

$root.ReqGetContrast = (function () {
  /**
   * Properties of a ReqGetContrast.
   * @exports IReqGetContrast
   * @interface IReqGetContrast
   */

  /**
   * Constructs a new ReqGetContrast.
   * @exports ReqGetContrast
   * @classdesc Represents a ReqGetContrast.
   * @implements IReqGetContrast
   * @constructor
   * @param {IReqGetContrast=} [properties] Properties to set
   */
  function ReqGetContrast(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetContrast instance using the specified properties.
   * @function create
   * @memberof ReqGetContrast
   * @static
   * @param {IReqGetContrast=} [properties] Properties to set
   * @returns {ReqGetContrast} ReqGetContrast instance
   */
  ReqGetContrast.create = function create(properties) {
    return new ReqGetContrast(properties);
  };

  /**
   * Encodes the specified ReqGetContrast message. Does not implicitly {@link ReqGetContrast.verify|verify} messages.
   * @function encode
   * @memberof ReqGetContrast
   * @static
   * @param {IReqGetContrast} message ReqGetContrast message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetContrast.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetContrast message, length delimited. Does not implicitly {@link ReqGetContrast.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetContrast
   * @static
   * @param {IReqGetContrast} message ReqGetContrast message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetContrast.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetContrast message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetContrast
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetContrast} ReqGetContrast
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetContrast.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetContrast();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetContrast message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetContrast
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetContrast} ReqGetContrast
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetContrast.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetContrast message.
   * @function verify
   * @memberof ReqGetContrast
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetContrast.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetContrast message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetContrast
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetContrast} ReqGetContrast
   */
  ReqGetContrast.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetContrast) return object;
    return new $root.ReqGetContrast();
  };

  /**
   * Creates a plain object from a ReqGetContrast message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetContrast
   * @static
   * @param {ReqGetContrast} message ReqGetContrast
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetContrast.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetContrast to JSON.
   * @function toJSON
   * @memberof ReqGetContrast
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetContrast.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetContrast
   * @function getTypeUrl
   * @memberof ReqGetContrast
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetContrast.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetContrast";
  };

  return ReqGetContrast;
})();

$root.ReqSetHue = (function () {
  /**
   * Properties of a ReqSetHue.
   * @exports IReqSetHue
   * @interface IReqSetHue
   * @property {number|null} [value] ReqSetHue value
   */

  /**
   * Constructs a new ReqSetHue.
   * @exports ReqSetHue
   * @classdesc Represents a ReqSetHue.
   * @implements IReqSetHue
   * @constructor
   * @param {IReqSetHue=} [properties] Properties to set
   */
  function ReqSetHue(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetHue value.
   * @member {number} value
   * @memberof ReqSetHue
   * @instance
   */
  ReqSetHue.prototype.value = 0;

  /**
   * Creates a new ReqSetHue instance using the specified properties.
   * @function create
   * @memberof ReqSetHue
   * @static
   * @param {IReqSetHue=} [properties] Properties to set
   * @returns {ReqSetHue} ReqSetHue instance
   */
  ReqSetHue.create = function create(properties) {
    return new ReqSetHue(properties);
  };

  /**
   * Encodes the specified ReqSetHue message. Does not implicitly {@link ReqSetHue.verify|verify} messages.
   * @function encode
   * @memberof ReqSetHue
   * @static
   * @param {IReqSetHue} message ReqSetHue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetHue.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetHue message, length delimited. Does not implicitly {@link ReqSetHue.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetHue
   * @static
   * @param {IReqSetHue} message ReqSetHue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetHue.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetHue message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetHue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetHue} ReqSetHue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetHue.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetHue();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetHue message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetHue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetHue} ReqSetHue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetHue.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetHue message.
   * @function verify
   * @memberof ReqSetHue
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetHue.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetHue message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetHue
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetHue} ReqSetHue
   */
  ReqSetHue.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetHue) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqSetHue: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetHue();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetHue message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetHue
   * @static
   * @param {ReqSetHue} message ReqSetHue
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetHue.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetHue to JSON.
   * @function toJSON
   * @memberof ReqSetHue
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetHue.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetHue
   * @function getTypeUrl
   * @memberof ReqSetHue
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetHue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetHue";
  };

  return ReqSetHue;
})();

$root.ReqGetHue = (function () {
  /**
   * Properties of a ReqGetHue.
   * @exports IReqGetHue
   * @interface IReqGetHue
   */

  /**
   * Constructs a new ReqGetHue.
   * @exports ReqGetHue
   * @classdesc Represents a ReqGetHue.
   * @implements IReqGetHue
   * @constructor
   * @param {IReqGetHue=} [properties] Properties to set
   */
  function ReqGetHue(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetHue instance using the specified properties.
   * @function create
   * @memberof ReqGetHue
   * @static
   * @param {IReqGetHue=} [properties] Properties to set
   * @returns {ReqGetHue} ReqGetHue instance
   */
  ReqGetHue.create = function create(properties) {
    return new ReqGetHue(properties);
  };

  /**
   * Encodes the specified ReqGetHue message. Does not implicitly {@link ReqGetHue.verify|verify} messages.
   * @function encode
   * @memberof ReqGetHue
   * @static
   * @param {IReqGetHue} message ReqGetHue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetHue.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetHue message, length delimited. Does not implicitly {@link ReqGetHue.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetHue
   * @static
   * @param {IReqGetHue} message ReqGetHue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetHue.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetHue message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetHue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetHue} ReqGetHue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetHue.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetHue();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetHue message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetHue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetHue} ReqGetHue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetHue.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetHue message.
   * @function verify
   * @memberof ReqGetHue
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetHue.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetHue message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetHue
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetHue} ReqGetHue
   */
  ReqGetHue.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetHue) return object;
    return new $root.ReqGetHue();
  };

  /**
   * Creates a plain object from a ReqGetHue message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetHue
   * @static
   * @param {ReqGetHue} message ReqGetHue
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetHue.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetHue to JSON.
   * @function toJSON
   * @memberof ReqGetHue
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetHue.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetHue
   * @function getTypeUrl
   * @memberof ReqGetHue
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetHue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetHue";
  };

  return ReqGetHue;
})();

$root.ReqSetSaturation = (function () {
  /**
   * Properties of a ReqSetSaturation.
   * @exports IReqSetSaturation
   * @interface IReqSetSaturation
   * @property {number|null} [value] ReqSetSaturation value
   */

  /**
   * Constructs a new ReqSetSaturation.
   * @exports ReqSetSaturation
   * @classdesc Represents a ReqSetSaturation.
   * @implements IReqSetSaturation
   * @constructor
   * @param {IReqSetSaturation=} [properties] Properties to set
   */
  function ReqSetSaturation(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetSaturation value.
   * @member {number} value
   * @memberof ReqSetSaturation
   * @instance
   */
  ReqSetSaturation.prototype.value = 0;

  /**
   * Creates a new ReqSetSaturation instance using the specified properties.
   * @function create
   * @memberof ReqSetSaturation
   * @static
   * @param {IReqSetSaturation=} [properties] Properties to set
   * @returns {ReqSetSaturation} ReqSetSaturation instance
   */
  ReqSetSaturation.create = function create(properties) {
    return new ReqSetSaturation(properties);
  };

  /**
   * Encodes the specified ReqSetSaturation message. Does not implicitly {@link ReqSetSaturation.verify|verify} messages.
   * @function encode
   * @memberof ReqSetSaturation
   * @static
   * @param {IReqSetSaturation} message ReqSetSaturation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSaturation.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetSaturation message, length delimited. Does not implicitly {@link ReqSetSaturation.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetSaturation
   * @static
   * @param {IReqSetSaturation} message ReqSetSaturation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSaturation.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetSaturation message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetSaturation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetSaturation} ReqSetSaturation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSaturation.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetSaturation();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetSaturation message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetSaturation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetSaturation} ReqSetSaturation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSaturation.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetSaturation message.
   * @function verify
   * @memberof ReqSetSaturation
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetSaturation.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetSaturation message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetSaturation
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetSaturation} ReqSetSaturation
   */
  ReqSetSaturation.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetSaturation) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetSaturation: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetSaturation();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetSaturation message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetSaturation
   * @static
   * @param {ReqSetSaturation} message ReqSetSaturation
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetSaturation.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetSaturation to JSON.
   * @function toJSON
   * @memberof ReqSetSaturation
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetSaturation.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetSaturation
   * @function getTypeUrl
   * @memberof ReqSetSaturation
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetSaturation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetSaturation";
  };

  return ReqSetSaturation;
})();

$root.ReqGetSaturation = (function () {
  /**
   * Properties of a ReqGetSaturation.
   * @exports IReqGetSaturation
   * @interface IReqGetSaturation
   */

  /**
   * Constructs a new ReqGetSaturation.
   * @exports ReqGetSaturation
   * @classdesc Represents a ReqGetSaturation.
   * @implements IReqGetSaturation
   * @constructor
   * @param {IReqGetSaturation=} [properties] Properties to set
   */
  function ReqGetSaturation(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetSaturation instance using the specified properties.
   * @function create
   * @memberof ReqGetSaturation
   * @static
   * @param {IReqGetSaturation=} [properties] Properties to set
   * @returns {ReqGetSaturation} ReqGetSaturation instance
   */
  ReqGetSaturation.create = function create(properties) {
    return new ReqGetSaturation(properties);
  };

  /**
   * Encodes the specified ReqGetSaturation message. Does not implicitly {@link ReqGetSaturation.verify|verify} messages.
   * @function encode
   * @memberof ReqGetSaturation
   * @static
   * @param {IReqGetSaturation} message ReqGetSaturation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSaturation.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetSaturation message, length delimited. Does not implicitly {@link ReqGetSaturation.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetSaturation
   * @static
   * @param {IReqGetSaturation} message ReqGetSaturation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSaturation.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetSaturation message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetSaturation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetSaturation} ReqGetSaturation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSaturation.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetSaturation();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetSaturation message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetSaturation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetSaturation} ReqGetSaturation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSaturation.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetSaturation message.
   * @function verify
   * @memberof ReqGetSaturation
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetSaturation.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetSaturation message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetSaturation
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetSaturation} ReqGetSaturation
   */
  ReqGetSaturation.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetSaturation) return object;
    return new $root.ReqGetSaturation();
  };

  /**
   * Creates a plain object from a ReqGetSaturation message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetSaturation
   * @static
   * @param {ReqGetSaturation} message ReqGetSaturation
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetSaturation.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetSaturation to JSON.
   * @function toJSON
   * @memberof ReqGetSaturation
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetSaturation.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetSaturation
   * @function getTypeUrl
   * @memberof ReqGetSaturation
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetSaturation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetSaturation";
  };

  return ReqGetSaturation;
})();

$root.ReqSetSharpness = (function () {
  /**
   * Properties of a ReqSetSharpness.
   * @exports IReqSetSharpness
   * @interface IReqSetSharpness
   * @property {number|null} [value] ReqSetSharpness value
   */

  /**
   * Constructs a new ReqSetSharpness.
   * @exports ReqSetSharpness
   * @classdesc Represents a ReqSetSharpness.
   * @implements IReqSetSharpness
   * @constructor
   * @param {IReqSetSharpness=} [properties] Properties to set
   */
  function ReqSetSharpness(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetSharpness value.
   * @member {number} value
   * @memberof ReqSetSharpness
   * @instance
   */
  ReqSetSharpness.prototype.value = 0;

  /**
   * Creates a new ReqSetSharpness instance using the specified properties.
   * @function create
   * @memberof ReqSetSharpness
   * @static
   * @param {IReqSetSharpness=} [properties] Properties to set
   * @returns {ReqSetSharpness} ReqSetSharpness instance
   */
  ReqSetSharpness.create = function create(properties) {
    return new ReqSetSharpness(properties);
  };

  /**
   * Encodes the specified ReqSetSharpness message. Does not implicitly {@link ReqSetSharpness.verify|verify} messages.
   * @function encode
   * @memberof ReqSetSharpness
   * @static
   * @param {IReqSetSharpness} message ReqSetSharpness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSharpness.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetSharpness message, length delimited. Does not implicitly {@link ReqSetSharpness.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetSharpness
   * @static
   * @param {IReqSetSharpness} message ReqSetSharpness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSharpness.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetSharpness message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetSharpness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetSharpness} ReqSetSharpness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSharpness.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetSharpness();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetSharpness message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetSharpness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetSharpness} ReqSetSharpness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSharpness.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetSharpness message.
   * @function verify
   * @memberof ReqSetSharpness
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetSharpness.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetSharpness message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetSharpness
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetSharpness} ReqSetSharpness
   */
  ReqSetSharpness.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetSharpness) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetSharpness: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetSharpness();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetSharpness message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetSharpness
   * @static
   * @param {ReqSetSharpness} message ReqSetSharpness
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetSharpness.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetSharpness to JSON.
   * @function toJSON
   * @memberof ReqSetSharpness
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetSharpness.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetSharpness
   * @function getTypeUrl
   * @memberof ReqSetSharpness
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetSharpness.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetSharpness";
  };

  return ReqSetSharpness;
})();

$root.ReqGetSharpness = (function () {
  /**
   * Properties of a ReqGetSharpness.
   * @exports IReqGetSharpness
   * @interface IReqGetSharpness
   */

  /**
   * Constructs a new ReqGetSharpness.
   * @exports ReqGetSharpness
   * @classdesc Represents a ReqGetSharpness.
   * @implements IReqGetSharpness
   * @constructor
   * @param {IReqGetSharpness=} [properties] Properties to set
   */
  function ReqGetSharpness(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetSharpness instance using the specified properties.
   * @function create
   * @memberof ReqGetSharpness
   * @static
   * @param {IReqGetSharpness=} [properties] Properties to set
   * @returns {ReqGetSharpness} ReqGetSharpness instance
   */
  ReqGetSharpness.create = function create(properties) {
    return new ReqGetSharpness(properties);
  };

  /**
   * Encodes the specified ReqGetSharpness message. Does not implicitly {@link ReqGetSharpness.verify|verify} messages.
   * @function encode
   * @memberof ReqGetSharpness
   * @static
   * @param {IReqGetSharpness} message ReqGetSharpness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSharpness.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetSharpness message, length delimited. Does not implicitly {@link ReqGetSharpness.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetSharpness
   * @static
   * @param {IReqGetSharpness} message ReqGetSharpness message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSharpness.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetSharpness message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetSharpness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetSharpness} ReqGetSharpness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSharpness.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetSharpness();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetSharpness message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetSharpness
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetSharpness} ReqGetSharpness
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSharpness.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetSharpness message.
   * @function verify
   * @memberof ReqGetSharpness
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetSharpness.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetSharpness message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetSharpness
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetSharpness} ReqGetSharpness
   */
  ReqGetSharpness.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetSharpness) return object;
    return new $root.ReqGetSharpness();
  };

  /**
   * Creates a plain object from a ReqGetSharpness message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetSharpness
   * @static
   * @param {ReqGetSharpness} message ReqGetSharpness
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetSharpness.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetSharpness to JSON.
   * @function toJSON
   * @memberof ReqGetSharpness
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetSharpness.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetSharpness
   * @function getTypeUrl
   * @memberof ReqGetSharpness
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetSharpness.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetSharpness";
  };

  return ReqGetSharpness;
})();

$root.ReqSetWBMode = (function () {
  /**
   * Properties of a ReqSetWBMode.
   * @exports IReqSetWBMode
   * @interface IReqSetWBMode
   * @property {number|null} [mode] ReqSetWBMode mode
   */

  /**
   * Constructs a new ReqSetWBMode.
   * @exports ReqSetWBMode
   * @classdesc Represents a ReqSetWBMode.
   * @implements IReqSetWBMode
   * @constructor
   * @param {IReqSetWBMode=} [properties] Properties to set
   */
  function ReqSetWBMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetWBMode mode.
   * @member {number} mode
   * @memberof ReqSetWBMode
   * @instance
   */
  ReqSetWBMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetWBMode instance using the specified properties.
   * @function create
   * @memberof ReqSetWBMode
   * @static
   * @param {IReqSetWBMode=} [properties] Properties to set
   * @returns {ReqSetWBMode} ReqSetWBMode instance
   */
  ReqSetWBMode.create = function create(properties) {
    return new ReqSetWBMode(properties);
  };

  /**
   * Encodes the specified ReqSetWBMode message. Does not implicitly {@link ReqSetWBMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetWBMode
   * @static
   * @param {IReqSetWBMode} message ReqSetWBMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetWBMode message, length delimited. Does not implicitly {@link ReqSetWBMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetWBMode
   * @static
   * @param {IReqSetWBMode} message ReqSetWBMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetWBMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetWBMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetWBMode} ReqSetWBMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetWBMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetWBMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetWBMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetWBMode} ReqSetWBMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetWBMode message.
   * @function verify
   * @memberof ReqSetWBMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetWBMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetWBMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetWBMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetWBMode} ReqSetWBMode
   */
  ReqSetWBMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetWBMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetWBMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetWBMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetWBMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetWBMode
   * @static
   * @param {ReqSetWBMode} message ReqSetWBMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetWBMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetWBMode to JSON.
   * @function toJSON
   * @memberof ReqSetWBMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetWBMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetWBMode
   * @function getTypeUrl
   * @memberof ReqSetWBMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetWBMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetWBMode";
  };

  return ReqSetWBMode;
})();

$root.ReqGetWBMode = (function () {
  /**
   * Properties of a ReqGetWBMode.
   * @exports IReqGetWBMode
   * @interface IReqGetWBMode
   */

  /**
   * Constructs a new ReqGetWBMode.
   * @exports ReqGetWBMode
   * @classdesc Represents a ReqGetWBMode.
   * @implements IReqGetWBMode
   * @constructor
   * @param {IReqGetWBMode=} [properties] Properties to set
   */
  function ReqGetWBMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetWBMode instance using the specified properties.
   * @function create
   * @memberof ReqGetWBMode
   * @static
   * @param {IReqGetWBMode=} [properties] Properties to set
   * @returns {ReqGetWBMode} ReqGetWBMode instance
   */
  ReqGetWBMode.create = function create(properties) {
    return new ReqGetWBMode(properties);
  };

  /**
   * Encodes the specified ReqGetWBMode message. Does not implicitly {@link ReqGetWBMode.verify|verify} messages.
   * @function encode
   * @memberof ReqGetWBMode
   * @static
   * @param {IReqGetWBMode} message ReqGetWBMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetWBMode message, length delimited. Does not implicitly {@link ReqGetWBMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetWBMode
   * @static
   * @param {IReqGetWBMode} message ReqGetWBMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetWBMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetWBMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetWBMode} ReqGetWBMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetWBMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetWBMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetWBMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetWBMode} ReqGetWBMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetWBMode message.
   * @function verify
   * @memberof ReqGetWBMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetWBMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetWBMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetWBMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetWBMode} ReqGetWBMode
   */
  ReqGetWBMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetWBMode) return object;
    return new $root.ReqGetWBMode();
  };

  /**
   * Creates a plain object from a ReqGetWBMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetWBMode
   * @static
   * @param {ReqGetWBMode} message ReqGetWBMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetWBMode.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetWBMode to JSON.
   * @function toJSON
   * @memberof ReqGetWBMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetWBMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetWBMode
   * @function getTypeUrl
   * @memberof ReqGetWBMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetWBMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetWBMode";
  };

  return ReqGetWBMode;
})();

$root.ReqSetWBSence = (function () {
  /**
   * Properties of a ReqSetWBSence.
   * @exports IReqSetWBSence
   * @interface IReqSetWBSence
   * @property {number|null} [value] ReqSetWBSence value
   */

  /**
   * Constructs a new ReqSetWBSence.
   * @exports ReqSetWBSence
   * @classdesc Represents a ReqSetWBSence.
   * @implements IReqSetWBSence
   * @constructor
   * @param {IReqSetWBSence=} [properties] Properties to set
   */
  function ReqSetWBSence(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetWBSence value.
   * @member {number} value
   * @memberof ReqSetWBSence
   * @instance
   */
  ReqSetWBSence.prototype.value = 0;

  /**
   * Creates a new ReqSetWBSence instance using the specified properties.
   * @function create
   * @memberof ReqSetWBSence
   * @static
   * @param {IReqSetWBSence=} [properties] Properties to set
   * @returns {ReqSetWBSence} ReqSetWBSence instance
   */
  ReqSetWBSence.create = function create(properties) {
    return new ReqSetWBSence(properties);
  };

  /**
   * Encodes the specified ReqSetWBSence message. Does not implicitly {@link ReqSetWBSence.verify|verify} messages.
   * @function encode
   * @memberof ReqSetWBSence
   * @static
   * @param {IReqSetWBSence} message ReqSetWBSence message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBSence.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetWBSence message, length delimited. Does not implicitly {@link ReqSetWBSence.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetWBSence
   * @static
   * @param {IReqSetWBSence} message ReqSetWBSence message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBSence.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetWBSence message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetWBSence
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetWBSence} ReqSetWBSence
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBSence.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetWBSence();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetWBSence message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetWBSence
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetWBSence} ReqSetWBSence
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBSence.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetWBSence message.
   * @function verify
   * @memberof ReqSetWBSence
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetWBSence.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetWBSence message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetWBSence
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetWBSence} ReqSetWBSence
   */
  ReqSetWBSence.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetWBSence) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetWBSence: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetWBSence();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetWBSence message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetWBSence
   * @static
   * @param {ReqSetWBSence} message ReqSetWBSence
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetWBSence.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetWBSence to JSON.
   * @function toJSON
   * @memberof ReqSetWBSence
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetWBSence.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetWBSence
   * @function getTypeUrl
   * @memberof ReqSetWBSence
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetWBSence.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetWBSence";
  };

  return ReqSetWBSence;
})();

$root.ReqGetWBSence = (function () {
  /**
   * Properties of a ReqGetWBSence.
   * @exports IReqGetWBSence
   * @interface IReqGetWBSence
   */

  /**
   * Constructs a new ReqGetWBSence.
   * @exports ReqGetWBSence
   * @classdesc Represents a ReqGetWBSence.
   * @implements IReqGetWBSence
   * @constructor
   * @param {IReqGetWBSence=} [properties] Properties to set
   */
  function ReqGetWBSence(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetWBSence instance using the specified properties.
   * @function create
   * @memberof ReqGetWBSence
   * @static
   * @param {IReqGetWBSence=} [properties] Properties to set
   * @returns {ReqGetWBSence} ReqGetWBSence instance
   */
  ReqGetWBSence.create = function create(properties) {
    return new ReqGetWBSence(properties);
  };

  /**
   * Encodes the specified ReqGetWBSence message. Does not implicitly {@link ReqGetWBSence.verify|verify} messages.
   * @function encode
   * @memberof ReqGetWBSence
   * @static
   * @param {IReqGetWBSence} message ReqGetWBSence message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBSence.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetWBSence message, length delimited. Does not implicitly {@link ReqGetWBSence.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetWBSence
   * @static
   * @param {IReqGetWBSence} message ReqGetWBSence message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBSence.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetWBSence message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetWBSence
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetWBSence} ReqGetWBSence
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBSence.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetWBSence();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetWBSence message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetWBSence
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetWBSence} ReqGetWBSence
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBSence.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetWBSence message.
   * @function verify
   * @memberof ReqGetWBSence
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetWBSence.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetWBSence message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetWBSence
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetWBSence} ReqGetWBSence
   */
  ReqGetWBSence.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetWBSence) return object;
    return new $root.ReqGetWBSence();
  };

  /**
   * Creates a plain object from a ReqGetWBSence message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetWBSence
   * @static
   * @param {ReqGetWBSence} message ReqGetWBSence
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetWBSence.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetWBSence to JSON.
   * @function toJSON
   * @memberof ReqGetWBSence
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetWBSence.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetWBSence
   * @function getTypeUrl
   * @memberof ReqGetWBSence
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetWBSence.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetWBSence";
  };

  return ReqGetWBSence;
})();

$root.ReqSetWBCT = (function () {
  /**
   * Properties of a ReqSetWBCT.
   * @exports IReqSetWBCT
   * @interface IReqSetWBCT
   * @property {number|null} [index] ReqSetWBCT index
   */

  /**
   * Constructs a new ReqSetWBCT.
   * @exports ReqSetWBCT
   * @classdesc Represents a ReqSetWBCT.
   * @implements IReqSetWBCT
   * @constructor
   * @param {IReqSetWBCT=} [properties] Properties to set
   */
  function ReqSetWBCT(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetWBCT index.
   * @member {number} index
   * @memberof ReqSetWBCT
   * @instance
   */
  ReqSetWBCT.prototype.index = 0;

  /**
   * Creates a new ReqSetWBCT instance using the specified properties.
   * @function create
   * @memberof ReqSetWBCT
   * @static
   * @param {IReqSetWBCT=} [properties] Properties to set
   * @returns {ReqSetWBCT} ReqSetWBCT instance
   */
  ReqSetWBCT.create = function create(properties) {
    return new ReqSetWBCT(properties);
  };

  /**
   * Encodes the specified ReqSetWBCT message. Does not implicitly {@link ReqSetWBCT.verify|verify} messages.
   * @function encode
   * @memberof ReqSetWBCT
   * @static
   * @param {IReqSetWBCT} message ReqSetWBCT message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBCT.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.index);
    return writer;
  };

  /**
   * Encodes the specified ReqSetWBCT message, length delimited. Does not implicitly {@link ReqSetWBCT.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetWBCT
   * @static
   * @param {IReqSetWBCT} message ReqSetWBCT message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWBCT.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetWBCT message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetWBCT
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetWBCT} ReqSetWBCT
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBCT.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetWBCT();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.index = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetWBCT message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetWBCT
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetWBCT} ReqSetWBCT
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWBCT.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetWBCT message.
   * @function verify
   * @memberof ReqSetWBCT
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetWBCT.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      if (!$util.isInteger(message.index)) return "index: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetWBCT message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetWBCT
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetWBCT} ReqSetWBCT
   */
  ReqSetWBCT.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetWBCT) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetWBCT: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetWBCT();
    if (object.index != null) message.index = object.index | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetWBCT message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetWBCT
   * @static
   * @param {ReqSetWBCT} message ReqSetWBCT
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetWBCT.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.index = 0;
    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
      object.index = message.index;
    return object;
  };

  /**
   * Converts this ReqSetWBCT to JSON.
   * @function toJSON
   * @memberof ReqSetWBCT
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetWBCT.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetWBCT
   * @function getTypeUrl
   * @memberof ReqSetWBCT
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetWBCT.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetWBCT";
  };

  return ReqSetWBCT;
})();

$root.ReqGetWBCT = (function () {
  /**
   * Properties of a ReqGetWBCT.
   * @exports IReqGetWBCT
   * @interface IReqGetWBCT
   */

  /**
   * Constructs a new ReqGetWBCT.
   * @exports ReqGetWBCT
   * @classdesc Represents a ReqGetWBCT.
   * @implements IReqGetWBCT
   * @constructor
   * @param {IReqGetWBCT=} [properties] Properties to set
   */
  function ReqGetWBCT(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetWBCT instance using the specified properties.
   * @function create
   * @memberof ReqGetWBCT
   * @static
   * @param {IReqGetWBCT=} [properties] Properties to set
   * @returns {ReqGetWBCT} ReqGetWBCT instance
   */
  ReqGetWBCT.create = function create(properties) {
    return new ReqGetWBCT(properties);
  };

  /**
   * Encodes the specified ReqGetWBCT message. Does not implicitly {@link ReqGetWBCT.verify|verify} messages.
   * @function encode
   * @memberof ReqGetWBCT
   * @static
   * @param {IReqGetWBCT} message ReqGetWBCT message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBCT.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetWBCT message, length delimited. Does not implicitly {@link ReqGetWBCT.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetWBCT
   * @static
   * @param {IReqGetWBCT} message ReqGetWBCT message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetWBCT.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetWBCT message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetWBCT
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetWBCT} ReqGetWBCT
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBCT.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetWBCT();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetWBCT message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetWBCT
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetWBCT} ReqGetWBCT
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetWBCT.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetWBCT message.
   * @function verify
   * @memberof ReqGetWBCT
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetWBCT.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetWBCT message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetWBCT
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetWBCT} ReqGetWBCT
   */
  ReqGetWBCT.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetWBCT) return object;
    return new $root.ReqGetWBCT();
  };

  /**
   * Creates a plain object from a ReqGetWBCT message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetWBCT
   * @static
   * @param {ReqGetWBCT} message ReqGetWBCT
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetWBCT.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetWBCT to JSON.
   * @function toJSON
   * @memberof ReqGetWBCT
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetWBCT.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetWBCT
   * @function getTypeUrl
   * @memberof ReqGetWBCT
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetWBCT.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetWBCT";
  };

  return ReqGetWBCT;
})();

$root.ReqSetIrCut = (function () {
  /**
   * Properties of a ReqSetIrCut.
   * @exports IReqSetIrCut
   * @interface IReqSetIrCut
   * @property {number|null} [value] ReqSetIrCut value
   */

  /**
   * Constructs a new ReqSetIrCut.
   * @exports ReqSetIrCut
   * @classdesc Represents a ReqSetIrCut.
   * @implements IReqSetIrCut
   * @constructor
   * @param {IReqSetIrCut=} [properties] Properties to set
   */
  function ReqSetIrCut(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetIrCut value.
   * @member {number} value
   * @memberof ReqSetIrCut
   * @instance
   */
  ReqSetIrCut.prototype.value = 0;

  /**
   * Creates a new ReqSetIrCut instance using the specified properties.
   * @function create
   * @memberof ReqSetIrCut
   * @static
   * @param {IReqSetIrCut=} [properties] Properties to set
   * @returns {ReqSetIrCut} ReqSetIrCut instance
   */
  ReqSetIrCut.create = function create(properties) {
    return new ReqSetIrCut(properties);
  };

  /**
   * Encodes the specified ReqSetIrCut message. Does not implicitly {@link ReqSetIrCut.verify|verify} messages.
   * @function encode
   * @memberof ReqSetIrCut
   * @static
   * @param {IReqSetIrCut} message ReqSetIrCut message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetIrCut.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetIrCut message, length delimited. Does not implicitly {@link ReqSetIrCut.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetIrCut
   * @static
   * @param {IReqSetIrCut} message ReqSetIrCut message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetIrCut.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetIrCut message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetIrCut
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetIrCut} ReqSetIrCut
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetIrCut.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetIrCut();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetIrCut message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetIrCut
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetIrCut} ReqSetIrCut
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetIrCut.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetIrCut message.
   * @function verify
   * @memberof ReqSetIrCut
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetIrCut.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetIrCut message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetIrCut
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetIrCut} ReqSetIrCut
   */
  ReqSetIrCut.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetIrCut) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetIrCut: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetIrCut();
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetIrCut message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetIrCut
   * @static
   * @param {ReqSetIrCut} message ReqSetIrCut
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetIrCut.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetIrCut to JSON.
   * @function toJSON
   * @memberof ReqSetIrCut
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetIrCut.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetIrCut
   * @function getTypeUrl
   * @memberof ReqSetIrCut
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetIrCut.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetIrCut";
  };

  return ReqSetIrCut;
})();

$root.ReqSetNdFilter = (function () {
  /**
   * Properties of a ReqSetNdFilter.
   * @exports IReqSetNdFilter
   * @interface IReqSetNdFilter
   * @property {boolean|null} [value] ReqSetNdFilter value
   */

  /**
   * Constructs a new ReqSetNdFilter.
   * @exports ReqSetNdFilter
   * @classdesc Represents a ReqSetNdFilter.
   * @implements IReqSetNdFilter
   * @constructor
   * @param {IReqSetNdFilter=} [properties] Properties to set
   */
  function ReqSetNdFilter(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetNdFilter value.
   * @member {boolean} value
   * @memberof ReqSetNdFilter
   * @instance
   */
  ReqSetNdFilter.prototype.value = false;

  /**
   * Creates a new ReqSetNdFilter instance using the specified properties.
   * @function create
   * @memberof ReqSetNdFilter
   * @static
   * @param {IReqSetNdFilter=} [properties] Properties to set
   * @returns {ReqSetNdFilter} ReqSetNdFilter instance
   */
  ReqSetNdFilter.create = function create(properties) {
    return new ReqSetNdFilter(properties);
  };

  /**
   * Encodes the specified ReqSetNdFilter message. Does not implicitly {@link ReqSetNdFilter.verify|verify} messages.
   * @function encode
   * @memberof ReqSetNdFilter
   * @static
   * @param {IReqSetNdFilter} message ReqSetNdFilter message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetNdFilter.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetNdFilter message, length delimited. Does not implicitly {@link ReqSetNdFilter.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetNdFilter
   * @static
   * @param {IReqSetNdFilter} message ReqSetNdFilter message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetNdFilter.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetNdFilter message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetNdFilter
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetNdFilter} ReqSetNdFilter
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetNdFilter.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetNdFilter();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetNdFilter message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetNdFilter
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetNdFilter} ReqSetNdFilter
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetNdFilter.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetNdFilter message.
   * @function verify
   * @memberof ReqSetNdFilter
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetNdFilter.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (typeof message.value !== "boolean") return "value: boolean expected";
    return null;
  };

  /**
   * Creates a ReqSetNdFilter message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetNdFilter
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetNdFilter} ReqSetNdFilter
   */
  ReqSetNdFilter.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetNdFilter) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetNdFilter: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetNdFilter();
    if (object.value != null) message.value = Boolean(object.value);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetNdFilter message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetNdFilter
   * @static
   * @param {ReqSetNdFilter} message ReqSetNdFilter
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetNdFilter.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = false;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetNdFilter to JSON.
   * @function toJSON
   * @memberof ReqSetNdFilter
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetNdFilter.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetNdFilter
   * @function getTypeUrl
   * @memberof ReqSetNdFilter
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetNdFilter.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetNdFilter";
  };

  return ReqSetNdFilter;
})();

$root.ReqGetIrcut = (function () {
  /**
   * Properties of a ReqGetIrcut.
   * @exports IReqGetIrcut
   * @interface IReqGetIrcut
   */

  /**
   * Constructs a new ReqGetIrcut.
   * @exports ReqGetIrcut
   * @classdesc Represents a ReqGetIrcut.
   * @implements IReqGetIrcut
   * @constructor
   * @param {IReqGetIrcut=} [properties] Properties to set
   */
  function ReqGetIrcut(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetIrcut instance using the specified properties.
   * @function create
   * @memberof ReqGetIrcut
   * @static
   * @param {IReqGetIrcut=} [properties] Properties to set
   * @returns {ReqGetIrcut} ReqGetIrcut instance
   */
  ReqGetIrcut.create = function create(properties) {
    return new ReqGetIrcut(properties);
  };

  /**
   * Encodes the specified ReqGetIrcut message. Does not implicitly {@link ReqGetIrcut.verify|verify} messages.
   * @function encode
   * @memberof ReqGetIrcut
   * @static
   * @param {IReqGetIrcut} message ReqGetIrcut message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetIrcut.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetIrcut message, length delimited. Does not implicitly {@link ReqGetIrcut.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetIrcut
   * @static
   * @param {IReqGetIrcut} message ReqGetIrcut message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetIrcut.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetIrcut message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetIrcut
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetIrcut} ReqGetIrcut
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetIrcut.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetIrcut();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetIrcut message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetIrcut
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetIrcut} ReqGetIrcut
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetIrcut.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetIrcut message.
   * @function verify
   * @memberof ReqGetIrcut
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetIrcut.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetIrcut message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetIrcut
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetIrcut} ReqGetIrcut
   */
  ReqGetIrcut.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetIrcut) return object;
    return new $root.ReqGetIrcut();
  };

  /**
   * Creates a plain object from a ReqGetIrcut message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetIrcut
   * @static
   * @param {ReqGetIrcut} message ReqGetIrcut
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetIrcut.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetIrcut to JSON.
   * @function toJSON
   * @memberof ReqGetIrcut
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetIrcut.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetIrcut
   * @function getTypeUrl
   * @memberof ReqGetIrcut
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetIrcut.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetIrcut";
  };

  return ReqGetIrcut;
})();

$root.ReqStartTimeLapse = (function () {
  /**
   * Properties of a ReqStartTimeLapse.
   * @exports IReqStartTimeLapse
   * @interface IReqStartTimeLapse
   */

  /**
   * Constructs a new ReqStartTimeLapse.
   * @exports ReqStartTimeLapse
   * @classdesc Represents a ReqStartTimeLapse.
   * @implements IReqStartTimeLapse
   * @constructor
   * @param {IReqStartTimeLapse=} [properties] Properties to set
   */
  function ReqStartTimeLapse(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStartTimeLapse instance using the specified properties.
   * @function create
   * @memberof ReqStartTimeLapse
   * @static
   * @param {IReqStartTimeLapse=} [properties] Properties to set
   * @returns {ReqStartTimeLapse} ReqStartTimeLapse instance
   */
  ReqStartTimeLapse.create = function create(properties) {
    return new ReqStartTimeLapse(properties);
  };

  /**
   * Encodes the specified ReqStartTimeLapse message. Does not implicitly {@link ReqStartTimeLapse.verify|verify} messages.
   * @function encode
   * @memberof ReqStartTimeLapse
   * @static
   * @param {IReqStartTimeLapse} message ReqStartTimeLapse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTimeLapse.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStartTimeLapse message, length delimited. Does not implicitly {@link ReqStartTimeLapse.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartTimeLapse
   * @static
   * @param {IReqStartTimeLapse} message ReqStartTimeLapse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTimeLapse.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartTimeLapse message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartTimeLapse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartTimeLapse} ReqStartTimeLapse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTimeLapse.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartTimeLapse();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartTimeLapse message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartTimeLapse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartTimeLapse} ReqStartTimeLapse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTimeLapse.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartTimeLapse message.
   * @function verify
   * @memberof ReqStartTimeLapse
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartTimeLapse.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStartTimeLapse message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartTimeLapse
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartTimeLapse} ReqStartTimeLapse
   */
  ReqStartTimeLapse.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartTimeLapse) return object;
    return new $root.ReqStartTimeLapse();
  };

  /**
   * Creates a plain object from a ReqStartTimeLapse message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartTimeLapse
   * @static
   * @param {ReqStartTimeLapse} message ReqStartTimeLapse
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartTimeLapse.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStartTimeLapse to JSON.
   * @function toJSON
   * @memberof ReqStartTimeLapse
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartTimeLapse.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartTimeLapse
   * @function getTypeUrl
   * @memberof ReqStartTimeLapse
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartTimeLapse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartTimeLapse";
  };

  return ReqStartTimeLapse;
})();

$root.ReqStopTimeLapse = (function () {
  /**
   * Properties of a ReqStopTimeLapse.
   * @exports IReqStopTimeLapse
   * @interface IReqStopTimeLapse
   */

  /**
   * Constructs a new ReqStopTimeLapse.
   * @exports ReqStopTimeLapse
   * @classdesc Represents a ReqStopTimeLapse.
   * @implements IReqStopTimeLapse
   * @constructor
   * @param {IReqStopTimeLapse=} [properties] Properties to set
   */
  function ReqStopTimeLapse(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopTimeLapse instance using the specified properties.
   * @function create
   * @memberof ReqStopTimeLapse
   * @static
   * @param {IReqStopTimeLapse=} [properties] Properties to set
   * @returns {ReqStopTimeLapse} ReqStopTimeLapse instance
   */
  ReqStopTimeLapse.create = function create(properties) {
    return new ReqStopTimeLapse(properties);
  };

  /**
   * Encodes the specified ReqStopTimeLapse message. Does not implicitly {@link ReqStopTimeLapse.verify|verify} messages.
   * @function encode
   * @memberof ReqStopTimeLapse
   * @static
   * @param {IReqStopTimeLapse} message ReqStopTimeLapse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTimeLapse.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopTimeLapse message, length delimited. Does not implicitly {@link ReqStopTimeLapse.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopTimeLapse
   * @static
   * @param {IReqStopTimeLapse} message ReqStopTimeLapse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTimeLapse.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopTimeLapse message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopTimeLapse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopTimeLapse} ReqStopTimeLapse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTimeLapse.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopTimeLapse();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopTimeLapse message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopTimeLapse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopTimeLapse} ReqStopTimeLapse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTimeLapse.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopTimeLapse message.
   * @function verify
   * @memberof ReqStopTimeLapse
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopTimeLapse.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopTimeLapse message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopTimeLapse
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopTimeLapse} ReqStopTimeLapse
   */
  ReqStopTimeLapse.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopTimeLapse) return object;
    return new $root.ReqStopTimeLapse();
  };

  /**
   * Creates a plain object from a ReqStopTimeLapse message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopTimeLapse
   * @static
   * @param {ReqStopTimeLapse} message ReqStopTimeLapse
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopTimeLapse.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopTimeLapse to JSON.
   * @function toJSON
   * @memberof ReqStopTimeLapse
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopTimeLapse.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopTimeLapse
   * @function getTypeUrl
   * @memberof ReqStopTimeLapse
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopTimeLapse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopTimeLapse";
  };

  return ReqStopTimeLapse;
})();

$root.ReqSetAllParams = (function () {
  /**
   * Properties of a ReqSetAllParams.
   * @exports IReqSetAllParams
   * @interface IReqSetAllParams
   * @property {number|null} [expMode] ReqSetAllParams expMode
   * @property {number|null} [expIndex] ReqSetAllParams expIndex
   * @property {number|null} [gainMode] ReqSetAllParams gainMode
   * @property {number|null} [gainIndex] ReqSetAllParams gainIndex
   * @property {number|null} [ircutValue] ReqSetAllParams ircutValue
   * @property {number|null} [wbMode] ReqSetAllParams wbMode
   * @property {number|null} [wbIndexType] ReqSetAllParams wbIndexType
   * @property {number|null} [wbIndex] ReqSetAllParams wbIndex
   * @property {number|null} [brightness] ReqSetAllParams brightness
   * @property {number|null} [contrast] ReqSetAllParams contrast
   * @property {number|null} [hue] ReqSetAllParams hue
   * @property {number|null} [saturation] ReqSetAllParams saturation
   * @property {number|null} [sharpness] ReqSetAllParams sharpness
   * @property {number|null} [jpgQuality] ReqSetAllParams jpgQuality
   */

  /**
   * Constructs a new ReqSetAllParams.
   * @exports ReqSetAllParams
   * @classdesc Represents a ReqSetAllParams.
   * @implements IReqSetAllParams
   * @constructor
   * @param {IReqSetAllParams=} [properties] Properties to set
   */
  function ReqSetAllParams(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetAllParams expMode.
   * @member {number} expMode
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.expMode = 0;

  /**
   * ReqSetAllParams expIndex.
   * @member {number} expIndex
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.expIndex = 0;

  /**
   * ReqSetAllParams gainMode.
   * @member {number} gainMode
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.gainMode = 0;

  /**
   * ReqSetAllParams gainIndex.
   * @member {number} gainIndex
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.gainIndex = 0;

  /**
   * ReqSetAllParams ircutValue.
   * @member {number} ircutValue
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.ircutValue = 0;

  /**
   * ReqSetAllParams wbMode.
   * @member {number} wbMode
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.wbMode = 0;

  /**
   * ReqSetAllParams wbIndexType.
   * @member {number} wbIndexType
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.wbIndexType = 0;

  /**
   * ReqSetAllParams wbIndex.
   * @member {number} wbIndex
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.wbIndex = 0;

  /**
   * ReqSetAllParams brightness.
   * @member {number} brightness
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.brightness = 0;

  /**
   * ReqSetAllParams contrast.
   * @member {number} contrast
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.contrast = 0;

  /**
   * ReqSetAllParams hue.
   * @member {number} hue
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.hue = 0;

  /**
   * ReqSetAllParams saturation.
   * @member {number} saturation
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.saturation = 0;

  /**
   * ReqSetAllParams sharpness.
   * @member {number} sharpness
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.sharpness = 0;

  /**
   * ReqSetAllParams jpgQuality.
   * @member {number} jpgQuality
   * @memberof ReqSetAllParams
   * @instance
   */
  ReqSetAllParams.prototype.jpgQuality = 0;

  /**
   * Creates a new ReqSetAllParams instance using the specified properties.
   * @function create
   * @memberof ReqSetAllParams
   * @static
   * @param {IReqSetAllParams=} [properties] Properties to set
   * @returns {ReqSetAllParams} ReqSetAllParams instance
   */
  ReqSetAllParams.create = function create(properties) {
    return new ReqSetAllParams(properties);
  };

  /**
   * Encodes the specified ReqSetAllParams message. Does not implicitly {@link ReqSetAllParams.verify|verify} messages.
   * @function encode
   * @memberof ReqSetAllParams
   * @static
   * @param {IReqSetAllParams} message ReqSetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAllParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.expMode != null &&
      Object.hasOwnProperty.call(message, "expMode")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.expMode);
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.expIndex);
    if (
      message.gainMode != null &&
      Object.hasOwnProperty.call(message, "gainMode")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.gainMode);
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.gainIndex);
    if (
      message.ircutValue != null &&
      Object.hasOwnProperty.call(message, "ircutValue")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.ircutValue);
    if (message.wbMode != null && Object.hasOwnProperty.call(message, "wbMode"))
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.wbMode);
    if (
      message.wbIndexType != null &&
      Object.hasOwnProperty.call(message, "wbIndexType")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.wbIndexType);
    if (
      message.wbIndex != null &&
      Object.hasOwnProperty.call(message, "wbIndex")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.wbIndex);
    if (
      message.brightness != null &&
      Object.hasOwnProperty.call(message, "brightness")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.brightness);
    if (
      message.contrast != null &&
      Object.hasOwnProperty.call(message, "contrast")
    )
      writer.uint32(/* id 10, wireType 0 =*/ 80).int32(message.contrast);
    if (message.hue != null && Object.hasOwnProperty.call(message, "hue"))
      writer.uint32(/* id 11, wireType 0 =*/ 88).int32(message.hue);
    if (
      message.saturation != null &&
      Object.hasOwnProperty.call(message, "saturation")
    )
      writer.uint32(/* id 12, wireType 0 =*/ 96).int32(message.saturation);
    if (
      message.sharpness != null &&
      Object.hasOwnProperty.call(message, "sharpness")
    )
      writer.uint32(/* id 13, wireType 0 =*/ 104).int32(message.sharpness);
    if (
      message.jpgQuality != null &&
      Object.hasOwnProperty.call(message, "jpgQuality")
    )
      writer.uint32(/* id 14, wireType 0 =*/ 112).int32(message.jpgQuality);
    return writer;
  };

  /**
   * Encodes the specified ReqSetAllParams message, length delimited. Does not implicitly {@link ReqSetAllParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetAllParams
   * @static
   * @param {IReqSetAllParams} message ReqSetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAllParams.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetAllParams message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetAllParams} ReqSetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAllParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetAllParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.expMode = reader.int32();
          break;
        }
        case 2: {
          message.expIndex = reader.int32();
          break;
        }
        case 3: {
          message.gainMode = reader.int32();
          break;
        }
        case 4: {
          message.gainIndex = reader.int32();
          break;
        }
        case 5: {
          message.ircutValue = reader.int32();
          break;
        }
        case 6: {
          message.wbMode = reader.int32();
          break;
        }
        case 7: {
          message.wbIndexType = reader.int32();
          break;
        }
        case 8: {
          message.wbIndex = reader.int32();
          break;
        }
        case 9: {
          message.brightness = reader.int32();
          break;
        }
        case 10: {
          message.contrast = reader.int32();
          break;
        }
        case 11: {
          message.hue = reader.int32();
          break;
        }
        case 12: {
          message.saturation = reader.int32();
          break;
        }
        case 13: {
          message.sharpness = reader.int32();
          break;
        }
        case 14: {
          message.jpgQuality = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetAllParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetAllParams} ReqSetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAllParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetAllParams message.
   * @function verify
   * @memberof ReqSetAllParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetAllParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.expMode != null &&
      Object.hasOwnProperty.call(message, "expMode")
    )
      if (!$util.isInteger(message.expMode)) return "expMode: integer expected";
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      if (!$util.isInteger(message.expIndex))
        return "expIndex: integer expected";
    if (
      message.gainMode != null &&
      Object.hasOwnProperty.call(message, "gainMode")
    )
      if (!$util.isInteger(message.gainMode))
        return "gainMode: integer expected";
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      if (!$util.isInteger(message.gainIndex))
        return "gainIndex: integer expected";
    if (
      message.ircutValue != null &&
      Object.hasOwnProperty.call(message, "ircutValue")
    )
      if (!$util.isInteger(message.ircutValue))
        return "ircutValue: integer expected";
    if (message.wbMode != null && Object.hasOwnProperty.call(message, "wbMode"))
      if (!$util.isInteger(message.wbMode)) return "wbMode: integer expected";
    if (
      message.wbIndexType != null &&
      Object.hasOwnProperty.call(message, "wbIndexType")
    )
      if (!$util.isInteger(message.wbIndexType))
        return "wbIndexType: integer expected";
    if (
      message.wbIndex != null &&
      Object.hasOwnProperty.call(message, "wbIndex")
    )
      if (!$util.isInteger(message.wbIndex)) return "wbIndex: integer expected";
    if (
      message.brightness != null &&
      Object.hasOwnProperty.call(message, "brightness")
    )
      if (!$util.isInteger(message.brightness))
        return "brightness: integer expected";
    if (
      message.contrast != null &&
      Object.hasOwnProperty.call(message, "contrast")
    )
      if (!$util.isInteger(message.contrast))
        return "contrast: integer expected";
    if (message.hue != null && Object.hasOwnProperty.call(message, "hue"))
      if (!$util.isInteger(message.hue)) return "hue: integer expected";
    if (
      message.saturation != null &&
      Object.hasOwnProperty.call(message, "saturation")
    )
      if (!$util.isInteger(message.saturation))
        return "saturation: integer expected";
    if (
      message.sharpness != null &&
      Object.hasOwnProperty.call(message, "sharpness")
    )
      if (!$util.isInteger(message.sharpness))
        return "sharpness: integer expected";
    if (
      message.jpgQuality != null &&
      Object.hasOwnProperty.call(message, "jpgQuality")
    )
      if (!$util.isInteger(message.jpgQuality))
        return "jpgQuality: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetAllParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetAllParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetAllParams} ReqSetAllParams
   */
  ReqSetAllParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetAllParams) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetAllParams: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetAllParams();
    if (object.expMode != null) message.expMode = object.expMode | 0;
    if (object.expIndex != null) message.expIndex = object.expIndex | 0;
    if (object.gainMode != null) message.gainMode = object.gainMode | 0;
    if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
    if (object.ircutValue != null) message.ircutValue = object.ircutValue | 0;
    if (object.wbMode != null) message.wbMode = object.wbMode | 0;
    if (object.wbIndexType != null)
      message.wbIndexType = object.wbIndexType | 0;
    if (object.wbIndex != null) message.wbIndex = object.wbIndex | 0;
    if (object.brightness != null) message.brightness = object.brightness | 0;
    if (object.contrast != null) message.contrast = object.contrast | 0;
    if (object.hue != null) message.hue = object.hue | 0;
    if (object.saturation != null) message.saturation = object.saturation | 0;
    if (object.sharpness != null) message.sharpness = object.sharpness | 0;
    if (object.jpgQuality != null) message.jpgQuality = object.jpgQuality | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetAllParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetAllParams
   * @static
   * @param {ReqSetAllParams} message ReqSetAllParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetAllParams.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.expMode = 0;
      object.expIndex = 0;
      object.gainMode = 0;
      object.gainIndex = 0;
      object.ircutValue = 0;
      object.wbMode = 0;
      object.wbIndexType = 0;
      object.wbIndex = 0;
      object.brightness = 0;
      object.contrast = 0;
      object.hue = 0;
      object.saturation = 0;
      object.sharpness = 0;
      object.jpgQuality = 0;
    }
    if (
      message.expMode != null &&
      Object.hasOwnProperty.call(message, "expMode")
    )
      object.expMode = message.expMode;
    if (
      message.expIndex != null &&
      Object.hasOwnProperty.call(message, "expIndex")
    )
      object.expIndex = message.expIndex;
    if (
      message.gainMode != null &&
      Object.hasOwnProperty.call(message, "gainMode")
    )
      object.gainMode = message.gainMode;
    if (
      message.gainIndex != null &&
      Object.hasOwnProperty.call(message, "gainIndex")
    )
      object.gainIndex = message.gainIndex;
    if (
      message.ircutValue != null &&
      Object.hasOwnProperty.call(message, "ircutValue")
    )
      object.ircutValue = message.ircutValue;
    if (message.wbMode != null && Object.hasOwnProperty.call(message, "wbMode"))
      object.wbMode = message.wbMode;
    if (
      message.wbIndexType != null &&
      Object.hasOwnProperty.call(message, "wbIndexType")
    )
      object.wbIndexType = message.wbIndexType;
    if (
      message.wbIndex != null &&
      Object.hasOwnProperty.call(message, "wbIndex")
    )
      object.wbIndex = message.wbIndex;
    if (
      message.brightness != null &&
      Object.hasOwnProperty.call(message, "brightness")
    )
      object.brightness = message.brightness;
    if (
      message.contrast != null &&
      Object.hasOwnProperty.call(message, "contrast")
    )
      object.contrast = message.contrast;
    if (message.hue != null && Object.hasOwnProperty.call(message, "hue"))
      object.hue = message.hue;
    if (
      message.saturation != null &&
      Object.hasOwnProperty.call(message, "saturation")
    )
      object.saturation = message.saturation;
    if (
      message.sharpness != null &&
      Object.hasOwnProperty.call(message, "sharpness")
    )
      object.sharpness = message.sharpness;
    if (
      message.jpgQuality != null &&
      Object.hasOwnProperty.call(message, "jpgQuality")
    )
      object.jpgQuality = message.jpgQuality;
    return object;
  };

  /**
   * Converts this ReqSetAllParams to JSON.
   * @function toJSON
   * @memberof ReqSetAllParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetAllParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetAllParams
   * @function getTypeUrl
   * @memberof ReqSetAllParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetAllParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetAllParams";
  };

  return ReqSetAllParams;
})();

$root.ReqGetAllParams = (function () {
  /**
   * Properties of a ReqGetAllParams.
   * @exports IReqGetAllParams
   * @interface IReqGetAllParams
   */

  /**
   * Constructs a new ReqGetAllParams.
   * @exports ReqGetAllParams
   * @classdesc Represents a ReqGetAllParams.
   * @implements IReqGetAllParams
   * @constructor
   * @param {IReqGetAllParams=} [properties] Properties to set
   */
  function ReqGetAllParams(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetAllParams instance using the specified properties.
   * @function create
   * @memberof ReqGetAllParams
   * @static
   * @param {IReqGetAllParams=} [properties] Properties to set
   * @returns {ReqGetAllParams} ReqGetAllParams instance
   */
  ReqGetAllParams.create = function create(properties) {
    return new ReqGetAllParams(properties);
  };

  /**
   * Encodes the specified ReqGetAllParams message. Does not implicitly {@link ReqGetAllParams.verify|verify} messages.
   * @function encode
   * @memberof ReqGetAllParams
   * @static
   * @param {IReqGetAllParams} message ReqGetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetAllParams message, length delimited. Does not implicitly {@link ReqGetAllParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetAllParams
   * @static
   * @param {IReqGetAllParams} message ReqGetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllParams.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetAllParams message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetAllParams} ReqGetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetAllParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetAllParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetAllParams} ReqGetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetAllParams message.
   * @function verify
   * @memberof ReqGetAllParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetAllParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetAllParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetAllParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetAllParams} ReqGetAllParams
   */
  ReqGetAllParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetAllParams) return object;
    return new $root.ReqGetAllParams();
  };

  /**
   * Creates a plain object from a ReqGetAllParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetAllParams
   * @static
   * @param {ReqGetAllParams} message ReqGetAllParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetAllParams.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetAllParams to JSON.
   * @function toJSON
   * @memberof ReqGetAllParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetAllParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetAllParams
   * @function getTypeUrl
   * @memberof ReqGetAllParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetAllParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetAllParams";
  };

  return ReqGetAllParams;
})();

$root.ResGetAllParams = (function () {
  /**
   * Properties of a ResGetAllParams.
   * @exports IResGetAllParams
   * @interface IResGetAllParams
   * @property {Array.<ICommonParam>|null} [allParams] ResGetAllParams allParams
   * @property {number|null} [code] ResGetAllParams code
   */

  /**
   * Constructs a new ResGetAllParams.
   * @exports ResGetAllParams
   * @classdesc Represents a ResGetAllParams.
   * @implements IResGetAllParams
   * @constructor
   * @param {IResGetAllParams=} [properties] Properties to set
   */
  function ResGetAllParams(properties) {
    this.allParams = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetAllParams allParams.
   * @member {Array.<ICommonParam>} allParams
   * @memberof ResGetAllParams
   * @instance
   */
  ResGetAllParams.prototype.allParams = $util.emptyArray;

  /**
   * ResGetAllParams code.
   * @member {number} code
   * @memberof ResGetAllParams
   * @instance
   */
  ResGetAllParams.prototype.code = 0;

  /**
   * Creates a new ResGetAllParams instance using the specified properties.
   * @function create
   * @memberof ResGetAllParams
   * @static
   * @param {IResGetAllParams=} [properties] Properties to set
   * @returns {ResGetAllParams} ResGetAllParams instance
   */
  ResGetAllParams.create = function create(properties) {
    return new ResGetAllParams(properties);
  };

  /**
   * Encodes the specified ResGetAllParams message. Does not implicitly {@link ResGetAllParams.verify|verify} messages.
   * @function encode
   * @memberof ResGetAllParams
   * @static
   * @param {IResGetAllParams} message ResGetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.allParams != null && message.allParams.length)
      for (var i = 0; i < message.allParams.length; ++i)
        $root.CommonParam.encode(
          message.allParams[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResGetAllParams message, length delimited. Does not implicitly {@link ResGetAllParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetAllParams
   * @static
   * @param {IResGetAllParams} message ResGetAllParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllParams.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetAllParams message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetAllParams} ResGetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetAllParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.allParams && message.allParams.length))
            message.allParams = [];
          message.allParams.push(
            $root.CommonParam.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetAllParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetAllParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetAllParams} ResGetAllParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetAllParams message.
   * @function verify
   * @memberof ResGetAllParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetAllParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.allParams != null &&
      Object.hasOwnProperty.call(message, "allParams")
    ) {
      if (!Array.isArray(message.allParams)) return "allParams: array expected";
      for (var i = 0; i < message.allParams.length; ++i) {
        var error = $root.CommonParam.verify(message.allParams[i], long + 1);
        if (error) return "allParams." + error;
      }
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResGetAllParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetAllParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetAllParams} ResGetAllParams
   */
  ResGetAllParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetAllParams) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetAllParams: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetAllParams();
    if (object.allParams) {
      if (!Array.isArray(object.allParams))
        throw TypeError(".ResGetAllParams.allParams: array expected");
      message.allParams = [];
      for (var i = 0; i < object.allParams.length; ++i) {
        if (!$util.isObject(object.allParams[i]))
          throw TypeError(".ResGetAllParams.allParams: object expected");
        message.allParams[i] = $root.CommonParam.fromObject(
          object.allParams[i],
          long + 1,
        );
      }
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetAllParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetAllParams
   * @static
   * @param {ResGetAllParams} message ResGetAllParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetAllParams.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.allParams = [];
    if (options.defaults) object.code = 0;
    if (message.allParams && message.allParams.length) {
      object.allParams = [];
      for (var j = 0; j < message.allParams.length; ++j)
        object.allParams[j] = $root.CommonParam.toObject(
          message.allParams[j],
          options,
          q + 1,
        );
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResGetAllParams to JSON.
   * @function toJSON
   * @memberof ResGetAllParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetAllParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetAllParams
   * @function getTypeUrl
   * @memberof ResGetAllParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetAllParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetAllParams";
  };

  return ResGetAllParams;
})();

$root.ReqSetFeatureParams = (function () {
  /**
   * Properties of a ReqSetFeatureParams.
   * @exports IReqSetFeatureParams
   * @interface IReqSetFeatureParams
   * @property {ICommonParam|null} [param] ReqSetFeatureParams param
   */

  /**
   * Constructs a new ReqSetFeatureParams.
   * @exports ReqSetFeatureParams
   * @classdesc Represents a ReqSetFeatureParams.
   * @implements IReqSetFeatureParams
   * @constructor
   * @param {IReqSetFeatureParams=} [properties] Properties to set
   */
  function ReqSetFeatureParams(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetFeatureParams param.
   * @member {ICommonParam|null|undefined} param
   * @memberof ReqSetFeatureParams
   * @instance
   */
  ReqSetFeatureParams.prototype.param = null;

  /**
   * Creates a new ReqSetFeatureParams instance using the specified properties.
   * @function create
   * @memberof ReqSetFeatureParams
   * @static
   * @param {IReqSetFeatureParams=} [properties] Properties to set
   * @returns {ReqSetFeatureParams} ReqSetFeatureParams instance
   */
  ReqSetFeatureParams.create = function create(properties) {
    return new ReqSetFeatureParams(properties);
  };

  /**
   * Encodes the specified ReqSetFeatureParams message. Does not implicitly {@link ReqSetFeatureParams.verify|verify} messages.
   * @function encode
   * @memberof ReqSetFeatureParams
   * @static
   * @param {IReqSetFeatureParams} message ReqSetFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetFeatureParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.param != null && Object.hasOwnProperty.call(message, "param"))
      $root.CommonParam.encode(
        message.param,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqSetFeatureParams message, length delimited. Does not implicitly {@link ReqSetFeatureParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetFeatureParams
   * @static
   * @param {IReqSetFeatureParams} message ReqSetFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetFeatureParams.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetFeatureParams message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetFeatureParams} ReqSetFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetFeatureParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetFeatureParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.param = $root.CommonParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetFeatureParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetFeatureParams} ReqSetFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetFeatureParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetFeatureParams message.
   * @function verify
   * @memberof ReqSetFeatureParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetFeatureParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.param != null && Object.hasOwnProperty.call(message, "param")) {
      var error = $root.CommonParam.verify(message.param, long + 1);
      if (error) return "param." + error;
    }
    return null;
  };

  /**
   * Creates a ReqSetFeatureParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetFeatureParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetFeatureParams} ReqSetFeatureParams
   */
  ReqSetFeatureParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetFeatureParams) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetFeatureParams: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetFeatureParams();
    if (object.param != null) {
      if (!$util.isObject(object.param))
        throw TypeError(".ReqSetFeatureParams.param: object expected");
      message.param = $root.CommonParam.fromObject(object.param, long + 1);
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqSetFeatureParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetFeatureParams
   * @static
   * @param {ReqSetFeatureParams} message ReqSetFeatureParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetFeatureParams.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.param = null;
    if (message.param != null && Object.hasOwnProperty.call(message, "param"))
      object.param = $root.CommonParam.toObject(message.param, options, q + 1);
    return object;
  };

  /**
   * Converts this ReqSetFeatureParams to JSON.
   * @function toJSON
   * @memberof ReqSetFeatureParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetFeatureParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetFeatureParams
   * @function getTypeUrl
   * @memberof ReqSetFeatureParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetFeatureParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetFeatureParams";
  };

  return ReqSetFeatureParams;
})();

$root.ReqGetAllFeatureParams = (function () {
  /**
   * Properties of a ReqGetAllFeatureParams.
   * @exports IReqGetAllFeatureParams
   * @interface IReqGetAllFeatureParams
   */

  /**
   * Constructs a new ReqGetAllFeatureParams.
   * @exports ReqGetAllFeatureParams
   * @classdesc Represents a ReqGetAllFeatureParams.
   * @implements IReqGetAllFeatureParams
   * @constructor
   * @param {IReqGetAllFeatureParams=} [properties] Properties to set
   */
  function ReqGetAllFeatureParams(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetAllFeatureParams instance using the specified properties.
   * @function create
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {IReqGetAllFeatureParams=} [properties] Properties to set
   * @returns {ReqGetAllFeatureParams} ReqGetAllFeatureParams instance
   */
  ReqGetAllFeatureParams.create = function create(properties) {
    return new ReqGetAllFeatureParams(properties);
  };

  /**
   * Encodes the specified ReqGetAllFeatureParams message. Does not implicitly {@link ReqGetAllFeatureParams.verify|verify} messages.
   * @function encode
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {IReqGetAllFeatureParams} message ReqGetAllFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllFeatureParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetAllFeatureParams message, length delimited. Does not implicitly {@link ReqGetAllFeatureParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {IReqGetAllFeatureParams} message ReqGetAllFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllFeatureParams.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetAllFeatureParams message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetAllFeatureParams} ReqGetAllFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllFeatureParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetAllFeatureParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetAllFeatureParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetAllFeatureParams} ReqGetAllFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllFeatureParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetAllFeatureParams message.
   * @function verify
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetAllFeatureParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetAllFeatureParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetAllFeatureParams} ReqGetAllFeatureParams
   */
  ReqGetAllFeatureParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetAllFeatureParams) return object;
    return new $root.ReqGetAllFeatureParams();
  };

  /**
   * Creates a plain object from a ReqGetAllFeatureParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {ReqGetAllFeatureParams} message ReqGetAllFeatureParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetAllFeatureParams.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetAllFeatureParams to JSON.
   * @function toJSON
   * @memberof ReqGetAllFeatureParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetAllFeatureParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetAllFeatureParams
   * @function getTypeUrl
   * @memberof ReqGetAllFeatureParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetAllFeatureParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetAllFeatureParams";
  };

  return ReqGetAllFeatureParams;
})();

$root.ResGetAllFeatureParams = (function () {
  /**
   * Properties of a ResGetAllFeatureParams.
   * @exports IResGetAllFeatureParams
   * @interface IResGetAllFeatureParams
   * @property {Array.<ICommonParam>|null} [allFeatureParams] ResGetAllFeatureParams allFeatureParams
   * @property {number|null} [code] ResGetAllFeatureParams code
   */

  /**
   * Constructs a new ResGetAllFeatureParams.
   * @exports ResGetAllFeatureParams
   * @classdesc Represents a ResGetAllFeatureParams.
   * @implements IResGetAllFeatureParams
   * @constructor
   * @param {IResGetAllFeatureParams=} [properties] Properties to set
   */
  function ResGetAllFeatureParams(properties) {
    this.allFeatureParams = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetAllFeatureParams allFeatureParams.
   * @member {Array.<ICommonParam>} allFeatureParams
   * @memberof ResGetAllFeatureParams
   * @instance
   */
  ResGetAllFeatureParams.prototype.allFeatureParams = $util.emptyArray;

  /**
   * ResGetAllFeatureParams code.
   * @member {number} code
   * @memberof ResGetAllFeatureParams
   * @instance
   */
  ResGetAllFeatureParams.prototype.code = 0;

  /**
   * Creates a new ResGetAllFeatureParams instance using the specified properties.
   * @function create
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {IResGetAllFeatureParams=} [properties] Properties to set
   * @returns {ResGetAllFeatureParams} ResGetAllFeatureParams instance
   */
  ResGetAllFeatureParams.create = function create(properties) {
    return new ResGetAllFeatureParams(properties);
  };

  /**
   * Encodes the specified ResGetAllFeatureParams message. Does not implicitly {@link ResGetAllFeatureParams.verify|verify} messages.
   * @function encode
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {IResGetAllFeatureParams} message ResGetAllFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllFeatureParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.allFeatureParams != null && message.allFeatureParams.length)
      for (var i = 0; i < message.allFeatureParams.length; ++i)
        $root.CommonParam.encode(
          message.allFeatureParams[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResGetAllFeatureParams message, length delimited. Does not implicitly {@link ResGetAllFeatureParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {IResGetAllFeatureParams} message ResGetAllFeatureParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllFeatureParams.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetAllFeatureParams message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetAllFeatureParams} ResGetAllFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllFeatureParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetAllFeatureParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.allFeatureParams && message.allFeatureParams.length))
            message.allFeatureParams = [];
          message.allFeatureParams.push(
            $root.CommonParam.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetAllFeatureParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetAllFeatureParams} ResGetAllFeatureParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllFeatureParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetAllFeatureParams message.
   * @function verify
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetAllFeatureParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.allFeatureParams != null &&
      Object.hasOwnProperty.call(message, "allFeatureParams")
    ) {
      if (!Array.isArray(message.allFeatureParams))
        return "allFeatureParams: array expected";
      for (var i = 0; i < message.allFeatureParams.length; ++i) {
        var error = $root.CommonParam.verify(
          message.allFeatureParams[i],
          long + 1,
        );
        if (error) return "allFeatureParams." + error;
      }
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResGetAllFeatureParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetAllFeatureParams} ResGetAllFeatureParams
   */
  ResGetAllFeatureParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetAllFeatureParams) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetAllFeatureParams: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetAllFeatureParams();
    if (object.allFeatureParams) {
      if (!Array.isArray(object.allFeatureParams))
        throw TypeError(
          ".ResGetAllFeatureParams.allFeatureParams: array expected",
        );
      message.allFeatureParams = [];
      for (var i = 0; i < object.allFeatureParams.length; ++i) {
        if (!$util.isObject(object.allFeatureParams[i]))
          throw TypeError(
            ".ResGetAllFeatureParams.allFeatureParams: object expected",
          );
        message.allFeatureParams[i] = $root.CommonParam.fromObject(
          object.allFeatureParams[i],
          long + 1,
        );
      }
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetAllFeatureParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {ResGetAllFeatureParams} message ResGetAllFeatureParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetAllFeatureParams.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.allFeatureParams = [];
    if (options.defaults) object.code = 0;
    if (message.allFeatureParams && message.allFeatureParams.length) {
      object.allFeatureParams = [];
      for (var j = 0; j < message.allFeatureParams.length; ++j)
        object.allFeatureParams[j] = $root.CommonParam.toObject(
          message.allFeatureParams[j],
          options,
          q + 1,
        );
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResGetAllFeatureParams to JSON.
   * @function toJSON
   * @memberof ResGetAllFeatureParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetAllFeatureParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetAllFeatureParams
   * @function getTypeUrl
   * @memberof ResGetAllFeatureParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetAllFeatureParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetAllFeatureParams";
  };

  return ResGetAllFeatureParams;
})();

$root.ReqGetSystemWorkingState = (function () {
  /**
   * Properties of a ReqGetSystemWorkingState.
   * @exports IReqGetSystemWorkingState
   * @interface IReqGetSystemWorkingState
   */

  /**
   * Constructs a new ReqGetSystemWorkingState.
   * @exports ReqGetSystemWorkingState
   * @classdesc Represents a ReqGetSystemWorkingState.
   * @implements IReqGetSystemWorkingState
   * @constructor
   * @param {IReqGetSystemWorkingState=} [properties] Properties to set
   */
  function ReqGetSystemWorkingState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetSystemWorkingState instance using the specified properties.
   * @function create
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {IReqGetSystemWorkingState=} [properties] Properties to set
   * @returns {ReqGetSystemWorkingState} ReqGetSystemWorkingState instance
   */
  ReqGetSystemWorkingState.create = function create(properties) {
    return new ReqGetSystemWorkingState(properties);
  };

  /**
   * Encodes the specified ReqGetSystemWorkingState message. Does not implicitly {@link ReqGetSystemWorkingState.verify|verify} messages.
   * @function encode
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {IReqGetSystemWorkingState} message ReqGetSystemWorkingState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSystemWorkingState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetSystemWorkingState message, length delimited. Does not implicitly {@link ReqGetSystemWorkingState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {IReqGetSystemWorkingState} message ReqGetSystemWorkingState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSystemWorkingState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetSystemWorkingState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetSystemWorkingState} ReqGetSystemWorkingState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSystemWorkingState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetSystemWorkingState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetSystemWorkingState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetSystemWorkingState} ReqGetSystemWorkingState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSystemWorkingState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetSystemWorkingState message.
   * @function verify
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetSystemWorkingState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetSystemWorkingState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetSystemWorkingState} ReqGetSystemWorkingState
   */
  ReqGetSystemWorkingState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetSystemWorkingState) return object;
    return new $root.ReqGetSystemWorkingState();
  };

  /**
   * Creates a plain object from a ReqGetSystemWorkingState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {ReqGetSystemWorkingState} message ReqGetSystemWorkingState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetSystemWorkingState.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetSystemWorkingState to JSON.
   * @function toJSON
   * @memberof ReqGetSystemWorkingState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetSystemWorkingState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetSystemWorkingState
   * @function getTypeUrl
   * @memberof ReqGetSystemWorkingState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetSystemWorkingState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetSystemWorkingState";
  };

  return ReqGetSystemWorkingState;
})();

$root.ReqSetJpgQuality = (function () {
  /**
   * Properties of a ReqSetJpgQuality.
   * @exports IReqSetJpgQuality
   * @interface IReqSetJpgQuality
   * @property {number|null} [quality] ReqSetJpgQuality quality
   */

  /**
   * Constructs a new ReqSetJpgQuality.
   * @exports ReqSetJpgQuality
   * @classdesc Represents a ReqSetJpgQuality.
   * @implements IReqSetJpgQuality
   * @constructor
   * @param {IReqSetJpgQuality=} [properties] Properties to set
   */
  function ReqSetJpgQuality(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetJpgQuality quality.
   * @member {number} quality
   * @memberof ReqSetJpgQuality
   * @instance
   */
  ReqSetJpgQuality.prototype.quality = 0;

  /**
   * Creates a new ReqSetJpgQuality instance using the specified properties.
   * @function create
   * @memberof ReqSetJpgQuality
   * @static
   * @param {IReqSetJpgQuality=} [properties] Properties to set
   * @returns {ReqSetJpgQuality} ReqSetJpgQuality instance
   */
  ReqSetJpgQuality.create = function create(properties) {
    return new ReqSetJpgQuality(properties);
  };

  /**
   * Encodes the specified ReqSetJpgQuality message. Does not implicitly {@link ReqSetJpgQuality.verify|verify} messages.
   * @function encode
   * @memberof ReqSetJpgQuality
   * @static
   * @param {IReqSetJpgQuality} message ReqSetJpgQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetJpgQuality.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.quality);
    return writer;
  };

  /**
   * Encodes the specified ReqSetJpgQuality message, length delimited. Does not implicitly {@link ReqSetJpgQuality.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetJpgQuality
   * @static
   * @param {IReqSetJpgQuality} message ReqSetJpgQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetJpgQuality.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetJpgQuality message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetJpgQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetJpgQuality} ReqSetJpgQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetJpgQuality.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetJpgQuality();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.quality = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetJpgQuality message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetJpgQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetJpgQuality} ReqSetJpgQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetJpgQuality.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetJpgQuality message.
   * @function verify
   * @memberof ReqSetJpgQuality
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetJpgQuality.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      if (!$util.isInteger(message.quality)) return "quality: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetJpgQuality message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetJpgQuality
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetJpgQuality} ReqSetJpgQuality
   */
  ReqSetJpgQuality.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetJpgQuality) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetJpgQuality: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetJpgQuality();
    if (object.quality != null) message.quality = object.quality | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetJpgQuality message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetJpgQuality
   * @static
   * @param {ReqSetJpgQuality} message ReqSetJpgQuality
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetJpgQuality.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.quality = 0;
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      object.quality = message.quality;
    return object;
  };

  /**
   * Converts this ReqSetJpgQuality to JSON.
   * @function toJSON
   * @memberof ReqSetJpgQuality
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetJpgQuality.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetJpgQuality
   * @function getTypeUrl
   * @memberof ReqSetJpgQuality
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetJpgQuality.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetJpgQuality";
  };

  return ReqSetJpgQuality;
})();

$root.ReqGetJpgQuality = (function () {
  /**
   * Properties of a ReqGetJpgQuality.
   * @exports IReqGetJpgQuality
   * @interface IReqGetJpgQuality
   */

  /**
   * Constructs a new ReqGetJpgQuality.
   * @exports ReqGetJpgQuality
   * @classdesc Represents a ReqGetJpgQuality.
   * @implements IReqGetJpgQuality
   * @constructor
   * @param {IReqGetJpgQuality=} [properties] Properties to set
   */
  function ReqGetJpgQuality(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetJpgQuality instance using the specified properties.
   * @function create
   * @memberof ReqGetJpgQuality
   * @static
   * @param {IReqGetJpgQuality=} [properties] Properties to set
   * @returns {ReqGetJpgQuality} ReqGetJpgQuality instance
   */
  ReqGetJpgQuality.create = function create(properties) {
    return new ReqGetJpgQuality(properties);
  };

  /**
   * Encodes the specified ReqGetJpgQuality message. Does not implicitly {@link ReqGetJpgQuality.verify|verify} messages.
   * @function encode
   * @memberof ReqGetJpgQuality
   * @static
   * @param {IReqGetJpgQuality} message ReqGetJpgQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetJpgQuality.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetJpgQuality message, length delimited. Does not implicitly {@link ReqGetJpgQuality.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetJpgQuality
   * @static
   * @param {IReqGetJpgQuality} message ReqGetJpgQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetJpgQuality.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetJpgQuality message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetJpgQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetJpgQuality} ReqGetJpgQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetJpgQuality.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetJpgQuality();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetJpgQuality message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetJpgQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetJpgQuality} ReqGetJpgQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetJpgQuality.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetJpgQuality message.
   * @function verify
   * @memberof ReqGetJpgQuality
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetJpgQuality.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetJpgQuality message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetJpgQuality
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetJpgQuality} ReqGetJpgQuality
   */
  ReqGetJpgQuality.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetJpgQuality) return object;
    return new $root.ReqGetJpgQuality();
  };

  /**
   * Creates a plain object from a ReqGetJpgQuality message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetJpgQuality
   * @static
   * @param {ReqGetJpgQuality} message ReqGetJpgQuality
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetJpgQuality.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetJpgQuality to JSON.
   * @function toJSON
   * @memberof ReqGetJpgQuality
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetJpgQuality.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetJpgQuality
   * @function getTypeUrl
   * @memberof ReqGetJpgQuality
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetJpgQuality.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetJpgQuality";
  };

  return ReqGetJpgQuality;
})();

$root.ReqPhotoRaw = (function () {
  /**
   * Properties of a ReqPhotoRaw.
   * @exports IReqPhotoRaw
   * @interface IReqPhotoRaw
   */

  /**
   * Constructs a new ReqPhotoRaw.
   * @exports ReqPhotoRaw
   * @classdesc Represents a ReqPhotoRaw.
   * @implements IReqPhotoRaw
   * @constructor
   * @param {IReqPhotoRaw=} [properties] Properties to set
   */
  function ReqPhotoRaw(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPhotoRaw instance using the specified properties.
   * @function create
   * @memberof ReqPhotoRaw
   * @static
   * @param {IReqPhotoRaw=} [properties] Properties to set
   * @returns {ReqPhotoRaw} ReqPhotoRaw instance
   */
  ReqPhotoRaw.create = function create(properties) {
    return new ReqPhotoRaw(properties);
  };

  /**
   * Encodes the specified ReqPhotoRaw message. Does not implicitly {@link ReqPhotoRaw.verify|verify} messages.
   * @function encode
   * @memberof ReqPhotoRaw
   * @static
   * @param {IReqPhotoRaw} message ReqPhotoRaw message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPhotoRaw.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPhotoRaw message, length delimited. Does not implicitly {@link ReqPhotoRaw.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPhotoRaw
   * @static
   * @param {IReqPhotoRaw} message ReqPhotoRaw message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPhotoRaw.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPhotoRaw message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPhotoRaw
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPhotoRaw} ReqPhotoRaw
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPhotoRaw.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPhotoRaw();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPhotoRaw message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPhotoRaw
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPhotoRaw} ReqPhotoRaw
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPhotoRaw.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPhotoRaw message.
   * @function verify
   * @memberof ReqPhotoRaw
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPhotoRaw.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPhotoRaw message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPhotoRaw
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPhotoRaw} ReqPhotoRaw
   */
  ReqPhotoRaw.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPhotoRaw) return object;
    return new $root.ReqPhotoRaw();
  };

  /**
   * Creates a plain object from a ReqPhotoRaw message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPhotoRaw
   * @static
   * @param {ReqPhotoRaw} message ReqPhotoRaw
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPhotoRaw.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPhotoRaw to JSON.
   * @function toJSON
   * @memberof ReqPhotoRaw
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPhotoRaw.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPhotoRaw
   * @function getTypeUrl
   * @memberof ReqPhotoRaw
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPhotoRaw.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPhotoRaw";
  };

  return ReqPhotoRaw;
})();

$root.ReqSetRtspBitRateType = (function () {
  /**
   * Properties of a ReqSetRtspBitRateType.
   * @exports IReqSetRtspBitRateType
   * @interface IReqSetRtspBitRateType
   * @property {number|null} [bitrateType] ReqSetRtspBitRateType bitrateType
   */

  /**
   * Constructs a new ReqSetRtspBitRateType.
   * @exports ReqSetRtspBitRateType
   * @classdesc Represents a ReqSetRtspBitRateType.
   * @implements IReqSetRtspBitRateType
   * @constructor
   * @param {IReqSetRtspBitRateType=} [properties] Properties to set
   */
  function ReqSetRtspBitRateType(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetRtspBitRateType bitrateType.
   * @member {number} bitrateType
   * @memberof ReqSetRtspBitRateType
   * @instance
   */
  ReqSetRtspBitRateType.prototype.bitrateType = 0;

  /**
   * Creates a new ReqSetRtspBitRateType instance using the specified properties.
   * @function create
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {IReqSetRtspBitRateType=} [properties] Properties to set
   * @returns {ReqSetRtspBitRateType} ReqSetRtspBitRateType instance
   */
  ReqSetRtspBitRateType.create = function create(properties) {
    return new ReqSetRtspBitRateType(properties);
  };

  /**
   * Encodes the specified ReqSetRtspBitRateType message. Does not implicitly {@link ReqSetRtspBitRateType.verify|verify} messages.
   * @function encode
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {IReqSetRtspBitRateType} message ReqSetRtspBitRateType message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetRtspBitRateType.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.bitrateType != null &&
      Object.hasOwnProperty.call(message, "bitrateType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.bitrateType);
    return writer;
  };

  /**
   * Encodes the specified ReqSetRtspBitRateType message, length delimited. Does not implicitly {@link ReqSetRtspBitRateType.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {IReqSetRtspBitRateType} message ReqSetRtspBitRateType message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetRtspBitRateType.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetRtspBitRateType message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetRtspBitRateType} ReqSetRtspBitRateType
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetRtspBitRateType.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetRtspBitRateType();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.bitrateType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetRtspBitRateType message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetRtspBitRateType} ReqSetRtspBitRateType
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetRtspBitRateType.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetRtspBitRateType message.
   * @function verify
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetRtspBitRateType.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.bitrateType != null &&
      Object.hasOwnProperty.call(message, "bitrateType")
    )
      if (!$util.isInteger(message.bitrateType))
        return "bitrateType: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetRtspBitRateType message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetRtspBitRateType} ReqSetRtspBitRateType
   */
  ReqSetRtspBitRateType.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetRtspBitRateType) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetRtspBitRateType: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetRtspBitRateType();
    if (object.bitrateType != null)
      message.bitrateType = object.bitrateType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetRtspBitRateType message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {ReqSetRtspBitRateType} message ReqSetRtspBitRateType
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetRtspBitRateType.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.bitrateType = 0;
    if (
      message.bitrateType != null &&
      Object.hasOwnProperty.call(message, "bitrateType")
    )
      object.bitrateType = message.bitrateType;
    return object;
  };

  /**
   * Converts this ReqSetRtspBitRateType to JSON.
   * @function toJSON
   * @memberof ReqSetRtspBitRateType
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetRtspBitRateType.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetRtspBitRateType
   * @function getTypeUrl
   * @memberof ReqSetRtspBitRateType
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetRtspBitRateType.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetRtspBitRateType";
  };

  return ReqSetRtspBitRateType;
})();

$root.ReqDisableAllIspProcessing = (function () {
  /**
   * Properties of a ReqDisableAllIspProcessing.
   * @exports IReqDisableAllIspProcessing
   * @interface IReqDisableAllIspProcessing
   */

  /**
   * Constructs a new ReqDisableAllIspProcessing.
   * @exports ReqDisableAllIspProcessing
   * @classdesc Represents a ReqDisableAllIspProcessing.
   * @implements IReqDisableAllIspProcessing
   * @constructor
   * @param {IReqDisableAllIspProcessing=} [properties] Properties to set
   */
  function ReqDisableAllIspProcessing(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqDisableAllIspProcessing instance using the specified properties.
   * @function create
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {IReqDisableAllIspProcessing=} [properties] Properties to set
   * @returns {ReqDisableAllIspProcessing} ReqDisableAllIspProcessing instance
   */
  ReqDisableAllIspProcessing.create = function create(properties) {
    return new ReqDisableAllIspProcessing(properties);
  };

  /**
   * Encodes the specified ReqDisableAllIspProcessing message. Does not implicitly {@link ReqDisableAllIspProcessing.verify|verify} messages.
   * @function encode
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {IReqDisableAllIspProcessing} message ReqDisableAllIspProcessing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDisableAllIspProcessing.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqDisableAllIspProcessing message, length delimited. Does not implicitly {@link ReqDisableAllIspProcessing.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {IReqDisableAllIspProcessing} message ReqDisableAllIspProcessing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDisableAllIspProcessing.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDisableAllIspProcessing message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDisableAllIspProcessing} ReqDisableAllIspProcessing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDisableAllIspProcessing.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDisableAllIspProcessing();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDisableAllIspProcessing message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDisableAllIspProcessing} ReqDisableAllIspProcessing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDisableAllIspProcessing.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDisableAllIspProcessing message.
   * @function verify
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDisableAllIspProcessing.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqDisableAllIspProcessing message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDisableAllIspProcessing} ReqDisableAllIspProcessing
   */
  ReqDisableAllIspProcessing.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDisableAllIspProcessing) return object;
    return new $root.ReqDisableAllIspProcessing();
  };

  /**
   * Creates a plain object from a ReqDisableAllIspProcessing message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {ReqDisableAllIspProcessing} message ReqDisableAllIspProcessing
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDisableAllIspProcessing.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqDisableAllIspProcessing to JSON.
   * @function toJSON
   * @memberof ReqDisableAllIspProcessing
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDisableAllIspProcessing.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDisableAllIspProcessing
   * @function getTypeUrl
   * @memberof ReqDisableAllIspProcessing
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDisableAllIspProcessing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDisableAllIspProcessing";
  };

  return ReqDisableAllIspProcessing;
})();

$root.ReqEnableAllIspProcessing = (function () {
  /**
   * Properties of a ReqEnableAllIspProcessing.
   * @exports IReqEnableAllIspProcessing
   * @interface IReqEnableAllIspProcessing
   */

  /**
   * Constructs a new ReqEnableAllIspProcessing.
   * @exports ReqEnableAllIspProcessing
   * @classdesc Represents a ReqEnableAllIspProcessing.
   * @implements IReqEnableAllIspProcessing
   * @constructor
   * @param {IReqEnableAllIspProcessing=} [properties] Properties to set
   */
  function ReqEnableAllIspProcessing(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqEnableAllIspProcessing instance using the specified properties.
   * @function create
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {IReqEnableAllIspProcessing=} [properties] Properties to set
   * @returns {ReqEnableAllIspProcessing} ReqEnableAllIspProcessing instance
   */
  ReqEnableAllIspProcessing.create = function create(properties) {
    return new ReqEnableAllIspProcessing(properties);
  };

  /**
   * Encodes the specified ReqEnableAllIspProcessing message. Does not implicitly {@link ReqEnableAllIspProcessing.verify|verify} messages.
   * @function encode
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {IReqEnableAllIspProcessing} message ReqEnableAllIspProcessing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqEnableAllIspProcessing.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqEnableAllIspProcessing message, length delimited. Does not implicitly {@link ReqEnableAllIspProcessing.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {IReqEnableAllIspProcessing} message ReqEnableAllIspProcessing message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqEnableAllIspProcessing.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqEnableAllIspProcessing message from the specified reader or buffer.
   * @function decode
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqEnableAllIspProcessing} ReqEnableAllIspProcessing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqEnableAllIspProcessing.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqEnableAllIspProcessing();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqEnableAllIspProcessing message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqEnableAllIspProcessing} ReqEnableAllIspProcessing
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqEnableAllIspProcessing.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqEnableAllIspProcessing message.
   * @function verify
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqEnableAllIspProcessing.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqEnableAllIspProcessing message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqEnableAllIspProcessing} ReqEnableAllIspProcessing
   */
  ReqEnableAllIspProcessing.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqEnableAllIspProcessing) return object;
    return new $root.ReqEnableAllIspProcessing();
  };

  /**
   * Creates a plain object from a ReqEnableAllIspProcessing message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {ReqEnableAllIspProcessing} message ReqEnableAllIspProcessing
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqEnableAllIspProcessing.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqEnableAllIspProcessing to JSON.
   * @function toJSON
   * @memberof ReqEnableAllIspProcessing
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqEnableAllIspProcessing.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqEnableAllIspProcessing
   * @function getTypeUrl
   * @memberof ReqEnableAllIspProcessing
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqEnableAllIspProcessing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqEnableAllIspProcessing";
  };

  return ReqEnableAllIspProcessing;
})();

$root.IspModuleState = (function () {
  /**
   * Properties of an IspModuleState.
   * @exports IIspModuleState
   * @interface IIspModuleState
   * @property {number|null} [moduleId] IspModuleState moduleId
   * @property {boolean|null} [state] IspModuleState state
   */

  /**
   * Constructs a new IspModuleState.
   * @exports IspModuleState
   * @classdesc Represents an IspModuleState.
   * @implements IIspModuleState
   * @constructor
   * @param {IIspModuleState=} [properties] Properties to set
   */
  function IspModuleState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * IspModuleState moduleId.
   * @member {number} moduleId
   * @memberof IspModuleState
   * @instance
   */
  IspModuleState.prototype.moduleId = 0;

  /**
   * IspModuleState state.
   * @member {boolean} state
   * @memberof IspModuleState
   * @instance
   */
  IspModuleState.prototype.state = false;

  /**
   * Creates a new IspModuleState instance using the specified properties.
   * @function create
   * @memberof IspModuleState
   * @static
   * @param {IIspModuleState=} [properties] Properties to set
   * @returns {IspModuleState} IspModuleState instance
   */
  IspModuleState.create = function create(properties) {
    return new IspModuleState(properties);
  };

  /**
   * Encodes the specified IspModuleState message. Does not implicitly {@link IspModuleState.verify|verify} messages.
   * @function encode
   * @memberof IspModuleState
   * @static
   * @param {IIspModuleState} message IspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  IspModuleState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.moduleId);
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.state);
    return writer;
  };

  /**
   * Encodes the specified IspModuleState message, length delimited. Does not implicitly {@link IspModuleState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof IspModuleState
   * @static
   * @param {IIspModuleState} message IspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  IspModuleState.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes an IspModuleState message from the specified reader or buffer.
   * @function decode
   * @memberof IspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {IspModuleState} IspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  IspModuleState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.IspModuleState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.moduleId = reader.int32();
          break;
        }
        case 2: {
          message.state = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes an IspModuleState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof IspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {IspModuleState} IspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  IspModuleState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies an IspModuleState message.
   * @function verify
   * @memberof IspModuleState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  IspModuleState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      if (!$util.isInteger(message.moduleId))
        return "moduleId: integer expected";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (typeof message.state !== "boolean") return "state: boolean expected";
    return null;
  };

  /**
   * Creates an IspModuleState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof IspModuleState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {IspModuleState} IspModuleState
   */
  IspModuleState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.IspModuleState) return object;
    if (!$util.isObject(object))
      throw TypeError(".IspModuleState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.IspModuleState();
    if (object.moduleId != null) message.moduleId = object.moduleId | 0;
    if (object.state != null) message.state = Boolean(object.state);
    return message;
  };

  /**
   * Creates a plain object from an IspModuleState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof IspModuleState
   * @static
   * @param {IspModuleState} message IspModuleState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  IspModuleState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.moduleId = 0;
      object.state = false;
    }
    if (
      message.moduleId != null &&
      Object.hasOwnProperty.call(message, "moduleId")
    )
      object.moduleId = message.moduleId;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    return object;
  };

  /**
   * Converts this IspModuleState to JSON.
   * @function toJSON
   * @memberof IspModuleState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  IspModuleState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for IspModuleState
   * @function getTypeUrl
   * @memberof IspModuleState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  IspModuleState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/IspModuleState";
  };

  return IspModuleState;
})();

$root.ReqSetIspModuleState = (function () {
  /**
   * Properties of a ReqSetIspModuleState.
   * @exports IReqSetIspModuleState
   * @interface IReqSetIspModuleState
   * @property {Array.<IIspModuleState>|null} [moduleStates] ReqSetIspModuleState moduleStates
   */

  /**
   * Constructs a new ReqSetIspModuleState.
   * @exports ReqSetIspModuleState
   * @classdesc Represents a ReqSetIspModuleState.
   * @implements IReqSetIspModuleState
   * @constructor
   * @param {IReqSetIspModuleState=} [properties] Properties to set
   */
  function ReqSetIspModuleState(properties) {
    this.moduleStates = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetIspModuleState moduleStates.
   * @member {Array.<IIspModuleState>} moduleStates
   * @memberof ReqSetIspModuleState
   * @instance
   */
  ReqSetIspModuleState.prototype.moduleStates = $util.emptyArray;

  /**
   * Creates a new ReqSetIspModuleState instance using the specified properties.
   * @function create
   * @memberof ReqSetIspModuleState
   * @static
   * @param {IReqSetIspModuleState=} [properties] Properties to set
   * @returns {ReqSetIspModuleState} ReqSetIspModuleState instance
   */
  ReqSetIspModuleState.create = function create(properties) {
    return new ReqSetIspModuleState(properties);
  };

  /**
   * Encodes the specified ReqSetIspModuleState message. Does not implicitly {@link ReqSetIspModuleState.verify|verify} messages.
   * @function encode
   * @memberof ReqSetIspModuleState
   * @static
   * @param {IReqSetIspModuleState} message ReqSetIspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetIspModuleState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.moduleStates != null && message.moduleStates.length)
      for (var i = 0; i < message.moduleStates.length; ++i)
        $root.IspModuleState.encode(
          message.moduleStates[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqSetIspModuleState message, length delimited. Does not implicitly {@link ReqSetIspModuleState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetIspModuleState
   * @static
   * @param {IReqSetIspModuleState} message ReqSetIspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetIspModuleState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetIspModuleState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetIspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetIspModuleState} ReqSetIspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetIspModuleState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetIspModuleState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.moduleStates && message.moduleStates.length))
            message.moduleStates = [];
          message.moduleStates.push(
            $root.IspModuleState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetIspModuleState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetIspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetIspModuleState} ReqSetIspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetIspModuleState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetIspModuleState message.
   * @function verify
   * @memberof ReqSetIspModuleState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetIspModuleState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.moduleStates != null &&
      Object.hasOwnProperty.call(message, "moduleStates")
    ) {
      if (!Array.isArray(message.moduleStates))
        return "moduleStates: array expected";
      for (var i = 0; i < message.moduleStates.length; ++i) {
        var error = $root.IspModuleState.verify(
          message.moduleStates[i],
          long + 1,
        );
        if (error) return "moduleStates." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ReqSetIspModuleState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetIspModuleState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetIspModuleState} ReqSetIspModuleState
   */
  ReqSetIspModuleState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetIspModuleState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetIspModuleState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetIspModuleState();
    if (object.moduleStates) {
      if (!Array.isArray(object.moduleStates))
        throw TypeError(".ReqSetIspModuleState.moduleStates: array expected");
      message.moduleStates = [];
      for (var i = 0; i < object.moduleStates.length; ++i) {
        if (!$util.isObject(object.moduleStates[i]))
          throw TypeError(
            ".ReqSetIspModuleState.moduleStates: object expected",
          );
        message.moduleStates[i] = $root.IspModuleState.fromObject(
          object.moduleStates[i],
          long + 1,
        );
      }
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqSetIspModuleState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetIspModuleState
   * @static
   * @param {ReqSetIspModuleState} message ReqSetIspModuleState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetIspModuleState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.moduleStates = [];
    if (message.moduleStates && message.moduleStates.length) {
      object.moduleStates = [];
      for (var j = 0; j < message.moduleStates.length; ++j)
        object.moduleStates[j] = $root.IspModuleState.toObject(
          message.moduleStates[j],
          options,
          q + 1,
        );
    }
    return object;
  };

  /**
   * Converts this ReqSetIspModuleState to JSON.
   * @function toJSON
   * @memberof ReqSetIspModuleState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetIspModuleState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetIspModuleState
   * @function getTypeUrl
   * @memberof ReqSetIspModuleState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetIspModuleState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetIspModuleState";
  };

  return ReqSetIspModuleState;
})();

$root.ReqGetIspModuleState = (function () {
  /**
   * Properties of a ReqGetIspModuleState.
   * @exports IReqGetIspModuleState
   * @interface IReqGetIspModuleState
   * @property {Array.<number>|null} [moduleIds] ReqGetIspModuleState moduleIds
   */

  /**
   * Constructs a new ReqGetIspModuleState.
   * @exports ReqGetIspModuleState
   * @classdesc Represents a ReqGetIspModuleState.
   * @implements IReqGetIspModuleState
   * @constructor
   * @param {IReqGetIspModuleState=} [properties] Properties to set
   */
  function ReqGetIspModuleState(properties) {
    this.moduleIds = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetIspModuleState moduleIds.
   * @member {Array.<number>} moduleIds
   * @memberof ReqGetIspModuleState
   * @instance
   */
  ReqGetIspModuleState.prototype.moduleIds = $util.emptyArray;

  /**
   * Creates a new ReqGetIspModuleState instance using the specified properties.
   * @function create
   * @memberof ReqGetIspModuleState
   * @static
   * @param {IReqGetIspModuleState=} [properties] Properties to set
   * @returns {ReqGetIspModuleState} ReqGetIspModuleState instance
   */
  ReqGetIspModuleState.create = function create(properties) {
    return new ReqGetIspModuleState(properties);
  };

  /**
   * Encodes the specified ReqGetIspModuleState message. Does not implicitly {@link ReqGetIspModuleState.verify|verify} messages.
   * @function encode
   * @memberof ReqGetIspModuleState
   * @static
   * @param {IReqGetIspModuleState} message ReqGetIspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetIspModuleState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.moduleIds != null && message.moduleIds.length) {
      writer.uint32(/* id 1, wireType 2 =*/ 10).fork();
      for (var i = 0; i < message.moduleIds.length; ++i)
        writer.int32(message.moduleIds[i]);
      writer.ldelim();
    }
    return writer;
  };

  /**
   * Encodes the specified ReqGetIspModuleState message, length delimited. Does not implicitly {@link ReqGetIspModuleState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetIspModuleState
   * @static
   * @param {IReqGetIspModuleState} message ReqGetIspModuleState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetIspModuleState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetIspModuleState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetIspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetIspModuleState} ReqGetIspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetIspModuleState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetIspModuleState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.moduleIds && message.moduleIds.length))
            message.moduleIds = [];
          if ((tag & 7) === 2) {
            var end2 = reader.uint32() + reader.pos;
            if (end2 > reader.len) throw RangeError("index out of range");
            reader.len = end2;
            while (reader.pos < end2) message.moduleIds.push(reader.int32());
            if (reader.pos !== end2) throw RangeError("index out of range");
            reader.len = end;
          } else message.moduleIds.push(reader.int32());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetIspModuleState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetIspModuleState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetIspModuleState} ReqGetIspModuleState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetIspModuleState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetIspModuleState message.
   * @function verify
   * @memberof ReqGetIspModuleState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetIspModuleState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.moduleIds != null &&
      Object.hasOwnProperty.call(message, "moduleIds")
    ) {
      if (!Array.isArray(message.moduleIds)) return "moduleIds: array expected";
      for (var i = 0; i < message.moduleIds.length; ++i)
        if (!$util.isInteger(message.moduleIds[i]))
          return "moduleIds: integer[] expected";
    }
    return null;
  };

  /**
   * Creates a ReqGetIspModuleState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetIspModuleState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetIspModuleState} ReqGetIspModuleState
   */
  ReqGetIspModuleState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetIspModuleState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetIspModuleState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetIspModuleState();
    if (object.moduleIds) {
      if (!Array.isArray(object.moduleIds))
        throw TypeError(".ReqGetIspModuleState.moduleIds: array expected");
      message.moduleIds = [];
      for (var i = 0; i < object.moduleIds.length; ++i)
        message.moduleIds[i] = object.moduleIds[i] | 0;
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqGetIspModuleState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetIspModuleState
   * @static
   * @param {ReqGetIspModuleState} message ReqGetIspModuleState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetIspModuleState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.moduleIds = [];
    if (message.moduleIds && message.moduleIds.length) {
      object.moduleIds = [];
      for (var j = 0; j < message.moduleIds.length; ++j)
        object.moduleIds[j] = message.moduleIds[j];
    }
    return object;
  };

  /**
   * Converts this ReqGetIspModuleState to JSON.
   * @function toJSON
   * @memberof ReqGetIspModuleState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetIspModuleState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetIspModuleState
   * @function getTypeUrl
   * @memberof ReqGetIspModuleState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetIspModuleState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetIspModuleState";
  };

  return ReqGetIspModuleState;
})();

$root.ReqSwitchResolution = (function () {
  /**
   * Properties of a ReqSwitchResolution.
   * @exports IReqSwitchResolution
   * @interface IReqSwitchResolution
   * @property {number|null} [resolutionIndex] ReqSwitchResolution resolutionIndex
   */

  /**
   * Constructs a new ReqSwitchResolution.
   * @exports ReqSwitchResolution
   * @classdesc Represents a ReqSwitchResolution.
   * @implements IReqSwitchResolution
   * @constructor
   * @param {IReqSwitchResolution=} [properties] Properties to set
   */
  function ReqSwitchResolution(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSwitchResolution resolutionIndex.
   * @member {number} resolutionIndex
   * @memberof ReqSwitchResolution
   * @instance
   */
  ReqSwitchResolution.prototype.resolutionIndex = 0;

  /**
   * Creates a new ReqSwitchResolution instance using the specified properties.
   * @function create
   * @memberof ReqSwitchResolution
   * @static
   * @param {IReqSwitchResolution=} [properties] Properties to set
   * @returns {ReqSwitchResolution} ReqSwitchResolution instance
   */
  ReqSwitchResolution.create = function create(properties) {
    return new ReqSwitchResolution(properties);
  };

  /**
   * Encodes the specified ReqSwitchResolution message. Does not implicitly {@link ReqSwitchResolution.verify|verify} messages.
   * @function encode
   * @memberof ReqSwitchResolution
   * @static
   * @param {IReqSwitchResolution} message ReqSwitchResolution message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchResolution.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.resolutionIndex != null &&
      Object.hasOwnProperty.call(message, "resolutionIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.resolutionIndex);
    return writer;
  };

  /**
   * Encodes the specified ReqSwitchResolution message, length delimited. Does not implicitly {@link ReqSwitchResolution.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSwitchResolution
   * @static
   * @param {IReqSwitchResolution} message ReqSwitchResolution message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchResolution.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSwitchResolution message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSwitchResolution
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSwitchResolution} ReqSwitchResolution
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchResolution.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSwitchResolution();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.resolutionIndex = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSwitchResolution message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSwitchResolution
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSwitchResolution} ReqSwitchResolution
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchResolution.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSwitchResolution message.
   * @function verify
   * @memberof ReqSwitchResolution
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSwitchResolution.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.resolutionIndex != null &&
      Object.hasOwnProperty.call(message, "resolutionIndex")
    )
      if (!$util.isInteger(message.resolutionIndex))
        return "resolutionIndex: integer expected";
    return null;
  };

  /**
   * Creates a ReqSwitchResolution message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSwitchResolution
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSwitchResolution} ReqSwitchResolution
   */
  ReqSwitchResolution.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSwitchResolution) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSwitchResolution: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSwitchResolution();
    if (object.resolutionIndex != null)
      message.resolutionIndex = object.resolutionIndex | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSwitchResolution message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSwitchResolution
   * @static
   * @param {ReqSwitchResolution} message ReqSwitchResolution
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSwitchResolution.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.resolutionIndex = 0;
    if (
      message.resolutionIndex != null &&
      Object.hasOwnProperty.call(message, "resolutionIndex")
    )
      object.resolutionIndex = message.resolutionIndex;
    return object;
  };

  /**
   * Converts this ReqSwitchResolution to JSON.
   * @function toJSON
   * @memberof ReqSwitchResolution
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSwitchResolution.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSwitchResolution
   * @function getTypeUrl
   * @memberof ReqSwitchResolution
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSwitchResolution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSwitchResolution";
  };

  return ReqSwitchResolution;
})();

$root.ReqSwitchFrameRate = (function () {
  /**
   * Properties of a ReqSwitchFrameRate.
   * @exports IReqSwitchFrameRate
   * @interface IReqSwitchFrameRate
   * @property {number|null} [fpsIndex] ReqSwitchFrameRate fpsIndex
   */

  /**
   * Constructs a new ReqSwitchFrameRate.
   * @exports ReqSwitchFrameRate
   * @classdesc Represents a ReqSwitchFrameRate.
   * @implements IReqSwitchFrameRate
   * @constructor
   * @param {IReqSwitchFrameRate=} [properties] Properties to set
   */
  function ReqSwitchFrameRate(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSwitchFrameRate fpsIndex.
   * @member {number} fpsIndex
   * @memberof ReqSwitchFrameRate
   * @instance
   */
  ReqSwitchFrameRate.prototype.fpsIndex = 0;

  /**
   * Creates a new ReqSwitchFrameRate instance using the specified properties.
   * @function create
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {IReqSwitchFrameRate=} [properties] Properties to set
   * @returns {ReqSwitchFrameRate} ReqSwitchFrameRate instance
   */
  ReqSwitchFrameRate.create = function create(properties) {
    return new ReqSwitchFrameRate(properties);
  };

  /**
   * Encodes the specified ReqSwitchFrameRate message. Does not implicitly {@link ReqSwitchFrameRate.verify|verify} messages.
   * @function encode
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {IReqSwitchFrameRate} message ReqSwitchFrameRate message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchFrameRate.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.fpsIndex != null &&
      Object.hasOwnProperty.call(message, "fpsIndex")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.fpsIndex);
    return writer;
  };

  /**
   * Encodes the specified ReqSwitchFrameRate message, length delimited. Does not implicitly {@link ReqSwitchFrameRate.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {IReqSwitchFrameRate} message ReqSwitchFrameRate message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchFrameRate.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSwitchFrameRate message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSwitchFrameRate} ReqSwitchFrameRate
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchFrameRate.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSwitchFrameRate();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.fpsIndex = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSwitchFrameRate message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSwitchFrameRate} ReqSwitchFrameRate
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchFrameRate.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSwitchFrameRate message.
   * @function verify
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSwitchFrameRate.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.fpsIndex != null &&
      Object.hasOwnProperty.call(message, "fpsIndex")
    )
      if (!$util.isInteger(message.fpsIndex))
        return "fpsIndex: integer expected";
    return null;
  };

  /**
   * Creates a ReqSwitchFrameRate message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSwitchFrameRate} ReqSwitchFrameRate
   */
  ReqSwitchFrameRate.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSwitchFrameRate) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSwitchFrameRate: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSwitchFrameRate();
    if (object.fpsIndex != null) message.fpsIndex = object.fpsIndex | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSwitchFrameRate message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {ReqSwitchFrameRate} message ReqSwitchFrameRate
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSwitchFrameRate.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.fpsIndex = 0;
    if (
      message.fpsIndex != null &&
      Object.hasOwnProperty.call(message, "fpsIndex")
    )
      object.fpsIndex = message.fpsIndex;
    return object;
  };

  /**
   * Converts this ReqSwitchFrameRate to JSON.
   * @function toJSON
   * @memberof ReqSwitchFrameRate
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSwitchFrameRate.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSwitchFrameRate
   * @function getTypeUrl
   * @memberof ReqSwitchFrameRate
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSwitchFrameRate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSwitchFrameRate";
  };

  return ReqSwitchFrameRate;
})();

$root.ReqSwitchCropRatio = (function () {
  /**
   * Properties of a ReqSwitchCropRatio.
   * @exports IReqSwitchCropRatio
   * @interface IReqSwitchCropRatio
   * @property {number|null} [cropRatio] ReqSwitchCropRatio cropRatio
   */

  /**
   * Constructs a new ReqSwitchCropRatio.
   * @exports ReqSwitchCropRatio
   * @classdesc Represents a ReqSwitchCropRatio.
   * @implements IReqSwitchCropRatio
   * @constructor
   * @param {IReqSwitchCropRatio=} [properties] Properties to set
   */
  function ReqSwitchCropRatio(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSwitchCropRatio cropRatio.
   * @member {number} cropRatio
   * @memberof ReqSwitchCropRatio
   * @instance
   */
  ReqSwitchCropRatio.prototype.cropRatio = 0;

  /**
   * Creates a new ReqSwitchCropRatio instance using the specified properties.
   * @function create
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {IReqSwitchCropRatio=} [properties] Properties to set
   * @returns {ReqSwitchCropRatio} ReqSwitchCropRatio instance
   */
  ReqSwitchCropRatio.create = function create(properties) {
    return new ReqSwitchCropRatio(properties);
  };

  /**
   * Encodes the specified ReqSwitchCropRatio message. Does not implicitly {@link ReqSwitchCropRatio.verify|verify} messages.
   * @function encode
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {IReqSwitchCropRatio} message ReqSwitchCropRatio message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchCropRatio.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.cropRatio != null &&
      Object.hasOwnProperty.call(message, "cropRatio")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cropRatio);
    return writer;
  };

  /**
   * Encodes the specified ReqSwitchCropRatio message, length delimited. Does not implicitly {@link ReqSwitchCropRatio.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {IReqSwitchCropRatio} message ReqSwitchCropRatio message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchCropRatio.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSwitchCropRatio message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSwitchCropRatio} ReqSwitchCropRatio
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchCropRatio.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSwitchCropRatio();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cropRatio = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSwitchCropRatio message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSwitchCropRatio} ReqSwitchCropRatio
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchCropRatio.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSwitchCropRatio message.
   * @function verify
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSwitchCropRatio.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.cropRatio != null &&
      Object.hasOwnProperty.call(message, "cropRatio")
    )
      if (!$util.isInteger(message.cropRatio))
        return "cropRatio: integer expected";
    return null;
  };

  /**
   * Creates a ReqSwitchCropRatio message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSwitchCropRatio} ReqSwitchCropRatio
   */
  ReqSwitchCropRatio.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSwitchCropRatio) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSwitchCropRatio: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSwitchCropRatio();
    if (object.cropRatio != null) message.cropRatio = object.cropRatio | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSwitchCropRatio message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {ReqSwitchCropRatio} message ReqSwitchCropRatio
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSwitchCropRatio.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.cropRatio = 0;
    if (
      message.cropRatio != null &&
      Object.hasOwnProperty.call(message, "cropRatio")
    )
      object.cropRatio = message.cropRatio;
    return object;
  };

  /**
   * Converts this ReqSwitchCropRatio to JSON.
   * @function toJSON
   * @memberof ReqSwitchCropRatio
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSwitchCropRatio.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSwitchCropRatio
   * @function getTypeUrl
   * @memberof ReqSwitchCropRatio
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSwitchCropRatio.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSwitchCropRatio";
  };

  return ReqSwitchCropRatio;
})();

$root.ReqSetPreviewQuality = (function () {
  /**
   * Properties of a ReqSetPreviewQuality.
   * @exports IReqSetPreviewQuality
   * @interface IReqSetPreviewQuality
   * @property {number|null} [level] ReqSetPreviewQuality level
   * @property {number|null} [quality] ReqSetPreviewQuality quality
   */

  /**
   * Constructs a new ReqSetPreviewQuality.
   * @exports ReqSetPreviewQuality
   * @classdesc Represents a ReqSetPreviewQuality.
   * @implements IReqSetPreviewQuality
   * @constructor
   * @param {IReqSetPreviewQuality=} [properties] Properties to set
   */
  function ReqSetPreviewQuality(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetPreviewQuality level.
   * @member {number} level
   * @memberof ReqSetPreviewQuality
   * @instance
   */
  ReqSetPreviewQuality.prototype.level = 0;

  /**
   * ReqSetPreviewQuality quality.
   * @member {number} quality
   * @memberof ReqSetPreviewQuality
   * @instance
   */
  ReqSetPreviewQuality.prototype.quality = 0;

  /**
   * Creates a new ReqSetPreviewQuality instance using the specified properties.
   * @function create
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {IReqSetPreviewQuality=} [properties] Properties to set
   * @returns {ReqSetPreviewQuality} ReqSetPreviewQuality instance
   */
  ReqSetPreviewQuality.create = function create(properties) {
    return new ReqSetPreviewQuality(properties);
  };

  /**
   * Encodes the specified ReqSetPreviewQuality message. Does not implicitly {@link ReqSetPreviewQuality.verify|verify} messages.
   * @function encode
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {IReqSetPreviewQuality} message ReqSetPreviewQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetPreviewQuality.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.level != null && Object.hasOwnProperty.call(message, "level"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.level);
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.quality);
    return writer;
  };

  /**
   * Encodes the specified ReqSetPreviewQuality message, length delimited. Does not implicitly {@link ReqSetPreviewQuality.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {IReqSetPreviewQuality} message ReqSetPreviewQuality message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetPreviewQuality.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetPreviewQuality message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetPreviewQuality} ReqSetPreviewQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetPreviewQuality.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetPreviewQuality();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.level = reader.uint32();
          break;
        }
        case 2: {
          message.quality = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetPreviewQuality message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetPreviewQuality} ReqSetPreviewQuality
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetPreviewQuality.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetPreviewQuality message.
   * @function verify
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetPreviewQuality.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.level != null && Object.hasOwnProperty.call(message, "level"))
      if (!$util.isInteger(message.level)) return "level: integer expected";
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      if (!$util.isInteger(message.quality)) return "quality: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetPreviewQuality message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetPreviewQuality} ReqSetPreviewQuality
   */
  ReqSetPreviewQuality.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetPreviewQuality) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetPreviewQuality: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetPreviewQuality();
    if (object.level != null) message.level = object.level >>> 0;
    if (object.quality != null) message.quality = object.quality >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetPreviewQuality message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {ReqSetPreviewQuality} message ReqSetPreviewQuality
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetPreviewQuality.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.level = 0;
      object.quality = 0;
    }
    if (message.level != null && Object.hasOwnProperty.call(message, "level"))
      object.level = message.level;
    if (
      message.quality != null &&
      Object.hasOwnProperty.call(message, "quality")
    )
      object.quality = message.quality;
    return object;
  };

  /**
   * Converts this ReqSetPreviewQuality to JSON.
   * @function toJSON
   * @memberof ReqSetPreviewQuality
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetPreviewQuality.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetPreviewQuality
   * @function getTypeUrl
   * @memberof ReqSetPreviewQuality
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetPreviewQuality.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetPreviewQuality";
  };

  return ReqSetPreviewQuality;
})();

$root.ReqLensDefog = (function () {
  /**
   * Properties of a ReqLensDefog.
   * @exports IReqLensDefog
   * @interface IReqLensDefog
   * @property {number|null} [state] ReqLensDefog state
   */

  /**
   * Constructs a new ReqLensDefog.
   * @exports ReqLensDefog
   * @classdesc Represents a ReqLensDefog.
   * @implements IReqLensDefog
   * @constructor
   * @param {IReqLensDefog=} [properties] Properties to set
   */
  function ReqLensDefog(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqLensDefog state.
   * @member {number} state
   * @memberof ReqLensDefog
   * @instance
   */
  ReqLensDefog.prototype.state = 0;

  /**
   * Creates a new ReqLensDefog instance using the specified properties.
   * @function create
   * @memberof ReqLensDefog
   * @static
   * @param {IReqLensDefog=} [properties] Properties to set
   * @returns {ReqLensDefog} ReqLensDefog instance
   */
  ReqLensDefog.create = function create(properties) {
    return new ReqLensDefog(properties);
  };

  /**
   * Encodes the specified ReqLensDefog message. Does not implicitly {@link ReqLensDefog.verify|verify} messages.
   * @function encode
   * @memberof ReqLensDefog
   * @static
   * @param {IReqLensDefog} message ReqLensDefog message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqLensDefog.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
    return writer;
  };

  /**
   * Encodes the specified ReqLensDefog message, length delimited. Does not implicitly {@link ReqLensDefog.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqLensDefog
   * @static
   * @param {IReqLensDefog} message ReqLensDefog message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqLensDefog.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqLensDefog message from the specified reader or buffer.
   * @function decode
   * @memberof ReqLensDefog
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqLensDefog} ReqLensDefog
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqLensDefog.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqLensDefog();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.state = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqLensDefog message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqLensDefog
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqLensDefog} ReqLensDefog
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqLensDefog.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqLensDefog message.
   * @function verify
   * @memberof ReqLensDefog
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqLensDefog.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (!$util.isInteger(message.state)) return "state: integer expected";
    return null;
  };

  /**
   * Creates a ReqLensDefog message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqLensDefog
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqLensDefog} ReqLensDefog
   */
  ReqLensDefog.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqLensDefog) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqLensDefog: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqLensDefog();
    if (object.state != null) message.state = object.state | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqLensDefog message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqLensDefog
   * @static
   * @param {ReqLensDefog} message ReqLensDefog
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqLensDefog.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.state = 0;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    return object;
  };

  /**
   * Converts this ReqLensDefog to JSON.
   * @function toJSON
   * @memberof ReqLensDefog
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqLensDefog.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqLensDefog
   * @function getTypeUrl
   * @memberof ReqLensDefog
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqLensDefog.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqLensDefog";
  };

  return ReqLensDefog;
})();

$root.ReqAutoCooling = (function () {
  /**
   * Properties of a ReqAutoCooling.
   * @exports IReqAutoCooling
   * @interface IReqAutoCooling
   * @property {number|null} [state] ReqAutoCooling state
   */

  /**
   * Constructs a new ReqAutoCooling.
   * @exports ReqAutoCooling
   * @classdesc Represents a ReqAutoCooling.
   * @implements IReqAutoCooling
   * @constructor
   * @param {IReqAutoCooling=} [properties] Properties to set
   */
  function ReqAutoCooling(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqAutoCooling state.
   * @member {number} state
   * @memberof ReqAutoCooling
   * @instance
   */
  ReqAutoCooling.prototype.state = 0;

  /**
   * Creates a new ReqAutoCooling instance using the specified properties.
   * @function create
   * @memberof ReqAutoCooling
   * @static
   * @param {IReqAutoCooling=} [properties] Properties to set
   * @returns {ReqAutoCooling} ReqAutoCooling instance
   */
  ReqAutoCooling.create = function create(properties) {
    return new ReqAutoCooling(properties);
  };

  /**
   * Encodes the specified ReqAutoCooling message. Does not implicitly {@link ReqAutoCooling.verify|verify} messages.
   * @function encode
   * @memberof ReqAutoCooling
   * @static
   * @param {IReqAutoCooling} message ReqAutoCooling message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAutoCooling.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
    return writer;
  };

  /**
   * Encodes the specified ReqAutoCooling message, length delimited. Does not implicitly {@link ReqAutoCooling.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqAutoCooling
   * @static
   * @param {IReqAutoCooling} message ReqAutoCooling message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAutoCooling.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqAutoCooling message from the specified reader or buffer.
   * @function decode
   * @memberof ReqAutoCooling
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqAutoCooling} ReqAutoCooling
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAutoCooling.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqAutoCooling();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.state = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqAutoCooling message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqAutoCooling
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqAutoCooling} ReqAutoCooling
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAutoCooling.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqAutoCooling message.
   * @function verify
   * @memberof ReqAutoCooling
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqAutoCooling.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (!$util.isInteger(message.state)) return "state: integer expected";
    return null;
  };

  /**
   * Creates a ReqAutoCooling message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqAutoCooling
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqAutoCooling} ReqAutoCooling
   */
  ReqAutoCooling.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqAutoCooling) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqAutoCooling: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqAutoCooling();
    if (object.state != null) message.state = object.state | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqAutoCooling message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqAutoCooling
   * @static
   * @param {ReqAutoCooling} message ReqAutoCooling
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqAutoCooling.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.state = 0;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    return object;
  };

  /**
   * Converts this ReqAutoCooling to JSON.
   * @function toJSON
   * @memberof ReqAutoCooling
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqAutoCooling.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqAutoCooling
   * @function getTypeUrl
   * @memberof ReqAutoCooling
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqAutoCooling.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqAutoCooling";
  };

  return ReqAutoCooling;
})();

$root.ReqAutoShutdown = (function () {
  /**
   * Properties of a ReqAutoShutdown.
   * @exports IReqAutoShutdown
   * @interface IReqAutoShutdown
   * @property {number|null} [state] ReqAutoShutdown state
   */

  /**
   * Constructs a new ReqAutoShutdown.
   * @exports ReqAutoShutdown
   * @classdesc Represents a ReqAutoShutdown.
   * @implements IReqAutoShutdown
   * @constructor
   * @param {IReqAutoShutdown=} [properties] Properties to set
   */
  function ReqAutoShutdown(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqAutoShutdown state.
   * @member {number} state
   * @memberof ReqAutoShutdown
   * @instance
   */
  ReqAutoShutdown.prototype.state = 0;

  /**
   * Creates a new ReqAutoShutdown instance using the specified properties.
   * @function create
   * @memberof ReqAutoShutdown
   * @static
   * @param {IReqAutoShutdown=} [properties] Properties to set
   * @returns {ReqAutoShutdown} ReqAutoShutdown instance
   */
  ReqAutoShutdown.create = function create(properties) {
    return new ReqAutoShutdown(properties);
  };

  /**
   * Encodes the specified ReqAutoShutdown message. Does not implicitly {@link ReqAutoShutdown.verify|verify} messages.
   * @function encode
   * @memberof ReqAutoShutdown
   * @static
   * @param {IReqAutoShutdown} message ReqAutoShutdown message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAutoShutdown.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
    return writer;
  };

  /**
   * Encodes the specified ReqAutoShutdown message, length delimited. Does not implicitly {@link ReqAutoShutdown.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqAutoShutdown
   * @static
   * @param {IReqAutoShutdown} message ReqAutoShutdown message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAutoShutdown.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqAutoShutdown message from the specified reader or buffer.
   * @function decode
   * @memberof ReqAutoShutdown
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqAutoShutdown} ReqAutoShutdown
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAutoShutdown.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqAutoShutdown();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.state = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqAutoShutdown message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqAutoShutdown
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqAutoShutdown} ReqAutoShutdown
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAutoShutdown.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqAutoShutdown message.
   * @function verify
   * @memberof ReqAutoShutdown
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqAutoShutdown.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (!$util.isInteger(message.state)) return "state: integer expected";
    return null;
  };

  /**
   * Creates a ReqAutoShutdown message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqAutoShutdown
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqAutoShutdown} ReqAutoShutdown
   */
  ReqAutoShutdown.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqAutoShutdown) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqAutoShutdown: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqAutoShutdown();
    if (object.state != null) message.state = object.state | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqAutoShutdown message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqAutoShutdown
   * @static
   * @param {ReqAutoShutdown} message ReqAutoShutdown
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqAutoShutdown.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.state = 0;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    return object;
  };

  /**
   * Converts this ReqAutoShutdown to JSON.
   * @function toJSON
   * @memberof ReqAutoShutdown
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqAutoShutdown.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqAutoShutdown
   * @function getTypeUrl
   * @memberof ReqAutoShutdown
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqAutoShutdown.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqAutoShutdown";
  };

  return ReqAutoShutdown;
})();

$root.ReqSetSN = (function () {
  /**
   * Properties of a ReqSetSN.
   * @exports IReqSetSN
   * @interface IReqSetSN
   * @property {string|null} [sn] ReqSetSN sn
   */

  /**
   * Constructs a new ReqSetSN.
   * @exports ReqSetSN
   * @classdesc Represents a ReqSetSN.
   * @implements IReqSetSN
   * @constructor
   * @param {IReqSetSN=} [properties] Properties to set
   */
  function ReqSetSN(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetSN sn.
   * @member {string} sn
   * @memberof ReqSetSN
   * @instance
   */
  ReqSetSN.prototype.sn = "";

  /**
   * Creates a new ReqSetSN instance using the specified properties.
   * @function create
   * @memberof ReqSetSN
   * @static
   * @param {IReqSetSN=} [properties] Properties to set
   * @returns {ReqSetSN} ReqSetSN instance
   */
  ReqSetSN.create = function create(properties) {
    return new ReqSetSN(properties);
  };

  /**
   * Encodes the specified ReqSetSN message. Does not implicitly {@link ReqSetSN.verify|verify} messages.
   * @function encode
   * @memberof ReqSetSN
   * @static
   * @param {IReqSetSN} message ReqSetSN message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSN.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.sn);
    return writer;
  };

  /**
   * Encodes the specified ReqSetSN message, length delimited. Does not implicitly {@link ReqSetSN.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetSN
   * @static
   * @param {IReqSetSN} message ReqSetSN message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetSN.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetSN message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetSN
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetSN} ReqSetSN
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSN.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetSN();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.sn = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetSN message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetSN
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetSN} ReqSetSN
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetSN.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetSN message.
   * @function verify
   * @memberof ReqSetSN
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetSN.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      if (!$util.isString(message.sn)) return "sn: string expected";
    return null;
  };

  /**
   * Creates a ReqSetSN message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetSN
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetSN} ReqSetSN
   */
  ReqSetSN.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetSN) return object;
    if (!$util.isObject(object)) throw TypeError(".ReqSetSN: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetSN();
    if (object.sn != null) message.sn = String(object.sn);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetSN message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetSN
   * @static
   * @param {ReqSetSN} message ReqSetSN
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetSN.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.sn = "";
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      object.sn = message.sn;
    return object;
  };

  /**
   * Converts this ReqSetSN to JSON.
   * @function toJSON
   * @memberof ReqSetSN
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetSN.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetSN
   * @function getTypeUrl
   * @memberof ReqSetSN
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetSN.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetSN";
  };

  return ReqSetSN;
})();

$root.ReqGetSN = (function () {
  /**
   * Properties of a ReqGetSN.
   * @exports IReqGetSN
   * @interface IReqGetSN
   */

  /**
   * Constructs a new ReqGetSN.
   * @exports ReqGetSN
   * @classdesc Represents a ReqGetSN.
   * @implements IReqGetSN
   * @constructor
   * @param {IReqGetSN=} [properties] Properties to set
   */
  function ReqGetSN(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetSN instance using the specified properties.
   * @function create
   * @memberof ReqGetSN
   * @static
   * @param {IReqGetSN=} [properties] Properties to set
   * @returns {ReqGetSN} ReqGetSN instance
   */
  ReqGetSN.create = function create(properties) {
    return new ReqGetSN(properties);
  };

  /**
   * Encodes the specified ReqGetSN message. Does not implicitly {@link ReqGetSN.verify|verify} messages.
   * @function encode
   * @memberof ReqGetSN
   * @static
   * @param {IReqGetSN} message ReqGetSN message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSN.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetSN message, length delimited. Does not implicitly {@link ReqGetSN.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetSN
   * @static
   * @param {IReqGetSN} message ReqGetSN message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetSN.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetSN message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetSN
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetSN} ReqGetSN
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSN.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetSN();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetSN message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetSN
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetSN} ReqGetSN
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetSN.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetSN message.
   * @function verify
   * @memberof ReqGetSN
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetSN.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetSN message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetSN
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetSN} ReqGetSN
   */
  ReqGetSN.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetSN) return object;
    return new $root.ReqGetSN();
  };

  /**
   * Creates a plain object from a ReqGetSN message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetSN
   * @static
   * @param {ReqGetSN} message ReqGetSN
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetSN.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetSN to JSON.
   * @function toJSON
   * @memberof ReqGetSN
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetSN.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetSN
   * @function getTypeUrl
   * @memberof ReqGetSN
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetSN.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetSN";
  };

  return ReqGetSN;
})();

$root.ReqGetNTCValue = (function () {
  /**
   * Properties of a ReqGetNTCValue.
   * @exports IReqGetNTCValue
   * @interface IReqGetNTCValue
   */

  /**
   * Constructs a new ReqGetNTCValue.
   * @exports ReqGetNTCValue
   * @classdesc Represents a ReqGetNTCValue.
   * @implements IReqGetNTCValue
   * @constructor
   * @param {IReqGetNTCValue=} [properties] Properties to set
   */
  function ReqGetNTCValue(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetNTCValue instance using the specified properties.
   * @function create
   * @memberof ReqGetNTCValue
   * @static
   * @param {IReqGetNTCValue=} [properties] Properties to set
   * @returns {ReqGetNTCValue} ReqGetNTCValue instance
   */
  ReqGetNTCValue.create = function create(properties) {
    return new ReqGetNTCValue(properties);
  };

  /**
   * Encodes the specified ReqGetNTCValue message. Does not implicitly {@link ReqGetNTCValue.verify|verify} messages.
   * @function encode
   * @memberof ReqGetNTCValue
   * @static
   * @param {IReqGetNTCValue} message ReqGetNTCValue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetNTCValue.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetNTCValue message, length delimited. Does not implicitly {@link ReqGetNTCValue.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetNTCValue
   * @static
   * @param {IReqGetNTCValue} message ReqGetNTCValue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetNTCValue.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetNTCValue message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetNTCValue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetNTCValue} ReqGetNTCValue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetNTCValue.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetNTCValue();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetNTCValue message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetNTCValue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetNTCValue} ReqGetNTCValue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetNTCValue.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetNTCValue message.
   * @function verify
   * @memberof ReqGetNTCValue
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetNTCValue.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetNTCValue message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetNTCValue
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetNTCValue} ReqGetNTCValue
   */
  ReqGetNTCValue.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetNTCValue) return object;
    return new $root.ReqGetNTCValue();
  };

  /**
   * Creates a plain object from a ReqGetNTCValue message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetNTCValue
   * @static
   * @param {ReqGetNTCValue} message ReqGetNTCValue
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetNTCValue.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetNTCValue to JSON.
   * @function toJSON
   * @memberof ReqGetNTCValue
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetNTCValue.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetNTCValue
   * @function getTypeUrl
   * @memberof ReqGetNTCValue
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetNTCValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetNTCValue";
  };

  return ReqGetNTCValue;
})();

$root.ReqGetEMMCValue = (function () {
  /**
   * Properties of a ReqGetEMMCValue.
   * @exports IReqGetEMMCValue
   * @interface IReqGetEMMCValue
   */

  /**
   * Constructs a new ReqGetEMMCValue.
   * @exports ReqGetEMMCValue
   * @classdesc Represents a ReqGetEMMCValue.
   * @implements IReqGetEMMCValue
   * @constructor
   * @param {IReqGetEMMCValue=} [properties] Properties to set
   */
  function ReqGetEMMCValue(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetEMMCValue instance using the specified properties.
   * @function create
   * @memberof ReqGetEMMCValue
   * @static
   * @param {IReqGetEMMCValue=} [properties] Properties to set
   * @returns {ReqGetEMMCValue} ReqGetEMMCValue instance
   */
  ReqGetEMMCValue.create = function create(properties) {
    return new ReqGetEMMCValue(properties);
  };

  /**
   * Encodes the specified ReqGetEMMCValue message. Does not implicitly {@link ReqGetEMMCValue.verify|verify} messages.
   * @function encode
   * @memberof ReqGetEMMCValue
   * @static
   * @param {IReqGetEMMCValue} message ReqGetEMMCValue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetEMMCValue.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetEMMCValue message, length delimited. Does not implicitly {@link ReqGetEMMCValue.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetEMMCValue
   * @static
   * @param {IReqGetEMMCValue} message ReqGetEMMCValue message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetEMMCValue.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetEMMCValue message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetEMMCValue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetEMMCValue} ReqGetEMMCValue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetEMMCValue.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetEMMCValue();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetEMMCValue message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetEMMCValue
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetEMMCValue} ReqGetEMMCValue
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetEMMCValue.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetEMMCValue message.
   * @function verify
   * @memberof ReqGetEMMCValue
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetEMMCValue.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetEMMCValue message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetEMMCValue
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetEMMCValue} ReqGetEMMCValue
   */
  ReqGetEMMCValue.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetEMMCValue) return object;
    return new $root.ReqGetEMMCValue();
  };

  /**
   * Creates a plain object from a ReqGetEMMCValue message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetEMMCValue
   * @static
   * @param {ReqGetEMMCValue} message ReqGetEMMCValue
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetEMMCValue.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetEMMCValue to JSON.
   * @function toJSON
   * @memberof ReqGetEMMCValue
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetEMMCValue.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetEMMCValue
   * @function getTypeUrl
   * @memberof ReqGetEMMCValue
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetEMMCValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetEMMCValue";
  };

  return ReqGetEMMCValue;
})();

$root.ReqSetAUDSTART = (function () {
  /**
   * Properties of a ReqSetAUDSTART.
   * @exports IReqSetAUDSTART
   * @interface IReqSetAUDSTART
   */

  /**
   * Constructs a new ReqSetAUDSTART.
   * @exports ReqSetAUDSTART
   * @classdesc Represents a ReqSetAUDSTART.
   * @implements IReqSetAUDSTART
   * @constructor
   * @param {IReqSetAUDSTART=} [properties] Properties to set
   */
  function ReqSetAUDSTART(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqSetAUDSTART instance using the specified properties.
   * @function create
   * @memberof ReqSetAUDSTART
   * @static
   * @param {IReqSetAUDSTART=} [properties] Properties to set
   * @returns {ReqSetAUDSTART} ReqSetAUDSTART instance
   */
  ReqSetAUDSTART.create = function create(properties) {
    return new ReqSetAUDSTART(properties);
  };

  /**
   * Encodes the specified ReqSetAUDSTART message. Does not implicitly {@link ReqSetAUDSTART.verify|verify} messages.
   * @function encode
   * @memberof ReqSetAUDSTART
   * @static
   * @param {IReqSetAUDSTART} message ReqSetAUDSTART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDSTART.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqSetAUDSTART message, length delimited. Does not implicitly {@link ReqSetAUDSTART.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetAUDSTART
   * @static
   * @param {IReqSetAUDSTART} message ReqSetAUDSTART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDSTART.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetAUDSTART message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetAUDSTART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetAUDSTART} ReqSetAUDSTART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDSTART.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetAUDSTART();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetAUDSTART message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetAUDSTART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetAUDSTART} ReqSetAUDSTART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDSTART.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetAUDSTART message.
   * @function verify
   * @memberof ReqSetAUDSTART
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetAUDSTART.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqSetAUDSTART message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetAUDSTART
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetAUDSTART} ReqSetAUDSTART
   */
  ReqSetAUDSTART.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetAUDSTART) return object;
    return new $root.ReqSetAUDSTART();
  };

  /**
   * Creates a plain object from a ReqSetAUDSTART message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetAUDSTART
   * @static
   * @param {ReqSetAUDSTART} message ReqSetAUDSTART
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetAUDSTART.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqSetAUDSTART to JSON.
   * @function toJSON
   * @memberof ReqSetAUDSTART
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetAUDSTART.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetAUDSTART
   * @function getTypeUrl
   * @memberof ReqSetAUDSTART
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetAUDSTART.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetAUDSTART";
  };

  return ReqSetAUDSTART;
})();

$root.ReqSetAUDSTOP = (function () {
  /**
   * Properties of a ReqSetAUDSTOP.
   * @exports IReqSetAUDSTOP
   * @interface IReqSetAUDSTOP
   */

  /**
   * Constructs a new ReqSetAUDSTOP.
   * @exports ReqSetAUDSTOP
   * @classdesc Represents a ReqSetAUDSTOP.
   * @implements IReqSetAUDSTOP
   * @constructor
   * @param {IReqSetAUDSTOP=} [properties] Properties to set
   */
  function ReqSetAUDSTOP(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqSetAUDSTOP instance using the specified properties.
   * @function create
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {IReqSetAUDSTOP=} [properties] Properties to set
   * @returns {ReqSetAUDSTOP} ReqSetAUDSTOP instance
   */
  ReqSetAUDSTOP.create = function create(properties) {
    return new ReqSetAUDSTOP(properties);
  };

  /**
   * Encodes the specified ReqSetAUDSTOP message. Does not implicitly {@link ReqSetAUDSTOP.verify|verify} messages.
   * @function encode
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {IReqSetAUDSTOP} message ReqSetAUDSTOP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDSTOP.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqSetAUDSTOP message, length delimited. Does not implicitly {@link ReqSetAUDSTOP.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {IReqSetAUDSTOP} message ReqSetAUDSTOP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDSTOP.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetAUDSTOP message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetAUDSTOP} ReqSetAUDSTOP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDSTOP.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetAUDSTOP();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetAUDSTOP message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetAUDSTOP} ReqSetAUDSTOP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDSTOP.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetAUDSTOP message.
   * @function verify
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetAUDSTOP.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqSetAUDSTOP message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetAUDSTOP} ReqSetAUDSTOP
   */
  ReqSetAUDSTOP.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetAUDSTOP) return object;
    return new $root.ReqSetAUDSTOP();
  };

  /**
   * Creates a plain object from a ReqSetAUDSTOP message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {ReqSetAUDSTOP} message ReqSetAUDSTOP
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetAUDSTOP.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqSetAUDSTOP to JSON.
   * @function toJSON
   * @memberof ReqSetAUDSTOP
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetAUDSTOP.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetAUDSTOP
   * @function getTypeUrl
   * @memberof ReqSetAUDSTOP
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetAUDSTOP.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetAUDSTOP";
  };

  return ReqSetAUDSTOP;
})();

$root.ReqSetAUDPLAYSTART = (function () {
  /**
   * Properties of a ReqSetAUDPLAYSTART.
   * @exports IReqSetAUDPLAYSTART
   * @interface IReqSetAUDPLAYSTART
   */

  /**
   * Constructs a new ReqSetAUDPLAYSTART.
   * @exports ReqSetAUDPLAYSTART
   * @classdesc Represents a ReqSetAUDPLAYSTART.
   * @implements IReqSetAUDPLAYSTART
   * @constructor
   * @param {IReqSetAUDPLAYSTART=} [properties] Properties to set
   */
  function ReqSetAUDPLAYSTART(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqSetAUDPLAYSTART instance using the specified properties.
   * @function create
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {IReqSetAUDPLAYSTART=} [properties] Properties to set
   * @returns {ReqSetAUDPLAYSTART} ReqSetAUDPLAYSTART instance
   */
  ReqSetAUDPLAYSTART.create = function create(properties) {
    return new ReqSetAUDPLAYSTART(properties);
  };

  /**
   * Encodes the specified ReqSetAUDPLAYSTART message. Does not implicitly {@link ReqSetAUDPLAYSTART.verify|verify} messages.
   * @function encode
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {IReqSetAUDPLAYSTART} message ReqSetAUDPLAYSTART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDPLAYSTART.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqSetAUDPLAYSTART message, length delimited. Does not implicitly {@link ReqSetAUDPLAYSTART.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {IReqSetAUDPLAYSTART} message ReqSetAUDPLAYSTART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDPLAYSTART.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetAUDPLAYSTART message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetAUDPLAYSTART} ReqSetAUDPLAYSTART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDPLAYSTART.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetAUDPLAYSTART();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetAUDPLAYSTART message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetAUDPLAYSTART} ReqSetAUDPLAYSTART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDPLAYSTART.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetAUDPLAYSTART message.
   * @function verify
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetAUDPLAYSTART.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqSetAUDPLAYSTART message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetAUDPLAYSTART} ReqSetAUDPLAYSTART
   */
  ReqSetAUDPLAYSTART.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetAUDPLAYSTART) return object;
    return new $root.ReqSetAUDPLAYSTART();
  };

  /**
   * Creates a plain object from a ReqSetAUDPLAYSTART message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {ReqSetAUDPLAYSTART} message ReqSetAUDPLAYSTART
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetAUDPLAYSTART.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqSetAUDPLAYSTART to JSON.
   * @function toJSON
   * @memberof ReqSetAUDPLAYSTART
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetAUDPLAYSTART.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetAUDPLAYSTART
   * @function getTypeUrl
   * @memberof ReqSetAUDPLAYSTART
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetAUDPLAYSTART.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetAUDPLAYSTART";
  };

  return ReqSetAUDPLAYSTART;
})();

$root.ReqSetAUDPLAYSTOP = (function () {
  /**
   * Properties of a ReqSetAUDPLAYSTOP.
   * @exports IReqSetAUDPLAYSTOP
   * @interface IReqSetAUDPLAYSTOP
   */

  /**
   * Constructs a new ReqSetAUDPLAYSTOP.
   * @exports ReqSetAUDPLAYSTOP
   * @classdesc Represents a ReqSetAUDPLAYSTOP.
   * @implements IReqSetAUDPLAYSTOP
   * @constructor
   * @param {IReqSetAUDPLAYSTOP=} [properties] Properties to set
   */
  function ReqSetAUDPLAYSTOP(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqSetAUDPLAYSTOP instance using the specified properties.
   * @function create
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {IReqSetAUDPLAYSTOP=} [properties] Properties to set
   * @returns {ReqSetAUDPLAYSTOP} ReqSetAUDPLAYSTOP instance
   */
  ReqSetAUDPLAYSTOP.create = function create(properties) {
    return new ReqSetAUDPLAYSTOP(properties);
  };

  /**
   * Encodes the specified ReqSetAUDPLAYSTOP message. Does not implicitly {@link ReqSetAUDPLAYSTOP.verify|verify} messages.
   * @function encode
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {IReqSetAUDPLAYSTOP} message ReqSetAUDPLAYSTOP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDPLAYSTOP.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqSetAUDPLAYSTOP message, length delimited. Does not implicitly {@link ReqSetAUDPLAYSTOP.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {IReqSetAUDPLAYSTOP} message ReqSetAUDPLAYSTOP message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetAUDPLAYSTOP.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetAUDPLAYSTOP message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetAUDPLAYSTOP} ReqSetAUDPLAYSTOP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDPLAYSTOP.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetAUDPLAYSTOP();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetAUDPLAYSTOP message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetAUDPLAYSTOP} ReqSetAUDPLAYSTOP
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetAUDPLAYSTOP.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetAUDPLAYSTOP message.
   * @function verify
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetAUDPLAYSTOP.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqSetAUDPLAYSTOP message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetAUDPLAYSTOP} ReqSetAUDPLAYSTOP
   */
  ReqSetAUDPLAYSTOP.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetAUDPLAYSTOP) return object;
    return new $root.ReqSetAUDPLAYSTOP();
  };

  /**
   * Creates a plain object from a ReqSetAUDPLAYSTOP message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {ReqSetAUDPLAYSTOP} message ReqSetAUDPLAYSTOP
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetAUDPLAYSTOP.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqSetAUDPLAYSTOP to JSON.
   * @function toJSON
   * @memberof ReqSetAUDPLAYSTOP
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetAUDPLAYSTOP.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetAUDPLAYSTOP
   * @function getTypeUrl
   * @memberof ReqSetAUDPLAYSTOP
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetAUDPLAYSTOP.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetAUDPLAYSTOP";
  };

  return ReqSetAUDPLAYSTOP;
})();

$root.ReqSetFatigueTestMode = (function () {
  /**
   * Properties of a ReqSetFatigueTestMode.
   * @exports IReqSetFatigueTestMode
   * @interface IReqSetFatigueTestMode
   * @property {number|null} [mode] ReqSetFatigueTestMode mode
   */

  /**
   * Constructs a new ReqSetFatigueTestMode.
   * @exports ReqSetFatigueTestMode
   * @classdesc Represents a ReqSetFatigueTestMode.
   * @implements IReqSetFatigueTestMode
   * @constructor
   * @param {IReqSetFatigueTestMode=} [properties] Properties to set
   */
  function ReqSetFatigueTestMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetFatigueTestMode mode.
   * @member {number} mode
   * @memberof ReqSetFatigueTestMode
   * @instance
   */
  ReqSetFatigueTestMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetFatigueTestMode instance using the specified properties.
   * @function create
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {IReqSetFatigueTestMode=} [properties] Properties to set
   * @returns {ReqSetFatigueTestMode} ReqSetFatigueTestMode instance
   */
  ReqSetFatigueTestMode.create = function create(properties) {
    return new ReqSetFatigueTestMode(properties);
  };

  /**
   * Encodes the specified ReqSetFatigueTestMode message. Does not implicitly {@link ReqSetFatigueTestMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {IReqSetFatigueTestMode} message ReqSetFatigueTestMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetFatigueTestMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetFatigueTestMode message, length delimited. Does not implicitly {@link ReqSetFatigueTestMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {IReqSetFatigueTestMode} message ReqSetFatigueTestMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetFatigueTestMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetFatigueTestMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetFatigueTestMode} ReqSetFatigueTestMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetFatigueTestMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetFatigueTestMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetFatigueTestMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetFatigueTestMode} ReqSetFatigueTestMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetFatigueTestMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetFatigueTestMode message.
   * @function verify
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetFatigueTestMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetFatigueTestMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetFatigueTestMode} ReqSetFatigueTestMode
   */
  ReqSetFatigueTestMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetFatigueTestMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetFatigueTestMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetFatigueTestMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetFatigueTestMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {ReqSetFatigueTestMode} message ReqSetFatigueTestMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetFatigueTestMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetFatigueTestMode to JSON.
   * @function toJSON
   * @memberof ReqSetFatigueTestMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetFatigueTestMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetFatigueTestMode
   * @function getTypeUrl
   * @memberof ReqSetFatigueTestMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetFatigueTestMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetFatigueTestMode";
  };

  return ReqSetFatigueTestMode;
})();

$root.ReqSetRgb = (function () {
  /**
   * Properties of a ReqSetRgb.
   * @exports IReqSetRgb
   * @interface IReqSetRgb
   */

  /**
   * Constructs a new ReqSetRgb.
   * @exports ReqSetRgb
   * @classdesc Represents a ReqSetRgb.
   * @implements IReqSetRgb
   * @constructor
   * @param {IReqSetRgb=} [properties] Properties to set
   */
  function ReqSetRgb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqSetRgb instance using the specified properties.
   * @function create
   * @memberof ReqSetRgb
   * @static
   * @param {IReqSetRgb=} [properties] Properties to set
   * @returns {ReqSetRgb} ReqSetRgb instance
   */
  ReqSetRgb.create = function create(properties) {
    return new ReqSetRgb(properties);
  };

  /**
   * Encodes the specified ReqSetRgb message. Does not implicitly {@link ReqSetRgb.verify|verify} messages.
   * @function encode
   * @memberof ReqSetRgb
   * @static
   * @param {IReqSetRgb} message ReqSetRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetRgb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqSetRgb message, length delimited. Does not implicitly {@link ReqSetRgb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetRgb
   * @static
   * @param {IReqSetRgb} message ReqSetRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetRgb.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetRgb message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetRgb} ReqSetRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetRgb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetRgb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetRgb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetRgb} ReqSetRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetRgb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetRgb message.
   * @function verify
   * @memberof ReqSetRgb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetRgb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqSetRgb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetRgb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetRgb} ReqSetRgb
   */
  ReqSetRgb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetRgb) return object;
    return new $root.ReqSetRgb();
  };

  /**
   * Creates a plain object from a ReqSetRgb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetRgb
   * @static
   * @param {ReqSetRgb} message ReqSetRgb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetRgb.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqSetRgb to JSON.
   * @function toJSON
   * @memberof ReqSetRgb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetRgb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetRgb
   * @function getTypeUrl
   * @memberof ReqSetRgb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetRgb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetRgb";
  };

  return ReqSetRgb;
})();

$root.ReqGetELE = (function () {
  /**
   * Properties of a ReqGetELE.
   * @exports IReqGetELE
   * @interface IReqGetELE
   */

  /**
   * Constructs a new ReqGetELE.
   * @exports ReqGetELE
   * @classdesc Represents a ReqGetELE.
   * @implements IReqGetELE
   * @constructor
   * @param {IReqGetELE=} [properties] Properties to set
   */
  function ReqGetELE(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetELE instance using the specified properties.
   * @function create
   * @memberof ReqGetELE
   * @static
   * @param {IReqGetELE=} [properties] Properties to set
   * @returns {ReqGetELE} ReqGetELE instance
   */
  ReqGetELE.create = function create(properties) {
    return new ReqGetELE(properties);
  };

  /**
   * Encodes the specified ReqGetELE message. Does not implicitly {@link ReqGetELE.verify|verify} messages.
   * @function encode
   * @memberof ReqGetELE
   * @static
   * @param {IReqGetELE} message ReqGetELE message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetELE.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetELE message, length delimited. Does not implicitly {@link ReqGetELE.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetELE
   * @static
   * @param {IReqGetELE} message ReqGetELE message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetELE.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetELE message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetELE
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetELE} ReqGetELE
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetELE.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetELE();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetELE message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetELE
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetELE} ReqGetELE
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetELE.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetELE message.
   * @function verify
   * @memberof ReqGetELE
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetELE.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetELE message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetELE
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetELE} ReqGetELE
   */
  ReqGetELE.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetELE) return object;
    return new $root.ReqGetELE();
  };

  /**
   * Creates a plain object from a ReqGetELE message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetELE
   * @static
   * @param {ReqGetELE} message ReqGetELE
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetELE.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetELE to JSON.
   * @function toJSON
   * @memberof ReqGetELE
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetELE.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetELE
   * @function getTypeUrl
   * @memberof ReqGetELE
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetELE.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetELE";
  };

  return ReqGetELE;
})();

$root.ReqGetFactoryTestState = (function () {
  /**
   * Properties of a ReqGetFactoryTestState.
   * @exports IReqGetFactoryTestState
   * @interface IReqGetFactoryTestState
   */

  /**
   * Constructs a new ReqGetFactoryTestState.
   * @exports ReqGetFactoryTestState
   * @classdesc Represents a ReqGetFactoryTestState.
   * @implements IReqGetFactoryTestState
   * @constructor
   * @param {IReqGetFactoryTestState=} [properties] Properties to set
   */
  function ReqGetFactoryTestState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetFactoryTestState instance using the specified properties.
   * @function create
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {IReqGetFactoryTestState=} [properties] Properties to set
   * @returns {ReqGetFactoryTestState} ReqGetFactoryTestState instance
   */
  ReqGetFactoryTestState.create = function create(properties) {
    return new ReqGetFactoryTestState(properties);
  };

  /**
   * Encodes the specified ReqGetFactoryTestState message. Does not implicitly {@link ReqGetFactoryTestState.verify|verify} messages.
   * @function encode
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {IReqGetFactoryTestState} message ReqGetFactoryTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetFactoryTestState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetFactoryTestState message, length delimited. Does not implicitly {@link ReqGetFactoryTestState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {IReqGetFactoryTestState} message ReqGetFactoryTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetFactoryTestState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetFactoryTestState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetFactoryTestState} ReqGetFactoryTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetFactoryTestState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetFactoryTestState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetFactoryTestState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetFactoryTestState} ReqGetFactoryTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetFactoryTestState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetFactoryTestState message.
   * @function verify
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetFactoryTestState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetFactoryTestState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetFactoryTestState} ReqGetFactoryTestState
   */
  ReqGetFactoryTestState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetFactoryTestState) return object;
    return new $root.ReqGetFactoryTestState();
  };

  /**
   * Creates a plain object from a ReqGetFactoryTestState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {ReqGetFactoryTestState} message ReqGetFactoryTestState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetFactoryTestState.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetFactoryTestState to JSON.
   * @function toJSON
   * @memberof ReqGetFactoryTestState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetFactoryTestState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetFactoryTestState
   * @function getTypeUrl
   * @memberof ReqGetFactoryTestState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetFactoryTestState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetFactoryTestState";
  };

  return ReqGetFactoryTestState;
})();

$root.ReqSetWiFiStartUpMode = (function () {
  /**
   * Properties of a ReqSetWiFiStartUpMode.
   * @exports IReqSetWiFiStartUpMode
   * @interface IReqSetWiFiStartUpMode
   * @property {number|null} [mode] ReqSetWiFiStartUpMode mode
   */

  /**
   * Constructs a new ReqSetWiFiStartUpMode.
   * @exports ReqSetWiFiStartUpMode
   * @classdesc Represents a ReqSetWiFiStartUpMode.
   * @implements IReqSetWiFiStartUpMode
   * @constructor
   * @param {IReqSetWiFiStartUpMode=} [properties] Properties to set
   */
  function ReqSetWiFiStartUpMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetWiFiStartUpMode mode.
   * @member {number} mode
   * @memberof ReqSetWiFiStartUpMode
   * @instance
   */
  ReqSetWiFiStartUpMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetWiFiStartUpMode instance using the specified properties.
   * @function create
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {IReqSetWiFiStartUpMode=} [properties] Properties to set
   * @returns {ReqSetWiFiStartUpMode} ReqSetWiFiStartUpMode instance
   */
  ReqSetWiFiStartUpMode.create = function create(properties) {
    return new ReqSetWiFiStartUpMode(properties);
  };

  /**
   * Encodes the specified ReqSetWiFiStartUpMode message. Does not implicitly {@link ReqSetWiFiStartUpMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {IReqSetWiFiStartUpMode} message ReqSetWiFiStartUpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWiFiStartUpMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetWiFiStartUpMode message, length delimited. Does not implicitly {@link ReqSetWiFiStartUpMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {IReqSetWiFiStartUpMode} message ReqSetWiFiStartUpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetWiFiStartUpMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetWiFiStartUpMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetWiFiStartUpMode} ReqSetWiFiStartUpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWiFiStartUpMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetWiFiStartUpMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetWiFiStartUpMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetWiFiStartUpMode} ReqSetWiFiStartUpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetWiFiStartUpMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetWiFiStartUpMode message.
   * @function verify
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetWiFiStartUpMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetWiFiStartUpMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetWiFiStartUpMode} ReqSetWiFiStartUpMode
   */
  ReqSetWiFiStartUpMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetWiFiStartUpMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetWiFiStartUpMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetWiFiStartUpMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetWiFiStartUpMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {ReqSetWiFiStartUpMode} message ReqSetWiFiStartUpMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetWiFiStartUpMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetWiFiStartUpMode to JSON.
   * @function toJSON
   * @memberof ReqSetWiFiStartUpMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetWiFiStartUpMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetWiFiStartUpMode
   * @function getTypeUrl
   * @memberof ReqSetWiFiStartUpMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetWiFiStartUpMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetWiFiStartUpMode";
  };

  return ReqSetWiFiStartUpMode;
})();

$root.ResGetFactoryTestState = (function () {
  /**
   * Properties of a ResGetFactoryTestState.
   * @exports IResGetFactoryTestState
   * @interface IResGetFactoryTestState
   * @property {string|null} [sn] ResGetFactoryTestState sn
   * @property {number|null} [fatigueTestMode] ResGetFactoryTestState fatigueTestMode
   * @property {boolean|null} [wifiStartUp] ResGetFactoryTestState wifiStartUp
   * @property {string|null} [macAddress] ResGetFactoryTestState macAddress
   */

  /**
   * Constructs a new ResGetFactoryTestState.
   * @exports ResGetFactoryTestState
   * @classdesc Represents a ResGetFactoryTestState.
   * @implements IResGetFactoryTestState
   * @constructor
   * @param {IResGetFactoryTestState=} [properties] Properties to set
   */
  function ResGetFactoryTestState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetFactoryTestState sn.
   * @member {string} sn
   * @memberof ResGetFactoryTestState
   * @instance
   */
  ResGetFactoryTestState.prototype.sn = "";

  /**
   * ResGetFactoryTestState fatigueTestMode.
   * @member {number} fatigueTestMode
   * @memberof ResGetFactoryTestState
   * @instance
   */
  ResGetFactoryTestState.prototype.fatigueTestMode = 0;

  /**
   * ResGetFactoryTestState wifiStartUp.
   * @member {boolean} wifiStartUp
   * @memberof ResGetFactoryTestState
   * @instance
   */
  ResGetFactoryTestState.prototype.wifiStartUp = false;

  /**
   * ResGetFactoryTestState macAddress.
   * @member {string} macAddress
   * @memberof ResGetFactoryTestState
   * @instance
   */
  ResGetFactoryTestState.prototype.macAddress = "";

  /**
   * Creates a new ResGetFactoryTestState instance using the specified properties.
   * @function create
   * @memberof ResGetFactoryTestState
   * @static
   * @param {IResGetFactoryTestState=} [properties] Properties to set
   * @returns {ResGetFactoryTestState} ResGetFactoryTestState instance
   */
  ResGetFactoryTestState.create = function create(properties) {
    return new ResGetFactoryTestState(properties);
  };

  /**
   * Encodes the specified ResGetFactoryTestState message. Does not implicitly {@link ResGetFactoryTestState.verify|verify} messages.
   * @function encode
   * @memberof ResGetFactoryTestState
   * @static
   * @param {IResGetFactoryTestState} message ResGetFactoryTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetFactoryTestState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.sn);
    if (
      message.fatigueTestMode != null &&
      Object.hasOwnProperty.call(message, "fatigueTestMode")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.fatigueTestMode);
    if (
      message.wifiStartUp != null &&
      Object.hasOwnProperty.call(message, "wifiStartUp")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.wifiStartUp);
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.macAddress);
    return writer;
  };

  /**
   * Encodes the specified ResGetFactoryTestState message, length delimited. Does not implicitly {@link ResGetFactoryTestState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetFactoryTestState
   * @static
   * @param {IResGetFactoryTestState} message ResGetFactoryTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetFactoryTestState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetFactoryTestState message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetFactoryTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetFactoryTestState} ResGetFactoryTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetFactoryTestState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetFactoryTestState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.sn = reader.string();
          break;
        }
        case 2: {
          message.fatigueTestMode = reader.int32();
          break;
        }
        case 3: {
          message.wifiStartUp = reader.bool();
          break;
        }
        case 4: {
          message.macAddress = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetFactoryTestState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetFactoryTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetFactoryTestState} ResGetFactoryTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetFactoryTestState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetFactoryTestState message.
   * @function verify
   * @memberof ResGetFactoryTestState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetFactoryTestState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      if (!$util.isString(message.sn)) return "sn: string expected";
    if (
      message.fatigueTestMode != null &&
      Object.hasOwnProperty.call(message, "fatigueTestMode")
    )
      if (!$util.isInteger(message.fatigueTestMode))
        return "fatigueTestMode: integer expected";
    if (
      message.wifiStartUp != null &&
      Object.hasOwnProperty.call(message, "wifiStartUp")
    )
      if (typeof message.wifiStartUp !== "boolean")
        return "wifiStartUp: boolean expected";
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      if (!$util.isString(message.macAddress))
        return "macAddress: string expected";
    return null;
  };

  /**
   * Creates a ResGetFactoryTestState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetFactoryTestState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetFactoryTestState} ResGetFactoryTestState
   */
  ResGetFactoryTestState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetFactoryTestState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetFactoryTestState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetFactoryTestState();
    if (object.sn != null) message.sn = String(object.sn);
    if (object.fatigueTestMode != null)
      message.fatigueTestMode = object.fatigueTestMode | 0;
    if (object.wifiStartUp != null)
      message.wifiStartUp = Boolean(object.wifiStartUp);
    if (object.macAddress != null)
      message.macAddress = String(object.macAddress);
    return message;
  };

  /**
   * Creates a plain object from a ResGetFactoryTestState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetFactoryTestState
   * @static
   * @param {ResGetFactoryTestState} message ResGetFactoryTestState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetFactoryTestState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.sn = "";
      object.fatigueTestMode = 0;
      object.wifiStartUp = false;
      object.macAddress = "";
    }
    if (message.sn != null && Object.hasOwnProperty.call(message, "sn"))
      object.sn = message.sn;
    if (
      message.fatigueTestMode != null &&
      Object.hasOwnProperty.call(message, "fatigueTestMode")
    )
      object.fatigueTestMode = message.fatigueTestMode;
    if (
      message.wifiStartUp != null &&
      Object.hasOwnProperty.call(message, "wifiStartUp")
    )
      object.wifiStartUp = message.wifiStartUp;
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      object.macAddress = message.macAddress;
    return object;
  };

  /**
   * Converts this ResGetFactoryTestState to JSON.
   * @function toJSON
   * @memberof ResGetFactoryTestState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetFactoryTestState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetFactoryTestState
   * @function getTypeUrl
   * @memberof ResGetFactoryTestState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetFactoryTestState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetFactoryTestState";
  };

  return ResGetFactoryTestState;
})();

$root.FatigueTestIrcutParam = (function () {
  /**
   * Properties of a FatigueTestIrcutParam.
   * @exports IFatigueTestIrcutParam
   * @interface IFatigueTestIrcutParam
   * @property {boolean|null} [enable] FatigueTestIrcutParam enable
   * @property {number|null} [interval] FatigueTestIrcutParam interval
   * @property {number|null} [time] FatigueTestIrcutParam time
   */

  /**
   * Constructs a new FatigueTestIrcutParam.
   * @exports FatigueTestIrcutParam
   * @classdesc Represents a FatigueTestIrcutParam.
   * @implements IFatigueTestIrcutParam
   * @constructor
   * @param {IFatigueTestIrcutParam=} [properties] Properties to set
   */
  function FatigueTestIrcutParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FatigueTestIrcutParam enable.
   * @member {boolean} enable
   * @memberof FatigueTestIrcutParam
   * @instance
   */
  FatigueTestIrcutParam.prototype.enable = false;

  /**
   * FatigueTestIrcutParam interval.
   * @member {number} interval
   * @memberof FatigueTestIrcutParam
   * @instance
   */
  FatigueTestIrcutParam.prototype.interval = 0;

  /**
   * FatigueTestIrcutParam time.
   * @member {number} time
   * @memberof FatigueTestIrcutParam
   * @instance
   */
  FatigueTestIrcutParam.prototype.time = 0;

  /**
   * Creates a new FatigueTestIrcutParam instance using the specified properties.
   * @function create
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {IFatigueTestIrcutParam=} [properties] Properties to set
   * @returns {FatigueTestIrcutParam} FatigueTestIrcutParam instance
   */
  FatigueTestIrcutParam.create = function create(properties) {
    return new FatigueTestIrcutParam(properties);
  };

  /**
   * Encodes the specified FatigueTestIrcutParam message. Does not implicitly {@link FatigueTestIrcutParam.verify|verify} messages.
   * @function encode
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {IFatigueTestIrcutParam} message FatigueTestIrcutParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestIrcutParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.interval);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.time);
    return writer;
  };

  /**
   * Encodes the specified FatigueTestIrcutParam message, length delimited. Does not implicitly {@link FatigueTestIrcutParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {IFatigueTestIrcutParam} message FatigueTestIrcutParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestIrcutParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FatigueTestIrcutParam message from the specified reader or buffer.
   * @function decode
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FatigueTestIrcutParam} FatigueTestIrcutParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestIrcutParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FatigueTestIrcutParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.interval = reader.int32();
          break;
        }
        case 3: {
          message.time = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FatigueTestIrcutParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FatigueTestIrcutParam} FatigueTestIrcutParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestIrcutParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FatigueTestIrcutParam message.
   * @function verify
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FatigueTestIrcutParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      if (!$util.isInteger(message.interval))
        return "interval: integer expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    return null;
  };

  /**
   * Creates a FatigueTestIrcutParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FatigueTestIrcutParam} FatigueTestIrcutParam
   */
  FatigueTestIrcutParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FatigueTestIrcutParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".FatigueTestIrcutParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FatigueTestIrcutParam();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.interval != null) message.interval = object.interval | 0;
    if (object.time != null) message.time = object.time | 0;
    return message;
  };

  /**
   * Creates a plain object from a FatigueTestIrcutParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {FatigueTestIrcutParam} message FatigueTestIrcutParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FatigueTestIrcutParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.interval = 0;
      object.time = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      object.interval = message.interval;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    return object;
  };

  /**
   * Converts this FatigueTestIrcutParam to JSON.
   * @function toJSON
   * @memberof FatigueTestIrcutParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FatigueTestIrcutParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FatigueTestIrcutParam
   * @function getTypeUrl
   * @memberof FatigueTestIrcutParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FatigueTestIrcutParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FatigueTestIrcutParam";
  };

  return FatigueTestIrcutParam;
})();

$root.FatigueTestMotorParam = (function () {
  /**
   * Properties of a FatigueTestMotorParam.
   * @exports IFatigueTestMotorParam
   * @interface IFatigueTestMotorParam
   * @property {boolean|null} [enable] FatigueTestMotorParam enable
   * @property {number|null} [mStep] FatigueTestMotorParam mStep
   * @property {number|null} [speed] FatigueTestMotorParam speed
   * @property {number|null} [time] FatigueTestMotorParam time
   * @property {number|null} [interval] FatigueTestMotorParam interval
   */

  /**
   * Constructs a new FatigueTestMotorParam.
   * @exports FatigueTestMotorParam
   * @classdesc Represents a FatigueTestMotorParam.
   * @implements IFatigueTestMotorParam
   * @constructor
   * @param {IFatigueTestMotorParam=} [properties] Properties to set
   */
  function FatigueTestMotorParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FatigueTestMotorParam enable.
   * @member {boolean} enable
   * @memberof FatigueTestMotorParam
   * @instance
   */
  FatigueTestMotorParam.prototype.enable = false;

  /**
   * FatigueTestMotorParam mStep.
   * @member {number} mStep
   * @memberof FatigueTestMotorParam
   * @instance
   */
  FatigueTestMotorParam.prototype.mStep = 0;

  /**
   * FatigueTestMotorParam speed.
   * @member {number} speed
   * @memberof FatigueTestMotorParam
   * @instance
   */
  FatigueTestMotorParam.prototype.speed = 0;

  /**
   * FatigueTestMotorParam time.
   * @member {number} time
   * @memberof FatigueTestMotorParam
   * @instance
   */
  FatigueTestMotorParam.prototype.time = 0;

  /**
   * FatigueTestMotorParam interval.
   * @member {number} interval
   * @memberof FatigueTestMotorParam
   * @instance
   */
  FatigueTestMotorParam.prototype.interval = 0;

  /**
   * Creates a new FatigueTestMotorParam instance using the specified properties.
   * @function create
   * @memberof FatigueTestMotorParam
   * @static
   * @param {IFatigueTestMotorParam=} [properties] Properties to set
   * @returns {FatigueTestMotorParam} FatigueTestMotorParam instance
   */
  FatigueTestMotorParam.create = function create(properties) {
    return new FatigueTestMotorParam(properties);
  };

  /**
   * Encodes the specified FatigueTestMotorParam message. Does not implicitly {@link FatigueTestMotorParam.verify|verify} messages.
   * @function encode
   * @memberof FatigueTestMotorParam
   * @static
   * @param {IFatigueTestMotorParam} message FatigueTestMotorParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestMotorParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (message.mStep != null && Object.hasOwnProperty.call(message, "mStep"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mStep);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.speed);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.time);
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.interval);
    return writer;
  };

  /**
   * Encodes the specified FatigueTestMotorParam message, length delimited. Does not implicitly {@link FatigueTestMotorParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FatigueTestMotorParam
   * @static
   * @param {IFatigueTestMotorParam} message FatigueTestMotorParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestMotorParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FatigueTestMotorParam message from the specified reader or buffer.
   * @function decode
   * @memberof FatigueTestMotorParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FatigueTestMotorParam} FatigueTestMotorParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestMotorParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FatigueTestMotorParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.mStep = reader.int32();
          break;
        }
        case 3: {
          message.speed = reader.int32();
          break;
        }
        case 4: {
          message.time = reader.int32();
          break;
        }
        case 5: {
          message.interval = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FatigueTestMotorParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FatigueTestMotorParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FatigueTestMotorParam} FatigueTestMotorParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestMotorParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FatigueTestMotorParam message.
   * @function verify
   * @memberof FatigueTestMotorParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FatigueTestMotorParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (message.mStep != null && Object.hasOwnProperty.call(message, "mStep"))
      if (!$util.isInteger(message.mStep)) return "mStep: integer expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (!$util.isInteger(message.speed)) return "speed: integer expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      if (!$util.isInteger(message.interval))
        return "interval: integer expected";
    return null;
  };

  /**
   * Creates a FatigueTestMotorParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FatigueTestMotorParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FatigueTestMotorParam} FatigueTestMotorParam
   */
  FatigueTestMotorParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FatigueTestMotorParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".FatigueTestMotorParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FatigueTestMotorParam();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.mStep != null) message.mStep = object.mStep | 0;
    if (object.speed != null) message.speed = object.speed | 0;
    if (object.time != null) message.time = object.time | 0;
    if (object.interval != null) message.interval = object.interval | 0;
    return message;
  };

  /**
   * Creates a plain object from a FatigueTestMotorParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FatigueTestMotorParam
   * @static
   * @param {FatigueTestMotorParam} message FatigueTestMotorParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FatigueTestMotorParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.mStep = 0;
      object.speed = 0;
      object.time = 0;
      object.interval = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (message.mStep != null && Object.hasOwnProperty.call(message, "mStep"))
      object.mStep = message.mStep;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed = message.speed;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      object.interval = message.interval;
    return object;
  };

  /**
   * Converts this FatigueTestMotorParam to JSON.
   * @function toJSON
   * @memberof FatigueTestMotorParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FatigueTestMotorParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FatigueTestMotorParam
   * @function getTypeUrl
   * @memberof FatigueTestMotorParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FatigueTestMotorParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FatigueTestMotorParam";
  };

  return FatigueTestMotorParam;
})();

$root.FatigueTestLightsParam = (function () {
  /**
   * Properties of a FatigueTestLightsParam.
   * @exports IFatigueTestLightsParam
   * @interface IFatigueTestLightsParam
   * @property {boolean|null} [enable] FatigueTestLightsParam enable
   * @property {number|null} [interval] FatigueTestLightsParam interval
   * @property {number|null} [time] FatigueTestLightsParam time
   */

  /**
   * Constructs a new FatigueTestLightsParam.
   * @exports FatigueTestLightsParam
   * @classdesc Represents a FatigueTestLightsParam.
   * @implements IFatigueTestLightsParam
   * @constructor
   * @param {IFatigueTestLightsParam=} [properties] Properties to set
   */
  function FatigueTestLightsParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FatigueTestLightsParam enable.
   * @member {boolean} enable
   * @memberof FatigueTestLightsParam
   * @instance
   */
  FatigueTestLightsParam.prototype.enable = false;

  /**
   * FatigueTestLightsParam interval.
   * @member {number} interval
   * @memberof FatigueTestLightsParam
   * @instance
   */
  FatigueTestLightsParam.prototype.interval = 0;

  /**
   * FatigueTestLightsParam time.
   * @member {number} time
   * @memberof FatigueTestLightsParam
   * @instance
   */
  FatigueTestLightsParam.prototype.time = 0;

  /**
   * Creates a new FatigueTestLightsParam instance using the specified properties.
   * @function create
   * @memberof FatigueTestLightsParam
   * @static
   * @param {IFatigueTestLightsParam=} [properties] Properties to set
   * @returns {FatigueTestLightsParam} FatigueTestLightsParam instance
   */
  FatigueTestLightsParam.create = function create(properties) {
    return new FatigueTestLightsParam(properties);
  };

  /**
   * Encodes the specified FatigueTestLightsParam message. Does not implicitly {@link FatigueTestLightsParam.verify|verify} messages.
   * @function encode
   * @memberof FatigueTestLightsParam
   * @static
   * @param {IFatigueTestLightsParam} message FatigueTestLightsParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestLightsParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.interval);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.time);
    return writer;
  };

  /**
   * Encodes the specified FatigueTestLightsParam message, length delimited. Does not implicitly {@link FatigueTestLightsParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FatigueTestLightsParam
   * @static
   * @param {IFatigueTestLightsParam} message FatigueTestLightsParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestLightsParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FatigueTestLightsParam message from the specified reader or buffer.
   * @function decode
   * @memberof FatigueTestLightsParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FatigueTestLightsParam} FatigueTestLightsParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestLightsParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FatigueTestLightsParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.interval = reader.int32();
          break;
        }
        case 3: {
          message.time = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FatigueTestLightsParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FatigueTestLightsParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FatigueTestLightsParam} FatigueTestLightsParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestLightsParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FatigueTestLightsParam message.
   * @function verify
   * @memberof FatigueTestLightsParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FatigueTestLightsParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      if (!$util.isInteger(message.interval))
        return "interval: integer expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    return null;
  };

  /**
   * Creates a FatigueTestLightsParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FatigueTestLightsParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FatigueTestLightsParam} FatigueTestLightsParam
   */
  FatigueTestLightsParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FatigueTestLightsParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".FatigueTestLightsParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FatigueTestLightsParam();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.interval != null) message.interval = object.interval | 0;
    if (object.time != null) message.time = object.time | 0;
    return message;
  };

  /**
   * Creates a plain object from a FatigueTestLightsParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FatigueTestLightsParam
   * @static
   * @param {FatigueTestLightsParam} message FatigueTestLightsParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FatigueTestLightsParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.interval = 0;
      object.time = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (
      message.interval != null &&
      Object.hasOwnProperty.call(message, "interval")
    )
      object.interval = message.interval;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    return object;
  };

  /**
   * Converts this FatigueTestLightsParam to JSON.
   * @function toJSON
   * @memberof FatigueTestLightsParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FatigueTestLightsParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FatigueTestLightsParam
   * @function getTypeUrl
   * @memberof FatigueTestLightsParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FatigueTestLightsParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FatigueTestLightsParam";
  };

  return FatigueTestLightsParam;
})();

$root.FatigueTestCameraParam = (function () {
  /**
   * Properties of a FatigueTestCameraParam.
   * @exports IFatigueTestCameraParam
   * @interface IFatigueTestCameraParam
   * @property {boolean|null} [enable] FatigueTestCameraParam enable
   */

  /**
   * Constructs a new FatigueTestCameraParam.
   * @exports FatigueTestCameraParam
   * @classdesc Represents a FatigueTestCameraParam.
   * @implements IFatigueTestCameraParam
   * @constructor
   * @param {IFatigueTestCameraParam=} [properties] Properties to set
   */
  function FatigueTestCameraParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FatigueTestCameraParam enable.
   * @member {boolean} enable
   * @memberof FatigueTestCameraParam
   * @instance
   */
  FatigueTestCameraParam.prototype.enable = false;

  /**
   * Creates a new FatigueTestCameraParam instance using the specified properties.
   * @function create
   * @memberof FatigueTestCameraParam
   * @static
   * @param {IFatigueTestCameraParam=} [properties] Properties to set
   * @returns {FatigueTestCameraParam} FatigueTestCameraParam instance
   */
  FatigueTestCameraParam.create = function create(properties) {
    return new FatigueTestCameraParam(properties);
  };

  /**
   * Encodes the specified FatigueTestCameraParam message. Does not implicitly {@link FatigueTestCameraParam.verify|verify} messages.
   * @function encode
   * @memberof FatigueTestCameraParam
   * @static
   * @param {IFatigueTestCameraParam} message FatigueTestCameraParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestCameraParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    return writer;
  };

  /**
   * Encodes the specified FatigueTestCameraParam message, length delimited. Does not implicitly {@link FatigueTestCameraParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FatigueTestCameraParam
   * @static
   * @param {IFatigueTestCameraParam} message FatigueTestCameraParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestCameraParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FatigueTestCameraParam message from the specified reader or buffer.
   * @function decode
   * @memberof FatigueTestCameraParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FatigueTestCameraParam} FatigueTestCameraParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestCameraParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FatigueTestCameraParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FatigueTestCameraParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FatigueTestCameraParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FatigueTestCameraParam} FatigueTestCameraParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestCameraParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FatigueTestCameraParam message.
   * @function verify
   * @memberof FatigueTestCameraParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FatigueTestCameraParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    return null;
  };

  /**
   * Creates a FatigueTestCameraParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FatigueTestCameraParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FatigueTestCameraParam} FatigueTestCameraParam
   */
  FatigueTestCameraParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FatigueTestCameraParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".FatigueTestCameraParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FatigueTestCameraParam();
    if (object.enable != null) message.enable = Boolean(object.enable);
    return message;
  };

  /**
   * Creates a plain object from a FatigueTestCameraParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FatigueTestCameraParam
   * @static
   * @param {FatigueTestCameraParam} message FatigueTestCameraParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FatigueTestCameraParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.enable = false;
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    return object;
  };

  /**
   * Converts this FatigueTestCameraParam to JSON.
   * @function toJSON
   * @memberof FatigueTestCameraParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FatigueTestCameraParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FatigueTestCameraParam
   * @function getTypeUrl
   * @memberof FatigueTestCameraParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FatigueTestCameraParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FatigueTestCameraParam";
  };

  return FatigueTestCameraParam;
})();

$root.FatigueTestParam = (function () {
  /**
   * Properties of a FatigueTestParam.
   * @exports IFatigueTestParam
   * @interface IFatigueTestParam
   * @property {IFatigueTestIrcutParam|null} [ircutParam] FatigueTestParam ircutParam
   * @property {IFatigueTestMotorParam|null} [motorRotateParam] FatigueTestParam motorRotateParam
   * @property {IFatigueTestMotorParam|null} [motorPitchParam] FatigueTestParam motorPitchParam
   * @property {IFatigueTestMotorParam|null} [motorAfParam] FatigueTestParam motorAfParam
   * @property {IFatigueTestLightsParam|null} [lightParam] FatigueTestParam lightParam
   * @property {IFatigueTestCameraParam|null} [cameraParam] FatigueTestParam cameraParam
   */

  /**
   * Constructs a new FatigueTestParam.
   * @exports FatigueTestParam
   * @classdesc Represents a FatigueTestParam.
   * @implements IFatigueTestParam
   * @constructor
   * @param {IFatigueTestParam=} [properties] Properties to set
   */
  function FatigueTestParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FatigueTestParam ircutParam.
   * @member {IFatigueTestIrcutParam|null|undefined} ircutParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.ircutParam = null;

  /**
   * FatigueTestParam motorRotateParam.
   * @member {IFatigueTestMotorParam|null|undefined} motorRotateParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.motorRotateParam = null;

  /**
   * FatigueTestParam motorPitchParam.
   * @member {IFatigueTestMotorParam|null|undefined} motorPitchParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.motorPitchParam = null;

  /**
   * FatigueTestParam motorAfParam.
   * @member {IFatigueTestMotorParam|null|undefined} motorAfParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.motorAfParam = null;

  /**
   * FatigueTestParam lightParam.
   * @member {IFatigueTestLightsParam|null|undefined} lightParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.lightParam = null;

  /**
   * FatigueTestParam cameraParam.
   * @member {IFatigueTestCameraParam|null|undefined} cameraParam
   * @memberof FatigueTestParam
   * @instance
   */
  FatigueTestParam.prototype.cameraParam = null;

  /**
   * Creates a new FatigueTestParam instance using the specified properties.
   * @function create
   * @memberof FatigueTestParam
   * @static
   * @param {IFatigueTestParam=} [properties] Properties to set
   * @returns {FatigueTestParam} FatigueTestParam instance
   */
  FatigueTestParam.create = function create(properties) {
    return new FatigueTestParam(properties);
  };

  /**
   * Encodes the specified FatigueTestParam message. Does not implicitly {@link FatigueTestParam.verify|verify} messages.
   * @function encode
   * @memberof FatigueTestParam
   * @static
   * @param {IFatigueTestParam} message FatigueTestParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.ircutParam != null &&
      Object.hasOwnProperty.call(message, "ircutParam")
    )
      $root.FatigueTestIrcutParam.encode(
        message.ircutParam,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.motorRotateParam != null &&
      Object.hasOwnProperty.call(message, "motorRotateParam")
    )
      $root.FatigueTestMotorParam.encode(
        message.motorRotateParam,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.motorPitchParam != null &&
      Object.hasOwnProperty.call(message, "motorPitchParam")
    )
      $root.FatigueTestMotorParam.encode(
        message.motorPitchParam,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.motorAfParam != null &&
      Object.hasOwnProperty.call(message, "motorAfParam")
    )
      $root.FatigueTestMotorParam.encode(
        message.motorAfParam,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    if (
      message.lightParam != null &&
      Object.hasOwnProperty.call(message, "lightParam")
    )
      $root.FatigueTestLightsParam.encode(
        message.lightParam,
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
        q + 1,
      ).ldelim();
    if (
      message.cameraParam != null &&
      Object.hasOwnProperty.call(message, "cameraParam")
    )
      $root.FatigueTestCameraParam.encode(
        message.cameraParam,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified FatigueTestParam message, length delimited. Does not implicitly {@link FatigueTestParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FatigueTestParam
   * @static
   * @param {IFatigueTestParam} message FatigueTestParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FatigueTestParam.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FatigueTestParam message from the specified reader or buffer.
   * @function decode
   * @memberof FatigueTestParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FatigueTestParam} FatigueTestParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FatigueTestParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.ircutParam = $root.FatigueTestIrcutParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.motorRotateParam = $root.FatigueTestMotorParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.motorPitchParam = $root.FatigueTestMotorParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.motorAfParam = $root.FatigueTestMotorParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 5: {
          message.lightParam = $root.FatigueTestLightsParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 6: {
          message.cameraParam = $root.FatigueTestCameraParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FatigueTestParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FatigueTestParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FatigueTestParam} FatigueTestParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FatigueTestParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FatigueTestParam message.
   * @function verify
   * @memberof FatigueTestParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FatigueTestParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.ircutParam != null &&
      Object.hasOwnProperty.call(message, "ircutParam")
    ) {
      var error = $root.FatigueTestIrcutParam.verify(
        message.ircutParam,
        long + 1,
      );
      if (error) return "ircutParam." + error;
    }
    if (
      message.motorRotateParam != null &&
      Object.hasOwnProperty.call(message, "motorRotateParam")
    ) {
      var error = $root.FatigueTestMotorParam.verify(
        message.motorRotateParam,
        long + 1,
      );
      if (error) return "motorRotateParam." + error;
    }
    if (
      message.motorPitchParam != null &&
      Object.hasOwnProperty.call(message, "motorPitchParam")
    ) {
      var error = $root.FatigueTestMotorParam.verify(
        message.motorPitchParam,
        long + 1,
      );
      if (error) return "motorPitchParam." + error;
    }
    if (
      message.motorAfParam != null &&
      Object.hasOwnProperty.call(message, "motorAfParam")
    ) {
      var error = $root.FatigueTestMotorParam.verify(
        message.motorAfParam,
        long + 1,
      );
      if (error) return "motorAfParam." + error;
    }
    if (
      message.lightParam != null &&
      Object.hasOwnProperty.call(message, "lightParam")
    ) {
      var error = $root.FatigueTestLightsParam.verify(
        message.lightParam,
        long + 1,
      );
      if (error) return "lightParam." + error;
    }
    if (
      message.cameraParam != null &&
      Object.hasOwnProperty.call(message, "cameraParam")
    ) {
      var error = $root.FatigueTestCameraParam.verify(
        message.cameraParam,
        long + 1,
      );
      if (error) return "cameraParam." + error;
    }
    return null;
  };

  /**
   * Creates a FatigueTestParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FatigueTestParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FatigueTestParam} FatigueTestParam
   */
  FatigueTestParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FatigueTestParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".FatigueTestParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FatigueTestParam();
    if (object.ircutParam != null) {
      if (!$util.isObject(object.ircutParam))
        throw TypeError(".FatigueTestParam.ircutParam: object expected");
      message.ircutParam = $root.FatigueTestIrcutParam.fromObject(
        object.ircutParam,
        long + 1,
      );
    }
    if (object.motorRotateParam != null) {
      if (!$util.isObject(object.motorRotateParam))
        throw TypeError(".FatigueTestParam.motorRotateParam: object expected");
      message.motorRotateParam = $root.FatigueTestMotorParam.fromObject(
        object.motorRotateParam,
        long + 1,
      );
    }
    if (object.motorPitchParam != null) {
      if (!$util.isObject(object.motorPitchParam))
        throw TypeError(".FatigueTestParam.motorPitchParam: object expected");
      message.motorPitchParam = $root.FatigueTestMotorParam.fromObject(
        object.motorPitchParam,
        long + 1,
      );
    }
    if (object.motorAfParam != null) {
      if (!$util.isObject(object.motorAfParam))
        throw TypeError(".FatigueTestParam.motorAfParam: object expected");
      message.motorAfParam = $root.FatigueTestMotorParam.fromObject(
        object.motorAfParam,
        long + 1,
      );
    }
    if (object.lightParam != null) {
      if (!$util.isObject(object.lightParam))
        throw TypeError(".FatigueTestParam.lightParam: object expected");
      message.lightParam = $root.FatigueTestLightsParam.fromObject(
        object.lightParam,
        long + 1,
      );
    }
    if (object.cameraParam != null) {
      if (!$util.isObject(object.cameraParam))
        throw TypeError(".FatigueTestParam.cameraParam: object expected");
      message.cameraParam = $root.FatigueTestCameraParam.fromObject(
        object.cameraParam,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a FatigueTestParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FatigueTestParam
   * @static
   * @param {FatigueTestParam} message FatigueTestParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FatigueTestParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.ircutParam = null;
      object.motorRotateParam = null;
      object.motorPitchParam = null;
      object.motorAfParam = null;
      object.lightParam = null;
      object.cameraParam = null;
    }
    if (
      message.ircutParam != null &&
      Object.hasOwnProperty.call(message, "ircutParam")
    )
      object.ircutParam = $root.FatigueTestIrcutParam.toObject(
        message.ircutParam,
        options,
        q + 1,
      );
    if (
      message.motorRotateParam != null &&
      Object.hasOwnProperty.call(message, "motorRotateParam")
    )
      object.motorRotateParam = $root.FatigueTestMotorParam.toObject(
        message.motorRotateParam,
        options,
        q + 1,
      );
    if (
      message.motorPitchParam != null &&
      Object.hasOwnProperty.call(message, "motorPitchParam")
    )
      object.motorPitchParam = $root.FatigueTestMotorParam.toObject(
        message.motorPitchParam,
        options,
        q + 1,
      );
    if (
      message.motorAfParam != null &&
      Object.hasOwnProperty.call(message, "motorAfParam")
    )
      object.motorAfParam = $root.FatigueTestMotorParam.toObject(
        message.motorAfParam,
        options,
        q + 1,
      );
    if (
      message.lightParam != null &&
      Object.hasOwnProperty.call(message, "lightParam")
    )
      object.lightParam = $root.FatigueTestLightsParam.toObject(
        message.lightParam,
        options,
        q + 1,
      );
    if (
      message.cameraParam != null &&
      Object.hasOwnProperty.call(message, "cameraParam")
    )
      object.cameraParam = $root.FatigueTestCameraParam.toObject(
        message.cameraParam,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this FatigueTestParam to JSON.
   * @function toJSON
   * @memberof FatigueTestParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FatigueTestParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FatigueTestParam
   * @function getTypeUrl
   * @memberof FatigueTestParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FatigueTestParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FatigueTestParam";
  };

  return FatigueTestParam;
})();

$root.ResEMMCInAadOutSpeed = (function () {
  /**
   * Properties of a ResEMMCInAadOutSpeed.
   * @exports IResEMMCInAadOutSpeed
   * @interface IResEMMCInAadOutSpeed
   * @property {number|null} [cmd] ResEMMCInAadOutSpeed cmd
   * @property {number|null} [code] ResEMMCInAadOutSpeed code
   * @property {number|null} [readspeed] ResEMMCInAadOutSpeed readspeed
   * @property {number|null} [writespeed] ResEMMCInAadOutSpeed writespeed
   */

  /**
   * Constructs a new ResEMMCInAadOutSpeed.
   * @exports ResEMMCInAadOutSpeed
   * @classdesc Represents a ResEMMCInAadOutSpeed.
   * @implements IResEMMCInAadOutSpeed
   * @constructor
   * @param {IResEMMCInAadOutSpeed=} [properties] Properties to set
   */
  function ResEMMCInAadOutSpeed(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResEMMCInAadOutSpeed cmd.
   * @member {number} cmd
   * @memberof ResEMMCInAadOutSpeed
   * @instance
   */
  ResEMMCInAadOutSpeed.prototype.cmd = 0;

  /**
   * ResEMMCInAadOutSpeed code.
   * @member {number} code
   * @memberof ResEMMCInAadOutSpeed
   * @instance
   */
  ResEMMCInAadOutSpeed.prototype.code = 0;

  /**
   * ResEMMCInAadOutSpeed readspeed.
   * @member {number} readspeed
   * @memberof ResEMMCInAadOutSpeed
   * @instance
   */
  ResEMMCInAadOutSpeed.prototype.readspeed = 0;

  /**
   * ResEMMCInAadOutSpeed writespeed.
   * @member {number} writespeed
   * @memberof ResEMMCInAadOutSpeed
   * @instance
   */
  ResEMMCInAadOutSpeed.prototype.writespeed = 0;

  /**
   * Creates a new ResEMMCInAadOutSpeed instance using the specified properties.
   * @function create
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {IResEMMCInAadOutSpeed=} [properties] Properties to set
   * @returns {ResEMMCInAadOutSpeed} ResEMMCInAadOutSpeed instance
   */
  ResEMMCInAadOutSpeed.create = function create(properties) {
    return new ResEMMCInAadOutSpeed(properties);
  };

  /**
   * Encodes the specified ResEMMCInAadOutSpeed message. Does not implicitly {@link ResEMMCInAadOutSpeed.verify|verify} messages.
   * @function encode
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {IResEMMCInAadOutSpeed} message ResEMMCInAadOutSpeed message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResEMMCInAadOutSpeed.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (
      message.readspeed != null &&
      Object.hasOwnProperty.call(message, "readspeed")
    )
      writer.uint32(/* id 3, wireType 5 =*/ 29).float(message.readspeed);
    if (
      message.writespeed != null &&
      Object.hasOwnProperty.call(message, "writespeed")
    )
      writer.uint32(/* id 4, wireType 5 =*/ 37).float(message.writespeed);
    return writer;
  };

  /**
   * Encodes the specified ResEMMCInAadOutSpeed message, length delimited. Does not implicitly {@link ResEMMCInAadOutSpeed.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {IResEMMCInAadOutSpeed} message ResEMMCInAadOutSpeed message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResEMMCInAadOutSpeed.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResEMMCInAadOutSpeed message from the specified reader or buffer.
   * @function decode
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResEMMCInAadOutSpeed} ResEMMCInAadOutSpeed
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResEMMCInAadOutSpeed.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResEMMCInAadOutSpeed();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.readspeed = reader.float();
          break;
        }
        case 4: {
          message.writespeed = reader.float();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResEMMCInAadOutSpeed message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResEMMCInAadOutSpeed} ResEMMCInAadOutSpeed
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResEMMCInAadOutSpeed.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResEMMCInAadOutSpeed message.
   * @function verify
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResEMMCInAadOutSpeed.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.readspeed != null &&
      Object.hasOwnProperty.call(message, "readspeed")
    )
      if (typeof message.readspeed !== "number")
        return "readspeed: number expected";
    if (
      message.writespeed != null &&
      Object.hasOwnProperty.call(message, "writespeed")
    )
      if (typeof message.writespeed !== "number")
        return "writespeed: number expected";
    return null;
  };

  /**
   * Creates a ResEMMCInAadOutSpeed message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResEMMCInAadOutSpeed} ResEMMCInAadOutSpeed
   */
  ResEMMCInAadOutSpeed.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResEMMCInAadOutSpeed) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResEMMCInAadOutSpeed: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResEMMCInAadOutSpeed();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.readspeed != null) message.readspeed = Number(object.readspeed);
    if (object.writespeed != null)
      message.writespeed = Number(object.writespeed);
    return message;
  };

  /**
   * Creates a plain object from a ResEMMCInAadOutSpeed message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {ResEMMCInAadOutSpeed} message ResEMMCInAadOutSpeed
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResEMMCInAadOutSpeed.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.readspeed = 0;
      object.writespeed = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.readspeed != null &&
      Object.hasOwnProperty.call(message, "readspeed")
    )
      object.readspeed =
        options.json && !isFinite(message.readspeed)
          ? String(message.readspeed)
          : message.readspeed;
    if (
      message.writespeed != null &&
      Object.hasOwnProperty.call(message, "writespeed")
    )
      object.writespeed =
        options.json && !isFinite(message.writespeed)
          ? String(message.writespeed)
          : message.writespeed;
    return object;
  };

  /**
   * Converts this ResEMMCInAadOutSpeed to JSON.
   * @function toJSON
   * @memberof ResEMMCInAadOutSpeed
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResEMMCInAadOutSpeed.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResEMMCInAadOutSpeed
   * @function getTypeUrl
   * @memberof ResEMMCInAadOutSpeed
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResEMMCInAadOutSpeed.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResEMMCInAadOutSpeed";
  };

  return ResEMMCInAadOutSpeed;
})();

$root.ReqFocusMotorBacklashTest = (function () {
  /**
   * Properties of a ReqFocusMotorBacklashTest.
   * @exports IReqFocusMotorBacklashTest
   * @interface IReqFocusMotorBacklashTest
   * @property {number|null} [motorId] ReqFocusMotorBacklashTest motorId
   */

  /**
   * Constructs a new ReqFocusMotorBacklashTest.
   * @exports ReqFocusMotorBacklashTest
   * @classdesc Represents a ReqFocusMotorBacklashTest.
   * @implements IReqFocusMotorBacklashTest
   * @constructor
   * @param {IReqFocusMotorBacklashTest=} [properties] Properties to set
   */
  function ReqFocusMotorBacklashTest(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFocusMotorBacklashTest motorId.
   * @member {number|null|undefined} motorId
   * @memberof ReqFocusMotorBacklashTest
   * @instance
   */
  ReqFocusMotorBacklashTest.prototype.motorId = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqFocusMotorBacklashTest.prototype, "_motorId", {
    get: $util.oneOfGetter(($oneOfFields = ["motorId"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqFocusMotorBacklashTest instance using the specified properties.
   * @function create
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {IReqFocusMotorBacklashTest=} [properties] Properties to set
   * @returns {ReqFocusMotorBacklashTest} ReqFocusMotorBacklashTest instance
   */
  ReqFocusMotorBacklashTest.create = function create(properties) {
    return new ReqFocusMotorBacklashTest(properties);
  };

  /**
   * Encodes the specified ReqFocusMotorBacklashTest message. Does not implicitly {@link ReqFocusMotorBacklashTest.verify|verify} messages.
   * @function encode
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {IReqFocusMotorBacklashTest} message ReqFocusMotorBacklashTest message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFocusMotorBacklashTest.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.motorId);
    return writer;
  };

  /**
   * Encodes the specified ReqFocusMotorBacklashTest message, length delimited. Does not implicitly {@link ReqFocusMotorBacklashTest.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {IReqFocusMotorBacklashTest} message ReqFocusMotorBacklashTest message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFocusMotorBacklashTest.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFocusMotorBacklashTest message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFocusMotorBacklashTest} ReqFocusMotorBacklashTest
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFocusMotorBacklashTest.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFocusMotorBacklashTest();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.motorId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFocusMotorBacklashTest message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFocusMotorBacklashTest} ReqFocusMotorBacklashTest
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFocusMotorBacklashTest.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFocusMotorBacklashTest message.
   * @function verify
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFocusMotorBacklashTest.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    ) {
      properties._motorId = 1;
      if (!$util.isInteger(message.motorId)) return "motorId: integer expected";
    }
    return null;
  };

  /**
   * Creates a ReqFocusMotorBacklashTest message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFocusMotorBacklashTest} ReqFocusMotorBacklashTest
   */
  ReqFocusMotorBacklashTest.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFocusMotorBacklashTest) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFocusMotorBacklashTest: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFocusMotorBacklashTest();
    if (object.motorId != null) message.motorId = object.motorId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFocusMotorBacklashTest message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {ReqFocusMotorBacklashTest} message ReqFocusMotorBacklashTest
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFocusMotorBacklashTest.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    ) {
      object.motorId = message.motorId;
      if (options.oneofs) object._motorId = "motorId";
    }
    return object;
  };

  /**
   * Converts this ReqFocusMotorBacklashTest to JSON.
   * @function toJSON
   * @memberof ReqFocusMotorBacklashTest
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFocusMotorBacklashTest.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFocusMotorBacklashTest
   * @function getTypeUrl
   * @memberof ReqFocusMotorBacklashTest
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFocusMotorBacklashTest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFocusMotorBacklashTest";
  };

  return ReqFocusMotorBacklashTest;
})();

$root.ReqCaptureTeleDark = (function () {
  /**
   * Properties of a ReqCaptureTeleDark.
   * @exports IReqCaptureTeleDark
   * @interface IReqCaptureTeleDark
   */

  /**
   * Constructs a new ReqCaptureTeleDark.
   * @exports ReqCaptureTeleDark
   * @classdesc Represents a ReqCaptureTeleDark.
   * @implements IReqCaptureTeleDark
   * @constructor
   * @param {IReqCaptureTeleDark=} [properties] Properties to set
   */
  function ReqCaptureTeleDark(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureTeleDark instance using the specified properties.
   * @function create
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {IReqCaptureTeleDark=} [properties] Properties to set
   * @returns {ReqCaptureTeleDark} ReqCaptureTeleDark instance
   */
  ReqCaptureTeleDark.create = function create(properties) {
    return new ReqCaptureTeleDark(properties);
  };

  /**
   * Encodes the specified ReqCaptureTeleDark message. Does not implicitly {@link ReqCaptureTeleDark.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {IReqCaptureTeleDark} message ReqCaptureTeleDark message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleDark.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureTeleDark message, length delimited. Does not implicitly {@link ReqCaptureTeleDark.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {IReqCaptureTeleDark} message ReqCaptureTeleDark message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleDark.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureTeleDark message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureTeleDark} ReqCaptureTeleDark
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleDark.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureTeleDark();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureTeleDark message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureTeleDark} ReqCaptureTeleDark
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleDark.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureTeleDark message.
   * @function verify
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureTeleDark.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureTeleDark message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureTeleDark} ReqCaptureTeleDark
   */
  ReqCaptureTeleDark.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureTeleDark) return object;
    return new $root.ReqCaptureTeleDark();
  };

  /**
   * Creates a plain object from a ReqCaptureTeleDark message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {ReqCaptureTeleDark} message ReqCaptureTeleDark
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureTeleDark.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureTeleDark to JSON.
   * @function toJSON
   * @memberof ReqCaptureTeleDark
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureTeleDark.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureTeleDark
   * @function getTypeUrl
   * @memberof ReqCaptureTeleDark
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureTeleDark.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureTeleDark";
  };

  return ReqCaptureTeleDark;
})();

$root.ReqCaptureTeleBias = (function () {
  /**
   * Properties of a ReqCaptureTeleBias.
   * @exports IReqCaptureTeleBias
   * @interface IReqCaptureTeleBias
   */

  /**
   * Constructs a new ReqCaptureTeleBias.
   * @exports ReqCaptureTeleBias
   * @classdesc Represents a ReqCaptureTeleBias.
   * @implements IReqCaptureTeleBias
   * @constructor
   * @param {IReqCaptureTeleBias=} [properties] Properties to set
   */
  function ReqCaptureTeleBias(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureTeleBias instance using the specified properties.
   * @function create
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {IReqCaptureTeleBias=} [properties] Properties to set
   * @returns {ReqCaptureTeleBias} ReqCaptureTeleBias instance
   */
  ReqCaptureTeleBias.create = function create(properties) {
    return new ReqCaptureTeleBias(properties);
  };

  /**
   * Encodes the specified ReqCaptureTeleBias message. Does not implicitly {@link ReqCaptureTeleBias.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {IReqCaptureTeleBias} message ReqCaptureTeleBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleBias.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureTeleBias message, length delimited. Does not implicitly {@link ReqCaptureTeleBias.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {IReqCaptureTeleBias} message ReqCaptureTeleBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleBias.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureTeleBias message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureTeleBias} ReqCaptureTeleBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleBias.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureTeleBias();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureTeleBias message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureTeleBias} ReqCaptureTeleBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleBias.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureTeleBias message.
   * @function verify
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureTeleBias.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureTeleBias message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureTeleBias} ReqCaptureTeleBias
   */
  ReqCaptureTeleBias.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureTeleBias) return object;
    return new $root.ReqCaptureTeleBias();
  };

  /**
   * Creates a plain object from a ReqCaptureTeleBias message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {ReqCaptureTeleBias} message ReqCaptureTeleBias
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureTeleBias.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureTeleBias to JSON.
   * @function toJSON
   * @memberof ReqCaptureTeleBias
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureTeleBias.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureTeleBias
   * @function getTypeUrl
   * @memberof ReqCaptureTeleBias
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureTeleBias.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureTeleBias";
  };

  return ReqCaptureTeleBias;
})();

$root.ReqCaptureTeleFlat = (function () {
  /**
   * Properties of a ReqCaptureTeleFlat.
   * @exports IReqCaptureTeleFlat
   * @interface IReqCaptureTeleFlat
   */

  /**
   * Constructs a new ReqCaptureTeleFlat.
   * @exports ReqCaptureTeleFlat
   * @classdesc Represents a ReqCaptureTeleFlat.
   * @implements IReqCaptureTeleFlat
   * @constructor
   * @param {IReqCaptureTeleFlat=} [properties] Properties to set
   */
  function ReqCaptureTeleFlat(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureTeleFlat instance using the specified properties.
   * @function create
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {IReqCaptureTeleFlat=} [properties] Properties to set
   * @returns {ReqCaptureTeleFlat} ReqCaptureTeleFlat instance
   */
  ReqCaptureTeleFlat.create = function create(properties) {
    return new ReqCaptureTeleFlat(properties);
  };

  /**
   * Encodes the specified ReqCaptureTeleFlat message. Does not implicitly {@link ReqCaptureTeleFlat.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {IReqCaptureTeleFlat} message ReqCaptureTeleFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleFlat.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureTeleFlat message, length delimited. Does not implicitly {@link ReqCaptureTeleFlat.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {IReqCaptureTeleFlat} message ReqCaptureTeleFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureTeleFlat.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureTeleFlat message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureTeleFlat} ReqCaptureTeleFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleFlat.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureTeleFlat();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureTeleFlat message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureTeleFlat} ReqCaptureTeleFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureTeleFlat.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureTeleFlat message.
   * @function verify
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureTeleFlat.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureTeleFlat message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureTeleFlat} ReqCaptureTeleFlat
   */
  ReqCaptureTeleFlat.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureTeleFlat) return object;
    return new $root.ReqCaptureTeleFlat();
  };

  /**
   * Creates a plain object from a ReqCaptureTeleFlat message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {ReqCaptureTeleFlat} message ReqCaptureTeleFlat
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureTeleFlat.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureTeleFlat to JSON.
   * @function toJSON
   * @memberof ReqCaptureTeleFlat
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureTeleFlat.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureTeleFlat
   * @function getTypeUrl
   * @memberof ReqCaptureTeleFlat
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureTeleFlat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureTeleFlat";
  };

  return ReqCaptureTeleFlat;
})();

$root.ReqCaptureWideDark = (function () {
  /**
   * Properties of a ReqCaptureWideDark.
   * @exports IReqCaptureWideDark
   * @interface IReqCaptureWideDark
   */

  /**
   * Constructs a new ReqCaptureWideDark.
   * @exports ReqCaptureWideDark
   * @classdesc Represents a ReqCaptureWideDark.
   * @implements IReqCaptureWideDark
   * @constructor
   * @param {IReqCaptureWideDark=} [properties] Properties to set
   */
  function ReqCaptureWideDark(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureWideDark instance using the specified properties.
   * @function create
   * @memberof ReqCaptureWideDark
   * @static
   * @param {IReqCaptureWideDark=} [properties] Properties to set
   * @returns {ReqCaptureWideDark} ReqCaptureWideDark instance
   */
  ReqCaptureWideDark.create = function create(properties) {
    return new ReqCaptureWideDark(properties);
  };

  /**
   * Encodes the specified ReqCaptureWideDark message. Does not implicitly {@link ReqCaptureWideDark.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureWideDark
   * @static
   * @param {IReqCaptureWideDark} message ReqCaptureWideDark message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideDark.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureWideDark message, length delimited. Does not implicitly {@link ReqCaptureWideDark.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureWideDark
   * @static
   * @param {IReqCaptureWideDark} message ReqCaptureWideDark message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideDark.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureWideDark message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureWideDark
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureWideDark} ReqCaptureWideDark
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideDark.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureWideDark();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureWideDark message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureWideDark
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureWideDark} ReqCaptureWideDark
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideDark.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureWideDark message.
   * @function verify
   * @memberof ReqCaptureWideDark
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureWideDark.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureWideDark message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureWideDark
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureWideDark} ReqCaptureWideDark
   */
  ReqCaptureWideDark.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureWideDark) return object;
    return new $root.ReqCaptureWideDark();
  };

  /**
   * Creates a plain object from a ReqCaptureWideDark message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureWideDark
   * @static
   * @param {ReqCaptureWideDark} message ReqCaptureWideDark
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureWideDark.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureWideDark to JSON.
   * @function toJSON
   * @memberof ReqCaptureWideDark
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureWideDark.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureWideDark
   * @function getTypeUrl
   * @memberof ReqCaptureWideDark
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureWideDark.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureWideDark";
  };

  return ReqCaptureWideDark;
})();

$root.ReqCaptureWideBias = (function () {
  /**
   * Properties of a ReqCaptureWideBias.
   * @exports IReqCaptureWideBias
   * @interface IReqCaptureWideBias
   */

  /**
   * Constructs a new ReqCaptureWideBias.
   * @exports ReqCaptureWideBias
   * @classdesc Represents a ReqCaptureWideBias.
   * @implements IReqCaptureWideBias
   * @constructor
   * @param {IReqCaptureWideBias=} [properties] Properties to set
   */
  function ReqCaptureWideBias(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureWideBias instance using the specified properties.
   * @function create
   * @memberof ReqCaptureWideBias
   * @static
   * @param {IReqCaptureWideBias=} [properties] Properties to set
   * @returns {ReqCaptureWideBias} ReqCaptureWideBias instance
   */
  ReqCaptureWideBias.create = function create(properties) {
    return new ReqCaptureWideBias(properties);
  };

  /**
   * Encodes the specified ReqCaptureWideBias message. Does not implicitly {@link ReqCaptureWideBias.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureWideBias
   * @static
   * @param {IReqCaptureWideBias} message ReqCaptureWideBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideBias.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureWideBias message, length delimited. Does not implicitly {@link ReqCaptureWideBias.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureWideBias
   * @static
   * @param {IReqCaptureWideBias} message ReqCaptureWideBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideBias.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureWideBias message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureWideBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureWideBias} ReqCaptureWideBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideBias.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureWideBias();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureWideBias message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureWideBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureWideBias} ReqCaptureWideBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideBias.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureWideBias message.
   * @function verify
   * @memberof ReqCaptureWideBias
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureWideBias.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureWideBias message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureWideBias
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureWideBias} ReqCaptureWideBias
   */
  ReqCaptureWideBias.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureWideBias) return object;
    return new $root.ReqCaptureWideBias();
  };

  /**
   * Creates a plain object from a ReqCaptureWideBias message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureWideBias
   * @static
   * @param {ReqCaptureWideBias} message ReqCaptureWideBias
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureWideBias.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureWideBias to JSON.
   * @function toJSON
   * @memberof ReqCaptureWideBias
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureWideBias.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureWideBias
   * @function getTypeUrl
   * @memberof ReqCaptureWideBias
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureWideBias.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureWideBias";
  };

  return ReqCaptureWideBias;
})();

$root.ReqCaptureWideFlat = (function () {
  /**
   * Properties of a ReqCaptureWideFlat.
   * @exports IReqCaptureWideFlat
   * @interface IReqCaptureWideFlat
   */

  /**
   * Constructs a new ReqCaptureWideFlat.
   * @exports ReqCaptureWideFlat
   * @classdesc Represents a ReqCaptureWideFlat.
   * @implements IReqCaptureWideFlat
   * @constructor
   * @param {IReqCaptureWideFlat=} [properties] Properties to set
   */
  function ReqCaptureWideFlat(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureWideFlat instance using the specified properties.
   * @function create
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {IReqCaptureWideFlat=} [properties] Properties to set
   * @returns {ReqCaptureWideFlat} ReqCaptureWideFlat instance
   */
  ReqCaptureWideFlat.create = function create(properties) {
    return new ReqCaptureWideFlat(properties);
  };

  /**
   * Encodes the specified ReqCaptureWideFlat message. Does not implicitly {@link ReqCaptureWideFlat.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {IReqCaptureWideFlat} message ReqCaptureWideFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideFlat.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureWideFlat message, length delimited. Does not implicitly {@link ReqCaptureWideFlat.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {IReqCaptureWideFlat} message ReqCaptureWideFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureWideFlat.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureWideFlat message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureWideFlat} ReqCaptureWideFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideFlat.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureWideFlat();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureWideFlat message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureWideFlat} ReqCaptureWideFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureWideFlat.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureWideFlat message.
   * @function verify
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureWideFlat.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureWideFlat message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureWideFlat} ReqCaptureWideFlat
   */
  ReqCaptureWideFlat.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureWideFlat) return object;
    return new $root.ReqCaptureWideFlat();
  };

  /**
   * Creates a plain object from a ReqCaptureWideFlat message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {ReqCaptureWideFlat} message ReqCaptureWideFlat
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureWideFlat.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureWideFlat to JSON.
   * @function toJSON
   * @memberof ReqCaptureWideFlat
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureWideFlat.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureWideFlat
   * @function getTypeUrl
   * @memberof ReqCaptureWideFlat
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureWideFlat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureWideFlat";
  };

  return ReqCaptureWideFlat;
})();

$root.ReqCaptureGuideFlat = (function () {
  /**
   * Properties of a ReqCaptureGuideFlat.
   * @exports IReqCaptureGuideFlat
   * @interface IReqCaptureGuideFlat
   */

  /**
   * Constructs a new ReqCaptureGuideFlat.
   * @exports ReqCaptureGuideFlat
   * @classdesc Represents a ReqCaptureGuideFlat.
   * @implements IReqCaptureGuideFlat
   * @constructor
   * @param {IReqCaptureGuideFlat=} [properties] Properties to set
   */
  function ReqCaptureGuideFlat(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureGuideFlat instance using the specified properties.
   * @function create
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {IReqCaptureGuideFlat=} [properties] Properties to set
   * @returns {ReqCaptureGuideFlat} ReqCaptureGuideFlat instance
   */
  ReqCaptureGuideFlat.create = function create(properties) {
    return new ReqCaptureGuideFlat(properties);
  };

  /**
   * Encodes the specified ReqCaptureGuideFlat message. Does not implicitly {@link ReqCaptureGuideFlat.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {IReqCaptureGuideFlat} message ReqCaptureGuideFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureGuideFlat.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureGuideFlat message, length delimited. Does not implicitly {@link ReqCaptureGuideFlat.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {IReqCaptureGuideFlat} message ReqCaptureGuideFlat message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureGuideFlat.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureGuideFlat message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureGuideFlat} ReqCaptureGuideFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureGuideFlat.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureGuideFlat();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureGuideFlat message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureGuideFlat} ReqCaptureGuideFlat
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureGuideFlat.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureGuideFlat message.
   * @function verify
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureGuideFlat.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureGuideFlat message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureGuideFlat} ReqCaptureGuideFlat
   */
  ReqCaptureGuideFlat.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureGuideFlat) return object;
    return new $root.ReqCaptureGuideFlat();
  };

  /**
   * Creates a plain object from a ReqCaptureGuideFlat message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {ReqCaptureGuideFlat} message ReqCaptureGuideFlat
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureGuideFlat.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureGuideFlat to JSON.
   * @function toJSON
   * @memberof ReqCaptureGuideFlat
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureGuideFlat.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureGuideFlat
   * @function getTypeUrl
   * @memberof ReqCaptureGuideFlat
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureGuideFlat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureGuideFlat";
  };

  return ReqCaptureGuideFlat;
})();

$root.ReqCaptureGuideBias = (function () {
  /**
   * Properties of a ReqCaptureGuideBias.
   * @exports IReqCaptureGuideBias
   * @interface IReqCaptureGuideBias
   */

  /**
   * Constructs a new ReqCaptureGuideBias.
   * @exports ReqCaptureGuideBias
   * @classdesc Represents a ReqCaptureGuideBias.
   * @implements IReqCaptureGuideBias
   * @constructor
   * @param {IReqCaptureGuideBias=} [properties] Properties to set
   */
  function ReqCaptureGuideBias(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCaptureGuideBias instance using the specified properties.
   * @function create
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {IReqCaptureGuideBias=} [properties] Properties to set
   * @returns {ReqCaptureGuideBias} ReqCaptureGuideBias instance
   */
  ReqCaptureGuideBias.create = function create(properties) {
    return new ReqCaptureGuideBias(properties);
  };

  /**
   * Encodes the specified ReqCaptureGuideBias message. Does not implicitly {@link ReqCaptureGuideBias.verify|verify} messages.
   * @function encode
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {IReqCaptureGuideBias} message ReqCaptureGuideBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureGuideBias.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCaptureGuideBias message, length delimited. Does not implicitly {@link ReqCaptureGuideBias.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {IReqCaptureGuideBias} message ReqCaptureGuideBias message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCaptureGuideBias.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCaptureGuideBias message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCaptureGuideBias} ReqCaptureGuideBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureGuideBias.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCaptureGuideBias();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCaptureGuideBias message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCaptureGuideBias} ReqCaptureGuideBias
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCaptureGuideBias.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCaptureGuideBias message.
   * @function verify
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCaptureGuideBias.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCaptureGuideBias message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCaptureGuideBias} ReqCaptureGuideBias
   */
  ReqCaptureGuideBias.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCaptureGuideBias) return object;
    return new $root.ReqCaptureGuideBias();
  };

  /**
   * Creates a plain object from a ReqCaptureGuideBias message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {ReqCaptureGuideBias} message ReqCaptureGuideBias
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCaptureGuideBias.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCaptureGuideBias to JSON.
   * @function toJSON
   * @memberof ReqCaptureGuideBias
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCaptureGuideBias.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCaptureGuideBias
   * @function getTypeUrl
   * @memberof ReqCaptureGuideBias
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCaptureGuideBias.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCaptureGuideBias";
  };

  return ReqCaptureGuideBias;
})();

$root.ReqResetMotorMcu = (function () {
  /**
   * Properties of a ReqResetMotorMcu.
   * @exports IReqResetMotorMcu
   * @interface IReqResetMotorMcu
   */

  /**
   * Constructs a new ReqResetMotorMcu.
   * @exports ReqResetMotorMcu
   * @classdesc Represents a ReqResetMotorMcu.
   * @implements IReqResetMotorMcu
   * @constructor
   * @param {IReqResetMotorMcu=} [properties] Properties to set
   */
  function ReqResetMotorMcu(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqResetMotorMcu instance using the specified properties.
   * @function create
   * @memberof ReqResetMotorMcu
   * @static
   * @param {IReqResetMotorMcu=} [properties] Properties to set
   * @returns {ReqResetMotorMcu} ReqResetMotorMcu instance
   */
  ReqResetMotorMcu.create = function create(properties) {
    return new ReqResetMotorMcu(properties);
  };

  /**
   * Encodes the specified ReqResetMotorMcu message. Does not implicitly {@link ReqResetMotorMcu.verify|verify} messages.
   * @function encode
   * @memberof ReqResetMotorMcu
   * @static
   * @param {IReqResetMotorMcu} message ReqResetMotorMcu message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqResetMotorMcu.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqResetMotorMcu message, length delimited. Does not implicitly {@link ReqResetMotorMcu.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqResetMotorMcu
   * @static
   * @param {IReqResetMotorMcu} message ReqResetMotorMcu message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqResetMotorMcu.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqResetMotorMcu message from the specified reader or buffer.
   * @function decode
   * @memberof ReqResetMotorMcu
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqResetMotorMcu} ReqResetMotorMcu
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqResetMotorMcu.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqResetMotorMcu();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqResetMotorMcu message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqResetMotorMcu
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqResetMotorMcu} ReqResetMotorMcu
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqResetMotorMcu.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqResetMotorMcu message.
   * @function verify
   * @memberof ReqResetMotorMcu
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqResetMotorMcu.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqResetMotorMcu message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqResetMotorMcu
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqResetMotorMcu} ReqResetMotorMcu
   */
  ReqResetMotorMcu.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqResetMotorMcu) return object;
    return new $root.ReqResetMotorMcu();
  };

  /**
   * Creates a plain object from a ReqResetMotorMcu message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqResetMotorMcu
   * @static
   * @param {ReqResetMotorMcu} message ReqResetMotorMcu
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqResetMotorMcu.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqResetMotorMcu to JSON.
   * @function toJSON
   * @memberof ReqResetMotorMcu
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqResetMotorMcu.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqResetMotorMcu
   * @function getTypeUrl
   * @memberof ReqResetMotorMcu
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqResetMotorMcu.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqResetMotorMcu";
  };

  return ReqResetMotorMcu;
})();

$root.ResErrorTeststart = (function () {
  /**
   * Properties of a ResErrorTeststart.
   * @exports IResErrorTeststart
   * @interface IResErrorTeststart
   */

  /**
   * Constructs a new ResErrorTeststart.
   * @exports ResErrorTeststart
   * @classdesc Represents a ResErrorTeststart.
   * @implements IResErrorTeststart
   * @constructor
   * @param {IResErrorTeststart=} [properties] Properties to set
   */
  function ResErrorTeststart(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ResErrorTeststart instance using the specified properties.
   * @function create
   * @memberof ResErrorTeststart
   * @static
   * @param {IResErrorTeststart=} [properties] Properties to set
   * @returns {ResErrorTeststart} ResErrorTeststart instance
   */
  ResErrorTeststart.create = function create(properties) {
    return new ResErrorTeststart(properties);
  };

  /**
   * Encodes the specified ResErrorTeststart message. Does not implicitly {@link ResErrorTeststart.verify|verify} messages.
   * @function encode
   * @memberof ResErrorTeststart
   * @static
   * @param {IResErrorTeststart} message ResErrorTeststart message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResErrorTeststart.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ResErrorTeststart message, length delimited. Does not implicitly {@link ResErrorTeststart.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResErrorTeststart
   * @static
   * @param {IResErrorTeststart} message ResErrorTeststart message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResErrorTeststart.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResErrorTeststart message from the specified reader or buffer.
   * @function decode
   * @memberof ResErrorTeststart
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResErrorTeststart} ResErrorTeststart
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResErrorTeststart.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResErrorTeststart();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResErrorTeststart message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResErrorTeststart
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResErrorTeststart} ResErrorTeststart
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResErrorTeststart.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResErrorTeststart message.
   * @function verify
   * @memberof ResErrorTeststart
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResErrorTeststart.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ResErrorTeststart message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResErrorTeststart
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResErrorTeststart} ResErrorTeststart
   */
  ResErrorTeststart.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResErrorTeststart) return object;
    return new $root.ResErrorTeststart();
  };

  /**
   * Creates a plain object from a ResErrorTeststart message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResErrorTeststart
   * @static
   * @param {ResErrorTeststart} message ResErrorTeststart
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResErrorTeststart.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ResErrorTeststart to JSON.
   * @function toJSON
   * @memberof ResErrorTeststart
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResErrorTeststart.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResErrorTeststart
   * @function getTypeUrl
   * @memberof ResErrorTeststart
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResErrorTeststart.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResErrorTeststart";
  };

  return ResErrorTeststart;
})();

$root.ResErrorTestState = (function () {
  /**
   * Properties of a ResErrorTestState.
   * @exports IResErrorTestState
   * @interface IResErrorTestState
   * @property {number|null} [state] ResErrorTestState state
   * @property {number|null} [angle] ResErrorTestState angle
   * @property {number|null} [dir] ResErrorTestState dir
   */

  /**
   * Constructs a new ResErrorTestState.
   * @exports ResErrorTestState
   * @classdesc Represents a ResErrorTestState.
   * @implements IResErrorTestState
   * @constructor
   * @param {IResErrorTestState=} [properties] Properties to set
   */
  function ResErrorTestState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResErrorTestState state.
   * @member {number} state
   * @memberof ResErrorTestState
   * @instance
   */
  ResErrorTestState.prototype.state = 0;

  /**
   * ResErrorTestState angle.
   * @member {number} angle
   * @memberof ResErrorTestState
   * @instance
   */
  ResErrorTestState.prototype.angle = 0;

  /**
   * ResErrorTestState dir.
   * @member {number} dir
   * @memberof ResErrorTestState
   * @instance
   */
  ResErrorTestState.prototype.dir = 0;

  /**
   * Creates a new ResErrorTestState instance using the specified properties.
   * @function create
   * @memberof ResErrorTestState
   * @static
   * @param {IResErrorTestState=} [properties] Properties to set
   * @returns {ResErrorTestState} ResErrorTestState instance
   */
  ResErrorTestState.create = function create(properties) {
    return new ResErrorTestState(properties);
  };

  /**
   * Encodes the specified ResErrorTestState message. Does not implicitly {@link ResErrorTestState.verify|verify} messages.
   * @function encode
   * @memberof ResErrorTestState
   * @static
   * @param {IResErrorTestState} message ResErrorTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResErrorTestState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
    if (message.angle != null && Object.hasOwnProperty.call(message, "angle"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.angle);
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.dir);
    return writer;
  };

  /**
   * Encodes the specified ResErrorTestState message, length delimited. Does not implicitly {@link ResErrorTestState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResErrorTestState
   * @static
   * @param {IResErrorTestState} message ResErrorTestState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResErrorTestState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResErrorTestState message from the specified reader or buffer.
   * @function decode
   * @memberof ResErrorTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResErrorTestState} ResErrorTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResErrorTestState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResErrorTestState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.state = reader.int32();
          break;
        }
        case 2: {
          message.angle = reader.double();
          break;
        }
        case 3: {
          message.dir = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResErrorTestState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResErrorTestState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResErrorTestState} ResErrorTestState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResErrorTestState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResErrorTestState message.
   * @function verify
   * @memberof ResErrorTestState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResErrorTestState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      if (!$util.isInteger(message.state)) return "state: integer expected";
    if (message.angle != null && Object.hasOwnProperty.call(message, "angle"))
      if (typeof message.angle !== "number") return "angle: number expected";
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      if (!$util.isInteger(message.dir)) return "dir: integer expected";
    return null;
  };

  /**
   * Creates a ResErrorTestState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResErrorTestState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResErrorTestState} ResErrorTestState
   */
  ResErrorTestState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResErrorTestState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResErrorTestState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResErrorTestState();
    if (object.state != null) message.state = object.state | 0;
    if (object.angle != null) message.angle = Number(object.angle);
    if (object.dir != null) message.dir = object.dir | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResErrorTestState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResErrorTestState
   * @static
   * @param {ResErrorTestState} message ResErrorTestState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResErrorTestState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.state = 0;
      object.angle = 0;
      object.dir = 0;
    }
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = message.state;
    if (message.angle != null && Object.hasOwnProperty.call(message, "angle"))
      object.angle =
        options.json && !isFinite(message.angle)
          ? String(message.angle)
          : message.angle;
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      object.dir = message.dir;
    return object;
  };

  /**
   * Converts this ResErrorTestState to JSON.
   * @function toJSON
   * @memberof ResErrorTestState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResErrorTestState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResErrorTestState
   * @function getTypeUrl
   * @memberof ResErrorTestState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResErrorTestState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResErrorTestState";
  };

  return ResErrorTestState;
})();

$root.ReqTestOpenAllCamera = (function () {
  /**
   * Properties of a ReqTestOpenAllCamera.
   * @exports IReqTestOpenAllCamera
   * @interface IReqTestOpenAllCamera
   */

  /**
   * Constructs a new ReqTestOpenAllCamera.
   * @exports ReqTestOpenAllCamera
   * @classdesc Represents a ReqTestOpenAllCamera.
   * @implements IReqTestOpenAllCamera
   * @constructor
   * @param {IReqTestOpenAllCamera=} [properties] Properties to set
   */
  function ReqTestOpenAllCamera(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqTestOpenAllCamera instance using the specified properties.
   * @function create
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {IReqTestOpenAllCamera=} [properties] Properties to set
   * @returns {ReqTestOpenAllCamera} ReqTestOpenAllCamera instance
   */
  ReqTestOpenAllCamera.create = function create(properties) {
    return new ReqTestOpenAllCamera(properties);
  };

  /**
   * Encodes the specified ReqTestOpenAllCamera message. Does not implicitly {@link ReqTestOpenAllCamera.verify|verify} messages.
   * @function encode
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {IReqTestOpenAllCamera} message ReqTestOpenAllCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTestOpenAllCamera.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqTestOpenAllCamera message, length delimited. Does not implicitly {@link ReqTestOpenAllCamera.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {IReqTestOpenAllCamera} message ReqTestOpenAllCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTestOpenAllCamera.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqTestOpenAllCamera message from the specified reader or buffer.
   * @function decode
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqTestOpenAllCamera} ReqTestOpenAllCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTestOpenAllCamera.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqTestOpenAllCamera();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqTestOpenAllCamera message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqTestOpenAllCamera} ReqTestOpenAllCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTestOpenAllCamera.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqTestOpenAllCamera message.
   * @function verify
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqTestOpenAllCamera.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqTestOpenAllCamera message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqTestOpenAllCamera} ReqTestOpenAllCamera
   */
  ReqTestOpenAllCamera.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqTestOpenAllCamera) return object;
    return new $root.ReqTestOpenAllCamera();
  };

  /**
   * Creates a plain object from a ReqTestOpenAllCamera message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {ReqTestOpenAllCamera} message ReqTestOpenAllCamera
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqTestOpenAllCamera.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqTestOpenAllCamera to JSON.
   * @function toJSON
   * @memberof ReqTestOpenAllCamera
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqTestOpenAllCamera.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqTestOpenAllCamera
   * @function getTypeUrl
   * @memberof ReqTestOpenAllCamera
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqTestOpenAllCamera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqTestOpenAllCamera";
  };

  return ReqTestOpenAllCamera;
})();

$root.ReqFactoryTestSuccess = (function () {
  /**
   * Properties of a ReqFactoryTestSuccess.
   * @exports IReqFactoryTestSuccess
   * @interface IReqFactoryTestSuccess
   */

  /**
   * Constructs a new ReqFactoryTestSuccess.
   * @exports ReqFactoryTestSuccess
   * @classdesc Represents a ReqFactoryTestSuccess.
   * @implements IReqFactoryTestSuccess
   * @constructor
   * @param {IReqFactoryTestSuccess=} [properties] Properties to set
   */
  function ReqFactoryTestSuccess(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqFactoryTestSuccess instance using the specified properties.
   * @function create
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {IReqFactoryTestSuccess=} [properties] Properties to set
   * @returns {ReqFactoryTestSuccess} ReqFactoryTestSuccess instance
   */
  ReqFactoryTestSuccess.create = function create(properties) {
    return new ReqFactoryTestSuccess(properties);
  };

  /**
   * Encodes the specified ReqFactoryTestSuccess message. Does not implicitly {@link ReqFactoryTestSuccess.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {IReqFactoryTestSuccess} message ReqFactoryTestSuccess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryTestSuccess.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryTestSuccess message, length delimited. Does not implicitly {@link ReqFactoryTestSuccess.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {IReqFactoryTestSuccess} message ReqFactoryTestSuccess message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryTestSuccess.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryTestSuccess message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryTestSuccess} ReqFactoryTestSuccess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryTestSuccess.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryTestSuccess();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryTestSuccess message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryTestSuccess} ReqFactoryTestSuccess
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryTestSuccess.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryTestSuccess message.
   * @function verify
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryTestSuccess.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqFactoryTestSuccess message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryTestSuccess} ReqFactoryTestSuccess
   */
  ReqFactoryTestSuccess.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryTestSuccess) return object;
    return new $root.ReqFactoryTestSuccess();
  };

  /**
   * Creates a plain object from a ReqFactoryTestSuccess message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {ReqFactoryTestSuccess} message ReqFactoryTestSuccess
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryTestSuccess.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqFactoryTestSuccess to JSON.
   * @function toJSON
   * @memberof ReqFactoryTestSuccess
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryTestSuccess.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryTestSuccess
   * @function getTypeUrl
   * @memberof ReqFactoryTestSuccess
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryTestSuccess.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryTestSuccess";
  };

  return ReqFactoryTestSuccess;
})();

$root.ReqGetRssi = (function () {
  /**
   * Properties of a ReqGetRssi.
   * @exports IReqGetRssi
   * @interface IReqGetRssi
   */

  /**
   * Constructs a new ReqGetRssi.
   * @exports ReqGetRssi
   * @classdesc Represents a ReqGetRssi.
   * @implements IReqGetRssi
   * @constructor
   * @param {IReqGetRssi=} [properties] Properties to set
   */
  function ReqGetRssi(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetRssi instance using the specified properties.
   * @function create
   * @memberof ReqGetRssi
   * @static
   * @param {IReqGetRssi=} [properties] Properties to set
   * @returns {ReqGetRssi} ReqGetRssi instance
   */
  ReqGetRssi.create = function create(properties) {
    return new ReqGetRssi(properties);
  };

  /**
   * Encodes the specified ReqGetRssi message. Does not implicitly {@link ReqGetRssi.verify|verify} messages.
   * @function encode
   * @memberof ReqGetRssi
   * @static
   * @param {IReqGetRssi} message ReqGetRssi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetRssi.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetRssi message, length delimited. Does not implicitly {@link ReqGetRssi.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetRssi
   * @static
   * @param {IReqGetRssi} message ReqGetRssi message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetRssi.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetRssi message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetRssi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetRssi} ReqGetRssi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetRssi.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetRssi();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetRssi message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetRssi
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetRssi} ReqGetRssi
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetRssi.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetRssi message.
   * @function verify
   * @memberof ReqGetRssi
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetRssi.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetRssi message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetRssi
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetRssi} ReqGetRssi
   */
  ReqGetRssi.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetRssi) return object;
    return new $root.ReqGetRssi();
  };

  /**
   * Creates a plain object from a ReqGetRssi message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetRssi
   * @static
   * @param {ReqGetRssi} message ReqGetRssi
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetRssi.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetRssi to JSON.
   * @function toJSON
   * @memberof ReqGetRssi
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetRssi.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetRssi
   * @function getTypeUrl
   * @memberof ReqGetRssi
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetRssi.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetRssi";
  };

  return ReqGetRssi;
})();

$root.ReqGetMCUUART = (function () {
  /**
   * Properties of a ReqGetMCUUART.
   * @exports IReqGetMCUUART
   * @interface IReqGetMCUUART
   */

  /**
   * Constructs a new ReqGetMCUUART.
   * @exports ReqGetMCUUART
   * @classdesc Represents a ReqGetMCUUART.
   * @implements IReqGetMCUUART
   * @constructor
   * @param {IReqGetMCUUART=} [properties] Properties to set
   */
  function ReqGetMCUUART(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetMCUUART instance using the specified properties.
   * @function create
   * @memberof ReqGetMCUUART
   * @static
   * @param {IReqGetMCUUART=} [properties] Properties to set
   * @returns {ReqGetMCUUART} ReqGetMCUUART instance
   */
  ReqGetMCUUART.create = function create(properties) {
    return new ReqGetMCUUART(properties);
  };

  /**
   * Encodes the specified ReqGetMCUUART message. Does not implicitly {@link ReqGetMCUUART.verify|verify} messages.
   * @function encode
   * @memberof ReqGetMCUUART
   * @static
   * @param {IReqGetMCUUART} message ReqGetMCUUART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetMCUUART.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetMCUUART message, length delimited. Does not implicitly {@link ReqGetMCUUART.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetMCUUART
   * @static
   * @param {IReqGetMCUUART} message ReqGetMCUUART message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetMCUUART.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetMCUUART message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetMCUUART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetMCUUART} ReqGetMCUUART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetMCUUART.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetMCUUART();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetMCUUART message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetMCUUART
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetMCUUART} ReqGetMCUUART
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetMCUUART.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetMCUUART message.
   * @function verify
   * @memberof ReqGetMCUUART
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetMCUUART.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetMCUUART message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetMCUUART
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetMCUUART} ReqGetMCUUART
   */
  ReqGetMCUUART.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetMCUUART) return object;
    return new $root.ReqGetMCUUART();
  };

  /**
   * Creates a plain object from a ReqGetMCUUART message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetMCUUART
   * @static
   * @param {ReqGetMCUUART} message ReqGetMCUUART
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetMCUUART.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetMCUUART to JSON.
   * @function toJSON
   * @memberof ReqGetMCUUART
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetMCUUART.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetMCUUART
   * @function getTypeUrl
   * @memberof ReqGetMCUUART
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetMCUUART.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetMCUUART";
  };

  return ReqGetMCUUART;
})();

$root.ReqGetFIRMWAREVERISON = (function () {
  /**
   * Properties of a ReqGetFIRMWAREVERISON.
   * @exports IReqGetFIRMWAREVERISON
   * @interface IReqGetFIRMWAREVERISON
   */

  /**
   * Constructs a new ReqGetFIRMWAREVERISON.
   * @exports ReqGetFIRMWAREVERISON
   * @classdesc Represents a ReqGetFIRMWAREVERISON.
   * @implements IReqGetFIRMWAREVERISON
   * @constructor
   * @param {IReqGetFIRMWAREVERISON=} [properties] Properties to set
   */
  function ReqGetFIRMWAREVERISON(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetFIRMWAREVERISON instance using the specified properties.
   * @function create
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {IReqGetFIRMWAREVERISON=} [properties] Properties to set
   * @returns {ReqGetFIRMWAREVERISON} ReqGetFIRMWAREVERISON instance
   */
  ReqGetFIRMWAREVERISON.create = function create(properties) {
    return new ReqGetFIRMWAREVERISON(properties);
  };

  /**
   * Encodes the specified ReqGetFIRMWAREVERISON message. Does not implicitly {@link ReqGetFIRMWAREVERISON.verify|verify} messages.
   * @function encode
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {IReqGetFIRMWAREVERISON} message ReqGetFIRMWAREVERISON message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetFIRMWAREVERISON.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetFIRMWAREVERISON message, length delimited. Does not implicitly {@link ReqGetFIRMWAREVERISON.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {IReqGetFIRMWAREVERISON} message ReqGetFIRMWAREVERISON message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetFIRMWAREVERISON.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetFIRMWAREVERISON message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetFIRMWAREVERISON} ReqGetFIRMWAREVERISON
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetFIRMWAREVERISON.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetFIRMWAREVERISON();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetFIRMWAREVERISON message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetFIRMWAREVERISON} ReqGetFIRMWAREVERISON
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetFIRMWAREVERISON.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetFIRMWAREVERISON message.
   * @function verify
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetFIRMWAREVERISON.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetFIRMWAREVERISON message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetFIRMWAREVERISON} ReqGetFIRMWAREVERISON
   */
  ReqGetFIRMWAREVERISON.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetFIRMWAREVERISON) return object;
    return new $root.ReqGetFIRMWAREVERISON();
  };

  /**
   * Creates a plain object from a ReqGetFIRMWAREVERISON message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {ReqGetFIRMWAREVERISON} message ReqGetFIRMWAREVERISON
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetFIRMWAREVERISON.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetFIRMWAREVERISON to JSON.
   * @function toJSON
   * @memberof ReqGetFIRMWAREVERISON
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetFIRMWAREVERISON.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetFIRMWAREVERISON
   * @function getTypeUrl
   * @memberof ReqGetFIRMWAREVERISON
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetFIRMWAREVERISON.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetFIRMWAREVERISON";
  };

  return ReqGetFIRMWAREVERISON;
})();

$root.ReqTestRgbUart = (function () {
  /**
   * Properties of a ReqTestRgbUart.
   * @exports IReqTestRgbUart
   * @interface IReqTestRgbUart
   * @property {number|null} [count] ReqTestRgbUart count
   */

  /**
   * Constructs a new ReqTestRgbUart.
   * @exports ReqTestRgbUart
   * @classdesc Represents a ReqTestRgbUart.
   * @implements IReqTestRgbUart
   * @constructor
   * @param {IReqTestRgbUart=} [properties] Properties to set
   */
  function ReqTestRgbUart(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqTestRgbUart count.
   * @member {number} count
   * @memberof ReqTestRgbUart
   * @instance
   */
  ReqTestRgbUart.prototype.count = 0;

  /**
   * Creates a new ReqTestRgbUart instance using the specified properties.
   * @function create
   * @memberof ReqTestRgbUart
   * @static
   * @param {IReqTestRgbUart=} [properties] Properties to set
   * @returns {ReqTestRgbUart} ReqTestRgbUart instance
   */
  ReqTestRgbUart.create = function create(properties) {
    return new ReqTestRgbUart(properties);
  };

  /**
   * Encodes the specified ReqTestRgbUart message. Does not implicitly {@link ReqTestRgbUart.verify|verify} messages.
   * @function encode
   * @memberof ReqTestRgbUart
   * @static
   * @param {IReqTestRgbUart} message ReqTestRgbUart message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTestRgbUart.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.count);
    return writer;
  };

  /**
   * Encodes the specified ReqTestRgbUart message, length delimited. Does not implicitly {@link ReqTestRgbUart.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqTestRgbUart
   * @static
   * @param {IReqTestRgbUart} message ReqTestRgbUart message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqTestRgbUart.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqTestRgbUart message from the specified reader or buffer.
   * @function decode
   * @memberof ReqTestRgbUart
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqTestRgbUart} ReqTestRgbUart
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTestRgbUart.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqTestRgbUart();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.count = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqTestRgbUart message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqTestRgbUart
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqTestRgbUart} ReqTestRgbUart
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqTestRgbUart.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqTestRgbUart message.
   * @function verify
   * @memberof ReqTestRgbUart
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqTestRgbUart.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      if (!$util.isInteger(message.count)) return "count: integer expected";
    return null;
  };

  /**
   * Creates a ReqTestRgbUart message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqTestRgbUart
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqTestRgbUart} ReqTestRgbUart
   */
  ReqTestRgbUart.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqTestRgbUart) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqTestRgbUart: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqTestRgbUart();
    if (object.count != null) message.count = object.count | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqTestRgbUart message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqTestRgbUart
   * @static
   * @param {ReqTestRgbUart} message ReqTestRgbUart
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqTestRgbUart.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.count = 0;
    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
      object.count = message.count;
    return object;
  };

  /**
   * Converts this ReqTestRgbUart to JSON.
   * @function toJSON
   * @memberof ReqTestRgbUart
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqTestRgbUart.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqTestRgbUart
   * @function getTypeUrl
   * @memberof ReqTestRgbUart
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqTestRgbUart.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqTestRgbUart";
  };

  return ReqTestRgbUart;
})();

$root.ReqPasswordEncryption = (function () {
  /**
   * Properties of a ReqPasswordEncryption.
   * @exports IReqPasswordEncryption
   * @interface IReqPasswordEncryption
   */

  /**
   * Constructs a new ReqPasswordEncryption.
   * @exports ReqPasswordEncryption
   * @classdesc Represents a ReqPasswordEncryption.
   * @implements IReqPasswordEncryption
   * @constructor
   * @param {IReqPasswordEncryption=} [properties] Properties to set
   */
  function ReqPasswordEncryption(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPasswordEncryption instance using the specified properties.
   * @function create
   * @memberof ReqPasswordEncryption
   * @static
   * @param {IReqPasswordEncryption=} [properties] Properties to set
   * @returns {ReqPasswordEncryption} ReqPasswordEncryption instance
   */
  ReqPasswordEncryption.create = function create(properties) {
    return new ReqPasswordEncryption(properties);
  };

  /**
   * Encodes the specified ReqPasswordEncryption message. Does not implicitly {@link ReqPasswordEncryption.verify|verify} messages.
   * @function encode
   * @memberof ReqPasswordEncryption
   * @static
   * @param {IReqPasswordEncryption} message ReqPasswordEncryption message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPasswordEncryption.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPasswordEncryption message, length delimited. Does not implicitly {@link ReqPasswordEncryption.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPasswordEncryption
   * @static
   * @param {IReqPasswordEncryption} message ReqPasswordEncryption message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPasswordEncryption.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPasswordEncryption message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPasswordEncryption
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPasswordEncryption} ReqPasswordEncryption
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPasswordEncryption.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPasswordEncryption();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPasswordEncryption message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPasswordEncryption
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPasswordEncryption} ReqPasswordEncryption
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPasswordEncryption.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPasswordEncryption message.
   * @function verify
   * @memberof ReqPasswordEncryption
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPasswordEncryption.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPasswordEncryption message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPasswordEncryption
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPasswordEncryption} ReqPasswordEncryption
   */
  ReqPasswordEncryption.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPasswordEncryption) return object;
    return new $root.ReqPasswordEncryption();
  };

  /**
   * Creates a plain object from a ReqPasswordEncryption message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPasswordEncryption
   * @static
   * @param {ReqPasswordEncryption} message ReqPasswordEncryption
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPasswordEncryption.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPasswordEncryption to JSON.
   * @function toJSON
   * @memberof ReqPasswordEncryption
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPasswordEncryption.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPasswordEncryption
   * @function getTypeUrl
   * @memberof ReqPasswordEncryption
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPasswordEncryption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPasswordEncryption";
  };

  return ReqPasswordEncryption;
})();

$root.ReqPasswordEncryptionState = (function () {
  /**
   * Properties of a ReqPasswordEncryptionState.
   * @exports IReqPasswordEncryptionState
   * @interface IReqPasswordEncryptionState
   */

  /**
   * Constructs a new ReqPasswordEncryptionState.
   * @exports ReqPasswordEncryptionState
   * @classdesc Represents a ReqPasswordEncryptionState.
   * @implements IReqPasswordEncryptionState
   * @constructor
   * @param {IReqPasswordEncryptionState=} [properties] Properties to set
   */
  function ReqPasswordEncryptionState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPasswordEncryptionState instance using the specified properties.
   * @function create
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {IReqPasswordEncryptionState=} [properties] Properties to set
   * @returns {ReqPasswordEncryptionState} ReqPasswordEncryptionState instance
   */
  ReqPasswordEncryptionState.create = function create(properties) {
    return new ReqPasswordEncryptionState(properties);
  };

  /**
   * Encodes the specified ReqPasswordEncryptionState message. Does not implicitly {@link ReqPasswordEncryptionState.verify|verify} messages.
   * @function encode
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {IReqPasswordEncryptionState} message ReqPasswordEncryptionState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPasswordEncryptionState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPasswordEncryptionState message, length delimited. Does not implicitly {@link ReqPasswordEncryptionState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {IReqPasswordEncryptionState} message ReqPasswordEncryptionState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPasswordEncryptionState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPasswordEncryptionState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPasswordEncryptionState} ReqPasswordEncryptionState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPasswordEncryptionState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPasswordEncryptionState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPasswordEncryptionState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPasswordEncryptionState} ReqPasswordEncryptionState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPasswordEncryptionState.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPasswordEncryptionState message.
   * @function verify
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPasswordEncryptionState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPasswordEncryptionState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPasswordEncryptionState} ReqPasswordEncryptionState
   */
  ReqPasswordEncryptionState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPasswordEncryptionState) return object;
    return new $root.ReqPasswordEncryptionState();
  };

  /**
   * Creates a plain object from a ReqPasswordEncryptionState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {ReqPasswordEncryptionState} message ReqPasswordEncryptionState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPasswordEncryptionState.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPasswordEncryptionState to JSON.
   * @function toJSON
   * @memberof ReqPasswordEncryptionState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPasswordEncryptionState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPasswordEncryptionState
   * @function getTypeUrl
   * @memberof ReqPasswordEncryptionState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPasswordEncryptionState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPasswordEncryptionState";
  };

  return ReqPasswordEncryptionState;
})();

$root.ReqRestPasswordEncryption = (function () {
  /**
   * Properties of a ReqRestPasswordEncryption.
   * @exports IReqRestPasswordEncryption
   * @interface IReqRestPasswordEncryption
   */

  /**
   * Constructs a new ReqRestPasswordEncryption.
   * @exports ReqRestPasswordEncryption
   * @classdesc Represents a ReqRestPasswordEncryption.
   * @implements IReqRestPasswordEncryption
   * @constructor
   * @param {IReqRestPasswordEncryption=} [properties] Properties to set
   */
  function ReqRestPasswordEncryption(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqRestPasswordEncryption instance using the specified properties.
   * @function create
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {IReqRestPasswordEncryption=} [properties] Properties to set
   * @returns {ReqRestPasswordEncryption} ReqRestPasswordEncryption instance
   */
  ReqRestPasswordEncryption.create = function create(properties) {
    return new ReqRestPasswordEncryption(properties);
  };

  /**
   * Encodes the specified ReqRestPasswordEncryption message. Does not implicitly {@link ReqRestPasswordEncryption.verify|verify} messages.
   * @function encode
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {IReqRestPasswordEncryption} message ReqRestPasswordEncryption message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqRestPasswordEncryption.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqRestPasswordEncryption message, length delimited. Does not implicitly {@link ReqRestPasswordEncryption.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {IReqRestPasswordEncryption} message ReqRestPasswordEncryption message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqRestPasswordEncryption.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqRestPasswordEncryption message from the specified reader or buffer.
   * @function decode
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqRestPasswordEncryption} ReqRestPasswordEncryption
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqRestPasswordEncryption.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqRestPasswordEncryption();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqRestPasswordEncryption message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqRestPasswordEncryption} ReqRestPasswordEncryption
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqRestPasswordEncryption.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqRestPasswordEncryption message.
   * @function verify
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqRestPasswordEncryption.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqRestPasswordEncryption message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqRestPasswordEncryption} ReqRestPasswordEncryption
   */
  ReqRestPasswordEncryption.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqRestPasswordEncryption) return object;
    return new $root.ReqRestPasswordEncryption();
  };

  /**
   * Creates a plain object from a ReqRestPasswordEncryption message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {ReqRestPasswordEncryption} message ReqRestPasswordEncryption
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqRestPasswordEncryption.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqRestPasswordEncryption to JSON.
   * @function toJSON
   * @memberof ReqRestPasswordEncryption
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqRestPasswordEncryption.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqRestPasswordEncryption
   * @function getTypeUrl
   * @memberof ReqRestPasswordEncryption
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqRestPasswordEncryption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqRestPasswordEncryption";
  };

  return ReqRestPasswordEncryption;
})();

$root.ReqFactoryTecState = (function () {
  /**
   * Properties of a ReqFactoryTecState.
   * @exports IReqFactoryTecState
   * @interface IReqFactoryTecState
   * @property {boolean|null} [enable] ReqFactoryTecState enable
   * @property {number|null} [speed] ReqFactoryTecState speed
   * @property {number|null} [time] ReqFactoryTecState time
   */

  /**
   * Constructs a new ReqFactoryTecState.
   * @exports ReqFactoryTecState
   * @classdesc Represents a ReqFactoryTecState.
   * @implements IReqFactoryTecState
   * @constructor
   * @param {IReqFactoryTecState=} [properties] Properties to set
   */
  function ReqFactoryTecState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryTecState enable.
   * @member {boolean} enable
   * @memberof ReqFactoryTecState
   * @instance
   */
  ReqFactoryTecState.prototype.enable = false;

  /**
   * ReqFactoryTecState speed.
   * @member {number} speed
   * @memberof ReqFactoryTecState
   * @instance
   */
  ReqFactoryTecState.prototype.speed = 0;

  /**
   * ReqFactoryTecState time.
   * @member {number} time
   * @memberof ReqFactoryTecState
   * @instance
   */
  ReqFactoryTecState.prototype.time = 0;

  /**
   * Creates a new ReqFactoryTecState instance using the specified properties.
   * @function create
   * @memberof ReqFactoryTecState
   * @static
   * @param {IReqFactoryTecState=} [properties] Properties to set
   * @returns {ReqFactoryTecState} ReqFactoryTecState instance
   */
  ReqFactoryTecState.create = function create(properties) {
    return new ReqFactoryTecState(properties);
  };

  /**
   * Encodes the specified ReqFactoryTecState message. Does not implicitly {@link ReqFactoryTecState.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryTecState
   * @static
   * @param {IReqFactoryTecState} message ReqFactoryTecState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryTecState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.speed);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.time);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryTecState message, length delimited. Does not implicitly {@link ReqFactoryTecState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryTecState
   * @static
   * @param {IReqFactoryTecState} message ReqFactoryTecState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryTecState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryTecState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryTecState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryTecState} ReqFactoryTecState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryTecState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryTecState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.speed = reader.int32();
          break;
        }
        case 3: {
          message.time = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryTecState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryTecState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryTecState} ReqFactoryTecState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryTecState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryTecState message.
   * @function verify
   * @memberof ReqFactoryTecState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryTecState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (!$util.isInteger(message.speed)) return "speed: integer expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryTecState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryTecState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryTecState} ReqFactoryTecState
   */
  ReqFactoryTecState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryTecState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryTecState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryTecState();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.speed != null) message.speed = object.speed | 0;
    if (object.time != null) message.time = object.time | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryTecState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryTecState
   * @static
   * @param {ReqFactoryTecState} message ReqFactoryTecState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryTecState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.speed = 0;
      object.time = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed = message.speed;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    return object;
  };

  /**
   * Converts this ReqFactoryTecState to JSON.
   * @function toJSON
   * @memberof ReqFactoryTecState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryTecState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryTecState
   * @function getTypeUrl
   * @memberof ReqFactoryTecState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryTecState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryTecState";
  };

  return ReqFactoryTecState;
})();

$root.ReqFactoryFanState = (function () {
  /**
   * Properties of a ReqFactoryFanState.
   * @exports IReqFactoryFanState
   * @interface IReqFactoryFanState
   * @property {boolean|null} [enable] ReqFactoryFanState enable
   * @property {number|null} [speed] ReqFactoryFanState speed
   * @property {number|null} [time] ReqFactoryFanState time
   */

  /**
   * Constructs a new ReqFactoryFanState.
   * @exports ReqFactoryFanState
   * @classdesc Represents a ReqFactoryFanState.
   * @implements IReqFactoryFanState
   * @constructor
   * @param {IReqFactoryFanState=} [properties] Properties to set
   */
  function ReqFactoryFanState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryFanState enable.
   * @member {boolean} enable
   * @memberof ReqFactoryFanState
   * @instance
   */
  ReqFactoryFanState.prototype.enable = false;

  /**
   * ReqFactoryFanState speed.
   * @member {number} speed
   * @memberof ReqFactoryFanState
   * @instance
   */
  ReqFactoryFanState.prototype.speed = 0;

  /**
   * ReqFactoryFanState time.
   * @member {number} time
   * @memberof ReqFactoryFanState
   * @instance
   */
  ReqFactoryFanState.prototype.time = 0;

  /**
   * Creates a new ReqFactoryFanState instance using the specified properties.
   * @function create
   * @memberof ReqFactoryFanState
   * @static
   * @param {IReqFactoryFanState=} [properties] Properties to set
   * @returns {ReqFactoryFanState} ReqFactoryFanState instance
   */
  ReqFactoryFanState.create = function create(properties) {
    return new ReqFactoryFanState(properties);
  };

  /**
   * Encodes the specified ReqFactoryFanState message. Does not implicitly {@link ReqFactoryFanState.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryFanState
   * @static
   * @param {IReqFactoryFanState} message ReqFactoryFanState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryFanState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.speed);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.time);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryFanState message, length delimited. Does not implicitly {@link ReqFactoryFanState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryFanState
   * @static
   * @param {IReqFactoryFanState} message ReqFactoryFanState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryFanState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryFanState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryFanState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryFanState} ReqFactoryFanState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryFanState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryFanState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.speed = reader.int32();
          break;
        }
        case 3: {
          message.time = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryFanState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryFanState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryFanState} ReqFactoryFanState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryFanState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryFanState message.
   * @function verify
   * @memberof ReqFactoryFanState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryFanState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (!$util.isInteger(message.speed)) return "speed: integer expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryFanState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryFanState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryFanState} ReqFactoryFanState
   */
  ReqFactoryFanState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryFanState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryFanState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryFanState();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.speed != null) message.speed = object.speed | 0;
    if (object.time != null) message.time = object.time | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryFanState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryFanState
   * @static
   * @param {ReqFactoryFanState} message ReqFactoryFanState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryFanState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.speed = 0;
      object.time = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed = message.speed;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    return object;
  };

  /**
   * Converts this ReqFactoryFanState to JSON.
   * @function toJSON
   * @memberof ReqFactoryFanState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryFanState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryFanState
   * @function getTypeUrl
   * @memberof ReqFactoryFanState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryFanState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryFanState";
  };

  return ReqFactoryFanState;
})();

$root.ReqFactoryHeadTapeState = (function () {
  /**
   * Properties of a ReqFactoryHeadTapeState.
   * @exports IReqFactoryHeadTapeState
   * @interface IReqFactoryHeadTapeState
   * @property {boolean|null} [enable] ReqFactoryHeadTapeState enable
   * @property {number|null} [time] ReqFactoryHeadTapeState time
   */

  /**
   * Constructs a new ReqFactoryHeadTapeState.
   * @exports ReqFactoryHeadTapeState
   * @classdesc Represents a ReqFactoryHeadTapeState.
   * @implements IReqFactoryHeadTapeState
   * @constructor
   * @param {IReqFactoryHeadTapeState=} [properties] Properties to set
   */
  function ReqFactoryHeadTapeState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryHeadTapeState enable.
   * @member {boolean} enable
   * @memberof ReqFactoryHeadTapeState
   * @instance
   */
  ReqFactoryHeadTapeState.prototype.enable = false;

  /**
   * ReqFactoryHeadTapeState time.
   * @member {number} time
   * @memberof ReqFactoryHeadTapeState
   * @instance
   */
  ReqFactoryHeadTapeState.prototype.time = 0;

  /**
   * Creates a new ReqFactoryHeadTapeState instance using the specified properties.
   * @function create
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {IReqFactoryHeadTapeState=} [properties] Properties to set
   * @returns {ReqFactoryHeadTapeState} ReqFactoryHeadTapeState instance
   */
  ReqFactoryHeadTapeState.create = function create(properties) {
    return new ReqFactoryHeadTapeState(properties);
  };

  /**
   * Encodes the specified ReqFactoryHeadTapeState message. Does not implicitly {@link ReqFactoryHeadTapeState.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {IReqFactoryHeadTapeState} message ReqFactoryHeadTapeState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryHeadTapeState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.time);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryHeadTapeState message, length delimited. Does not implicitly {@link ReqFactoryHeadTapeState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {IReqFactoryHeadTapeState} message ReqFactoryHeadTapeState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryHeadTapeState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryHeadTapeState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryHeadTapeState} ReqFactoryHeadTapeState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryHeadTapeState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryHeadTapeState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        case 2: {
          message.time = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryHeadTapeState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryHeadTapeState} ReqFactoryHeadTapeState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryHeadTapeState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryHeadTapeState message.
   * @function verify
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryHeadTapeState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      if (!$util.isInteger(message.time)) return "time: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryHeadTapeState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryHeadTapeState} ReqFactoryHeadTapeState
   */
  ReqFactoryHeadTapeState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryHeadTapeState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryHeadTapeState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryHeadTapeState();
    if (object.enable != null) message.enable = Boolean(object.enable);
    if (object.time != null) message.time = object.time | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryHeadTapeState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {ReqFactoryHeadTapeState} message ReqFactoryHeadTapeState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryHeadTapeState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.enable = false;
      object.time = 0;
    }
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
      object.time = message.time;
    return object;
  };

  /**
   * Converts this ReqFactoryHeadTapeState to JSON.
   * @function toJSON
   * @memberof ReqFactoryHeadTapeState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryHeadTapeState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryHeadTapeState
   * @function getTypeUrl
   * @memberof ReqFactoryHeadTapeState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryHeadTapeState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryHeadTapeState";
  };

  return ReqFactoryHeadTapeState;
})();

$root.ReqSetGyroCal = (function () {
  /**
   * Properties of a ReqSetGyroCal.
   * @exports IReqSetGyroCal
   * @interface IReqSetGyroCal
   * @property {boolean|null} [enable] ReqSetGyroCal enable
   */

  /**
   * Constructs a new ReqSetGyroCal.
   * @exports ReqSetGyroCal
   * @classdesc Represents a ReqSetGyroCal.
   * @implements IReqSetGyroCal
   * @constructor
   * @param {IReqSetGyroCal=} [properties] Properties to set
   */
  function ReqSetGyroCal(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetGyroCal enable.
   * @member {boolean} enable
   * @memberof ReqSetGyroCal
   * @instance
   */
  ReqSetGyroCal.prototype.enable = false;

  /**
   * Creates a new ReqSetGyroCal instance using the specified properties.
   * @function create
   * @memberof ReqSetGyroCal
   * @static
   * @param {IReqSetGyroCal=} [properties] Properties to set
   * @returns {ReqSetGyroCal} ReqSetGyroCal instance
   */
  ReqSetGyroCal.create = function create(properties) {
    return new ReqSetGyroCal(properties);
  };

  /**
   * Encodes the specified ReqSetGyroCal message. Does not implicitly {@link ReqSetGyroCal.verify|verify} messages.
   * @function encode
   * @memberof ReqSetGyroCal
   * @static
   * @param {IReqSetGyroCal} message ReqSetGyroCal message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGyroCal.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.enable);
    return writer;
  };

  /**
   * Encodes the specified ReqSetGyroCal message, length delimited. Does not implicitly {@link ReqSetGyroCal.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetGyroCal
   * @static
   * @param {IReqSetGyroCal} message ReqSetGyroCal message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetGyroCal.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetGyroCal message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetGyroCal
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetGyroCal} ReqSetGyroCal
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGyroCal.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetGyroCal();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.enable = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetGyroCal message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetGyroCal
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetGyroCal} ReqSetGyroCal
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetGyroCal.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetGyroCal message.
   * @function verify
   * @memberof ReqSetGyroCal
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetGyroCal.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    return null;
  };

  /**
   * Creates a ReqSetGyroCal message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetGyroCal
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetGyroCal} ReqSetGyroCal
   */
  ReqSetGyroCal.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetGyroCal) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetGyroCal: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetGyroCal();
    if (object.enable != null) message.enable = Boolean(object.enable);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetGyroCal message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetGyroCal
   * @static
   * @param {ReqSetGyroCal} message ReqSetGyroCal
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetGyroCal.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.enable = false;
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    return object;
  };

  /**
   * Converts this ReqSetGyroCal to JSON.
   * @function toJSON
   * @memberof ReqSetGyroCal
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetGyroCal.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetGyroCal
   * @function getTypeUrl
   * @memberof ReqSetGyroCal
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetGyroCal.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetGyroCal";
  };

  return ReqSetGyroCal;
})();

$root.ReqFactoryGyroAttitude = (function () {
  /**
   * Properties of a ReqFactoryGyroAttitude.
   * @exports IReqFactoryGyroAttitude
   * @interface IReqFactoryGyroAttitude
   * @property {number|null} [mode] ReqFactoryGyroAttitude mode
   */

  /**
   * Constructs a new ReqFactoryGyroAttitude.
   * @exports ReqFactoryGyroAttitude
   * @classdesc Represents a ReqFactoryGyroAttitude.
   * @implements IReqFactoryGyroAttitude
   * @constructor
   * @param {IReqFactoryGyroAttitude=} [properties] Properties to set
   */
  function ReqFactoryGyroAttitude(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryGyroAttitude mode.
   * @member {number} mode
   * @memberof ReqFactoryGyroAttitude
   * @instance
   */
  ReqFactoryGyroAttitude.prototype.mode = 0;

  /**
   * Creates a new ReqFactoryGyroAttitude instance using the specified properties.
   * @function create
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {IReqFactoryGyroAttitude=} [properties] Properties to set
   * @returns {ReqFactoryGyroAttitude} ReqFactoryGyroAttitude instance
   */
  ReqFactoryGyroAttitude.create = function create(properties) {
    return new ReqFactoryGyroAttitude(properties);
  };

  /**
   * Encodes the specified ReqFactoryGyroAttitude message. Does not implicitly {@link ReqFactoryGyroAttitude.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {IReqFactoryGyroAttitude} message ReqFactoryGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryGyroAttitude.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryGyroAttitude message, length delimited. Does not implicitly {@link ReqFactoryGyroAttitude.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {IReqFactoryGyroAttitude} message ReqFactoryGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryGyroAttitude.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryGyroAttitude message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryGyroAttitude} ReqFactoryGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryGyroAttitude.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryGyroAttitude();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryGyroAttitude message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryGyroAttitude} ReqFactoryGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryGyroAttitude.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryGyroAttitude message.
   * @function verify
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryGyroAttitude.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryGyroAttitude message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryGyroAttitude} ReqFactoryGyroAttitude
   */
  ReqFactoryGyroAttitude.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryGyroAttitude) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryGyroAttitude: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryGyroAttitude();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryGyroAttitude message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {ReqFactoryGyroAttitude} message ReqFactoryGyroAttitude
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryGyroAttitude.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqFactoryGyroAttitude to JSON.
   * @function toJSON
   * @memberof ReqFactoryGyroAttitude
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryGyroAttitude.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryGyroAttitude
   * @function getTypeUrl
   * @memberof ReqFactoryGyroAttitude
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryGyroAttitude.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryGyroAttitude";
  };

  return ReqFactoryGyroAttitude;
})();

$root.ResFactoryGyroAttitude = (function () {
  /**
   * Properties of a ResFactoryGyroAttitude.
   * @exports IResFactoryGyroAttitude
   * @interface IResFactoryGyroAttitude
   * @property {number|null} [cmd] ResFactoryGyroAttitude cmd
   * @property {number|null} [code] ResFactoryGyroAttitude code
   * @property {number|null} [pitch] ResFactoryGyroAttitude pitch
   * @property {number|null} [yaw] ResFactoryGyroAttitude yaw
   * @property {number|null} [roll] ResFactoryGyroAttitude roll
   */

  /**
   * Constructs a new ResFactoryGyroAttitude.
   * @exports ResFactoryGyroAttitude
   * @classdesc Represents a ResFactoryGyroAttitude.
   * @implements IResFactoryGyroAttitude
   * @constructor
   * @param {IResFactoryGyroAttitude=} [properties] Properties to set
   */
  function ResFactoryGyroAttitude(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResFactoryGyroAttitude cmd.
   * @member {number} cmd
   * @memberof ResFactoryGyroAttitude
   * @instance
   */
  ResFactoryGyroAttitude.prototype.cmd = 0;

  /**
   * ResFactoryGyroAttitude code.
   * @member {number} code
   * @memberof ResFactoryGyroAttitude
   * @instance
   */
  ResFactoryGyroAttitude.prototype.code = 0;

  /**
   * ResFactoryGyroAttitude pitch.
   * @member {number} pitch
   * @memberof ResFactoryGyroAttitude
   * @instance
   */
  ResFactoryGyroAttitude.prototype.pitch = 0;

  /**
   * ResFactoryGyroAttitude yaw.
   * @member {number} yaw
   * @memberof ResFactoryGyroAttitude
   * @instance
   */
  ResFactoryGyroAttitude.prototype.yaw = 0;

  /**
   * ResFactoryGyroAttitude roll.
   * @member {number} roll
   * @memberof ResFactoryGyroAttitude
   * @instance
   */
  ResFactoryGyroAttitude.prototype.roll = 0;

  /**
   * Creates a new ResFactoryGyroAttitude instance using the specified properties.
   * @function create
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {IResFactoryGyroAttitude=} [properties] Properties to set
   * @returns {ResFactoryGyroAttitude} ResFactoryGyroAttitude instance
   */
  ResFactoryGyroAttitude.create = function create(properties) {
    return new ResFactoryGyroAttitude(properties);
  };

  /**
   * Encodes the specified ResFactoryGyroAttitude message. Does not implicitly {@link ResFactoryGyroAttitude.verify|verify} messages.
   * @function encode
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {IResFactoryGyroAttitude} message ResFactoryGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryGyroAttitude.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cmd);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.pitch);
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.yaw);
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      writer.uint32(/* id 5, wireType 1 =*/ 41).double(message.roll);
    return writer;
  };

  /**
   * Encodes the specified ResFactoryGyroAttitude message, length delimited. Does not implicitly {@link ResFactoryGyroAttitude.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {IResFactoryGyroAttitude} message ResFactoryGyroAttitude message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryGyroAttitude.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResFactoryGyroAttitude message from the specified reader or buffer.
   * @function decode
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResFactoryGyroAttitude} ResFactoryGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryGyroAttitude.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResFactoryGyroAttitude();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.cmd = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.pitch = reader.double();
          break;
        }
        case 4: {
          message.yaw = reader.double();
          break;
        }
        case 5: {
          message.roll = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResFactoryGyroAttitude message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResFactoryGyroAttitude} ResFactoryGyroAttitude
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryGyroAttitude.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResFactoryGyroAttitude message.
   * @function verify
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResFactoryGyroAttitude.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      if (!$util.isInteger(message.cmd)) return "cmd: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      if (typeof message.pitch !== "number") return "pitch: number expected";
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      if (typeof message.yaw !== "number") return "yaw: number expected";
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      if (typeof message.roll !== "number") return "roll: number expected";
    return null;
  };

  /**
   * Creates a ResFactoryGyroAttitude message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResFactoryGyroAttitude} ResFactoryGyroAttitude
   */
  ResFactoryGyroAttitude.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResFactoryGyroAttitude) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResFactoryGyroAttitude: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResFactoryGyroAttitude();
    if (object.cmd != null) message.cmd = object.cmd | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.pitch != null) message.pitch = Number(object.pitch);
    if (object.yaw != null) message.yaw = Number(object.yaw);
    if (object.roll != null) message.roll = Number(object.roll);
    return message;
  };

  /**
   * Creates a plain object from a ResFactoryGyroAttitude message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {ResFactoryGyroAttitude} message ResFactoryGyroAttitude
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResFactoryGyroAttitude.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.cmd = 0;
      object.code = 0;
      object.pitch = 0;
      object.yaw = 0;
      object.roll = 0;
    }
    if (message.cmd != null && Object.hasOwnProperty.call(message, "cmd"))
      object.cmd = message.cmd;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
      object.pitch =
        options.json && !isFinite(message.pitch)
          ? String(message.pitch)
          : message.pitch;
    if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
      object.yaw =
        options.json && !isFinite(message.yaw)
          ? String(message.yaw)
          : message.yaw;
    if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
      object.roll =
        options.json && !isFinite(message.roll)
          ? String(message.roll)
          : message.roll;
    return object;
  };

  /**
   * Converts this ResFactoryGyroAttitude to JSON.
   * @function toJSON
   * @memberof ResFactoryGyroAttitude
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResFactoryGyroAttitude.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResFactoryGyroAttitude
   * @function getTypeUrl
   * @memberof ResFactoryGyroAttitude
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResFactoryGyroAttitude.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResFactoryGyroAttitude";
  };

  return ResFactoryGyroAttitude;
})();

$root.TemperatureItem = (function () {
  /**
   * Properties of a TemperatureItem.
   * @exports ITemperatureItem
   * @interface ITemperatureItem
   * @property {number|null} [id] TemperatureItem id
   * @property {string|null} [name] TemperatureItem name
   * @property {number|null} [value] TemperatureItem value
   */

  /**
   * Constructs a new TemperatureItem.
   * @exports TemperatureItem
   * @classdesc Represents a TemperatureItem.
   * @implements ITemperatureItem
   * @constructor
   * @param {ITemperatureItem=} [properties] Properties to set
   */
  function TemperatureItem(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * TemperatureItem id.
   * @member {number} id
   * @memberof TemperatureItem
   * @instance
   */
  TemperatureItem.prototype.id = 0;

  /**
   * TemperatureItem name.
   * @member {string} name
   * @memberof TemperatureItem
   * @instance
   */
  TemperatureItem.prototype.name = "";

  /**
   * TemperatureItem value.
   * @member {number} value
   * @memberof TemperatureItem
   * @instance
   */
  TemperatureItem.prototype.value = 0;

  /**
   * Creates a new TemperatureItem instance using the specified properties.
   * @function create
   * @memberof TemperatureItem
   * @static
   * @param {ITemperatureItem=} [properties] Properties to set
   * @returns {TemperatureItem} TemperatureItem instance
   */
  TemperatureItem.create = function create(properties) {
    return new TemperatureItem(properties);
  };

  /**
   * Encodes the specified TemperatureItem message. Does not implicitly {@link TemperatureItem.verify|verify} messages.
   * @function encode
   * @memberof TemperatureItem
   * @static
   * @param {ITemperatureItem} message TemperatureItem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TemperatureItem.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.name);
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.value);
    return writer;
  };

  /**
   * Encodes the specified TemperatureItem message, length delimited. Does not implicitly {@link TemperatureItem.verify|verify} messages.
   * @function encodeDelimited
   * @memberof TemperatureItem
   * @static
   * @param {ITemperatureItem} message TemperatureItem message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TemperatureItem.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a TemperatureItem message from the specified reader or buffer.
   * @function decode
   * @memberof TemperatureItem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {TemperatureItem} TemperatureItem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TemperatureItem.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.TemperatureItem();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.name = reader.string();
          break;
        }
        case 3: {
          message.value = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a TemperatureItem message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof TemperatureItem
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {TemperatureItem} TemperatureItem
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TemperatureItem.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a TemperatureItem message.
   * @function verify
   * @memberof TemperatureItem
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  TemperatureItem.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      if (!$util.isString(message.name)) return "name: string expected";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (typeof message.value !== "number") return "value: number expected";
    return null;
  };

  /**
   * Creates a TemperatureItem message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof TemperatureItem
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {TemperatureItem} TemperatureItem
   */
  TemperatureItem.fromObject = function fromObject(object, long) {
    if (object instanceof $root.TemperatureItem) return object;
    if (!$util.isObject(object))
      throw TypeError(".TemperatureItem: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.TemperatureItem();
    if (object.id != null) message.id = object.id | 0;
    if (object.name != null) message.name = String(object.name);
    if (object.value != null) message.value = Number(object.value);
    return message;
  };

  /**
   * Creates a plain object from a TemperatureItem message. Also converts values to other types if specified.
   * @function toObject
   * @memberof TemperatureItem
   * @static
   * @param {TemperatureItem} message TemperatureItem
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  TemperatureItem.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.name = "";
      object.value = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
      object.name = message.name;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value =
        options.json && !isFinite(message.value)
          ? String(message.value)
          : message.value;
    return object;
  };

  /**
   * Converts this TemperatureItem to JSON.
   * @function toJSON
   * @memberof TemperatureItem
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  TemperatureItem.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for TemperatureItem
   * @function getTypeUrl
   * @memberof TemperatureItem
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  TemperatureItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/TemperatureItem";
  };

  return TemperatureItem;
})();

$root.ResAllTemperature = (function () {
  /**
   * Properties of a ResAllTemperature.
   * @exports IResAllTemperature
   * @interface IResAllTemperature
   * @property {Array.<ITemperatureItem>|null} [temps] ResAllTemperature temps
   * @property {number|null} [code] ResAllTemperature code
   */

  /**
   * Constructs a new ResAllTemperature.
   * @exports ResAllTemperature
   * @classdesc Represents a ResAllTemperature.
   * @implements IResAllTemperature
   * @constructor
   * @param {IResAllTemperature=} [properties] Properties to set
   */
  function ResAllTemperature(properties) {
    this.temps = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResAllTemperature temps.
   * @member {Array.<ITemperatureItem>} temps
   * @memberof ResAllTemperature
   * @instance
   */
  ResAllTemperature.prototype.temps = $util.emptyArray;

  /**
   * ResAllTemperature code.
   * @member {number} code
   * @memberof ResAllTemperature
   * @instance
   */
  ResAllTemperature.prototype.code = 0;

  /**
   * Creates a new ResAllTemperature instance using the specified properties.
   * @function create
   * @memberof ResAllTemperature
   * @static
   * @param {IResAllTemperature=} [properties] Properties to set
   * @returns {ResAllTemperature} ResAllTemperature instance
   */
  ResAllTemperature.create = function create(properties) {
    return new ResAllTemperature(properties);
  };

  /**
   * Encodes the specified ResAllTemperature message. Does not implicitly {@link ResAllTemperature.verify|verify} messages.
   * @function encode
   * @memberof ResAllTemperature
   * @static
   * @param {IResAllTemperature} message ResAllTemperature message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAllTemperature.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.temps != null && message.temps.length)
      for (var i = 0; i < message.temps.length; ++i)
        $root.TemperatureItem.encode(
          message.temps[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResAllTemperature message, length delimited. Does not implicitly {@link ResAllTemperature.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResAllTemperature
   * @static
   * @param {IResAllTemperature} message ResAllTemperature message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResAllTemperature.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResAllTemperature message from the specified reader or buffer.
   * @function decode
   * @memberof ResAllTemperature
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResAllTemperature} ResAllTemperature
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAllTemperature.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResAllTemperature();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.temps && message.temps.length)) message.temps = [];
          message.temps.push(
            $root.TemperatureItem.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResAllTemperature message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResAllTemperature
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResAllTemperature} ResAllTemperature
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResAllTemperature.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResAllTemperature message.
   * @function verify
   * @memberof ResAllTemperature
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResAllTemperature.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.temps != null && Object.hasOwnProperty.call(message, "temps")) {
      if (!Array.isArray(message.temps)) return "temps: array expected";
      for (var i = 0; i < message.temps.length; ++i) {
        var error = $root.TemperatureItem.verify(message.temps[i], long + 1);
        if (error) return "temps." + error;
      }
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResAllTemperature message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResAllTemperature
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResAllTemperature} ResAllTemperature
   */
  ResAllTemperature.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResAllTemperature) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResAllTemperature: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResAllTemperature();
    if (object.temps) {
      if (!Array.isArray(object.temps))
        throw TypeError(".ResAllTemperature.temps: array expected");
      message.temps = [];
      for (var i = 0; i < object.temps.length; ++i) {
        if (!$util.isObject(object.temps[i]))
          throw TypeError(".ResAllTemperature.temps: object expected");
        message.temps[i] = $root.TemperatureItem.fromObject(
          object.temps[i],
          long + 1,
        );
      }
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResAllTemperature message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResAllTemperature
   * @static
   * @param {ResAllTemperature} message ResAllTemperature
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResAllTemperature.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.temps = [];
    if (options.defaults) object.code = 0;
    if (message.temps && message.temps.length) {
      object.temps = [];
      for (var j = 0; j < message.temps.length; ++j)
        object.temps[j] = $root.TemperatureItem.toObject(
          message.temps[j],
          options,
          q + 1,
        );
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResAllTemperature to JSON.
   * @function toJSON
   * @memberof ResAllTemperature
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResAllTemperature.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResAllTemperature
   * @function getTypeUrl
   * @memberof ResAllTemperature
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResAllTemperature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResAllTemperature";
  };

  return ResAllTemperature;
})();

$root.ReqFactoryMoveIrcutFloor = (function () {
  /**
   * Properties of a ReqFactoryMoveIrcutFloor.
   * @exports IReqFactoryMoveIrcutFloor
   * @interface IReqFactoryMoveIrcutFloor
   * @property {number|null} [floor] ReqFactoryMoveIrcutFloor floor
   * @property {number|null} [dir] ReqFactoryMoveIrcutFloor dir
   */

  /**
   * Constructs a new ReqFactoryMoveIrcutFloor.
   * @exports ReqFactoryMoveIrcutFloor
   * @classdesc Represents a ReqFactoryMoveIrcutFloor.
   * @implements IReqFactoryMoveIrcutFloor
   * @constructor
   * @param {IReqFactoryMoveIrcutFloor=} [properties] Properties to set
   */
  function ReqFactoryMoveIrcutFloor(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryMoveIrcutFloor floor.
   * @member {number} floor
   * @memberof ReqFactoryMoveIrcutFloor
   * @instance
   */
  ReqFactoryMoveIrcutFloor.prototype.floor = 0;

  /**
   * ReqFactoryMoveIrcutFloor dir.
   * @member {number} dir
   * @memberof ReqFactoryMoveIrcutFloor
   * @instance
   */
  ReqFactoryMoveIrcutFloor.prototype.dir = 0;

  /**
   * Creates a new ReqFactoryMoveIrcutFloor instance using the specified properties.
   * @function create
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {IReqFactoryMoveIrcutFloor=} [properties] Properties to set
   * @returns {ReqFactoryMoveIrcutFloor} ReqFactoryMoveIrcutFloor instance
   */
  ReqFactoryMoveIrcutFloor.create = function create(properties) {
    return new ReqFactoryMoveIrcutFloor(properties);
  };

  /**
   * Encodes the specified ReqFactoryMoveIrcutFloor message. Does not implicitly {@link ReqFactoryMoveIrcutFloor.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {IReqFactoryMoveIrcutFloor} message ReqFactoryMoveIrcutFloor message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryMoveIrcutFloor.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.floor != null && Object.hasOwnProperty.call(message, "floor"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.floor);
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.dir);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryMoveIrcutFloor message, length delimited. Does not implicitly {@link ReqFactoryMoveIrcutFloor.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {IReqFactoryMoveIrcutFloor} message ReqFactoryMoveIrcutFloor message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryMoveIrcutFloor.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryMoveIrcutFloor message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryMoveIrcutFloor} ReqFactoryMoveIrcutFloor
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryMoveIrcutFloor.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryMoveIrcutFloor();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.floor = reader.int32();
          break;
        }
        case 2: {
          message.dir = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryMoveIrcutFloor message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryMoveIrcutFloor} ReqFactoryMoveIrcutFloor
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryMoveIrcutFloor.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryMoveIrcutFloor message.
   * @function verify
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryMoveIrcutFloor.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.floor != null && Object.hasOwnProperty.call(message, "floor"))
      if (!$util.isInteger(message.floor)) return "floor: integer expected";
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      if (!$util.isInteger(message.dir)) return "dir: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryMoveIrcutFloor message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryMoveIrcutFloor} ReqFactoryMoveIrcutFloor
   */
  ReqFactoryMoveIrcutFloor.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryMoveIrcutFloor) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryMoveIrcutFloor: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryMoveIrcutFloor();
    if (object.floor != null) message.floor = object.floor | 0;
    if (object.dir != null) message.dir = object.dir | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryMoveIrcutFloor message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {ReqFactoryMoveIrcutFloor} message ReqFactoryMoveIrcutFloor
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryMoveIrcutFloor.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.floor = 0;
      object.dir = 0;
    }
    if (message.floor != null && Object.hasOwnProperty.call(message, "floor"))
      object.floor = message.floor;
    if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
      object.dir = message.dir;
    return object;
  };

  /**
   * Converts this ReqFactoryMoveIrcutFloor to JSON.
   * @function toJSON
   * @memberof ReqFactoryMoveIrcutFloor
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryMoveIrcutFloor.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryMoveIrcutFloor
   * @function getTypeUrl
   * @memberof ReqFactoryMoveIrcutFloor
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryMoveIrcutFloor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryMoveIrcutFloor";
  };

  return ReqFactoryMoveIrcutFloor;
})();

$root.ResFactoryMicRms = (function () {
  /**
   * Properties of a ResFactoryMicRms.
   * @exports IResFactoryMicRms
   * @interface IResFactoryMicRms
   * @property {Array.<number>|null} [allMicRms] ResFactoryMicRms allMicRms
   * @property {number|null} [code] ResFactoryMicRms code
   */

  /**
   * Constructs a new ResFactoryMicRms.
   * @exports ResFactoryMicRms
   * @classdesc Represents a ResFactoryMicRms.
   * @implements IResFactoryMicRms
   * @constructor
   * @param {IResFactoryMicRms=} [properties] Properties to set
   */
  function ResFactoryMicRms(properties) {
    this.allMicRms = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResFactoryMicRms allMicRms.
   * @member {Array.<number>} allMicRms
   * @memberof ResFactoryMicRms
   * @instance
   */
  ResFactoryMicRms.prototype.allMicRms = $util.emptyArray;

  /**
   * ResFactoryMicRms code.
   * @member {number} code
   * @memberof ResFactoryMicRms
   * @instance
   */
  ResFactoryMicRms.prototype.code = 0;

  /**
   * Creates a new ResFactoryMicRms instance using the specified properties.
   * @function create
   * @memberof ResFactoryMicRms
   * @static
   * @param {IResFactoryMicRms=} [properties] Properties to set
   * @returns {ResFactoryMicRms} ResFactoryMicRms instance
   */
  ResFactoryMicRms.create = function create(properties) {
    return new ResFactoryMicRms(properties);
  };

  /**
   * Encodes the specified ResFactoryMicRms message. Does not implicitly {@link ResFactoryMicRms.verify|verify} messages.
   * @function encode
   * @memberof ResFactoryMicRms
   * @static
   * @param {IResFactoryMicRms} message ResFactoryMicRms message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryMicRms.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.allMicRms != null && message.allMicRms.length) {
      writer.uint32(/* id 1, wireType 2 =*/ 10).fork();
      for (var i = 0; i < message.allMicRms.length; ++i)
        writer.double(message.allMicRms[i]);
      writer.ldelim();
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResFactoryMicRms message, length delimited. Does not implicitly {@link ResFactoryMicRms.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResFactoryMicRms
   * @static
   * @param {IResFactoryMicRms} message ResFactoryMicRms message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryMicRms.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResFactoryMicRms message from the specified reader or buffer.
   * @function decode
   * @memberof ResFactoryMicRms
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResFactoryMicRms} ResFactoryMicRms
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryMicRms.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResFactoryMicRms();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.allMicRms && message.allMicRms.length))
            message.allMicRms = [];
          if ((tag & 7) === 2) {
            var end2 = reader.uint32() + reader.pos;
            if (end2 > reader.len) throw RangeError("index out of range");
            reader.len = end2;
            while (reader.pos < end2) message.allMicRms.push(reader.double());
            if (reader.pos !== end2) throw RangeError("index out of range");
            reader.len = end;
          } else message.allMicRms.push(reader.double());
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResFactoryMicRms message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResFactoryMicRms
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResFactoryMicRms} ResFactoryMicRms
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryMicRms.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResFactoryMicRms message.
   * @function verify
   * @memberof ResFactoryMicRms
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResFactoryMicRms.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.allMicRms != null &&
      Object.hasOwnProperty.call(message, "allMicRms")
    ) {
      if (!Array.isArray(message.allMicRms)) return "allMicRms: array expected";
      for (var i = 0; i < message.allMicRms.length; ++i)
        if (typeof message.allMicRms[i] !== "number")
          return "allMicRms: number[] expected";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResFactoryMicRms message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResFactoryMicRms
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResFactoryMicRms} ResFactoryMicRms
   */
  ResFactoryMicRms.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResFactoryMicRms) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResFactoryMicRms: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResFactoryMicRms();
    if (object.allMicRms) {
      if (!Array.isArray(object.allMicRms))
        throw TypeError(".ResFactoryMicRms.allMicRms: array expected");
      message.allMicRms = [];
      for (var i = 0; i < object.allMicRms.length; ++i)
        message.allMicRms[i] = Number(object.allMicRms[i]);
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResFactoryMicRms message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResFactoryMicRms
   * @static
   * @param {ResFactoryMicRms} message ResFactoryMicRms
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResFactoryMicRms.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.allMicRms = [];
    if (options.defaults) object.code = 0;
    if (message.allMicRms && message.allMicRms.length) {
      object.allMicRms = [];
      for (var j = 0; j < message.allMicRms.length; ++j)
        object.allMicRms[j] =
          options.json && !isFinite(message.allMicRms[j])
            ? String(message.allMicRms[j])
            : message.allMicRms[j];
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResFactoryMicRms to JSON.
   * @function toJSON
   * @memberof ResFactoryMicRms
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResFactoryMicRms.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResFactoryMicRms
   * @function getTypeUrl
   * @memberof ResFactoryMicRms
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResFactoryMicRms.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResFactoryMicRms";
  };

  return ResFactoryMicRms;
})();

$root.ReqSetVcmRom = (function () {
  /**
   * Properties of a ReqSetVcmRom.
   * @exports IReqSetVcmRom
   * @interface IReqSetVcmRom
   * @property {number|null} [addr] ReqSetVcmRom addr
   * @property {number|null} [value] ReqSetVcmRom value
   */

  /**
   * Constructs a new ReqSetVcmRom.
   * @exports ReqSetVcmRom
   * @classdesc Represents a ReqSetVcmRom.
   * @implements IReqSetVcmRom
   * @constructor
   * @param {IReqSetVcmRom=} [properties] Properties to set
   */
  function ReqSetVcmRom(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetVcmRom addr.
   * @member {number} addr
   * @memberof ReqSetVcmRom
   * @instance
   */
  ReqSetVcmRom.prototype.addr = 0;

  /**
   * ReqSetVcmRom value.
   * @member {number} value
   * @memberof ReqSetVcmRom
   * @instance
   */
  ReqSetVcmRom.prototype.value = 0;

  /**
   * Creates a new ReqSetVcmRom instance using the specified properties.
   * @function create
   * @memberof ReqSetVcmRom
   * @static
   * @param {IReqSetVcmRom=} [properties] Properties to set
   * @returns {ReqSetVcmRom} ReqSetVcmRom instance
   */
  ReqSetVcmRom.create = function create(properties) {
    return new ReqSetVcmRom(properties);
  };

  /**
   * Encodes the specified ReqSetVcmRom message. Does not implicitly {@link ReqSetVcmRom.verify|verify} messages.
   * @function encode
   * @memberof ReqSetVcmRom
   * @static
   * @param {IReqSetVcmRom} message ReqSetVcmRom message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetVcmRom.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.addr != null && Object.hasOwnProperty.call(message, "addr"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.addr);
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.value);
    return writer;
  };

  /**
   * Encodes the specified ReqSetVcmRom message, length delimited. Does not implicitly {@link ReqSetVcmRom.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetVcmRom
   * @static
   * @param {IReqSetVcmRom} message ReqSetVcmRom message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetVcmRom.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetVcmRom message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetVcmRom
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetVcmRom} ReqSetVcmRom
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetVcmRom.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetVcmRom();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.addr = reader.int32();
          break;
        }
        case 2: {
          message.value = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetVcmRom message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetVcmRom
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetVcmRom} ReqSetVcmRom
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetVcmRom.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetVcmRom message.
   * @function verify
   * @memberof ReqSetVcmRom
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetVcmRom.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.addr != null && Object.hasOwnProperty.call(message, "addr"))
      if (!$util.isInteger(message.addr)) return "addr: integer expected";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (!$util.isInteger(message.value)) return "value: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetVcmRom message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetVcmRom
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetVcmRom} ReqSetVcmRom
   */
  ReqSetVcmRom.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetVcmRom) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetVcmRom: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetVcmRom();
    if (object.addr != null) message.addr = object.addr | 0;
    if (object.value != null) message.value = object.value | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetVcmRom message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetVcmRom
   * @static
   * @param {ReqSetVcmRom} message ReqSetVcmRom
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetVcmRom.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.addr = 0;
      object.value = 0;
    }
    if (message.addr != null && Object.hasOwnProperty.call(message, "addr"))
      object.addr = message.addr;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value = message.value;
    return object;
  };

  /**
   * Converts this ReqSetVcmRom to JSON.
   * @function toJSON
   * @memberof ReqSetVcmRom
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetVcmRom.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetVcmRom
   * @function getTypeUrl
   * @memberof ReqSetVcmRom
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetVcmRom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetVcmRom";
  };

  return ReqSetVcmRom;
})();

$root.ResVcmK = (function () {
  /**
   * Properties of a ResVcmK.
   * @exports IResVcmK
   * @interface IResVcmK
   * @property {number|null} [value] ResVcmK value
   */

  /**
   * Constructs a new ResVcmK.
   * @exports ResVcmK
   * @classdesc Represents a ResVcmK.
   * @implements IResVcmK
   * @constructor
   * @param {IResVcmK=} [properties] Properties to set
   */
  function ResVcmK(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResVcmK value.
   * @member {number} value
   * @memberof ResVcmK
   * @instance
   */
  ResVcmK.prototype.value = 0;

  /**
   * Creates a new ResVcmK instance using the specified properties.
   * @function create
   * @memberof ResVcmK
   * @static
   * @param {IResVcmK=} [properties] Properties to set
   * @returns {ResVcmK} ResVcmK instance
   */
  ResVcmK.create = function create(properties) {
    return new ResVcmK(properties);
  };

  /**
   * Encodes the specified ResVcmK message. Does not implicitly {@link ResVcmK.verify|verify} messages.
   * @function encode
   * @memberof ResVcmK
   * @static
   * @param {IResVcmK} message ResVcmK message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResVcmK.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.value);
    return writer;
  };

  /**
   * Encodes the specified ResVcmK message, length delimited. Does not implicitly {@link ResVcmK.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResVcmK
   * @static
   * @param {IResVcmK} message ResVcmK message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResVcmK.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResVcmK message from the specified reader or buffer.
   * @function decode
   * @memberof ResVcmK
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResVcmK} ResVcmK
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResVcmK.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResVcmK();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.value = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResVcmK message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResVcmK
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResVcmK} ResVcmK
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResVcmK.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResVcmK message.
   * @function verify
   * @memberof ResVcmK
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResVcmK.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      if (typeof message.value !== "number") return "value: number expected";
    return null;
  };

  /**
   * Creates a ResVcmK message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResVcmK
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResVcmK} ResVcmK
   */
  ResVcmK.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResVcmK) return object;
    if (!$util.isObject(object)) throw TypeError(".ResVcmK: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResVcmK();
    if (object.value != null) message.value = Number(object.value);
    return message;
  };

  /**
   * Creates a plain object from a ResVcmK message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResVcmK
   * @static
   * @param {ResVcmK} message ResVcmK
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResVcmK.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.value = 0;
    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
      object.value =
        options.json && !isFinite(message.value)
          ? String(message.value)
          : message.value;
    return object;
  };

  /**
   * Converts this ResVcmK to JSON.
   * @function toJSON
   * @memberof ResVcmK
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResVcmK.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResVcmK
   * @function getTypeUrl
   * @memberof ResVcmK
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResVcmK.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResVcmK";
  };

  return ResVcmK;
})();

$root.ReqFactorySetUsbMode = (function () {
  /**
   * Properties of a ReqFactorySetUsbMode.
   * @exports IReqFactorySetUsbMode
   * @interface IReqFactorySetUsbMode
   * @property {number|null} [mode] ReqFactorySetUsbMode mode
   */

  /**
   * Constructs a new ReqFactorySetUsbMode.
   * @exports ReqFactorySetUsbMode
   * @classdesc Represents a ReqFactorySetUsbMode.
   * @implements IReqFactorySetUsbMode
   * @constructor
   * @param {IReqFactorySetUsbMode=} [properties] Properties to set
   */
  function ReqFactorySetUsbMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactorySetUsbMode mode.
   * @member {number} mode
   * @memberof ReqFactorySetUsbMode
   * @instance
   */
  ReqFactorySetUsbMode.prototype.mode = 0;

  /**
   * Creates a new ReqFactorySetUsbMode instance using the specified properties.
   * @function create
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {IReqFactorySetUsbMode=} [properties] Properties to set
   * @returns {ReqFactorySetUsbMode} ReqFactorySetUsbMode instance
   */
  ReqFactorySetUsbMode.create = function create(properties) {
    return new ReqFactorySetUsbMode(properties);
  };

  /**
   * Encodes the specified ReqFactorySetUsbMode message. Does not implicitly {@link ReqFactorySetUsbMode.verify|verify} messages.
   * @function encode
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {IReqFactorySetUsbMode} message ReqFactorySetUsbMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetUsbMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqFactorySetUsbMode message, length delimited. Does not implicitly {@link ReqFactorySetUsbMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {IReqFactorySetUsbMode} message ReqFactorySetUsbMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetUsbMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactorySetUsbMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactorySetUsbMode} ReqFactorySetUsbMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetUsbMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactorySetUsbMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactorySetUsbMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactorySetUsbMode} ReqFactorySetUsbMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetUsbMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactorySetUsbMode message.
   * @function verify
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactorySetUsbMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactorySetUsbMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactorySetUsbMode} ReqFactorySetUsbMode
   */
  ReqFactorySetUsbMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactorySetUsbMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactorySetUsbMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactorySetUsbMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactorySetUsbMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {ReqFactorySetUsbMode} message ReqFactorySetUsbMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactorySetUsbMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqFactorySetUsbMode to JSON.
   * @function toJSON
   * @memberof ReqFactorySetUsbMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactorySetUsbMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactorySetUsbMode
   * @function getTypeUrl
   * @memberof ReqFactorySetUsbMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactorySetUsbMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactorySetUsbMode";
  };

  return ReqFactorySetUsbMode;
})();

$root.ReqFactoryDualPeSwitchState = (function () {
  /**
   * Properties of a ReqFactoryDualPeSwitchState.
   * @exports IReqFactoryDualPeSwitchState
   * @interface IReqFactoryDualPeSwitchState
   * @property {number|null} [motorId] ReqFactoryDualPeSwitchState motorId
   */

  /**
   * Constructs a new ReqFactoryDualPeSwitchState.
   * @exports ReqFactoryDualPeSwitchState
   * @classdesc Represents a ReqFactoryDualPeSwitchState.
   * @implements IReqFactoryDualPeSwitchState
   * @constructor
   * @param {IReqFactoryDualPeSwitchState=} [properties] Properties to set
   */
  function ReqFactoryDualPeSwitchState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactoryDualPeSwitchState motorId.
   * @member {number} motorId
   * @memberof ReqFactoryDualPeSwitchState
   * @instance
   */
  ReqFactoryDualPeSwitchState.prototype.motorId = 0;

  /**
   * Creates a new ReqFactoryDualPeSwitchState instance using the specified properties.
   * @function create
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {IReqFactoryDualPeSwitchState=} [properties] Properties to set
   * @returns {ReqFactoryDualPeSwitchState} ReqFactoryDualPeSwitchState instance
   */
  ReqFactoryDualPeSwitchState.create = function create(properties) {
    return new ReqFactoryDualPeSwitchState(properties);
  };

  /**
   * Encodes the specified ReqFactoryDualPeSwitchState message. Does not implicitly {@link ReqFactoryDualPeSwitchState.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {IReqFactoryDualPeSwitchState} message ReqFactoryDualPeSwitchState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryDualPeSwitchState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.motorId);
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryDualPeSwitchState message, length delimited. Does not implicitly {@link ReqFactoryDualPeSwitchState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {IReqFactoryDualPeSwitchState} message ReqFactoryDualPeSwitchState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryDualPeSwitchState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryDualPeSwitchState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryDualPeSwitchState} ReqFactoryDualPeSwitchState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryDualPeSwitchState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryDualPeSwitchState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.motorId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryDualPeSwitchState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryDualPeSwitchState} ReqFactoryDualPeSwitchState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryDualPeSwitchState.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryDualPeSwitchState message.
   * @function verify
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryDualPeSwitchState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      if (!$util.isInteger(message.motorId)) return "motorId: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactoryDualPeSwitchState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryDualPeSwitchState} ReqFactoryDualPeSwitchState
   */
  ReqFactoryDualPeSwitchState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqFactoryDualPeSwitchState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactoryDualPeSwitchState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactoryDualPeSwitchState();
    if (object.motorId != null) message.motorId = object.motorId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactoryDualPeSwitchState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {ReqFactoryDualPeSwitchState} message ReqFactoryDualPeSwitchState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryDualPeSwitchState.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.motorId = 0;
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      object.motorId = message.motorId;
    return object;
  };

  /**
   * Converts this ReqFactoryDualPeSwitchState to JSON.
   * @function toJSON
   * @memberof ReqFactoryDualPeSwitchState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryDualPeSwitchState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryDualPeSwitchState
   * @function getTypeUrl
   * @memberof ReqFactoryDualPeSwitchState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryDualPeSwitchState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryDualPeSwitchState";
  };

  return ReqFactoryDualPeSwitchState;
})();

$root.ResFactoryDualPeSwitchState = (function () {
  /**
   * Properties of a ResFactoryDualPeSwitchState.
   * @exports IResFactoryDualPeSwitchState
   * @interface IResFactoryDualPeSwitchState
   * @property {number|null} [motorId] ResFactoryDualPeSwitchState motorId
   * @property {number|null} [state1] ResFactoryDualPeSwitchState state1
   * @property {number|null} [state2] ResFactoryDualPeSwitchState state2
   */

  /**
   * Constructs a new ResFactoryDualPeSwitchState.
   * @exports ResFactoryDualPeSwitchState
   * @classdesc Represents a ResFactoryDualPeSwitchState.
   * @implements IResFactoryDualPeSwitchState
   * @constructor
   * @param {IResFactoryDualPeSwitchState=} [properties] Properties to set
   */
  function ResFactoryDualPeSwitchState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResFactoryDualPeSwitchState motorId.
   * @member {number} motorId
   * @memberof ResFactoryDualPeSwitchState
   * @instance
   */
  ResFactoryDualPeSwitchState.prototype.motorId = 0;

  /**
   * ResFactoryDualPeSwitchState state1.
   * @member {number} state1
   * @memberof ResFactoryDualPeSwitchState
   * @instance
   */
  ResFactoryDualPeSwitchState.prototype.state1 = 0;

  /**
   * ResFactoryDualPeSwitchState state2.
   * @member {number} state2
   * @memberof ResFactoryDualPeSwitchState
   * @instance
   */
  ResFactoryDualPeSwitchState.prototype.state2 = 0;

  /**
   * Creates a new ResFactoryDualPeSwitchState instance using the specified properties.
   * @function create
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {IResFactoryDualPeSwitchState=} [properties] Properties to set
   * @returns {ResFactoryDualPeSwitchState} ResFactoryDualPeSwitchState instance
   */
  ResFactoryDualPeSwitchState.create = function create(properties) {
    return new ResFactoryDualPeSwitchState(properties);
  };

  /**
   * Encodes the specified ResFactoryDualPeSwitchState message. Does not implicitly {@link ResFactoryDualPeSwitchState.verify|verify} messages.
   * @function encode
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {IResFactoryDualPeSwitchState} message ResFactoryDualPeSwitchState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryDualPeSwitchState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.motorId);
    if (message.state1 != null && Object.hasOwnProperty.call(message, "state1"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.state1);
    if (message.state2 != null && Object.hasOwnProperty.call(message, "state2"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.state2);
    return writer;
  };

  /**
   * Encodes the specified ResFactoryDualPeSwitchState message, length delimited. Does not implicitly {@link ResFactoryDualPeSwitchState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {IResFactoryDualPeSwitchState} message ResFactoryDualPeSwitchState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryDualPeSwitchState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResFactoryDualPeSwitchState message from the specified reader or buffer.
   * @function decode
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResFactoryDualPeSwitchState} ResFactoryDualPeSwitchState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryDualPeSwitchState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResFactoryDualPeSwitchState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.motorId = reader.int32();
          break;
        }
        case 2: {
          message.state1 = reader.int32();
          break;
        }
        case 3: {
          message.state2 = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResFactoryDualPeSwitchState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResFactoryDualPeSwitchState} ResFactoryDualPeSwitchState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryDualPeSwitchState.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResFactoryDualPeSwitchState message.
   * @function verify
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResFactoryDualPeSwitchState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      if (!$util.isInteger(message.motorId)) return "motorId: integer expected";
    if (message.state1 != null && Object.hasOwnProperty.call(message, "state1"))
      if (!$util.isInteger(message.state1)) return "state1: integer expected";
    if (message.state2 != null && Object.hasOwnProperty.call(message, "state2"))
      if (!$util.isInteger(message.state2)) return "state2: integer expected";
    return null;
  };

  /**
   * Creates a ResFactoryDualPeSwitchState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResFactoryDualPeSwitchState} ResFactoryDualPeSwitchState
   */
  ResFactoryDualPeSwitchState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResFactoryDualPeSwitchState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResFactoryDualPeSwitchState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResFactoryDualPeSwitchState();
    if (object.motorId != null) message.motorId = object.motorId | 0;
    if (object.state1 != null) message.state1 = object.state1 | 0;
    if (object.state2 != null) message.state2 = object.state2 | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResFactoryDualPeSwitchState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {ResFactoryDualPeSwitchState} message ResFactoryDualPeSwitchState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResFactoryDualPeSwitchState.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.motorId = 0;
      object.state1 = 0;
      object.state2 = 0;
    }
    if (
      message.motorId != null &&
      Object.hasOwnProperty.call(message, "motorId")
    )
      object.motorId = message.motorId;
    if (message.state1 != null && Object.hasOwnProperty.call(message, "state1"))
      object.state1 = message.state1;
    if (message.state2 != null && Object.hasOwnProperty.call(message, "state2"))
      object.state2 = message.state2;
    return object;
  };

  /**
   * Converts this ResFactoryDualPeSwitchState to JSON.
   * @function toJSON
   * @memberof ResFactoryDualPeSwitchState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResFactoryDualPeSwitchState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResFactoryDualPeSwitchState
   * @function getTypeUrl
   * @memberof ResFactoryDualPeSwitchState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResFactoryDualPeSwitchState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResFactoryDualPeSwitchState";
  };

  return ResFactoryDualPeSwitchState;
})();

$root.ReqFactorySetRollHorizontalDegree = (function () {
  /**
   * Properties of a ReqFactorySetRollHorizontalDegree.
   * @exports IReqFactorySetRollHorizontalDegree
   * @interface IReqFactorySetRollHorizontalDegree
   */

  /**
   * Constructs a new ReqFactorySetRollHorizontalDegree.
   * @exports ReqFactorySetRollHorizontalDegree
   * @classdesc Represents a ReqFactorySetRollHorizontalDegree.
   * @implements IReqFactorySetRollHorizontalDegree
   * @constructor
   * @param {IReqFactorySetRollHorizontalDegree=} [properties] Properties to set
   */
  function ReqFactorySetRollHorizontalDegree(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqFactorySetRollHorizontalDegree instance using the specified properties.
   * @function create
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {IReqFactorySetRollHorizontalDegree=} [properties] Properties to set
   * @returns {ReqFactorySetRollHorizontalDegree} ReqFactorySetRollHorizontalDegree instance
   */
  ReqFactorySetRollHorizontalDegree.create = function create(properties) {
    return new ReqFactorySetRollHorizontalDegree(properties);
  };

  /**
   * Encodes the specified ReqFactorySetRollHorizontalDegree message. Does not implicitly {@link ReqFactorySetRollHorizontalDegree.verify|verify} messages.
   * @function encode
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {IReqFactorySetRollHorizontalDegree} message ReqFactorySetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetRollHorizontalDegree.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqFactorySetRollHorizontalDegree message, length delimited. Does not implicitly {@link ReqFactorySetRollHorizontalDegree.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {IReqFactorySetRollHorizontalDegree} message ReqFactorySetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetRollHorizontalDegree.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactorySetRollHorizontalDegree message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactorySetRollHorizontalDegree} ReqFactorySetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetRollHorizontalDegree.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactorySetRollHorizontalDegree();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactorySetRollHorizontalDegree message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactorySetRollHorizontalDegree} ReqFactorySetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetRollHorizontalDegree.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactorySetRollHorizontalDegree message.
   * @function verify
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactorySetRollHorizontalDegree.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqFactorySetRollHorizontalDegree message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactorySetRollHorizontalDegree} ReqFactorySetRollHorizontalDegree
   */
  ReqFactorySetRollHorizontalDegree.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqFactorySetRollHorizontalDegree)
      return object;
    return new $root.ReqFactorySetRollHorizontalDegree();
  };

  /**
   * Creates a plain object from a ReqFactorySetRollHorizontalDegree message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {ReqFactorySetRollHorizontalDegree} message ReqFactorySetRollHorizontalDegree
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactorySetRollHorizontalDegree.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqFactorySetRollHorizontalDegree to JSON.
   * @function toJSON
   * @memberof ReqFactorySetRollHorizontalDegree
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactorySetRollHorizontalDegree.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactorySetRollHorizontalDegree
   * @function getTypeUrl
   * @memberof ReqFactorySetRollHorizontalDegree
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactorySetRollHorizontalDegree.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactorySetRollHorizontalDegree";
  };

  return ReqFactorySetRollHorizontalDegree;
})();

$root.ReqFactoryGetRollHorizontalDegree = (function () {
  /**
   * Properties of a ReqFactoryGetRollHorizontalDegree.
   * @exports IReqFactoryGetRollHorizontalDegree
   * @interface IReqFactoryGetRollHorizontalDegree
   */

  /**
   * Constructs a new ReqFactoryGetRollHorizontalDegree.
   * @exports ReqFactoryGetRollHorizontalDegree
   * @classdesc Represents a ReqFactoryGetRollHorizontalDegree.
   * @implements IReqFactoryGetRollHorizontalDegree
   * @constructor
   * @param {IReqFactoryGetRollHorizontalDegree=} [properties] Properties to set
   */
  function ReqFactoryGetRollHorizontalDegree(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqFactoryGetRollHorizontalDegree instance using the specified properties.
   * @function create
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {IReqFactoryGetRollHorizontalDegree=} [properties] Properties to set
   * @returns {ReqFactoryGetRollHorizontalDegree} ReqFactoryGetRollHorizontalDegree instance
   */
  ReqFactoryGetRollHorizontalDegree.create = function create(properties) {
    return new ReqFactoryGetRollHorizontalDegree(properties);
  };

  /**
   * Encodes the specified ReqFactoryGetRollHorizontalDegree message. Does not implicitly {@link ReqFactoryGetRollHorizontalDegree.verify|verify} messages.
   * @function encode
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {IReqFactoryGetRollHorizontalDegree} message ReqFactoryGetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryGetRollHorizontalDegree.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqFactoryGetRollHorizontalDegree message, length delimited. Does not implicitly {@link ReqFactoryGetRollHorizontalDegree.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {IReqFactoryGetRollHorizontalDegree} message ReqFactoryGetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactoryGetRollHorizontalDegree.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqFactoryGetRollHorizontalDegree message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactoryGetRollHorizontalDegree} ReqFactoryGetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryGetRollHorizontalDegree.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactoryGetRollHorizontalDegree();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactoryGetRollHorizontalDegree message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactoryGetRollHorizontalDegree} ReqFactoryGetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactoryGetRollHorizontalDegree.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqFactoryGetRollHorizontalDegree message.
   * @function verify
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactoryGetRollHorizontalDegree.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqFactoryGetRollHorizontalDegree message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactoryGetRollHorizontalDegree} ReqFactoryGetRollHorizontalDegree
   */
  ReqFactoryGetRollHorizontalDegree.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqFactoryGetRollHorizontalDegree)
      return object;
    return new $root.ReqFactoryGetRollHorizontalDegree();
  };

  /**
   * Creates a plain object from a ReqFactoryGetRollHorizontalDegree message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {ReqFactoryGetRollHorizontalDegree} message ReqFactoryGetRollHorizontalDegree
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactoryGetRollHorizontalDegree.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqFactoryGetRollHorizontalDegree to JSON.
   * @function toJSON
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactoryGetRollHorizontalDegree.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactoryGetRollHorizontalDegree
   * @function getTypeUrl
   * @memberof ReqFactoryGetRollHorizontalDegree
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactoryGetRollHorizontalDegree.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactoryGetRollHorizontalDegree";
  };

  return ReqFactoryGetRollHorizontalDegree;
})();

$root.ResFactoryGetRollHorizontalDegree = (function () {
  /**
   * Properties of a ResFactoryGetRollHorizontalDegree.
   * @exports IResFactoryGetRollHorizontalDegree
   * @interface IResFactoryGetRollHorizontalDegree
   * @property {number|null} [code] ResFactoryGetRollHorizontalDegree code
   * @property {number|null} [degree] ResFactoryGetRollHorizontalDegree degree
   */

  /**
   * Constructs a new ResFactoryGetRollHorizontalDegree.
   * @exports ResFactoryGetRollHorizontalDegree
   * @classdesc Represents a ResFactoryGetRollHorizontalDegree.
   * @implements IResFactoryGetRollHorizontalDegree
   * @constructor
   * @param {IResFactoryGetRollHorizontalDegree=} [properties] Properties to set
   */
  function ResFactoryGetRollHorizontalDegree(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResFactoryGetRollHorizontalDegree code.
   * @member {number} code
   * @memberof ResFactoryGetRollHorizontalDegree
   * @instance
   */
  ResFactoryGetRollHorizontalDegree.prototype.code = 0;

  /**
   * ResFactoryGetRollHorizontalDegree degree.
   * @member {number} degree
   * @memberof ResFactoryGetRollHorizontalDegree
   * @instance
   */
  ResFactoryGetRollHorizontalDegree.prototype.degree = 0;

  /**
   * Creates a new ResFactoryGetRollHorizontalDegree instance using the specified properties.
   * @function create
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {IResFactoryGetRollHorizontalDegree=} [properties] Properties to set
   * @returns {ResFactoryGetRollHorizontalDegree} ResFactoryGetRollHorizontalDegree instance
   */
  ResFactoryGetRollHorizontalDegree.create = function create(properties) {
    return new ResFactoryGetRollHorizontalDegree(properties);
  };

  /**
   * Encodes the specified ResFactoryGetRollHorizontalDegree message. Does not implicitly {@link ResFactoryGetRollHorizontalDegree.verify|verify} messages.
   * @function encode
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {IResFactoryGetRollHorizontalDegree} message ResFactoryGetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryGetRollHorizontalDegree.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.degree != null && Object.hasOwnProperty.call(message, "degree"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.degree);
    return writer;
  };

  /**
   * Encodes the specified ResFactoryGetRollHorizontalDegree message, length delimited. Does not implicitly {@link ResFactoryGetRollHorizontalDegree.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {IResFactoryGetRollHorizontalDegree} message ResFactoryGetRollHorizontalDegree message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResFactoryGetRollHorizontalDegree.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResFactoryGetRollHorizontalDegree message from the specified reader or buffer.
   * @function decode
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResFactoryGetRollHorizontalDegree} ResFactoryGetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryGetRollHorizontalDegree.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResFactoryGetRollHorizontalDegree();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.degree = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResFactoryGetRollHorizontalDegree message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResFactoryGetRollHorizontalDegree} ResFactoryGetRollHorizontalDegree
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResFactoryGetRollHorizontalDegree.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResFactoryGetRollHorizontalDegree message.
   * @function verify
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResFactoryGetRollHorizontalDegree.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.degree != null && Object.hasOwnProperty.call(message, "degree"))
      if (typeof message.degree !== "number") return "degree: number expected";
    return null;
  };

  /**
   * Creates a ResFactoryGetRollHorizontalDegree message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResFactoryGetRollHorizontalDegree} ResFactoryGetRollHorizontalDegree
   */
  ResFactoryGetRollHorizontalDegree.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ResFactoryGetRollHorizontalDegree)
      return object;
    if (!$util.isObject(object))
      throw TypeError(".ResFactoryGetRollHorizontalDegree: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResFactoryGetRollHorizontalDegree();
    if (object.code != null) message.code = object.code | 0;
    if (object.degree != null) message.degree = Number(object.degree);
    return message;
  };

  /**
   * Creates a plain object from a ResFactoryGetRollHorizontalDegree message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {ResFactoryGetRollHorizontalDegree} message ResFactoryGetRollHorizontalDegree
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResFactoryGetRollHorizontalDegree.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.degree = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.degree != null && Object.hasOwnProperty.call(message, "degree"))
      object.degree =
        options.json && !isFinite(message.degree)
          ? String(message.degree)
          : message.degree;
    return object;
  };

  /**
   * Converts this ResFactoryGetRollHorizontalDegree to JSON.
   * @function toJSON
   * @memberof ResFactoryGetRollHorizontalDegree
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResFactoryGetRollHorizontalDegree.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResFactoryGetRollHorizontalDegree
   * @function getTypeUrl
   * @memberof ResFactoryGetRollHorizontalDegree
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResFactoryGetRollHorizontalDegree.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResFactoryGetRollHorizontalDegree";
  };

  return ResFactoryGetRollHorizontalDegree;
})();

$root.ReqFactorySetRollHorizontalPosition = (function () {
  /**
   * Properties of a ReqFactorySetRollHorizontalPosition.
   * @exports IReqFactorySetRollHorizontalPosition
   * @interface IReqFactorySetRollHorizontalPosition
   * @property {number|null} [offsetMode] ReqFactorySetRollHorizontalPosition offsetMode
   * @property {number|null} [moveMode] ReqFactorySetRollHorizontalPosition moveMode
   */

  /**
   * Constructs a new ReqFactorySetRollHorizontalPosition.
   * @exports ReqFactorySetRollHorizontalPosition
   * @classdesc Represents a ReqFactorySetRollHorizontalPosition.
   * @implements IReqFactorySetRollHorizontalPosition
   * @constructor
   * @param {IReqFactorySetRollHorizontalPosition=} [properties] Properties to set
   */
  function ReqFactorySetRollHorizontalPosition(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqFactorySetRollHorizontalPosition offsetMode.
   * @member {number} offsetMode
   * @memberof ReqFactorySetRollHorizontalPosition
   * @instance
   */
  ReqFactorySetRollHorizontalPosition.prototype.offsetMode = 0;

  /**
   * ReqFactorySetRollHorizontalPosition moveMode.
   * @member {number} moveMode
   * @memberof ReqFactorySetRollHorizontalPosition
   * @instance
   */
  ReqFactorySetRollHorizontalPosition.prototype.moveMode = 0;

  /**
   * Creates a new ReqFactorySetRollHorizontalPosition instance using the specified properties.
   * @function create
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {IReqFactorySetRollHorizontalPosition=} [properties] Properties to set
   * @returns {ReqFactorySetRollHorizontalPosition} ReqFactorySetRollHorizontalPosition instance
   */
  ReqFactorySetRollHorizontalPosition.create = function create(properties) {
    return new ReqFactorySetRollHorizontalPosition(properties);
  };

  /**
   * Encodes the specified ReqFactorySetRollHorizontalPosition message. Does not implicitly {@link ReqFactorySetRollHorizontalPosition.verify|verify} messages.
   * @function encode
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {IReqFactorySetRollHorizontalPosition} message ReqFactorySetRollHorizontalPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetRollHorizontalPosition.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.offsetMode != null &&
      Object.hasOwnProperty.call(message, "offsetMode")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.offsetMode);
    if (
      message.moveMode != null &&
      Object.hasOwnProperty.call(message, "moveMode")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.moveMode);
    return writer;
  };

  /**
   * Encodes the specified ReqFactorySetRollHorizontalPosition message, length delimited. Does not implicitly {@link ReqFactorySetRollHorizontalPosition.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {IReqFactorySetRollHorizontalPosition} message ReqFactorySetRollHorizontalPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqFactorySetRollHorizontalPosition.encodeDelimited =
    function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

  /**
   * Decodes a ReqFactorySetRollHorizontalPosition message from the specified reader or buffer.
   * @function decode
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqFactorySetRollHorizontalPosition} ReqFactorySetRollHorizontalPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetRollHorizontalPosition.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqFactorySetRollHorizontalPosition();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.offsetMode = reader.int32();
          break;
        }
        case 2: {
          message.moveMode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqFactorySetRollHorizontalPosition message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqFactorySetRollHorizontalPosition} ReqFactorySetRollHorizontalPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqFactorySetRollHorizontalPosition.decodeDelimited =
    function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

  /**
   * Verifies a ReqFactorySetRollHorizontalPosition message.
   * @function verify
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqFactorySetRollHorizontalPosition.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.offsetMode != null &&
      Object.hasOwnProperty.call(message, "offsetMode")
    )
      if (!$util.isInteger(message.offsetMode))
        return "offsetMode: integer expected";
    if (
      message.moveMode != null &&
      Object.hasOwnProperty.call(message, "moveMode")
    )
      if (!$util.isInteger(message.moveMode))
        return "moveMode: integer expected";
    return null;
  };

  /**
   * Creates a ReqFactorySetRollHorizontalPosition message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqFactorySetRollHorizontalPosition} ReqFactorySetRollHorizontalPosition
   */
  ReqFactorySetRollHorizontalPosition.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqFactorySetRollHorizontalPosition)
      return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqFactorySetRollHorizontalPosition: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqFactorySetRollHorizontalPosition();
    if (object.offsetMode != null) message.offsetMode = object.offsetMode | 0;
    if (object.moveMode != null) message.moveMode = object.moveMode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqFactorySetRollHorizontalPosition message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {ReqFactorySetRollHorizontalPosition} message ReqFactorySetRollHorizontalPosition
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqFactorySetRollHorizontalPosition.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.offsetMode = 0;
      object.moveMode = 0;
    }
    if (
      message.offsetMode != null &&
      Object.hasOwnProperty.call(message, "offsetMode")
    )
      object.offsetMode = message.offsetMode;
    if (
      message.moveMode != null &&
      Object.hasOwnProperty.call(message, "moveMode")
    )
      object.moveMode = message.moveMode;
    return object;
  };

  /**
   * Converts this ReqFactorySetRollHorizontalPosition to JSON.
   * @function toJSON
   * @memberof ReqFactorySetRollHorizontalPosition
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqFactorySetRollHorizontalPosition.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqFactorySetRollHorizontalPosition
   * @function getTypeUrl
   * @memberof ReqFactorySetRollHorizontalPosition
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqFactorySetRollHorizontalPosition.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqFactorySetRollHorizontalPosition";
  };

  return ReqFactorySetRollHorizontalPosition;
})();

$root.ReqManualSingleStepFocus = (function () {
  /**
   * Properties of a ReqManualSingleStepFocus.
   * @exports IReqManualSingleStepFocus
   * @interface IReqManualSingleStepFocus
   * @property {number|null} [direction] ReqManualSingleStepFocus direction
   */

  /**
   * Constructs a new ReqManualSingleStepFocus.
   * @exports ReqManualSingleStepFocus
   * @classdesc Represents a ReqManualSingleStepFocus.
   * @implements IReqManualSingleStepFocus
   * @constructor
   * @param {IReqManualSingleStepFocus=} [properties] Properties to set
   */
  function ReqManualSingleStepFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqManualSingleStepFocus direction.
   * @member {number} direction
   * @memberof ReqManualSingleStepFocus
   * @instance
   */
  ReqManualSingleStepFocus.prototype.direction = 0;

  /**
   * Creates a new ReqManualSingleStepFocus instance using the specified properties.
   * @function create
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {IReqManualSingleStepFocus=} [properties] Properties to set
   * @returns {ReqManualSingleStepFocus} ReqManualSingleStepFocus instance
   */
  ReqManualSingleStepFocus.create = function create(properties) {
    return new ReqManualSingleStepFocus(properties);
  };

  /**
   * Encodes the specified ReqManualSingleStepFocus message. Does not implicitly {@link ReqManualSingleStepFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {IReqManualSingleStepFocus} message ReqManualSingleStepFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualSingleStepFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.direction);
    return writer;
  };

  /**
   * Encodes the specified ReqManualSingleStepFocus message, length delimited. Does not implicitly {@link ReqManualSingleStepFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {IReqManualSingleStepFocus} message ReqManualSingleStepFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualSingleStepFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqManualSingleStepFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqManualSingleStepFocus} ReqManualSingleStepFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualSingleStepFocus.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqManualSingleStepFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.direction = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqManualSingleStepFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqManualSingleStepFocus} ReqManualSingleStepFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualSingleStepFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqManualSingleStepFocus message.
   * @function verify
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqManualSingleStepFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (!$util.isInteger(message.direction))
        return "direction: integer expected";
    return null;
  };

  /**
   * Creates a ReqManualSingleStepFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqManualSingleStepFocus} ReqManualSingleStepFocus
   */
  ReqManualSingleStepFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqManualSingleStepFocus) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqManualSingleStepFocus: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqManualSingleStepFocus();
    if (object.direction != null) message.direction = object.direction >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqManualSingleStepFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {ReqManualSingleStepFocus} message ReqManualSingleStepFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqManualSingleStepFocus.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.direction = 0;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    return object;
  };

  /**
   * Converts this ReqManualSingleStepFocus to JSON.
   * @function toJSON
   * @memberof ReqManualSingleStepFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqManualSingleStepFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqManualSingleStepFocus
   * @function getTypeUrl
   * @memberof ReqManualSingleStepFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqManualSingleStepFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqManualSingleStepFocus";
  };

  return ReqManualSingleStepFocus;
})();

$root.ReqManualContinuFocus = (function () {
  /**
   * Properties of a ReqManualContinuFocus.
   * @exports IReqManualContinuFocus
   * @interface IReqManualContinuFocus
   * @property {number|null} [direction] ReqManualContinuFocus direction
   */

  /**
   * Constructs a new ReqManualContinuFocus.
   * @exports ReqManualContinuFocus
   * @classdesc Represents a ReqManualContinuFocus.
   * @implements IReqManualContinuFocus
   * @constructor
   * @param {IReqManualContinuFocus=} [properties] Properties to set
   */
  function ReqManualContinuFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqManualContinuFocus direction.
   * @member {number} direction
   * @memberof ReqManualContinuFocus
   * @instance
   */
  ReqManualContinuFocus.prototype.direction = 0;

  /**
   * Creates a new ReqManualContinuFocus instance using the specified properties.
   * @function create
   * @memberof ReqManualContinuFocus
   * @static
   * @param {IReqManualContinuFocus=} [properties] Properties to set
   * @returns {ReqManualContinuFocus} ReqManualContinuFocus instance
   */
  ReqManualContinuFocus.create = function create(properties) {
    return new ReqManualContinuFocus(properties);
  };

  /**
   * Encodes the specified ReqManualContinuFocus message. Does not implicitly {@link ReqManualContinuFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqManualContinuFocus
   * @static
   * @param {IReqManualContinuFocus} message ReqManualContinuFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualContinuFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.direction);
    return writer;
  };

  /**
   * Encodes the specified ReqManualContinuFocus message, length delimited. Does not implicitly {@link ReqManualContinuFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqManualContinuFocus
   * @static
   * @param {IReqManualContinuFocus} message ReqManualContinuFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualContinuFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqManualContinuFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqManualContinuFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqManualContinuFocus} ReqManualContinuFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualContinuFocus.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqManualContinuFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.direction = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqManualContinuFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqManualContinuFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqManualContinuFocus} ReqManualContinuFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualContinuFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqManualContinuFocus message.
   * @function verify
   * @memberof ReqManualContinuFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqManualContinuFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (!$util.isInteger(message.direction))
        return "direction: integer expected";
    return null;
  };

  /**
   * Creates a ReqManualContinuFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqManualContinuFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqManualContinuFocus} ReqManualContinuFocus
   */
  ReqManualContinuFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqManualContinuFocus) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqManualContinuFocus: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqManualContinuFocus();
    if (object.direction != null) message.direction = object.direction >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqManualContinuFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqManualContinuFocus
   * @static
   * @param {ReqManualContinuFocus} message ReqManualContinuFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqManualContinuFocus.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.direction = 0;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    return object;
  };

  /**
   * Converts this ReqManualContinuFocus to JSON.
   * @function toJSON
   * @memberof ReqManualContinuFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqManualContinuFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqManualContinuFocus
   * @function getTypeUrl
   * @memberof ReqManualContinuFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqManualContinuFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqManualContinuFocus";
  };

  return ReqManualContinuFocus;
})();

$root.ReqStopManualContinuFocus = (function () {
  /**
   * Properties of a ReqStopManualContinuFocus.
   * @exports IReqStopManualContinuFocus
   * @interface IReqStopManualContinuFocus
   */

  /**
   * Constructs a new ReqStopManualContinuFocus.
   * @exports ReqStopManualContinuFocus
   * @classdesc Represents a ReqStopManualContinuFocus.
   * @implements IReqStopManualContinuFocus
   * @constructor
   * @param {IReqStopManualContinuFocus=} [properties] Properties to set
   */
  function ReqStopManualContinuFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopManualContinuFocus instance using the specified properties.
   * @function create
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {IReqStopManualContinuFocus=} [properties] Properties to set
   * @returns {ReqStopManualContinuFocus} ReqStopManualContinuFocus instance
   */
  ReqStopManualContinuFocus.create = function create(properties) {
    return new ReqStopManualContinuFocus(properties);
  };

  /**
   * Encodes the specified ReqStopManualContinuFocus message. Does not implicitly {@link ReqStopManualContinuFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {IReqStopManualContinuFocus} message ReqStopManualContinuFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopManualContinuFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopManualContinuFocus message, length delimited. Does not implicitly {@link ReqStopManualContinuFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {IReqStopManualContinuFocus} message ReqStopManualContinuFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopManualContinuFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopManualContinuFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopManualContinuFocus} ReqStopManualContinuFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopManualContinuFocus.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopManualContinuFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopManualContinuFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopManualContinuFocus} ReqStopManualContinuFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopManualContinuFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopManualContinuFocus message.
   * @function verify
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopManualContinuFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopManualContinuFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopManualContinuFocus} ReqStopManualContinuFocus
   */
  ReqStopManualContinuFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopManualContinuFocus) return object;
    return new $root.ReqStopManualContinuFocus();
  };

  /**
   * Creates a plain object from a ReqStopManualContinuFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {ReqStopManualContinuFocus} message ReqStopManualContinuFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopManualContinuFocus.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopManualContinuFocus to JSON.
   * @function toJSON
   * @memberof ReqStopManualContinuFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopManualContinuFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopManualContinuFocus
   * @function getTypeUrl
   * @memberof ReqStopManualContinuFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopManualContinuFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopManualContinuFocus";
  };

  return ReqStopManualContinuFocus;
})();

$root.ReqNormalAutoFocus = (function () {
  /**
   * Properties of a ReqNormalAutoFocus.
   * @exports IReqNormalAutoFocus
   * @interface IReqNormalAutoFocus
   * @property {number|null} [mode] ReqNormalAutoFocus mode
   * @property {number|null} [centerX] ReqNormalAutoFocus centerX
   * @property {number|null} [centerY] ReqNormalAutoFocus centerY
   */

  /**
   * Constructs a new ReqNormalAutoFocus.
   * @exports ReqNormalAutoFocus
   * @classdesc Represents a ReqNormalAutoFocus.
   * @implements IReqNormalAutoFocus
   * @constructor
   * @param {IReqNormalAutoFocus=} [properties] Properties to set
   */
  function ReqNormalAutoFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqNormalAutoFocus mode.
   * @member {number} mode
   * @memberof ReqNormalAutoFocus
   * @instance
   */
  ReqNormalAutoFocus.prototype.mode = 0;

  /**
   * ReqNormalAutoFocus centerX.
   * @member {number} centerX
   * @memberof ReqNormalAutoFocus
   * @instance
   */
  ReqNormalAutoFocus.prototype.centerX = 0;

  /**
   * ReqNormalAutoFocus centerY.
   * @member {number} centerY
   * @memberof ReqNormalAutoFocus
   * @instance
   */
  ReqNormalAutoFocus.prototype.centerY = 0;

  /**
   * Creates a new ReqNormalAutoFocus instance using the specified properties.
   * @function create
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {IReqNormalAutoFocus=} [properties] Properties to set
   * @returns {ReqNormalAutoFocus} ReqNormalAutoFocus instance
   */
  ReqNormalAutoFocus.create = function create(properties) {
    return new ReqNormalAutoFocus(properties);
  };

  /**
   * Encodes the specified ReqNormalAutoFocus message. Does not implicitly {@link ReqNormalAutoFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {IReqNormalAutoFocus} message ReqNormalAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqNormalAutoFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.mode);
    if (
      message.centerX != null &&
      Object.hasOwnProperty.call(message, "centerX")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.centerX);
    if (
      message.centerY != null &&
      Object.hasOwnProperty.call(message, "centerY")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.centerY);
    return writer;
  };

  /**
   * Encodes the specified ReqNormalAutoFocus message, length delimited. Does not implicitly {@link ReqNormalAutoFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {IReqNormalAutoFocus} message ReqNormalAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqNormalAutoFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqNormalAutoFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqNormalAutoFocus} ReqNormalAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqNormalAutoFocus.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqNormalAutoFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.uint32();
          break;
        }
        case 2: {
          message.centerX = reader.uint32();
          break;
        }
        case 3: {
          message.centerY = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqNormalAutoFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqNormalAutoFocus} ReqNormalAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqNormalAutoFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqNormalAutoFocus message.
   * @function verify
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqNormalAutoFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (
      message.centerX != null &&
      Object.hasOwnProperty.call(message, "centerX")
    )
      if (!$util.isInteger(message.centerX)) return "centerX: integer expected";
    if (
      message.centerY != null &&
      Object.hasOwnProperty.call(message, "centerY")
    )
      if (!$util.isInteger(message.centerY)) return "centerY: integer expected";
    return null;
  };

  /**
   * Creates a ReqNormalAutoFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqNormalAutoFocus} ReqNormalAutoFocus
   */
  ReqNormalAutoFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqNormalAutoFocus) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqNormalAutoFocus: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqNormalAutoFocus();
    if (object.mode != null) message.mode = object.mode >>> 0;
    if (object.centerX != null) message.centerX = object.centerX >>> 0;
    if (object.centerY != null) message.centerY = object.centerY >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqNormalAutoFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {ReqNormalAutoFocus} message ReqNormalAutoFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqNormalAutoFocus.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.mode = 0;
      object.centerX = 0;
      object.centerY = 0;
    }
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (
      message.centerX != null &&
      Object.hasOwnProperty.call(message, "centerX")
    )
      object.centerX = message.centerX;
    if (
      message.centerY != null &&
      Object.hasOwnProperty.call(message, "centerY")
    )
      object.centerY = message.centerY;
    return object;
  };

  /**
   * Converts this ReqNormalAutoFocus to JSON.
   * @function toJSON
   * @memberof ReqNormalAutoFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqNormalAutoFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqNormalAutoFocus
   * @function getTypeUrl
   * @memberof ReqNormalAutoFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqNormalAutoFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqNormalAutoFocus";
  };

  return ReqNormalAutoFocus;
})();

$root.ReqAstroAutoFocus = (function () {
  /**
   * Properties of a ReqAstroAutoFocus.
   * @exports IReqAstroAutoFocus
   * @interface IReqAstroAutoFocus
   * @property {number|null} [mode] ReqAstroAutoFocus mode
   */

  /**
   * Constructs a new ReqAstroAutoFocus.
   * @exports ReqAstroAutoFocus
   * @classdesc Represents a ReqAstroAutoFocus.
   * @implements IReqAstroAutoFocus
   * @constructor
   * @param {IReqAstroAutoFocus=} [properties] Properties to set
   */
  function ReqAstroAutoFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqAstroAutoFocus mode.
   * @member {number} mode
   * @memberof ReqAstroAutoFocus
   * @instance
   */
  ReqAstroAutoFocus.prototype.mode = 0;

  /**
   * Creates a new ReqAstroAutoFocus instance using the specified properties.
   * @function create
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {IReqAstroAutoFocus=} [properties] Properties to set
   * @returns {ReqAstroAutoFocus} ReqAstroAutoFocus instance
   */
  ReqAstroAutoFocus.create = function create(properties) {
    return new ReqAstroAutoFocus(properties);
  };

  /**
   * Encodes the specified ReqAstroAutoFocus message. Does not implicitly {@link ReqAstroAutoFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {IReqAstroAutoFocus} message ReqAstroAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAstroAutoFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqAstroAutoFocus message, length delimited. Does not implicitly {@link ReqAstroAutoFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {IReqAstroAutoFocus} message ReqAstroAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqAstroAutoFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqAstroAutoFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqAstroAutoFocus} ReqAstroAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAstroAutoFocus.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqAstroAutoFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqAstroAutoFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqAstroAutoFocus} ReqAstroAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqAstroAutoFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqAstroAutoFocus message.
   * @function verify
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqAstroAutoFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqAstroAutoFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqAstroAutoFocus} ReqAstroAutoFocus
   */
  ReqAstroAutoFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqAstroAutoFocus) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqAstroAutoFocus: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqAstroAutoFocus();
    if (object.mode != null) message.mode = object.mode >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqAstroAutoFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {ReqAstroAutoFocus} message ReqAstroAutoFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqAstroAutoFocus.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqAstroAutoFocus to JSON.
   * @function toJSON
   * @memberof ReqAstroAutoFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqAstroAutoFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqAstroAutoFocus
   * @function getTypeUrl
   * @memberof ReqAstroAutoFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqAstroAutoFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqAstroAutoFocus";
  };

  return ReqAstroAutoFocus;
})();

$root.ReqStopAstroAutoFocus = (function () {
  /**
   * Properties of a ReqStopAstroAutoFocus.
   * @exports IReqStopAstroAutoFocus
   * @interface IReqStopAstroAutoFocus
   */

  /**
   * Constructs a new ReqStopAstroAutoFocus.
   * @exports ReqStopAstroAutoFocus
   * @classdesc Represents a ReqStopAstroAutoFocus.
   * @implements IReqStopAstroAutoFocus
   * @constructor
   * @param {IReqStopAstroAutoFocus=} [properties] Properties to set
   */
  function ReqStopAstroAutoFocus(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopAstroAutoFocus instance using the specified properties.
   * @function create
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {IReqStopAstroAutoFocus=} [properties] Properties to set
   * @returns {ReqStopAstroAutoFocus} ReqStopAstroAutoFocus instance
   */
  ReqStopAstroAutoFocus.create = function create(properties) {
    return new ReqStopAstroAutoFocus(properties);
  };

  /**
   * Encodes the specified ReqStopAstroAutoFocus message. Does not implicitly {@link ReqStopAstroAutoFocus.verify|verify} messages.
   * @function encode
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {IReqStopAstroAutoFocus} message ReqStopAstroAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopAstroAutoFocus.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopAstroAutoFocus message, length delimited. Does not implicitly {@link ReqStopAstroAutoFocus.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {IReqStopAstroAutoFocus} message ReqStopAstroAutoFocus message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopAstroAutoFocus.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopAstroAutoFocus message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopAstroAutoFocus} ReqStopAstroAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopAstroAutoFocus.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopAstroAutoFocus();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopAstroAutoFocus message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopAstroAutoFocus} ReqStopAstroAutoFocus
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopAstroAutoFocus.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopAstroAutoFocus message.
   * @function verify
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopAstroAutoFocus.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopAstroAutoFocus message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopAstroAutoFocus} ReqStopAstroAutoFocus
   */
  ReqStopAstroAutoFocus.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopAstroAutoFocus) return object;
    return new $root.ReqStopAstroAutoFocus();
  };

  /**
   * Creates a plain object from a ReqStopAstroAutoFocus message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {ReqStopAstroAutoFocus} message ReqStopAstroAutoFocus
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopAstroAutoFocus.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopAstroAutoFocus to JSON.
   * @function toJSON
   * @memberof ReqStopAstroAutoFocus
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopAstroAutoFocus.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopAstroAutoFocus
   * @function getTypeUrl
   * @memberof ReqStopAstroAutoFocus
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopAstroAutoFocus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopAstroAutoFocus";
  };

  return ReqStopAstroAutoFocus;
})();

$root.ReqManualSingleStepFocusForFactory = (function () {
  /**
   * Properties of a ReqManualSingleStepFocusForFactory.
   * @exports IReqManualSingleStepFocusForFactory
   * @interface IReqManualSingleStepFocusForFactory
   * @property {number|null} [direction] ReqManualSingleStepFocusForFactory direction
   * @property {number|null} [resolutionLevel] ReqManualSingleStepFocusForFactory resolutionLevel
   */

  /**
   * Constructs a new ReqManualSingleStepFocusForFactory.
   * @exports ReqManualSingleStepFocusForFactory
   * @classdesc Represents a ReqManualSingleStepFocusForFactory.
   * @implements IReqManualSingleStepFocusForFactory
   * @constructor
   * @param {IReqManualSingleStepFocusForFactory=} [properties] Properties to set
   */
  function ReqManualSingleStepFocusForFactory(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqManualSingleStepFocusForFactory direction.
   * @member {number} direction
   * @memberof ReqManualSingleStepFocusForFactory
   * @instance
   */
  ReqManualSingleStepFocusForFactory.prototype.direction = 0;

  /**
   * ReqManualSingleStepFocusForFactory resolutionLevel.
   * @member {number} resolutionLevel
   * @memberof ReqManualSingleStepFocusForFactory
   * @instance
   */
  ReqManualSingleStepFocusForFactory.prototype.resolutionLevel = 0;

  /**
   * Creates a new ReqManualSingleStepFocusForFactory instance using the specified properties.
   * @function create
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {IReqManualSingleStepFocusForFactory=} [properties] Properties to set
   * @returns {ReqManualSingleStepFocusForFactory} ReqManualSingleStepFocusForFactory instance
   */
  ReqManualSingleStepFocusForFactory.create = function create(properties) {
    return new ReqManualSingleStepFocusForFactory(properties);
  };

  /**
   * Encodes the specified ReqManualSingleStepFocusForFactory message. Does not implicitly {@link ReqManualSingleStepFocusForFactory.verify|verify} messages.
   * @function encode
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {IReqManualSingleStepFocusForFactory} message ReqManualSingleStepFocusForFactory message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualSingleStepFocusForFactory.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.direction);
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.resolutionLevel);
    return writer;
  };

  /**
   * Encodes the specified ReqManualSingleStepFocusForFactory message, length delimited. Does not implicitly {@link ReqManualSingleStepFocusForFactory.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {IReqManualSingleStepFocusForFactory} message ReqManualSingleStepFocusForFactory message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqManualSingleStepFocusForFactory.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqManualSingleStepFocusForFactory message from the specified reader or buffer.
   * @function decode
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqManualSingleStepFocusForFactory} ReqManualSingleStepFocusForFactory
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualSingleStepFocusForFactory.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqManualSingleStepFocusForFactory();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.direction = reader.uint32();
          break;
        }
        case 2: {
          message.resolutionLevel = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqManualSingleStepFocusForFactory message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqManualSingleStepFocusForFactory} ReqManualSingleStepFocusForFactory
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqManualSingleStepFocusForFactory.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqManualSingleStepFocusForFactory message.
   * @function verify
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqManualSingleStepFocusForFactory.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (!$util.isInteger(message.direction))
        return "direction: integer expected";
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      if (!$util.isInteger(message.resolutionLevel))
        return "resolutionLevel: integer expected";
    return null;
  };

  /**
   * Creates a ReqManualSingleStepFocusForFactory message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqManualSingleStepFocusForFactory} ReqManualSingleStepFocusForFactory
   */
  ReqManualSingleStepFocusForFactory.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqManualSingleStepFocusForFactory)
      return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqManualSingleStepFocusForFactory: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqManualSingleStepFocusForFactory();
    if (object.direction != null) message.direction = object.direction >>> 0;
    if (object.resolutionLevel != null)
      message.resolutionLevel = object.resolutionLevel >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqManualSingleStepFocusForFactory message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {ReqManualSingleStepFocusForFactory} message ReqManualSingleStepFocusForFactory
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqManualSingleStepFocusForFactory.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.direction = 0;
      object.resolutionLevel = 0;
    }
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      object.resolutionLevel = message.resolutionLevel;
    return object;
  };

  /**
   * Converts this ReqManualSingleStepFocusForFactory to JSON.
   * @function toJSON
   * @memberof ReqManualSingleStepFocusForFactory
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqManualSingleStepFocusForFactory.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqManualSingleStepFocusForFactory
   * @function getTypeUrl
   * @memberof ReqManualSingleStepFocusForFactory
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqManualSingleStepFocusForFactory.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqManualSingleStepFocusForFactory";
  };

  return ReqManualSingleStepFocusForFactory;
})();

$root.ReqGetUserInfinityPos = (function () {
  /**
   * Properties of a ReqGetUserInfinityPos.
   * @exports IReqGetUserInfinityPos
   * @interface IReqGetUserInfinityPos
   */

  /**
   * Constructs a new ReqGetUserInfinityPos.
   * @exports ReqGetUserInfinityPos
   * @classdesc Represents a ReqGetUserInfinityPos.
   * @implements IReqGetUserInfinityPos
   * @constructor
   * @param {IReqGetUserInfinityPos=} [properties] Properties to set
   */
  function ReqGetUserInfinityPos(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetUserInfinityPos instance using the specified properties.
   * @function create
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {IReqGetUserInfinityPos=} [properties] Properties to set
   * @returns {ReqGetUserInfinityPos} ReqGetUserInfinityPos instance
   */
  ReqGetUserInfinityPos.create = function create(properties) {
    return new ReqGetUserInfinityPos(properties);
  };

  /**
   * Encodes the specified ReqGetUserInfinityPos message. Does not implicitly {@link ReqGetUserInfinityPos.verify|verify} messages.
   * @function encode
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {IReqGetUserInfinityPos} message ReqGetUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetUserInfinityPos.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetUserInfinityPos message, length delimited. Does not implicitly {@link ReqGetUserInfinityPos.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {IReqGetUserInfinityPos} message ReqGetUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetUserInfinityPos.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetUserInfinityPos message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetUserInfinityPos} ReqGetUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetUserInfinityPos.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetUserInfinityPos();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetUserInfinityPos message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetUserInfinityPos} ReqGetUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetUserInfinityPos.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetUserInfinityPos message.
   * @function verify
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetUserInfinityPos.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetUserInfinityPos message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetUserInfinityPos} ReqGetUserInfinityPos
   */
  ReqGetUserInfinityPos.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetUserInfinityPos) return object;
    return new $root.ReqGetUserInfinityPos();
  };

  /**
   * Creates a plain object from a ReqGetUserInfinityPos message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {ReqGetUserInfinityPos} message ReqGetUserInfinityPos
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetUserInfinityPos.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetUserInfinityPos to JSON.
   * @function toJSON
   * @memberof ReqGetUserInfinityPos
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetUserInfinityPos.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetUserInfinityPos
   * @function getTypeUrl
   * @memberof ReqGetUserInfinityPos
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetUserInfinityPos.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetUserInfinityPos";
  };

  return ReqGetUserInfinityPos;
})();

$root.ReqSetUserInfinityPos = (function () {
  /**
   * Properties of a ReqSetUserInfinityPos.
   * @exports IReqSetUserInfinityPos
   * @interface IReqSetUserInfinityPos
   * @property {number|null} [pos] ReqSetUserInfinityPos pos
   */

  /**
   * Constructs a new ReqSetUserInfinityPos.
   * @exports ReqSetUserInfinityPos
   * @classdesc Represents a ReqSetUserInfinityPos.
   * @implements IReqSetUserInfinityPos
   * @constructor
   * @param {IReqSetUserInfinityPos=} [properties] Properties to set
   */
  function ReqSetUserInfinityPos(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetUserInfinityPos pos.
   * @member {number} pos
   * @memberof ReqSetUserInfinityPos
   * @instance
   */
  ReqSetUserInfinityPos.prototype.pos = 0;

  /**
   * Creates a new ReqSetUserInfinityPos instance using the specified properties.
   * @function create
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {IReqSetUserInfinityPos=} [properties] Properties to set
   * @returns {ReqSetUserInfinityPos} ReqSetUserInfinityPos instance
   */
  ReqSetUserInfinityPos.create = function create(properties) {
    return new ReqSetUserInfinityPos(properties);
  };

  /**
   * Encodes the specified ReqSetUserInfinityPos message. Does not implicitly {@link ReqSetUserInfinityPos.verify|verify} messages.
   * @function encode
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {IReqSetUserInfinityPos} message ReqSetUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetUserInfinityPos.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.pos);
    return writer;
  };

  /**
   * Encodes the specified ReqSetUserInfinityPos message, length delimited. Does not implicitly {@link ReqSetUserInfinityPos.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {IReqSetUserInfinityPos} message ReqSetUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetUserInfinityPos.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetUserInfinityPos message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetUserInfinityPos} ReqSetUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetUserInfinityPos.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetUserInfinityPos();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.pos = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetUserInfinityPos message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetUserInfinityPos} ReqSetUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetUserInfinityPos.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetUserInfinityPos message.
   * @function verify
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetUserInfinityPos.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      if (!$util.isInteger(message.pos)) return "pos: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetUserInfinityPos message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetUserInfinityPos} ReqSetUserInfinityPos
   */
  ReqSetUserInfinityPos.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetUserInfinityPos) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetUserInfinityPos: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetUserInfinityPos();
    if (object.pos != null) message.pos = object.pos | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetUserInfinityPos message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {ReqSetUserInfinityPos} message ReqSetUserInfinityPos
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetUserInfinityPos.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.pos = 0;
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      object.pos = message.pos;
    return object;
  };

  /**
   * Converts this ReqSetUserInfinityPos to JSON.
   * @function toJSON
   * @memberof ReqSetUserInfinityPos
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetUserInfinityPos.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetUserInfinityPos
   * @function getTypeUrl
   * @memberof ReqSetUserInfinityPos
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetUserInfinityPos.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetUserInfinityPos";
  };

  return ReqSetUserInfinityPos;
})();

$root.ResUserInfinityPos = (function () {
  /**
   * Properties of a ResUserInfinityPos.
   * @exports IResUserInfinityPos
   * @interface IResUserInfinityPos
   * @property {number|null} [code] ResUserInfinityPos code
   * @property {number|null} [pos] ResUserInfinityPos pos
   */

  /**
   * Constructs a new ResUserInfinityPos.
   * @exports ResUserInfinityPos
   * @classdesc Represents a ResUserInfinityPos.
   * @implements IResUserInfinityPos
   * @constructor
   * @param {IResUserInfinityPos=} [properties] Properties to set
   */
  function ResUserInfinityPos(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResUserInfinityPos code.
   * @member {number} code
   * @memberof ResUserInfinityPos
   * @instance
   */
  ResUserInfinityPos.prototype.code = 0;

  /**
   * ResUserInfinityPos pos.
   * @member {number} pos
   * @memberof ResUserInfinityPos
   * @instance
   */
  ResUserInfinityPos.prototype.pos = 0;

  /**
   * Creates a new ResUserInfinityPos instance using the specified properties.
   * @function create
   * @memberof ResUserInfinityPos
   * @static
   * @param {IResUserInfinityPos=} [properties] Properties to set
   * @returns {ResUserInfinityPos} ResUserInfinityPos instance
   */
  ResUserInfinityPos.create = function create(properties) {
    return new ResUserInfinityPos(properties);
  };

  /**
   * Encodes the specified ResUserInfinityPos message. Does not implicitly {@link ResUserInfinityPos.verify|verify} messages.
   * @function encode
   * @memberof ResUserInfinityPos
   * @static
   * @param {IResUserInfinityPos} message ResUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResUserInfinityPos.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.pos);
    return writer;
  };

  /**
   * Encodes the specified ResUserInfinityPos message, length delimited. Does not implicitly {@link ResUserInfinityPos.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResUserInfinityPos
   * @static
   * @param {IResUserInfinityPos} message ResUserInfinityPos message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResUserInfinityPos.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResUserInfinityPos message from the specified reader or buffer.
   * @function decode
   * @memberof ResUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResUserInfinityPos} ResUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResUserInfinityPos.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResUserInfinityPos();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.pos = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResUserInfinityPos message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResUserInfinityPos
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResUserInfinityPos} ResUserInfinityPos
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResUserInfinityPos.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResUserInfinityPos message.
   * @function verify
   * @memberof ResUserInfinityPos
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResUserInfinityPos.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      if (!$util.isInteger(message.pos)) return "pos: integer expected";
    return null;
  };

  /**
   * Creates a ResUserInfinityPos message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResUserInfinityPos
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResUserInfinityPos} ResUserInfinityPos
   */
  ResUserInfinityPos.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResUserInfinityPos) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResUserInfinityPos: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResUserInfinityPos();
    if (object.code != null) message.code = object.code | 0;
    if (object.pos != null) message.pos = object.pos | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResUserInfinityPos message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResUserInfinityPos
   * @static
   * @param {ResUserInfinityPos} message ResUserInfinityPos
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResUserInfinityPos.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.pos = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
      object.pos = message.pos;
    return object;
  };

  /**
   * Converts this ResUserInfinityPos to JSON.
   * @function toJSON
   * @memberof ResUserInfinityPos
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResUserInfinityPos.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResUserInfinityPos
   * @function getTypeUrl
   * @memberof ResUserInfinityPos
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResUserInfinityPos.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResUserInfinityPos";
  };

  return ResUserInfinityPos;
})();

$root.ReqSfrAutoFocusForFactory = (function () {
  /**
   * Properties of a ReqSfrAutoFocusForFactory.
   * @exports IReqSfrAutoFocusForFactory
   * @interface IReqSfrAutoFocusForFactory
   * @property {number|null} [mode] ReqSfrAutoFocusForFactory mode
   * @property {number|null} [fastSpos] ReqSfrAutoFocusForFactory fastSpos
   * @property {number|null} [fastPulse] ReqSfrAutoFocusForFactory fastPulse
   * @property {number|null} [fastScope] ReqSfrAutoFocusForFactory fastScope
   * @property {number|null} [slowPulse] ReqSfrAutoFocusForFactory slowPulse
   * @property {number|null} [slowScope] ReqSfrAutoFocusForFactory slowScope
   */

  /**
   * Constructs a new ReqSfrAutoFocusForFactory.
   * @exports ReqSfrAutoFocusForFactory
   * @classdesc Represents a ReqSfrAutoFocusForFactory.
   * @implements IReqSfrAutoFocusForFactory
   * @constructor
   * @param {IReqSfrAutoFocusForFactory=} [properties] Properties to set
   */
  function ReqSfrAutoFocusForFactory(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSfrAutoFocusForFactory mode.
   * @member {number} mode
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.mode = 0;

  /**
   * ReqSfrAutoFocusForFactory fastSpos.
   * @member {number|null|undefined} fastSpos
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.fastSpos = null;

  /**
   * ReqSfrAutoFocusForFactory fastPulse.
   * @member {number|null|undefined} fastPulse
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.fastPulse = null;

  /**
   * ReqSfrAutoFocusForFactory fastScope.
   * @member {number|null|undefined} fastScope
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.fastScope = null;

  /**
   * ReqSfrAutoFocusForFactory slowPulse.
   * @member {number|null|undefined} slowPulse
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.slowPulse = null;

  /**
   * ReqSfrAutoFocusForFactory slowScope.
   * @member {number|null|undefined} slowScope
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   */
  ReqSfrAutoFocusForFactory.prototype.slowScope = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqSfrAutoFocusForFactory.prototype, "_fastSpos", {
    get: $util.oneOfGetter(($oneOfFields = ["fastSpos"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqSfrAutoFocusForFactory.prototype, "_fastPulse", {
    get: $util.oneOfGetter(($oneOfFields = ["fastPulse"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqSfrAutoFocusForFactory.prototype, "_fastScope", {
    get: $util.oneOfGetter(($oneOfFields = ["fastScope"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqSfrAutoFocusForFactory.prototype, "_slowPulse", {
    get: $util.oneOfGetter(($oneOfFields = ["slowPulse"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(ReqSfrAutoFocusForFactory.prototype, "_slowScope", {
    get: $util.oneOfGetter(($oneOfFields = ["slowScope"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqSfrAutoFocusForFactory instance using the specified properties.
   * @function create
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {IReqSfrAutoFocusForFactory=} [properties] Properties to set
   * @returns {ReqSfrAutoFocusForFactory} ReqSfrAutoFocusForFactory instance
   */
  ReqSfrAutoFocusForFactory.create = function create(properties) {
    return new ReqSfrAutoFocusForFactory(properties);
  };

  /**
   * Encodes the specified ReqSfrAutoFocusForFactory message. Does not implicitly {@link ReqSfrAutoFocusForFactory.verify|verify} messages.
   * @function encode
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {IReqSfrAutoFocusForFactory} message ReqSfrAutoFocusForFactory message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSfrAutoFocusForFactory.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.mode);
    if (
      message.fastSpos != null &&
      Object.hasOwnProperty.call(message, "fastSpos")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.fastSpos);
    if (
      message.fastPulse != null &&
      Object.hasOwnProperty.call(message, "fastPulse")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.fastPulse);
    if (
      message.fastScope != null &&
      Object.hasOwnProperty.call(message, "fastScope")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.fastScope);
    if (
      message.slowPulse != null &&
      Object.hasOwnProperty.call(message, "slowPulse")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.slowPulse);
    if (
      message.slowScope != null &&
      Object.hasOwnProperty.call(message, "slowScope")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.slowScope);
    return writer;
  };

  /**
   * Encodes the specified ReqSfrAutoFocusForFactory message, length delimited. Does not implicitly {@link ReqSfrAutoFocusForFactory.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {IReqSfrAutoFocusForFactory} message ReqSfrAutoFocusForFactory message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSfrAutoFocusForFactory.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSfrAutoFocusForFactory message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSfrAutoFocusForFactory} ReqSfrAutoFocusForFactory
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSfrAutoFocusForFactory.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSfrAutoFocusForFactory();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.uint32();
          break;
        }
        case 2: {
          message.fastSpos = reader.int32();
          break;
        }
        case 3: {
          message.fastPulse = reader.int32();
          break;
        }
        case 4: {
          message.fastScope = reader.int32();
          break;
        }
        case 5: {
          message.slowPulse = reader.int32();
          break;
        }
        case 6: {
          message.slowScope = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSfrAutoFocusForFactory message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSfrAutoFocusForFactory} ReqSfrAutoFocusForFactory
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSfrAutoFocusForFactory.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSfrAutoFocusForFactory message.
   * @function verify
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSfrAutoFocusForFactory.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    if (
      message.fastSpos != null &&
      Object.hasOwnProperty.call(message, "fastSpos")
    ) {
      properties._fastSpos = 1;
      if (!$util.isInteger(message.fastSpos))
        return "fastSpos: integer expected";
    }
    if (
      message.fastPulse != null &&
      Object.hasOwnProperty.call(message, "fastPulse")
    ) {
      properties._fastPulse = 1;
      if (!$util.isInteger(message.fastPulse))
        return "fastPulse: integer expected";
    }
    if (
      message.fastScope != null &&
      Object.hasOwnProperty.call(message, "fastScope")
    ) {
      properties._fastScope = 1;
      if (!$util.isInteger(message.fastScope))
        return "fastScope: integer expected";
    }
    if (
      message.slowPulse != null &&
      Object.hasOwnProperty.call(message, "slowPulse")
    ) {
      properties._slowPulse = 1;
      if (!$util.isInteger(message.slowPulse))
        return "slowPulse: integer expected";
    }
    if (
      message.slowScope != null &&
      Object.hasOwnProperty.call(message, "slowScope")
    ) {
      properties._slowScope = 1;
      if (!$util.isInteger(message.slowScope))
        return "slowScope: integer expected";
    }
    return null;
  };

  /**
   * Creates a ReqSfrAutoFocusForFactory message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSfrAutoFocusForFactory} ReqSfrAutoFocusForFactory
   */
  ReqSfrAutoFocusForFactory.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSfrAutoFocusForFactory) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSfrAutoFocusForFactory: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSfrAutoFocusForFactory();
    if (object.mode != null) message.mode = object.mode >>> 0;
    if (object.fastSpos != null) message.fastSpos = object.fastSpos | 0;
    if (object.fastPulse != null) message.fastPulse = object.fastPulse | 0;
    if (object.fastScope != null) message.fastScope = object.fastScope | 0;
    if (object.slowPulse != null) message.slowPulse = object.slowPulse | 0;
    if (object.slowScope != null) message.slowScope = object.slowScope | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSfrAutoFocusForFactory message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {ReqSfrAutoFocusForFactory} message ReqSfrAutoFocusForFactory
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSfrAutoFocusForFactory.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    if (
      message.fastSpos != null &&
      Object.hasOwnProperty.call(message, "fastSpos")
    ) {
      object.fastSpos = message.fastSpos;
      if (options.oneofs) object._fastSpos = "fastSpos";
    }
    if (
      message.fastPulse != null &&
      Object.hasOwnProperty.call(message, "fastPulse")
    ) {
      object.fastPulse = message.fastPulse;
      if (options.oneofs) object._fastPulse = "fastPulse";
    }
    if (
      message.fastScope != null &&
      Object.hasOwnProperty.call(message, "fastScope")
    ) {
      object.fastScope = message.fastScope;
      if (options.oneofs) object._fastScope = "fastScope";
    }
    if (
      message.slowPulse != null &&
      Object.hasOwnProperty.call(message, "slowPulse")
    ) {
      object.slowPulse = message.slowPulse;
      if (options.oneofs) object._slowPulse = "slowPulse";
    }
    if (
      message.slowScope != null &&
      Object.hasOwnProperty.call(message, "slowScope")
    ) {
      object.slowScope = message.slowScope;
      if (options.oneofs) object._slowScope = "slowScope";
    }
    return object;
  };

  /**
   * Converts this ReqSfrAutoFocusForFactory to JSON.
   * @function toJSON
   * @memberof ReqSfrAutoFocusForFactory
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSfrAutoFocusForFactory.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSfrAutoFocusForFactory
   * @function getTypeUrl
   * @memberof ReqSfrAutoFocusForFactory
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSfrAutoFocusForFactory.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSfrAutoFocusForFactory";
  };

  return ReqSfrAutoFocusForFactory;
})();

$root.ReqSetVcmCode = (function () {
  /**
   * Properties of a ReqSetVcmCode.
   * @exports IReqSetVcmCode
   * @interface IReqSetVcmCode
   * @property {number|null} [codeValue] ReqSetVcmCode codeValue
   */

  /**
   * Constructs a new ReqSetVcmCode.
   * @exports ReqSetVcmCode
   * @classdesc Represents a ReqSetVcmCode.
   * @implements IReqSetVcmCode
   * @constructor
   * @param {IReqSetVcmCode=} [properties] Properties to set
   */
  function ReqSetVcmCode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetVcmCode codeValue.
   * @member {number} codeValue
   * @memberof ReqSetVcmCode
   * @instance
   */
  ReqSetVcmCode.prototype.codeValue = 0;

  /**
   * Creates a new ReqSetVcmCode instance using the specified properties.
   * @function create
   * @memberof ReqSetVcmCode
   * @static
   * @param {IReqSetVcmCode=} [properties] Properties to set
   * @returns {ReqSetVcmCode} ReqSetVcmCode instance
   */
  ReqSetVcmCode.create = function create(properties) {
    return new ReqSetVcmCode(properties);
  };

  /**
   * Encodes the specified ReqSetVcmCode message. Does not implicitly {@link ReqSetVcmCode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetVcmCode
   * @static
   * @param {IReqSetVcmCode} message ReqSetVcmCode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetVcmCode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.codeValue != null &&
      Object.hasOwnProperty.call(message, "codeValue")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.codeValue);
    return writer;
  };

  /**
   * Encodes the specified ReqSetVcmCode message, length delimited. Does not implicitly {@link ReqSetVcmCode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetVcmCode
   * @static
   * @param {IReqSetVcmCode} message ReqSetVcmCode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetVcmCode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetVcmCode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetVcmCode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetVcmCode} ReqSetVcmCode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetVcmCode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetVcmCode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.codeValue = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetVcmCode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetVcmCode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetVcmCode} ReqSetVcmCode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetVcmCode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetVcmCode message.
   * @function verify
   * @memberof ReqSetVcmCode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetVcmCode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.codeValue != null &&
      Object.hasOwnProperty.call(message, "codeValue")
    )
      if (!$util.isInteger(message.codeValue))
        return "codeValue: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetVcmCode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetVcmCode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetVcmCode} ReqSetVcmCode
   */
  ReqSetVcmCode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetVcmCode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetVcmCode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetVcmCode();
    if (object.codeValue != null) message.codeValue = object.codeValue | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetVcmCode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetVcmCode
   * @static
   * @param {ReqSetVcmCode} message ReqSetVcmCode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetVcmCode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.codeValue = 0;
    if (
      message.codeValue != null &&
      Object.hasOwnProperty.call(message, "codeValue")
    )
      object.codeValue = message.codeValue;
    return object;
  };

  /**
   * Converts this ReqSetVcmCode to JSON.
   * @function toJSON
   * @memberof ReqSetVcmCode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetVcmCode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetVcmCode
   * @function getTypeUrl
   * @memberof ReqSetVcmCode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetVcmCode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetVcmCode";
  };

  return ReqSetVcmCode;
})();

/**
 * MotorLevelCalibrationDirection enum.
 * @exports MotorLevelCalibrationDirection
 * @enum {number}
 * @property {number} MOTOR_LEVEL_CALIBRATION_DIRECTION_CW=0 MOTOR_LEVEL_CALIBRATION_DIRECTION_CW value
 * @property {number} MOTOR_LEVEL_CALIBRATION_DIRECTION_CCW=1 MOTOR_LEVEL_CALIBRATION_DIRECTION_CCW value
 */
$root.MotorLevelCalibrationDirection = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "MOTOR_LEVEL_CALIBRATION_DIRECTION_CW")] = 0;
  values[(valuesById[1] = "MOTOR_LEVEL_CALIBRATION_DIRECTION_CCW")] = 1;
  return values;
})();

$root.ReqMotorServiceJoystick = (function () {
  /**
   * Properties of a ReqMotorServiceJoystick.
   * @exports IReqMotorServiceJoystick
   * @interface IReqMotorServiceJoystick
   * @property {number|null} [vectorAngle] ReqMotorServiceJoystick vectorAngle
   * @property {number|null} [vectorLength] ReqMotorServiceJoystick vectorLength
   */

  /**
   * Constructs a new ReqMotorServiceJoystick.
   * @exports ReqMotorServiceJoystick
   * @classdesc Represents a ReqMotorServiceJoystick.
   * @implements IReqMotorServiceJoystick
   * @constructor
   * @param {IReqMotorServiceJoystick=} [properties] Properties to set
   */
  function ReqMotorServiceJoystick(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorServiceJoystick vectorAngle.
   * @member {number} vectorAngle
   * @memberof ReqMotorServiceJoystick
   * @instance
   */
  ReqMotorServiceJoystick.prototype.vectorAngle = 0;

  /**
   * ReqMotorServiceJoystick vectorLength.
   * @member {number} vectorLength
   * @memberof ReqMotorServiceJoystick
   * @instance
   */
  ReqMotorServiceJoystick.prototype.vectorLength = 0;

  /**
   * Creates a new ReqMotorServiceJoystick instance using the specified properties.
   * @function create
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {IReqMotorServiceJoystick=} [properties] Properties to set
   * @returns {ReqMotorServiceJoystick} ReqMotorServiceJoystick instance
   */
  ReqMotorServiceJoystick.create = function create(properties) {
    return new ReqMotorServiceJoystick(properties);
  };

  /**
   * Encodes the specified ReqMotorServiceJoystick message. Does not implicitly {@link ReqMotorServiceJoystick.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {IReqMotorServiceJoystick} message ReqMotorServiceJoystick message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystick.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.vectorAngle);
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.vectorLength);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorServiceJoystick message, length delimited. Does not implicitly {@link ReqMotorServiceJoystick.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {IReqMotorServiceJoystick} message ReqMotorServiceJoystick message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystick.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorServiceJoystick message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorServiceJoystick} ReqMotorServiceJoystick
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystick.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorServiceJoystick();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.vectorAngle = reader.double();
          break;
        }
        case 2: {
          message.vectorLength = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorServiceJoystick message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorServiceJoystick} ReqMotorServiceJoystick
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystick.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorServiceJoystick message.
   * @function verify
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorServiceJoystick.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      if (typeof message.vectorAngle !== "number")
        return "vectorAngle: number expected";
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      if (typeof message.vectorLength !== "number")
        return "vectorLength: number expected";
    return null;
  };

  /**
   * Creates a ReqMotorServiceJoystick message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorServiceJoystick} ReqMotorServiceJoystick
   */
  ReqMotorServiceJoystick.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorServiceJoystick) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorServiceJoystick: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorServiceJoystick();
    if (object.vectorAngle != null)
      message.vectorAngle = Number(object.vectorAngle);
    if (object.vectorLength != null)
      message.vectorLength = Number(object.vectorLength);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorServiceJoystick message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {ReqMotorServiceJoystick} message ReqMotorServiceJoystick
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorServiceJoystick.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.vectorAngle = 0;
      object.vectorLength = 0;
    }
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      object.vectorAngle =
        options.json && !isFinite(message.vectorAngle)
          ? String(message.vectorAngle)
          : message.vectorAngle;
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      object.vectorLength =
        options.json && !isFinite(message.vectorLength)
          ? String(message.vectorLength)
          : message.vectorLength;
    return object;
  };

  /**
   * Converts this ReqMotorServiceJoystick to JSON.
   * @function toJSON
   * @memberof ReqMotorServiceJoystick
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorServiceJoystick.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorServiceJoystick
   * @function getTypeUrl
   * @memberof ReqMotorServiceJoystick
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorServiceJoystick.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorServiceJoystick";
  };

  return ReqMotorServiceJoystick;
})();

$root.ReqMotorServiceJoystickFixedAngle = (function () {
  /**
   * Properties of a ReqMotorServiceJoystickFixedAngle.
   * @exports IReqMotorServiceJoystickFixedAngle
   * @interface IReqMotorServiceJoystickFixedAngle
   * @property {number|null} [vectorAngle] ReqMotorServiceJoystickFixedAngle vectorAngle
   * @property {number|null} [vectorLength] ReqMotorServiceJoystickFixedAngle vectorLength
   */

  /**
   * Constructs a new ReqMotorServiceJoystickFixedAngle.
   * @exports ReqMotorServiceJoystickFixedAngle
   * @classdesc Represents a ReqMotorServiceJoystickFixedAngle.
   * @implements IReqMotorServiceJoystickFixedAngle
   * @constructor
   * @param {IReqMotorServiceJoystickFixedAngle=} [properties] Properties to set
   */
  function ReqMotorServiceJoystickFixedAngle(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorServiceJoystickFixedAngle vectorAngle.
   * @member {number} vectorAngle
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @instance
   */
  ReqMotorServiceJoystickFixedAngle.prototype.vectorAngle = 0;

  /**
   * ReqMotorServiceJoystickFixedAngle vectorLength.
   * @member {number} vectorLength
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @instance
   */
  ReqMotorServiceJoystickFixedAngle.prototype.vectorLength = 0;

  /**
   * Creates a new ReqMotorServiceJoystickFixedAngle instance using the specified properties.
   * @function create
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {IReqMotorServiceJoystickFixedAngle=} [properties] Properties to set
   * @returns {ReqMotorServiceJoystickFixedAngle} ReqMotorServiceJoystickFixedAngle instance
   */
  ReqMotorServiceJoystickFixedAngle.create = function create(properties) {
    return new ReqMotorServiceJoystickFixedAngle(properties);
  };

  /**
   * Encodes the specified ReqMotorServiceJoystickFixedAngle message. Does not implicitly {@link ReqMotorServiceJoystickFixedAngle.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {IReqMotorServiceJoystickFixedAngle} message ReqMotorServiceJoystickFixedAngle message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystickFixedAngle.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.vectorAngle);
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.vectorLength);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorServiceJoystickFixedAngle message, length delimited. Does not implicitly {@link ReqMotorServiceJoystickFixedAngle.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {IReqMotorServiceJoystickFixedAngle} message ReqMotorServiceJoystickFixedAngle message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystickFixedAngle.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorServiceJoystickFixedAngle message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorServiceJoystickFixedAngle} ReqMotorServiceJoystickFixedAngle
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystickFixedAngle.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorServiceJoystickFixedAngle();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.vectorAngle = reader.double();
          break;
        }
        case 2: {
          message.vectorLength = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorServiceJoystickFixedAngle message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorServiceJoystickFixedAngle} ReqMotorServiceJoystickFixedAngle
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystickFixedAngle.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorServiceJoystickFixedAngle message.
   * @function verify
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorServiceJoystickFixedAngle.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      if (typeof message.vectorAngle !== "number")
        return "vectorAngle: number expected";
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      if (typeof message.vectorLength !== "number")
        return "vectorLength: number expected";
    return null;
  };

  /**
   * Creates a ReqMotorServiceJoystickFixedAngle message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorServiceJoystickFixedAngle} ReqMotorServiceJoystickFixedAngle
   */
  ReqMotorServiceJoystickFixedAngle.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqMotorServiceJoystickFixedAngle)
      return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorServiceJoystickFixedAngle: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorServiceJoystickFixedAngle();
    if (object.vectorAngle != null)
      message.vectorAngle = Number(object.vectorAngle);
    if (object.vectorLength != null)
      message.vectorLength = Number(object.vectorLength);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorServiceJoystickFixedAngle message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {ReqMotorServiceJoystickFixedAngle} message ReqMotorServiceJoystickFixedAngle
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorServiceJoystickFixedAngle.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.vectorAngle = 0;
      object.vectorLength = 0;
    }
    if (
      message.vectorAngle != null &&
      Object.hasOwnProperty.call(message, "vectorAngle")
    )
      object.vectorAngle =
        options.json && !isFinite(message.vectorAngle)
          ? String(message.vectorAngle)
          : message.vectorAngle;
    if (
      message.vectorLength != null &&
      Object.hasOwnProperty.call(message, "vectorLength")
    )
      object.vectorLength =
        options.json && !isFinite(message.vectorLength)
          ? String(message.vectorLength)
          : message.vectorLength;
    return object;
  };

  /**
   * Converts this ReqMotorServiceJoystickFixedAngle to JSON.
   * @function toJSON
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorServiceJoystickFixedAngle.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorServiceJoystickFixedAngle
   * @function getTypeUrl
   * @memberof ReqMotorServiceJoystickFixedAngle
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorServiceJoystickFixedAngle.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorServiceJoystickFixedAngle";
  };

  return ReqMotorServiceJoystickFixedAngle;
})();

$root.ReqMotorServiceJoystickStop = (function () {
  /**
   * Properties of a ReqMotorServiceJoystickStop.
   * @exports IReqMotorServiceJoystickStop
   * @interface IReqMotorServiceJoystickStop
   */

  /**
   * Constructs a new ReqMotorServiceJoystickStop.
   * @exports ReqMotorServiceJoystickStop
   * @classdesc Represents a ReqMotorServiceJoystickStop.
   * @implements IReqMotorServiceJoystickStop
   * @constructor
   * @param {IReqMotorServiceJoystickStop=} [properties] Properties to set
   */
  function ReqMotorServiceJoystickStop(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqMotorServiceJoystickStop instance using the specified properties.
   * @function create
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {IReqMotorServiceJoystickStop=} [properties] Properties to set
   * @returns {ReqMotorServiceJoystickStop} ReqMotorServiceJoystickStop instance
   */
  ReqMotorServiceJoystickStop.create = function create(properties) {
    return new ReqMotorServiceJoystickStop(properties);
  };

  /**
   * Encodes the specified ReqMotorServiceJoystickStop message. Does not implicitly {@link ReqMotorServiceJoystickStop.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {IReqMotorServiceJoystickStop} message ReqMotorServiceJoystickStop message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystickStop.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqMotorServiceJoystickStop message, length delimited. Does not implicitly {@link ReqMotorServiceJoystickStop.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {IReqMotorServiceJoystickStop} message ReqMotorServiceJoystickStop message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorServiceJoystickStop.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorServiceJoystickStop message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorServiceJoystickStop} ReqMotorServiceJoystickStop
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystickStop.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorServiceJoystickStop();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorServiceJoystickStop message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorServiceJoystickStop} ReqMotorServiceJoystickStop
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorServiceJoystickStop.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorServiceJoystickStop message.
   * @function verify
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorServiceJoystickStop.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqMotorServiceJoystickStop message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorServiceJoystickStop} ReqMotorServiceJoystickStop
   */
  ReqMotorServiceJoystickStop.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorServiceJoystickStop) return object;
    return new $root.ReqMotorServiceJoystickStop();
  };

  /**
   * Creates a plain object from a ReqMotorServiceJoystickStop message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {ReqMotorServiceJoystickStop} message ReqMotorServiceJoystickStop
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorServiceJoystickStop.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqMotorServiceJoystickStop to JSON.
   * @function toJSON
   * @memberof ReqMotorServiceJoystickStop
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorServiceJoystickStop.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorServiceJoystickStop
   * @function getTypeUrl
   * @memberof ReqMotorServiceJoystickStop
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorServiceJoystickStop.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorServiceJoystickStop";
  };

  return ReqMotorServiceJoystickStop;
})();

$root.ReqMotorRun = (function () {
  /**
   * Properties of a ReqMotorRun.
   * @exports IReqMotorRun
   * @interface IReqMotorRun
   * @property {number|null} [id] ReqMotorRun id
   * @property {number|null} [speed] ReqMotorRun speed
   * @property {boolean|null} [direction] ReqMotorRun direction
   * @property {number|null} [speedRamping] ReqMotorRun speedRamping
   * @property {number|null} [resolutionLevel] ReqMotorRun resolutionLevel
   */

  /**
   * Constructs a new ReqMotorRun.
   * @exports ReqMotorRun
   * @classdesc Represents a ReqMotorRun.
   * @implements IReqMotorRun
   * @constructor
   * @param {IReqMotorRun=} [properties] Properties to set
   */
  function ReqMotorRun(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorRun id.
   * @member {number} id
   * @memberof ReqMotorRun
   * @instance
   */
  ReqMotorRun.prototype.id = 0;

  /**
   * ReqMotorRun speed.
   * @member {number} speed
   * @memberof ReqMotorRun
   * @instance
   */
  ReqMotorRun.prototype.speed = 0;

  /**
   * ReqMotorRun direction.
   * @member {boolean} direction
   * @memberof ReqMotorRun
   * @instance
   */
  ReqMotorRun.prototype.direction = false;

  /**
   * ReqMotorRun speedRamping.
   * @member {number} speedRamping
   * @memberof ReqMotorRun
   * @instance
   */
  ReqMotorRun.prototype.speedRamping = 0;

  /**
   * ReqMotorRun resolutionLevel.
   * @member {number} resolutionLevel
   * @memberof ReqMotorRun
   * @instance
   */
  ReqMotorRun.prototype.resolutionLevel = 0;

  /**
   * Creates a new ReqMotorRun instance using the specified properties.
   * @function create
   * @memberof ReqMotorRun
   * @static
   * @param {IReqMotorRun=} [properties] Properties to set
   * @returns {ReqMotorRun} ReqMotorRun instance
   */
  ReqMotorRun.create = function create(properties) {
    return new ReqMotorRun(properties);
  };

  /**
   * Encodes the specified ReqMotorRun message. Does not implicitly {@link ReqMotorRun.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorRun
   * @static
   * @param {IReqMotorRun} message ReqMotorRun message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRun.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.speed);
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.direction);
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.speedRamping);
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.resolutionLevel);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorRun message, length delimited. Does not implicitly {@link ReqMotorRun.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorRun
   * @static
   * @param {IReqMotorRun} message ReqMotorRun message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRun.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorRun message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorRun
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorRun} ReqMotorRun
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRun.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorRun();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.speed = reader.double();
          break;
        }
        case 3: {
          message.direction = reader.bool();
          break;
        }
        case 4: {
          message.speedRamping = reader.int32();
          break;
        }
        case 5: {
          message.resolutionLevel = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorRun message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorRun
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorRun} ReqMotorRun
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRun.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorRun message.
   * @function verify
   * @memberof ReqMotorRun
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorRun.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (typeof message.speed !== "number") return "speed: number expected";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (typeof message.direction !== "boolean")
        return "direction: boolean expected";
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      if (!$util.isInteger(message.speedRamping))
        return "speedRamping: integer expected";
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      if (!$util.isInteger(message.resolutionLevel))
        return "resolutionLevel: integer expected";
    return null;
  };

  /**
   * Creates a ReqMotorRun message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorRun
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorRun} ReqMotorRun
   */
  ReqMotorRun.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorRun) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorRun: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorRun();
    if (object.id != null) message.id = object.id | 0;
    if (object.speed != null) message.speed = Number(object.speed);
    if (object.direction != null) message.direction = Boolean(object.direction);
    if (object.speedRamping != null)
      message.speedRamping = object.speedRamping | 0;
    if (object.resolutionLevel != null)
      message.resolutionLevel = object.resolutionLevel | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorRun message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorRun
   * @static
   * @param {ReqMotorRun} message ReqMotorRun
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorRun.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.speed = 0;
      object.direction = false;
      object.speedRamping = 0;
      object.resolutionLevel = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed =
        options.json && !isFinite(message.speed)
          ? String(message.speed)
          : message.speed;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      object.speedRamping = message.speedRamping;
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      object.resolutionLevel = message.resolutionLevel;
    return object;
  };

  /**
   * Converts this ReqMotorRun to JSON.
   * @function toJSON
   * @memberof ReqMotorRun
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorRun.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorRun
   * @function getTypeUrl
   * @memberof ReqMotorRun
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorRun.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorRun";
  };

  return ReqMotorRun;
})();

$root.ReqMotorRunInPulse = (function () {
  /**
   * Properties of a ReqMotorRunInPulse.
   * @exports IReqMotorRunInPulse
   * @interface IReqMotorRunInPulse
   * @property {number|null} [id] ReqMotorRunInPulse id
   * @property {number|null} [frequency] ReqMotorRunInPulse frequency
   * @property {boolean|null} [direction] ReqMotorRunInPulse direction
   * @property {number|null} [speedRamping] ReqMotorRunInPulse speedRamping
   * @property {number|null} [resolution] ReqMotorRunInPulse resolution
   * @property {number|null} [pulse] ReqMotorRunInPulse pulse
   * @property {boolean|null} [mode] ReqMotorRunInPulse mode
   */

  /**
   * Constructs a new ReqMotorRunInPulse.
   * @exports ReqMotorRunInPulse
   * @classdesc Represents a ReqMotorRunInPulse.
   * @implements IReqMotorRunInPulse
   * @constructor
   * @param {IReqMotorRunInPulse=} [properties] Properties to set
   */
  function ReqMotorRunInPulse(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorRunInPulse id.
   * @member {number} id
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.id = 0;

  /**
   * ReqMotorRunInPulse frequency.
   * @member {number} frequency
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.frequency = 0;

  /**
   * ReqMotorRunInPulse direction.
   * @member {boolean} direction
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.direction = false;

  /**
   * ReqMotorRunInPulse speedRamping.
   * @member {number} speedRamping
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.speedRamping = 0;

  /**
   * ReqMotorRunInPulse resolution.
   * @member {number} resolution
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.resolution = 0;

  /**
   * ReqMotorRunInPulse pulse.
   * @member {number} pulse
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.pulse = 0;

  /**
   * ReqMotorRunInPulse mode.
   * @member {boolean} mode
   * @memberof ReqMotorRunInPulse
   * @instance
   */
  ReqMotorRunInPulse.prototype.mode = false;

  /**
   * Creates a new ReqMotorRunInPulse instance using the specified properties.
   * @function create
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {IReqMotorRunInPulse=} [properties] Properties to set
   * @returns {ReqMotorRunInPulse} ReqMotorRunInPulse instance
   */
  ReqMotorRunInPulse.create = function create(properties) {
    return new ReqMotorRunInPulse(properties);
  };

  /**
   * Encodes the specified ReqMotorRunInPulse message. Does not implicitly {@link ReqMotorRunInPulse.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {IReqMotorRunInPulse} message ReqMotorRunInPulse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRunInPulse.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (
      message.frequency != null &&
      Object.hasOwnProperty.call(message, "frequency")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.frequency);
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.direction);
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.speedRamping);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.resolution);
    if (message.pulse != null && Object.hasOwnProperty.call(message, "pulse"))
      writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.pulse);
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 7, wireType 0 =*/ 56).bool(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorRunInPulse message, length delimited. Does not implicitly {@link ReqMotorRunInPulse.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {IReqMotorRunInPulse} message ReqMotorRunInPulse message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRunInPulse.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorRunInPulse message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorRunInPulse} ReqMotorRunInPulse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRunInPulse.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorRunInPulse();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.frequency = reader.int32();
          break;
        }
        case 3: {
          message.direction = reader.bool();
          break;
        }
        case 4: {
          message.speedRamping = reader.int32();
          break;
        }
        case 5: {
          message.resolution = reader.int32();
          break;
        }
        case 6: {
          message.pulse = reader.int32();
          break;
        }
        case 7: {
          message.mode = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorRunInPulse message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorRunInPulse} ReqMotorRunInPulse
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRunInPulse.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorRunInPulse message.
   * @function verify
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorRunInPulse.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (
      message.frequency != null &&
      Object.hasOwnProperty.call(message, "frequency")
    )
      if (!$util.isInteger(message.frequency))
        return "frequency: integer expected";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (typeof message.direction !== "boolean")
        return "direction: boolean expected";
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      if (!$util.isInteger(message.speedRamping))
        return "speedRamping: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isInteger(message.resolution))
        return "resolution: integer expected";
    if (message.pulse != null && Object.hasOwnProperty.call(message, "pulse"))
      if (!$util.isInteger(message.pulse)) return "pulse: integer expected";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (typeof message.mode !== "boolean") return "mode: boolean expected";
    return null;
  };

  /**
   * Creates a ReqMotorRunInPulse message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorRunInPulse} ReqMotorRunInPulse
   */
  ReqMotorRunInPulse.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorRunInPulse) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorRunInPulse: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorRunInPulse();
    if (object.id != null) message.id = object.id | 0;
    if (object.frequency != null) message.frequency = object.frequency | 0;
    if (object.direction != null) message.direction = Boolean(object.direction);
    if (object.speedRamping != null)
      message.speedRamping = object.speedRamping | 0;
    if (object.resolution != null) message.resolution = object.resolution | 0;
    if (object.pulse != null) message.pulse = object.pulse | 0;
    if (object.mode != null) message.mode = Boolean(object.mode);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorRunInPulse message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {ReqMotorRunInPulse} message ReqMotorRunInPulse
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorRunInPulse.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.frequency = 0;
      object.direction = false;
      object.speedRamping = 0;
      object.resolution = 0;
      object.pulse = 0;
      object.mode = false;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.frequency != null &&
      Object.hasOwnProperty.call(message, "frequency")
    )
      object.frequency = message.frequency;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      object.speedRamping = message.speedRamping;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    if (message.pulse != null && Object.hasOwnProperty.call(message, "pulse"))
      object.pulse = message.pulse;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqMotorRunInPulse to JSON.
   * @function toJSON
   * @memberof ReqMotorRunInPulse
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorRunInPulse.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorRunInPulse
   * @function getTypeUrl
   * @memberof ReqMotorRunInPulse
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorRunInPulse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorRunInPulse";
  };

  return ReqMotorRunInPulse;
})();

$root.ReqMotorRunTo = (function () {
  /**
   * Properties of a ReqMotorRunTo.
   * @exports IReqMotorRunTo
   * @interface IReqMotorRunTo
   * @property {number|null} [id] ReqMotorRunTo id
   * @property {number|null} [endPosition] ReqMotorRunTo endPosition
   * @property {number|null} [speed] ReqMotorRunTo speed
   * @property {number|null} [speedRamping] ReqMotorRunTo speedRamping
   * @property {number|null} [resolutionLevel] ReqMotorRunTo resolutionLevel
   */

  /**
   * Constructs a new ReqMotorRunTo.
   * @exports ReqMotorRunTo
   * @classdesc Represents a ReqMotorRunTo.
   * @implements IReqMotorRunTo
   * @constructor
   * @param {IReqMotorRunTo=} [properties] Properties to set
   */
  function ReqMotorRunTo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorRunTo id.
   * @member {number} id
   * @memberof ReqMotorRunTo
   * @instance
   */
  ReqMotorRunTo.prototype.id = 0;

  /**
   * ReqMotorRunTo endPosition.
   * @member {number} endPosition
   * @memberof ReqMotorRunTo
   * @instance
   */
  ReqMotorRunTo.prototype.endPosition = 0;

  /**
   * ReqMotorRunTo speed.
   * @member {number} speed
   * @memberof ReqMotorRunTo
   * @instance
   */
  ReqMotorRunTo.prototype.speed = 0;

  /**
   * ReqMotorRunTo speedRamping.
   * @member {number} speedRamping
   * @memberof ReqMotorRunTo
   * @instance
   */
  ReqMotorRunTo.prototype.speedRamping = 0;

  /**
   * ReqMotorRunTo resolutionLevel.
   * @member {number} resolutionLevel
   * @memberof ReqMotorRunTo
   * @instance
   */
  ReqMotorRunTo.prototype.resolutionLevel = 0;

  /**
   * Creates a new ReqMotorRunTo instance using the specified properties.
   * @function create
   * @memberof ReqMotorRunTo
   * @static
   * @param {IReqMotorRunTo=} [properties] Properties to set
   * @returns {ReqMotorRunTo} ReqMotorRunTo instance
   */
  ReqMotorRunTo.create = function create(properties) {
    return new ReqMotorRunTo(properties);
  };

  /**
   * Encodes the specified ReqMotorRunTo message. Does not implicitly {@link ReqMotorRunTo.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorRunTo
   * @static
   * @param {IReqMotorRunTo} message ReqMotorRunTo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRunTo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (
      message.endPosition != null &&
      Object.hasOwnProperty.call(message, "endPosition")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.endPosition);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.speed);
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.speedRamping);
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.resolutionLevel);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorRunTo message, length delimited. Does not implicitly {@link ReqMotorRunTo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorRunTo
   * @static
   * @param {IReqMotorRunTo} message ReqMotorRunTo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorRunTo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorRunTo message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorRunTo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorRunTo} ReqMotorRunTo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRunTo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorRunTo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.endPosition = reader.double();
          break;
        }
        case 3: {
          message.speed = reader.double();
          break;
        }
        case 4: {
          message.speedRamping = reader.int32();
          break;
        }
        case 5: {
          message.resolutionLevel = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorRunTo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorRunTo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorRunTo} ReqMotorRunTo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorRunTo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorRunTo message.
   * @function verify
   * @memberof ReqMotorRunTo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorRunTo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (
      message.endPosition != null &&
      Object.hasOwnProperty.call(message, "endPosition")
    )
      if (typeof message.endPosition !== "number")
        return "endPosition: number expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (typeof message.speed !== "number") return "speed: number expected";
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      if (!$util.isInteger(message.speedRamping))
        return "speedRamping: integer expected";
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      if (!$util.isInteger(message.resolutionLevel))
        return "resolutionLevel: integer expected";
    return null;
  };

  /**
   * Creates a ReqMotorRunTo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorRunTo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorRunTo} ReqMotorRunTo
   */
  ReqMotorRunTo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorRunTo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorRunTo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorRunTo();
    if (object.id != null) message.id = object.id | 0;
    if (object.endPosition != null)
      message.endPosition = Number(object.endPosition);
    if (object.speed != null) message.speed = Number(object.speed);
    if (object.speedRamping != null)
      message.speedRamping = object.speedRamping | 0;
    if (object.resolutionLevel != null)
      message.resolutionLevel = object.resolutionLevel | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorRunTo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorRunTo
   * @static
   * @param {ReqMotorRunTo} message ReqMotorRunTo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorRunTo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.endPosition = 0;
      object.speed = 0;
      object.speedRamping = 0;
      object.resolutionLevel = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.endPosition != null &&
      Object.hasOwnProperty.call(message, "endPosition")
    )
      object.endPosition =
        options.json && !isFinite(message.endPosition)
          ? String(message.endPosition)
          : message.endPosition;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed =
        options.json && !isFinite(message.speed)
          ? String(message.speed)
          : message.speed;
    if (
      message.speedRamping != null &&
      Object.hasOwnProperty.call(message, "speedRamping")
    )
      object.speedRamping = message.speedRamping;
    if (
      message.resolutionLevel != null &&
      Object.hasOwnProperty.call(message, "resolutionLevel")
    )
      object.resolutionLevel = message.resolutionLevel;
    return object;
  };

  /**
   * Converts this ReqMotorRunTo to JSON.
   * @function toJSON
   * @memberof ReqMotorRunTo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorRunTo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorRunTo
   * @function getTypeUrl
   * @memberof ReqMotorRunTo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorRunTo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorRunTo";
  };

  return ReqMotorRunTo;
})();

$root.ReqMotorGetPosition = (function () {
  /**
   * Properties of a ReqMotorGetPosition.
   * @exports IReqMotorGetPosition
   * @interface IReqMotorGetPosition
   * @property {number|null} [id] ReqMotorGetPosition id
   */

  /**
   * Constructs a new ReqMotorGetPosition.
   * @exports ReqMotorGetPosition
   * @classdesc Represents a ReqMotorGetPosition.
   * @implements IReqMotorGetPosition
   * @constructor
   * @param {IReqMotorGetPosition=} [properties] Properties to set
   */
  function ReqMotorGetPosition(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorGetPosition id.
   * @member {number} id
   * @memberof ReqMotorGetPosition
   * @instance
   */
  ReqMotorGetPosition.prototype.id = 0;

  /**
   * Creates a new ReqMotorGetPosition instance using the specified properties.
   * @function create
   * @memberof ReqMotorGetPosition
   * @static
   * @param {IReqMotorGetPosition=} [properties] Properties to set
   * @returns {ReqMotorGetPosition} ReqMotorGetPosition instance
   */
  ReqMotorGetPosition.create = function create(properties) {
    return new ReqMotorGetPosition(properties);
  };

  /**
   * Encodes the specified ReqMotorGetPosition message. Does not implicitly {@link ReqMotorGetPosition.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorGetPosition
   * @static
   * @param {IReqMotorGetPosition} message ReqMotorGetPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorGetPosition.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorGetPosition message, length delimited. Does not implicitly {@link ReqMotorGetPosition.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorGetPosition
   * @static
   * @param {IReqMotorGetPosition} message ReqMotorGetPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorGetPosition.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorGetPosition message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorGetPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorGetPosition} ReqMotorGetPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorGetPosition.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorGetPosition();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorGetPosition message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorGetPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorGetPosition} ReqMotorGetPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorGetPosition.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorGetPosition message.
   * @function verify
   * @memberof ReqMotorGetPosition
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorGetPosition.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    return null;
  };

  /**
   * Creates a ReqMotorGetPosition message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorGetPosition
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorGetPosition} ReqMotorGetPosition
   */
  ReqMotorGetPosition.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorGetPosition) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorGetPosition: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorGetPosition();
    if (object.id != null) message.id = object.id | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorGetPosition message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorGetPosition
   * @static
   * @param {ReqMotorGetPosition} message ReqMotorGetPosition
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorGetPosition.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.id = 0;
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    return object;
  };

  /**
   * Converts this ReqMotorGetPosition to JSON.
   * @function toJSON
   * @memberof ReqMotorGetPosition
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorGetPosition.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorGetPosition
   * @function getTypeUrl
   * @memberof ReqMotorGetPosition
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorGetPosition.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorGetPosition";
  };

  return ReqMotorGetPosition;
})();

$root.ReqMotorStop = (function () {
  /**
   * Properties of a ReqMotorStop.
   * @exports IReqMotorStop
   * @interface IReqMotorStop
   * @property {number|null} [id] ReqMotorStop id
   */

  /**
   * Constructs a new ReqMotorStop.
   * @exports ReqMotorStop
   * @classdesc Represents a ReqMotorStop.
   * @implements IReqMotorStop
   * @constructor
   * @param {IReqMotorStop=} [properties] Properties to set
   */
  function ReqMotorStop(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorStop id.
   * @member {number} id
   * @memberof ReqMotorStop
   * @instance
   */
  ReqMotorStop.prototype.id = 0;

  /**
   * Creates a new ReqMotorStop instance using the specified properties.
   * @function create
   * @memberof ReqMotorStop
   * @static
   * @param {IReqMotorStop=} [properties] Properties to set
   * @returns {ReqMotorStop} ReqMotorStop instance
   */
  ReqMotorStop.create = function create(properties) {
    return new ReqMotorStop(properties);
  };

  /**
   * Encodes the specified ReqMotorStop message. Does not implicitly {@link ReqMotorStop.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorStop
   * @static
   * @param {IReqMotorStop} message ReqMotorStop message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStop.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorStop message, length delimited. Does not implicitly {@link ReqMotorStop.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorStop
   * @static
   * @param {IReqMotorStop} message ReqMotorStop message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStop.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorStop message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorStop
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorStop} ReqMotorStop
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStop.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorStop();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorStop message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorStop
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorStop} ReqMotorStop
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStop.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorStop message.
   * @function verify
   * @memberof ReqMotorStop
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorStop.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    return null;
  };

  /**
   * Creates a ReqMotorStop message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorStop
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorStop} ReqMotorStop
   */
  ReqMotorStop.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorStop) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorStop: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorStop();
    if (object.id != null) message.id = object.id | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorStop message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorStop
   * @static
   * @param {ReqMotorStop} message ReqMotorStop
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorStop.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.id = 0;
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    return object;
  };

  /**
   * Converts this ReqMotorStop to JSON.
   * @function toJSON
   * @memberof ReqMotorStop
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorStop.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorStop
   * @function getTypeUrl
   * @memberof ReqMotorStop
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorStop.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorStop";
  };

  return ReqMotorStop;
})();

$root.ReqMotorReset = (function () {
  /**
   * Properties of a ReqMotorReset.
   * @exports IReqMotorReset
   * @interface IReqMotorReset
   * @property {number|null} [id] ReqMotorReset id
   * @property {boolean|null} [direction] ReqMotorReset direction
   */

  /**
   * Constructs a new ReqMotorReset.
   * @exports ReqMotorReset
   * @classdesc Represents a ReqMotorReset.
   * @implements IReqMotorReset
   * @constructor
   * @param {IReqMotorReset=} [properties] Properties to set
   */
  function ReqMotorReset(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorReset id.
   * @member {number} id
   * @memberof ReqMotorReset
   * @instance
   */
  ReqMotorReset.prototype.id = 0;

  /**
   * ReqMotorReset direction.
   * @member {boolean} direction
   * @memberof ReqMotorReset
   * @instance
   */
  ReqMotorReset.prototype.direction = false;

  /**
   * Creates a new ReqMotorReset instance using the specified properties.
   * @function create
   * @memberof ReqMotorReset
   * @static
   * @param {IReqMotorReset=} [properties] Properties to set
   * @returns {ReqMotorReset} ReqMotorReset instance
   */
  ReqMotorReset.create = function create(properties) {
    return new ReqMotorReset(properties);
  };

  /**
   * Encodes the specified ReqMotorReset message. Does not implicitly {@link ReqMotorReset.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorReset
   * @static
   * @param {IReqMotorReset} message ReqMotorReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorReset.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.direction);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorReset message, length delimited. Does not implicitly {@link ReqMotorReset.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorReset
   * @static
   * @param {IReqMotorReset} message ReqMotorReset message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorReset.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorReset message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorReset} ReqMotorReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorReset.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorReset();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.direction = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorReset message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorReset
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorReset} ReqMotorReset
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorReset.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorReset message.
   * @function verify
   * @memberof ReqMotorReset
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorReset.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (typeof message.direction !== "boolean")
        return "direction: boolean expected";
    return null;
  };

  /**
   * Creates a ReqMotorReset message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorReset
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorReset} ReqMotorReset
   */
  ReqMotorReset.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorReset) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorReset: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorReset();
    if (object.id != null) message.id = object.id | 0;
    if (object.direction != null) message.direction = Boolean(object.direction);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorReset message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorReset
   * @static
   * @param {ReqMotorReset} message ReqMotorReset
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorReset.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.direction = false;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    return object;
  };

  /**
   * Converts this ReqMotorReset to JSON.
   * @function toJSON
   * @memberof ReqMotorReset
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorReset.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorReset
   * @function getTypeUrl
   * @memberof ReqMotorReset
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorReset.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorReset";
  };

  return ReqMotorReset;
})();

$root.ReqMotorChangeSpeed = (function () {
  /**
   * Properties of a ReqMotorChangeSpeed.
   * @exports IReqMotorChangeSpeed
   * @interface IReqMotorChangeSpeed
   * @property {number|null} [id] ReqMotorChangeSpeed id
   * @property {number|null} [speed] ReqMotorChangeSpeed speed
   */

  /**
   * Constructs a new ReqMotorChangeSpeed.
   * @exports ReqMotorChangeSpeed
   * @classdesc Represents a ReqMotorChangeSpeed.
   * @implements IReqMotorChangeSpeed
   * @constructor
   * @param {IReqMotorChangeSpeed=} [properties] Properties to set
   */
  function ReqMotorChangeSpeed(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorChangeSpeed id.
   * @member {number} id
   * @memberof ReqMotorChangeSpeed
   * @instance
   */
  ReqMotorChangeSpeed.prototype.id = 0;

  /**
   * ReqMotorChangeSpeed speed.
   * @member {number} speed
   * @memberof ReqMotorChangeSpeed
   * @instance
   */
  ReqMotorChangeSpeed.prototype.speed = 0;

  /**
   * Creates a new ReqMotorChangeSpeed instance using the specified properties.
   * @function create
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {IReqMotorChangeSpeed=} [properties] Properties to set
   * @returns {ReqMotorChangeSpeed} ReqMotorChangeSpeed instance
   */
  ReqMotorChangeSpeed.create = function create(properties) {
    return new ReqMotorChangeSpeed(properties);
  };

  /**
   * Encodes the specified ReqMotorChangeSpeed message. Does not implicitly {@link ReqMotorChangeSpeed.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {IReqMotorChangeSpeed} message ReqMotorChangeSpeed message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorChangeSpeed.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.speed);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorChangeSpeed message, length delimited. Does not implicitly {@link ReqMotorChangeSpeed.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {IReqMotorChangeSpeed} message ReqMotorChangeSpeed message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorChangeSpeed.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorChangeSpeed message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorChangeSpeed} ReqMotorChangeSpeed
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorChangeSpeed.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorChangeSpeed();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.speed = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorChangeSpeed message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorChangeSpeed} ReqMotorChangeSpeed
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorChangeSpeed.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorChangeSpeed message.
   * @function verify
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorChangeSpeed.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      if (typeof message.speed !== "number") return "speed: number expected";
    return null;
  };

  /**
   * Creates a ReqMotorChangeSpeed message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorChangeSpeed} ReqMotorChangeSpeed
   */
  ReqMotorChangeSpeed.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorChangeSpeed) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorChangeSpeed: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorChangeSpeed();
    if (object.id != null) message.id = object.id | 0;
    if (object.speed != null) message.speed = Number(object.speed);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorChangeSpeed message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {ReqMotorChangeSpeed} message ReqMotorChangeSpeed
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorChangeSpeed.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.speed = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
      object.speed =
        options.json && !isFinite(message.speed)
          ? String(message.speed)
          : message.speed;
    return object;
  };

  /**
   * Converts this ReqMotorChangeSpeed to JSON.
   * @function toJSON
   * @memberof ReqMotorChangeSpeed
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorChangeSpeed.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorChangeSpeed
   * @function getTypeUrl
   * @memberof ReqMotorChangeSpeed
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorChangeSpeed.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorChangeSpeed";
  };

  return ReqMotorChangeSpeed;
})();

$root.ReqMotorChangeDirection = (function () {
  /**
   * Properties of a ReqMotorChangeDirection.
   * @exports IReqMotorChangeDirection
   * @interface IReqMotorChangeDirection
   * @property {number|null} [id] ReqMotorChangeDirection id
   * @property {boolean|null} [direction] ReqMotorChangeDirection direction
   */

  /**
   * Constructs a new ReqMotorChangeDirection.
   * @exports ReqMotorChangeDirection
   * @classdesc Represents a ReqMotorChangeDirection.
   * @implements IReqMotorChangeDirection
   * @constructor
   * @param {IReqMotorChangeDirection=} [properties] Properties to set
   */
  function ReqMotorChangeDirection(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorChangeDirection id.
   * @member {number} id
   * @memberof ReqMotorChangeDirection
   * @instance
   */
  ReqMotorChangeDirection.prototype.id = 0;

  /**
   * ReqMotorChangeDirection direction.
   * @member {boolean} direction
   * @memberof ReqMotorChangeDirection
   * @instance
   */
  ReqMotorChangeDirection.prototype.direction = false;

  /**
   * Creates a new ReqMotorChangeDirection instance using the specified properties.
   * @function create
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {IReqMotorChangeDirection=} [properties] Properties to set
   * @returns {ReqMotorChangeDirection} ReqMotorChangeDirection instance
   */
  ReqMotorChangeDirection.create = function create(properties) {
    return new ReqMotorChangeDirection(properties);
  };

  /**
   * Encodes the specified ReqMotorChangeDirection message. Does not implicitly {@link ReqMotorChangeDirection.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {IReqMotorChangeDirection} message ReqMotorChangeDirection message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorChangeDirection.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.direction);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorChangeDirection message, length delimited. Does not implicitly {@link ReqMotorChangeDirection.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {IReqMotorChangeDirection} message ReqMotorChangeDirection message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorChangeDirection.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorChangeDirection message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorChangeDirection} ReqMotorChangeDirection
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorChangeDirection.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorChangeDirection();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.direction = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorChangeDirection message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorChangeDirection} ReqMotorChangeDirection
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorChangeDirection.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorChangeDirection message.
   * @function verify
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorChangeDirection.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      if (typeof message.direction !== "boolean")
        return "direction: boolean expected";
    return null;
  };

  /**
   * Creates a ReqMotorChangeDirection message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorChangeDirection} ReqMotorChangeDirection
   */
  ReqMotorChangeDirection.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorChangeDirection) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorChangeDirection: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorChangeDirection();
    if (object.id != null) message.id = object.id | 0;
    if (object.direction != null) message.direction = Boolean(object.direction);
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorChangeDirection message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {ReqMotorChangeDirection} message ReqMotorChangeDirection
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorChangeDirection.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.direction = false;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction = message.direction;
    return object;
  };

  /**
   * Converts this ReqMotorChangeDirection to JSON.
   * @function toJSON
   * @memberof ReqMotorChangeDirection
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorChangeDirection.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorChangeDirection
   * @function getTypeUrl
   * @memberof ReqMotorChangeDirection
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorChangeDirection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorChangeDirection";
  };

  return ReqMotorChangeDirection;
})();

$root.ResMotor = (function () {
  /**
   * Properties of a ResMotor.
   * @exports IResMotor
   * @interface IResMotor
   * @property {number|null} [id] ResMotor id
   * @property {number|null} [code] ResMotor code
   */

  /**
   * Constructs a new ResMotor.
   * @exports ResMotor
   * @classdesc Represents a ResMotor.
   * @implements IResMotor
   * @constructor
   * @param {IResMotor=} [properties] Properties to set
   */
  function ResMotor(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResMotor id.
   * @member {number} id
   * @memberof ResMotor
   * @instance
   */
  ResMotor.prototype.id = 0;

  /**
   * ResMotor code.
   * @member {number} code
   * @memberof ResMotor
   * @instance
   */
  ResMotor.prototype.code = 0;

  /**
   * Creates a new ResMotor instance using the specified properties.
   * @function create
   * @memberof ResMotor
   * @static
   * @param {IResMotor=} [properties] Properties to set
   * @returns {ResMotor} ResMotor instance
   */
  ResMotor.create = function create(properties) {
    return new ResMotor(properties);
  };

  /**
   * Encodes the specified ResMotor message. Does not implicitly {@link ResMotor.verify|verify} messages.
   * @function encode
   * @memberof ResMotor
   * @static
   * @param {IResMotor} message ResMotor message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMotor.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResMotor message, length delimited. Does not implicitly {@link ResMotor.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResMotor
   * @static
   * @param {IResMotor} message ResMotor message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMotor.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResMotor message from the specified reader or buffer.
   * @function decode
   * @memberof ResMotor
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResMotor} ResMotor
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMotor.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResMotor();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResMotor message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResMotor
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResMotor} ResMotor
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMotor.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResMotor message.
   * @function verify
   * @memberof ResMotor
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResMotor.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResMotor message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResMotor
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResMotor} ResMotor
   */
  ResMotor.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResMotor) return object;
    if (!$util.isObject(object)) throw TypeError(".ResMotor: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResMotor();
    if (object.id != null) message.id = object.id | 0;
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResMotor message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResMotor
   * @static
   * @param {ResMotor} message ResMotor
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResMotor.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.code = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResMotor to JSON.
   * @function toJSON
   * @memberof ResMotor
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResMotor.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResMotor
   * @function getTypeUrl
   * @memberof ResMotor
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResMotor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResMotor";
  };

  return ResMotor;
})();

$root.ResMotorPosition = (function () {
  /**
   * Properties of a ResMotorPosition.
   * @exports IResMotorPosition
   * @interface IResMotorPosition
   * @property {number|null} [id] ResMotorPosition id
   * @property {number|null} [code] ResMotorPosition code
   * @property {number|null} [position] ResMotorPosition position
   */

  /**
   * Constructs a new ResMotorPosition.
   * @exports ResMotorPosition
   * @classdesc Represents a ResMotorPosition.
   * @implements IResMotorPosition
   * @constructor
   * @param {IResMotorPosition=} [properties] Properties to set
   */
  function ResMotorPosition(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResMotorPosition id.
   * @member {number} id
   * @memberof ResMotorPosition
   * @instance
   */
  ResMotorPosition.prototype.id = 0;

  /**
   * ResMotorPosition code.
   * @member {number} code
   * @memberof ResMotorPosition
   * @instance
   */
  ResMotorPosition.prototype.code = 0;

  /**
   * ResMotorPosition position.
   * @member {number} position
   * @memberof ResMotorPosition
   * @instance
   */
  ResMotorPosition.prototype.position = 0;

  /**
   * Creates a new ResMotorPosition instance using the specified properties.
   * @function create
   * @memberof ResMotorPosition
   * @static
   * @param {IResMotorPosition=} [properties] Properties to set
   * @returns {ResMotorPosition} ResMotorPosition instance
   */
  ResMotorPosition.create = function create(properties) {
    return new ResMotorPosition(properties);
  };

  /**
   * Encodes the specified ResMotorPosition message. Does not implicitly {@link ResMotorPosition.verify|verify} messages.
   * @function encode
   * @memberof ResMotorPosition
   * @static
   * @param {IResMotorPosition} message ResMotorPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMotorPosition.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.id);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    if (
      message.position != null &&
      Object.hasOwnProperty.call(message, "position")
    )
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.position);
    return writer;
  };

  /**
   * Encodes the specified ResMotorPosition message, length delimited. Does not implicitly {@link ResMotorPosition.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResMotorPosition
   * @static
   * @param {IResMotorPosition} message ResMotorPosition message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResMotorPosition.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResMotorPosition message from the specified reader or buffer.
   * @function decode
   * @memberof ResMotorPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResMotorPosition} ResMotorPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMotorPosition.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResMotorPosition();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.int32();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        case 3: {
          message.position = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResMotorPosition message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResMotorPosition
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResMotorPosition} ResMotorPosition
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResMotorPosition.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResMotorPosition message.
   * @function verify
   * @memberof ResMotorPosition
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResMotorPosition.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isInteger(message.id)) return "id: integer expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.position != null &&
      Object.hasOwnProperty.call(message, "position")
    )
      if (typeof message.position !== "number")
        return "position: number expected";
    return null;
  };

  /**
   * Creates a ResMotorPosition message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResMotorPosition
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResMotorPosition} ResMotorPosition
   */
  ResMotorPosition.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResMotorPosition) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResMotorPosition: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResMotorPosition();
    if (object.id != null) message.id = object.id | 0;
    if (object.code != null) message.code = object.code | 0;
    if (object.position != null) message.position = Number(object.position);
    return message;
  };

  /**
   * Creates a plain object from a ResMotorPosition message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResMotorPosition
   * @static
   * @param {ResMotorPosition} message ResMotorPosition
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResMotorPosition.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = 0;
      object.code = 0;
      object.position = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.position != null &&
      Object.hasOwnProperty.call(message, "position")
    )
      object.position =
        options.json && !isFinite(message.position)
          ? String(message.position)
          : message.position;
    return object;
  };

  /**
   * Converts this ResMotorPosition to JSON.
   * @function toJSON
   * @memberof ResMotorPosition
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResMotorPosition.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResMotorPosition
   * @function getTypeUrl
   * @memberof ResMotorPosition
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResMotorPosition.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResMotorPosition";
  };

  return ResMotorPosition;
})();

$root.ReqMotorLevelCalibrationMove = (function () {
  /**
   * Properties of a ReqMotorLevelCalibrationMove.
   * @exports IReqMotorLevelCalibrationMove
   * @interface IReqMotorLevelCalibrationMove
   * @property {MotorLevelCalibrationDirection|null} [direction] ReqMotorLevelCalibrationMove direction
   */

  /**
   * Constructs a new ReqMotorLevelCalibrationMove.
   * @exports ReqMotorLevelCalibrationMove
   * @classdesc Represents a ReqMotorLevelCalibrationMove.
   * @implements IReqMotorLevelCalibrationMove
   * @constructor
   * @param {IReqMotorLevelCalibrationMove=} [properties] Properties to set
   */
  function ReqMotorLevelCalibrationMove(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqMotorLevelCalibrationMove direction.
   * @member {MotorLevelCalibrationDirection} direction
   * @memberof ReqMotorLevelCalibrationMove
   * @instance
   */
  ReqMotorLevelCalibrationMove.prototype.direction = 0;

  /**
   * Creates a new ReqMotorLevelCalibrationMove instance using the specified properties.
   * @function create
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {IReqMotorLevelCalibrationMove=} [properties] Properties to set
   * @returns {ReqMotorLevelCalibrationMove} ReqMotorLevelCalibrationMove instance
   */
  ReqMotorLevelCalibrationMove.create = function create(properties) {
    return new ReqMotorLevelCalibrationMove(properties);
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationMove message. Does not implicitly {@link ReqMotorLevelCalibrationMove.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {IReqMotorLevelCalibrationMove} message ReqMotorLevelCalibrationMove message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationMove.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.direction);
    return writer;
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationMove message, length delimited. Does not implicitly {@link ReqMotorLevelCalibrationMove.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {IReqMotorLevelCalibrationMove} message ReqMotorLevelCalibrationMove message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationMove.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorLevelCalibrationMove message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorLevelCalibrationMove} ReqMotorLevelCalibrationMove
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationMove.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorLevelCalibrationMove();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.direction = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorLevelCalibrationMove message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorLevelCalibrationMove} ReqMotorLevelCalibrationMove
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationMove.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorLevelCalibrationMove message.
   * @function verify
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorLevelCalibrationMove.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      switch (message.direction) {
        default:
          return "direction: enum value expected";
        case 0:
        case 1:
          break;
      }
    return null;
  };

  /**
   * Creates a ReqMotorLevelCalibrationMove message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorLevelCalibrationMove} ReqMotorLevelCalibrationMove
   */
  ReqMotorLevelCalibrationMove.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorLevelCalibrationMove) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqMotorLevelCalibrationMove: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqMotorLevelCalibrationMove();
    switch (object.direction) {
      default:
        if (typeof object.direction === "number") {
          message.direction = object.direction;
          break;
        }
        break;
      case "MOTOR_LEVEL_CALIBRATION_DIRECTION_CW":
      case 0:
        message.direction = 0;
        break;
      case "MOTOR_LEVEL_CALIBRATION_DIRECTION_CCW":
      case 1:
        message.direction = 1;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqMotorLevelCalibrationMove message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {ReqMotorLevelCalibrationMove} message ReqMotorLevelCalibrationMove
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorLevelCalibrationMove.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults)
      object.direction =
        options.enums === String ? "MOTOR_LEVEL_CALIBRATION_DIRECTION_CW" : 0;
    if (
      message.direction != null &&
      Object.hasOwnProperty.call(message, "direction")
    )
      object.direction =
        options.enums === String
          ? $root.MotorLevelCalibrationDirection[message.direction] ===
            undefined
            ? message.direction
            : $root.MotorLevelCalibrationDirection[message.direction]
          : message.direction;
    return object;
  };

  /**
   * Converts this ReqMotorLevelCalibrationMove to JSON.
   * @function toJSON
   * @memberof ReqMotorLevelCalibrationMove
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorLevelCalibrationMove.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorLevelCalibrationMove
   * @function getTypeUrl
   * @memberof ReqMotorLevelCalibrationMove
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorLevelCalibrationMove.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorLevelCalibrationMove";
  };

  return ReqMotorLevelCalibrationMove;
})();

$root.ReqMotorLevelCalibrationSave = (function () {
  /**
   * Properties of a ReqMotorLevelCalibrationSave.
   * @exports IReqMotorLevelCalibrationSave
   * @interface IReqMotorLevelCalibrationSave
   */

  /**
   * Constructs a new ReqMotorLevelCalibrationSave.
   * @exports ReqMotorLevelCalibrationSave
   * @classdesc Represents a ReqMotorLevelCalibrationSave.
   * @implements IReqMotorLevelCalibrationSave
   * @constructor
   * @param {IReqMotorLevelCalibrationSave=} [properties] Properties to set
   */
  function ReqMotorLevelCalibrationSave(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqMotorLevelCalibrationSave instance using the specified properties.
   * @function create
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {IReqMotorLevelCalibrationSave=} [properties] Properties to set
   * @returns {ReqMotorLevelCalibrationSave} ReqMotorLevelCalibrationSave instance
   */
  ReqMotorLevelCalibrationSave.create = function create(properties) {
    return new ReqMotorLevelCalibrationSave(properties);
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationSave message. Does not implicitly {@link ReqMotorLevelCalibrationSave.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {IReqMotorLevelCalibrationSave} message ReqMotorLevelCalibrationSave message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationSave.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationSave message, length delimited. Does not implicitly {@link ReqMotorLevelCalibrationSave.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {IReqMotorLevelCalibrationSave} message ReqMotorLevelCalibrationSave message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationSave.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorLevelCalibrationSave message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorLevelCalibrationSave} ReqMotorLevelCalibrationSave
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationSave.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorLevelCalibrationSave();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorLevelCalibrationSave message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorLevelCalibrationSave} ReqMotorLevelCalibrationSave
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationSave.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorLevelCalibrationSave message.
   * @function verify
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorLevelCalibrationSave.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqMotorLevelCalibrationSave message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorLevelCalibrationSave} ReqMotorLevelCalibrationSave
   */
  ReqMotorLevelCalibrationSave.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqMotorLevelCalibrationSave) return object;
    return new $root.ReqMotorLevelCalibrationSave();
  };

  /**
   * Creates a plain object from a ReqMotorLevelCalibrationSave message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {ReqMotorLevelCalibrationSave} message ReqMotorLevelCalibrationSave
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorLevelCalibrationSave.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqMotorLevelCalibrationSave to JSON.
   * @function toJSON
   * @memberof ReqMotorLevelCalibrationSave
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorLevelCalibrationSave.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorLevelCalibrationSave
   * @function getTypeUrl
   * @memberof ReqMotorLevelCalibrationSave
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorLevelCalibrationSave.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorLevelCalibrationSave";
  };

  return ReqMotorLevelCalibrationSave;
})();

$root.ReqMotorLevelCalibrationResetDefault = (function () {
  /**
   * Properties of a ReqMotorLevelCalibrationResetDefault.
   * @exports IReqMotorLevelCalibrationResetDefault
   * @interface IReqMotorLevelCalibrationResetDefault
   */

  /**
   * Constructs a new ReqMotorLevelCalibrationResetDefault.
   * @exports ReqMotorLevelCalibrationResetDefault
   * @classdesc Represents a ReqMotorLevelCalibrationResetDefault.
   * @implements IReqMotorLevelCalibrationResetDefault
   * @constructor
   * @param {IReqMotorLevelCalibrationResetDefault=} [properties] Properties to set
   */
  function ReqMotorLevelCalibrationResetDefault(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqMotorLevelCalibrationResetDefault instance using the specified properties.
   * @function create
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {IReqMotorLevelCalibrationResetDefault=} [properties] Properties to set
   * @returns {ReqMotorLevelCalibrationResetDefault} ReqMotorLevelCalibrationResetDefault instance
   */
  ReqMotorLevelCalibrationResetDefault.create = function create(properties) {
    return new ReqMotorLevelCalibrationResetDefault(properties);
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationResetDefault message. Does not implicitly {@link ReqMotorLevelCalibrationResetDefault.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {IReqMotorLevelCalibrationResetDefault} message ReqMotorLevelCalibrationResetDefault message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationResetDefault.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqMotorLevelCalibrationResetDefault message, length delimited. Does not implicitly {@link ReqMotorLevelCalibrationResetDefault.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {IReqMotorLevelCalibrationResetDefault} message ReqMotorLevelCalibrationResetDefault message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorLevelCalibrationResetDefault.encodeDelimited =
    function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

  /**
   * Decodes a ReqMotorLevelCalibrationResetDefault message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorLevelCalibrationResetDefault} ReqMotorLevelCalibrationResetDefault
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationResetDefault.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorLevelCalibrationResetDefault();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorLevelCalibrationResetDefault message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorLevelCalibrationResetDefault} ReqMotorLevelCalibrationResetDefault
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorLevelCalibrationResetDefault.decodeDelimited =
    function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

  /**
   * Verifies a ReqMotorLevelCalibrationResetDefault message.
   * @function verify
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorLevelCalibrationResetDefault.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqMotorLevelCalibrationResetDefault message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorLevelCalibrationResetDefault} ReqMotorLevelCalibrationResetDefault
   */
  ReqMotorLevelCalibrationResetDefault.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqMotorLevelCalibrationResetDefault)
      return object;
    return new $root.ReqMotorLevelCalibrationResetDefault();
  };

  /**
   * Creates a plain object from a ReqMotorLevelCalibrationResetDefault message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {ReqMotorLevelCalibrationResetDefault} message ReqMotorLevelCalibrationResetDefault
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorLevelCalibrationResetDefault.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqMotorLevelCalibrationResetDefault to JSON.
   * @function toJSON
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorLevelCalibrationResetDefault.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorLevelCalibrationResetDefault
   * @function getTypeUrl
   * @memberof ReqMotorLevelCalibrationResetDefault
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorLevelCalibrationResetDefault.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorLevelCalibrationResetDefault";
  };

  return ReqMotorLevelCalibrationResetDefault;
})();

$root.ReqDualCameraLinkage = (function () {
  /**
   * Properties of a ReqDualCameraLinkage.
   * @exports IReqDualCameraLinkage
   * @interface IReqDualCameraLinkage
   * @property {number|null} [x] ReqDualCameraLinkage x
   * @property {number|null} [y] ReqDualCameraLinkage y
   */

  /**
   * Constructs a new ReqDualCameraLinkage.
   * @exports ReqDualCameraLinkage
   * @classdesc Represents a ReqDualCameraLinkage.
   * @implements IReqDualCameraLinkage
   * @constructor
   * @param {IReqDualCameraLinkage=} [properties] Properties to set
   */
  function ReqDualCameraLinkage(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqDualCameraLinkage x.
   * @member {number} x
   * @memberof ReqDualCameraLinkage
   * @instance
   */
  ReqDualCameraLinkage.prototype.x = 0;

  /**
   * ReqDualCameraLinkage y.
   * @member {number} y
   * @memberof ReqDualCameraLinkage
   * @instance
   */
  ReqDualCameraLinkage.prototype.y = 0;

  /**
   * Creates a new ReqDualCameraLinkage instance using the specified properties.
   * @function create
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {IReqDualCameraLinkage=} [properties] Properties to set
   * @returns {ReqDualCameraLinkage} ReqDualCameraLinkage instance
   */
  ReqDualCameraLinkage.create = function create(properties) {
    return new ReqDualCameraLinkage(properties);
  };

  /**
   * Encodes the specified ReqDualCameraLinkage message. Does not implicitly {@link ReqDualCameraLinkage.verify|verify} messages.
   * @function encode
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {IReqDualCameraLinkage} message ReqDualCameraLinkage message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDualCameraLinkage.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.x);
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.y);
    return writer;
  };

  /**
   * Encodes the specified ReqDualCameraLinkage message, length delimited. Does not implicitly {@link ReqDualCameraLinkage.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {IReqDualCameraLinkage} message ReqDualCameraLinkage message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDualCameraLinkage.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDualCameraLinkage message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDualCameraLinkage} ReqDualCameraLinkage
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDualCameraLinkage.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDualCameraLinkage();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.x = reader.int32();
          break;
        }
        case 2: {
          message.y = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDualCameraLinkage message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDualCameraLinkage} ReqDualCameraLinkage
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDualCameraLinkage.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDualCameraLinkage message.
   * @function verify
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDualCameraLinkage.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      if (!$util.isInteger(message.x)) return "x: integer expected";
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      if (!$util.isInteger(message.y)) return "y: integer expected";
    return null;
  };

  /**
   * Creates a ReqDualCameraLinkage message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDualCameraLinkage} ReqDualCameraLinkage
   */
  ReqDualCameraLinkage.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDualCameraLinkage) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqDualCameraLinkage: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqDualCameraLinkage();
    if (object.x != null) message.x = object.x | 0;
    if (object.y != null) message.y = object.y | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqDualCameraLinkage message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {ReqDualCameraLinkage} message ReqDualCameraLinkage
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDualCameraLinkage.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.x = 0;
      object.y = 0;
    }
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      object.x = message.x;
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      object.y = message.y;
    return object;
  };

  /**
   * Converts this ReqDualCameraLinkage to JSON.
   * @function toJSON
   * @memberof ReqDualCameraLinkage
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDualCameraLinkage.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDualCameraLinkage
   * @function getTypeUrl
   * @memberof ReqDualCameraLinkage
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDualCameraLinkage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDualCameraLinkage";
  };

  return ReqDualCameraLinkage;
})();

$root.ReqMotorStartDeviceAttitudeNotify = (function () {
  /**
   * Properties of a ReqMotorStartDeviceAttitudeNotify.
   * @exports IReqMotorStartDeviceAttitudeNotify
   * @interface IReqMotorStartDeviceAttitudeNotify
   */

  /**
   * Constructs a new ReqMotorStartDeviceAttitudeNotify.
   * @exports ReqMotorStartDeviceAttitudeNotify
   * @classdesc Represents a ReqMotorStartDeviceAttitudeNotify.
   * @implements IReqMotorStartDeviceAttitudeNotify
   * @constructor
   * @param {IReqMotorStartDeviceAttitudeNotify=} [properties] Properties to set
   */
  function ReqMotorStartDeviceAttitudeNotify(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqMotorStartDeviceAttitudeNotify instance using the specified properties.
   * @function create
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStartDeviceAttitudeNotify=} [properties] Properties to set
   * @returns {ReqMotorStartDeviceAttitudeNotify} ReqMotorStartDeviceAttitudeNotify instance
   */
  ReqMotorStartDeviceAttitudeNotify.create = function create(properties) {
    return new ReqMotorStartDeviceAttitudeNotify(properties);
  };

  /**
   * Encodes the specified ReqMotorStartDeviceAttitudeNotify message. Does not implicitly {@link ReqMotorStartDeviceAttitudeNotify.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStartDeviceAttitudeNotify} message ReqMotorStartDeviceAttitudeNotify message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStartDeviceAttitudeNotify.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqMotorStartDeviceAttitudeNotify message, length delimited. Does not implicitly {@link ReqMotorStartDeviceAttitudeNotify.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStartDeviceAttitudeNotify} message ReqMotorStartDeviceAttitudeNotify message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStartDeviceAttitudeNotify.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorStartDeviceAttitudeNotify message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorStartDeviceAttitudeNotify} ReqMotorStartDeviceAttitudeNotify
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStartDeviceAttitudeNotify.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorStartDeviceAttitudeNotify();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorStartDeviceAttitudeNotify message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorStartDeviceAttitudeNotify} ReqMotorStartDeviceAttitudeNotify
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStartDeviceAttitudeNotify.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorStartDeviceAttitudeNotify message.
   * @function verify
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorStartDeviceAttitudeNotify.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqMotorStartDeviceAttitudeNotify message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorStartDeviceAttitudeNotify} ReqMotorStartDeviceAttitudeNotify
   */
  ReqMotorStartDeviceAttitudeNotify.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqMotorStartDeviceAttitudeNotify)
      return object;
    return new $root.ReqMotorStartDeviceAttitudeNotify();
  };

  /**
   * Creates a plain object from a ReqMotorStartDeviceAttitudeNotify message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {ReqMotorStartDeviceAttitudeNotify} message ReqMotorStartDeviceAttitudeNotify
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorStartDeviceAttitudeNotify.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqMotorStartDeviceAttitudeNotify to JSON.
   * @function toJSON
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorStartDeviceAttitudeNotify.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorStartDeviceAttitudeNotify
   * @function getTypeUrl
   * @memberof ReqMotorStartDeviceAttitudeNotify
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorStartDeviceAttitudeNotify.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorStartDeviceAttitudeNotify";
  };

  return ReqMotorStartDeviceAttitudeNotify;
})();

$root.ReqMotorStopDeviceAttitudeNotify = (function () {
  /**
   * Properties of a ReqMotorStopDeviceAttitudeNotify.
   * @exports IReqMotorStopDeviceAttitudeNotify
   * @interface IReqMotorStopDeviceAttitudeNotify
   */

  /**
   * Constructs a new ReqMotorStopDeviceAttitudeNotify.
   * @exports ReqMotorStopDeviceAttitudeNotify
   * @classdesc Represents a ReqMotorStopDeviceAttitudeNotify.
   * @implements IReqMotorStopDeviceAttitudeNotify
   * @constructor
   * @param {IReqMotorStopDeviceAttitudeNotify=} [properties] Properties to set
   */
  function ReqMotorStopDeviceAttitudeNotify(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqMotorStopDeviceAttitudeNotify instance using the specified properties.
   * @function create
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStopDeviceAttitudeNotify=} [properties] Properties to set
   * @returns {ReqMotorStopDeviceAttitudeNotify} ReqMotorStopDeviceAttitudeNotify instance
   */
  ReqMotorStopDeviceAttitudeNotify.create = function create(properties) {
    return new ReqMotorStopDeviceAttitudeNotify(properties);
  };

  /**
   * Encodes the specified ReqMotorStopDeviceAttitudeNotify message. Does not implicitly {@link ReqMotorStopDeviceAttitudeNotify.verify|verify} messages.
   * @function encode
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStopDeviceAttitudeNotify} message ReqMotorStopDeviceAttitudeNotify message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStopDeviceAttitudeNotify.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqMotorStopDeviceAttitudeNotify message, length delimited. Does not implicitly {@link ReqMotorStopDeviceAttitudeNotify.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {IReqMotorStopDeviceAttitudeNotify} message ReqMotorStopDeviceAttitudeNotify message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqMotorStopDeviceAttitudeNotify.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqMotorStopDeviceAttitudeNotify message from the specified reader or buffer.
   * @function decode
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqMotorStopDeviceAttitudeNotify} ReqMotorStopDeviceAttitudeNotify
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStopDeviceAttitudeNotify.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqMotorStopDeviceAttitudeNotify();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqMotorStopDeviceAttitudeNotify message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqMotorStopDeviceAttitudeNotify} ReqMotorStopDeviceAttitudeNotify
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqMotorStopDeviceAttitudeNotify.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqMotorStopDeviceAttitudeNotify message.
   * @function verify
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqMotorStopDeviceAttitudeNotify.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqMotorStopDeviceAttitudeNotify message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqMotorStopDeviceAttitudeNotify} ReqMotorStopDeviceAttitudeNotify
   */
  ReqMotorStopDeviceAttitudeNotify.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqMotorStopDeviceAttitudeNotify) return object;
    return new $root.ReqMotorStopDeviceAttitudeNotify();
  };

  /**
   * Creates a plain object from a ReqMotorStopDeviceAttitudeNotify message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {ReqMotorStopDeviceAttitudeNotify} message ReqMotorStopDeviceAttitudeNotify
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqMotorStopDeviceAttitudeNotify.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqMotorStopDeviceAttitudeNotify to JSON.
   * @function toJSON
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqMotorStopDeviceAttitudeNotify.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqMotorStopDeviceAttitudeNotify
   * @function getTypeUrl
   * @memberof ReqMotorStopDeviceAttitudeNotify
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqMotorStopDeviceAttitudeNotify.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqMotorStopDeviceAttitudeNotify";
  };

  return ReqMotorStopDeviceAttitudeNotify;
})();

$root.notify = (function () {
  /**
   * Namespace notify.
   * @exports notify
   * @namespace
   */
  var notify = {};

  /**
   * OperationState enum.
   * @name notify.OperationState
   * @enum {number}
   * @property {number} OPERATION_STATE_IDLE=0 OPERATION_STATE_IDLE value
   * @property {number} OPERATION_STATE_RUNNING=1 OPERATION_STATE_RUNNING value
   * @property {number} OPERATION_STATE_STOPPING=2 OPERATION_STATE_STOPPING value
   * @property {number} OPERATION_STATE_STOPPED=3 OPERATION_STATE_STOPPED value
   */
  notify.OperationState = (function () {
    var valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = "OPERATION_STATE_IDLE")] = 0;
    values[(valuesById[1] = "OPERATION_STATE_RUNNING")] = 1;
    values[(valuesById[2] = "OPERATION_STATE_STOPPING")] = 2;
    values[(valuesById[3] = "OPERATION_STATE_STOPPED")] = 3;
    return values;
  })();

  /**
   * AstroState enum.
   * @name notify.AstroState
   * @enum {number}
   * @property {number} ASTRO_STATE_IDLE=0 ASTRO_STATE_IDLE value
   * @property {number} ASTRO_STATE_RUNNING=1 ASTRO_STATE_RUNNING value
   * @property {number} ASTRO_STATE_STOPPING=2 ASTRO_STATE_STOPPING value
   * @property {number} ASTRO_STATE_STOPPED=3 ASTRO_STATE_STOPPED value
   * @property {number} ASTRO_STATE_PLATE_SOLVING=4 ASTRO_STATE_PLATE_SOLVING value
   */
  notify.AstroState = (function () {
    var valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = "ASTRO_STATE_IDLE")] = 0;
    values[(valuesById[1] = "ASTRO_STATE_RUNNING")] = 1;
    values[(valuesById[2] = "ASTRO_STATE_STOPPING")] = 2;
    values[(valuesById[3] = "ASTRO_STATE_STOPPED")] = 3;
    values[(valuesById[4] = "ASTRO_STATE_PLATE_SOLVING")] = 4;
    return values;
  })();

  /**
   * SentryModeState enum.
   * @name notify.SentryModeState
   * @enum {number}
   * @property {number} SENTRY_MODE_STATE_IDLE=0 SENTRY_MODE_STATE_IDLE value
   * @property {number} SENTRY_MODE_STATE_INIT=1 SENTRY_MODE_STATE_INIT value
   * @property {number} SENTRY_MODE_STATE_DETECT=2 SENTRY_MODE_STATE_DETECT value
   * @property {number} SENTRY_MODE_STATE_TRACK=3 SENTRY_MODE_STATE_TRACK value
   * @property {number} SENTRY_MODE_STATE_TRACK_FINISH=4 SENTRY_MODE_STATE_TRACK_FINISH value
   * @property {number} SENTRY_MODE_STATE_STOPPING=5 SENTRY_MODE_STATE_STOPPING value
   */
  notify.SentryModeState = (function () {
    var valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = "SENTRY_MODE_STATE_IDLE")] = 0;
    values[(valuesById[1] = "SENTRY_MODE_STATE_INIT")] = 1;
    values[(valuesById[2] = "SENTRY_MODE_STATE_DETECT")] = 2;
    values[(valuesById[3] = "SENTRY_MODE_STATE_TRACK")] = 3;
    values[(valuesById[4] = "SENTRY_MODE_STATE_TRACK_FINISH")] = 4;
    values[(valuesById[5] = "SENTRY_MODE_STATE_STOPPING")] = 5;
    return values;
  })();

  /**
   * SentryObjectType enum.
   * @name notify.SentryObjectType
   * @enum {number}
   * @property {number} SENTRY_OBJECT_TYPE_UNKNOWN=0 SENTRY_OBJECT_TYPE_UNKNOWN value
   * @property {number} SENTRY_OBJECT_TYPE_UFO=1 SENTRY_OBJECT_TYPE_UFO value
   * @property {number} SENTRY_OBJECT_TYPE_BIRD=2 SENTRY_OBJECT_TYPE_BIRD value
   * @property {number} SENTRY_OBJECT_TYPE_PERSON=3 SENTRY_OBJECT_TYPE_PERSON value
   * @property {number} SENTRY_OBJECT_TYPE_ANIMAL=4 SENTRY_OBJECT_TYPE_ANIMAL value
   * @property {number} SENTRY_OBJECT_TYPE_VEHICLE=5 SENTRY_OBJECT_TYPE_VEHICLE value
   * @property {number} SENTRY_OBJECT_TYPE_FLYING=6 SENTRY_OBJECT_TYPE_FLYING value
   * @property {number} SENTRY_OBJECT_TYPE_BOAT=7 SENTRY_OBJECT_TYPE_BOAT value
   */
  notify.SentryObjectType = (function () {
    var valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = "SENTRY_OBJECT_TYPE_UNKNOWN")] = 0;
    values[(valuesById[1] = "SENTRY_OBJECT_TYPE_UFO")] = 1;
    values[(valuesById[2] = "SENTRY_OBJECT_TYPE_BIRD")] = 2;
    values[(valuesById[3] = "SENTRY_OBJECT_TYPE_PERSON")] = 3;
    values[(valuesById[4] = "SENTRY_OBJECT_TYPE_ANIMAL")] = 4;
    values[(valuesById[5] = "SENTRY_OBJECT_TYPE_VEHICLE")] = 5;
    values[(valuesById[6] = "SENTRY_OBJECT_TYPE_FLYING")] = 6;
    values[(valuesById[7] = "SENTRY_OBJECT_TYPE_BOAT")] = 7;
    return values;
  })();

  notify.PictureMatching = (function () {
    /**
     * Properties of a PictureMatching.
     * @memberof notify
     * @interface IPictureMatching
     * @property {number|null} [x] PictureMatching x
     * @property {number|null} [y] PictureMatching y
     * @property {number|null} [width] PictureMatching width
     * @property {number|null} [height] PictureMatching height
     */

    /**
     * Constructs a new PictureMatching.
     * @memberof notify
     * @classdesc Represents a PictureMatching.
     * @implements IPictureMatching
     * @constructor
     * @param {notify.IPictureMatching=} [properties] Properties to set
     */
    function PictureMatching(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PictureMatching x.
     * @member {number} x
     * @memberof notify.PictureMatching
     * @instance
     */
    PictureMatching.prototype.x = 0;

    /**
     * PictureMatching y.
     * @member {number} y
     * @memberof notify.PictureMatching
     * @instance
     */
    PictureMatching.prototype.y = 0;

    /**
     * PictureMatching width.
     * @member {number} width
     * @memberof notify.PictureMatching
     * @instance
     */
    PictureMatching.prototype.width = 0;

    /**
     * PictureMatching height.
     * @member {number} height
     * @memberof notify.PictureMatching
     * @instance
     */
    PictureMatching.prototype.height = 0;

    /**
     * Creates a new PictureMatching instance using the specified properties.
     * @function create
     * @memberof notify.PictureMatching
     * @static
     * @param {notify.IPictureMatching=} [properties] Properties to set
     * @returns {notify.PictureMatching} PictureMatching instance
     */
    PictureMatching.create = function create(properties) {
      return new PictureMatching(properties);
    };

    /**
     * Encodes the specified PictureMatching message. Does not implicitly {@link notify.PictureMatching.verify|verify} messages.
     * @function encode
     * @memberof notify.PictureMatching
     * @static
     * @param {notify.IPictureMatching} message PictureMatching message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PictureMatching.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.x);
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.y);
      if (message.width != null && Object.hasOwnProperty.call(message, "width"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.width);
      if (
        message.height != null &&
        Object.hasOwnProperty.call(message, "height")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint32(message.height);
      return writer;
    };

    /**
     * Encodes the specified PictureMatching message, length delimited. Does not implicitly {@link notify.PictureMatching.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PictureMatching
     * @static
     * @param {notify.IPictureMatching} message PictureMatching message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PictureMatching.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PictureMatching message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PictureMatching
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PictureMatching} PictureMatching
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PictureMatching.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PictureMatching();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.x = reader.uint32();
            break;
          }
          case 2: {
            message.y = reader.uint32();
            break;
          }
          case 3: {
            message.width = reader.uint32();
            break;
          }
          case 4: {
            message.height = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PictureMatching message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PictureMatching
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PictureMatching} PictureMatching
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PictureMatching.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PictureMatching message.
     * @function verify
     * @memberof notify.PictureMatching
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PictureMatching.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        if (!$util.isInteger(message.x)) return "x: integer expected";
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        if (!$util.isInteger(message.y)) return "y: integer expected";
      if (message.width != null && Object.hasOwnProperty.call(message, "width"))
        if (!$util.isInteger(message.width)) return "width: integer expected";
      if (
        message.height != null &&
        Object.hasOwnProperty.call(message, "height")
      )
        if (!$util.isInteger(message.height)) return "height: integer expected";
      return null;
    };

    /**
     * Creates a PictureMatching message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PictureMatching
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PictureMatching} PictureMatching
     */
    PictureMatching.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PictureMatching) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PictureMatching: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PictureMatching();
      if (object.x != null) message.x = object.x >>> 0;
      if (object.y != null) message.y = object.y >>> 0;
      if (object.width != null) message.width = object.width >>> 0;
      if (object.height != null) message.height = object.height >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a PictureMatching message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PictureMatching
     * @static
     * @param {notify.PictureMatching} message PictureMatching
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PictureMatching.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.x = 0;
        object.y = 0;
        object.width = 0;
        object.height = 0;
      }
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        object.x = message.x;
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        object.y = message.y;
      if (message.width != null && Object.hasOwnProperty.call(message, "width"))
        object.width = message.width;
      if (
        message.height != null &&
        Object.hasOwnProperty.call(message, "height")
      )
        object.height = message.height;
      return object;
    };

    /**
     * Converts this PictureMatching to JSON.
     * @function toJSON
     * @memberof notify.PictureMatching
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PictureMatching.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PictureMatching
     * @function getTypeUrl
     * @memberof notify.PictureMatching
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PictureMatching.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PictureMatching";
    };

    return PictureMatching;
  })();

  notify.StorageInfo = (function () {
    /**
     * Properties of a StorageInfo.
     * @memberof notify
     * @interface IStorageInfo
     * @property {number|null} [availableSize] StorageInfo availableSize
     * @property {number|null} [totalSize] StorageInfo totalSize
     * @property {number|null} [storageType] StorageInfo storageType
     * @property {boolean|null} [isValid] StorageInfo isValid
     */

    /**
     * Constructs a new StorageInfo.
     * @memberof notify
     * @classdesc Represents a StorageInfo.
     * @implements IStorageInfo
     * @constructor
     * @param {notify.IStorageInfo=} [properties] Properties to set
     */
    function StorageInfo(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageInfo availableSize.
     * @member {number} availableSize
     * @memberof notify.StorageInfo
     * @instance
     */
    StorageInfo.prototype.availableSize = 0;

    /**
     * StorageInfo totalSize.
     * @member {number} totalSize
     * @memberof notify.StorageInfo
     * @instance
     */
    StorageInfo.prototype.totalSize = 0;

    /**
     * StorageInfo storageType.
     * @member {number} storageType
     * @memberof notify.StorageInfo
     * @instance
     */
    StorageInfo.prototype.storageType = 0;

    /**
     * StorageInfo isValid.
     * @member {boolean} isValid
     * @memberof notify.StorageInfo
     * @instance
     */
    StorageInfo.prototype.isValid = false;

    /**
     * Creates a new StorageInfo instance using the specified properties.
     * @function create
     * @memberof notify.StorageInfo
     * @static
     * @param {notify.IStorageInfo=} [properties] Properties to set
     * @returns {notify.StorageInfo} StorageInfo instance
     */
    StorageInfo.create = function create(properties) {
      return new StorageInfo(properties);
    };

    /**
     * Encodes the specified StorageInfo message. Does not implicitly {@link notify.StorageInfo.verify|verify} messages.
     * @function encode
     * @memberof notify.StorageInfo
     * @static
     * @param {notify.IStorageInfo} message StorageInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageInfo.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.availableSize != null &&
        Object.hasOwnProperty.call(message, "availableSize")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.availableSize);
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.totalSize);
      if (
        message.storageType != null &&
        Object.hasOwnProperty.call(message, "storageType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.storageType);
      if (
        message.isValid != null &&
        Object.hasOwnProperty.call(message, "isValid")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).bool(message.isValid);
      return writer;
    };

    /**
     * Encodes the specified StorageInfo message, length delimited. Does not implicitly {@link notify.StorageInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.StorageInfo
     * @static
     * @param {notify.IStorageInfo} message StorageInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageInfo.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a StorageInfo message from the specified reader or buffer.
     * @function decode
     * @memberof notify.StorageInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.StorageInfo} StorageInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageInfo.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.StorageInfo();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.availableSize = reader.uint32();
            break;
          }
          case 2: {
            message.totalSize = reader.uint32();
            break;
          }
          case 3: {
            message.storageType = reader.int32();
            break;
          }
          case 4: {
            message.isValid = reader.bool();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a StorageInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.StorageInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.StorageInfo} StorageInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageInfo.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageInfo message.
     * @function verify
     * @memberof notify.StorageInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageInfo.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.availableSize != null &&
        Object.hasOwnProperty.call(message, "availableSize")
      )
        if (!$util.isInteger(message.availableSize))
          return "availableSize: integer expected";
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        if (!$util.isInteger(message.totalSize))
          return "totalSize: integer expected";
      if (
        message.storageType != null &&
        Object.hasOwnProperty.call(message, "storageType")
      )
        if (!$util.isInteger(message.storageType))
          return "storageType: integer expected";
      if (
        message.isValid != null &&
        Object.hasOwnProperty.call(message, "isValid")
      )
        if (typeof message.isValid !== "boolean")
          return "isValid: boolean expected";
      return null;
    };

    /**
     * Creates a StorageInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.StorageInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.StorageInfo} StorageInfo
     */
    StorageInfo.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.StorageInfo) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.StorageInfo: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.StorageInfo();
      if (object.availableSize != null)
        message.availableSize = object.availableSize >>> 0;
      if (object.totalSize != null) message.totalSize = object.totalSize >>> 0;
      if (object.storageType != null)
        message.storageType = object.storageType | 0;
      if (object.isValid != null) message.isValid = Boolean(object.isValid);
      return message;
    };

    /**
     * Creates a plain object from a StorageInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.StorageInfo
     * @static
     * @param {notify.StorageInfo} message StorageInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageInfo.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.availableSize = 0;
        object.totalSize = 0;
        object.storageType = 0;
        object.isValid = false;
      }
      if (
        message.availableSize != null &&
        Object.hasOwnProperty.call(message, "availableSize")
      )
        object.availableSize = message.availableSize;
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        object.totalSize = message.totalSize;
      if (
        message.storageType != null &&
        Object.hasOwnProperty.call(message, "storageType")
      )
        object.storageType = message.storageType;
      if (
        message.isValid != null &&
        Object.hasOwnProperty.call(message, "isValid")
      )
        object.isValid = message.isValid;
      return object;
    };

    /**
     * Converts this StorageInfo to JSON.
     * @function toJSON
     * @memberof notify.StorageInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageInfo.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageInfo
     * @function getTypeUrl
     * @memberof notify.StorageInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.StorageInfo";
    };

    return StorageInfo;
  })();

  notify.Temperature = (function () {
    /**
     * Properties of a Temperature.
     * @memberof notify
     * @interface ITemperature
     * @property {number|null} [code] Temperature code
     * @property {number|null} [temperature] Temperature temperature
     */

    /**
     * Constructs a new Temperature.
     * @memberof notify
     * @classdesc Represents a Temperature.
     * @implements ITemperature
     * @constructor
     * @param {notify.ITemperature=} [properties] Properties to set
     */
    function Temperature(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * Temperature code.
     * @member {number} code
     * @memberof notify.Temperature
     * @instance
     */
    Temperature.prototype.code = 0;

    /**
     * Temperature temperature.
     * @member {number} temperature
     * @memberof notify.Temperature
     * @instance
     */
    Temperature.prototype.temperature = 0;

    /**
     * Creates a new Temperature instance using the specified properties.
     * @function create
     * @memberof notify.Temperature
     * @static
     * @param {notify.ITemperature=} [properties] Properties to set
     * @returns {notify.Temperature} Temperature instance
     */
    Temperature.create = function create(properties) {
      return new Temperature(properties);
    };

    /**
     * Encodes the specified Temperature message. Does not implicitly {@link notify.Temperature.verify|verify} messages.
     * @function encode
     * @memberof notify.Temperature
     * @static
     * @param {notify.ITemperature} message Temperature message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Temperature.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.temperature);
      return writer;
    };

    /**
     * Encodes the specified Temperature message, length delimited. Does not implicitly {@link notify.Temperature.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.Temperature
     * @static
     * @param {notify.ITemperature} message Temperature message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Temperature.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a Temperature message from the specified reader or buffer.
     * @function decode
     * @memberof notify.Temperature
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.Temperature} Temperature
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Temperature.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.Temperature();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.code = reader.int32();
            break;
          }
          case 2: {
            message.temperature = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a Temperature message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.Temperature
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.Temperature} Temperature
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Temperature.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Temperature message.
     * @function verify
     * @memberof notify.Temperature
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Temperature.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      )
        if (!$util.isInteger(message.temperature))
          return "temperature: integer expected";
      return null;
    };

    /**
     * Creates a Temperature message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.Temperature
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.Temperature} Temperature
     */
    Temperature.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.Temperature) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.Temperature: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.Temperature();
      if (object.code != null) message.code = object.code | 0;
      if (object.temperature != null)
        message.temperature = object.temperature | 0;
      return message;
    };

    /**
     * Creates a plain object from a Temperature message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.Temperature
     * @static
     * @param {notify.Temperature} message Temperature
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Temperature.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.code = 0;
        object.temperature = 0;
      }
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      )
        object.temperature = message.temperature;
      return object;
    };

    /**
     * Converts this Temperature to JSON.
     * @function toJSON
     * @memberof notify.Temperature
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Temperature.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Temperature
     * @function getTypeUrl
     * @memberof notify.Temperature
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Temperature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.Temperature";
    };

    return Temperature;
  })();

  notify.CmosTemperature = (function () {
    /**
     * Properties of a CmosTemperature.
     * @memberof notify
     * @interface ICmosTemperature
     * @property {number|null} [temperature] CmosTemperature temperature
     * @property {number|null} [cameraType] CmosTemperature cameraType
     */

    /**
     * Constructs a new CmosTemperature.
     * @memberof notify
     * @classdesc Represents a CmosTemperature.
     * @implements ICmosTemperature
     * @constructor
     * @param {notify.ICmosTemperature=} [properties] Properties to set
     */
    function CmosTemperature(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CmosTemperature temperature.
     * @member {number|null|undefined} temperature
     * @memberof notify.CmosTemperature
     * @instance
     */
    CmosTemperature.prototype.temperature = null;

    /**
     * CmosTemperature cameraType.
     * @member {number} cameraType
     * @memberof notify.CmosTemperature
     * @instance
     */
    CmosTemperature.prototype.cameraType = 0;

    // OneOf field names bound to virtual getters and setters
    var $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(CmosTemperature.prototype, "_temperature", {
      get: $util.oneOfGetter(($oneOfFields = ["temperature"])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new CmosTemperature instance using the specified properties.
     * @function create
     * @memberof notify.CmosTemperature
     * @static
     * @param {notify.ICmosTemperature=} [properties] Properties to set
     * @returns {notify.CmosTemperature} CmosTemperature instance
     */
    CmosTemperature.create = function create(properties) {
      return new CmosTemperature(properties);
    };

    /**
     * Encodes the specified CmosTemperature message. Does not implicitly {@link notify.CmosTemperature.verify|verify} messages.
     * @function encode
     * @memberof notify.CmosTemperature
     * @static
     * @param {notify.ICmosTemperature} message CmosTemperature message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CmosTemperature.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.temperature);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified CmosTemperature message, length delimited. Does not implicitly {@link notify.CmosTemperature.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CmosTemperature
     * @static
     * @param {notify.ICmosTemperature} message CmosTemperature message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CmosTemperature.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CmosTemperature message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CmosTemperature
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CmosTemperature} CmosTemperature
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CmosTemperature.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CmosTemperature();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.temperature = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CmosTemperature message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CmosTemperature
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CmosTemperature} CmosTemperature
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CmosTemperature.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CmosTemperature message.
     * @function verify
     * @memberof notify.CmosTemperature
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CmosTemperature.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      var properties = {};
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      ) {
        properties._temperature = 1;
        if (!$util.isInteger(message.temperature))
          return "temperature: integer expected";
      }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a CmosTemperature message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CmosTemperature
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CmosTemperature} CmosTemperature
     */
    CmosTemperature.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CmosTemperature) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CmosTemperature: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CmosTemperature();
      if (object.temperature != null)
        message.temperature = object.temperature | 0;
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a CmosTemperature message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CmosTemperature
     * @static
     * @param {notify.CmosTemperature} message CmosTemperature
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CmosTemperature.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.cameraType = 0;
      if (
        message.temperature != null &&
        Object.hasOwnProperty.call(message, "temperature")
      ) {
        object.temperature = message.temperature;
        if (options.oneofs) object._temperature = "temperature";
      }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this CmosTemperature to JSON.
     * @function toJSON
     * @memberof notify.CmosTemperature
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CmosTemperature.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CmosTemperature
     * @function getTypeUrl
     * @memberof notify.CmosTemperature
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CmosTemperature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CmosTemperature";
    };

    return CmosTemperature;
  })();

  notify.RecordTime = (function () {
    /**
     * Properties of a RecordTime.
     * @memberof notify
     * @interface IRecordTime
     * @property {number|null} [recordTime] RecordTime recordTime
     * @property {number|null} [cameraType] RecordTime cameraType
     */

    /**
     * Constructs a new RecordTime.
     * @memberof notify
     * @classdesc Represents a RecordTime.
     * @implements IRecordTime
     * @constructor
     * @param {notify.IRecordTime=} [properties] Properties to set
     */
    function RecordTime(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * RecordTime recordTime.
     * @member {number} recordTime
     * @memberof notify.RecordTime
     * @instance
     */
    RecordTime.prototype.recordTime = 0;

    /**
     * RecordTime cameraType.
     * @member {number} cameraType
     * @memberof notify.RecordTime
     * @instance
     */
    RecordTime.prototype.cameraType = 0;

    /**
     * Creates a new RecordTime instance using the specified properties.
     * @function create
     * @memberof notify.RecordTime
     * @static
     * @param {notify.IRecordTime=} [properties] Properties to set
     * @returns {notify.RecordTime} RecordTime instance
     */
    RecordTime.create = function create(properties) {
      return new RecordTime(properties);
    };

    /**
     * Encodes the specified RecordTime message. Does not implicitly {@link notify.RecordTime.verify|verify} messages.
     * @function encode
     * @memberof notify.RecordTime
     * @static
     * @param {notify.IRecordTime} message RecordTime message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RecordTime.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.recordTime != null &&
        Object.hasOwnProperty.call(message, "recordTime")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.recordTime);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified RecordTime message, length delimited. Does not implicitly {@link notify.RecordTime.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.RecordTime
     * @static
     * @param {notify.IRecordTime} message RecordTime message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RecordTime.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a RecordTime message from the specified reader or buffer.
     * @function decode
     * @memberof notify.RecordTime
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.RecordTime} RecordTime
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RecordTime.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.RecordTime();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.recordTime = reader.uint32();
            break;
          }
          case 2: {
            message.cameraType = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a RecordTime message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.RecordTime
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.RecordTime} RecordTime
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RecordTime.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RecordTime message.
     * @function verify
     * @memberof notify.RecordTime
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RecordTime.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.recordTime != null &&
        Object.hasOwnProperty.call(message, "recordTime")
      )
        if (!$util.isInteger(message.recordTime))
          return "recordTime: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a RecordTime message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.RecordTime
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.RecordTime} RecordTime
     */
    RecordTime.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.RecordTime) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.RecordTime: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.RecordTime();
      if (object.recordTime != null)
        message.recordTime = object.recordTime >>> 0;
      if (object.cameraType != null)
        message.cameraType = object.cameraType >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a RecordTime message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.RecordTime
     * @static
     * @param {notify.RecordTime} message RecordTime
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RecordTime.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.recordTime = 0;
        object.cameraType = 0;
      }
      if (
        message.recordTime != null &&
        Object.hasOwnProperty.call(message, "recordTime")
      )
        object.recordTime = message.recordTime;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this RecordTime to JSON.
     * @function toJSON
     * @memberof notify.RecordTime
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RecordTime.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RecordTime
     * @function getTypeUrl
     * @memberof notify.RecordTime
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RecordTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.RecordTime";
    };

    return RecordTime;
  })();

  notify.TimeLapseOutTime = (function () {
    /**
     * Properties of a TimeLapseOutTime.
     * @memberof notify
     * @interface ITimeLapseOutTime
     * @property {number|null} [interval] TimeLapseOutTime interval
     * @property {number|null} [outTime] TimeLapseOutTime outTime
     * @property {number|null} [totalTime] TimeLapseOutTime totalTime
     * @property {number|null} [cameraType] TimeLapseOutTime cameraType
     */

    /**
     * Constructs a new TimeLapseOutTime.
     * @memberof notify
     * @classdesc Represents a TimeLapseOutTime.
     * @implements ITimeLapseOutTime
     * @constructor
     * @param {notify.ITimeLapseOutTime=} [properties] Properties to set
     */
    function TimeLapseOutTime(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * TimeLapseOutTime interval.
     * @member {number} interval
     * @memberof notify.TimeLapseOutTime
     * @instance
     */
    TimeLapseOutTime.prototype.interval = 0;

    /**
     * TimeLapseOutTime outTime.
     * @member {number} outTime
     * @memberof notify.TimeLapseOutTime
     * @instance
     */
    TimeLapseOutTime.prototype.outTime = 0;

    /**
     * TimeLapseOutTime totalTime.
     * @member {number} totalTime
     * @memberof notify.TimeLapseOutTime
     * @instance
     */
    TimeLapseOutTime.prototype.totalTime = 0;

    /**
     * TimeLapseOutTime cameraType.
     * @member {number} cameraType
     * @memberof notify.TimeLapseOutTime
     * @instance
     */
    TimeLapseOutTime.prototype.cameraType = 0;

    /**
     * Creates a new TimeLapseOutTime instance using the specified properties.
     * @function create
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {notify.ITimeLapseOutTime=} [properties] Properties to set
     * @returns {notify.TimeLapseOutTime} TimeLapseOutTime instance
     */
    TimeLapseOutTime.create = function create(properties) {
      return new TimeLapseOutTime(properties);
    };

    /**
     * Encodes the specified TimeLapseOutTime message. Does not implicitly {@link notify.TimeLapseOutTime.verify|verify} messages.
     * @function encode
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {notify.ITimeLapseOutTime} message TimeLapseOutTime message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TimeLapseOutTime.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.interval != null &&
        Object.hasOwnProperty.call(message, "interval")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.interval);
      if (
        message.outTime != null &&
        Object.hasOwnProperty.call(message, "outTime")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.outTime);
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.totalTime);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified TimeLapseOutTime message, length delimited. Does not implicitly {@link notify.TimeLapseOutTime.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {notify.ITimeLapseOutTime} message TimeLapseOutTime message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TimeLapseOutTime.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a TimeLapseOutTime message from the specified reader or buffer.
     * @function decode
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.TimeLapseOutTime} TimeLapseOutTime
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TimeLapseOutTime.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.TimeLapseOutTime();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.interval = reader.uint32();
            break;
          }
          case 2: {
            message.outTime = reader.uint32();
            break;
          }
          case 3: {
            message.totalTime = reader.uint32();
            break;
          }
          case 4: {
            message.cameraType = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a TimeLapseOutTime message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.TimeLapseOutTime} TimeLapseOutTime
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TimeLapseOutTime.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a TimeLapseOutTime message.
     * @function verify
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    TimeLapseOutTime.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.interval != null &&
        Object.hasOwnProperty.call(message, "interval")
      )
        if (!$util.isInteger(message.interval))
          return "interval: integer expected";
      if (
        message.outTime != null &&
        Object.hasOwnProperty.call(message, "outTime")
      )
        if (!$util.isInteger(message.outTime))
          return "outTime: integer expected";
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        if (!$util.isInteger(message.totalTime))
          return "totalTime: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a TimeLapseOutTime message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.TimeLapseOutTime} TimeLapseOutTime
     */
    TimeLapseOutTime.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.TimeLapseOutTime) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.TimeLapseOutTime: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.TimeLapseOutTime();
      if (object.interval != null) message.interval = object.interval >>> 0;
      if (object.outTime != null) message.outTime = object.outTime >>> 0;
      if (object.totalTime != null) message.totalTime = object.totalTime >>> 0;
      if (object.cameraType != null)
        message.cameraType = object.cameraType >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a TimeLapseOutTime message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {notify.TimeLapseOutTime} message TimeLapseOutTime
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    TimeLapseOutTime.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.interval = 0;
        object.outTime = 0;
        object.totalTime = 0;
        object.cameraType = 0;
      }
      if (
        message.interval != null &&
        Object.hasOwnProperty.call(message, "interval")
      )
        object.interval = message.interval;
      if (
        message.outTime != null &&
        Object.hasOwnProperty.call(message, "outTime")
      )
        object.outTime = message.outTime;
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        object.totalTime = message.totalTime;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this TimeLapseOutTime to JSON.
     * @function toJSON
     * @memberof notify.TimeLapseOutTime
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    TimeLapseOutTime.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for TimeLapseOutTime
     * @function getTypeUrl
     * @memberof notify.TimeLapseOutTime
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    TimeLapseOutTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.TimeLapseOutTime";
    };

    return TimeLapseOutTime;
  })();

  notify.OperationStateNotify = (function () {
    /**
     * Properties of an OperationStateNotify.
     * @memberof notify
     * @interface IOperationStateNotify
     * @property {notify.OperationState|null} [state] OperationStateNotify state
     */

    /**
     * Constructs a new OperationStateNotify.
     * @memberof notify
     * @classdesc Represents an OperationStateNotify.
     * @implements IOperationStateNotify
     * @constructor
     * @param {notify.IOperationStateNotify=} [properties] Properties to set
     */
    function OperationStateNotify(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * OperationStateNotify state.
     * @member {notify.OperationState} state
     * @memberof notify.OperationStateNotify
     * @instance
     */
    OperationStateNotify.prototype.state = 0;

    /**
     * Creates a new OperationStateNotify instance using the specified properties.
     * @function create
     * @memberof notify.OperationStateNotify
     * @static
     * @param {notify.IOperationStateNotify=} [properties] Properties to set
     * @returns {notify.OperationStateNotify} OperationStateNotify instance
     */
    OperationStateNotify.create = function create(properties) {
      return new OperationStateNotify(properties);
    };

    /**
     * Encodes the specified OperationStateNotify message. Does not implicitly {@link notify.OperationStateNotify.verify|verify} messages.
     * @function encode
     * @memberof notify.OperationStateNotify
     * @static
     * @param {notify.IOperationStateNotify} message OperationStateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OperationStateNotify.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified OperationStateNotify message, length delimited. Does not implicitly {@link notify.OperationStateNotify.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.OperationStateNotify
     * @static
     * @param {notify.IOperationStateNotify} message OperationStateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OperationStateNotify.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an OperationStateNotify message from the specified reader or buffer.
     * @function decode
     * @memberof notify.OperationStateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.OperationStateNotify} OperationStateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OperationStateNotify.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.OperationStateNotify();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an OperationStateNotify message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.OperationStateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.OperationStateNotify} OperationStateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OperationStateNotify.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OperationStateNotify message.
     * @function verify
     * @memberof notify.OperationStateNotify
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OperationStateNotify.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates an OperationStateNotify message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.OperationStateNotify
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.OperationStateNotify} OperationStateNotify
     */
    OperationStateNotify.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.OperationStateNotify) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.OperationStateNotify: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.OperationStateNotify();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from an OperationStateNotify message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.OperationStateNotify
     * @static
     * @param {notify.OperationStateNotify} message OperationStateNotify
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OperationStateNotify.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this OperationStateNotify to JSON.
     * @function toJSON
     * @memberof notify.OperationStateNotify
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OperationStateNotify.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OperationStateNotify
     * @function getTypeUrl
     * @memberof notify.OperationStateNotify
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OperationStateNotify.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.OperationStateNotify";
    };

    return OperationStateNotify;
  })();

  notify.AstroCalibrationState = (function () {
    /**
     * Properties of an AstroCalibrationState.
     * @memberof notify
     * @interface IAstroCalibrationState
     * @property {notify.AstroState|null} [state] AstroCalibrationState state
     * @property {number|null} [plateSolvingTimes] AstroCalibrationState plateSolvingTimes
     */

    /**
     * Constructs a new AstroCalibrationState.
     * @memberof notify
     * @classdesc Represents an AstroCalibrationState.
     * @implements IAstroCalibrationState
     * @constructor
     * @param {notify.IAstroCalibrationState=} [properties] Properties to set
     */
    function AstroCalibrationState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroCalibrationState state.
     * @member {notify.AstroState} state
     * @memberof notify.AstroCalibrationState
     * @instance
     */
    AstroCalibrationState.prototype.state = 0;

    /**
     * AstroCalibrationState plateSolvingTimes.
     * @member {number} plateSolvingTimes
     * @memberof notify.AstroCalibrationState
     * @instance
     */
    AstroCalibrationState.prototype.plateSolvingTimes = 0;

    /**
     * Creates a new AstroCalibrationState instance using the specified properties.
     * @function create
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {notify.IAstroCalibrationState=} [properties] Properties to set
     * @returns {notify.AstroCalibrationState} AstroCalibrationState instance
     */
    AstroCalibrationState.create = function create(properties) {
      return new AstroCalibrationState(properties);
    };

    /**
     * Encodes the specified AstroCalibrationState message. Does not implicitly {@link notify.AstroCalibrationState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {notify.IAstroCalibrationState} message AstroCalibrationState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroCalibrationState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.plateSolvingTimes != null &&
        Object.hasOwnProperty.call(message, "plateSolvingTimes")
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .int32(message.plateSolvingTimes);
      return writer;
    };

    /**
     * Encodes the specified AstroCalibrationState message, length delimited. Does not implicitly {@link notify.AstroCalibrationState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {notify.IAstroCalibrationState} message AstroCalibrationState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroCalibrationState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroCalibrationState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroCalibrationState} AstroCalibrationState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroCalibrationState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroCalibrationState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.plateSolvingTimes = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroCalibrationState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroCalibrationState} AstroCalibrationState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroCalibrationState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroCalibrationState message.
     * @function verify
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroCalibrationState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
            break;
        }
      if (
        message.plateSolvingTimes != null &&
        Object.hasOwnProperty.call(message, "plateSolvingTimes")
      )
        if (!$util.isInteger(message.plateSolvingTimes))
          return "plateSolvingTimes: integer expected";
      return null;
    };

    /**
     * Creates an AstroCalibrationState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroCalibrationState} AstroCalibrationState
     */
    AstroCalibrationState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroCalibrationState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroCalibrationState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroCalibrationState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "ASTRO_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "ASTRO_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "ASTRO_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "ASTRO_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
        case "ASTRO_STATE_PLATE_SOLVING":
        case 4:
          message.state = 4;
          break;
      }
      if (object.plateSolvingTimes != null)
        message.plateSolvingTimes = object.plateSolvingTimes | 0;
      return message;
    };

    /**
     * Creates a plain object from an AstroCalibrationState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {notify.AstroCalibrationState} message AstroCalibrationState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroCalibrationState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "ASTRO_STATE_IDLE" : 0;
        object.plateSolvingTimes = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.AstroState[message.state] === undefined
              ? message.state
              : $root.notify.AstroState[message.state]
            : message.state;
      if (
        message.plateSolvingTimes != null &&
        Object.hasOwnProperty.call(message, "plateSolvingTimes")
      )
        object.plateSolvingTimes = message.plateSolvingTimes;
      return object;
    };

    /**
     * Converts this AstroCalibrationState to JSON.
     * @function toJSON
     * @memberof notify.AstroCalibrationState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroCalibrationState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroCalibrationState
     * @function getTypeUrl
     * @memberof notify.AstroCalibrationState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroCalibrationState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroCalibrationState";
    };

    return AstroCalibrationState;
  })();

  notify.AstroGotoState = (function () {
    /**
     * Properties of an AstroGotoState.
     * @memberof notify
     * @interface IAstroGotoState
     * @property {notify.AstroState|null} [state] AstroGotoState state
     * @property {string|null} [targetName] AstroGotoState targetName
     */

    /**
     * Constructs a new AstroGotoState.
     * @memberof notify
     * @classdesc Represents an AstroGotoState.
     * @implements IAstroGotoState
     * @constructor
     * @param {notify.IAstroGotoState=} [properties] Properties to set
     */
    function AstroGotoState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroGotoState state.
     * @member {notify.AstroState} state
     * @memberof notify.AstroGotoState
     * @instance
     */
    AstroGotoState.prototype.state = 0;

    /**
     * AstroGotoState targetName.
     * @member {string} targetName
     * @memberof notify.AstroGotoState
     * @instance
     */
    AstroGotoState.prototype.targetName = "";

    /**
     * Creates a new AstroGotoState instance using the specified properties.
     * @function create
     * @memberof notify.AstroGotoState
     * @static
     * @param {notify.IAstroGotoState=} [properties] Properties to set
     * @returns {notify.AstroGotoState} AstroGotoState instance
     */
    AstroGotoState.create = function create(properties) {
      return new AstroGotoState(properties);
    };

    /**
     * Encodes the specified AstroGotoState message. Does not implicitly {@link notify.AstroGotoState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroGotoState
     * @static
     * @param {notify.IAstroGotoState} message AstroGotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroGotoState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.targetName);
      return writer;
    };

    /**
     * Encodes the specified AstroGotoState message, length delimited. Does not implicitly {@link notify.AstroGotoState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroGotoState
     * @static
     * @param {notify.IAstroGotoState} message AstroGotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroGotoState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroGotoState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroGotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroGotoState} AstroGotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroGotoState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroGotoState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.targetName = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroGotoState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroGotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroGotoState} AstroGotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroGotoState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroGotoState message.
     * @function verify
     * @memberof notify.AstroGotoState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroGotoState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
            break;
        }
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        if (!$util.isString(message.targetName))
          return "targetName: string expected";
      return null;
    };

    /**
     * Creates an AstroGotoState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroGotoState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroGotoState} AstroGotoState
     */
    AstroGotoState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroGotoState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroGotoState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroGotoState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "ASTRO_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "ASTRO_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "ASTRO_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "ASTRO_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
        case "ASTRO_STATE_PLATE_SOLVING":
        case 4:
          message.state = 4;
          break;
      }
      if (object.targetName != null)
        message.targetName = String(object.targetName);
      return message;
    };

    /**
     * Creates a plain object from an AstroGotoState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroGotoState
     * @static
     * @param {notify.AstroGotoState} message AstroGotoState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroGotoState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "ASTRO_STATE_IDLE" : 0;
        object.targetName = "";
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.AstroState[message.state] === undefined
              ? message.state
              : $root.notify.AstroState[message.state]
            : message.state;
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        object.targetName = message.targetName;
      return object;
    };

    /**
     * Converts this AstroGotoState to JSON.
     * @function toJSON
     * @memberof notify.AstroGotoState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroGotoState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroGotoState
     * @function getTypeUrl
     * @memberof notify.AstroGotoState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroGotoState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroGotoState";
    };

    return AstroGotoState;
  })();

  notify.AstroTrackingState = (function () {
    /**
     * Properties of an AstroTrackingState.
     * @memberof notify
     * @interface IAstroTrackingState
     * @property {notify.OperationState|null} [state] AstroTrackingState state
     * @property {string|null} [targetName] AstroTrackingState targetName
     */

    /**
     * Constructs a new AstroTrackingState.
     * @memberof notify
     * @classdesc Represents an AstroTrackingState.
     * @implements IAstroTrackingState
     * @constructor
     * @param {notify.IAstroTrackingState=} [properties] Properties to set
     */
    function AstroTrackingState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroTrackingState state.
     * @member {notify.OperationState} state
     * @memberof notify.AstroTrackingState
     * @instance
     */
    AstroTrackingState.prototype.state = 0;

    /**
     * AstroTrackingState targetName.
     * @member {string} targetName
     * @memberof notify.AstroTrackingState
     * @instance
     */
    AstroTrackingState.prototype.targetName = "";

    /**
     * Creates a new AstroTrackingState instance using the specified properties.
     * @function create
     * @memberof notify.AstroTrackingState
     * @static
     * @param {notify.IAstroTrackingState=} [properties] Properties to set
     * @returns {notify.AstroTrackingState} AstroTrackingState instance
     */
    AstroTrackingState.create = function create(properties) {
      return new AstroTrackingState(properties);
    };

    /**
     * Encodes the specified AstroTrackingState message. Does not implicitly {@link notify.AstroTrackingState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroTrackingState
     * @static
     * @param {notify.IAstroTrackingState} message AstroTrackingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroTrackingState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.targetName);
      return writer;
    };

    /**
     * Encodes the specified AstroTrackingState message, length delimited. Does not implicitly {@link notify.AstroTrackingState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroTrackingState
     * @static
     * @param {notify.IAstroTrackingState} message AstroTrackingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroTrackingState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroTrackingState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroTrackingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroTrackingState} AstroTrackingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroTrackingState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroTrackingState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.targetName = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroTrackingState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroTrackingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroTrackingState} AstroTrackingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroTrackingState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroTrackingState message.
     * @function verify
     * @memberof notify.AstroTrackingState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroTrackingState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        if (!$util.isString(message.targetName))
          return "targetName: string expected";
      return null;
    };

    /**
     * Creates an AstroTrackingState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroTrackingState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroTrackingState} AstroTrackingState
     */
    AstroTrackingState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroTrackingState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroTrackingState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroTrackingState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.targetName != null)
        message.targetName = String(object.targetName);
      return message;
    };

    /**
     * Creates a plain object from an AstroTrackingState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroTrackingState
     * @static
     * @param {notify.AstroTrackingState} message AstroTrackingState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroTrackingState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.targetName = "";
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        object.targetName = message.targetName;
      return object;
    };

    /**
     * Converts this AstroTrackingState to JSON.
     * @function toJSON
     * @memberof notify.AstroTrackingState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroTrackingState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroTrackingState
     * @function getTypeUrl
     * @memberof notify.AstroTrackingState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroTrackingState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroTrackingState";
    };

    return AstroTrackingState;
  })();

  notify.ProgressCaptureRawDark = (function () {
    /**
     * Properties of a ProgressCaptureRawDark.
     * @memberof notify
     * @interface IProgressCaptureRawDark
     * @property {number|null} [progress] ProgressCaptureRawDark progress
     * @property {number|null} [remainingTime] ProgressCaptureRawDark remainingTime
     * @property {number|null} [cameraType] ProgressCaptureRawDark cameraType
     */

    /**
     * Constructs a new ProgressCaptureRawDark.
     * @memberof notify
     * @classdesc Represents a ProgressCaptureRawDark.
     * @implements IProgressCaptureRawDark
     * @constructor
     * @param {notify.IProgressCaptureRawDark=} [properties] Properties to set
     */
    function ProgressCaptureRawDark(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProgressCaptureRawDark progress.
     * @member {number} progress
     * @memberof notify.ProgressCaptureRawDark
     * @instance
     */
    ProgressCaptureRawDark.prototype.progress = 0;

    /**
     * ProgressCaptureRawDark remainingTime.
     * @member {number} remainingTime
     * @memberof notify.ProgressCaptureRawDark
     * @instance
     */
    ProgressCaptureRawDark.prototype.remainingTime = 0;

    /**
     * ProgressCaptureRawDark cameraType.
     * @member {number} cameraType
     * @memberof notify.ProgressCaptureRawDark
     * @instance
     */
    ProgressCaptureRawDark.prototype.cameraType = 0;

    /**
     * Creates a new ProgressCaptureRawDark instance using the specified properties.
     * @function create
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {notify.IProgressCaptureRawDark=} [properties] Properties to set
     * @returns {notify.ProgressCaptureRawDark} ProgressCaptureRawDark instance
     */
    ProgressCaptureRawDark.create = function create(properties) {
      return new ProgressCaptureRawDark(properties);
    };

    /**
     * Encodes the specified ProgressCaptureRawDark message. Does not implicitly {@link notify.ProgressCaptureRawDark.verify|verify} messages.
     * @function encode
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {notify.IProgressCaptureRawDark} message ProgressCaptureRawDark message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureRawDark.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.progress);
      if (
        message.remainingTime != null &&
        Object.hasOwnProperty.call(message, "remainingTime")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.remainingTime);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified ProgressCaptureRawDark message, length delimited. Does not implicitly {@link notify.ProgressCaptureRawDark.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {notify.IProgressCaptureRawDark} message ProgressCaptureRawDark message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureRawDark.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ProgressCaptureRawDark message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ProgressCaptureRawDark} ProgressCaptureRawDark
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureRawDark.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ProgressCaptureRawDark();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.progress = reader.int32();
            break;
          }
          case 2: {
            message.remainingTime = reader.int32();
            break;
          }
          case 3: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ProgressCaptureRawDark message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ProgressCaptureRawDark} ProgressCaptureRawDark
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureRawDark.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProgressCaptureRawDark message.
     * @function verify
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProgressCaptureRawDark.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        if (!$util.isInteger(message.progress))
          return "progress: integer expected";
      if (
        message.remainingTime != null &&
        Object.hasOwnProperty.call(message, "remainingTime")
      )
        if (!$util.isInteger(message.remainingTime))
          return "remainingTime: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a ProgressCaptureRawDark message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ProgressCaptureRawDark} ProgressCaptureRawDark
     */
    ProgressCaptureRawDark.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ProgressCaptureRawDark) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ProgressCaptureRawDark: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ProgressCaptureRawDark();
      if (object.progress != null) message.progress = object.progress | 0;
      if (object.remainingTime != null)
        message.remainingTime = object.remainingTime | 0;
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a ProgressCaptureRawDark message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {notify.ProgressCaptureRawDark} message ProgressCaptureRawDark
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProgressCaptureRawDark.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.progress = 0;
        object.remainingTime = 0;
        object.cameraType = 0;
      }
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        object.progress = message.progress;
      if (
        message.remainingTime != null &&
        Object.hasOwnProperty.call(message, "remainingTime")
      )
        object.remainingTime = message.remainingTime;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this ProgressCaptureRawDark to JSON.
     * @function toJSON
     * @memberof notify.ProgressCaptureRawDark
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProgressCaptureRawDark.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProgressCaptureRawDark
     * @function getTypeUrl
     * @memberof notify.ProgressCaptureRawDark
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProgressCaptureRawDark.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ProgressCaptureRawDark";
    };

    return ProgressCaptureRawDark;
  })();

  notify.ProgressCaptureRawLiveStacking = (function () {
    /**
     * Properties of a ProgressCaptureRawLiveStacking.
     * @memberof notify
     * @interface IProgressCaptureRawLiveStacking
     * @property {number|null} [totalCount] ProgressCaptureRawLiveStacking totalCount
     * @property {number|null} [updateType] ProgressCaptureRawLiveStacking updateType
     * @property {number|null} [currentCount] ProgressCaptureRawLiveStacking currentCount
     * @property {number|null} [stackedCount] ProgressCaptureRawLiveStacking stackedCount
     * @property {number|null} [expIndex] ProgressCaptureRawLiveStacking expIndex
     * @property {number|null} [gainIndex] ProgressCaptureRawLiveStacking gainIndex
     * @property {string|null} [targetName] ProgressCaptureRawLiveStacking targetName
     * @property {number|null} [cameraType] ProgressCaptureRawLiveStacking cameraType
     * @property {number|null} [shootingTime] ProgressCaptureRawLiveStacking shootingTime
     * @property {number|null} [stackedTime] ProgressCaptureRawLiveStacking stackedTime
     */

    /**
     * Constructs a new ProgressCaptureRawLiveStacking.
     * @memberof notify
     * @classdesc Represents a ProgressCaptureRawLiveStacking.
     * @implements IProgressCaptureRawLiveStacking
     * @constructor
     * @param {notify.IProgressCaptureRawLiveStacking=} [properties] Properties to set
     */
    function ProgressCaptureRawLiveStacking(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProgressCaptureRawLiveStacking totalCount.
     * @member {number} totalCount
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.totalCount = 0;

    /**
     * ProgressCaptureRawLiveStacking updateType.
     * @member {number} updateType
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.updateType = 0;

    /**
     * ProgressCaptureRawLiveStacking currentCount.
     * @member {number} currentCount
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.currentCount = 0;

    /**
     * ProgressCaptureRawLiveStacking stackedCount.
     * @member {number} stackedCount
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.stackedCount = 0;

    /**
     * ProgressCaptureRawLiveStacking expIndex.
     * @member {number} expIndex
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.expIndex = 0;

    /**
     * ProgressCaptureRawLiveStacking gainIndex.
     * @member {number} gainIndex
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.gainIndex = 0;

    /**
     * ProgressCaptureRawLiveStacking targetName.
     * @member {string} targetName
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.targetName = "";

    /**
     * ProgressCaptureRawLiveStacking cameraType.
     * @member {number} cameraType
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.cameraType = 0;

    /**
     * ProgressCaptureRawLiveStacking shootingTime.
     * @member {number|null|undefined} shootingTime
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.shootingTime = null;

    /**
     * ProgressCaptureRawLiveStacking stackedTime.
     * @member {number|null|undefined} stackedTime
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     */
    ProgressCaptureRawLiveStacking.prototype.stackedTime = null;

    // OneOf field names bound to virtual getters and setters
    var $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      ProgressCaptureRawLiveStacking.prototype,
      "_shootingTime",
      {
        get: $util.oneOfGetter(($oneOfFields = ["shootingTime"])),
        set: $util.oneOfSetter($oneOfFields),
      },
    );

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      ProgressCaptureRawLiveStacking.prototype,
      "_stackedTime",
      {
        get: $util.oneOfGetter(($oneOfFields = ["stackedTime"])),
        set: $util.oneOfSetter($oneOfFields),
      },
    );

    /**
     * Creates a new ProgressCaptureRawLiveStacking instance using the specified properties.
     * @function create
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {notify.IProgressCaptureRawLiveStacking=} [properties] Properties to set
     * @returns {notify.ProgressCaptureRawLiveStacking} ProgressCaptureRawLiveStacking instance
     */
    ProgressCaptureRawLiveStacking.create = function create(properties) {
      return new ProgressCaptureRawLiveStacking(properties);
    };

    /**
     * Encodes the specified ProgressCaptureRawLiveStacking message. Does not implicitly {@link notify.ProgressCaptureRawLiveStacking.verify|verify} messages.
     * @function encode
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {notify.IProgressCaptureRawLiveStacking} message ProgressCaptureRawLiveStacking message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureRawLiveStacking.encode = function encode(
      message,
      writer,
      q,
    ) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.totalCount);
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.updateType);
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.currentCount);
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.stackedCount);
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.expIndex);
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.gainIndex);
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.targetName);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.cameraType);
      if (
        message.shootingTime != null &&
        Object.hasOwnProperty.call(message, "shootingTime")
      )
        writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.shootingTime);
      if (
        message.stackedTime != null &&
        Object.hasOwnProperty.call(message, "stackedTime")
      )
        writer.uint32(/* id 10, wireType 0 =*/ 80).int32(message.stackedTime);
      return writer;
    };

    /**
     * Encodes the specified ProgressCaptureRawLiveStacking message, length delimited. Does not implicitly {@link notify.ProgressCaptureRawLiveStacking.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {notify.IProgressCaptureRawLiveStacking} message ProgressCaptureRawLiveStacking message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureRawLiveStacking.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ProgressCaptureRawLiveStacking message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ProgressCaptureRawLiveStacking} ProgressCaptureRawLiveStacking
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureRawLiveStacking.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ProgressCaptureRawLiveStacking();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalCount = reader.int32();
            break;
          }
          case 2: {
            message.updateType = reader.int32();
            break;
          }
          case 3: {
            message.currentCount = reader.int32();
            break;
          }
          case 4: {
            message.stackedCount = reader.int32();
            break;
          }
          case 5: {
            message.expIndex = reader.int32();
            break;
          }
          case 6: {
            message.gainIndex = reader.int32();
            break;
          }
          case 7: {
            message.targetName = reader.string();
            break;
          }
          case 8: {
            message.cameraType = reader.int32();
            break;
          }
          case 9: {
            message.shootingTime = reader.int32();
            break;
          }
          case 10: {
            message.stackedTime = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ProgressCaptureRawLiveStacking message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ProgressCaptureRawLiveStacking} ProgressCaptureRawLiveStacking
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureRawLiveStacking.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProgressCaptureRawLiveStacking message.
     * @function verify
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProgressCaptureRawLiveStacking.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      var properties = {};
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        if (!$util.isInteger(message.totalCount))
          return "totalCount: integer expected";
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        if (!$util.isInteger(message.updateType))
          return "updateType: integer expected";
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        if (!$util.isInteger(message.currentCount))
          return "currentCount: integer expected";
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        if (!$util.isInteger(message.stackedCount))
          return "stackedCount: integer expected";
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        if (!$util.isInteger(message.expIndex))
          return "expIndex: integer expected";
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        if (!$util.isInteger(message.gainIndex))
          return "gainIndex: integer expected";
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        if (!$util.isString(message.targetName))
          return "targetName: string expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      if (
        message.shootingTime != null &&
        Object.hasOwnProperty.call(message, "shootingTime")
      ) {
        properties._shootingTime = 1;
        if (!$util.isInteger(message.shootingTime))
          return "shootingTime: integer expected";
      }
      if (
        message.stackedTime != null &&
        Object.hasOwnProperty.call(message, "stackedTime")
      ) {
        properties._stackedTime = 1;
        if (!$util.isInteger(message.stackedTime))
          return "stackedTime: integer expected";
      }
      return null;
    };

    /**
     * Creates a ProgressCaptureRawLiveStacking message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ProgressCaptureRawLiveStacking} ProgressCaptureRawLiveStacking
     */
    ProgressCaptureRawLiveStacking.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.ProgressCaptureRawLiveStacking)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.ProgressCaptureRawLiveStacking: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ProgressCaptureRawLiveStacking();
      if (object.totalCount != null) message.totalCount = object.totalCount | 0;
      if (object.updateType != null) message.updateType = object.updateType | 0;
      if (object.currentCount != null)
        message.currentCount = object.currentCount | 0;
      if (object.stackedCount != null)
        message.stackedCount = object.stackedCount | 0;
      if (object.expIndex != null) message.expIndex = object.expIndex | 0;
      if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
      if (object.targetName != null)
        message.targetName = String(object.targetName);
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      if (object.shootingTime != null)
        message.shootingTime = object.shootingTime | 0;
      if (object.stackedTime != null)
        message.stackedTime = object.stackedTime | 0;
      return message;
    };

    /**
     * Creates a plain object from a ProgressCaptureRawLiveStacking message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {notify.ProgressCaptureRawLiveStacking} message ProgressCaptureRawLiveStacking
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProgressCaptureRawLiveStacking.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.totalCount = 0;
        object.updateType = 0;
        object.currentCount = 0;
        object.stackedCount = 0;
        object.expIndex = 0;
        object.gainIndex = 0;
        object.targetName = "";
        object.cameraType = 0;
      }
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        object.totalCount = message.totalCount;
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        object.updateType = message.updateType;
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        object.currentCount = message.currentCount;
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        object.stackedCount = message.stackedCount;
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        object.expIndex = message.expIndex;
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        object.gainIndex = message.gainIndex;
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        object.targetName = message.targetName;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      if (
        message.shootingTime != null &&
        Object.hasOwnProperty.call(message, "shootingTime")
      ) {
        object.shootingTime = message.shootingTime;
        if (options.oneofs) object._shootingTime = "shootingTime";
      }
      if (
        message.stackedTime != null &&
        Object.hasOwnProperty.call(message, "stackedTime")
      ) {
        object.stackedTime = message.stackedTime;
        if (options.oneofs) object._stackedTime = "stackedTime";
      }
      return object;
    };

    /**
     * Converts this ProgressCaptureRawLiveStacking to JSON.
     * @function toJSON
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProgressCaptureRawLiveStacking.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProgressCaptureRawLiveStacking
     * @function getTypeUrl
     * @memberof notify.ProgressCaptureRawLiveStacking
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProgressCaptureRawLiveStacking.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ProgressCaptureRawLiveStacking";
    };

    return ProgressCaptureRawLiveStacking;
  })();

  notify.Param = (function () {
    /**
     * Properties of a Param.
     * @memberof notify
     * @interface IParam
     * @property {Array.<ICommonParam>|null} [param] Param param
     */

    /**
     * Constructs a new Param.
     * @memberof notify
     * @classdesc Represents a Param.
     * @implements IParam
     * @constructor
     * @param {notify.IParam=} [properties] Properties to set
     */
    function Param(properties) {
      this.param = [];
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * Param param.
     * @member {Array.<ICommonParam>} param
     * @memberof notify.Param
     * @instance
     */
    Param.prototype.param = $util.emptyArray;

    /**
     * Creates a new Param instance using the specified properties.
     * @function create
     * @memberof notify.Param
     * @static
     * @param {notify.IParam=} [properties] Properties to set
     * @returns {notify.Param} Param instance
     */
    Param.create = function create(properties) {
      return new Param(properties);
    };

    /**
     * Encodes the specified Param message. Does not implicitly {@link notify.Param.verify|verify} messages.
     * @function encode
     * @memberof notify.Param
     * @static
     * @param {notify.IParam} message Param message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Param.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.param != null && message.param.length)
        for (var i = 0; i < message.param.length; ++i)
          $root.CommonParam.encode(
            message.param[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
            q + 1,
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Param message, length delimited. Does not implicitly {@link notify.Param.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.Param
     * @static
     * @param {notify.IParam} message Param message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Param.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a Param message from the specified reader or buffer.
     * @function decode
     * @memberof notify.Param
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.Param} Param
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Param.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.Param();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.param && message.param.length)) message.param = [];
            message.param.push(
              $root.CommonParam.decode(
                reader,
                reader.uint32(),
                undefined,
                long + 1,
              ),
            );
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a Param message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.Param
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.Param} Param
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Param.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Param message.
     * @function verify
     * @memberof notify.Param
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Param.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.param != null &&
        Object.hasOwnProperty.call(message, "param")
      ) {
        if (!Array.isArray(message.param)) return "param: array expected";
        for (var i = 0; i < message.param.length; ++i) {
          var error = $root.CommonParam.verify(message.param[i], long + 1);
          if (error) return "param." + error;
        }
      }
      return null;
    };

    /**
     * Creates a Param message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.Param
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.Param} Param
     */
    Param.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.Param) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.Param: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.Param();
      if (object.param) {
        if (!Array.isArray(object.param))
          throw TypeError(".notify.Param.param: array expected");
        message.param = [];
        for (var i = 0; i < object.param.length; ++i) {
          if (!$util.isObject(object.param[i]))
            throw TypeError(".notify.Param.param: object expected");
          message.param[i] = $root.CommonParam.fromObject(
            object.param[i],
            long + 1,
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a Param message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.Param
     * @static
     * @param {notify.Param} message Param
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Param.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.arrays || options.defaults) object.param = [];
      if (message.param && message.param.length) {
        object.param = [];
        for (var j = 0; j < message.param.length; ++j)
          object.param[j] = $root.CommonParam.toObject(
            message.param[j],
            options,
            q + 1,
          );
      }
      return object;
    };

    /**
     * Converts this Param to JSON.
     * @function toJSON
     * @memberof notify.Param
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Param.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Param
     * @function getTypeUrl
     * @memberof notify.Param
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Param.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.Param";
    };

    return Param;
  })();

  notify.BurstProgress = (function () {
    /**
     * Properties of a BurstProgress.
     * @memberof notify
     * @interface IBurstProgress
     * @property {number|null} [totalCount] BurstProgress totalCount
     * @property {number|null} [completedCount] BurstProgress completedCount
     * @property {number|null} [cameraType] BurstProgress cameraType
     */

    /**
     * Constructs a new BurstProgress.
     * @memberof notify
     * @classdesc Represents a BurstProgress.
     * @implements IBurstProgress
     * @constructor
     * @param {notify.IBurstProgress=} [properties] Properties to set
     */
    function BurstProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * BurstProgress totalCount.
     * @member {number} totalCount
     * @memberof notify.BurstProgress
     * @instance
     */
    BurstProgress.prototype.totalCount = 0;

    /**
     * BurstProgress completedCount.
     * @member {number} completedCount
     * @memberof notify.BurstProgress
     * @instance
     */
    BurstProgress.prototype.completedCount = 0;

    /**
     * BurstProgress cameraType.
     * @member {number} cameraType
     * @memberof notify.BurstProgress
     * @instance
     */
    BurstProgress.prototype.cameraType = 0;

    /**
     * Creates a new BurstProgress instance using the specified properties.
     * @function create
     * @memberof notify.BurstProgress
     * @static
     * @param {notify.IBurstProgress=} [properties] Properties to set
     * @returns {notify.BurstProgress} BurstProgress instance
     */
    BurstProgress.create = function create(properties) {
      return new BurstProgress(properties);
    };

    /**
     * Encodes the specified BurstProgress message. Does not implicitly {@link notify.BurstProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.BurstProgress
     * @static
     * @param {notify.IBurstProgress} message BurstProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BurstProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint32(message.totalCount);
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .uint32(message.completedCount);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified BurstProgress message, length delimited. Does not implicitly {@link notify.BurstProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.BurstProgress
     * @static
     * @param {notify.IBurstProgress} message BurstProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BurstProgress.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a BurstProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.BurstProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.BurstProgress} BurstProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BurstProgress.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.BurstProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalCount = reader.uint32();
            break;
          }
          case 2: {
            message.completedCount = reader.uint32();
            break;
          }
          case 3: {
            message.cameraType = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a BurstProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.BurstProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.BurstProgress} BurstProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BurstProgress.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BurstProgress message.
     * @function verify
     * @memberof notify.BurstProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BurstProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        if (!$util.isInteger(message.totalCount))
          return "totalCount: integer expected";
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        if (!$util.isInteger(message.completedCount))
          return "completedCount: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a BurstProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.BurstProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.BurstProgress} BurstProgress
     */
    BurstProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.BurstProgress) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.BurstProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.BurstProgress();
      if (object.totalCount != null)
        message.totalCount = object.totalCount >>> 0;
      if (object.completedCount != null)
        message.completedCount = object.completedCount >>> 0;
      if (object.cameraType != null)
        message.cameraType = object.cameraType >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a BurstProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.BurstProgress
     * @static
     * @param {notify.BurstProgress} message BurstProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BurstProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.totalCount = 0;
        object.completedCount = 0;
        object.cameraType = 0;
      }
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        object.totalCount = message.totalCount;
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        object.completedCount = message.completedCount;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this BurstProgress to JSON.
     * @function toJSON
     * @memberof notify.BurstProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BurstProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BurstProgress
     * @function getTypeUrl
     * @memberof notify.BurstProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BurstProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.BurstProgress";
    };

    return BurstProgress;
  })();

  notify.PanoramaProgress = (function () {
    /**
     * Properties of a PanoramaProgress.
     * @memberof notify
     * @interface IPanoramaProgress
     * @property {number|null} [totalCount] PanoramaProgress totalCount
     * @property {number|null} [completedCount] PanoramaProgress completedCount
     * @property {number|null} [cameraType] PanoramaProgress cameraType
     */

    /**
     * Constructs a new PanoramaProgress.
     * @memberof notify
     * @classdesc Represents a PanoramaProgress.
     * @implements IPanoramaProgress
     * @constructor
     * @param {notify.IPanoramaProgress=} [properties] Properties to set
     */
    function PanoramaProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaProgress totalCount.
     * @member {number} totalCount
     * @memberof notify.PanoramaProgress
     * @instance
     */
    PanoramaProgress.prototype.totalCount = 0;

    /**
     * PanoramaProgress completedCount.
     * @member {number} completedCount
     * @memberof notify.PanoramaProgress
     * @instance
     */
    PanoramaProgress.prototype.completedCount = 0;

    /**
     * PanoramaProgress cameraType.
     * @member {number} cameraType
     * @memberof notify.PanoramaProgress
     * @instance
     */
    PanoramaProgress.prototype.cameraType = 0;

    /**
     * Creates a new PanoramaProgress instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaProgress
     * @static
     * @param {notify.IPanoramaProgress=} [properties] Properties to set
     * @returns {notify.PanoramaProgress} PanoramaProgress instance
     */
    PanoramaProgress.create = function create(properties) {
      return new PanoramaProgress(properties);
    };

    /**
     * Encodes the specified PanoramaProgress message. Does not implicitly {@link notify.PanoramaProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaProgress
     * @static
     * @param {notify.IPanoramaProgress} message PanoramaProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.totalCount);
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.completedCount);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified PanoramaProgress message, length delimited. Does not implicitly {@link notify.PanoramaProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaProgress
     * @static
     * @param {notify.IPanoramaProgress} message PanoramaProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaProgress.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaProgress} PanoramaProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaProgress.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalCount = reader.int32();
            break;
          }
          case 2: {
            message.completedCount = reader.int32();
            break;
          }
          case 3: {
            message.cameraType = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaProgress} PanoramaProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaProgress.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaProgress message.
     * @function verify
     * @memberof notify.PanoramaProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        if (!$util.isInteger(message.totalCount))
          return "totalCount: integer expected";
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        if (!$util.isInteger(message.completedCount))
          return "completedCount: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a PanoramaProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaProgress} PanoramaProgress
     */
    PanoramaProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaProgress) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaProgress();
      if (object.totalCount != null) message.totalCount = object.totalCount | 0;
      if (object.completedCount != null)
        message.completedCount = object.completedCount | 0;
      if (object.cameraType != null)
        message.cameraType = object.cameraType >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoramaProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaProgress
     * @static
     * @param {notify.PanoramaProgress} message PanoramaProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.totalCount = 0;
        object.completedCount = 0;
        object.cameraType = 0;
      }
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        object.totalCount = message.totalCount;
      if (
        message.completedCount != null &&
        Object.hasOwnProperty.call(message, "completedCount")
      )
        object.completedCount = message.completedCount;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this PanoramaProgress to JSON.
     * @function toJSON
     * @memberof notify.PanoramaProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaProgress
     * @function getTypeUrl
     * @memberof notify.PanoramaProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaProgress";
    };

    return PanoramaProgress;
  })();

  notify.RgbState = (function () {
    /**
     * Properties of a RgbState.
     * @memberof notify
     * @interface IRgbState
     * @property {number|null} [state] RgbState state
     */

    /**
     * Constructs a new RgbState.
     * @memberof notify
     * @classdesc Represents a RgbState.
     * @implements IRgbState
     * @constructor
     * @param {notify.IRgbState=} [properties] Properties to set
     */
    function RgbState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * RgbState state.
     * @member {number} state
     * @memberof notify.RgbState
     * @instance
     */
    RgbState.prototype.state = 0;

    /**
     * Creates a new RgbState instance using the specified properties.
     * @function create
     * @memberof notify.RgbState
     * @static
     * @param {notify.IRgbState=} [properties] Properties to set
     * @returns {notify.RgbState} RgbState instance
     */
    RgbState.create = function create(properties) {
      return new RgbState(properties);
    };

    /**
     * Encodes the specified RgbState message. Does not implicitly {@link notify.RgbState.verify|verify} messages.
     * @function encode
     * @memberof notify.RgbState
     * @static
     * @param {notify.IRgbState} message RgbState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RgbState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified RgbState message, length delimited. Does not implicitly {@link notify.RgbState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.RgbState
     * @static
     * @param {notify.IRgbState} message RgbState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RgbState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a RgbState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.RgbState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.RgbState} RgbState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RgbState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.RgbState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a RgbState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.RgbState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.RgbState} RgbState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RgbState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RgbState message.
     * @function verify
     * @memberof notify.RgbState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RgbState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a RgbState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.RgbState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.RgbState} RgbState
     */
    RgbState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.RgbState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.RgbState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.RgbState();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a RgbState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.RgbState
     * @static
     * @param {notify.RgbState} message RgbState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RgbState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this RgbState to JSON.
     * @function toJSON
     * @memberof notify.RgbState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RgbState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RgbState
     * @function getTypeUrl
     * @memberof notify.RgbState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RgbState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.RgbState";
    };

    return RgbState;
  })();

  notify.PowerIndState = (function () {
    /**
     * Properties of a PowerIndState.
     * @memberof notify
     * @interface IPowerIndState
     * @property {number|null} [state] PowerIndState state
     */

    /**
     * Constructs a new PowerIndState.
     * @memberof notify
     * @classdesc Represents a PowerIndState.
     * @implements IPowerIndState
     * @constructor
     * @param {notify.IPowerIndState=} [properties] Properties to set
     */
    function PowerIndState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PowerIndState state.
     * @member {number} state
     * @memberof notify.PowerIndState
     * @instance
     */
    PowerIndState.prototype.state = 0;

    /**
     * Creates a new PowerIndState instance using the specified properties.
     * @function create
     * @memberof notify.PowerIndState
     * @static
     * @param {notify.IPowerIndState=} [properties] Properties to set
     * @returns {notify.PowerIndState} PowerIndState instance
     */
    PowerIndState.create = function create(properties) {
      return new PowerIndState(properties);
    };

    /**
     * Encodes the specified PowerIndState message. Does not implicitly {@link notify.PowerIndState.verify|verify} messages.
     * @function encode
     * @memberof notify.PowerIndState
     * @static
     * @param {notify.IPowerIndState} message PowerIndState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PowerIndState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified PowerIndState message, length delimited. Does not implicitly {@link notify.PowerIndState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PowerIndState
     * @static
     * @param {notify.IPowerIndState} message PowerIndState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PowerIndState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PowerIndState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PowerIndState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PowerIndState} PowerIndState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PowerIndState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PowerIndState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PowerIndState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PowerIndState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PowerIndState} PowerIndState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PowerIndState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PowerIndState message.
     * @function verify
     * @memberof notify.PowerIndState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PowerIndState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a PowerIndState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PowerIndState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PowerIndState} PowerIndState
     */
    PowerIndState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PowerIndState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PowerIndState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PowerIndState();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a PowerIndState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PowerIndState
     * @static
     * @param {notify.PowerIndState} message PowerIndState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PowerIndState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this PowerIndState to JSON.
     * @function toJSON
     * @memberof notify.PowerIndState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PowerIndState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PowerIndState
     * @function getTypeUrl
     * @memberof notify.PowerIndState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PowerIndState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PowerIndState";
    };

    return PowerIndState;
  })();

  notify.ChargingState = (function () {
    /**
     * Properties of a ChargingState.
     * @memberof notify
     * @interface IChargingState
     * @property {number|null} [state] ChargingState state
     */

    /**
     * Constructs a new ChargingState.
     * @memberof notify
     * @classdesc Represents a ChargingState.
     * @implements IChargingState
     * @constructor
     * @param {notify.IChargingState=} [properties] Properties to set
     */
    function ChargingState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ChargingState state.
     * @member {number} state
     * @memberof notify.ChargingState
     * @instance
     */
    ChargingState.prototype.state = 0;

    /**
     * Creates a new ChargingState instance using the specified properties.
     * @function create
     * @memberof notify.ChargingState
     * @static
     * @param {notify.IChargingState=} [properties] Properties to set
     * @returns {notify.ChargingState} ChargingState instance
     */
    ChargingState.create = function create(properties) {
      return new ChargingState(properties);
    };

    /**
     * Encodes the specified ChargingState message. Does not implicitly {@link notify.ChargingState.verify|verify} messages.
     * @function encode
     * @memberof notify.ChargingState
     * @static
     * @param {notify.IChargingState} message ChargingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ChargingState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified ChargingState message, length delimited. Does not implicitly {@link notify.ChargingState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ChargingState
     * @static
     * @param {notify.IChargingState} message ChargingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ChargingState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ChargingState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ChargingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ChargingState} ChargingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ChargingState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ChargingState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ChargingState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ChargingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ChargingState} ChargingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ChargingState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ChargingState message.
     * @function verify
     * @memberof notify.ChargingState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ChargingState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a ChargingState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ChargingState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ChargingState} ChargingState
     */
    ChargingState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ChargingState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ChargingState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ChargingState();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a ChargingState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ChargingState
     * @static
     * @param {notify.ChargingState} message ChargingState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ChargingState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this ChargingState to JSON.
     * @function toJSON
     * @memberof notify.ChargingState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ChargingState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ChargingState
     * @function getTypeUrl
     * @memberof notify.ChargingState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ChargingState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ChargingState";
    };

    return ChargingState;
  })();

  notify.BatteryInfo = (function () {
    /**
     * Properties of a BatteryInfo.
     * @memberof notify
     * @interface IBatteryInfo
     * @property {number|null} [percentage] BatteryInfo percentage
     * @property {number|null} [cycleCount] BatteryInfo cycleCount
     * @property {number|null} [soh] BatteryInfo soh
     */

    /**
     * Constructs a new BatteryInfo.
     * @memberof notify
     * @classdesc Represents a BatteryInfo.
     * @implements IBatteryInfo
     * @constructor
     * @param {notify.IBatteryInfo=} [properties] Properties to set
     */
    function BatteryInfo(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * BatteryInfo percentage.
     * @member {number} percentage
     * @memberof notify.BatteryInfo
     * @instance
     */
    BatteryInfo.prototype.percentage = 0;

    /**
     * BatteryInfo cycleCount.
     * @member {number|null|undefined} cycleCount
     * @memberof notify.BatteryInfo
     * @instance
     */
    BatteryInfo.prototype.cycleCount = null;

    /**
     * BatteryInfo soh.
     * @member {number|null|undefined} soh
     * @memberof notify.BatteryInfo
     * @instance
     */
    BatteryInfo.prototype.soh = null;

    // OneOf field names bound to virtual getters and setters
    var $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(BatteryInfo.prototype, "_cycleCount", {
      get: $util.oneOfGetter(($oneOfFields = ["cycleCount"])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(BatteryInfo.prototype, "_soh", {
      get: $util.oneOfGetter(($oneOfFields = ["soh"])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new BatteryInfo instance using the specified properties.
     * @function create
     * @memberof notify.BatteryInfo
     * @static
     * @param {notify.IBatteryInfo=} [properties] Properties to set
     * @returns {notify.BatteryInfo} BatteryInfo instance
     */
    BatteryInfo.create = function create(properties) {
      return new BatteryInfo(properties);
    };

    /**
     * Encodes the specified BatteryInfo message. Does not implicitly {@link notify.BatteryInfo.verify|verify} messages.
     * @function encode
     * @memberof notify.BatteryInfo
     * @static
     * @param {notify.IBatteryInfo} message BatteryInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BatteryInfo.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.percentage != null &&
        Object.hasOwnProperty.call(message, "percentage")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.percentage);
      if (
        message.cycleCount != null &&
        Object.hasOwnProperty.call(message, "cycleCount")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cycleCount);
      if (message.soh != null && Object.hasOwnProperty.call(message, "soh"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.soh);
      return writer;
    };

    /**
     * Encodes the specified BatteryInfo message, length delimited. Does not implicitly {@link notify.BatteryInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.BatteryInfo
     * @static
     * @param {notify.IBatteryInfo} message BatteryInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BatteryInfo.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a BatteryInfo message from the specified reader or buffer.
     * @function decode
     * @memberof notify.BatteryInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.BatteryInfo} BatteryInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BatteryInfo.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.BatteryInfo();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.percentage = reader.int32();
            break;
          }
          case 2: {
            message.cycleCount = reader.int32();
            break;
          }
          case 3: {
            message.soh = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a BatteryInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.BatteryInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.BatteryInfo} BatteryInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BatteryInfo.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BatteryInfo message.
     * @function verify
     * @memberof notify.BatteryInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BatteryInfo.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      var properties = {};
      if (
        message.percentage != null &&
        Object.hasOwnProperty.call(message, "percentage")
      )
        if (!$util.isInteger(message.percentage))
          return "percentage: integer expected";
      if (
        message.cycleCount != null &&
        Object.hasOwnProperty.call(message, "cycleCount")
      ) {
        properties._cycleCount = 1;
        if (!$util.isInteger(message.cycleCount))
          return "cycleCount: integer expected";
      }
      if (message.soh != null && Object.hasOwnProperty.call(message, "soh")) {
        properties._soh = 1;
        if (!$util.isInteger(message.soh)) return "soh: integer expected";
      }
      return null;
    };

    /**
     * Creates a BatteryInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.BatteryInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.BatteryInfo} BatteryInfo
     */
    BatteryInfo.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.BatteryInfo) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.BatteryInfo: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.BatteryInfo();
      if (object.percentage != null) message.percentage = object.percentage | 0;
      if (object.cycleCount != null) message.cycleCount = object.cycleCount | 0;
      if (object.soh != null) message.soh = object.soh | 0;
      return message;
    };

    /**
     * Creates a plain object from a BatteryInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.BatteryInfo
     * @static
     * @param {notify.BatteryInfo} message BatteryInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BatteryInfo.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.percentage = 0;
      if (
        message.percentage != null &&
        Object.hasOwnProperty.call(message, "percentage")
      )
        object.percentage = message.percentage;
      if (
        message.cycleCount != null &&
        Object.hasOwnProperty.call(message, "cycleCount")
      ) {
        object.cycleCount = message.cycleCount;
        if (options.oneofs) object._cycleCount = "cycleCount";
      }
      if (message.soh != null && Object.hasOwnProperty.call(message, "soh")) {
        object.soh = message.soh;
        if (options.oneofs) object._soh = "soh";
      }
      return object;
    };

    /**
     * Converts this BatteryInfo to JSON.
     * @function toJSON
     * @memberof notify.BatteryInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BatteryInfo.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BatteryInfo
     * @function getTypeUrl
     * @memberof notify.BatteryInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BatteryInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.BatteryInfo";
    };

    return BatteryInfo;
  })();

  notify.HostSlaveMode = (function () {
    /**
     * Properties of a HostSlaveMode.
     * @memberof notify
     * @interface IHostSlaveMode
     * @property {number|null} [mode] HostSlaveMode mode
     * @property {boolean|null} [lock] HostSlaveMode lock
     */

    /**
     * Constructs a new HostSlaveMode.
     * @memberof notify
     * @classdesc Represents a HostSlaveMode.
     * @implements IHostSlaveMode
     * @constructor
     * @param {notify.IHostSlaveMode=} [properties] Properties to set
     */
    function HostSlaveMode(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * HostSlaveMode mode.
     * @member {number} mode
     * @memberof notify.HostSlaveMode
     * @instance
     */
    HostSlaveMode.prototype.mode = 0;

    /**
     * HostSlaveMode lock.
     * @member {boolean} lock
     * @memberof notify.HostSlaveMode
     * @instance
     */
    HostSlaveMode.prototype.lock = false;

    /**
     * Creates a new HostSlaveMode instance using the specified properties.
     * @function create
     * @memberof notify.HostSlaveMode
     * @static
     * @param {notify.IHostSlaveMode=} [properties] Properties to set
     * @returns {notify.HostSlaveMode} HostSlaveMode instance
     */
    HostSlaveMode.create = function create(properties) {
      return new HostSlaveMode(properties);
    };

    /**
     * Encodes the specified HostSlaveMode message. Does not implicitly {@link notify.HostSlaveMode.verify|verify} messages.
     * @function encode
     * @memberof notify.HostSlaveMode
     * @static
     * @param {notify.IHostSlaveMode} message HostSlaveMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HostSlaveMode.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
      if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.lock);
      return writer;
    };

    /**
     * Encodes the specified HostSlaveMode message, length delimited. Does not implicitly {@link notify.HostSlaveMode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.HostSlaveMode
     * @static
     * @param {notify.IHostSlaveMode} message HostSlaveMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HostSlaveMode.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a HostSlaveMode message from the specified reader or buffer.
     * @function decode
     * @memberof notify.HostSlaveMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.HostSlaveMode} HostSlaveMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HostSlaveMode.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.HostSlaveMode();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.int32();
            break;
          }
          case 2: {
            message.lock = reader.bool();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a HostSlaveMode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.HostSlaveMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.HostSlaveMode} HostSlaveMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HostSlaveMode.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a HostSlaveMode message.
     * @function verify
     * @memberof notify.HostSlaveMode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    HostSlaveMode.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
        if (typeof message.lock !== "boolean") return "lock: boolean expected";
      return null;
    };

    /**
     * Creates a HostSlaveMode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.HostSlaveMode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.HostSlaveMode} HostSlaveMode
     */
    HostSlaveMode.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.HostSlaveMode) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.HostSlaveMode: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.HostSlaveMode();
      if (object.mode != null) message.mode = object.mode | 0;
      if (object.lock != null) message.lock = Boolean(object.lock);
      return message;
    };

    /**
     * Creates a plain object from a HostSlaveMode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.HostSlaveMode
     * @static
     * @param {notify.HostSlaveMode} message HostSlaveMode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    HostSlaveMode.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.mode = 0;
        object.lock = false;
      }
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
        object.lock = message.lock;
      return object;
    };

    /**
     * Converts this HostSlaveMode to JSON.
     * @function toJSON
     * @memberof notify.HostSlaveMode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    HostSlaveMode.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for HostSlaveMode
     * @function getTypeUrl
     * @memberof notify.HostSlaveMode
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    HostSlaveMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.HostSlaveMode";
    };

    return HostSlaveMode;
  })();

  notify.MTPState = (function () {
    /**
     * Properties of a MTPState.
     * @memberof notify
     * @interface IMTPState
     * @property {number|null} [mode] MTPState mode
     */

    /**
     * Constructs a new MTPState.
     * @memberof notify
     * @classdesc Represents a MTPState.
     * @implements IMTPState
     * @constructor
     * @param {notify.IMTPState=} [properties] Properties to set
     */
    function MTPState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * MTPState mode.
     * @member {number} mode
     * @memberof notify.MTPState
     * @instance
     */
    MTPState.prototype.mode = 0;

    /**
     * Creates a new MTPState instance using the specified properties.
     * @function create
     * @memberof notify.MTPState
     * @static
     * @param {notify.IMTPState=} [properties] Properties to set
     * @returns {notify.MTPState} MTPState instance
     */
    MTPState.create = function create(properties) {
      return new MTPState(properties);
    };

    /**
     * Encodes the specified MTPState message. Does not implicitly {@link notify.MTPState.verify|verify} messages.
     * @function encode
     * @memberof notify.MTPState
     * @static
     * @param {notify.IMTPState} message MTPState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MTPState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
      return writer;
    };

    /**
     * Encodes the specified MTPState message, length delimited. Does not implicitly {@link notify.MTPState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.MTPState
     * @static
     * @param {notify.IMTPState} message MTPState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    MTPState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a MTPState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.MTPState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.MTPState} MTPState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MTPState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.MTPState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a MTPState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.MTPState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.MTPState} MTPState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    MTPState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a MTPState message.
     * @function verify
     * @memberof notify.MTPState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    MTPState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      return null;
    };

    /**
     * Creates a MTPState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.MTPState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.MTPState} MTPState
     */
    MTPState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.MTPState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.MTPState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.MTPState();
      if (object.mode != null) message.mode = object.mode | 0;
      return message;
    };

    /**
     * Creates a plain object from a MTPState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.MTPState
     * @static
     * @param {notify.MTPState} message MTPState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    MTPState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.mode = 0;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      return object;
    };

    /**
     * Converts this MTPState to JSON.
     * @function toJSON
     * @memberof notify.MTPState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    MTPState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for MTPState
     * @function getTypeUrl
     * @memberof notify.MTPState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    MTPState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.MTPState";
    };

    return MTPState;
  })();

  notify.TrackResult = (function () {
    /**
     * Properties of a TrackResult.
     * @memberof notify
     * @interface ITrackResult
     * @property {number|null} [x] TrackResult x
     * @property {number|null} [y] TrackResult y
     * @property {number|null} [w] TrackResult w
     * @property {number|null} [h] TrackResult h
     */

    /**
     * Constructs a new TrackResult.
     * @memberof notify
     * @classdesc Represents a TrackResult.
     * @implements ITrackResult
     * @constructor
     * @param {notify.ITrackResult=} [properties] Properties to set
     */
    function TrackResult(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * TrackResult x.
     * @member {number} x
     * @memberof notify.TrackResult
     * @instance
     */
    TrackResult.prototype.x = 0;

    /**
     * TrackResult y.
     * @member {number} y
     * @memberof notify.TrackResult
     * @instance
     */
    TrackResult.prototype.y = 0;

    /**
     * TrackResult w.
     * @member {number} w
     * @memberof notify.TrackResult
     * @instance
     */
    TrackResult.prototype.w = 0;

    /**
     * TrackResult h.
     * @member {number} h
     * @memberof notify.TrackResult
     * @instance
     */
    TrackResult.prototype.h = 0;

    /**
     * Creates a new TrackResult instance using the specified properties.
     * @function create
     * @memberof notify.TrackResult
     * @static
     * @param {notify.ITrackResult=} [properties] Properties to set
     * @returns {notify.TrackResult} TrackResult instance
     */
    TrackResult.create = function create(properties) {
      return new TrackResult(properties);
    };

    /**
     * Encodes the specified TrackResult message. Does not implicitly {@link notify.TrackResult.verify|verify} messages.
     * @function encode
     * @memberof notify.TrackResult
     * @static
     * @param {notify.ITrackResult} message TrackResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TrackResult.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.x);
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.y);
      if (message.w != null && Object.hasOwnProperty.call(message, "w"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.w);
      if (message.h != null && Object.hasOwnProperty.call(message, "h"))
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.h);
      return writer;
    };

    /**
     * Encodes the specified TrackResult message, length delimited. Does not implicitly {@link notify.TrackResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.TrackResult
     * @static
     * @param {notify.ITrackResult} message TrackResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TrackResult.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a TrackResult message from the specified reader or buffer.
     * @function decode
     * @memberof notify.TrackResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.TrackResult} TrackResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TrackResult.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.TrackResult();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.x = reader.int32();
            break;
          }
          case 2: {
            message.y = reader.int32();
            break;
          }
          case 3: {
            message.w = reader.int32();
            break;
          }
          case 4: {
            message.h = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a TrackResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.TrackResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.TrackResult} TrackResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TrackResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a TrackResult message.
     * @function verify
     * @memberof notify.TrackResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    TrackResult.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        if (!$util.isInteger(message.x)) return "x: integer expected";
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        if (!$util.isInteger(message.y)) return "y: integer expected";
      if (message.w != null && Object.hasOwnProperty.call(message, "w"))
        if (!$util.isInteger(message.w)) return "w: integer expected";
      if (message.h != null && Object.hasOwnProperty.call(message, "h"))
        if (!$util.isInteger(message.h)) return "h: integer expected";
      return null;
    };

    /**
     * Creates a TrackResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.TrackResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.TrackResult} TrackResult
     */
    TrackResult.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.TrackResult) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.TrackResult: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.TrackResult();
      if (object.x != null) message.x = object.x | 0;
      if (object.y != null) message.y = object.y | 0;
      if (object.w != null) message.w = object.w | 0;
      if (object.h != null) message.h = object.h | 0;
      return message;
    };

    /**
     * Creates a plain object from a TrackResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.TrackResult
     * @static
     * @param {notify.TrackResult} message TrackResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    TrackResult.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.x = 0;
        object.y = 0;
        object.w = 0;
        object.h = 0;
      }
      if (message.x != null && Object.hasOwnProperty.call(message, "x"))
        object.x = message.x;
      if (message.y != null && Object.hasOwnProperty.call(message, "y"))
        object.y = message.y;
      if (message.w != null && Object.hasOwnProperty.call(message, "w"))
        object.w = message.w;
      if (message.h != null && Object.hasOwnProperty.call(message, "h"))
        object.h = message.h;
      return object;
    };

    /**
     * Converts this TrackResult to JSON.
     * @function toJSON
     * @memberof notify.TrackResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    TrackResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for TrackResult
     * @function getTypeUrl
     * @memberof notify.TrackResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    TrackResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.TrackResult";
    };

    return TrackResult;
  })();

  notify.CPUMode = (function () {
    /**
     * Properties of a CPUMode.
     * @memberof notify
     * @interface ICPUMode
     * @property {number|null} [mode] CPUMode mode
     */

    /**
     * Constructs a new CPUMode.
     * @memberof notify
     * @classdesc Represents a CPUMode.
     * @implements ICPUMode
     * @constructor
     * @param {notify.ICPUMode=} [properties] Properties to set
     */
    function CPUMode(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CPUMode mode.
     * @member {number} mode
     * @memberof notify.CPUMode
     * @instance
     */
    CPUMode.prototype.mode = 0;

    /**
     * Creates a new CPUMode instance using the specified properties.
     * @function create
     * @memberof notify.CPUMode
     * @static
     * @param {notify.ICPUMode=} [properties] Properties to set
     * @returns {notify.CPUMode} CPUMode instance
     */
    CPUMode.create = function create(properties) {
      return new CPUMode(properties);
    };

    /**
     * Encodes the specified CPUMode message. Does not implicitly {@link notify.CPUMode.verify|verify} messages.
     * @function encode
     * @memberof notify.CPUMode
     * @static
     * @param {notify.ICPUMode} message CPUMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CPUMode.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
      return writer;
    };

    /**
     * Encodes the specified CPUMode message, length delimited. Does not implicitly {@link notify.CPUMode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CPUMode
     * @static
     * @param {notify.ICPUMode} message CPUMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CPUMode.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CPUMode message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CPUMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CPUMode} CPUMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CPUMode.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CPUMode();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CPUMode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CPUMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CPUMode} CPUMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CPUMode.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CPUMode message.
     * @function verify
     * @memberof notify.CPUMode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CPUMode.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      return null;
    };

    /**
     * Creates a CPUMode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CPUMode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CPUMode} CPUMode
     */
    CPUMode.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CPUMode) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CPUMode: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CPUMode();
      if (object.mode != null) message.mode = object.mode | 0;
      return message;
    };

    /**
     * Creates a plain object from a CPUMode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CPUMode
     * @static
     * @param {notify.CPUMode} message CPUMode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CPUMode.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.mode = 0;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      return object;
    };

    /**
     * Converts this CPUMode to JSON.
     * @function toJSON
     * @memberof notify.CPUMode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CPUMode.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CPUMode
     * @function getTypeUrl
     * @memberof notify.CPUMode
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CPUMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CPUMode";
    };

    return CPUMode;
  })();

  notify.AstroTrackingSpecialState = (function () {
    /**
     * Properties of an AstroTrackingSpecialState.
     * @memberof notify
     * @interface IAstroTrackingSpecialState
     * @property {notify.OperationState|null} [state] AstroTrackingSpecialState state
     * @property {string|null} [targetName] AstroTrackingSpecialState targetName
     * @property {number|null} [index] AstroTrackingSpecialState index
     */

    /**
     * Constructs a new AstroTrackingSpecialState.
     * @memberof notify
     * @classdesc Represents an AstroTrackingSpecialState.
     * @implements IAstroTrackingSpecialState
     * @constructor
     * @param {notify.IAstroTrackingSpecialState=} [properties] Properties to set
     */
    function AstroTrackingSpecialState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroTrackingSpecialState state.
     * @member {notify.OperationState} state
     * @memberof notify.AstroTrackingSpecialState
     * @instance
     */
    AstroTrackingSpecialState.prototype.state = 0;

    /**
     * AstroTrackingSpecialState targetName.
     * @member {string} targetName
     * @memberof notify.AstroTrackingSpecialState
     * @instance
     */
    AstroTrackingSpecialState.prototype.targetName = "";

    /**
     * AstroTrackingSpecialState index.
     * @member {number} index
     * @memberof notify.AstroTrackingSpecialState
     * @instance
     */
    AstroTrackingSpecialState.prototype.index = 0;

    /**
     * Creates a new AstroTrackingSpecialState instance using the specified properties.
     * @function create
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {notify.IAstroTrackingSpecialState=} [properties] Properties to set
     * @returns {notify.AstroTrackingSpecialState} AstroTrackingSpecialState instance
     */
    AstroTrackingSpecialState.create = function create(properties) {
      return new AstroTrackingSpecialState(properties);
    };

    /**
     * Encodes the specified AstroTrackingSpecialState message. Does not implicitly {@link notify.AstroTrackingSpecialState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {notify.IAstroTrackingSpecialState} message AstroTrackingSpecialState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroTrackingSpecialState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.targetName);
      if (message.index != null && Object.hasOwnProperty.call(message, "index"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.index);
      return writer;
    };

    /**
     * Encodes the specified AstroTrackingSpecialState message, length delimited. Does not implicitly {@link notify.AstroTrackingSpecialState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {notify.IAstroTrackingSpecialState} message AstroTrackingSpecialState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroTrackingSpecialState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroTrackingSpecialState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroTrackingSpecialState} AstroTrackingSpecialState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroTrackingSpecialState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroTrackingSpecialState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.targetName = reader.string();
            break;
          }
          case 3: {
            message.index = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroTrackingSpecialState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroTrackingSpecialState} AstroTrackingSpecialState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroTrackingSpecialState.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroTrackingSpecialState message.
     * @function verify
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroTrackingSpecialState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        if (!$util.isString(message.targetName))
          return "targetName: string expected";
      if (message.index != null && Object.hasOwnProperty.call(message, "index"))
        if (!$util.isInteger(message.index)) return "index: integer expected";
      return null;
    };

    /**
     * Creates an AstroTrackingSpecialState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroTrackingSpecialState} AstroTrackingSpecialState
     */
    AstroTrackingSpecialState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroTrackingSpecialState)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroTrackingSpecialState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroTrackingSpecialState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.targetName != null)
        message.targetName = String(object.targetName);
      if (object.index != null) message.index = object.index | 0;
      return message;
    };

    /**
     * Creates a plain object from an AstroTrackingSpecialState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {notify.AstroTrackingSpecialState} message AstroTrackingSpecialState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroTrackingSpecialState.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.targetName = "";
        object.index = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        object.targetName = message.targetName;
      if (message.index != null && Object.hasOwnProperty.call(message, "index"))
        object.index = message.index;
      return object;
    };

    /**
     * Converts this AstroTrackingSpecialState to JSON.
     * @function toJSON
     * @memberof notify.AstroTrackingSpecialState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroTrackingSpecialState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroTrackingSpecialState
     * @function getTypeUrl
     * @memberof notify.AstroTrackingSpecialState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroTrackingSpecialState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroTrackingSpecialState";
    };

    return AstroTrackingSpecialState;
  })();

  notify.PowerOff = (function () {
    /**
     * Properties of a PowerOff.
     * @memberof notify
     * @interface IPowerOff
     */

    /**
     * Constructs a new PowerOff.
     * @memberof notify
     * @classdesc Represents a PowerOff.
     * @implements IPowerOff
     * @constructor
     * @param {notify.IPowerOff=} [properties] Properties to set
     */
    function PowerOff(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * Creates a new PowerOff instance using the specified properties.
     * @function create
     * @memberof notify.PowerOff
     * @static
     * @param {notify.IPowerOff=} [properties] Properties to set
     * @returns {notify.PowerOff} PowerOff instance
     */
    PowerOff.create = function create(properties) {
      return new PowerOff(properties);
    };

    /**
     * Encodes the specified PowerOff message. Does not implicitly {@link notify.PowerOff.verify|verify} messages.
     * @function encode
     * @memberof notify.PowerOff
     * @static
     * @param {notify.IPowerOff} message PowerOff message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PowerOff.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      return writer;
    };

    /**
     * Encodes the specified PowerOff message, length delimited. Does not implicitly {@link notify.PowerOff.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PowerOff
     * @static
     * @param {notify.IPowerOff} message PowerOff message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PowerOff.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PowerOff message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PowerOff
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PowerOff} PowerOff
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PowerOff.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PowerOff();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PowerOff message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PowerOff
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PowerOff} PowerOff
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PowerOff.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PowerOff message.
     * @function verify
     * @memberof notify.PowerOff
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PowerOff.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      return null;
    };

    /**
     * Creates a PowerOff message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PowerOff
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PowerOff} PowerOff
     */
    PowerOff.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PowerOff) return object;
      return new $root.notify.PowerOff();
    };

    /**
     * Creates a plain object from a PowerOff message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PowerOff
     * @static
     * @param {notify.PowerOff} message PowerOff
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PowerOff.toObject = function toObject() {
      return {};
    };

    /**
     * Converts this PowerOff to JSON.
     * @function toJSON
     * @memberof notify.PowerOff
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PowerOff.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PowerOff
     * @function getTypeUrl
     * @memberof notify.PowerOff
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PowerOff.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PowerOff";
    };

    return PowerOff;
  })();

  notify.AlbumUpdate = (function () {
    /**
     * Properties of an AlbumUpdate.
     * @memberof notify
     * @interface IAlbumUpdate
     * @property {number|null} [mediaType] AlbumUpdate mediaType
     */

    /**
     * Constructs a new AlbumUpdate.
     * @memberof notify
     * @classdesc Represents an AlbumUpdate.
     * @implements IAlbumUpdate
     * @constructor
     * @param {notify.IAlbumUpdate=} [properties] Properties to set
     */
    function AlbumUpdate(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AlbumUpdate mediaType.
     * @member {number} mediaType
     * @memberof notify.AlbumUpdate
     * @instance
     */
    AlbumUpdate.prototype.mediaType = 0;

    /**
     * Creates a new AlbumUpdate instance using the specified properties.
     * @function create
     * @memberof notify.AlbumUpdate
     * @static
     * @param {notify.IAlbumUpdate=} [properties] Properties to set
     * @returns {notify.AlbumUpdate} AlbumUpdate instance
     */
    AlbumUpdate.create = function create(properties) {
      return new AlbumUpdate(properties);
    };

    /**
     * Encodes the specified AlbumUpdate message. Does not implicitly {@link notify.AlbumUpdate.verify|verify} messages.
     * @function encode
     * @memberof notify.AlbumUpdate
     * @static
     * @param {notify.IAlbumUpdate} message AlbumUpdate message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AlbumUpdate.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.mediaType != null &&
        Object.hasOwnProperty.call(message, "mediaType")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mediaType);
      return writer;
    };

    /**
     * Encodes the specified AlbumUpdate message, length delimited. Does not implicitly {@link notify.AlbumUpdate.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AlbumUpdate
     * @static
     * @param {notify.IAlbumUpdate} message AlbumUpdate message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AlbumUpdate.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AlbumUpdate message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AlbumUpdate
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AlbumUpdate} AlbumUpdate
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AlbumUpdate.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AlbumUpdate();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mediaType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AlbumUpdate message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AlbumUpdate
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AlbumUpdate} AlbumUpdate
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AlbumUpdate.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AlbumUpdate message.
     * @function verify
     * @memberof notify.AlbumUpdate
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AlbumUpdate.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.mediaType != null &&
        Object.hasOwnProperty.call(message, "mediaType")
      )
        if (!$util.isInteger(message.mediaType))
          return "mediaType: integer expected";
      return null;
    };

    /**
     * Creates an AlbumUpdate message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AlbumUpdate
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AlbumUpdate} AlbumUpdate
     */
    AlbumUpdate.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AlbumUpdate) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AlbumUpdate: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AlbumUpdate();
      if (object.mediaType != null) message.mediaType = object.mediaType | 0;
      return message;
    };

    /**
     * Creates a plain object from an AlbumUpdate message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AlbumUpdate
     * @static
     * @param {notify.AlbumUpdate} message AlbumUpdate
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AlbumUpdate.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.mediaType = 0;
      if (
        message.mediaType != null &&
        Object.hasOwnProperty.call(message, "mediaType")
      )
        object.mediaType = message.mediaType;
      return object;
    };

    /**
     * Converts this AlbumUpdate to JSON.
     * @function toJSON
     * @memberof notify.AlbumUpdate
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AlbumUpdate.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AlbumUpdate
     * @function getTypeUrl
     * @memberof notify.AlbumUpdate
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AlbumUpdate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AlbumUpdate";
    };

    return AlbumUpdate;
  })();

  notify.SentryState = (function () {
    /**
     * Properties of a SentryState.
     * @memberof notify
     * @interface ISentryState
     * @property {notify.SentryModeState|null} [state] SentryState state
     * @property {notify.SentryObjectType|null} [objectType] SentryState objectType
     */

    /**
     * Constructs a new SentryState.
     * @memberof notify
     * @classdesc Represents a SentryState.
     * @implements ISentryState
     * @constructor
     * @param {notify.ISentryState=} [properties] Properties to set
     */
    function SentryState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SentryState state.
     * @member {notify.SentryModeState} state
     * @memberof notify.SentryState
     * @instance
     */
    SentryState.prototype.state = 0;

    /**
     * SentryState objectType.
     * @member {notify.SentryObjectType} objectType
     * @memberof notify.SentryState
     * @instance
     */
    SentryState.prototype.objectType = 0;

    /**
     * Creates a new SentryState instance using the specified properties.
     * @function create
     * @memberof notify.SentryState
     * @static
     * @param {notify.ISentryState=} [properties] Properties to set
     * @returns {notify.SentryState} SentryState instance
     */
    SentryState.create = function create(properties) {
      return new SentryState(properties);
    };

    /**
     * Encodes the specified SentryState message. Does not implicitly {@link notify.SentryState.verify|verify} messages.
     * @function encode
     * @memberof notify.SentryState
     * @static
     * @param {notify.ISentryState} message SentryState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SentryState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.objectType != null &&
        Object.hasOwnProperty.call(message, "objectType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.objectType);
      return writer;
    };

    /**
     * Encodes the specified SentryState message, length delimited. Does not implicitly {@link notify.SentryState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SentryState
     * @static
     * @param {notify.ISentryState} message SentryState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SentryState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SentryState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SentryState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SentryState} SentryState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SentryState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SentryState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.objectType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SentryState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SentryState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SentryState} SentryState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SentryState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SentryState message.
     * @function verify
     * @memberof notify.SentryState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SentryState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
            break;
        }
      if (
        message.objectType != null &&
        Object.hasOwnProperty.call(message, "objectType")
      )
        switch (message.objectType) {
          default:
            return "objectType: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
          case 6:
          case 7:
            break;
        }
      return null;
    };

    /**
     * Creates a SentryState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SentryState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SentryState} SentryState
     */
    SentryState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SentryState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SentryState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SentryState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "SENTRY_MODE_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "SENTRY_MODE_STATE_INIT":
        case 1:
          message.state = 1;
          break;
        case "SENTRY_MODE_STATE_DETECT":
        case 2:
          message.state = 2;
          break;
        case "SENTRY_MODE_STATE_TRACK":
        case 3:
          message.state = 3;
          break;
        case "SENTRY_MODE_STATE_TRACK_FINISH":
        case 4:
          message.state = 4;
          break;
        case "SENTRY_MODE_STATE_STOPPING":
        case 5:
          message.state = 5;
          break;
      }
      switch (object.objectType) {
        default:
          if (typeof object.objectType === "number") {
            message.objectType = object.objectType;
            break;
          }
          break;
        case "SENTRY_OBJECT_TYPE_UNKNOWN":
        case 0:
          message.objectType = 0;
          break;
        case "SENTRY_OBJECT_TYPE_UFO":
        case 1:
          message.objectType = 1;
          break;
        case "SENTRY_OBJECT_TYPE_BIRD":
        case 2:
          message.objectType = 2;
          break;
        case "SENTRY_OBJECT_TYPE_PERSON":
        case 3:
          message.objectType = 3;
          break;
        case "SENTRY_OBJECT_TYPE_ANIMAL":
        case 4:
          message.objectType = 4;
          break;
        case "SENTRY_OBJECT_TYPE_VEHICLE":
        case 5:
          message.objectType = 5;
          break;
        case "SENTRY_OBJECT_TYPE_FLYING":
        case 6:
          message.objectType = 6;
          break;
        case "SENTRY_OBJECT_TYPE_BOAT":
        case 7:
          message.objectType = 7;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a SentryState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SentryState
     * @static
     * @param {notify.SentryState} message SentryState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SentryState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "SENTRY_MODE_STATE_IDLE" : 0;
        object.objectType =
          options.enums === String ? "SENTRY_OBJECT_TYPE_UNKNOWN" : 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.SentryModeState[message.state] === undefined
              ? message.state
              : $root.notify.SentryModeState[message.state]
            : message.state;
      if (
        message.objectType != null &&
        Object.hasOwnProperty.call(message, "objectType")
      )
        object.objectType =
          options.enums === String
            ? $root.notify.SentryObjectType[message.objectType] === undefined
              ? message.objectType
              : $root.notify.SentryObjectType[message.objectType]
            : message.objectType;
      return object;
    };

    /**
     * Converts this SentryState to JSON.
     * @function toJSON
     * @memberof notify.SentryState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SentryState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SentryState
     * @function getTypeUrl
     * @memberof notify.SentryState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SentryState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SentryState";
    };

    return SentryState;
  })();

  notify.OneClickGotoState = (function () {
    /**
     * Properties of an OneClickGotoState.
     * @memberof notify
     * @interface IOneClickGotoState
     * @property {notify.IAstroAutoFocusState|null} [astroAutoFocusState] OneClickGotoState astroAutoFocusState
     * @property {notify.IAstroCalibrationState|null} [astroCalibrationState] OneClickGotoState astroCalibrationState
     * @property {notify.IAstroGotoState|null} [astroGotoState] OneClickGotoState astroGotoState
     * @property {notify.IAstroTrackingState|null} [astroTrackingState] OneClickGotoState astroTrackingState
     */

    /**
     * Constructs a new OneClickGotoState.
     * @memberof notify
     * @classdesc Represents an OneClickGotoState.
     * @implements IOneClickGotoState
     * @constructor
     * @param {notify.IOneClickGotoState=} [properties] Properties to set
     */
    function OneClickGotoState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * OneClickGotoState astroAutoFocusState.
     * @member {notify.IAstroAutoFocusState|null|undefined} astroAutoFocusState
     * @memberof notify.OneClickGotoState
     * @instance
     */
    OneClickGotoState.prototype.astroAutoFocusState = null;

    /**
     * OneClickGotoState astroCalibrationState.
     * @member {notify.IAstroCalibrationState|null|undefined} astroCalibrationState
     * @memberof notify.OneClickGotoState
     * @instance
     */
    OneClickGotoState.prototype.astroCalibrationState = null;

    /**
     * OneClickGotoState astroGotoState.
     * @member {notify.IAstroGotoState|null|undefined} astroGotoState
     * @memberof notify.OneClickGotoState
     * @instance
     */
    OneClickGotoState.prototype.astroGotoState = null;

    /**
     * OneClickGotoState astroTrackingState.
     * @member {notify.IAstroTrackingState|null|undefined} astroTrackingState
     * @memberof notify.OneClickGotoState
     * @instance
     */
    OneClickGotoState.prototype.astroTrackingState = null;

    // OneOf field names bound to virtual getters and setters
    var $oneOfFields;

    /**
     * OneClickGotoState currentState.
     * @member {"astroAutoFocusState"|"astroCalibrationState"|"astroGotoState"|"astroTrackingState"|undefined} currentState
     * @memberof notify.OneClickGotoState
     * @instance
     */
    Object.defineProperty(OneClickGotoState.prototype, "currentState", {
      get: $util.oneOfGetter(
        ($oneOfFields = [
          "astroAutoFocusState",
          "astroCalibrationState",
          "astroGotoState",
          "astroTrackingState",
        ]),
      ),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new OneClickGotoState instance using the specified properties.
     * @function create
     * @memberof notify.OneClickGotoState
     * @static
     * @param {notify.IOneClickGotoState=} [properties] Properties to set
     * @returns {notify.OneClickGotoState} OneClickGotoState instance
     */
    OneClickGotoState.create = function create(properties) {
      return new OneClickGotoState(properties);
    };

    /**
     * Encodes the specified OneClickGotoState message. Does not implicitly {@link notify.OneClickGotoState.verify|verify} messages.
     * @function encode
     * @memberof notify.OneClickGotoState
     * @static
     * @param {notify.IOneClickGotoState} message OneClickGotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OneClickGotoState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.astroAutoFocusState != null &&
        Object.hasOwnProperty.call(message, "astroAutoFocusState")
      )
        $root.notify.AstroAutoFocusState.encode(
          message.astroAutoFocusState,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
      if (
        message.astroCalibrationState != null &&
        Object.hasOwnProperty.call(message, "astroCalibrationState")
      )
        $root.notify.AstroCalibrationState.encode(
          message.astroCalibrationState,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
      if (
        message.astroGotoState != null &&
        Object.hasOwnProperty.call(message, "astroGotoState")
      )
        $root.notify.AstroGotoState.encode(
          message.astroGotoState,
          writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
          q + 1,
        ).ldelim();
      if (
        message.astroTrackingState != null &&
        Object.hasOwnProperty.call(message, "astroTrackingState")
      )
        $root.notify.AstroTrackingState.encode(
          message.astroTrackingState,
          writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
          q + 1,
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified OneClickGotoState message, length delimited. Does not implicitly {@link notify.OneClickGotoState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.OneClickGotoState
     * @static
     * @param {notify.IOneClickGotoState} message OneClickGotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OneClickGotoState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an OneClickGotoState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.OneClickGotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.OneClickGotoState} OneClickGotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OneClickGotoState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.OneClickGotoState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.astroAutoFocusState =
              $root.notify.AstroAutoFocusState.decode(
                reader,
                reader.uint32(),
                undefined,
                long + 1,
              );
            break;
          }
          case 2: {
            message.astroCalibrationState =
              $root.notify.AstroCalibrationState.decode(
                reader,
                reader.uint32(),
                undefined,
                long + 1,
              );
            break;
          }
          case 3: {
            message.astroGotoState = $root.notify.AstroGotoState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
            break;
          }
          case 4: {
            message.astroTrackingState = $root.notify.AstroTrackingState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an OneClickGotoState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.OneClickGotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.OneClickGotoState} OneClickGotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OneClickGotoState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OneClickGotoState message.
     * @function verify
     * @memberof notify.OneClickGotoState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OneClickGotoState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      var properties = {};
      if (
        message.astroAutoFocusState != null &&
        Object.hasOwnProperty.call(message, "astroAutoFocusState")
      ) {
        properties.currentState = 1;
        {
          var error = $root.notify.AstroAutoFocusState.verify(
            message.astroAutoFocusState,
            long + 1,
          );
          if (error) return "astroAutoFocusState." + error;
        }
      }
      if (
        message.astroCalibrationState != null &&
        Object.hasOwnProperty.call(message, "astroCalibrationState")
      ) {
        if (properties.currentState === 1)
          return "currentState: multiple values";
        properties.currentState = 1;
        {
          var error = $root.notify.AstroCalibrationState.verify(
            message.astroCalibrationState,
            long + 1,
          );
          if (error) return "astroCalibrationState." + error;
        }
      }
      if (
        message.astroGotoState != null &&
        Object.hasOwnProperty.call(message, "astroGotoState")
      ) {
        if (properties.currentState === 1)
          return "currentState: multiple values";
        properties.currentState = 1;
        {
          var error = $root.notify.AstroGotoState.verify(
            message.astroGotoState,
            long + 1,
          );
          if (error) return "astroGotoState." + error;
        }
      }
      if (
        message.astroTrackingState != null &&
        Object.hasOwnProperty.call(message, "astroTrackingState")
      ) {
        if (properties.currentState === 1)
          return "currentState: multiple values";
        properties.currentState = 1;
        {
          var error = $root.notify.AstroTrackingState.verify(
            message.astroTrackingState,
            long + 1,
          );
          if (error) return "astroTrackingState." + error;
        }
      }
      return null;
    };

    /**
     * Creates an OneClickGotoState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.OneClickGotoState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.OneClickGotoState} OneClickGotoState
     */
    OneClickGotoState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.OneClickGotoState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.OneClickGotoState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.OneClickGotoState();
      if (object.astroAutoFocusState != null) {
        if (!$util.isObject(object.astroAutoFocusState))
          throw TypeError(
            ".notify.OneClickGotoState.astroAutoFocusState: object expected",
          );
        message.astroAutoFocusState =
          $root.notify.AstroAutoFocusState.fromObject(
            object.astroAutoFocusState,
            long + 1,
          );
      }
      if (object.astroCalibrationState != null) {
        if (!$util.isObject(object.astroCalibrationState))
          throw TypeError(
            ".notify.OneClickGotoState.astroCalibrationState: object expected",
          );
        message.astroCalibrationState =
          $root.notify.AstroCalibrationState.fromObject(
            object.astroCalibrationState,
            long + 1,
          );
      }
      if (object.astroGotoState != null) {
        if (!$util.isObject(object.astroGotoState))
          throw TypeError(
            ".notify.OneClickGotoState.astroGotoState: object expected",
          );
        message.astroGotoState = $root.notify.AstroGotoState.fromObject(
          object.astroGotoState,
          long + 1,
        );
      }
      if (object.astroTrackingState != null) {
        if (!$util.isObject(object.astroTrackingState))
          throw TypeError(
            ".notify.OneClickGotoState.astroTrackingState: object expected",
          );
        message.astroTrackingState = $root.notify.AstroTrackingState.fromObject(
          object.astroTrackingState,
          long + 1,
        );
      }
      return message;
    };

    /**
     * Creates a plain object from an OneClickGotoState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.OneClickGotoState
     * @static
     * @param {notify.OneClickGotoState} message OneClickGotoState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OneClickGotoState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (
        message.astroAutoFocusState != null &&
        Object.hasOwnProperty.call(message, "astroAutoFocusState")
      ) {
        object.astroAutoFocusState = $root.notify.AstroAutoFocusState.toObject(
          message.astroAutoFocusState,
          options,
          q + 1,
        );
        if (options.oneofs) object.currentState = "astroAutoFocusState";
      }
      if (
        message.astroCalibrationState != null &&
        Object.hasOwnProperty.call(message, "astroCalibrationState")
      ) {
        object.astroCalibrationState =
          $root.notify.AstroCalibrationState.toObject(
            message.astroCalibrationState,
            options,
            q + 1,
          );
        if (options.oneofs) object.currentState = "astroCalibrationState";
      }
      if (
        message.astroGotoState != null &&
        Object.hasOwnProperty.call(message, "astroGotoState")
      ) {
        object.astroGotoState = $root.notify.AstroGotoState.toObject(
          message.astroGotoState,
          options,
          q + 1,
        );
        if (options.oneofs) object.currentState = "astroGotoState";
      }
      if (
        message.astroTrackingState != null &&
        Object.hasOwnProperty.call(message, "astroTrackingState")
      ) {
        object.astroTrackingState = $root.notify.AstroTrackingState.toObject(
          message.astroTrackingState,
          options,
          q + 1,
        );
        if (options.oneofs) object.currentState = "astroTrackingState";
      }
      return object;
    };

    /**
     * Converts this OneClickGotoState to JSON.
     * @function toJSON
     * @memberof notify.OneClickGotoState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OneClickGotoState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OneClickGotoState
     * @function getTypeUrl
     * @memberof notify.OneClickGotoState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OneClickGotoState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.OneClickGotoState";
    };

    return OneClickGotoState;
  })();

  notify.StreamType = (function () {
    /**
     * Properties of a StreamType.
     * @memberof notify
     * @interface IStreamType
     * @property {number|null} [streamType] StreamType streamType
     * @property {number|null} [camId] StreamType camId
     */

    /**
     * Constructs a new StreamType.
     * @memberof notify
     * @classdesc Represents a StreamType.
     * @implements IStreamType
     * @constructor
     * @param {notify.IStreamType=} [properties] Properties to set
     */
    function StreamType(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * StreamType streamType.
     * @member {number} streamType
     * @memberof notify.StreamType
     * @instance
     */
    StreamType.prototype.streamType = 0;

    /**
     * StreamType camId.
     * @member {number} camId
     * @memberof notify.StreamType
     * @instance
     */
    StreamType.prototype.camId = 0;

    /**
     * Creates a new StreamType instance using the specified properties.
     * @function create
     * @memberof notify.StreamType
     * @static
     * @param {notify.IStreamType=} [properties] Properties to set
     * @returns {notify.StreamType} StreamType instance
     */
    StreamType.create = function create(properties) {
      return new StreamType(properties);
    };

    /**
     * Encodes the specified StreamType message. Does not implicitly {@link notify.StreamType.verify|verify} messages.
     * @function encode
     * @memberof notify.StreamType
     * @static
     * @param {notify.IStreamType} message StreamType message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StreamType.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.streamType != null &&
        Object.hasOwnProperty.call(message, "streamType")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.streamType);
      if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.camId);
      return writer;
    };

    /**
     * Encodes the specified StreamType message, length delimited. Does not implicitly {@link notify.StreamType.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.StreamType
     * @static
     * @param {notify.IStreamType} message StreamType message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StreamType.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a StreamType message from the specified reader or buffer.
     * @function decode
     * @memberof notify.StreamType
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.StreamType} StreamType
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StreamType.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.StreamType();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.streamType = reader.int32();
            break;
          }
          case 2: {
            message.camId = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a StreamType message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.StreamType
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.StreamType} StreamType
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StreamType.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StreamType message.
     * @function verify
     * @memberof notify.StreamType
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StreamType.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.streamType != null &&
        Object.hasOwnProperty.call(message, "streamType")
      )
        if (!$util.isInteger(message.streamType))
          return "streamType: integer expected";
      if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
        if (!$util.isInteger(message.camId)) return "camId: integer expected";
      return null;
    };

    /**
     * Creates a StreamType message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.StreamType
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.StreamType} StreamType
     */
    StreamType.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.StreamType) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.StreamType: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.StreamType();
      if (object.streamType != null) message.streamType = object.streamType | 0;
      if (object.camId != null) message.camId = object.camId | 0;
      return message;
    };

    /**
     * Creates a plain object from a StreamType message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.StreamType
     * @static
     * @param {notify.StreamType} message StreamType
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StreamType.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.streamType = 0;
        object.camId = 0;
      }
      if (
        message.streamType != null &&
        Object.hasOwnProperty.call(message, "streamType")
      )
        object.streamType = message.streamType;
      if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
        object.camId = message.camId;
      return object;
    };

    /**
     * Converts this StreamType to JSON.
     * @function toJSON
     * @memberof notify.StreamType
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StreamType.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StreamType
     * @function getTypeUrl
     * @memberof notify.StreamType
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StreamType.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.StreamType";
    };

    return StreamType;
  })();

  notify.EqSolvingState = (function () {
    /**
     * Properties of an EqSolvingState.
     * @memberof notify
     * @interface IEqSolvingState
     * @property {notify.OperationState|null} [state] EqSolvingState state
     */

    /**
     * Constructs a new EqSolvingState.
     * @memberof notify
     * @classdesc Represents an EqSolvingState.
     * @implements IEqSolvingState
     * @constructor
     * @param {notify.IEqSolvingState=} [properties] Properties to set
     */
    function EqSolvingState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * EqSolvingState state.
     * @member {notify.OperationState} state
     * @memberof notify.EqSolvingState
     * @instance
     */
    EqSolvingState.prototype.state = 0;

    /**
     * Creates a new EqSolvingState instance using the specified properties.
     * @function create
     * @memberof notify.EqSolvingState
     * @static
     * @param {notify.IEqSolvingState=} [properties] Properties to set
     * @returns {notify.EqSolvingState} EqSolvingState instance
     */
    EqSolvingState.create = function create(properties) {
      return new EqSolvingState(properties);
    };

    /**
     * Encodes the specified EqSolvingState message. Does not implicitly {@link notify.EqSolvingState.verify|verify} messages.
     * @function encode
     * @memberof notify.EqSolvingState
     * @static
     * @param {notify.IEqSolvingState} message EqSolvingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EqSolvingState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified EqSolvingState message, length delimited. Does not implicitly {@link notify.EqSolvingState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.EqSolvingState
     * @static
     * @param {notify.IEqSolvingState} message EqSolvingState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EqSolvingState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an EqSolvingState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.EqSolvingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.EqSolvingState} EqSolvingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EqSolvingState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.EqSolvingState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an EqSolvingState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.EqSolvingState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.EqSolvingState} EqSolvingState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EqSolvingState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an EqSolvingState message.
     * @function verify
     * @memberof notify.EqSolvingState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    EqSolvingState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates an EqSolvingState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.EqSolvingState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.EqSolvingState} EqSolvingState
     */
    EqSolvingState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.EqSolvingState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.EqSolvingState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.EqSolvingState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from an EqSolvingState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.EqSolvingState
     * @static
     * @param {notify.EqSolvingState} message EqSolvingState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    EqSolvingState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this EqSolvingState to JSON.
     * @function toJSON
     * @memberof notify.EqSolvingState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    EqSolvingState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for EqSolvingState
     * @function getTypeUrl
     * @memberof notify.EqSolvingState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    EqSolvingState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.EqSolvingState";
    };

    return EqSolvingState;
  })();

  notify.LongExpPhotoProgress = (function () {
    /**
     * Properties of a LongExpPhotoProgress.
     * @memberof notify
     * @interface ILongExpPhotoProgress
     * @property {number|null} [totalTime] LongExpPhotoProgress totalTime
     * @property {number|null} [exposuredTime] LongExpPhotoProgress exposuredTime
     * @property {number|null} [cameraType] LongExpPhotoProgress cameraType
     */

    /**
     * Constructs a new LongExpPhotoProgress.
     * @memberof notify
     * @classdesc Represents a LongExpPhotoProgress.
     * @implements ILongExpPhotoProgress
     * @constructor
     * @param {notify.ILongExpPhotoProgress=} [properties] Properties to set
     */
    function LongExpPhotoProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * LongExpPhotoProgress totalTime.
     * @member {number} totalTime
     * @memberof notify.LongExpPhotoProgress
     * @instance
     */
    LongExpPhotoProgress.prototype.totalTime = 0;

    /**
     * LongExpPhotoProgress exposuredTime.
     * @member {number} exposuredTime
     * @memberof notify.LongExpPhotoProgress
     * @instance
     */
    LongExpPhotoProgress.prototype.exposuredTime = 0;

    /**
     * LongExpPhotoProgress cameraType.
     * @member {number} cameraType
     * @memberof notify.LongExpPhotoProgress
     * @instance
     */
    LongExpPhotoProgress.prototype.cameraType = 0;

    /**
     * Creates a new LongExpPhotoProgress instance using the specified properties.
     * @function create
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {notify.ILongExpPhotoProgress=} [properties] Properties to set
     * @returns {notify.LongExpPhotoProgress} LongExpPhotoProgress instance
     */
    LongExpPhotoProgress.create = function create(properties) {
      return new LongExpPhotoProgress(properties);
    };

    /**
     * Encodes the specified LongExpPhotoProgress message. Does not implicitly {@link notify.LongExpPhotoProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {notify.ILongExpPhotoProgress} message LongExpPhotoProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LongExpPhotoProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.totalTime);
      if (
        message.exposuredTime != null &&
        Object.hasOwnProperty.call(message, "exposuredTime")
      )
        writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.exposuredTime);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified LongExpPhotoProgress message, length delimited. Does not implicitly {@link notify.LongExpPhotoProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {notify.ILongExpPhotoProgress} message LongExpPhotoProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LongExpPhotoProgress.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a LongExpPhotoProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.LongExpPhotoProgress} LongExpPhotoProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LongExpPhotoProgress.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.LongExpPhotoProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalTime = reader.double();
            break;
          }
          case 2: {
            message.exposuredTime = reader.double();
            break;
          }
          case 3: {
            message.cameraType = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a LongExpPhotoProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.LongExpPhotoProgress} LongExpPhotoProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LongExpPhotoProgress.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LongExpPhotoProgress message.
     * @function verify
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LongExpPhotoProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        if (typeof message.totalTime !== "number")
          return "totalTime: number expected";
      if (
        message.exposuredTime != null &&
        Object.hasOwnProperty.call(message, "exposuredTime")
      )
        if (typeof message.exposuredTime !== "number")
          return "exposuredTime: number expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a LongExpPhotoProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.LongExpPhotoProgress} LongExpPhotoProgress
     */
    LongExpPhotoProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.LongExpPhotoProgress) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.LongExpPhotoProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.LongExpPhotoProgress();
      if (object.totalTime != null)
        message.totalTime = Number(object.totalTime);
      if (object.exposuredTime != null)
        message.exposuredTime = Number(object.exposuredTime);
      if (object.cameraType != null)
        message.cameraType = object.cameraType >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a LongExpPhotoProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {notify.LongExpPhotoProgress} message LongExpPhotoProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LongExpPhotoProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.totalTime = 0;
        object.exposuredTime = 0;
        object.cameraType = 0;
      }
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        object.totalTime =
          options.json && !isFinite(message.totalTime)
            ? String(message.totalTime)
            : message.totalTime;
      if (
        message.exposuredTime != null &&
        Object.hasOwnProperty.call(message, "exposuredTime")
      )
        object.exposuredTime =
          options.json && !isFinite(message.exposuredTime)
            ? String(message.exposuredTime)
            : message.exposuredTime;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this LongExpPhotoProgress to JSON.
     * @function toJSON
     * @memberof notify.LongExpPhotoProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LongExpPhotoProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LongExpPhotoProgress
     * @function getTypeUrl
     * @memberof notify.LongExpPhotoProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LongExpPhotoProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.LongExpPhotoProgress";
    };

    return LongExpPhotoProgress;
  })();

  notify.ShootingScheduleResultAndState = (function () {
    /**
     * Properties of a ShootingScheduleResultAndState.
     * @memberof notify
     * @interface IShootingScheduleResultAndState
     * @property {string|null} [scheduleId] ShootingScheduleResultAndState scheduleId
     * @property {number|null} [result] ShootingScheduleResultAndState result
     * @property {number|null} [state] ShootingScheduleResultAndState state
     */

    /**
     * Constructs a new ShootingScheduleResultAndState.
     * @memberof notify
     * @classdesc Represents a ShootingScheduleResultAndState.
     * @implements IShootingScheduleResultAndState
     * @constructor
     * @param {notify.IShootingScheduleResultAndState=} [properties] Properties to set
     */
    function ShootingScheduleResultAndState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ShootingScheduleResultAndState scheduleId.
     * @member {string} scheduleId
     * @memberof notify.ShootingScheduleResultAndState
     * @instance
     */
    ShootingScheduleResultAndState.prototype.scheduleId = "";

    /**
     * ShootingScheduleResultAndState result.
     * @member {number} result
     * @memberof notify.ShootingScheduleResultAndState
     * @instance
     */
    ShootingScheduleResultAndState.prototype.result = 0;

    /**
     * ShootingScheduleResultAndState state.
     * @member {number} state
     * @memberof notify.ShootingScheduleResultAndState
     * @instance
     */
    ShootingScheduleResultAndState.prototype.state = 0;

    /**
     * Creates a new ShootingScheduleResultAndState instance using the specified properties.
     * @function create
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {notify.IShootingScheduleResultAndState=} [properties] Properties to set
     * @returns {notify.ShootingScheduleResultAndState} ShootingScheduleResultAndState instance
     */
    ShootingScheduleResultAndState.create = function create(properties) {
      return new ShootingScheduleResultAndState(properties);
    };

    /**
     * Encodes the specified ShootingScheduleResultAndState message. Does not implicitly {@link notify.ShootingScheduleResultAndState.verify|verify} messages.
     * @function encode
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {notify.IShootingScheduleResultAndState} message ShootingScheduleResultAndState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ShootingScheduleResultAndState.encode = function encode(
      message,
      writer,
      q,
    ) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.scheduleId != null &&
        Object.hasOwnProperty.call(message, "scheduleId")
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.scheduleId);
      if (
        message.result != null &&
        Object.hasOwnProperty.call(message, "result")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.result);
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified ShootingScheduleResultAndState message, length delimited. Does not implicitly {@link notify.ShootingScheduleResultAndState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {notify.IShootingScheduleResultAndState} message ShootingScheduleResultAndState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ShootingScheduleResultAndState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ShootingScheduleResultAndState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ShootingScheduleResultAndState} ShootingScheduleResultAndState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ShootingScheduleResultAndState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ShootingScheduleResultAndState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.scheduleId = reader.string();
            break;
          }
          case 2: {
            message.result = reader.int32();
            break;
          }
          case 3: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ShootingScheduleResultAndState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ShootingScheduleResultAndState} ShootingScheduleResultAndState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ShootingScheduleResultAndState.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ShootingScheduleResultAndState message.
     * @function verify
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ShootingScheduleResultAndState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.scheduleId != null &&
        Object.hasOwnProperty.call(message, "scheduleId")
      )
        if (!$util.isString(message.scheduleId))
          return "scheduleId: string expected";
      if (
        message.result != null &&
        Object.hasOwnProperty.call(message, "result")
      )
        if (!$util.isInteger(message.result)) return "result: integer expected";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a ShootingScheduleResultAndState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ShootingScheduleResultAndState} ShootingScheduleResultAndState
     */
    ShootingScheduleResultAndState.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.ShootingScheduleResultAndState)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.ShootingScheduleResultAndState: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ShootingScheduleResultAndState();
      if (object.scheduleId != null)
        message.scheduleId = String(object.scheduleId);
      if (object.result != null) message.result = object.result | 0;
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a ShootingScheduleResultAndState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {notify.ShootingScheduleResultAndState} message ShootingScheduleResultAndState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ShootingScheduleResultAndState.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.scheduleId = "";
        object.result = 0;
        object.state = 0;
      }
      if (
        message.scheduleId != null &&
        Object.hasOwnProperty.call(message, "scheduleId")
      )
        object.scheduleId = message.scheduleId;
      if (
        message.result != null &&
        Object.hasOwnProperty.call(message, "result")
      )
        object.result = message.result;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this ShootingScheduleResultAndState to JSON.
     * @function toJSON
     * @memberof notify.ShootingScheduleResultAndState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ShootingScheduleResultAndState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ShootingScheduleResultAndState
     * @function getTypeUrl
     * @memberof notify.ShootingScheduleResultAndState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ShootingScheduleResultAndState.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ShootingScheduleResultAndState";
    };

    return ShootingScheduleResultAndState;
  })();

  notify.ShootingTaskState = (function () {
    /**
     * Properties of a ShootingTaskState.
     * @memberof notify
     * @interface IShootingTaskState
     * @property {string|null} [scheduleTaskId] ShootingTaskState scheduleTaskId
     * @property {number|null} [state] ShootingTaskState state
     * @property {number|null} [code] ShootingTaskState code
     */

    /**
     * Constructs a new ShootingTaskState.
     * @memberof notify
     * @classdesc Represents a ShootingTaskState.
     * @implements IShootingTaskState
     * @constructor
     * @param {notify.IShootingTaskState=} [properties] Properties to set
     */
    function ShootingTaskState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ShootingTaskState scheduleTaskId.
     * @member {string} scheduleTaskId
     * @memberof notify.ShootingTaskState
     * @instance
     */
    ShootingTaskState.prototype.scheduleTaskId = "";

    /**
     * ShootingTaskState state.
     * @member {number} state
     * @memberof notify.ShootingTaskState
     * @instance
     */
    ShootingTaskState.prototype.state = 0;

    /**
     * ShootingTaskState code.
     * @member {number} code
     * @memberof notify.ShootingTaskState
     * @instance
     */
    ShootingTaskState.prototype.code = 0;

    /**
     * Creates a new ShootingTaskState instance using the specified properties.
     * @function create
     * @memberof notify.ShootingTaskState
     * @static
     * @param {notify.IShootingTaskState=} [properties] Properties to set
     * @returns {notify.ShootingTaskState} ShootingTaskState instance
     */
    ShootingTaskState.create = function create(properties) {
      return new ShootingTaskState(properties);
    };

    /**
     * Encodes the specified ShootingTaskState message. Does not implicitly {@link notify.ShootingTaskState.verify|verify} messages.
     * @function encode
     * @memberof notify.ShootingTaskState
     * @static
     * @param {notify.IShootingTaskState} message ShootingTaskState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ShootingTaskState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.scheduleTaskId != null &&
        Object.hasOwnProperty.call(message, "scheduleTaskId")
      )
        writer
          .uint32(/* id 1, wireType 2 =*/ 10)
          .string(message.scheduleTaskId);
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.state);
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.code);
      return writer;
    };

    /**
     * Encodes the specified ShootingTaskState message, length delimited. Does not implicitly {@link notify.ShootingTaskState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ShootingTaskState
     * @static
     * @param {notify.IShootingTaskState} message ShootingTaskState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ShootingTaskState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ShootingTaskState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ShootingTaskState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ShootingTaskState} ShootingTaskState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ShootingTaskState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ShootingTaskState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.scheduleTaskId = reader.string();
            break;
          }
          case 2: {
            message.state = reader.int32();
            break;
          }
          case 3: {
            message.code = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ShootingTaskState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ShootingTaskState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ShootingTaskState} ShootingTaskState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ShootingTaskState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ShootingTaskState message.
     * @function verify
     * @memberof notify.ShootingTaskState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ShootingTaskState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.scheduleTaskId != null &&
        Object.hasOwnProperty.call(message, "scheduleTaskId")
      )
        if (!$util.isString(message.scheduleTaskId))
          return "scheduleTaskId: string expected";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      return null;
    };

    /**
     * Creates a ShootingTaskState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ShootingTaskState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ShootingTaskState} ShootingTaskState
     */
    ShootingTaskState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ShootingTaskState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ShootingTaskState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ShootingTaskState();
      if (object.scheduleTaskId != null)
        message.scheduleTaskId = String(object.scheduleTaskId);
      if (object.state != null) message.state = object.state | 0;
      if (object.code != null) message.code = object.code | 0;
      return message;
    };

    /**
     * Creates a plain object from a ShootingTaskState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ShootingTaskState
     * @static
     * @param {notify.ShootingTaskState} message ShootingTaskState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ShootingTaskState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.scheduleTaskId = "";
        object.state = 0;
        object.code = 0;
      }
      if (
        message.scheduleTaskId != null &&
        Object.hasOwnProperty.call(message, "scheduleTaskId")
      )
        object.scheduleTaskId = message.scheduleTaskId;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      return object;
    };

    /**
     * Converts this ShootingTaskState to JSON.
     * @function toJSON
     * @memberof notify.ShootingTaskState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ShootingTaskState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ShootingTaskState
     * @function getTypeUrl
     * @memberof notify.ShootingTaskState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ShootingTaskState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ShootingTaskState";
    };

    return ShootingTaskState;
  })();

  notify.SkySeacherState = (function () {
    /**
     * Properties of a SkySeacherState.
     * @memberof notify
     * @interface ISkySeacherState
     * @property {notify.OperationState|null} [state] SkySeacherState state
     */

    /**
     * Constructs a new SkySeacherState.
     * @memberof notify
     * @classdesc Represents a SkySeacherState.
     * @implements ISkySeacherState
     * @constructor
     * @param {notify.ISkySeacherState=} [properties] Properties to set
     */
    function SkySeacherState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SkySeacherState state.
     * @member {notify.OperationState} state
     * @memberof notify.SkySeacherState
     * @instance
     */
    SkySeacherState.prototype.state = 0;

    /**
     * Creates a new SkySeacherState instance using the specified properties.
     * @function create
     * @memberof notify.SkySeacherState
     * @static
     * @param {notify.ISkySeacherState=} [properties] Properties to set
     * @returns {notify.SkySeacherState} SkySeacherState instance
     */
    SkySeacherState.create = function create(properties) {
      return new SkySeacherState(properties);
    };

    /**
     * Encodes the specified SkySeacherState message. Does not implicitly {@link notify.SkySeacherState.verify|verify} messages.
     * @function encode
     * @memberof notify.SkySeacherState
     * @static
     * @param {notify.ISkySeacherState} message SkySeacherState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SkySeacherState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified SkySeacherState message, length delimited. Does not implicitly {@link notify.SkySeacherState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SkySeacherState
     * @static
     * @param {notify.ISkySeacherState} message SkySeacherState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SkySeacherState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SkySeacherState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SkySeacherState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SkySeacherState} SkySeacherState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SkySeacherState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SkySeacherState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SkySeacherState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SkySeacherState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SkySeacherState} SkySeacherState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SkySeacherState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SkySeacherState message.
     * @function verify
     * @memberof notify.SkySeacherState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SkySeacherState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates a SkySeacherState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SkySeacherState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SkySeacherState} SkySeacherState
     */
    SkySeacherState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SkySeacherState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SkySeacherState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SkySeacherState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a SkySeacherState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SkySeacherState
     * @static
     * @param {notify.SkySeacherState} message SkySeacherState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SkySeacherState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this SkySeacherState to JSON.
     * @function toJSON
     * @memberof notify.SkySeacherState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SkySeacherState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SkySeacherState
     * @function getTypeUrl
     * @memberof notify.SkySeacherState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SkySeacherState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SkySeacherState";
    };

    return SkySeacherState;
  })();

  notify.ProgressAiEnhance = (function () {
    /**
     * Properties of a ProgressAiEnhance.
     * @memberof notify
     * @interface IProgressAiEnhance
     * @property {number|null} [progress] ProgressAiEnhance progress
     * @property {number|null} [totalTime] ProgressAiEnhance totalTime
     */

    /**
     * Constructs a new ProgressAiEnhance.
     * @memberof notify
     * @classdesc Represents a ProgressAiEnhance.
     * @implements IProgressAiEnhance
     * @constructor
     * @param {notify.IProgressAiEnhance=} [properties] Properties to set
     */
    function ProgressAiEnhance(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProgressAiEnhance progress.
     * @member {number} progress
     * @memberof notify.ProgressAiEnhance
     * @instance
     */
    ProgressAiEnhance.prototype.progress = 0;

    /**
     * ProgressAiEnhance totalTime.
     * @member {number} totalTime
     * @memberof notify.ProgressAiEnhance
     * @instance
     */
    ProgressAiEnhance.prototype.totalTime = 0;

    /**
     * Creates a new ProgressAiEnhance instance using the specified properties.
     * @function create
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {notify.IProgressAiEnhance=} [properties] Properties to set
     * @returns {notify.ProgressAiEnhance} ProgressAiEnhance instance
     */
    ProgressAiEnhance.create = function create(properties) {
      return new ProgressAiEnhance(properties);
    };

    /**
     * Encodes the specified ProgressAiEnhance message. Does not implicitly {@link notify.ProgressAiEnhance.verify|verify} messages.
     * @function encode
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {notify.IProgressAiEnhance} message ProgressAiEnhance message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressAiEnhance.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.progress);
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.totalTime);
      return writer;
    };

    /**
     * Encodes the specified ProgressAiEnhance message, length delimited. Does not implicitly {@link notify.ProgressAiEnhance.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {notify.IProgressAiEnhance} message ProgressAiEnhance message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressAiEnhance.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ProgressAiEnhance message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ProgressAiEnhance} ProgressAiEnhance
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressAiEnhance.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ProgressAiEnhance();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.progress = reader.int32();
            break;
          }
          case 2: {
            message.totalTime = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ProgressAiEnhance message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ProgressAiEnhance} ProgressAiEnhance
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressAiEnhance.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProgressAiEnhance message.
     * @function verify
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProgressAiEnhance.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        if (!$util.isInteger(message.progress))
          return "progress: integer expected";
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        if (!$util.isInteger(message.totalTime))
          return "totalTime: integer expected";
      return null;
    };

    /**
     * Creates a ProgressAiEnhance message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ProgressAiEnhance} ProgressAiEnhance
     */
    ProgressAiEnhance.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ProgressAiEnhance) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ProgressAiEnhance: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ProgressAiEnhance();
      if (object.progress != null) message.progress = object.progress | 0;
      if (object.totalTime != null) message.totalTime = object.totalTime | 0;
      return message;
    };

    /**
     * Creates a plain object from a ProgressAiEnhance message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {notify.ProgressAiEnhance} message ProgressAiEnhance
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProgressAiEnhance.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.progress = 0;
        object.totalTime = 0;
      }
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        object.progress = message.progress;
      if (
        message.totalTime != null &&
        Object.hasOwnProperty.call(message, "totalTime")
      )
        object.totalTime = message.totalTime;
      return object;
    };

    /**
     * Converts this ProgressAiEnhance to JSON.
     * @function toJSON
     * @memberof notify.ProgressAiEnhance
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProgressAiEnhance.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProgressAiEnhance
     * @function getTypeUrl
     * @memberof notify.ProgressAiEnhance
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProgressAiEnhance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ProgressAiEnhance";
    };

    return ProgressAiEnhance;
  })();

  notify.CommonProgress = (function () {
    /**
     * Properties of a CommonProgress.
     * @memberof notify
     * @interface ICommonProgress
     * @property {number|null} [current] CommonProgress current
     * @property {number|null} [total] CommonProgress total
     * @property {notify.CommonProgress.ProgressType|null} [progressType] CommonProgress progressType
     */

    /**
     * Constructs a new CommonProgress.
     * @memberof notify
     * @classdesc Represents a CommonProgress.
     * @implements ICommonProgress
     * @constructor
     * @param {notify.ICommonProgress=} [properties] Properties to set
     */
    function CommonProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CommonProgress current.
     * @member {number} current
     * @memberof notify.CommonProgress
     * @instance
     */
    CommonProgress.prototype.current = 0;

    /**
     * CommonProgress total.
     * @member {number} total
     * @memberof notify.CommonProgress
     * @instance
     */
    CommonProgress.prototype.total = 0;

    /**
     * CommonProgress progressType.
     * @member {notify.CommonProgress.ProgressType} progressType
     * @memberof notify.CommonProgress
     * @instance
     */
    CommonProgress.prototype.progressType = 0;

    /**
     * Creates a new CommonProgress instance using the specified properties.
     * @function create
     * @memberof notify.CommonProgress
     * @static
     * @param {notify.ICommonProgress=} [properties] Properties to set
     * @returns {notify.CommonProgress} CommonProgress instance
     */
    CommonProgress.create = function create(properties) {
      return new CommonProgress(properties);
    };

    /**
     * Encodes the specified CommonProgress message. Does not implicitly {@link notify.CommonProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.CommonProgress
     * @static
     * @param {notify.ICommonProgress} message CommonProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.current != null &&
        Object.hasOwnProperty.call(message, "current")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.current);
      if (message.total != null && Object.hasOwnProperty.call(message, "total"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.total);
      if (
        message.progressType != null &&
        Object.hasOwnProperty.call(message, "progressType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.progressType);
      return writer;
    };

    /**
     * Encodes the specified CommonProgress message, length delimited. Does not implicitly {@link notify.CommonProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CommonProgress
     * @static
     * @param {notify.ICommonProgress} message CommonProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommonProgress.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CommonProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CommonProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CommonProgress} CommonProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonProgress.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CommonProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.current = reader.int32();
            break;
          }
          case 2: {
            message.total = reader.int32();
            break;
          }
          case 3: {
            message.progressType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CommonProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CommonProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CommonProgress} CommonProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommonProgress.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CommonProgress message.
     * @function verify
     * @memberof notify.CommonProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CommonProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.current != null &&
        Object.hasOwnProperty.call(message, "current")
      )
        if (!$util.isInteger(message.current))
          return "current: integer expected";
      if (message.total != null && Object.hasOwnProperty.call(message, "total"))
        if (!$util.isInteger(message.total)) return "total: integer expected";
      if (
        message.progressType != null &&
        Object.hasOwnProperty.call(message, "progressType")
      )
        switch (message.progressType) {
          default:
            return "progressType: enum value expected";
          case 0:
          case 1:
            break;
        }
      return null;
    };

    /**
     * Creates a CommonProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CommonProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CommonProgress} CommonProgress
     */
    CommonProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CommonProgress) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CommonProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CommonProgress();
      if (object.current != null) message.current = object.current | 0;
      if (object.total != null) message.total = object.total | 0;
      switch (object.progressType) {
        default:
          if (typeof object.progressType === "number") {
            message.progressType = object.progressType;
            break;
          }
          break;
        case "PROGRESS_TYPE_INITING":
        case 0:
          message.progressType = 0;
          break;
        case "PROGRESS_TYPE_MOSAIC_MOVING":
        case 1:
          message.progressType = 1;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a CommonProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CommonProgress
     * @static
     * @param {notify.CommonProgress} message CommonProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CommonProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.current = 0;
        object.total = 0;
        object.progressType =
          options.enums === String ? "PROGRESS_TYPE_INITING" : 0;
      }
      if (
        message.current != null &&
        Object.hasOwnProperty.call(message, "current")
      )
        object.current = message.current;
      if (message.total != null && Object.hasOwnProperty.call(message, "total"))
        object.total = message.total;
      if (
        message.progressType != null &&
        Object.hasOwnProperty.call(message, "progressType")
      )
        object.progressType =
          options.enums === String
            ? $root.notify.CommonProgress.ProgressType[message.progressType] ===
              undefined
              ? message.progressType
              : $root.notify.CommonProgress.ProgressType[message.progressType]
            : message.progressType;
      return object;
    };

    /**
     * Converts this CommonProgress to JSON.
     * @function toJSON
     * @memberof notify.CommonProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CommonProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CommonProgress
     * @function getTypeUrl
     * @memberof notify.CommonProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CommonProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CommonProgress";
    };

    /**
     * ProgressType enum.
     * @name notify.CommonProgress.ProgressType
     * @enum {number}
     * @property {number} PROGRESS_TYPE_INITING=0 PROGRESS_TYPE_INITING value
     * @property {number} PROGRESS_TYPE_MOSAIC_MOVING=1 PROGRESS_TYPE_MOSAIC_MOVING value
     */
    CommonProgress.ProgressType = (function () {
      var valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = "PROGRESS_TYPE_INITING")] = 0;
      values[(valuesById[1] = "PROGRESS_TYPE_MOSAIC_MOVING")] = 1;
      return values;
    })();

    return CommonProgress;
  })();

  notify.CalibrationResult = (function () {
    /**
     * Properties of a CalibrationResult.
     * @memberof notify
     * @interface ICalibrationResult
     * @property {number|null} [azi] CalibrationResult azi
     * @property {number|null} [alt] CalibrationResult alt
     */

    /**
     * Constructs a new CalibrationResult.
     * @memberof notify
     * @classdesc Represents a CalibrationResult.
     * @implements ICalibrationResult
     * @constructor
     * @param {notify.ICalibrationResult=} [properties] Properties to set
     */
    function CalibrationResult(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CalibrationResult azi.
     * @member {number} azi
     * @memberof notify.CalibrationResult
     * @instance
     */
    CalibrationResult.prototype.azi = 0;

    /**
     * CalibrationResult alt.
     * @member {number} alt
     * @memberof notify.CalibrationResult
     * @instance
     */
    CalibrationResult.prototype.alt = 0;

    /**
     * Creates a new CalibrationResult instance using the specified properties.
     * @function create
     * @memberof notify.CalibrationResult
     * @static
     * @param {notify.ICalibrationResult=} [properties] Properties to set
     * @returns {notify.CalibrationResult} CalibrationResult instance
     */
    CalibrationResult.create = function create(properties) {
      return new CalibrationResult(properties);
    };

    /**
     * Encodes the specified CalibrationResult message. Does not implicitly {@link notify.CalibrationResult.verify|verify} messages.
     * @function encode
     * @memberof notify.CalibrationResult
     * @static
     * @param {notify.ICalibrationResult} message CalibrationResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CalibrationResult.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
        writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.azi);
      if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
        writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.alt);
      return writer;
    };

    /**
     * Encodes the specified CalibrationResult message, length delimited. Does not implicitly {@link notify.CalibrationResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CalibrationResult
     * @static
     * @param {notify.ICalibrationResult} message CalibrationResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CalibrationResult.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CalibrationResult message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CalibrationResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CalibrationResult} CalibrationResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CalibrationResult.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CalibrationResult();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.azi = reader.double();
            break;
          }
          case 2: {
            message.alt = reader.double();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CalibrationResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CalibrationResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CalibrationResult} CalibrationResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CalibrationResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CalibrationResult message.
     * @function verify
     * @memberof notify.CalibrationResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CalibrationResult.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
        if (typeof message.azi !== "number") return "azi: number expected";
      if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
        if (typeof message.alt !== "number") return "alt: number expected";
      return null;
    };

    /**
     * Creates a CalibrationResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CalibrationResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CalibrationResult} CalibrationResult
     */
    CalibrationResult.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CalibrationResult) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CalibrationResult: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CalibrationResult();
      if (object.azi != null) message.azi = Number(object.azi);
      if (object.alt != null) message.alt = Number(object.alt);
      return message;
    };

    /**
     * Creates a plain object from a CalibrationResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CalibrationResult
     * @static
     * @param {notify.CalibrationResult} message CalibrationResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CalibrationResult.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.azi = 0;
        object.alt = 0;
      }
      if (message.azi != null && Object.hasOwnProperty.call(message, "azi"))
        object.azi =
          options.json && !isFinite(message.azi)
            ? String(message.azi)
            : message.azi;
      if (message.alt != null && Object.hasOwnProperty.call(message, "alt"))
        object.alt =
          options.json && !isFinite(message.alt)
            ? String(message.alt)
            : message.alt;
      return object;
    };

    /**
     * Converts this CalibrationResult to JSON.
     * @function toJSON
     * @memberof notify.CalibrationResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CalibrationResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CalibrationResult
     * @function getTypeUrl
     * @memberof notify.CalibrationResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CalibrationResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CalibrationResult";
    };

    return CalibrationResult;
  })();

  notify.FocusPosition = (function () {
    /**
     * Properties of a FocusPosition.
     * @memberof notify
     * @interface IFocusPosition
     * @property {number|null} [pos] FocusPosition pos
     */

    /**
     * Constructs a new FocusPosition.
     * @memberof notify
     * @classdesc Represents a FocusPosition.
     * @implements IFocusPosition
     * @constructor
     * @param {notify.IFocusPosition=} [properties] Properties to set
     */
    function FocusPosition(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * FocusPosition pos.
     * @member {number} pos
     * @memberof notify.FocusPosition
     * @instance
     */
    FocusPosition.prototype.pos = 0;

    /**
     * Creates a new FocusPosition instance using the specified properties.
     * @function create
     * @memberof notify.FocusPosition
     * @static
     * @param {notify.IFocusPosition=} [properties] Properties to set
     * @returns {notify.FocusPosition} FocusPosition instance
     */
    FocusPosition.create = function create(properties) {
      return new FocusPosition(properties);
    };

    /**
     * Encodes the specified FocusPosition message. Does not implicitly {@link notify.FocusPosition.verify|verify} messages.
     * @function encode
     * @memberof notify.FocusPosition
     * @static
     * @param {notify.IFocusPosition} message FocusPosition message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FocusPosition.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.pos);
      return writer;
    };

    /**
     * Encodes the specified FocusPosition message, length delimited. Does not implicitly {@link notify.FocusPosition.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.FocusPosition
     * @static
     * @param {notify.IFocusPosition} message FocusPosition message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FocusPosition.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a FocusPosition message from the specified reader or buffer.
     * @function decode
     * @memberof notify.FocusPosition
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.FocusPosition} FocusPosition
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FocusPosition.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.FocusPosition();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.pos = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a FocusPosition message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.FocusPosition
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.FocusPosition} FocusPosition
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FocusPosition.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FocusPosition message.
     * @function verify
     * @memberof notify.FocusPosition
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FocusPosition.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
        if (!$util.isInteger(message.pos)) return "pos: integer expected";
      return null;
    };

    /**
     * Creates a FocusPosition message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.FocusPosition
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.FocusPosition} FocusPosition
     */
    FocusPosition.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.FocusPosition) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.FocusPosition: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.FocusPosition();
      if (object.pos != null) message.pos = object.pos | 0;
      return message;
    };

    /**
     * Creates a plain object from a FocusPosition message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.FocusPosition
     * @static
     * @param {notify.FocusPosition} message FocusPosition
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FocusPosition.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.pos = 0;
      if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
        object.pos = message.pos;
      return object;
    };

    /**
     * Converts this FocusPosition to JSON.
     * @function toJSON
     * @memberof notify.FocusPosition
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FocusPosition.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FocusPosition
     * @function getTypeUrl
     * @memberof notify.FocusPosition
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FocusPosition.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.FocusPosition";
    };

    return FocusPosition;
  })();

  notify.RollLevelCalibrationAngle = (function () {
    /**
     * Properties of a RollLevelCalibrationAngle.
     * @memberof notify
     * @interface IRollLevelCalibrationAngle
     * @property {number|null} [offsetDegree] RollLevelCalibrationAngle offsetDegree
     */

    /**
     * Constructs a new RollLevelCalibrationAngle.
     * @memberof notify
     * @classdesc Represents a RollLevelCalibrationAngle.
     * @implements IRollLevelCalibrationAngle
     * @constructor
     * @param {notify.IRollLevelCalibrationAngle=} [properties] Properties to set
     */
    function RollLevelCalibrationAngle(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * RollLevelCalibrationAngle offsetDegree.
     * @member {number} offsetDegree
     * @memberof notify.RollLevelCalibrationAngle
     * @instance
     */
    RollLevelCalibrationAngle.prototype.offsetDegree = 0;

    /**
     * Creates a new RollLevelCalibrationAngle instance using the specified properties.
     * @function create
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {notify.IRollLevelCalibrationAngle=} [properties] Properties to set
     * @returns {notify.RollLevelCalibrationAngle} RollLevelCalibrationAngle instance
     */
    RollLevelCalibrationAngle.create = function create(properties) {
      return new RollLevelCalibrationAngle(properties);
    };

    /**
     * Encodes the specified RollLevelCalibrationAngle message. Does not implicitly {@link notify.RollLevelCalibrationAngle.verify|verify} messages.
     * @function encode
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {notify.IRollLevelCalibrationAngle} message RollLevelCalibrationAngle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RollLevelCalibrationAngle.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.offsetDegree != null &&
        Object.hasOwnProperty.call(message, "offsetDegree")
      )
        writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.offsetDegree);
      return writer;
    };

    /**
     * Encodes the specified RollLevelCalibrationAngle message, length delimited. Does not implicitly {@link notify.RollLevelCalibrationAngle.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {notify.IRollLevelCalibrationAngle} message RollLevelCalibrationAngle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RollLevelCalibrationAngle.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a RollLevelCalibrationAngle message from the specified reader or buffer.
     * @function decode
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.RollLevelCalibrationAngle} RollLevelCalibrationAngle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RollLevelCalibrationAngle.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.RollLevelCalibrationAngle();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.offsetDegree = reader.double();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a RollLevelCalibrationAngle message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.RollLevelCalibrationAngle} RollLevelCalibrationAngle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RollLevelCalibrationAngle.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RollLevelCalibrationAngle message.
     * @function verify
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RollLevelCalibrationAngle.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.offsetDegree != null &&
        Object.hasOwnProperty.call(message, "offsetDegree")
      )
        if (typeof message.offsetDegree !== "number")
          return "offsetDegree: number expected";
      return null;
    };

    /**
     * Creates a RollLevelCalibrationAngle message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.RollLevelCalibrationAngle} RollLevelCalibrationAngle
     */
    RollLevelCalibrationAngle.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.RollLevelCalibrationAngle)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.RollLevelCalibrationAngle: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.RollLevelCalibrationAngle();
      if (object.offsetDegree != null)
        message.offsetDegree = Number(object.offsetDegree);
      return message;
    };

    /**
     * Creates a plain object from a RollLevelCalibrationAngle message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {notify.RollLevelCalibrationAngle} message RollLevelCalibrationAngle
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RollLevelCalibrationAngle.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.offsetDegree = 0;
      if (
        message.offsetDegree != null &&
        Object.hasOwnProperty.call(message, "offsetDegree")
      )
        object.offsetDegree =
          options.json && !isFinite(message.offsetDegree)
            ? String(message.offsetDegree)
            : message.offsetDegree;
      return object;
    };

    /**
     * Converts this RollLevelCalibrationAngle to JSON.
     * @function toJSON
     * @memberof notify.RollLevelCalibrationAngle
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RollLevelCalibrationAngle.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RollLevelCalibrationAngle
     * @function getTypeUrl
     * @memberof notify.RollLevelCalibrationAngle
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RollLevelCalibrationAngle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.RollLevelCalibrationAngle";
    };

    return RollLevelCalibrationAngle;
  })();

  notify.SentryAutoHand = (function () {
    /**
     * Properties of a SentryAutoHand.
     * @memberof notify
     * @interface ISentryAutoHand
     * @property {number|null} [mode] SentryAutoHand mode
     */

    /**
     * Constructs a new SentryAutoHand.
     * @memberof notify
     * @classdesc Represents a SentryAutoHand.
     * @implements ISentryAutoHand
     * @constructor
     * @param {notify.ISentryAutoHand=} [properties] Properties to set
     */
    function SentryAutoHand(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SentryAutoHand mode.
     * @member {number} mode
     * @memberof notify.SentryAutoHand
     * @instance
     */
    SentryAutoHand.prototype.mode = 0;

    /**
     * Creates a new SentryAutoHand instance using the specified properties.
     * @function create
     * @memberof notify.SentryAutoHand
     * @static
     * @param {notify.ISentryAutoHand=} [properties] Properties to set
     * @returns {notify.SentryAutoHand} SentryAutoHand instance
     */
    SentryAutoHand.create = function create(properties) {
      return new SentryAutoHand(properties);
    };

    /**
     * Encodes the specified SentryAutoHand message. Does not implicitly {@link notify.SentryAutoHand.verify|verify} messages.
     * @function encode
     * @memberof notify.SentryAutoHand
     * @static
     * @param {notify.ISentryAutoHand} message SentryAutoHand message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SentryAutoHand.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
      return writer;
    };

    /**
     * Encodes the specified SentryAutoHand message, length delimited. Does not implicitly {@link notify.SentryAutoHand.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SentryAutoHand
     * @static
     * @param {notify.ISentryAutoHand} message SentryAutoHand message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SentryAutoHand.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SentryAutoHand message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SentryAutoHand
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SentryAutoHand} SentryAutoHand
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SentryAutoHand.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SentryAutoHand();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SentryAutoHand message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SentryAutoHand
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SentryAutoHand} SentryAutoHand
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SentryAutoHand.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SentryAutoHand message.
     * @function verify
     * @memberof notify.SentryAutoHand
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SentryAutoHand.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      return null;
    };

    /**
     * Creates a SentryAutoHand message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SentryAutoHand
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SentryAutoHand} SentryAutoHand
     */
    SentryAutoHand.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SentryAutoHand) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SentryAutoHand: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SentryAutoHand();
      if (object.mode != null) message.mode = object.mode | 0;
      return message;
    };

    /**
     * Creates a plain object from a SentryAutoHand message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SentryAutoHand
     * @static
     * @param {notify.SentryAutoHand} message SentryAutoHand
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SentryAutoHand.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.mode = 0;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      return object;
    };

    /**
     * Converts this SentryAutoHand to JSON.
     * @function toJSON
     * @memberof notify.SentryAutoHand
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SentryAutoHand.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SentryAutoHand
     * @function getTypeUrl
     * @memberof notify.SentryAutoHand
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SentryAutoHand.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SentryAutoHand";
    };

    return SentryAutoHand;
  })();

  notify.PanoramaStitchUploadComplete = (function () {
    /**
     * Properties of a PanoramaStitchUploadComplete.
     * @memberof notify
     * @interface IPanoramaStitchUploadComplete
     * @property {number|null} [code] PanoramaStitchUploadComplete code
     * @property {string|null} [userId] PanoramaStitchUploadComplete userId
     * @property {string|null} [busiNo] PanoramaStitchUploadComplete busiNo
     * @property {string|null} [panoramaName] PanoramaStitchUploadComplete panoramaName
     * @property {string|null} [mac] PanoramaStitchUploadComplete mac
     */

    /**
     * Constructs a new PanoramaStitchUploadComplete.
     * @memberof notify
     * @classdesc Represents a PanoramaStitchUploadComplete.
     * @implements IPanoramaStitchUploadComplete
     * @constructor
     * @param {notify.IPanoramaStitchUploadComplete=} [properties] Properties to set
     */
    function PanoramaStitchUploadComplete(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaStitchUploadComplete code.
     * @member {number} code
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     */
    PanoramaStitchUploadComplete.prototype.code = 0;

    /**
     * PanoramaStitchUploadComplete userId.
     * @member {string} userId
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     */
    PanoramaStitchUploadComplete.prototype.userId = "";

    /**
     * PanoramaStitchUploadComplete busiNo.
     * @member {string} busiNo
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     */
    PanoramaStitchUploadComplete.prototype.busiNo = "";

    /**
     * PanoramaStitchUploadComplete panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     */
    PanoramaStitchUploadComplete.prototype.panoramaName = "";

    /**
     * PanoramaStitchUploadComplete mac.
     * @member {string} mac
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     */
    PanoramaStitchUploadComplete.prototype.mac = "";

    /**
     * Creates a new PanoramaStitchUploadComplete instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {notify.IPanoramaStitchUploadComplete=} [properties] Properties to set
     * @returns {notify.PanoramaStitchUploadComplete} PanoramaStitchUploadComplete instance
     */
    PanoramaStitchUploadComplete.create = function create(properties) {
      return new PanoramaStitchUploadComplete(properties);
    };

    /**
     * Encodes the specified PanoramaStitchUploadComplete message. Does not implicitly {@link notify.PanoramaStitchUploadComplete.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {notify.IPanoramaStitchUploadComplete} message PanoramaStitchUploadComplete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaStitchUploadComplete.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.userId);
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.busiNo);
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.panoramaName);
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.mac);
      return writer;
    };

    /**
     * Encodes the specified PanoramaStitchUploadComplete message, length delimited. Does not implicitly {@link notify.PanoramaStitchUploadComplete.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {notify.IPanoramaStitchUploadComplete} message PanoramaStitchUploadComplete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaStitchUploadComplete.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaStitchUploadComplete message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaStitchUploadComplete} PanoramaStitchUploadComplete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaStitchUploadComplete.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaStitchUploadComplete();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.code = reader.int32();
            break;
          }
          case 2: {
            message.userId = reader.string();
            break;
          }
          case 3: {
            message.busiNo = reader.string();
            break;
          }
          case 4: {
            message.panoramaName = reader.string();
            break;
          }
          case 5: {
            message.mac = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaStitchUploadComplete message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaStitchUploadComplete} PanoramaStitchUploadComplete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaStitchUploadComplete.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaStitchUploadComplete message.
     * @function verify
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaStitchUploadComplete.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        if (!$util.isString(message.userId)) return "userId: string expected";
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        if (!$util.isString(message.busiNo)) return "busiNo: string expected";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        if (!$util.isString(message.mac)) return "mac: string expected";
      return null;
    };

    /**
     * Creates a PanoramaStitchUploadComplete message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaStitchUploadComplete} PanoramaStitchUploadComplete
     */
    PanoramaStitchUploadComplete.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.PanoramaStitchUploadComplete)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.PanoramaStitchUploadComplete: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaStitchUploadComplete();
      if (object.code != null) message.code = object.code | 0;
      if (object.userId != null) message.userId = String(object.userId);
      if (object.busiNo != null) message.busiNo = String(object.busiNo);
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.mac != null) message.mac = String(object.mac);
      return message;
    };

    /**
     * Creates a plain object from a PanoramaStitchUploadComplete message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {notify.PanoramaStitchUploadComplete} message PanoramaStitchUploadComplete
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaStitchUploadComplete.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.code = 0;
        object.userId = "";
        object.busiNo = "";
        object.panoramaName = "";
        object.mac = "";
      }
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        object.userId = message.userId;
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        object.busiNo = message.busiNo;
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        object.mac = message.mac;
      return object;
    };

    /**
     * Converts this PanoramaStitchUploadComplete to JSON.
     * @function toJSON
     * @memberof notify.PanoramaStitchUploadComplete
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaStitchUploadComplete.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaStitchUploadComplete
     * @function getTypeUrl
     * @memberof notify.PanoramaStitchUploadComplete
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaStitchUploadComplete.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaStitchUploadComplete";
    };

    return PanoramaStitchUploadComplete;
  })();

  notify.PanoramaCompressionComplete = (function () {
    /**
     * Properties of a PanoramaCompressionComplete.
     * @memberof notify
     * @interface IPanoramaCompressionComplete
     * @property {number|null} [code] PanoramaCompressionComplete code
     * @property {string|null} [panoramaName] PanoramaCompressionComplete panoramaName
     * @property {string|null} [zipFilePath] PanoramaCompressionComplete zipFilePath
     * @property {string|null} [zipFileMd5] PanoramaCompressionComplete zipFileMd5
     * @property {number|Long|null} [zipFileSize] PanoramaCompressionComplete zipFileSize
     * @property {string|null} [stitchParam] PanoramaCompressionComplete stitchParam
     */

    /**
     * Constructs a new PanoramaCompressionComplete.
     * @memberof notify
     * @classdesc Represents a PanoramaCompressionComplete.
     * @implements IPanoramaCompressionComplete
     * @constructor
     * @param {notify.IPanoramaCompressionComplete=} [properties] Properties to set
     */
    function PanoramaCompressionComplete(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaCompressionComplete code.
     * @member {number} code
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.code = 0;

    /**
     * PanoramaCompressionComplete panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.panoramaName = "";

    /**
     * PanoramaCompressionComplete zipFilePath.
     * @member {string} zipFilePath
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.zipFilePath = "";

    /**
     * PanoramaCompressionComplete zipFileMd5.
     * @member {string} zipFileMd5
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.zipFileMd5 = "";

    /**
     * PanoramaCompressionComplete zipFileSize.
     * @member {number|Long} zipFileSize
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.zipFileSize = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PanoramaCompressionComplete stitchParam.
     * @member {string} stitchParam
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     */
    PanoramaCompressionComplete.prototype.stitchParam = "";

    /**
     * Creates a new PanoramaCompressionComplete instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {notify.IPanoramaCompressionComplete=} [properties] Properties to set
     * @returns {notify.PanoramaCompressionComplete} PanoramaCompressionComplete instance
     */
    PanoramaCompressionComplete.create = function create(properties) {
      return new PanoramaCompressionComplete(properties);
    };

    /**
     * Encodes the specified PanoramaCompressionComplete message. Does not implicitly {@link notify.PanoramaCompressionComplete.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {notify.IPanoramaCompressionComplete} message PanoramaCompressionComplete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCompressionComplete.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.panoramaName);
      if (
        message.zipFilePath != null &&
        Object.hasOwnProperty.call(message, "zipFilePath")
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.zipFilePath);
      if (
        message.zipFileMd5 != null &&
        Object.hasOwnProperty.call(message, "zipFileMd5")
      )
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.zipFileMd5);
      if (
        message.zipFileSize != null &&
        Object.hasOwnProperty.call(message, "zipFileSize")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).uint64(message.zipFileSize);
      if (
        message.stitchParam != null &&
        Object.hasOwnProperty.call(message, "stitchParam")
      )
        writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.stitchParam);
      return writer;
    };

    /**
     * Encodes the specified PanoramaCompressionComplete message, length delimited. Does not implicitly {@link notify.PanoramaCompressionComplete.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {notify.IPanoramaCompressionComplete} message PanoramaCompressionComplete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCompressionComplete.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaCompressionComplete message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaCompressionComplete} PanoramaCompressionComplete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCompressionComplete.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaCompressionComplete();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.code = reader.int32();
            break;
          }
          case 2: {
            message.panoramaName = reader.string();
            break;
          }
          case 3: {
            message.zipFilePath = reader.string();
            break;
          }
          case 4: {
            message.zipFileMd5 = reader.string();
            break;
          }
          case 5: {
            message.zipFileSize = reader.uint64();
            break;
          }
          case 6: {
            message.stitchParam = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaCompressionComplete message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaCompressionComplete} PanoramaCompressionComplete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCompressionComplete.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaCompressionComplete message.
     * @function verify
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaCompressionComplete.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (
        message.zipFilePath != null &&
        Object.hasOwnProperty.call(message, "zipFilePath")
      )
        if (!$util.isString(message.zipFilePath))
          return "zipFilePath: string expected";
      if (
        message.zipFileMd5 != null &&
        Object.hasOwnProperty.call(message, "zipFileMd5")
      )
        if (!$util.isString(message.zipFileMd5))
          return "zipFileMd5: string expected";
      if (
        message.zipFileSize != null &&
        Object.hasOwnProperty.call(message, "zipFileSize")
      )
        if (
          !$util.isInteger(message.zipFileSize) &&
          !(
            message.zipFileSize &&
            $util.isInteger(message.zipFileSize.low) &&
            $util.isInteger(message.zipFileSize.high)
          )
        )
          return "zipFileSize: integer|Long expected";
      if (
        message.stitchParam != null &&
        Object.hasOwnProperty.call(message, "stitchParam")
      )
        if (!$util.isString(message.stitchParam))
          return "stitchParam: string expected";
      return null;
    };

    /**
     * Creates a PanoramaCompressionComplete message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaCompressionComplete} PanoramaCompressionComplete
     */
    PanoramaCompressionComplete.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaCompressionComplete)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaCompressionComplete: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaCompressionComplete();
      if (object.code != null) message.code = object.code | 0;
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.zipFilePath != null)
        message.zipFilePath = String(object.zipFilePath);
      if (object.zipFileMd5 != null)
        message.zipFileMd5 = String(object.zipFileMd5);
      if (object.zipFileSize != null)
        if ($util.Long)
          message.zipFileSize = $util.Long.fromValue(object.zipFileSize, true);
        else if (typeof object.zipFileSize === "string")
          message.zipFileSize = parseInt(object.zipFileSize, 10);
        else if (typeof object.zipFileSize === "number")
          message.zipFileSize = object.zipFileSize;
        else if (typeof object.zipFileSize === "object")
          message.zipFileSize = new $util.LongBits(
            object.zipFileSize.low >>> 0,
            object.zipFileSize.high >>> 0,
          ).toNumber(true);
      if (object.stitchParam != null)
        message.stitchParam = String(object.stitchParam);
      return message;
    };

    /**
     * Creates a plain object from a PanoramaCompressionComplete message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {notify.PanoramaCompressionComplete} message PanoramaCompressionComplete
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaCompressionComplete.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.code = 0;
        object.panoramaName = "";
        object.zipFilePath = "";
        object.zipFileMd5 = "";
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.zipFileSize =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.zipFileSize =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.stitchParam = "";
      }
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (
        message.zipFilePath != null &&
        Object.hasOwnProperty.call(message, "zipFilePath")
      )
        object.zipFilePath = message.zipFilePath;
      if (
        message.zipFileMd5 != null &&
        Object.hasOwnProperty.call(message, "zipFileMd5")
      )
        object.zipFileMd5 = message.zipFileMd5;
      if (
        message.zipFileSize != null &&
        Object.hasOwnProperty.call(message, "zipFileSize")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.zipFileSize =
            typeof message.zipFileSize === "number"
              ? BigInt(message.zipFileSize)
              : $util.Long.fromBits(
                  message.zipFileSize.low >>> 0,
                  message.zipFileSize.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.zipFileSize === "number")
          object.zipFileSize =
            options.longs === String
              ? String(message.zipFileSize)
              : message.zipFileSize;
        else
          object.zipFileSize =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.zipFileSize)
              : options.longs === Number
                ? new $util.LongBits(
                    message.zipFileSize.low >>> 0,
                    message.zipFileSize.high >>> 0,
                  ).toNumber(true)
                : message.zipFileSize;
      if (
        message.stitchParam != null &&
        Object.hasOwnProperty.call(message, "stitchParam")
      )
        object.stitchParam = message.stitchParam;
      return object;
    };

    /**
     * Converts this PanoramaCompressionComplete to JSON.
     * @function toJSON
     * @memberof notify.PanoramaCompressionComplete
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaCompressionComplete.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaCompressionComplete
     * @function getTypeUrl
     * @memberof notify.PanoramaCompressionComplete
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaCompressionComplete.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaCompressionComplete";
    };

    return PanoramaCompressionComplete;
  })();

  notify.PanoramaCompressionProgress = (function () {
    /**
     * Properties of a PanoramaCompressionProgress.
     * @memberof notify
     * @interface IPanoramaCompressionProgress
     * @property {string|null} [panoramaName] PanoramaCompressionProgress panoramaName
     * @property {number|null} [totalFilesNum] PanoramaCompressionProgress totalFilesNum
     * @property {number|null} [compressedFilesNum] PanoramaCompressionProgress compressedFilesNum
     */

    /**
     * Constructs a new PanoramaCompressionProgress.
     * @memberof notify
     * @classdesc Represents a PanoramaCompressionProgress.
     * @implements IPanoramaCompressionProgress
     * @constructor
     * @param {notify.IPanoramaCompressionProgress=} [properties] Properties to set
     */
    function PanoramaCompressionProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaCompressionProgress panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaCompressionProgress
     * @instance
     */
    PanoramaCompressionProgress.prototype.panoramaName = "";

    /**
     * PanoramaCompressionProgress totalFilesNum.
     * @member {number} totalFilesNum
     * @memberof notify.PanoramaCompressionProgress
     * @instance
     */
    PanoramaCompressionProgress.prototype.totalFilesNum = 0;

    /**
     * PanoramaCompressionProgress compressedFilesNum.
     * @member {number} compressedFilesNum
     * @memberof notify.PanoramaCompressionProgress
     * @instance
     */
    PanoramaCompressionProgress.prototype.compressedFilesNum = 0;

    /**
     * Creates a new PanoramaCompressionProgress instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {notify.IPanoramaCompressionProgress=} [properties] Properties to set
     * @returns {notify.PanoramaCompressionProgress} PanoramaCompressionProgress instance
     */
    PanoramaCompressionProgress.create = function create(properties) {
      return new PanoramaCompressionProgress(properties);
    };

    /**
     * Encodes the specified PanoramaCompressionProgress message. Does not implicitly {@link notify.PanoramaCompressionProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {notify.IPanoramaCompressionProgress} message PanoramaCompressionProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCompressionProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.panoramaName);
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.totalFilesNum);
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .uint32(message.compressedFilesNum);
      return writer;
    };

    /**
     * Encodes the specified PanoramaCompressionProgress message, length delimited. Does not implicitly {@link notify.PanoramaCompressionProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {notify.IPanoramaCompressionProgress} message PanoramaCompressionProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCompressionProgress.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaCompressionProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaCompressionProgress} PanoramaCompressionProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCompressionProgress.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaCompressionProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.panoramaName = reader.string();
            break;
          }
          case 2: {
            message.totalFilesNum = reader.uint32();
            break;
          }
          case 3: {
            message.compressedFilesNum = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaCompressionProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaCompressionProgress} PanoramaCompressionProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCompressionProgress.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaCompressionProgress message.
     * @function verify
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaCompressionProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        if (!$util.isInteger(message.totalFilesNum))
          return "totalFilesNum: integer expected";
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        if (!$util.isInteger(message.compressedFilesNum))
          return "compressedFilesNum: integer expected";
      return null;
    };

    /**
     * Creates a PanoramaCompressionProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaCompressionProgress} PanoramaCompressionProgress
     */
    PanoramaCompressionProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaCompressionProgress)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaCompressionProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaCompressionProgress();
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.totalFilesNum != null)
        message.totalFilesNum = object.totalFilesNum >>> 0;
      if (object.compressedFilesNum != null)
        message.compressedFilesNum = object.compressedFilesNum >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoramaCompressionProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {notify.PanoramaCompressionProgress} message PanoramaCompressionProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaCompressionProgress.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.panoramaName = "";
        object.totalFilesNum = 0;
        object.compressedFilesNum = 0;
      }
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        object.totalFilesNum = message.totalFilesNum;
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        object.compressedFilesNum = message.compressedFilesNum;
      return object;
    };

    /**
     * Converts this PanoramaCompressionProgress to JSON.
     * @function toJSON
     * @memberof notify.PanoramaCompressionProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaCompressionProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaCompressionProgress
     * @function getTypeUrl
     * @memberof notify.PanoramaCompressionProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaCompressionProgress.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaCompressionProgress";
    };

    return PanoramaCompressionProgress;
  })();

  notify.PanoramaUploadCompressionProgress = (function () {
    /**
     * Properties of a PanoramaUploadCompressionProgress.
     * @memberof notify
     * @interface IPanoramaUploadCompressionProgress
     * @property {string|null} [userId] PanoramaUploadCompressionProgress userId
     * @property {string|null} [busiNo] PanoramaUploadCompressionProgress busiNo
     * @property {string|null} [panoramaName] PanoramaUploadCompressionProgress panoramaName
     * @property {string|null} [mac] PanoramaUploadCompressionProgress mac
     * @property {number|null} [totalFilesNum] PanoramaUploadCompressionProgress totalFilesNum
     * @property {number|null} [compressedFilesNum] PanoramaUploadCompressionProgress compressedFilesNum
     * @property {number|null} [speed] PanoramaUploadCompressionProgress speed
     */

    /**
     * Constructs a new PanoramaUploadCompressionProgress.
     * @memberof notify
     * @classdesc Represents a PanoramaUploadCompressionProgress.
     * @implements IPanoramaUploadCompressionProgress
     * @constructor
     * @param {notify.IPanoramaUploadCompressionProgress=} [properties] Properties to set
     */
    function PanoramaUploadCompressionProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaUploadCompressionProgress userId.
     * @member {string} userId
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.userId = "";

    /**
     * PanoramaUploadCompressionProgress busiNo.
     * @member {string} busiNo
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.busiNo = "";

    /**
     * PanoramaUploadCompressionProgress panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.panoramaName = "";

    /**
     * PanoramaUploadCompressionProgress mac.
     * @member {string} mac
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.mac = "";

    /**
     * PanoramaUploadCompressionProgress totalFilesNum.
     * @member {number} totalFilesNum
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.totalFilesNum = 0;

    /**
     * PanoramaUploadCompressionProgress compressedFilesNum.
     * @member {number} compressedFilesNum
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.compressedFilesNum = 0;

    /**
     * PanoramaUploadCompressionProgress speed.
     * @member {number} speed
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     */
    PanoramaUploadCompressionProgress.prototype.speed = 0;

    /**
     * Creates a new PanoramaUploadCompressionProgress instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {notify.IPanoramaUploadCompressionProgress=} [properties] Properties to set
     * @returns {notify.PanoramaUploadCompressionProgress} PanoramaUploadCompressionProgress instance
     */
    PanoramaUploadCompressionProgress.create = function create(properties) {
      return new PanoramaUploadCompressionProgress(properties);
    };

    /**
     * Encodes the specified PanoramaUploadCompressionProgress message. Does not implicitly {@link notify.PanoramaUploadCompressionProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {notify.IPanoramaUploadCompressionProgress} message PanoramaUploadCompressionProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaUploadCompressionProgress.encode = function encode(
      message,
      writer,
      q,
    ) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.userId);
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.busiNo);
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.panoramaName);
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.mac);
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).uint32(message.totalFilesNum);
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        writer
          .uint32(/* id 6, wireType 0 =*/ 48)
          .uint32(message.compressedFilesNum);
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        writer.uint32(/* id 7, wireType 1 =*/ 57).double(message.speed);
      return writer;
    };

    /**
     * Encodes the specified PanoramaUploadCompressionProgress message, length delimited. Does not implicitly {@link notify.PanoramaUploadCompressionProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {notify.IPanoramaUploadCompressionProgress} message PanoramaUploadCompressionProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaUploadCompressionProgress.encodeDelimited =
      function encodeDelimited(message, writer) {
        return this.encode(
          message,
          writer && writer.len ? writer.fork() : writer,
        ).ldelim();
      };

    /**
     * Decodes a PanoramaUploadCompressionProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaUploadCompressionProgress} PanoramaUploadCompressionProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaUploadCompressionProgress.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaUploadCompressionProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.userId = reader.string();
            break;
          }
          case 2: {
            message.busiNo = reader.string();
            break;
          }
          case 3: {
            message.panoramaName = reader.string();
            break;
          }
          case 4: {
            message.mac = reader.string();
            break;
          }
          case 5: {
            message.totalFilesNum = reader.uint32();
            break;
          }
          case 6: {
            message.compressedFilesNum = reader.uint32();
            break;
          }
          case 7: {
            message.speed = reader.double();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaUploadCompressionProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaUploadCompressionProgress} PanoramaUploadCompressionProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaUploadCompressionProgress.decodeDelimited =
      function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

    /**
     * Verifies a PanoramaUploadCompressionProgress message.
     * @function verify
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaUploadCompressionProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        if (!$util.isString(message.userId)) return "userId: string expected";
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        if (!$util.isString(message.busiNo)) return "busiNo: string expected";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        if (!$util.isString(message.mac)) return "mac: string expected";
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        if (!$util.isInteger(message.totalFilesNum))
          return "totalFilesNum: integer expected";
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        if (!$util.isInteger(message.compressedFilesNum))
          return "compressedFilesNum: integer expected";
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        if (typeof message.speed !== "number") return "speed: number expected";
      return null;
    };

    /**
     * Creates a PanoramaUploadCompressionProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaUploadCompressionProgress} PanoramaUploadCompressionProgress
     */
    PanoramaUploadCompressionProgress.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.PanoramaUploadCompressionProgress)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.PanoramaUploadCompressionProgress: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaUploadCompressionProgress();
      if (object.userId != null) message.userId = String(object.userId);
      if (object.busiNo != null) message.busiNo = String(object.busiNo);
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.mac != null) message.mac = String(object.mac);
      if (object.totalFilesNum != null)
        message.totalFilesNum = object.totalFilesNum >>> 0;
      if (object.compressedFilesNum != null)
        message.compressedFilesNum = object.compressedFilesNum >>> 0;
      if (object.speed != null) message.speed = Number(object.speed);
      return message;
    };

    /**
     * Creates a plain object from a PanoramaUploadCompressionProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {notify.PanoramaUploadCompressionProgress} message PanoramaUploadCompressionProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaUploadCompressionProgress.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.userId = "";
        object.busiNo = "";
        object.panoramaName = "";
        object.mac = "";
        object.totalFilesNum = 0;
        object.compressedFilesNum = 0;
        object.speed = 0;
      }
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        object.userId = message.userId;
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        object.busiNo = message.busiNo;
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        object.mac = message.mac;
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        object.totalFilesNum = message.totalFilesNum;
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        object.compressedFilesNum = message.compressedFilesNum;
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        object.speed =
          options.json && !isFinite(message.speed)
            ? String(message.speed)
            : message.speed;
      return object;
    };

    /**
     * Converts this PanoramaUploadCompressionProgress to JSON.
     * @function toJSON
     * @memberof notify.PanoramaUploadCompressionProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaUploadCompressionProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaUploadCompressionProgress
     * @function getTypeUrl
     * @memberof notify.PanoramaUploadCompressionProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaUploadCompressionProgress.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaUploadCompressionProgress";
    };

    return PanoramaUploadCompressionProgress;
  })();

  notify.PanoramaUploadProgress = (function () {
    /**
     * Properties of a PanoramaUploadProgress.
     * @memberof notify
     * @interface IPanoramaUploadProgress
     * @property {string|null} [userId] PanoramaUploadProgress userId
     * @property {string|null} [busiNo] PanoramaUploadProgress busiNo
     * @property {string|null} [panoramaName] PanoramaUploadProgress panoramaName
     * @property {string|null} [mac] PanoramaUploadProgress mac
     * @property {number|Long|null} [totalSize] PanoramaUploadProgress totalSize
     * @property {number|Long|null} [uploadedSize] PanoramaUploadProgress uploadedSize
     * @property {number|null} [speed] PanoramaUploadProgress speed
     */

    /**
     * Constructs a new PanoramaUploadProgress.
     * @memberof notify
     * @classdesc Represents a PanoramaUploadProgress.
     * @implements IPanoramaUploadProgress
     * @constructor
     * @param {notify.IPanoramaUploadProgress=} [properties] Properties to set
     */
    function PanoramaUploadProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaUploadProgress userId.
     * @member {string} userId
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.userId = "";

    /**
     * PanoramaUploadProgress busiNo.
     * @member {string} busiNo
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.busiNo = "";

    /**
     * PanoramaUploadProgress panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.panoramaName = "";

    /**
     * PanoramaUploadProgress mac.
     * @member {string} mac
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.mac = "";

    /**
     * PanoramaUploadProgress totalSize.
     * @member {number|Long} totalSize
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.totalSize = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PanoramaUploadProgress uploadedSize.
     * @member {number|Long} uploadedSize
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.uploadedSize = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PanoramaUploadProgress speed.
     * @member {number} speed
     * @memberof notify.PanoramaUploadProgress
     * @instance
     */
    PanoramaUploadProgress.prototype.speed = 0;

    /**
     * Creates a new PanoramaUploadProgress instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {notify.IPanoramaUploadProgress=} [properties] Properties to set
     * @returns {notify.PanoramaUploadProgress} PanoramaUploadProgress instance
     */
    PanoramaUploadProgress.create = function create(properties) {
      return new PanoramaUploadProgress(properties);
    };

    /**
     * Encodes the specified PanoramaUploadProgress message. Does not implicitly {@link notify.PanoramaUploadProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {notify.IPanoramaUploadProgress} message PanoramaUploadProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaUploadProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.userId);
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.busiNo);
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.panoramaName);
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.mac);
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).uint64(message.totalSize);
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        writer.uint32(/* id 6, wireType 0 =*/ 48).uint64(message.uploadedSize);
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        writer.uint32(/* id 7, wireType 1 =*/ 57).double(message.speed);
      return writer;
    };

    /**
     * Encodes the specified PanoramaUploadProgress message, length delimited. Does not implicitly {@link notify.PanoramaUploadProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {notify.IPanoramaUploadProgress} message PanoramaUploadProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaUploadProgress.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaUploadProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaUploadProgress} PanoramaUploadProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaUploadProgress.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaUploadProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.userId = reader.string();
            break;
          }
          case 2: {
            message.busiNo = reader.string();
            break;
          }
          case 3: {
            message.panoramaName = reader.string();
            break;
          }
          case 4: {
            message.mac = reader.string();
            break;
          }
          case 5: {
            message.totalSize = reader.uint64();
            break;
          }
          case 6: {
            message.uploadedSize = reader.uint64();
            break;
          }
          case 7: {
            message.speed = reader.double();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaUploadProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaUploadProgress} PanoramaUploadProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaUploadProgress.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaUploadProgress message.
     * @function verify
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaUploadProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        if (!$util.isString(message.userId)) return "userId: string expected";
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        if (!$util.isString(message.busiNo)) return "busiNo: string expected";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        if (!$util.isString(message.mac)) return "mac: string expected";
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        if (
          !$util.isInteger(message.totalSize) &&
          !(
            message.totalSize &&
            $util.isInteger(message.totalSize.low) &&
            $util.isInteger(message.totalSize.high)
          )
        )
          return "totalSize: integer|Long expected";
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        if (
          !$util.isInteger(message.uploadedSize) &&
          !(
            message.uploadedSize &&
            $util.isInteger(message.uploadedSize.low) &&
            $util.isInteger(message.uploadedSize.high)
          )
        )
          return "uploadedSize: integer|Long expected";
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        if (typeof message.speed !== "number") return "speed: number expected";
      return null;
    };

    /**
     * Creates a PanoramaUploadProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaUploadProgress} PanoramaUploadProgress
     */
    PanoramaUploadProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaUploadProgress) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaUploadProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaUploadProgress();
      if (object.userId != null) message.userId = String(object.userId);
      if (object.busiNo != null) message.busiNo = String(object.busiNo);
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.mac != null) message.mac = String(object.mac);
      if (object.totalSize != null)
        if ($util.Long)
          message.totalSize = $util.Long.fromValue(object.totalSize, true);
        else if (typeof object.totalSize === "string")
          message.totalSize = parseInt(object.totalSize, 10);
        else if (typeof object.totalSize === "number")
          message.totalSize = object.totalSize;
        else if (typeof object.totalSize === "object")
          message.totalSize = new $util.LongBits(
            object.totalSize.low >>> 0,
            object.totalSize.high >>> 0,
          ).toNumber(true);
      if (object.uploadedSize != null)
        if ($util.Long)
          message.uploadedSize = $util.Long.fromValue(
            object.uploadedSize,
            true,
          );
        else if (typeof object.uploadedSize === "string")
          message.uploadedSize = parseInt(object.uploadedSize, 10);
        else if (typeof object.uploadedSize === "number")
          message.uploadedSize = object.uploadedSize;
        else if (typeof object.uploadedSize === "object")
          message.uploadedSize = new $util.LongBits(
            object.uploadedSize.low >>> 0,
            object.uploadedSize.high >>> 0,
          ).toNumber(true);
      if (object.speed != null) message.speed = Number(object.speed);
      return message;
    };

    /**
     * Creates a plain object from a PanoramaUploadProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {notify.PanoramaUploadProgress} message PanoramaUploadProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaUploadProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.userId = "";
        object.busiNo = "";
        object.panoramaName = "";
        object.mac = "";
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.totalSize =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.totalSize =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.uploadedSize =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.uploadedSize =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.speed = 0;
      }
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        object.userId = message.userId;
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        object.busiNo = message.busiNo;
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        object.mac = message.mac;
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.totalSize =
            typeof message.totalSize === "number"
              ? BigInt(message.totalSize)
              : $util.Long.fromBits(
                  message.totalSize.low >>> 0,
                  message.totalSize.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.totalSize === "number")
          object.totalSize =
            options.longs === String
              ? String(message.totalSize)
              : message.totalSize;
        else
          object.totalSize =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.totalSize)
              : options.longs === Number
                ? new $util.LongBits(
                    message.totalSize.low >>> 0,
                    message.totalSize.high >>> 0,
                  ).toNumber(true)
                : message.totalSize;
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.uploadedSize =
            typeof message.uploadedSize === "number"
              ? BigInt(message.uploadedSize)
              : $util.Long.fromBits(
                  message.uploadedSize.low >>> 0,
                  message.uploadedSize.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.uploadedSize === "number")
          object.uploadedSize =
            options.longs === String
              ? String(message.uploadedSize)
              : message.uploadedSize;
        else
          object.uploadedSize =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.uploadedSize)
              : options.longs === Number
                ? new $util.LongBits(
                    message.uploadedSize.low >>> 0,
                    message.uploadedSize.high >>> 0,
                  ).toNumber(true)
                : message.uploadedSize;
      if (message.speed != null && Object.hasOwnProperty.call(message, "speed"))
        object.speed =
          options.json && !isFinite(message.speed)
            ? String(message.speed)
            : message.speed;
      return object;
    };

    /**
     * Converts this PanoramaUploadProgress to JSON.
     * @function toJSON
     * @memberof notify.PanoramaUploadProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaUploadProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaUploadProgress
     * @function getTypeUrl
     * @memberof notify.PanoramaUploadProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaUploadProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaUploadProgress";
    };

    return PanoramaUploadProgress;
  })();

  notify.PanoramaCurrentUploadState = (function () {
    /**
     * Properties of a PanoramaCurrentUploadState.
     * @memberof notify
     * @interface IPanoramaCurrentUploadState
     * @property {number|null} [code] PanoramaCurrentUploadState code
     * @property {string|null} [userId] PanoramaCurrentUploadState userId
     * @property {string|null} [busiNo] PanoramaCurrentUploadState busiNo
     * @property {string|null} [panoramaName] PanoramaCurrentUploadState panoramaName
     * @property {string|null} [mac] PanoramaCurrentUploadState mac
     * @property {number|null} [totalFilesNum] PanoramaCurrentUploadState totalFilesNum
     * @property {number|null} [compressedFilesNum] PanoramaCurrentUploadState compressedFilesNum
     * @property {number|Long|null} [totalSize] PanoramaCurrentUploadState totalSize
     * @property {number|Long|null} [uploadedSize] PanoramaCurrentUploadState uploadedSize
     * @property {number|null} [step] PanoramaCurrentUploadState step
     */

    /**
     * Constructs a new PanoramaCurrentUploadState.
     * @memberof notify
     * @classdesc Represents a PanoramaCurrentUploadState.
     * @implements IPanoramaCurrentUploadState
     * @constructor
     * @param {notify.IPanoramaCurrentUploadState=} [properties] Properties to set
     */
    function PanoramaCurrentUploadState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaCurrentUploadState code.
     * @member {number} code
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.code = 0;

    /**
     * PanoramaCurrentUploadState userId.
     * @member {string} userId
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.userId = "";

    /**
     * PanoramaCurrentUploadState busiNo.
     * @member {string} busiNo
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.busiNo = "";

    /**
     * PanoramaCurrentUploadState panoramaName.
     * @member {string} panoramaName
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.panoramaName = "";

    /**
     * PanoramaCurrentUploadState mac.
     * @member {string} mac
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.mac = "";

    /**
     * PanoramaCurrentUploadState totalFilesNum.
     * @member {number} totalFilesNum
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.totalFilesNum = 0;

    /**
     * PanoramaCurrentUploadState compressedFilesNum.
     * @member {number} compressedFilesNum
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.compressedFilesNum = 0;

    /**
     * PanoramaCurrentUploadState totalSize.
     * @member {number|Long} totalSize
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.totalSize = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PanoramaCurrentUploadState uploadedSize.
     * @member {number|Long} uploadedSize
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.uploadedSize = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PanoramaCurrentUploadState step.
     * @member {number} step
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     */
    PanoramaCurrentUploadState.prototype.step = 0;

    /**
     * Creates a new PanoramaCurrentUploadState instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {notify.IPanoramaCurrentUploadState=} [properties] Properties to set
     * @returns {notify.PanoramaCurrentUploadState} PanoramaCurrentUploadState instance
     */
    PanoramaCurrentUploadState.create = function create(properties) {
      return new PanoramaCurrentUploadState(properties);
    };

    /**
     * Encodes the specified PanoramaCurrentUploadState message. Does not implicitly {@link notify.PanoramaCurrentUploadState.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {notify.IPanoramaCurrentUploadState} message PanoramaCurrentUploadState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCurrentUploadState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.userId);
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.busiNo);
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.panoramaName);
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.mac);
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        writer.uint32(/* id 6, wireType 0 =*/ 48).uint32(message.totalFilesNum);
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        writer
          .uint32(/* id 7, wireType 0 =*/ 56)
          .uint32(message.compressedFilesNum);
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        writer.uint32(/* id 8, wireType 0 =*/ 64).uint64(message.totalSize);
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        writer.uint32(/* id 9, wireType 0 =*/ 72).uint64(message.uploadedSize);
      if (message.step != null && Object.hasOwnProperty.call(message, "step"))
        writer.uint32(/* id 10, wireType 0 =*/ 80).uint32(message.step);
      return writer;
    };

    /**
     * Encodes the specified PanoramaCurrentUploadState message, length delimited. Does not implicitly {@link notify.PanoramaCurrentUploadState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {notify.IPanoramaCurrentUploadState} message PanoramaCurrentUploadState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaCurrentUploadState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaCurrentUploadState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaCurrentUploadState} PanoramaCurrentUploadState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCurrentUploadState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaCurrentUploadState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.code = reader.int32();
            break;
          }
          case 2: {
            message.userId = reader.string();
            break;
          }
          case 3: {
            message.busiNo = reader.string();
            break;
          }
          case 4: {
            message.panoramaName = reader.string();
            break;
          }
          case 5: {
            message.mac = reader.string();
            break;
          }
          case 6: {
            message.totalFilesNum = reader.uint32();
            break;
          }
          case 7: {
            message.compressedFilesNum = reader.uint32();
            break;
          }
          case 8: {
            message.totalSize = reader.uint64();
            break;
          }
          case 9: {
            message.uploadedSize = reader.uint64();
            break;
          }
          case 10: {
            message.step = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaCurrentUploadState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaCurrentUploadState} PanoramaCurrentUploadState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaCurrentUploadState.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaCurrentUploadState message.
     * @function verify
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaCurrentUploadState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        if (!$util.isString(message.userId)) return "userId: string expected";
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        if (!$util.isString(message.busiNo)) return "busiNo: string expected";
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        if (!$util.isString(message.panoramaName))
          return "panoramaName: string expected";
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        if (!$util.isString(message.mac)) return "mac: string expected";
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        if (!$util.isInteger(message.totalFilesNum))
          return "totalFilesNum: integer expected";
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        if (!$util.isInteger(message.compressedFilesNum))
          return "compressedFilesNum: integer expected";
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        if (
          !$util.isInteger(message.totalSize) &&
          !(
            message.totalSize &&
            $util.isInteger(message.totalSize.low) &&
            $util.isInteger(message.totalSize.high)
          )
        )
          return "totalSize: integer|Long expected";
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        if (
          !$util.isInteger(message.uploadedSize) &&
          !(
            message.uploadedSize &&
            $util.isInteger(message.uploadedSize.low) &&
            $util.isInteger(message.uploadedSize.high)
          )
        )
          return "uploadedSize: integer|Long expected";
      if (message.step != null && Object.hasOwnProperty.call(message, "step"))
        if (!$util.isInteger(message.step)) return "step: integer expected";
      return null;
    };

    /**
     * Creates a PanoramaCurrentUploadState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaCurrentUploadState} PanoramaCurrentUploadState
     */
    PanoramaCurrentUploadState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaCurrentUploadState)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaCurrentUploadState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaCurrentUploadState();
      if (object.code != null) message.code = object.code | 0;
      if (object.userId != null) message.userId = String(object.userId);
      if (object.busiNo != null) message.busiNo = String(object.busiNo);
      if (object.panoramaName != null)
        message.panoramaName = String(object.panoramaName);
      if (object.mac != null) message.mac = String(object.mac);
      if (object.totalFilesNum != null)
        message.totalFilesNum = object.totalFilesNum >>> 0;
      if (object.compressedFilesNum != null)
        message.compressedFilesNum = object.compressedFilesNum >>> 0;
      if (object.totalSize != null)
        if ($util.Long)
          message.totalSize = $util.Long.fromValue(object.totalSize, true);
        else if (typeof object.totalSize === "string")
          message.totalSize = parseInt(object.totalSize, 10);
        else if (typeof object.totalSize === "number")
          message.totalSize = object.totalSize;
        else if (typeof object.totalSize === "object")
          message.totalSize = new $util.LongBits(
            object.totalSize.low >>> 0,
            object.totalSize.high >>> 0,
          ).toNumber(true);
      if (object.uploadedSize != null)
        if ($util.Long)
          message.uploadedSize = $util.Long.fromValue(
            object.uploadedSize,
            true,
          );
        else if (typeof object.uploadedSize === "string")
          message.uploadedSize = parseInt(object.uploadedSize, 10);
        else if (typeof object.uploadedSize === "number")
          message.uploadedSize = object.uploadedSize;
        else if (typeof object.uploadedSize === "object")
          message.uploadedSize = new $util.LongBits(
            object.uploadedSize.low >>> 0,
            object.uploadedSize.high >>> 0,
          ).toNumber(true);
      if (object.step != null) message.step = object.step >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoramaCurrentUploadState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {notify.PanoramaCurrentUploadState} message PanoramaCurrentUploadState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaCurrentUploadState.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.code = 0;
        object.userId = "";
        object.busiNo = "";
        object.panoramaName = "";
        object.mac = "";
        object.totalFilesNum = 0;
        object.compressedFilesNum = 0;
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.totalSize =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.totalSize =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.uploadedSize =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.uploadedSize =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.step = 0;
      }
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      if (
        message.userId != null &&
        Object.hasOwnProperty.call(message, "userId")
      )
        object.userId = message.userId;
      if (
        message.busiNo != null &&
        Object.hasOwnProperty.call(message, "busiNo")
      )
        object.busiNo = message.busiNo;
      if (
        message.panoramaName != null &&
        Object.hasOwnProperty.call(message, "panoramaName")
      )
        object.panoramaName = message.panoramaName;
      if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
        object.mac = message.mac;
      if (
        message.totalFilesNum != null &&
        Object.hasOwnProperty.call(message, "totalFilesNum")
      )
        object.totalFilesNum = message.totalFilesNum;
      if (
        message.compressedFilesNum != null &&
        Object.hasOwnProperty.call(message, "compressedFilesNum")
      )
        object.compressedFilesNum = message.compressedFilesNum;
      if (
        message.totalSize != null &&
        Object.hasOwnProperty.call(message, "totalSize")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.totalSize =
            typeof message.totalSize === "number"
              ? BigInt(message.totalSize)
              : $util.Long.fromBits(
                  message.totalSize.low >>> 0,
                  message.totalSize.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.totalSize === "number")
          object.totalSize =
            options.longs === String
              ? String(message.totalSize)
              : message.totalSize;
        else
          object.totalSize =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.totalSize)
              : options.longs === Number
                ? new $util.LongBits(
                    message.totalSize.low >>> 0,
                    message.totalSize.high >>> 0,
                  ).toNumber(true)
                : message.totalSize;
      if (
        message.uploadedSize != null &&
        Object.hasOwnProperty.call(message, "uploadedSize")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.uploadedSize =
            typeof message.uploadedSize === "number"
              ? BigInt(message.uploadedSize)
              : $util.Long.fromBits(
                  message.uploadedSize.low >>> 0,
                  message.uploadedSize.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.uploadedSize === "number")
          object.uploadedSize =
            options.longs === String
              ? String(message.uploadedSize)
              : message.uploadedSize;
        else
          object.uploadedSize =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.uploadedSize)
              : options.longs === Number
                ? new $util.LongBits(
                    message.uploadedSize.low >>> 0,
                    message.uploadedSize.high >>> 0,
                  ).toNumber(true)
                : message.uploadedSize;
      if (message.step != null && Object.hasOwnProperty.call(message, "step"))
        object.step = message.step;
      return object;
    };

    /**
     * Converts this PanoramaCurrentUploadState to JSON.
     * @function toJSON
     * @memberof notify.PanoramaCurrentUploadState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaCurrentUploadState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaCurrentUploadState
     * @function getTypeUrl
     * @memberof notify.PanoramaCurrentUploadState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaCurrentUploadState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaCurrentUploadState";
    };

    return PanoramaCurrentUploadState;
  })();

  notify.LowTempProtectionMode = (function () {
    /**
     * Properties of a LowTempProtectionMode.
     * @memberof notify
     * @interface ILowTempProtectionMode
     * @property {number|null} [mode] LowTempProtectionMode mode
     */

    /**
     * Constructs a new LowTempProtectionMode.
     * @memberof notify
     * @classdesc Represents a LowTempProtectionMode.
     * @implements ILowTempProtectionMode
     * @constructor
     * @param {notify.ILowTempProtectionMode=} [properties] Properties to set
     */
    function LowTempProtectionMode(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * LowTempProtectionMode mode.
     * @member {number} mode
     * @memberof notify.LowTempProtectionMode
     * @instance
     */
    LowTempProtectionMode.prototype.mode = 0;

    /**
     * Creates a new LowTempProtectionMode instance using the specified properties.
     * @function create
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {notify.ILowTempProtectionMode=} [properties] Properties to set
     * @returns {notify.LowTempProtectionMode} LowTempProtectionMode instance
     */
    LowTempProtectionMode.create = function create(properties) {
      return new LowTempProtectionMode(properties);
    };

    /**
     * Encodes the specified LowTempProtectionMode message. Does not implicitly {@link notify.LowTempProtectionMode.verify|verify} messages.
     * @function encode
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {notify.ILowTempProtectionMode} message LowTempProtectionMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LowTempProtectionMode.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
      return writer;
    };

    /**
     * Encodes the specified LowTempProtectionMode message, length delimited. Does not implicitly {@link notify.LowTempProtectionMode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {notify.ILowTempProtectionMode} message LowTempProtectionMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LowTempProtectionMode.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a LowTempProtectionMode message from the specified reader or buffer.
     * @function decode
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.LowTempProtectionMode} LowTempProtectionMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LowTempProtectionMode.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.LowTempProtectionMode();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a LowTempProtectionMode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.LowTempProtectionMode} LowTempProtectionMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LowTempProtectionMode.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LowTempProtectionMode message.
     * @function verify
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LowTempProtectionMode.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      return null;
    };

    /**
     * Creates a LowTempProtectionMode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.LowTempProtectionMode} LowTempProtectionMode
     */
    LowTempProtectionMode.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.LowTempProtectionMode) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.LowTempProtectionMode: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.LowTempProtectionMode();
      if (object.mode != null) message.mode = object.mode | 0;
      return message;
    };

    /**
     * Creates a plain object from a LowTempProtectionMode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {notify.LowTempProtectionMode} message LowTempProtectionMode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LowTempProtectionMode.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.mode = 0;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      return object;
    };

    /**
     * Converts this LowTempProtectionMode to JSON.
     * @function toJSON
     * @memberof notify.LowTempProtectionMode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LowTempProtectionMode.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LowTempProtectionMode
     * @function getTypeUrl
     * @memberof notify.LowTempProtectionMode
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LowTempProtectionMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.LowTempProtectionMode";
    };

    return LowTempProtectionMode;
  })();

  notify.StateSystemResourceOccupation = (function () {
    /**
     * Properties of a StateSystemResourceOccupation.
     * @memberof notify
     * @interface IStateSystemResourceOccupation
     * @property {notify.StateSystemResourceOccupation.TaskId|null} [taskId] StateSystemResourceOccupation taskId
     * @property {notify.OperationState|null} [state] StateSystemResourceOccupation state
     */

    /**
     * Constructs a new StateSystemResourceOccupation.
     * @memberof notify
     * @classdesc Represents a StateSystemResourceOccupation.
     * @implements IStateSystemResourceOccupation
     * @constructor
     * @param {notify.IStateSystemResourceOccupation=} [properties] Properties to set
     */
    function StateSystemResourceOccupation(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * StateSystemResourceOccupation taskId.
     * @member {notify.StateSystemResourceOccupation.TaskId} taskId
     * @memberof notify.StateSystemResourceOccupation
     * @instance
     */
    StateSystemResourceOccupation.prototype.taskId = 0;

    /**
     * StateSystemResourceOccupation state.
     * @member {notify.OperationState} state
     * @memberof notify.StateSystemResourceOccupation
     * @instance
     */
    StateSystemResourceOccupation.prototype.state = 0;

    /**
     * Creates a new StateSystemResourceOccupation instance using the specified properties.
     * @function create
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {notify.IStateSystemResourceOccupation=} [properties] Properties to set
     * @returns {notify.StateSystemResourceOccupation} StateSystemResourceOccupation instance
     */
    StateSystemResourceOccupation.create = function create(properties) {
      return new StateSystemResourceOccupation(properties);
    };

    /**
     * Encodes the specified StateSystemResourceOccupation message. Does not implicitly {@link notify.StateSystemResourceOccupation.verify|verify} messages.
     * @function encode
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {notify.IStateSystemResourceOccupation} message StateSystemResourceOccupation message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StateSystemResourceOccupation.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.taskId != null &&
        Object.hasOwnProperty.call(message, "taskId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.taskId);
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified StateSystemResourceOccupation message, length delimited. Does not implicitly {@link notify.StateSystemResourceOccupation.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {notify.IStateSystemResourceOccupation} message StateSystemResourceOccupation message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StateSystemResourceOccupation.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a StateSystemResourceOccupation message from the specified reader or buffer.
     * @function decode
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.StateSystemResourceOccupation} StateSystemResourceOccupation
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StateSystemResourceOccupation.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.StateSystemResourceOccupation();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.taskId = reader.int32();
            break;
          }
          case 2: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a StateSystemResourceOccupation message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.StateSystemResourceOccupation} StateSystemResourceOccupation
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StateSystemResourceOccupation.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StateSystemResourceOccupation message.
     * @function verify
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StateSystemResourceOccupation.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.taskId != null &&
        Object.hasOwnProperty.call(message, "taskId")
      )
        switch (message.taskId) {
          default:
            return "taskId: enum value expected";
          case 0:
          case 1:
          case 2:
            break;
        }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates a StateSystemResourceOccupation message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.StateSystemResourceOccupation} StateSystemResourceOccupation
     */
    StateSystemResourceOccupation.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.StateSystemResourceOccupation)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.StateSystemResourceOccupation: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.StateSystemResourceOccupation();
      switch (object.taskId) {
        default:
          if (typeof object.taskId === "number") {
            message.taskId = object.taskId;
            break;
          }
          break;
        case "IDLE":
        case 0:
          message.taskId = 0;
          break;
        case "PANORAMA_UPLOAD":
        case 1:
          message.taskId = 1;
          break;
        case "ASTRO_MULTI_STACK":
        case 2:
          message.taskId = 2;
          break;
      }
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a StateSystemResourceOccupation message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {notify.StateSystemResourceOccupation} message StateSystemResourceOccupation
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StateSystemResourceOccupation.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.taskId = options.enums === String ? "IDLE" : 0;
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      }
      if (
        message.taskId != null &&
        Object.hasOwnProperty.call(message, "taskId")
      )
        object.taskId =
          options.enums === String
            ? $root.notify.StateSystemResourceOccupation.TaskId[
                message.taskId
              ] === undefined
              ? message.taskId
              : $root.notify.StateSystemResourceOccupation.TaskId[
                  message.taskId
                ]
            : message.taskId;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this StateSystemResourceOccupation to JSON.
     * @function toJSON
     * @memberof notify.StateSystemResourceOccupation
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StateSystemResourceOccupation.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StateSystemResourceOccupation
     * @function getTypeUrl
     * @memberof notify.StateSystemResourceOccupation
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StateSystemResourceOccupation.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.StateSystemResourceOccupation";
    };

    /**
     * TaskId enum.
     * @name notify.StateSystemResourceOccupation.TaskId
     * @enum {number}
     * @property {number} IDLE=0 IDLE value
     * @property {number} PANORAMA_UPLOAD=1 PANORAMA_UPLOAD value
     * @property {number} ASTRO_MULTI_STACK=2 ASTRO_MULTI_STACK value
     */
    StateSystemResourceOccupation.TaskId = (function () {
      var valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = "IDLE")] = 0;
      values[(valuesById[1] = "PANORAMA_UPLOAD")] = 1;
      values[(valuesById[2] = "ASTRO_MULTI_STACK")] = 2;
      return values;
    })();

    return StateSystemResourceOccupation;
  })();

  notify.BodyStatus = (function () {
    /**
     * Properties of a BodyStatus.
     * @memberof notify
     * @interface IBodyStatus
     * @property {notify.BodyStatus.BodyStatusEnum|null} [bodyStatus] BodyStatus bodyStatus
     */

    /**
     * Constructs a new BodyStatus.
     * @memberof notify
     * @classdesc Represents a BodyStatus.
     * @implements IBodyStatus
     * @constructor
     * @param {notify.IBodyStatus=} [properties] Properties to set
     */
    function BodyStatus(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * BodyStatus bodyStatus.
     * @member {notify.BodyStatus.BodyStatusEnum} bodyStatus
     * @memberof notify.BodyStatus
     * @instance
     */
    BodyStatus.prototype.bodyStatus = 0;

    /**
     * Creates a new BodyStatus instance using the specified properties.
     * @function create
     * @memberof notify.BodyStatus
     * @static
     * @param {notify.IBodyStatus=} [properties] Properties to set
     * @returns {notify.BodyStatus} BodyStatus instance
     */
    BodyStatus.create = function create(properties) {
      return new BodyStatus(properties);
    };

    /**
     * Encodes the specified BodyStatus message. Does not implicitly {@link notify.BodyStatus.verify|verify} messages.
     * @function encode
     * @memberof notify.BodyStatus
     * @static
     * @param {notify.IBodyStatus} message BodyStatus message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BodyStatus.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.bodyStatus != null &&
        Object.hasOwnProperty.call(message, "bodyStatus")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.bodyStatus);
      return writer;
    };

    /**
     * Encodes the specified BodyStatus message, length delimited. Does not implicitly {@link notify.BodyStatus.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.BodyStatus
     * @static
     * @param {notify.IBodyStatus} message BodyStatus message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BodyStatus.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a BodyStatus message from the specified reader or buffer.
     * @function decode
     * @memberof notify.BodyStatus
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.BodyStatus} BodyStatus
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BodyStatus.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.BodyStatus();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.bodyStatus = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a BodyStatus message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.BodyStatus
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.BodyStatus} BodyStatus
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BodyStatus.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BodyStatus message.
     * @function verify
     * @memberof notify.BodyStatus
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BodyStatus.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.bodyStatus != null &&
        Object.hasOwnProperty.call(message, "bodyStatus")
      )
        switch (message.bodyStatus) {
          default:
            return "bodyStatus: enum value expected";
          case 0:
          case 1:
          case 2:
            break;
        }
      return null;
    };

    /**
     * Creates a BodyStatus message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.BodyStatus
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.BodyStatus} BodyStatus
     */
    BodyStatus.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.BodyStatus) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.BodyStatus: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.BodyStatus();
      switch (object.bodyStatus) {
        default:
          if (typeof object.bodyStatus === "number") {
            message.bodyStatus = object.bodyStatus;
            break;
          }
          break;
        case "UNKNOWN":
        case 0:
          message.bodyStatus = 0;
          break;
        case "EQ_MODE":
        case 1:
          message.bodyStatus = 1;
          break;
        case "AZI_MODE":
        case 2:
          message.bodyStatus = 2;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a BodyStatus message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.BodyStatus
     * @static
     * @param {notify.BodyStatus} message BodyStatus
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BodyStatus.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.bodyStatus = options.enums === String ? "UNKNOWN" : 0;
      if (
        message.bodyStatus != null &&
        Object.hasOwnProperty.call(message, "bodyStatus")
      )
        object.bodyStatus =
          options.enums === String
            ? $root.notify.BodyStatus.BodyStatusEnum[message.bodyStatus] ===
              undefined
              ? message.bodyStatus
              : $root.notify.BodyStatus.BodyStatusEnum[message.bodyStatus]
            : message.bodyStatus;
      return object;
    };

    /**
     * Converts this BodyStatus to JSON.
     * @function toJSON
     * @memberof notify.BodyStatus
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BodyStatus.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BodyStatus
     * @function getTypeUrl
     * @memberof notify.BodyStatus
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BodyStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.BodyStatus";
    };

    /**
     * BodyStatusEnum enum.
     * @name notify.BodyStatus.BodyStatusEnum
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} EQ_MODE=1 EQ_MODE value
     * @property {number} AZI_MODE=2 AZI_MODE value
     */
    BodyStatus.BodyStatusEnum = (function () {
      var valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = "UNKNOWN")] = 0;
      values[(valuesById[1] = "EQ_MODE")] = 1;
      values[(valuesById[2] = "AZI_MODE")] = 2;
      return values;
    })();

    return BodyStatus;
  })();

  notify.ProgressCaptureMosaic = (function () {
    /**
     * Properties of a ProgressCaptureMosaic.
     * @memberof notify
     * @interface IProgressCaptureMosaic
     * @property {number|null} [totalCount] ProgressCaptureMosaic totalCount
     * @property {number|null} [updateType] ProgressCaptureMosaic updateType
     * @property {number|null} [currentCount] ProgressCaptureMosaic currentCount
     * @property {number|null} [stackedCount] ProgressCaptureMosaic stackedCount
     * @property {number|null} [expIndex] ProgressCaptureMosaic expIndex
     * @property {number|null} [gainIndex] ProgressCaptureMosaic gainIndex
     * @property {string|null} [targetName] ProgressCaptureMosaic targetName
     * @property {number|null} [horizontalScale] ProgressCaptureMosaic horizontalScale
     * @property {number|null} [verticalScale] ProgressCaptureMosaic verticalScale
     * @property {number|null} [rotation] ProgressCaptureMosaic rotation
     * @property {number|null} [fovId] ProgressCaptureMosaic fovId
     * @property {number|null} [fovTotal] ProgressCaptureMosaic fovTotal
     */

    /**
     * Constructs a new ProgressCaptureMosaic.
     * @memberof notify
     * @classdesc Represents a ProgressCaptureMosaic.
     * @implements IProgressCaptureMosaic
     * @constructor
     * @param {notify.IProgressCaptureMosaic=} [properties] Properties to set
     */
    function ProgressCaptureMosaic(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProgressCaptureMosaic totalCount.
     * @member {number} totalCount
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.totalCount = 0;

    /**
     * ProgressCaptureMosaic updateType.
     * @member {number} updateType
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.updateType = 0;

    /**
     * ProgressCaptureMosaic currentCount.
     * @member {number} currentCount
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.currentCount = 0;

    /**
     * ProgressCaptureMosaic stackedCount.
     * @member {number} stackedCount
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.stackedCount = 0;

    /**
     * ProgressCaptureMosaic expIndex.
     * @member {number} expIndex
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.expIndex = 0;

    /**
     * ProgressCaptureMosaic gainIndex.
     * @member {number} gainIndex
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.gainIndex = 0;

    /**
     * ProgressCaptureMosaic targetName.
     * @member {string} targetName
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.targetName = "";

    /**
     * ProgressCaptureMosaic horizontalScale.
     * @member {number} horizontalScale
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.horizontalScale = 0;

    /**
     * ProgressCaptureMosaic verticalScale.
     * @member {number} verticalScale
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.verticalScale = 0;

    /**
     * ProgressCaptureMosaic rotation.
     * @member {number} rotation
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.rotation = 0;

    /**
     * ProgressCaptureMosaic fovId.
     * @member {number} fovId
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.fovId = 0;

    /**
     * ProgressCaptureMosaic fovTotal.
     * @member {number} fovTotal
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     */
    ProgressCaptureMosaic.prototype.fovTotal = 0;

    /**
     * Creates a new ProgressCaptureMosaic instance using the specified properties.
     * @function create
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {notify.IProgressCaptureMosaic=} [properties] Properties to set
     * @returns {notify.ProgressCaptureMosaic} ProgressCaptureMosaic instance
     */
    ProgressCaptureMosaic.create = function create(properties) {
      return new ProgressCaptureMosaic(properties);
    };

    /**
     * Encodes the specified ProgressCaptureMosaic message. Does not implicitly {@link notify.ProgressCaptureMosaic.verify|verify} messages.
     * @function encode
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {notify.IProgressCaptureMosaic} message ProgressCaptureMosaic message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureMosaic.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.totalCount);
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.updateType);
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.currentCount);
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.stackedCount);
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.expIndex);
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.gainIndex);
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.targetName);
      if (
        message.horizontalScale != null &&
        Object.hasOwnProperty.call(message, "horizontalScale")
      )
        writer
          .uint32(/* id 8, wireType 0 =*/ 64)
          .int32(message.horizontalScale);
      if (
        message.verticalScale != null &&
        Object.hasOwnProperty.call(message, "verticalScale")
      )
        writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.verticalScale);
      if (
        message.rotation != null &&
        Object.hasOwnProperty.call(message, "rotation")
      )
        writer.uint32(/* id 10, wireType 0 =*/ 80).int32(message.rotation);
      if (message.fovId != null && Object.hasOwnProperty.call(message, "fovId"))
        writer.uint32(/* id 11, wireType 0 =*/ 88).int32(message.fovId);
      if (
        message.fovTotal != null &&
        Object.hasOwnProperty.call(message, "fovTotal")
      )
        writer.uint32(/* id 12, wireType 0 =*/ 96).int32(message.fovTotal);
      return writer;
    };

    /**
     * Encodes the specified ProgressCaptureMosaic message, length delimited. Does not implicitly {@link notify.ProgressCaptureMosaic.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {notify.IProgressCaptureMosaic} message ProgressCaptureMosaic message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProgressCaptureMosaic.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ProgressCaptureMosaic message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ProgressCaptureMosaic} ProgressCaptureMosaic
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureMosaic.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ProgressCaptureMosaic();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalCount = reader.int32();
            break;
          }
          case 2: {
            message.updateType = reader.int32();
            break;
          }
          case 3: {
            message.currentCount = reader.int32();
            break;
          }
          case 4: {
            message.stackedCount = reader.int32();
            break;
          }
          case 5: {
            message.expIndex = reader.int32();
            break;
          }
          case 6: {
            message.gainIndex = reader.int32();
            break;
          }
          case 7: {
            message.targetName = reader.string();
            break;
          }
          case 8: {
            message.horizontalScale = reader.int32();
            break;
          }
          case 9: {
            message.verticalScale = reader.int32();
            break;
          }
          case 10: {
            message.rotation = reader.int32();
            break;
          }
          case 11: {
            message.fovId = reader.int32();
            break;
          }
          case 12: {
            message.fovTotal = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ProgressCaptureMosaic message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ProgressCaptureMosaic} ProgressCaptureMosaic
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProgressCaptureMosaic.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProgressCaptureMosaic message.
     * @function verify
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProgressCaptureMosaic.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        if (!$util.isInteger(message.totalCount))
          return "totalCount: integer expected";
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        if (!$util.isInteger(message.updateType))
          return "updateType: integer expected";
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        if (!$util.isInteger(message.currentCount))
          return "currentCount: integer expected";
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        if (!$util.isInteger(message.stackedCount))
          return "stackedCount: integer expected";
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        if (!$util.isInteger(message.expIndex))
          return "expIndex: integer expected";
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        if (!$util.isInteger(message.gainIndex))
          return "gainIndex: integer expected";
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        if (!$util.isString(message.targetName))
          return "targetName: string expected";
      if (
        message.horizontalScale != null &&
        Object.hasOwnProperty.call(message, "horizontalScale")
      )
        if (!$util.isInteger(message.horizontalScale))
          return "horizontalScale: integer expected";
      if (
        message.verticalScale != null &&
        Object.hasOwnProperty.call(message, "verticalScale")
      )
        if (!$util.isInteger(message.verticalScale))
          return "verticalScale: integer expected";
      if (
        message.rotation != null &&
        Object.hasOwnProperty.call(message, "rotation")
      )
        if (!$util.isInteger(message.rotation))
          return "rotation: integer expected";
      if (message.fovId != null && Object.hasOwnProperty.call(message, "fovId"))
        if (!$util.isInteger(message.fovId)) return "fovId: integer expected";
      if (
        message.fovTotal != null &&
        Object.hasOwnProperty.call(message, "fovTotal")
      )
        if (!$util.isInteger(message.fovTotal))
          return "fovTotal: integer expected";
      return null;
    };

    /**
     * Creates a ProgressCaptureMosaic message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ProgressCaptureMosaic} ProgressCaptureMosaic
     */
    ProgressCaptureMosaic.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ProgressCaptureMosaic) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ProgressCaptureMosaic: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ProgressCaptureMosaic();
      if (object.totalCount != null) message.totalCount = object.totalCount | 0;
      if (object.updateType != null) message.updateType = object.updateType | 0;
      if (object.currentCount != null)
        message.currentCount = object.currentCount | 0;
      if (object.stackedCount != null)
        message.stackedCount = object.stackedCount | 0;
      if (object.expIndex != null) message.expIndex = object.expIndex | 0;
      if (object.gainIndex != null) message.gainIndex = object.gainIndex | 0;
      if (object.targetName != null)
        message.targetName = String(object.targetName);
      if (object.horizontalScale != null)
        message.horizontalScale = object.horizontalScale | 0;
      if (object.verticalScale != null)
        message.verticalScale = object.verticalScale | 0;
      if (object.rotation != null) message.rotation = object.rotation | 0;
      if (object.fovId != null) message.fovId = object.fovId | 0;
      if (object.fovTotal != null) message.fovTotal = object.fovTotal | 0;
      return message;
    };

    /**
     * Creates a plain object from a ProgressCaptureMosaic message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {notify.ProgressCaptureMosaic} message ProgressCaptureMosaic
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProgressCaptureMosaic.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.totalCount = 0;
        object.updateType = 0;
        object.currentCount = 0;
        object.stackedCount = 0;
        object.expIndex = 0;
        object.gainIndex = 0;
        object.targetName = "";
        object.horizontalScale = 0;
        object.verticalScale = 0;
        object.rotation = 0;
        object.fovId = 0;
        object.fovTotal = 0;
      }
      if (
        message.totalCount != null &&
        Object.hasOwnProperty.call(message, "totalCount")
      )
        object.totalCount = message.totalCount;
      if (
        message.updateType != null &&
        Object.hasOwnProperty.call(message, "updateType")
      )
        object.updateType = message.updateType;
      if (
        message.currentCount != null &&
        Object.hasOwnProperty.call(message, "currentCount")
      )
        object.currentCount = message.currentCount;
      if (
        message.stackedCount != null &&
        Object.hasOwnProperty.call(message, "stackedCount")
      )
        object.stackedCount = message.stackedCount;
      if (
        message.expIndex != null &&
        Object.hasOwnProperty.call(message, "expIndex")
      )
        object.expIndex = message.expIndex;
      if (
        message.gainIndex != null &&
        Object.hasOwnProperty.call(message, "gainIndex")
      )
        object.gainIndex = message.gainIndex;
      if (
        message.targetName != null &&
        Object.hasOwnProperty.call(message, "targetName")
      )
        object.targetName = message.targetName;
      if (
        message.horizontalScale != null &&
        Object.hasOwnProperty.call(message, "horizontalScale")
      )
        object.horizontalScale = message.horizontalScale;
      if (
        message.verticalScale != null &&
        Object.hasOwnProperty.call(message, "verticalScale")
      )
        object.verticalScale = message.verticalScale;
      if (
        message.rotation != null &&
        Object.hasOwnProperty.call(message, "rotation")
      )
        object.rotation = message.rotation;
      if (message.fovId != null && Object.hasOwnProperty.call(message, "fovId"))
        object.fovId = message.fovId;
      if (
        message.fovTotal != null &&
        Object.hasOwnProperty.call(message, "fovTotal")
      )
        object.fovTotal = message.fovTotal;
      return object;
    };

    /**
     * Converts this ProgressCaptureMosaic to JSON.
     * @function toJSON
     * @memberof notify.ProgressCaptureMosaic
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProgressCaptureMosaic.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProgressCaptureMosaic
     * @function getTypeUrl
     * @memberof notify.ProgressCaptureMosaic
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProgressCaptureMosaic.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ProgressCaptureMosaic";
    };

    return ProgressCaptureMosaic;
  })();

  notify.Wb = (function () {
    /**
     * Properties of a Wb.
     * @memberof notify
     * @interface IWb
     * @property {number|Long|null} [mode] Wb mode
     * @property {number|null} [ct] Wb ct
     * @property {number|null} [scene] Wb scene
     * @property {number|null} [cameraType] Wb cameraType
     */

    /**
     * Constructs a new Wb.
     * @memberof notify
     * @classdesc Represents a Wb.
     * @implements IWb
     * @constructor
     * @param {notify.IWb=} [properties] Properties to set
     */
    function Wb(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * Wb mode.
     * @member {number|Long} mode
     * @memberof notify.Wb
     * @instance
     */
    Wb.prototype.mode = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;

    /**
     * Wb ct.
     * @member {number} ct
     * @memberof notify.Wb
     * @instance
     */
    Wb.prototype.ct = 0;

    /**
     * Wb scene.
     * @member {number} scene
     * @memberof notify.Wb
     * @instance
     */
    Wb.prototype.scene = 0;

    /**
     * Wb cameraType.
     * @member {number} cameraType
     * @memberof notify.Wb
     * @instance
     */
    Wb.prototype.cameraType = 0;

    /**
     * Creates a new Wb instance using the specified properties.
     * @function create
     * @memberof notify.Wb
     * @static
     * @param {notify.IWb=} [properties] Properties to set
     * @returns {notify.Wb} Wb instance
     */
    Wb.create = function create(properties) {
      return new Wb(properties);
    };

    /**
     * Encodes the specified Wb message. Does not implicitly {@link notify.Wb.verify|verify} messages.
     * @function encode
     * @memberof notify.Wb
     * @static
     * @param {notify.IWb} message Wb message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Wb.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.mode);
      if (message.ct != null && Object.hasOwnProperty.call(message, "ct"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.ct);
      if (message.scene != null && Object.hasOwnProperty.call(message, "scene"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.scene);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified Wb message, length delimited. Does not implicitly {@link notify.Wb.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.Wb
     * @static
     * @param {notify.IWb} message Wb message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Wb.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a Wb message from the specified reader or buffer.
     * @function decode
     * @memberof notify.Wb
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.Wb} Wb
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Wb.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.Wb();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mode = reader.uint64();
            break;
          }
          case 2: {
            message.ct = reader.int32();
            break;
          }
          case 3: {
            message.scene = reader.int32();
            break;
          }
          case 4: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a Wb message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.Wb
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.Wb} Wb
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Wb.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Wb message.
     * @function verify
     * @memberof notify.Wb
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Wb.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (
          !$util.isInteger(message.mode) &&
          !(
            message.mode &&
            $util.isInteger(message.mode.low) &&
            $util.isInteger(message.mode.high)
          )
        )
          return "mode: integer|Long expected";
      if (message.ct != null && Object.hasOwnProperty.call(message, "ct"))
        if (!$util.isInteger(message.ct)) return "ct: integer expected";
      if (message.scene != null && Object.hasOwnProperty.call(message, "scene"))
        if (!$util.isInteger(message.scene)) return "scene: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a Wb message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.Wb
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.Wb} Wb
     */
    Wb.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.Wb) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.Wb: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.Wb();
      if (object.mode != null)
        if ($util.Long) message.mode = $util.Long.fromValue(object.mode, true);
        else if (typeof object.mode === "string")
          message.mode = parseInt(object.mode, 10);
        else if (typeof object.mode === "number") message.mode = object.mode;
        else if (typeof object.mode === "object")
          message.mode = new $util.LongBits(
            object.mode.low >>> 0,
            object.mode.high >>> 0,
          ).toNumber(true);
      if (object.ct != null) message.ct = object.ct | 0;
      if (object.scene != null) message.scene = object.scene | 0;
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a Wb message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.Wb
     * @static
     * @param {notify.Wb} message Wb
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Wb.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.mode =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.mode =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.ct = 0;
        object.scene = 0;
        object.cameraType = 0;
      }
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.mode =
            typeof message.mode === "number"
              ? BigInt(message.mode)
              : $util.Long.fromBits(
                  message.mode.low >>> 0,
                  message.mode.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.mode === "number")
          object.mode =
            options.longs === String ? String(message.mode) : message.mode;
        else
          object.mode =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.mode)
              : options.longs === Number
                ? new $util.LongBits(
                    message.mode.low >>> 0,
                    message.mode.high >>> 0,
                  ).toNumber(true)
                : message.mode;
      if (message.ct != null && Object.hasOwnProperty.call(message, "ct"))
        object.ct = message.ct;
      if (message.scene != null && Object.hasOwnProperty.call(message, "scene"))
        object.scene = message.scene;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this Wb to JSON.
     * @function toJSON
     * @memberof notify.Wb
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Wb.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Wb
     * @function getTypeUrl
     * @memberof notify.Wb
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Wb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.Wb";
    };

    return Wb;
  })();

  notify.GeneralIntParam = (function () {
    /**
     * Properties of a GeneralIntParam.
     * @memberof notify
     * @interface IGeneralIntParam
     * @property {number|Long|null} [paramId] GeneralIntParam paramId
     * @property {number|null} [mode] GeneralIntParam mode
     * @property {number|null} [value] GeneralIntParam value
     */

    /**
     * Constructs a new GeneralIntParam.
     * @memberof notify
     * @classdesc Represents a GeneralIntParam.
     * @implements IGeneralIntParam
     * @constructor
     * @param {notify.IGeneralIntParam=} [properties] Properties to set
     */
    function GeneralIntParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * GeneralIntParam paramId.
     * @member {number|Long} paramId
     * @memberof notify.GeneralIntParam
     * @instance
     */
    GeneralIntParam.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * GeneralIntParam mode.
     * @member {number} mode
     * @memberof notify.GeneralIntParam
     * @instance
     */
    GeneralIntParam.prototype.mode = 0;

    /**
     * GeneralIntParam value.
     * @member {number} value
     * @memberof notify.GeneralIntParam
     * @instance
     */
    GeneralIntParam.prototype.value = 0;

    /**
     * Creates a new GeneralIntParam instance using the specified properties.
     * @function create
     * @memberof notify.GeneralIntParam
     * @static
     * @param {notify.IGeneralIntParam=} [properties] Properties to set
     * @returns {notify.GeneralIntParam} GeneralIntParam instance
     */
    GeneralIntParam.create = function create(properties) {
      return new GeneralIntParam(properties);
    };

    /**
     * Encodes the specified GeneralIntParam message. Does not implicitly {@link notify.GeneralIntParam.verify|verify} messages.
     * @function encode
     * @memberof notify.GeneralIntParam
     * @static
     * @param {notify.IGeneralIntParam} message GeneralIntParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralIntParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.value);
      return writer;
    };

    /**
     * Encodes the specified GeneralIntParam message, length delimited. Does not implicitly {@link notify.GeneralIntParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.GeneralIntParam
     * @static
     * @param {notify.IGeneralIntParam} message GeneralIntParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralIntParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a GeneralIntParam message from the specified reader or buffer.
     * @function decode
     * @memberof notify.GeneralIntParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.GeneralIntParam} GeneralIntParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralIntParam.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.GeneralIntParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.mode = reader.int32();
            break;
          }
          case 3: {
            message.value = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a GeneralIntParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.GeneralIntParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.GeneralIntParam} GeneralIntParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralIntParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GeneralIntParam message.
     * @function verify
     * @memberof notify.GeneralIntParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GeneralIntParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (!$util.isInteger(message.value)) return "value: integer expected";
      return null;
    };

    /**
     * Creates a GeneralIntParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.GeneralIntParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.GeneralIntParam} GeneralIntParam
     */
    GeneralIntParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.GeneralIntParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.GeneralIntParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.GeneralIntParam();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.mode != null) message.mode = object.mode | 0;
      if (object.value != null) message.value = object.value | 0;
      return message;
    };

    /**
     * Creates a plain object from a GeneralIntParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.GeneralIntParam
     * @static
     * @param {notify.GeneralIntParam} message GeneralIntParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GeneralIntParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.mode = 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this GeneralIntParam to JSON.
     * @function toJSON
     * @memberof notify.GeneralIntParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GeneralIntParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GeneralIntParam
     * @function getTypeUrl
     * @memberof notify.GeneralIntParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GeneralIntParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.GeneralIntParam";
    };

    return GeneralIntParam;
  })();

  notify.GeneralFloatParam = (function () {
    /**
     * Properties of a GeneralFloatParam.
     * @memberof notify
     * @interface IGeneralFloatParam
     * @property {number|Long|null} [paramId] GeneralFloatParam paramId
     * @property {number|null} [value] GeneralFloatParam value
     */

    /**
     * Constructs a new GeneralFloatParam.
     * @memberof notify
     * @classdesc Represents a GeneralFloatParam.
     * @implements IGeneralFloatParam
     * @constructor
     * @param {notify.IGeneralFloatParam=} [properties] Properties to set
     */
    function GeneralFloatParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * GeneralFloatParam paramId.
     * @member {number|Long} paramId
     * @memberof notify.GeneralFloatParam
     * @instance
     */
    GeneralFloatParam.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * GeneralFloatParam value.
     * @member {number} value
     * @memberof notify.GeneralFloatParam
     * @instance
     */
    GeneralFloatParam.prototype.value = 0;

    /**
     * Creates a new GeneralFloatParam instance using the specified properties.
     * @function create
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {notify.IGeneralFloatParam=} [properties] Properties to set
     * @returns {notify.GeneralFloatParam} GeneralFloatParam instance
     */
    GeneralFloatParam.create = function create(properties) {
      return new GeneralFloatParam(properties);
    };

    /**
     * Encodes the specified GeneralFloatParam message. Does not implicitly {@link notify.GeneralFloatParam.verify|verify} messages.
     * @function encode
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {notify.IGeneralFloatParam} message GeneralFloatParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralFloatParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 2, wireType 5 =*/ 21).float(message.value);
      return writer;
    };

    /**
     * Encodes the specified GeneralFloatParam message, length delimited. Does not implicitly {@link notify.GeneralFloatParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {notify.IGeneralFloatParam} message GeneralFloatParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralFloatParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a GeneralFloatParam message from the specified reader or buffer.
     * @function decode
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.GeneralFloatParam} GeneralFloatParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralFloatParam.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.GeneralFloatParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.float();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a GeneralFloatParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.GeneralFloatParam} GeneralFloatParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralFloatParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GeneralFloatParam message.
     * @function verify
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GeneralFloatParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (typeof message.value !== "number") return "value: number expected";
      return null;
    };

    /**
     * Creates a GeneralFloatParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.GeneralFloatParam} GeneralFloatParam
     */
    GeneralFloatParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.GeneralFloatParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.GeneralFloatParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.GeneralFloatParam();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.value != null) message.value = Number(object.value);
      return message;
    };

    /**
     * Creates a plain object from a GeneralFloatParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {notify.GeneralFloatParam} message GeneralFloatParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GeneralFloatParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value =
          options.json && !isFinite(message.value)
            ? String(message.value)
            : message.value;
      return object;
    };

    /**
     * Converts this GeneralFloatParam to JSON.
     * @function toJSON
     * @memberof notify.GeneralFloatParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GeneralFloatParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GeneralFloatParam
     * @function getTypeUrl
     * @memberof notify.GeneralFloatParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GeneralFloatParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.GeneralFloatParam";
    };

    return GeneralFloatParam;
  })();

  notify.GeneralBoolParams = (function () {
    /**
     * Properties of a GeneralBoolParams.
     * @memberof notify
     * @interface IGeneralBoolParams
     * @property {number|Long|null} [paramId] GeneralBoolParams paramId
     * @property {boolean|null} [value] GeneralBoolParams value
     */

    /**
     * Constructs a new GeneralBoolParams.
     * @memberof notify
     * @classdesc Represents a GeneralBoolParams.
     * @implements IGeneralBoolParams
     * @constructor
     * @param {notify.IGeneralBoolParams=} [properties] Properties to set
     */
    function GeneralBoolParams(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * GeneralBoolParams paramId.
     * @member {number|Long} paramId
     * @memberof notify.GeneralBoolParams
     * @instance
     */
    GeneralBoolParams.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * GeneralBoolParams value.
     * @member {boolean} value
     * @memberof notify.GeneralBoolParams
     * @instance
     */
    GeneralBoolParams.prototype.value = false;

    /**
     * Creates a new GeneralBoolParams instance using the specified properties.
     * @function create
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {notify.IGeneralBoolParams=} [properties] Properties to set
     * @returns {notify.GeneralBoolParams} GeneralBoolParams instance
     */
    GeneralBoolParams.create = function create(properties) {
      return new GeneralBoolParams(properties);
    };

    /**
     * Encodes the specified GeneralBoolParams message. Does not implicitly {@link notify.GeneralBoolParams.verify|verify} messages.
     * @function encode
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {notify.IGeneralBoolParams} message GeneralBoolParams message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralBoolParams.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.value);
      return writer;
    };

    /**
     * Encodes the specified GeneralBoolParams message, length delimited. Does not implicitly {@link notify.GeneralBoolParams.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {notify.IGeneralBoolParams} message GeneralBoolParams message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    GeneralBoolParams.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a GeneralBoolParams message from the specified reader or buffer.
     * @function decode
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.GeneralBoolParams} GeneralBoolParams
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralBoolParams.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.GeneralBoolParams();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.bool();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a GeneralBoolParams message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.GeneralBoolParams} GeneralBoolParams
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    GeneralBoolParams.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a GeneralBoolParams message.
     * @function verify
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    GeneralBoolParams.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (typeof message.value !== "boolean")
          return "value: boolean expected";
      return null;
    };

    /**
     * Creates a GeneralBoolParams message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.GeneralBoolParams} GeneralBoolParams
     */
    GeneralBoolParams.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.GeneralBoolParams) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.GeneralBoolParams: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.GeneralBoolParams();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.value != null) message.value = Boolean(object.value);
      return message;
    };

    /**
     * Creates a plain object from a GeneralBoolParams message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {notify.GeneralBoolParams} message GeneralBoolParams
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    GeneralBoolParams.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.value = false;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this GeneralBoolParams to JSON.
     * @function toJSON
     * @memberof notify.GeneralBoolParams
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    GeneralBoolParams.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for GeneralBoolParams
     * @function getTypeUrl
     * @memberof notify.GeneralBoolParams
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    GeneralBoolParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.GeneralBoolParams";
    };

    return GeneralBoolParams;
  })();

  notify.SwitchShootingMode = (function () {
    /**
     * Properties of a SwitchShootingMode.
     * @memberof notify
     * @interface ISwitchShootingMode
     * @property {number|null} [state] SwitchShootingMode state
     * @property {number|null} [sourceMode] SwitchShootingMode sourceMode
     * @property {number|null} [dstMode] SwitchShootingMode dstMode
     */

    /**
     * Constructs a new SwitchShootingMode.
     * @memberof notify
     * @classdesc Represents a SwitchShootingMode.
     * @implements ISwitchShootingMode
     * @constructor
     * @param {notify.ISwitchShootingMode=} [properties] Properties to set
     */
    function SwitchShootingMode(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SwitchShootingMode state.
     * @member {number} state
     * @memberof notify.SwitchShootingMode
     * @instance
     */
    SwitchShootingMode.prototype.state = 0;

    /**
     * SwitchShootingMode sourceMode.
     * @member {number} sourceMode
     * @memberof notify.SwitchShootingMode
     * @instance
     */
    SwitchShootingMode.prototype.sourceMode = 0;

    /**
     * SwitchShootingMode dstMode.
     * @member {number} dstMode
     * @memberof notify.SwitchShootingMode
     * @instance
     */
    SwitchShootingMode.prototype.dstMode = 0;

    /**
     * Creates a new SwitchShootingMode instance using the specified properties.
     * @function create
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {notify.ISwitchShootingMode=} [properties] Properties to set
     * @returns {notify.SwitchShootingMode} SwitchShootingMode instance
     */
    SwitchShootingMode.create = function create(properties) {
      return new SwitchShootingMode(properties);
    };

    /**
     * Encodes the specified SwitchShootingMode message. Does not implicitly {@link notify.SwitchShootingMode.verify|verify} messages.
     * @function encode
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {notify.ISwitchShootingMode} message SwitchShootingMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchShootingMode.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.sourceMode != null &&
        Object.hasOwnProperty.call(message, "sourceMode")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.sourceMode);
      if (
        message.dstMode != null &&
        Object.hasOwnProperty.call(message, "dstMode")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.dstMode);
      return writer;
    };

    /**
     * Encodes the specified SwitchShootingMode message, length delimited. Does not implicitly {@link notify.SwitchShootingMode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {notify.ISwitchShootingMode} message SwitchShootingMode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchShootingMode.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SwitchShootingMode message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SwitchShootingMode} SwitchShootingMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchShootingMode.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SwitchShootingMode();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.sourceMode = reader.int32();
            break;
          }
          case 3: {
            message.dstMode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SwitchShootingMode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SwitchShootingMode} SwitchShootingMode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchShootingMode.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SwitchShootingMode message.
     * @function verify
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SwitchShootingMode.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      if (
        message.sourceMode != null &&
        Object.hasOwnProperty.call(message, "sourceMode")
      )
        if (!$util.isInteger(message.sourceMode))
          return "sourceMode: integer expected";
      if (
        message.dstMode != null &&
        Object.hasOwnProperty.call(message, "dstMode")
      )
        if (!$util.isInteger(message.dstMode))
          return "dstMode: integer expected";
      return null;
    };

    /**
     * Creates a SwitchShootingMode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SwitchShootingMode} SwitchShootingMode
     */
    SwitchShootingMode.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SwitchShootingMode) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SwitchShootingMode: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SwitchShootingMode();
      if (object.state != null) message.state = object.state | 0;
      if (object.sourceMode != null) message.sourceMode = object.sourceMode | 0;
      if (object.dstMode != null) message.dstMode = object.dstMode | 0;
      return message;
    };

    /**
     * Creates a plain object from a SwitchShootingMode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {notify.SwitchShootingMode} message SwitchShootingMode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SwitchShootingMode.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = 0;
        object.sourceMode = 0;
        object.dstMode = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      if (
        message.sourceMode != null &&
        Object.hasOwnProperty.call(message, "sourceMode")
      )
        object.sourceMode = message.sourceMode;
      if (
        message.dstMode != null &&
        Object.hasOwnProperty.call(message, "dstMode")
      )
        object.dstMode = message.dstMode;
      return object;
    };

    /**
     * Converts this SwitchShootingMode to JSON.
     * @function toJSON
     * @memberof notify.SwitchShootingMode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SwitchShootingMode.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SwitchShootingMode
     * @function getTypeUrl
     * @memberof notify.SwitchShootingMode
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SwitchShootingMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SwitchShootingMode";
    };

    return SwitchShootingMode;
  })();

  notify.SwitchCropRatioState = (function () {
    /**
     * Properties of a SwitchCropRatioState.
     * @memberof notify
     * @interface ISwitchCropRatioState
     * @property {number|null} [state] SwitchCropRatioState state
     * @property {number|null} [cropRatio] SwitchCropRatioState cropRatio
     */

    /**
     * Constructs a new SwitchCropRatioState.
     * @memberof notify
     * @classdesc Represents a SwitchCropRatioState.
     * @implements ISwitchCropRatioState
     * @constructor
     * @param {notify.ISwitchCropRatioState=} [properties] Properties to set
     */
    function SwitchCropRatioState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SwitchCropRatioState state.
     * @member {number} state
     * @memberof notify.SwitchCropRatioState
     * @instance
     */
    SwitchCropRatioState.prototype.state = 0;

    /**
     * SwitchCropRatioState cropRatio.
     * @member {number} cropRatio
     * @memberof notify.SwitchCropRatioState
     * @instance
     */
    SwitchCropRatioState.prototype.cropRatio = 0;

    /**
     * Creates a new SwitchCropRatioState instance using the specified properties.
     * @function create
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {notify.ISwitchCropRatioState=} [properties] Properties to set
     * @returns {notify.SwitchCropRatioState} SwitchCropRatioState instance
     */
    SwitchCropRatioState.create = function create(properties) {
      return new SwitchCropRatioState(properties);
    };

    /**
     * Encodes the specified SwitchCropRatioState message. Does not implicitly {@link notify.SwitchCropRatioState.verify|verify} messages.
     * @function encode
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {notify.ISwitchCropRatioState} message SwitchCropRatioState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchCropRatioState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cropRatio != null &&
        Object.hasOwnProperty.call(message, "cropRatio")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cropRatio);
      return writer;
    };

    /**
     * Encodes the specified SwitchCropRatioState message, length delimited. Does not implicitly {@link notify.SwitchCropRatioState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {notify.ISwitchCropRatioState} message SwitchCropRatioState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchCropRatioState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SwitchCropRatioState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SwitchCropRatioState} SwitchCropRatioState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchCropRatioState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SwitchCropRatioState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cropRatio = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SwitchCropRatioState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SwitchCropRatioState} SwitchCropRatioState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchCropRatioState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SwitchCropRatioState message.
     * @function verify
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SwitchCropRatioState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      if (
        message.cropRatio != null &&
        Object.hasOwnProperty.call(message, "cropRatio")
      )
        if (!$util.isInteger(message.cropRatio))
          return "cropRatio: integer expected";
      return null;
    };

    /**
     * Creates a SwitchCropRatioState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SwitchCropRatioState} SwitchCropRatioState
     */
    SwitchCropRatioState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SwitchCropRatioState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SwitchCropRatioState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SwitchCropRatioState();
      if (object.state != null) message.state = object.state | 0;
      if (object.cropRatio != null) message.cropRatio = object.cropRatio | 0;
      return message;
    };

    /**
     * Creates a plain object from a SwitchCropRatioState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {notify.SwitchCropRatioState} message SwitchCropRatioState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SwitchCropRatioState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = 0;
        object.cropRatio = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      if (
        message.cropRatio != null &&
        Object.hasOwnProperty.call(message, "cropRatio")
      )
        object.cropRatio = message.cropRatio;
      return object;
    };

    /**
     * Converts this SwitchCropRatioState to JSON.
     * @function toJSON
     * @memberof notify.SwitchCropRatioState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SwitchCropRatioState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SwitchCropRatioState
     * @function getTypeUrl
     * @memberof notify.SwitchCropRatioState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SwitchCropRatioState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SwitchCropRatioState";
    };

    return SwitchCropRatioState;
  })();

  notify.ResolutionParam = (function () {
    /**
     * Properties of a ResolutionParam.
     * @memberof notify
     * @interface IResolutionParam
     * @property {number|Long|null} [paramId] ResolutionParam paramId
     * @property {number|null} [currentResValue] ResolutionParam currentResValue
     * @property {number|null} [currentFpsValue] ResolutionParam currentFpsValue
     * @property {Array.<number>|null} [supportedFpsList] ResolutionParam supportedFpsList
     * @property {Array.<number>|null} [supportedResolutionList] ResolutionParam supportedResolutionList
     */

    /**
     * Constructs a new ResolutionParam.
     * @memberof notify
     * @classdesc Represents a ResolutionParam.
     * @implements IResolutionParam
     * @constructor
     * @param {notify.IResolutionParam=} [properties] Properties to set
     */
    function ResolutionParam(properties) {
      this.supportedFpsList = [];
      this.supportedResolutionList = [];
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ResolutionParam paramId.
     * @member {number|Long} paramId
     * @memberof notify.ResolutionParam
     * @instance
     */
    ResolutionParam.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ResolutionParam currentResValue.
     * @member {number} currentResValue
     * @memberof notify.ResolutionParam
     * @instance
     */
    ResolutionParam.prototype.currentResValue = 0;

    /**
     * ResolutionParam currentFpsValue.
     * @member {number} currentFpsValue
     * @memberof notify.ResolutionParam
     * @instance
     */
    ResolutionParam.prototype.currentFpsValue = 0;

    /**
     * ResolutionParam supportedFpsList.
     * @member {Array.<number>} supportedFpsList
     * @memberof notify.ResolutionParam
     * @instance
     */
    ResolutionParam.prototype.supportedFpsList = $util.emptyArray;

    /**
     * ResolutionParam supportedResolutionList.
     * @member {Array.<number>} supportedResolutionList
     * @memberof notify.ResolutionParam
     * @instance
     */
    ResolutionParam.prototype.supportedResolutionList = $util.emptyArray;

    /**
     * Creates a new ResolutionParam instance using the specified properties.
     * @function create
     * @memberof notify.ResolutionParam
     * @static
     * @param {notify.IResolutionParam=} [properties] Properties to set
     * @returns {notify.ResolutionParam} ResolutionParam instance
     */
    ResolutionParam.create = function create(properties) {
      return new ResolutionParam(properties);
    };

    /**
     * Encodes the specified ResolutionParam message. Does not implicitly {@link notify.ResolutionParam.verify|verify} messages.
     * @function encode
     * @memberof notify.ResolutionParam
     * @static
     * @param {notify.IResolutionParam} message ResolutionParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResolutionParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (
        message.currentResValue != null &&
        Object.hasOwnProperty.call(message, "currentResValue")
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .int32(message.currentResValue);
      if (
        message.currentFpsValue != null &&
        Object.hasOwnProperty.call(message, "currentFpsValue")
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .int32(message.currentFpsValue);
      if (message.supportedFpsList != null && message.supportedFpsList.length) {
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork();
        for (var i = 0; i < message.supportedFpsList.length; ++i)
          writer.int32(message.supportedFpsList[i]);
        writer.ldelim();
      }
      if (
        message.supportedResolutionList != null &&
        message.supportedResolutionList.length
      ) {
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork();
        for (var i = 0; i < message.supportedResolutionList.length; ++i)
          writer.int32(message.supportedResolutionList[i]);
        writer.ldelim();
      }
      return writer;
    };

    /**
     * Encodes the specified ResolutionParam message, length delimited. Does not implicitly {@link notify.ResolutionParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.ResolutionParam
     * @static
     * @param {notify.IResolutionParam} message ResolutionParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResolutionParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ResolutionParam message from the specified reader or buffer.
     * @function decode
     * @memberof notify.ResolutionParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.ResolutionParam} ResolutionParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResolutionParam.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.ResolutionParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.currentResValue = reader.int32();
            break;
          }
          case 3: {
            message.currentFpsValue = reader.int32();
            break;
          }
          case 4: {
            if (!(message.supportedFpsList && message.supportedFpsList.length))
              message.supportedFpsList = [];
            if ((tag & 7) === 2) {
              var end2 = reader.uint32() + reader.pos;
              if (end2 > reader.len) throw RangeError("index out of range");
              reader.len = end2;
              while (reader.pos < end2)
                message.supportedFpsList.push(reader.int32());
              if (reader.pos !== end2) throw RangeError("index out of range");
              reader.len = end;
            } else message.supportedFpsList.push(reader.int32());
            break;
          }
          case 5: {
            if (!(
              message.supportedResolutionList &&
              message.supportedResolutionList.length
            ))
              message.supportedResolutionList = [];
            if ((tag & 7) === 2) {
              var end2 = reader.uint32() + reader.pos;
              if (end2 > reader.len) throw RangeError("index out of range");
              reader.len = end2;
              while (reader.pos < end2)
                message.supportedResolutionList.push(reader.int32());
              if (reader.pos !== end2) throw RangeError("index out of range");
              reader.len = end;
            } else message.supportedResolutionList.push(reader.int32());
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ResolutionParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.ResolutionParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.ResolutionParam} ResolutionParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResolutionParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ResolutionParam message.
     * @function verify
     * @memberof notify.ResolutionParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ResolutionParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (
        message.currentResValue != null &&
        Object.hasOwnProperty.call(message, "currentResValue")
      )
        if (!$util.isInteger(message.currentResValue))
          return "currentResValue: integer expected";
      if (
        message.currentFpsValue != null &&
        Object.hasOwnProperty.call(message, "currentFpsValue")
      )
        if (!$util.isInteger(message.currentFpsValue))
          return "currentFpsValue: integer expected";
      if (
        message.supportedFpsList != null &&
        Object.hasOwnProperty.call(message, "supportedFpsList")
      ) {
        if (!Array.isArray(message.supportedFpsList))
          return "supportedFpsList: array expected";
        for (var i = 0; i < message.supportedFpsList.length; ++i)
          if (!$util.isInteger(message.supportedFpsList[i]))
            return "supportedFpsList: integer[] expected";
      }
      if (
        message.supportedResolutionList != null &&
        Object.hasOwnProperty.call(message, "supportedResolutionList")
      ) {
        if (!Array.isArray(message.supportedResolutionList))
          return "supportedResolutionList: array expected";
        for (var i = 0; i < message.supportedResolutionList.length; ++i)
          if (!$util.isInteger(message.supportedResolutionList[i]))
            return "supportedResolutionList: integer[] expected";
      }
      return null;
    };

    /**
     * Creates a ResolutionParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.ResolutionParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.ResolutionParam} ResolutionParam
     */
    ResolutionParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.ResolutionParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.ResolutionParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.ResolutionParam();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.currentResValue != null)
        message.currentResValue = object.currentResValue | 0;
      if (object.currentFpsValue != null)
        message.currentFpsValue = object.currentFpsValue | 0;
      if (object.supportedFpsList) {
        if (!Array.isArray(object.supportedFpsList))
          throw TypeError(
            ".notify.ResolutionParam.supportedFpsList: array expected",
          );
        message.supportedFpsList = [];
        for (var i = 0; i < object.supportedFpsList.length; ++i)
          message.supportedFpsList[i] = object.supportedFpsList[i] | 0;
      }
      if (object.supportedResolutionList) {
        if (!Array.isArray(object.supportedResolutionList))
          throw TypeError(
            ".notify.ResolutionParam.supportedResolutionList: array expected",
          );
        message.supportedResolutionList = [];
        for (var i = 0; i < object.supportedResolutionList.length; ++i)
          message.supportedResolutionList[i] =
            object.supportedResolutionList[i] | 0;
      }
      return message;
    };

    /**
     * Creates a plain object from a ResolutionParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.ResolutionParam
     * @static
     * @param {notify.ResolutionParam} message ResolutionParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ResolutionParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.arrays || options.defaults) {
        object.supportedFpsList = [];
        object.supportedResolutionList = [];
      }
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.currentResValue = 0;
        object.currentFpsValue = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (
        message.currentResValue != null &&
        Object.hasOwnProperty.call(message, "currentResValue")
      )
        object.currentResValue = message.currentResValue;
      if (
        message.currentFpsValue != null &&
        Object.hasOwnProperty.call(message, "currentFpsValue")
      )
        object.currentFpsValue = message.currentFpsValue;
      if (message.supportedFpsList && message.supportedFpsList.length) {
        object.supportedFpsList = [];
        for (var j = 0; j < message.supportedFpsList.length; ++j)
          object.supportedFpsList[j] = message.supportedFpsList[j];
      }
      if (
        message.supportedResolutionList &&
        message.supportedResolutionList.length
      ) {
        object.supportedResolutionList = [];
        for (var j = 0; j < message.supportedResolutionList.length; ++j)
          object.supportedResolutionList[j] =
            message.supportedResolutionList[j];
      }
      return object;
    };

    /**
     * Converts this ResolutionParam to JSON.
     * @function toJSON
     * @memberof notify.ResolutionParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ResolutionParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ResolutionParam
     * @function getTypeUrl
     * @memberof notify.ResolutionParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ResolutionParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.ResolutionParam";
    };

    return ResolutionParam;
  })();

  notify.CaptureRawState = (function () {
    /**
     * Properties of a CaptureRawState.
     * @memberof notify
     * @interface ICaptureRawState
     * @property {notify.OperationState|null} [state] CaptureRawState state
     * @property {number|null} [cameraType] CaptureRawState cameraType
     */

    /**
     * Constructs a new CaptureRawState.
     * @memberof notify
     * @classdesc Represents a CaptureRawState.
     * @implements ICaptureRawState
     * @constructor
     * @param {notify.ICaptureRawState=} [properties] Properties to set
     */
    function CaptureRawState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CaptureRawState state.
     * @member {notify.OperationState} state
     * @memberof notify.CaptureRawState
     * @instance
     */
    CaptureRawState.prototype.state = 0;

    /**
     * CaptureRawState cameraType.
     * @member {number} cameraType
     * @memberof notify.CaptureRawState
     * @instance
     */
    CaptureRawState.prototype.cameraType = 0;

    /**
     * Creates a new CaptureRawState instance using the specified properties.
     * @function create
     * @memberof notify.CaptureRawState
     * @static
     * @param {notify.ICaptureRawState=} [properties] Properties to set
     * @returns {notify.CaptureRawState} CaptureRawState instance
     */
    CaptureRawState.create = function create(properties) {
      return new CaptureRawState(properties);
    };

    /**
     * Encodes the specified CaptureRawState message. Does not implicitly {@link notify.CaptureRawState.verify|verify} messages.
     * @function encode
     * @memberof notify.CaptureRawState
     * @static
     * @param {notify.ICaptureRawState} message CaptureRawState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureRawState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified CaptureRawState message, length delimited. Does not implicitly {@link notify.CaptureRawState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CaptureRawState
     * @static
     * @param {notify.ICaptureRawState} message CaptureRawState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureRawState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CaptureRawState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CaptureRawState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CaptureRawState} CaptureRawState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureRawState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CaptureRawState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CaptureRawState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CaptureRawState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CaptureRawState} CaptureRawState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureRawState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CaptureRawState message.
     * @function verify
     * @memberof notify.CaptureRawState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CaptureRawState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a CaptureRawState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CaptureRawState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CaptureRawState} CaptureRawState
     */
    CaptureRawState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CaptureRawState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CaptureRawState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CaptureRawState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a CaptureRawState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CaptureRawState
     * @static
     * @param {notify.CaptureRawState} message CaptureRawState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CaptureRawState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this CaptureRawState to JSON.
     * @function toJSON
     * @memberof notify.CaptureRawState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CaptureRawState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CaptureRawState
     * @function getTypeUrl
     * @memberof notify.CaptureRawState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CaptureRawState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CaptureRawState";
    };

    return CaptureRawState;
  })();

  notify.PhotoState = (function () {
    /**
     * Properties of a PhotoState.
     * @memberof notify
     * @interface IPhotoState
     * @property {notify.OperationState|null} [state] PhotoState state
     * @property {number|null} [cameraType] PhotoState cameraType
     */

    /**
     * Constructs a new PhotoState.
     * @memberof notify
     * @classdesc Represents a PhotoState.
     * @implements IPhotoState
     * @constructor
     * @param {notify.IPhotoState=} [properties] Properties to set
     */
    function PhotoState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PhotoState state.
     * @member {notify.OperationState} state
     * @memberof notify.PhotoState
     * @instance
     */
    PhotoState.prototype.state = 0;

    /**
     * PhotoState cameraType.
     * @member {number} cameraType
     * @memberof notify.PhotoState
     * @instance
     */
    PhotoState.prototype.cameraType = 0;

    /**
     * Creates a new PhotoState instance using the specified properties.
     * @function create
     * @memberof notify.PhotoState
     * @static
     * @param {notify.IPhotoState=} [properties] Properties to set
     * @returns {notify.PhotoState} PhotoState instance
     */
    PhotoState.create = function create(properties) {
      return new PhotoState(properties);
    };

    /**
     * Encodes the specified PhotoState message. Does not implicitly {@link notify.PhotoState.verify|verify} messages.
     * @function encode
     * @memberof notify.PhotoState
     * @static
     * @param {notify.IPhotoState} message PhotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PhotoState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified PhotoState message, length delimited. Does not implicitly {@link notify.PhotoState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PhotoState
     * @static
     * @param {notify.IPhotoState} message PhotoState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PhotoState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PhotoState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PhotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PhotoState} PhotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PhotoState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PhotoState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PhotoState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PhotoState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PhotoState} PhotoState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PhotoState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PhotoState message.
     * @function verify
     * @memberof notify.PhotoState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PhotoState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a PhotoState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PhotoState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PhotoState} PhotoState
     */
    PhotoState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PhotoState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PhotoState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PhotoState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a PhotoState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PhotoState
     * @static
     * @param {notify.PhotoState} message PhotoState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PhotoState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this PhotoState to JSON.
     * @function toJSON
     * @memberof notify.PhotoState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PhotoState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PhotoState
     * @function getTypeUrl
     * @memberof notify.PhotoState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PhotoState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PhotoState";
    };

    return PhotoState;
  })();

  notify.BurstState = (function () {
    /**
     * Properties of a BurstState.
     * @memberof notify
     * @interface IBurstState
     * @property {notify.OperationState|null} [state] BurstState state
     * @property {number|null} [cameraType] BurstState cameraType
     */

    /**
     * Constructs a new BurstState.
     * @memberof notify
     * @classdesc Represents a BurstState.
     * @implements IBurstState
     * @constructor
     * @param {notify.IBurstState=} [properties] Properties to set
     */
    function BurstState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * BurstState state.
     * @member {notify.OperationState} state
     * @memberof notify.BurstState
     * @instance
     */
    BurstState.prototype.state = 0;

    /**
     * BurstState cameraType.
     * @member {number} cameraType
     * @memberof notify.BurstState
     * @instance
     */
    BurstState.prototype.cameraType = 0;

    /**
     * Creates a new BurstState instance using the specified properties.
     * @function create
     * @memberof notify.BurstState
     * @static
     * @param {notify.IBurstState=} [properties] Properties to set
     * @returns {notify.BurstState} BurstState instance
     */
    BurstState.create = function create(properties) {
      return new BurstState(properties);
    };

    /**
     * Encodes the specified BurstState message. Does not implicitly {@link notify.BurstState.verify|verify} messages.
     * @function encode
     * @memberof notify.BurstState
     * @static
     * @param {notify.IBurstState} message BurstState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BurstState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified BurstState message, length delimited. Does not implicitly {@link notify.BurstState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.BurstState
     * @static
     * @param {notify.IBurstState} message BurstState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BurstState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a BurstState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.BurstState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.BurstState} BurstState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BurstState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.BurstState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a BurstState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.BurstState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.BurstState} BurstState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BurstState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BurstState message.
     * @function verify
     * @memberof notify.BurstState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BurstState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a BurstState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.BurstState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.BurstState} BurstState
     */
    BurstState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.BurstState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.BurstState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.BurstState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a BurstState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.BurstState
     * @static
     * @param {notify.BurstState} message BurstState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BurstState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this BurstState to JSON.
     * @function toJSON
     * @memberof notify.BurstState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BurstState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BurstState
     * @function getTypeUrl
     * @memberof notify.BurstState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BurstState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.BurstState";
    };

    return BurstState;
  })();

  notify.RecordState = (function () {
    /**
     * Properties of a RecordState.
     * @memberof notify
     * @interface IRecordState
     * @property {notify.OperationState|null} [state] RecordState state
     * @property {number|null} [cameraType] RecordState cameraType
     */

    /**
     * Constructs a new RecordState.
     * @memberof notify
     * @classdesc Represents a RecordState.
     * @implements IRecordState
     * @constructor
     * @param {notify.IRecordState=} [properties] Properties to set
     */
    function RecordState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * RecordState state.
     * @member {notify.OperationState} state
     * @memberof notify.RecordState
     * @instance
     */
    RecordState.prototype.state = 0;

    /**
     * RecordState cameraType.
     * @member {number} cameraType
     * @memberof notify.RecordState
     * @instance
     */
    RecordState.prototype.cameraType = 0;

    /**
     * Creates a new RecordState instance using the specified properties.
     * @function create
     * @memberof notify.RecordState
     * @static
     * @param {notify.IRecordState=} [properties] Properties to set
     * @returns {notify.RecordState} RecordState instance
     */
    RecordState.create = function create(properties) {
      return new RecordState(properties);
    };

    /**
     * Encodes the specified RecordState message. Does not implicitly {@link notify.RecordState.verify|verify} messages.
     * @function encode
     * @memberof notify.RecordState
     * @static
     * @param {notify.IRecordState} message RecordState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RecordState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified RecordState message, length delimited. Does not implicitly {@link notify.RecordState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.RecordState
     * @static
     * @param {notify.IRecordState} message RecordState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RecordState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a RecordState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.RecordState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.RecordState} RecordState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RecordState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.RecordState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a RecordState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.RecordState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.RecordState} RecordState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RecordState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RecordState message.
     * @function verify
     * @memberof notify.RecordState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RecordState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a RecordState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.RecordState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.RecordState} RecordState
     */
    RecordState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.RecordState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.RecordState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.RecordState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a RecordState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.RecordState
     * @static
     * @param {notify.RecordState} message RecordState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RecordState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this RecordState to JSON.
     * @function toJSON
     * @memberof notify.RecordState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RecordState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RecordState
     * @function getTypeUrl
     * @memberof notify.RecordState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RecordState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.RecordState";
    };

    return RecordState;
  })();

  notify.TimeLapseState = (function () {
    /**
     * Properties of a TimeLapseState.
     * @memberof notify
     * @interface ITimeLapseState
     * @property {notify.OperationState|null} [state] TimeLapseState state
     * @property {number|null} [cameraType] TimeLapseState cameraType
     */

    /**
     * Constructs a new TimeLapseState.
     * @memberof notify
     * @classdesc Represents a TimeLapseState.
     * @implements ITimeLapseState
     * @constructor
     * @param {notify.ITimeLapseState=} [properties] Properties to set
     */
    function TimeLapseState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * TimeLapseState state.
     * @member {notify.OperationState} state
     * @memberof notify.TimeLapseState
     * @instance
     */
    TimeLapseState.prototype.state = 0;

    /**
     * TimeLapseState cameraType.
     * @member {number} cameraType
     * @memberof notify.TimeLapseState
     * @instance
     */
    TimeLapseState.prototype.cameraType = 0;

    /**
     * Creates a new TimeLapseState instance using the specified properties.
     * @function create
     * @memberof notify.TimeLapseState
     * @static
     * @param {notify.ITimeLapseState=} [properties] Properties to set
     * @returns {notify.TimeLapseState} TimeLapseState instance
     */
    TimeLapseState.create = function create(properties) {
      return new TimeLapseState(properties);
    };

    /**
     * Encodes the specified TimeLapseState message. Does not implicitly {@link notify.TimeLapseState.verify|verify} messages.
     * @function encode
     * @memberof notify.TimeLapseState
     * @static
     * @param {notify.ITimeLapseState} message TimeLapseState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TimeLapseState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified TimeLapseState message, length delimited. Does not implicitly {@link notify.TimeLapseState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.TimeLapseState
     * @static
     * @param {notify.ITimeLapseState} message TimeLapseState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    TimeLapseState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a TimeLapseState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.TimeLapseState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.TimeLapseState} TimeLapseState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TimeLapseState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.TimeLapseState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a TimeLapseState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.TimeLapseState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.TimeLapseState} TimeLapseState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    TimeLapseState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a TimeLapseState message.
     * @function verify
     * @memberof notify.TimeLapseState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    TimeLapseState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a TimeLapseState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.TimeLapseState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.TimeLapseState} TimeLapseState
     */
    TimeLapseState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.TimeLapseState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.TimeLapseState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.TimeLapseState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a TimeLapseState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.TimeLapseState
     * @static
     * @param {notify.TimeLapseState} message TimeLapseState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    TimeLapseState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this TimeLapseState to JSON.
     * @function toJSON
     * @memberof notify.TimeLapseState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    TimeLapseState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for TimeLapseState
     * @function getTypeUrl
     * @memberof notify.TimeLapseState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    TimeLapseState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.TimeLapseState";
    };

    return TimeLapseState;
  })();

  notify.CaptureRawDarkState = (function () {
    /**
     * Properties of a CaptureRawDarkState.
     * @memberof notify
     * @interface ICaptureRawDarkState
     * @property {notify.OperationState|null} [state] CaptureRawDarkState state
     * @property {number|null} [cameraType] CaptureRawDarkState cameraType
     */

    /**
     * Constructs a new CaptureRawDarkState.
     * @memberof notify
     * @classdesc Represents a CaptureRawDarkState.
     * @implements ICaptureRawDarkState
     * @constructor
     * @param {notify.ICaptureRawDarkState=} [properties] Properties to set
     */
    function CaptureRawDarkState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CaptureRawDarkState state.
     * @member {notify.OperationState} state
     * @memberof notify.CaptureRawDarkState
     * @instance
     */
    CaptureRawDarkState.prototype.state = 0;

    /**
     * CaptureRawDarkState cameraType.
     * @member {number} cameraType
     * @memberof notify.CaptureRawDarkState
     * @instance
     */
    CaptureRawDarkState.prototype.cameraType = 0;

    /**
     * Creates a new CaptureRawDarkState instance using the specified properties.
     * @function create
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {notify.ICaptureRawDarkState=} [properties] Properties to set
     * @returns {notify.CaptureRawDarkState} CaptureRawDarkState instance
     */
    CaptureRawDarkState.create = function create(properties) {
      return new CaptureRawDarkState(properties);
    };

    /**
     * Encodes the specified CaptureRawDarkState message. Does not implicitly {@link notify.CaptureRawDarkState.verify|verify} messages.
     * @function encode
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {notify.ICaptureRawDarkState} message CaptureRawDarkState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureRawDarkState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified CaptureRawDarkState message, length delimited. Does not implicitly {@link notify.CaptureRawDarkState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {notify.ICaptureRawDarkState} message CaptureRawDarkState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureRawDarkState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CaptureRawDarkState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CaptureRawDarkState} CaptureRawDarkState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureRawDarkState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CaptureRawDarkState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CaptureRawDarkState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CaptureRawDarkState} CaptureRawDarkState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureRawDarkState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CaptureRawDarkState message.
     * @function verify
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CaptureRawDarkState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a CaptureRawDarkState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CaptureRawDarkState} CaptureRawDarkState
     */
    CaptureRawDarkState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CaptureRawDarkState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CaptureRawDarkState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CaptureRawDarkState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a CaptureRawDarkState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {notify.CaptureRawDarkState} message CaptureRawDarkState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CaptureRawDarkState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this CaptureRawDarkState to JSON.
     * @function toJSON
     * @memberof notify.CaptureRawDarkState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CaptureRawDarkState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CaptureRawDarkState
     * @function getTypeUrl
     * @memberof notify.CaptureRawDarkState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CaptureRawDarkState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CaptureRawDarkState";
    };

    return CaptureRawDarkState;
  })();

  notify.PanoramaState = (function () {
    /**
     * Properties of a PanoramaState.
     * @memberof notify
     * @interface IPanoramaState
     * @property {notify.OperationState|null} [state] PanoramaState state
     * @property {number|null} [cameraType] PanoramaState cameraType
     */

    /**
     * Constructs a new PanoramaState.
     * @memberof notify
     * @classdesc Represents a PanoramaState.
     * @implements IPanoramaState
     * @constructor
     * @param {notify.IPanoramaState=} [properties] Properties to set
     */
    function PanoramaState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoramaState state.
     * @member {notify.OperationState} state
     * @memberof notify.PanoramaState
     * @instance
     */
    PanoramaState.prototype.state = 0;

    /**
     * PanoramaState cameraType.
     * @member {number} cameraType
     * @memberof notify.PanoramaState
     * @instance
     */
    PanoramaState.prototype.cameraType = 0;

    /**
     * Creates a new PanoramaState instance using the specified properties.
     * @function create
     * @memberof notify.PanoramaState
     * @static
     * @param {notify.IPanoramaState=} [properties] Properties to set
     * @returns {notify.PanoramaState} PanoramaState instance
     */
    PanoramaState.create = function create(properties) {
      return new PanoramaState(properties);
    };

    /**
     * Encodes the specified PanoramaState message. Does not implicitly {@link notify.PanoramaState.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoramaState
     * @static
     * @param {notify.IPanoramaState} message PanoramaState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified PanoramaState message, length delimited. Does not implicitly {@link notify.PanoramaState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoramaState
     * @static
     * @param {notify.IPanoramaState} message PanoramaState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoramaState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoramaState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoramaState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoramaState} PanoramaState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoramaState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoramaState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoramaState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoramaState} PanoramaState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoramaState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoramaState message.
     * @function verify
     * @memberof notify.PanoramaState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoramaState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a PanoramaState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoramaState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoramaState} PanoramaState
     */
    PanoramaState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoramaState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoramaState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoramaState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoramaState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoramaState
     * @static
     * @param {notify.PanoramaState} message PanoramaState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoramaState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this PanoramaState to JSON.
     * @function toJSON
     * @memberof notify.PanoramaState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoramaState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoramaState
     * @function getTypeUrl
     * @memberof notify.PanoramaState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoramaState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoramaState";
    };

    return PanoramaState;
  })();

  notify.AstroAutoFocusState = (function () {
    /**
     * Properties of an AstroAutoFocusState.
     * @memberof notify
     * @interface IAstroAutoFocusState
     * @property {notify.OperationState|null} [state] AstroAutoFocusState state
     */

    /**
     * Constructs a new AstroAutoFocusState.
     * @memberof notify
     * @classdesc Represents an AstroAutoFocusState.
     * @implements IAstroAutoFocusState
     * @constructor
     * @param {notify.IAstroAutoFocusState=} [properties] Properties to set
     */
    function AstroAutoFocusState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroAutoFocusState state.
     * @member {notify.OperationState} state
     * @memberof notify.AstroAutoFocusState
     * @instance
     */
    AstroAutoFocusState.prototype.state = 0;

    /**
     * Creates a new AstroAutoFocusState instance using the specified properties.
     * @function create
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {notify.IAstroAutoFocusState=} [properties] Properties to set
     * @returns {notify.AstroAutoFocusState} AstroAutoFocusState instance
     */
    AstroAutoFocusState.create = function create(properties) {
      return new AstroAutoFocusState(properties);
    };

    /**
     * Encodes the specified AstroAutoFocusState message. Does not implicitly {@link notify.AstroAutoFocusState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {notify.IAstroAutoFocusState} message AstroAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroAutoFocusState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified AstroAutoFocusState message, length delimited. Does not implicitly {@link notify.AstroAutoFocusState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {notify.IAstroAutoFocusState} message AstroAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroAutoFocusState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroAutoFocusState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroAutoFocusState} AstroAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroAutoFocusState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroAutoFocusState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroAutoFocusState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroAutoFocusState} AstroAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroAutoFocusState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroAutoFocusState message.
     * @function verify
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroAutoFocusState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates an AstroAutoFocusState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroAutoFocusState} AstroAutoFocusState
     */
    AstroAutoFocusState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroAutoFocusState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroAutoFocusState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroAutoFocusState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from an AstroAutoFocusState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {notify.AstroAutoFocusState} message AstroAutoFocusState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroAutoFocusState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this AstroAutoFocusState to JSON.
     * @function toJSON
     * @memberof notify.AstroAutoFocusState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroAutoFocusState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroAutoFocusState
     * @function getTypeUrl
     * @memberof notify.AstroAutoFocusState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroAutoFocusState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroAutoFocusState";
    };

    return AstroAutoFocusState;
  })();

  notify.NormalAutoFocusState = (function () {
    /**
     * Properties of a NormalAutoFocusState.
     * @memberof notify
     * @interface INormalAutoFocusState
     * @property {notify.OperationState|null} [state] NormalAutoFocusState state
     */

    /**
     * Constructs a new NormalAutoFocusState.
     * @memberof notify
     * @classdesc Represents a NormalAutoFocusState.
     * @implements INormalAutoFocusState
     * @constructor
     * @param {notify.INormalAutoFocusState=} [properties] Properties to set
     */
    function NormalAutoFocusState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * NormalAutoFocusState state.
     * @member {notify.OperationState} state
     * @memberof notify.NormalAutoFocusState
     * @instance
     */
    NormalAutoFocusState.prototype.state = 0;

    /**
     * Creates a new NormalAutoFocusState instance using the specified properties.
     * @function create
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {notify.INormalAutoFocusState=} [properties] Properties to set
     * @returns {notify.NormalAutoFocusState} NormalAutoFocusState instance
     */
    NormalAutoFocusState.create = function create(properties) {
      return new NormalAutoFocusState(properties);
    };

    /**
     * Encodes the specified NormalAutoFocusState message. Does not implicitly {@link notify.NormalAutoFocusState.verify|verify} messages.
     * @function encode
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {notify.INormalAutoFocusState} message NormalAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NormalAutoFocusState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified NormalAutoFocusState message, length delimited. Does not implicitly {@link notify.NormalAutoFocusState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {notify.INormalAutoFocusState} message NormalAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NormalAutoFocusState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a NormalAutoFocusState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.NormalAutoFocusState} NormalAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NormalAutoFocusState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.NormalAutoFocusState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a NormalAutoFocusState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.NormalAutoFocusState} NormalAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NormalAutoFocusState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NormalAutoFocusState message.
     * @function verify
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NormalAutoFocusState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates a NormalAutoFocusState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.NormalAutoFocusState} NormalAutoFocusState
     */
    NormalAutoFocusState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.NormalAutoFocusState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.NormalAutoFocusState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.NormalAutoFocusState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a NormalAutoFocusState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {notify.NormalAutoFocusState} message NormalAutoFocusState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NormalAutoFocusState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this NormalAutoFocusState to JSON.
     * @function toJSON
     * @memberof notify.NormalAutoFocusState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NormalAutoFocusState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NormalAutoFocusState
     * @function getTypeUrl
     * @memberof notify.NormalAutoFocusState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NormalAutoFocusState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.NormalAutoFocusState";
    };

    return NormalAutoFocusState;
  })();

  notify.AstroAutoFocusFastState = (function () {
    /**
     * Properties of an AstroAutoFocusFastState.
     * @memberof notify
     * @interface IAstroAutoFocusFastState
     * @property {notify.OperationState|null} [state] AstroAutoFocusFastState state
     */

    /**
     * Constructs a new AstroAutoFocusFastState.
     * @memberof notify
     * @classdesc Represents an AstroAutoFocusFastState.
     * @implements IAstroAutoFocusFastState
     * @constructor
     * @param {notify.IAstroAutoFocusFastState=} [properties] Properties to set
     */
    function AstroAutoFocusFastState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AstroAutoFocusFastState state.
     * @member {notify.OperationState} state
     * @memberof notify.AstroAutoFocusFastState
     * @instance
     */
    AstroAutoFocusFastState.prototype.state = 0;

    /**
     * Creates a new AstroAutoFocusFastState instance using the specified properties.
     * @function create
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {notify.IAstroAutoFocusFastState=} [properties] Properties to set
     * @returns {notify.AstroAutoFocusFastState} AstroAutoFocusFastState instance
     */
    AstroAutoFocusFastState.create = function create(properties) {
      return new AstroAutoFocusFastState(properties);
    };

    /**
     * Encodes the specified AstroAutoFocusFastState message. Does not implicitly {@link notify.AstroAutoFocusFastState.verify|verify} messages.
     * @function encode
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {notify.IAstroAutoFocusFastState} message AstroAutoFocusFastState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroAutoFocusFastState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified AstroAutoFocusFastState message, length delimited. Does not implicitly {@link notify.AstroAutoFocusFastState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {notify.IAstroAutoFocusFastState} message AstroAutoFocusFastState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AstroAutoFocusFastState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AstroAutoFocusFastState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AstroAutoFocusFastState} AstroAutoFocusFastState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroAutoFocusFastState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AstroAutoFocusFastState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AstroAutoFocusFastState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AstroAutoFocusFastState} AstroAutoFocusFastState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AstroAutoFocusFastState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AstroAutoFocusFastState message.
     * @function verify
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AstroAutoFocusFastState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates an AstroAutoFocusFastState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AstroAutoFocusFastState} AstroAutoFocusFastState
     */
    AstroAutoFocusFastState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AstroAutoFocusFastState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AstroAutoFocusFastState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AstroAutoFocusFastState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from an AstroAutoFocusFastState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {notify.AstroAutoFocusFastState} message AstroAutoFocusFastState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AstroAutoFocusFastState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this AstroAutoFocusFastState to JSON.
     * @function toJSON
     * @memberof notify.AstroAutoFocusFastState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AstroAutoFocusFastState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AstroAutoFocusFastState
     * @function getTypeUrl
     * @memberof notify.AstroAutoFocusFastState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AstroAutoFocusFastState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AstroAutoFocusFastState";
    };

    return AstroAutoFocusFastState;
  })();

  notify.AreaAutoFocusState = (function () {
    /**
     * Properties of an AreaAutoFocusState.
     * @memberof notify
     * @interface IAreaAutoFocusState
     * @property {notify.OperationState|null} [state] AreaAutoFocusState state
     */

    /**
     * Constructs a new AreaAutoFocusState.
     * @memberof notify
     * @classdesc Represents an AreaAutoFocusState.
     * @implements IAreaAutoFocusState
     * @constructor
     * @param {notify.IAreaAutoFocusState=} [properties] Properties to set
     */
    function AreaAutoFocusState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AreaAutoFocusState state.
     * @member {notify.OperationState} state
     * @memberof notify.AreaAutoFocusState
     * @instance
     */
    AreaAutoFocusState.prototype.state = 0;

    /**
     * Creates a new AreaAutoFocusState instance using the specified properties.
     * @function create
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {notify.IAreaAutoFocusState=} [properties] Properties to set
     * @returns {notify.AreaAutoFocusState} AreaAutoFocusState instance
     */
    AreaAutoFocusState.create = function create(properties) {
      return new AreaAutoFocusState(properties);
    };

    /**
     * Encodes the specified AreaAutoFocusState message. Does not implicitly {@link notify.AreaAutoFocusState.verify|verify} messages.
     * @function encode
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {notify.IAreaAutoFocusState} message AreaAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AreaAutoFocusState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified AreaAutoFocusState message, length delimited. Does not implicitly {@link notify.AreaAutoFocusState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {notify.IAreaAutoFocusState} message AreaAutoFocusState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AreaAutoFocusState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AreaAutoFocusState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AreaAutoFocusState} AreaAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AreaAutoFocusState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AreaAutoFocusState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AreaAutoFocusState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AreaAutoFocusState} AreaAutoFocusState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AreaAutoFocusState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AreaAutoFocusState message.
     * @function verify
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AreaAutoFocusState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates an AreaAutoFocusState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AreaAutoFocusState} AreaAutoFocusState
     */
    AreaAutoFocusState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AreaAutoFocusState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AreaAutoFocusState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AreaAutoFocusState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from an AreaAutoFocusState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {notify.AreaAutoFocusState} message AreaAutoFocusState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AreaAutoFocusState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this AreaAutoFocusState to JSON.
     * @function toJSON
     * @memberof notify.AreaAutoFocusState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AreaAutoFocusState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AreaAutoFocusState
     * @function getTypeUrl
     * @memberof notify.AreaAutoFocusState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AreaAutoFocusState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AreaAutoFocusState";
    };

    return AreaAutoFocusState;
  })();

  notify.DualCameraLinkageState = (function () {
    /**
     * Properties of a DualCameraLinkageState.
     * @memberof notify
     * @interface IDualCameraLinkageState
     * @property {notify.OperationState|null} [state] DualCameraLinkageState state
     */

    /**
     * Constructs a new DualCameraLinkageState.
     * @memberof notify
     * @classdesc Represents a DualCameraLinkageState.
     * @implements IDualCameraLinkageState
     * @constructor
     * @param {notify.IDualCameraLinkageState=} [properties] Properties to set
     */
    function DualCameraLinkageState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * DualCameraLinkageState state.
     * @member {notify.OperationState} state
     * @memberof notify.DualCameraLinkageState
     * @instance
     */
    DualCameraLinkageState.prototype.state = 0;

    /**
     * Creates a new DualCameraLinkageState instance using the specified properties.
     * @function create
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {notify.IDualCameraLinkageState=} [properties] Properties to set
     * @returns {notify.DualCameraLinkageState} DualCameraLinkageState instance
     */
    DualCameraLinkageState.create = function create(properties) {
      return new DualCameraLinkageState(properties);
    };

    /**
     * Encodes the specified DualCameraLinkageState message. Does not implicitly {@link notify.DualCameraLinkageState.verify|verify} messages.
     * @function encode
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {notify.IDualCameraLinkageState} message DualCameraLinkageState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DualCameraLinkageState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified DualCameraLinkageState message, length delimited. Does not implicitly {@link notify.DualCameraLinkageState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {notify.IDualCameraLinkageState} message DualCameraLinkageState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DualCameraLinkageState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a DualCameraLinkageState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.DualCameraLinkageState} DualCameraLinkageState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DualCameraLinkageState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.DualCameraLinkageState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a DualCameraLinkageState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.DualCameraLinkageState} DualCameraLinkageState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DualCameraLinkageState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DualCameraLinkageState message.
     * @function verify
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DualCameraLinkageState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      return null;
    };

    /**
     * Creates a DualCameraLinkageState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.DualCameraLinkageState} DualCameraLinkageState
     */
    DualCameraLinkageState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.DualCameraLinkageState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.DualCameraLinkageState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.DualCameraLinkageState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a DualCameraLinkageState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {notify.DualCameraLinkageState} message DualCameraLinkageState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DualCameraLinkageState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      return object;
    };

    /**
     * Converts this DualCameraLinkageState to JSON.
     * @function toJSON
     * @memberof notify.DualCameraLinkageState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DualCameraLinkageState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DualCameraLinkageState
     * @function getTypeUrl
     * @memberof notify.DualCameraLinkageState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DualCameraLinkageState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.DualCameraLinkageState";
    };

    return DualCameraLinkageState;
  })();

  notify.NormalTrackState = (function () {
    /**
     * Properties of a NormalTrackState.
     * @memberof notify
     * @interface INormalTrackState
     * @property {notify.OperationState|null} [state] NormalTrackState state
     * @property {number|null} [cameraType] NormalTrackState cameraType
     */

    /**
     * Constructs a new NormalTrackState.
     * @memberof notify
     * @classdesc Represents a NormalTrackState.
     * @implements INormalTrackState
     * @constructor
     * @param {notify.INormalTrackState=} [properties] Properties to set
     */
    function NormalTrackState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * NormalTrackState state.
     * @member {notify.OperationState} state
     * @memberof notify.NormalTrackState
     * @instance
     */
    NormalTrackState.prototype.state = 0;

    /**
     * NormalTrackState cameraType.
     * @member {number} cameraType
     * @memberof notify.NormalTrackState
     * @instance
     */
    NormalTrackState.prototype.cameraType = 0;

    /**
     * Creates a new NormalTrackState instance using the specified properties.
     * @function create
     * @memberof notify.NormalTrackState
     * @static
     * @param {notify.INormalTrackState=} [properties] Properties to set
     * @returns {notify.NormalTrackState} NormalTrackState instance
     */
    NormalTrackState.create = function create(properties) {
      return new NormalTrackState(properties);
    };

    /**
     * Encodes the specified NormalTrackState message. Does not implicitly {@link notify.NormalTrackState.verify|verify} messages.
     * @function encode
     * @memberof notify.NormalTrackState
     * @static
     * @param {notify.INormalTrackState} message NormalTrackState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NormalTrackState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified NormalTrackState message, length delimited. Does not implicitly {@link notify.NormalTrackState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.NormalTrackState
     * @static
     * @param {notify.INormalTrackState} message NormalTrackState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NormalTrackState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a NormalTrackState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.NormalTrackState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.NormalTrackState} NormalTrackState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NormalTrackState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.NormalTrackState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a NormalTrackState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.NormalTrackState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.NormalTrackState} NormalTrackState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NormalTrackState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NormalTrackState message.
     * @function verify
     * @memberof notify.NormalTrackState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NormalTrackState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a NormalTrackState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.NormalTrackState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.NormalTrackState} NormalTrackState
     */
    NormalTrackState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.NormalTrackState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.NormalTrackState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.NormalTrackState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a NormalTrackState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.NormalTrackState
     * @static
     * @param {notify.NormalTrackState} message NormalTrackState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NormalTrackState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this NormalTrackState to JSON.
     * @function toJSON
     * @memberof notify.NormalTrackState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NormalTrackState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NormalTrackState
     * @function getTypeUrl
     * @memberof notify.NormalTrackState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NormalTrackState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.NormalTrackState";
    };

    return NormalTrackState;
  })();

  notify.SwitchResolutionFpsState = (function () {
    /**
     * Properties of a SwitchResolutionFpsState.
     * @memberof notify
     * @interface ISwitchResolutionFpsState
     * @property {notify.OperationState|null} [state] SwitchResolutionFpsState state
     * @property {number|null} [cameraType] SwitchResolutionFpsState cameraType
     */

    /**
     * Constructs a new SwitchResolutionFpsState.
     * @memberof notify
     * @classdesc Represents a SwitchResolutionFpsState.
     * @implements ISwitchResolutionFpsState
     * @constructor
     * @param {notify.ISwitchResolutionFpsState=} [properties] Properties to set
     */
    function SwitchResolutionFpsState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SwitchResolutionFpsState state.
     * @member {notify.OperationState} state
     * @memberof notify.SwitchResolutionFpsState
     * @instance
     */
    SwitchResolutionFpsState.prototype.state = 0;

    /**
     * SwitchResolutionFpsState cameraType.
     * @member {number} cameraType
     * @memberof notify.SwitchResolutionFpsState
     * @instance
     */
    SwitchResolutionFpsState.prototype.cameraType = 0;

    /**
     * Creates a new SwitchResolutionFpsState instance using the specified properties.
     * @function create
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {notify.ISwitchResolutionFpsState=} [properties] Properties to set
     * @returns {notify.SwitchResolutionFpsState} SwitchResolutionFpsState instance
     */
    SwitchResolutionFpsState.create = function create(properties) {
      return new SwitchResolutionFpsState(properties);
    };

    /**
     * Encodes the specified SwitchResolutionFpsState message. Does not implicitly {@link notify.SwitchResolutionFpsState.verify|verify} messages.
     * @function encode
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {notify.ISwitchResolutionFpsState} message SwitchResolutionFpsState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchResolutionFpsState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      return writer;
    };

    /**
     * Encodes the specified SwitchResolutionFpsState message, length delimited. Does not implicitly {@link notify.SwitchResolutionFpsState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {notify.ISwitchResolutionFpsState} message SwitchResolutionFpsState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SwitchResolutionFpsState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SwitchResolutionFpsState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SwitchResolutionFpsState} SwitchResolutionFpsState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchResolutionFpsState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SwitchResolutionFpsState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SwitchResolutionFpsState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SwitchResolutionFpsState} SwitchResolutionFpsState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SwitchResolutionFpsState.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SwitchResolutionFpsState message.
     * @function verify
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SwitchResolutionFpsState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      return null;
    };

    /**
     * Creates a SwitchResolutionFpsState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SwitchResolutionFpsState} SwitchResolutionFpsState
     */
    SwitchResolutionFpsState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SwitchResolutionFpsState)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SwitchResolutionFpsState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SwitchResolutionFpsState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      return message;
    };

    /**
     * Creates a plain object from a SwitchResolutionFpsState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {notify.SwitchResolutionFpsState} message SwitchResolutionFpsState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SwitchResolutionFpsState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      return object;
    };

    /**
     * Converts this SwitchResolutionFpsState to JSON.
     * @function toJSON
     * @memberof notify.SwitchResolutionFpsState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SwitchResolutionFpsState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SwitchResolutionFpsState
     * @function getTypeUrl
     * @memberof notify.SwitchResolutionFpsState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SwitchResolutionFpsState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SwitchResolutionFpsState";
    };

    return SwitchResolutionFpsState;
  })();

  notify.CaptureCaliFrameState = (function () {
    /**
     * Properties of a CaptureCaliFrameState.
     * @memberof notify
     * @interface ICaptureCaliFrameState
     * @property {notify.OperationState|null} [state] CaptureCaliFrameState state
     * @property {number|null} [cameraType] CaptureCaliFrameState cameraType
     * @property {number|null} [caliFrameType] CaptureCaliFrameState caliFrameType
     */

    /**
     * Constructs a new CaptureCaliFrameState.
     * @memberof notify
     * @classdesc Represents a CaptureCaliFrameState.
     * @implements ICaptureCaliFrameState
     * @constructor
     * @param {notify.ICaptureCaliFrameState=} [properties] Properties to set
     */
    function CaptureCaliFrameState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CaptureCaliFrameState state.
     * @member {notify.OperationState} state
     * @memberof notify.CaptureCaliFrameState
     * @instance
     */
    CaptureCaliFrameState.prototype.state = 0;

    /**
     * CaptureCaliFrameState cameraType.
     * @member {number} cameraType
     * @memberof notify.CaptureCaliFrameState
     * @instance
     */
    CaptureCaliFrameState.prototype.cameraType = 0;

    /**
     * CaptureCaliFrameState caliFrameType.
     * @member {number} caliFrameType
     * @memberof notify.CaptureCaliFrameState
     * @instance
     */
    CaptureCaliFrameState.prototype.caliFrameType = 0;

    /**
     * Creates a new CaptureCaliFrameState instance using the specified properties.
     * @function create
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {notify.ICaptureCaliFrameState=} [properties] Properties to set
     * @returns {notify.CaptureCaliFrameState} CaptureCaliFrameState instance
     */
    CaptureCaliFrameState.create = function create(properties) {
      return new CaptureCaliFrameState(properties);
    };

    /**
     * Encodes the specified CaptureCaliFrameState message. Does not implicitly {@link notify.CaptureCaliFrameState.verify|verify} messages.
     * @function encode
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {notify.ICaptureCaliFrameState} message CaptureCaliFrameState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureCaliFrameState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.caliFrameType);
      return writer;
    };

    /**
     * Encodes the specified CaptureCaliFrameState message, length delimited. Does not implicitly {@link notify.CaptureCaliFrameState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {notify.ICaptureCaliFrameState} message CaptureCaliFrameState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureCaliFrameState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CaptureCaliFrameState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CaptureCaliFrameState} CaptureCaliFrameState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureCaliFrameState.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CaptureCaliFrameState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          case 3: {
            message.caliFrameType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CaptureCaliFrameState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CaptureCaliFrameState} CaptureCaliFrameState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureCaliFrameState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CaptureCaliFrameState message.
     * @function verify
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CaptureCaliFrameState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        if (!$util.isInteger(message.caliFrameType))
          return "caliFrameType: integer expected";
      return null;
    };

    /**
     * Creates a CaptureCaliFrameState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CaptureCaliFrameState} CaptureCaliFrameState
     */
    CaptureCaliFrameState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CaptureCaliFrameState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CaptureCaliFrameState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CaptureCaliFrameState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      if (object.caliFrameType != null)
        message.caliFrameType = object.caliFrameType | 0;
      return message;
    };

    /**
     * Creates a plain object from a CaptureCaliFrameState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {notify.CaptureCaliFrameState} message CaptureCaliFrameState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CaptureCaliFrameState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.cameraType = 0;
        object.caliFrameType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        object.caliFrameType = message.caliFrameType;
      return object;
    };

    /**
     * Converts this CaptureCaliFrameState to JSON.
     * @function toJSON
     * @memberof notify.CaptureCaliFrameState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CaptureCaliFrameState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CaptureCaliFrameState
     * @function getTypeUrl
     * @memberof notify.CaptureCaliFrameState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CaptureCaliFrameState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CaptureCaliFrameState";
    };

    return CaptureCaliFrameState;
  })();

  notify.CaptureCaliFrameProgress = (function () {
    /**
     * Properties of a CaptureCaliFrameProgress.
     * @memberof notify
     * @interface ICaptureCaliFrameProgress
     * @property {number|null} [progress] CaptureCaliFrameProgress progress
     * @property {number|null} [cameraType] CaptureCaliFrameProgress cameraType
     * @property {number|null} [caliFrameType] CaptureCaliFrameProgress caliFrameType
     */

    /**
     * Constructs a new CaptureCaliFrameProgress.
     * @memberof notify
     * @classdesc Represents a CaptureCaliFrameProgress.
     * @implements ICaptureCaliFrameProgress
     * @constructor
     * @param {notify.ICaptureCaliFrameProgress=} [properties] Properties to set
     */
    function CaptureCaliFrameProgress(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * CaptureCaliFrameProgress progress.
     * @member {number} progress
     * @memberof notify.CaptureCaliFrameProgress
     * @instance
     */
    CaptureCaliFrameProgress.prototype.progress = 0;

    /**
     * CaptureCaliFrameProgress cameraType.
     * @member {number} cameraType
     * @memberof notify.CaptureCaliFrameProgress
     * @instance
     */
    CaptureCaliFrameProgress.prototype.cameraType = 0;

    /**
     * CaptureCaliFrameProgress caliFrameType.
     * @member {number} caliFrameType
     * @memberof notify.CaptureCaliFrameProgress
     * @instance
     */
    CaptureCaliFrameProgress.prototype.caliFrameType = 0;

    /**
     * Creates a new CaptureCaliFrameProgress instance using the specified properties.
     * @function create
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {notify.ICaptureCaliFrameProgress=} [properties] Properties to set
     * @returns {notify.CaptureCaliFrameProgress} CaptureCaliFrameProgress instance
     */
    CaptureCaliFrameProgress.create = function create(properties) {
      return new CaptureCaliFrameProgress(properties);
    };

    /**
     * Encodes the specified CaptureCaliFrameProgress message. Does not implicitly {@link notify.CaptureCaliFrameProgress.verify|verify} messages.
     * @function encode
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {notify.ICaptureCaliFrameProgress} message CaptureCaliFrameProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureCaliFrameProgress.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.progress);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.caliFrameType);
      return writer;
    };

    /**
     * Encodes the specified CaptureCaliFrameProgress message, length delimited. Does not implicitly {@link notify.CaptureCaliFrameProgress.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {notify.ICaptureCaliFrameProgress} message CaptureCaliFrameProgress message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CaptureCaliFrameProgress.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a CaptureCaliFrameProgress message from the specified reader or buffer.
     * @function decode
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.CaptureCaliFrameProgress} CaptureCaliFrameProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureCaliFrameProgress.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.CaptureCaliFrameProgress();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.progress = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          case 3: {
            message.caliFrameType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a CaptureCaliFrameProgress message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.CaptureCaliFrameProgress} CaptureCaliFrameProgress
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CaptureCaliFrameProgress.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CaptureCaliFrameProgress message.
     * @function verify
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CaptureCaliFrameProgress.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        if (!$util.isInteger(message.progress))
          return "progress: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        if (!$util.isInteger(message.caliFrameType))
          return "caliFrameType: integer expected";
      return null;
    };

    /**
     * Creates a CaptureCaliFrameProgress message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.CaptureCaliFrameProgress} CaptureCaliFrameProgress
     */
    CaptureCaliFrameProgress.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.CaptureCaliFrameProgress)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.CaptureCaliFrameProgress: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.CaptureCaliFrameProgress();
      if (object.progress != null) message.progress = object.progress | 0;
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      if (object.caliFrameType != null)
        message.caliFrameType = object.caliFrameType | 0;
      return message;
    };

    /**
     * Creates a plain object from a CaptureCaliFrameProgress message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {notify.CaptureCaliFrameProgress} message CaptureCaliFrameProgress
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CaptureCaliFrameProgress.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.progress = 0;
        object.cameraType = 0;
        object.caliFrameType = 0;
      }
      if (
        message.progress != null &&
        Object.hasOwnProperty.call(message, "progress")
      )
        object.progress = message.progress;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      if (
        message.caliFrameType != null &&
        Object.hasOwnProperty.call(message, "caliFrameType")
      )
        object.caliFrameType = message.caliFrameType;
      return object;
    };

    /**
     * Converts this CaptureCaliFrameProgress to JSON.
     * @function toJSON
     * @memberof notify.CaptureCaliFrameProgress
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CaptureCaliFrameProgress.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CaptureCaliFrameProgress
     * @function getTypeUrl
     * @memberof notify.CaptureCaliFrameProgress
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CaptureCaliFrameProgress.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.CaptureCaliFrameProgress";
    };

    return CaptureCaliFrameProgress;
  })();

  notify.DeviceAttitude = (function () {
    /**
     * Properties of a DeviceAttitude.
     * @memberof notify
     * @interface IDeviceAttitude
     * @property {number|null} [pitch] DeviceAttitude pitch
     * @property {number|null} [yaw] DeviceAttitude yaw
     * @property {number|null} [roll] DeviceAttitude roll
     */

    /**
     * Constructs a new DeviceAttitude.
     * @memberof notify
     * @classdesc Represents a DeviceAttitude.
     * @implements IDeviceAttitude
     * @constructor
     * @param {notify.IDeviceAttitude=} [properties] Properties to set
     */
    function DeviceAttitude(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * DeviceAttitude pitch.
     * @member {number} pitch
     * @memberof notify.DeviceAttitude
     * @instance
     */
    DeviceAttitude.prototype.pitch = 0;

    /**
     * DeviceAttitude yaw.
     * @member {number} yaw
     * @memberof notify.DeviceAttitude
     * @instance
     */
    DeviceAttitude.prototype.yaw = 0;

    /**
     * DeviceAttitude roll.
     * @member {number} roll
     * @memberof notify.DeviceAttitude
     * @instance
     */
    DeviceAttitude.prototype.roll = 0;

    /**
     * Creates a new DeviceAttitude instance using the specified properties.
     * @function create
     * @memberof notify.DeviceAttitude
     * @static
     * @param {notify.IDeviceAttitude=} [properties] Properties to set
     * @returns {notify.DeviceAttitude} DeviceAttitude instance
     */
    DeviceAttitude.create = function create(properties) {
      return new DeviceAttitude(properties);
    };

    /**
     * Encodes the specified DeviceAttitude message. Does not implicitly {@link notify.DeviceAttitude.verify|verify} messages.
     * @function encode
     * @memberof notify.DeviceAttitude
     * @static
     * @param {notify.IDeviceAttitude} message DeviceAttitude message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DeviceAttitude.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
        writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.pitch);
      if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
        writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.yaw);
      if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
        writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.roll);
      return writer;
    };

    /**
     * Encodes the specified DeviceAttitude message, length delimited. Does not implicitly {@link notify.DeviceAttitude.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.DeviceAttitude
     * @static
     * @param {notify.IDeviceAttitude} message DeviceAttitude message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DeviceAttitude.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a DeviceAttitude message from the specified reader or buffer.
     * @function decode
     * @memberof notify.DeviceAttitude
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.DeviceAttitude} DeviceAttitude
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DeviceAttitude.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.DeviceAttitude();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.pitch = reader.double();
            break;
          }
          case 2: {
            message.yaw = reader.double();
            break;
          }
          case 3: {
            message.roll = reader.double();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a DeviceAttitude message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.DeviceAttitude
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.DeviceAttitude} DeviceAttitude
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DeviceAttitude.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DeviceAttitude message.
     * @function verify
     * @memberof notify.DeviceAttitude
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DeviceAttitude.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
        if (typeof message.pitch !== "number") return "pitch: number expected";
      if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
        if (typeof message.yaw !== "number") return "yaw: number expected";
      if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
        if (typeof message.roll !== "number") return "roll: number expected";
      return null;
    };

    /**
     * Creates a DeviceAttitude message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.DeviceAttitude
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.DeviceAttitude} DeviceAttitude
     */
    DeviceAttitude.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.DeviceAttitude) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.DeviceAttitude: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.DeviceAttitude();
      if (object.pitch != null) message.pitch = Number(object.pitch);
      if (object.yaw != null) message.yaw = Number(object.yaw);
      if (object.roll != null) message.roll = Number(object.roll);
      return message;
    };

    /**
     * Creates a plain object from a DeviceAttitude message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.DeviceAttitude
     * @static
     * @param {notify.DeviceAttitude} message DeviceAttitude
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DeviceAttitude.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.pitch = 0;
        object.yaw = 0;
        object.roll = 0;
      }
      if (message.pitch != null && Object.hasOwnProperty.call(message, "pitch"))
        object.pitch =
          options.json && !isFinite(message.pitch)
            ? String(message.pitch)
            : message.pitch;
      if (message.yaw != null && Object.hasOwnProperty.call(message, "yaw"))
        object.yaw =
          options.json && !isFinite(message.yaw)
            ? String(message.yaw)
            : message.yaw;
      if (message.roll != null && Object.hasOwnProperty.call(message, "roll"))
        object.roll =
          options.json && !isFinite(message.roll)
            ? String(message.roll)
            : message.roll;
      return object;
    };

    /**
     * Converts this DeviceAttitude to JSON.
     * @function toJSON
     * @memberof notify.DeviceAttitude
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DeviceAttitude.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DeviceAttitude
     * @function getTypeUrl
     * @memberof notify.DeviceAttitude
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DeviceAttitude.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.DeviceAttitude";
    };

    return DeviceAttitude;
  })();

  notify.SkyTargetFinderState = (function () {
    /**
     * Properties of a SkyTargetFinderState.
     * @memberof notify
     * @interface ISkyTargetFinderState
     * @property {notify.OperationState|null} [state] SkyTargetFinderState state
     * @property {number|null} [sceneType] SkyTargetFinderState sceneType
     */

    /**
     * Constructs a new SkyTargetFinderState.
     * @memberof notify
     * @classdesc Represents a SkyTargetFinderState.
     * @implements ISkyTargetFinderState
     * @constructor
     * @param {notify.ISkyTargetFinderState=} [properties] Properties to set
     */
    function SkyTargetFinderState(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * SkyTargetFinderState state.
     * @member {notify.OperationState} state
     * @memberof notify.SkyTargetFinderState
     * @instance
     */
    SkyTargetFinderState.prototype.state = 0;

    /**
     * SkyTargetFinderState sceneType.
     * @member {number} sceneType
     * @memberof notify.SkyTargetFinderState
     * @instance
     */
    SkyTargetFinderState.prototype.sceneType = 0;

    /**
     * Creates a new SkyTargetFinderState instance using the specified properties.
     * @function create
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {notify.ISkyTargetFinderState=} [properties] Properties to set
     * @returns {notify.SkyTargetFinderState} SkyTargetFinderState instance
     */
    SkyTargetFinderState.create = function create(properties) {
      return new SkyTargetFinderState(properties);
    };

    /**
     * Encodes the specified SkyTargetFinderState message. Does not implicitly {@link notify.SkyTargetFinderState.verify|verify} messages.
     * @function encode
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {notify.ISkyTargetFinderState} message SkyTargetFinderState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SkyTargetFinderState.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      if (
        message.sceneType != null &&
        Object.hasOwnProperty.call(message, "sceneType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.sceneType);
      return writer;
    };

    /**
     * Encodes the specified SkyTargetFinderState message, length delimited. Does not implicitly {@link notify.SkyTargetFinderState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {notify.ISkyTargetFinderState} message SkyTargetFinderState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SkyTargetFinderState.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a SkyTargetFinderState message from the specified reader or buffer.
     * @function decode
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.SkyTargetFinderState} SkyTargetFinderState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SkyTargetFinderState.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.SkyTargetFinderState();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          case 2: {
            message.sceneType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a SkyTargetFinderState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.SkyTargetFinderState} SkyTargetFinderState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SkyTargetFinderState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SkyTargetFinderState message.
     * @function verify
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SkyTargetFinderState.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        switch (message.state) {
          default:
            return "state: enum value expected";
          case 0:
          case 1:
          case 2:
          case 3:
            break;
        }
      if (
        message.sceneType != null &&
        Object.hasOwnProperty.call(message, "sceneType")
      )
        if (!$util.isInteger(message.sceneType))
          return "sceneType: integer expected";
      return null;
    };

    /**
     * Creates a SkyTargetFinderState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.SkyTargetFinderState} SkyTargetFinderState
     */
    SkyTargetFinderState.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.SkyTargetFinderState) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.SkyTargetFinderState: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.SkyTargetFinderState();
      switch (object.state) {
        default:
          if (typeof object.state === "number") {
            message.state = object.state;
            break;
          }
          break;
        case "OPERATION_STATE_IDLE":
        case 0:
          message.state = 0;
          break;
        case "OPERATION_STATE_RUNNING":
        case 1:
          message.state = 1;
          break;
        case "OPERATION_STATE_STOPPING":
        case 2:
          message.state = 2;
          break;
        case "OPERATION_STATE_STOPPED":
        case 3:
          message.state = 3;
          break;
      }
      if (object.sceneType != null) message.sceneType = object.sceneType | 0;
      return message;
    };

    /**
     * Creates a plain object from a SkyTargetFinderState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {notify.SkyTargetFinderState} message SkyTargetFinderState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SkyTargetFinderState.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.state = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
        object.sceneType = 0;
      }
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state =
          options.enums === String
            ? $root.notify.OperationState[message.state] === undefined
              ? message.state
              : $root.notify.OperationState[message.state]
            : message.state;
      if (
        message.sceneType != null &&
        Object.hasOwnProperty.call(message, "sceneType")
      )
        object.sceneType = message.sceneType;
      return object;
    };

    /**
     * Converts this SkyTargetFinderState to JSON.
     * @function toJSON
     * @memberof notify.SkyTargetFinderState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SkyTargetFinderState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SkyTargetFinderState
     * @function getTypeUrl
     * @memberof notify.SkyTargetFinderState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SkyTargetFinderState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.SkyTargetFinderState";
    };

    return SkyTargetFinderState;
  })();

  notify.PanoFramingThumbnailUpdateNotify = (function () {
    /**
     * Properties of a PanoFramingThumbnailUpdateNotify.
     * @memberof notify
     * @interface IPanoFramingThumbnailUpdateNotify
     * @property {Uint8Array|null} [webpData] PanoFramingThumbnailUpdateNotify webpData
     */

    /**
     * Constructs a new PanoFramingThumbnailUpdateNotify.
     * @memberof notify
     * @classdesc Represents a PanoFramingThumbnailUpdateNotify.
     * @implements IPanoFramingThumbnailUpdateNotify
     * @constructor
     * @param {notify.IPanoFramingThumbnailUpdateNotify=} [properties] Properties to set
     */
    function PanoFramingThumbnailUpdateNotify(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoFramingThumbnailUpdateNotify webpData.
     * @member {Uint8Array} webpData
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @instance
     */
    PanoFramingThumbnailUpdateNotify.prototype.webpData = $util.newBuffer([]);

    /**
     * Creates a new PanoFramingThumbnailUpdateNotify instance using the specified properties.
     * @function create
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {notify.IPanoFramingThumbnailUpdateNotify=} [properties] Properties to set
     * @returns {notify.PanoFramingThumbnailUpdateNotify} PanoFramingThumbnailUpdateNotify instance
     */
    PanoFramingThumbnailUpdateNotify.create = function create(properties) {
      return new PanoFramingThumbnailUpdateNotify(properties);
    };

    /**
     * Encodes the specified PanoFramingThumbnailUpdateNotify message. Does not implicitly {@link notify.PanoFramingThumbnailUpdateNotify.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {notify.IPanoFramingThumbnailUpdateNotify} message PanoFramingThumbnailUpdateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingThumbnailUpdateNotify.encode = function encode(
      message,
      writer,
      q,
    ) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.webpData != null &&
        Object.hasOwnProperty.call(message, "webpData")
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.webpData);
      return writer;
    };

    /**
     * Encodes the specified PanoFramingThumbnailUpdateNotify message, length delimited. Does not implicitly {@link notify.PanoFramingThumbnailUpdateNotify.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {notify.IPanoFramingThumbnailUpdateNotify} message PanoFramingThumbnailUpdateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingThumbnailUpdateNotify.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoFramingThumbnailUpdateNotify message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoFramingThumbnailUpdateNotify} PanoFramingThumbnailUpdateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingThumbnailUpdateNotify.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoFramingThumbnailUpdateNotify();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.webpData = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoFramingThumbnailUpdateNotify message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoFramingThumbnailUpdateNotify} PanoFramingThumbnailUpdateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingThumbnailUpdateNotify.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoFramingThumbnailUpdateNotify message.
     * @function verify
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoFramingThumbnailUpdateNotify.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.webpData != null &&
        Object.hasOwnProperty.call(message, "webpData")
      )
        if (!(
          (message.webpData && typeof message.webpData.length === "number") ||
          $util.isString(message.webpData)
        ))
          return "webpData: buffer expected";
      return null;
    };

    /**
     * Creates a PanoFramingThumbnailUpdateNotify message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoFramingThumbnailUpdateNotify} PanoFramingThumbnailUpdateNotify
     */
    PanoFramingThumbnailUpdateNotify.fromObject = function fromObject(
      object,
      long,
    ) {
      if (object instanceof $root.notify.PanoFramingThumbnailUpdateNotify)
        return object;
      if (!$util.isObject(object))
        throw TypeError(
          ".notify.PanoFramingThumbnailUpdateNotify: object expected",
        );
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoFramingThumbnailUpdateNotify();
      if (object.webpData != null)
        if (typeof object.webpData === "string")
          $util.base64.decode(
            object.webpData,
            (message.webpData = $util.newBuffer(
              $util.base64.length(object.webpData),
            )),
            0,
          );
        else if (object.webpData.length >= 0)
          message.webpData = object.webpData;
      return message;
    };

    /**
     * Creates a plain object from a PanoFramingThumbnailUpdateNotify message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {notify.PanoFramingThumbnailUpdateNotify} message PanoFramingThumbnailUpdateNotify
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoFramingThumbnailUpdateNotify.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults)
        if (options.bytes === String) object.webpData = "";
        else {
          object.webpData = [];
          if (options.bytes !== Array)
            object.webpData = $util.newBuffer(object.webpData);
        }
      if (
        message.webpData != null &&
        Object.hasOwnProperty.call(message, "webpData")
      )
        object.webpData =
          options.bytes === String
            ? $util.base64.encode(message.webpData, 0, message.webpData.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.webpData)
              : message.webpData;
      return object;
    };

    /**
     * Converts this PanoFramingThumbnailUpdateNotify to JSON.
     * @function toJSON
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoFramingThumbnailUpdateNotify.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoFramingThumbnailUpdateNotify
     * @function getTypeUrl
     * @memberof notify.PanoFramingThumbnailUpdateNotify
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoFramingThumbnailUpdateNotify.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoFramingThumbnailUpdateNotify";
    };

    return PanoFramingThumbnailUpdateNotify;
  })();

  notify.PanoFramingRectUpdateNotify = (function () {
    /**
     * Properties of a PanoFramingRectUpdateNotify.
     * @memberof notify
     * @interface IPanoFramingRectUpdateNotify
     * @property {number|null} [normXTl] PanoFramingRectUpdateNotify normXTl
     * @property {number|null} [normYTl] PanoFramingRectUpdateNotify normYTl
     * @property {number|null} [normXBr] PanoFramingRectUpdateNotify normXBr
     * @property {number|null} [normYBr] PanoFramingRectUpdateNotify normYBr
     * @property {number|null} [normLimitXLeft] PanoFramingRectUpdateNotify normLimitXLeft
     * @property {number|null} [normLimitYTop] PanoFramingRectUpdateNotify normLimitYTop
     * @property {number|null} [normLimitXRight] PanoFramingRectUpdateNotify normLimitXRight
     * @property {number|null} [normLimitYBottom] PanoFramingRectUpdateNotify normLimitYBottom
     * @property {number|null} [rectHorFov] PanoFramingRectUpdateNotify rectHorFov
     * @property {number|null} [rectVerFov] PanoFramingRectUpdateNotify rectVerFov
     * @property {number|null} [errorCode] PanoFramingRectUpdateNotify errorCode
     */

    /**
     * Constructs a new PanoFramingRectUpdateNotify.
     * @memberof notify
     * @classdesc Represents a PanoFramingRectUpdateNotify.
     * @implements IPanoFramingRectUpdateNotify
     * @constructor
     * @param {notify.IPanoFramingRectUpdateNotify=} [properties] Properties to set
     */
    function PanoFramingRectUpdateNotify(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoFramingRectUpdateNotify normXTl.
     * @member {number} normXTl
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normXTl = 0;

    /**
     * PanoFramingRectUpdateNotify normYTl.
     * @member {number} normYTl
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normYTl = 0;

    /**
     * PanoFramingRectUpdateNotify normXBr.
     * @member {number} normXBr
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normXBr = 0;

    /**
     * PanoFramingRectUpdateNotify normYBr.
     * @member {number} normYBr
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normYBr = 0;

    /**
     * PanoFramingRectUpdateNotify normLimitXLeft.
     * @member {number} normLimitXLeft
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normLimitXLeft = 0;

    /**
     * PanoFramingRectUpdateNotify normLimitYTop.
     * @member {number} normLimitYTop
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normLimitYTop = 0;

    /**
     * PanoFramingRectUpdateNotify normLimitXRight.
     * @member {number} normLimitXRight
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normLimitXRight = 0;

    /**
     * PanoFramingRectUpdateNotify normLimitYBottom.
     * @member {number} normLimitYBottom
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.normLimitYBottom = 0;

    /**
     * PanoFramingRectUpdateNotify rectHorFov.
     * @member {number} rectHorFov
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.rectHorFov = 0;

    /**
     * PanoFramingRectUpdateNotify rectVerFov.
     * @member {number} rectVerFov
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.rectVerFov = 0;

    /**
     * PanoFramingRectUpdateNotify errorCode.
     * @member {number} errorCode
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     */
    PanoFramingRectUpdateNotify.prototype.errorCode = 0;

    /**
     * Creates a new PanoFramingRectUpdateNotify instance using the specified properties.
     * @function create
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {notify.IPanoFramingRectUpdateNotify=} [properties] Properties to set
     * @returns {notify.PanoFramingRectUpdateNotify} PanoFramingRectUpdateNotify instance
     */
    PanoFramingRectUpdateNotify.create = function create(properties) {
      return new PanoFramingRectUpdateNotify(properties);
    };

    /**
     * Encodes the specified PanoFramingRectUpdateNotify message. Does not implicitly {@link notify.PanoFramingRectUpdateNotify.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {notify.IPanoFramingRectUpdateNotify} message PanoFramingRectUpdateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingRectUpdateNotify.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.normXTl != null &&
        Object.hasOwnProperty.call(message, "normXTl")
      )
        writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.normXTl);
      if (
        message.normYTl != null &&
        Object.hasOwnProperty.call(message, "normYTl")
      )
        writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.normYTl);
      if (
        message.normXBr != null &&
        Object.hasOwnProperty.call(message, "normXBr")
      )
        writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.normXBr);
      if (
        message.normYBr != null &&
        Object.hasOwnProperty.call(message, "normYBr")
      )
        writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.normYBr);
      if (
        message.normLimitXLeft != null &&
        Object.hasOwnProperty.call(message, "normLimitXLeft")
      )
        writer
          .uint32(/* id 5, wireType 1 =*/ 41)
          .double(message.normLimitXLeft);
      if (
        message.normLimitYTop != null &&
        Object.hasOwnProperty.call(message, "normLimitYTop")
      )
        writer.uint32(/* id 6, wireType 1 =*/ 49).double(message.normLimitYTop);
      if (
        message.normLimitXRight != null &&
        Object.hasOwnProperty.call(message, "normLimitXRight")
      )
        writer
          .uint32(/* id 7, wireType 1 =*/ 57)
          .double(message.normLimitXRight);
      if (
        message.normLimitYBottom != null &&
        Object.hasOwnProperty.call(message, "normLimitYBottom")
      )
        writer
          .uint32(/* id 8, wireType 1 =*/ 65)
          .double(message.normLimitYBottom);
      if (
        message.rectHorFov != null &&
        Object.hasOwnProperty.call(message, "rectHorFov")
      )
        writer.uint32(/* id 9, wireType 1 =*/ 73).double(message.rectHorFov);
      if (
        message.rectVerFov != null &&
        Object.hasOwnProperty.call(message, "rectVerFov")
      )
        writer.uint32(/* id 10, wireType 1 =*/ 81).double(message.rectVerFov);
      if (
        message.errorCode != null &&
        Object.hasOwnProperty.call(message, "errorCode")
      )
        writer.uint32(/* id 11, wireType 0 =*/ 88).int32(message.errorCode);
      return writer;
    };

    /**
     * Encodes the specified PanoFramingRectUpdateNotify message, length delimited. Does not implicitly {@link notify.PanoFramingRectUpdateNotify.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {notify.IPanoFramingRectUpdateNotify} message PanoFramingRectUpdateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingRectUpdateNotify.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoFramingRectUpdateNotify message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoFramingRectUpdateNotify} PanoFramingRectUpdateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingRectUpdateNotify.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoFramingRectUpdateNotify();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.normXTl = reader.double();
            break;
          }
          case 2: {
            message.normYTl = reader.double();
            break;
          }
          case 3: {
            message.normXBr = reader.double();
            break;
          }
          case 4: {
            message.normYBr = reader.double();
            break;
          }
          case 5: {
            message.normLimitXLeft = reader.double();
            break;
          }
          case 6: {
            message.normLimitYTop = reader.double();
            break;
          }
          case 7: {
            message.normLimitXRight = reader.double();
            break;
          }
          case 8: {
            message.normLimitYBottom = reader.double();
            break;
          }
          case 9: {
            message.rectHorFov = reader.double();
            break;
          }
          case 10: {
            message.rectVerFov = reader.double();
            break;
          }
          case 11: {
            message.errorCode = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoFramingRectUpdateNotify message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoFramingRectUpdateNotify} PanoFramingRectUpdateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingRectUpdateNotify.decodeDelimited = function decodeDelimited(
      reader,
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoFramingRectUpdateNotify message.
     * @function verify
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoFramingRectUpdateNotify.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.normXTl != null &&
        Object.hasOwnProperty.call(message, "normXTl")
      )
        if (typeof message.normXTl !== "number")
          return "normXTl: number expected";
      if (
        message.normYTl != null &&
        Object.hasOwnProperty.call(message, "normYTl")
      )
        if (typeof message.normYTl !== "number")
          return "normYTl: number expected";
      if (
        message.normXBr != null &&
        Object.hasOwnProperty.call(message, "normXBr")
      )
        if (typeof message.normXBr !== "number")
          return "normXBr: number expected";
      if (
        message.normYBr != null &&
        Object.hasOwnProperty.call(message, "normYBr")
      )
        if (typeof message.normYBr !== "number")
          return "normYBr: number expected";
      if (
        message.normLimitXLeft != null &&
        Object.hasOwnProperty.call(message, "normLimitXLeft")
      )
        if (typeof message.normLimitXLeft !== "number")
          return "normLimitXLeft: number expected";
      if (
        message.normLimitYTop != null &&
        Object.hasOwnProperty.call(message, "normLimitYTop")
      )
        if (typeof message.normLimitYTop !== "number")
          return "normLimitYTop: number expected";
      if (
        message.normLimitXRight != null &&
        Object.hasOwnProperty.call(message, "normLimitXRight")
      )
        if (typeof message.normLimitXRight !== "number")
          return "normLimitXRight: number expected";
      if (
        message.normLimitYBottom != null &&
        Object.hasOwnProperty.call(message, "normLimitYBottom")
      )
        if (typeof message.normLimitYBottom !== "number")
          return "normLimitYBottom: number expected";
      if (
        message.rectHorFov != null &&
        Object.hasOwnProperty.call(message, "rectHorFov")
      )
        if (typeof message.rectHorFov !== "number")
          return "rectHorFov: number expected";
      if (
        message.rectVerFov != null &&
        Object.hasOwnProperty.call(message, "rectVerFov")
      )
        if (typeof message.rectVerFov !== "number")
          return "rectVerFov: number expected";
      if (
        message.errorCode != null &&
        Object.hasOwnProperty.call(message, "errorCode")
      )
        if (!$util.isInteger(message.errorCode))
          return "errorCode: integer expected";
      return null;
    };

    /**
     * Creates a PanoFramingRectUpdateNotify message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoFramingRectUpdateNotify} PanoFramingRectUpdateNotify
     */
    PanoFramingRectUpdateNotify.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoFramingRectUpdateNotify)
        return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoFramingRectUpdateNotify: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoFramingRectUpdateNotify();
      if (object.normXTl != null) message.normXTl = Number(object.normXTl);
      if (object.normYTl != null) message.normYTl = Number(object.normYTl);
      if (object.normXBr != null) message.normXBr = Number(object.normXBr);
      if (object.normYBr != null) message.normYBr = Number(object.normYBr);
      if (object.normLimitXLeft != null)
        message.normLimitXLeft = Number(object.normLimitXLeft);
      if (object.normLimitYTop != null)
        message.normLimitYTop = Number(object.normLimitYTop);
      if (object.normLimitXRight != null)
        message.normLimitXRight = Number(object.normLimitXRight);
      if (object.normLimitYBottom != null)
        message.normLimitYBottom = Number(object.normLimitYBottom);
      if (object.rectHorFov != null)
        message.rectHorFov = Number(object.rectHorFov);
      if (object.rectVerFov != null)
        message.rectVerFov = Number(object.rectVerFov);
      if (object.errorCode != null) message.errorCode = object.errorCode | 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoFramingRectUpdateNotify message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {notify.PanoFramingRectUpdateNotify} message PanoFramingRectUpdateNotify
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoFramingRectUpdateNotify.toObject = function toObject(
      message,
      options,
      q,
    ) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.normXTl = 0;
        object.normYTl = 0;
        object.normXBr = 0;
        object.normYBr = 0;
        object.normLimitXLeft = 0;
        object.normLimitYTop = 0;
        object.normLimitXRight = 0;
        object.normLimitYBottom = 0;
        object.rectHorFov = 0;
        object.rectVerFov = 0;
        object.errorCode = 0;
      }
      if (
        message.normXTl != null &&
        Object.hasOwnProperty.call(message, "normXTl")
      )
        object.normXTl =
          options.json && !isFinite(message.normXTl)
            ? String(message.normXTl)
            : message.normXTl;
      if (
        message.normYTl != null &&
        Object.hasOwnProperty.call(message, "normYTl")
      )
        object.normYTl =
          options.json && !isFinite(message.normYTl)
            ? String(message.normYTl)
            : message.normYTl;
      if (
        message.normXBr != null &&
        Object.hasOwnProperty.call(message, "normXBr")
      )
        object.normXBr =
          options.json && !isFinite(message.normXBr)
            ? String(message.normXBr)
            : message.normXBr;
      if (
        message.normYBr != null &&
        Object.hasOwnProperty.call(message, "normYBr")
      )
        object.normYBr =
          options.json && !isFinite(message.normYBr)
            ? String(message.normYBr)
            : message.normYBr;
      if (
        message.normLimitXLeft != null &&
        Object.hasOwnProperty.call(message, "normLimitXLeft")
      )
        object.normLimitXLeft =
          options.json && !isFinite(message.normLimitXLeft)
            ? String(message.normLimitXLeft)
            : message.normLimitXLeft;
      if (
        message.normLimitYTop != null &&
        Object.hasOwnProperty.call(message, "normLimitYTop")
      )
        object.normLimitYTop =
          options.json && !isFinite(message.normLimitYTop)
            ? String(message.normLimitYTop)
            : message.normLimitYTop;
      if (
        message.normLimitXRight != null &&
        Object.hasOwnProperty.call(message, "normLimitXRight")
      )
        object.normLimitXRight =
          options.json && !isFinite(message.normLimitXRight)
            ? String(message.normLimitXRight)
            : message.normLimitXRight;
      if (
        message.normLimitYBottom != null &&
        Object.hasOwnProperty.call(message, "normLimitYBottom")
      )
        object.normLimitYBottom =
          options.json && !isFinite(message.normLimitYBottom)
            ? String(message.normLimitYBottom)
            : message.normLimitYBottom;
      if (
        message.rectHorFov != null &&
        Object.hasOwnProperty.call(message, "rectHorFov")
      )
        object.rectHorFov =
          options.json && !isFinite(message.rectHorFov)
            ? String(message.rectHorFov)
            : message.rectHorFov;
      if (
        message.rectVerFov != null &&
        Object.hasOwnProperty.call(message, "rectVerFov")
      )
        object.rectVerFov =
          options.json && !isFinite(message.rectVerFov)
            ? String(message.rectVerFov)
            : message.rectVerFov;
      if (
        message.errorCode != null &&
        Object.hasOwnProperty.call(message, "errorCode")
      )
        object.errorCode = message.errorCode;
      return object;
    };

    /**
     * Converts this PanoFramingRectUpdateNotify to JSON.
     * @function toJSON
     * @memberof notify.PanoFramingRectUpdateNotify
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoFramingRectUpdateNotify.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoFramingRectUpdateNotify
     * @function getTypeUrl
     * @memberof notify.PanoFramingRectUpdateNotify
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoFramingRectUpdateNotify.getTypeUrl = function getTypeUrl(
      typeUrlPrefix,
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoFramingRectUpdateNotify";
    };

    return PanoFramingRectUpdateNotify;
  })();

  notify.PanoFramingStateNotify = (function () {
    /**
     * Properties of a PanoFramingStateNotify.
     * @memberof notify
     * @interface IPanoFramingStateNotify
     * @property {number|null} [state] PanoFramingStateNotify state
     */

    /**
     * Constructs a new PanoFramingStateNotify.
     * @memberof notify
     * @classdesc Represents a PanoFramingStateNotify.
     * @implements IPanoFramingStateNotify
     * @constructor
     * @param {notify.IPanoFramingStateNotify=} [properties] Properties to set
     */
    function PanoFramingStateNotify(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * PanoFramingStateNotify state.
     * @member {number} state
     * @memberof notify.PanoFramingStateNotify
     * @instance
     */
    PanoFramingStateNotify.prototype.state = 0;

    /**
     * Creates a new PanoFramingStateNotify instance using the specified properties.
     * @function create
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {notify.IPanoFramingStateNotify=} [properties] Properties to set
     * @returns {notify.PanoFramingStateNotify} PanoFramingStateNotify instance
     */
    PanoFramingStateNotify.create = function create(properties) {
      return new PanoFramingStateNotify(properties);
    };

    /**
     * Encodes the specified PanoFramingStateNotify message. Does not implicitly {@link notify.PanoFramingStateNotify.verify|verify} messages.
     * @function encode
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {notify.IPanoFramingStateNotify} message PanoFramingStateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingStateNotify.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified PanoFramingStateNotify message, length delimited. Does not implicitly {@link notify.PanoFramingStateNotify.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {notify.IPanoFramingStateNotify} message PanoFramingStateNotify message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PanoFramingStateNotify.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a PanoFramingStateNotify message from the specified reader or buffer.
     * @function decode
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.PanoFramingStateNotify} PanoFramingStateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingStateNotify.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.PanoFramingStateNotify();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a PanoFramingStateNotify message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.PanoFramingStateNotify} PanoFramingStateNotify
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PanoFramingStateNotify.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PanoFramingStateNotify message.
     * @function verify
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PanoFramingStateNotify.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a PanoFramingStateNotify message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.PanoFramingStateNotify} PanoFramingStateNotify
     */
    PanoFramingStateNotify.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.PanoFramingStateNotify) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.PanoFramingStateNotify: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.PanoFramingStateNotify();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a PanoFramingStateNotify message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {notify.PanoFramingStateNotify} message PanoFramingStateNotify
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PanoFramingStateNotify.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this PanoFramingStateNotify to JSON.
     * @function toJSON
     * @memberof notify.PanoFramingStateNotify
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PanoFramingStateNotify.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PanoFramingStateNotify
     * @function getTypeUrl
     * @memberof notify.PanoFramingStateNotify
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PanoFramingStateNotify.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.PanoFramingStateNotify";
    };

    return PanoFramingStateNotify;
  })();

  notify.AutoShutdown = (function () {
    /**
     * Properties of an AutoShutdown.
     * @memberof notify
     * @interface IAutoShutdown
     * @property {number|null} [state] AutoShutdown state
     */

    /**
     * Constructs a new AutoShutdown.
     * @memberof notify
     * @classdesc Represents an AutoShutdown.
     * @implements IAutoShutdown
     * @constructor
     * @param {notify.IAutoShutdown=} [properties] Properties to set
     */
    function AutoShutdown(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AutoShutdown state.
     * @member {number} state
     * @memberof notify.AutoShutdown
     * @instance
     */
    AutoShutdown.prototype.state = 0;

    /**
     * Creates a new AutoShutdown instance using the specified properties.
     * @function create
     * @memberof notify.AutoShutdown
     * @static
     * @param {notify.IAutoShutdown=} [properties] Properties to set
     * @returns {notify.AutoShutdown} AutoShutdown instance
     */
    AutoShutdown.create = function create(properties) {
      return new AutoShutdown(properties);
    };

    /**
     * Encodes the specified AutoShutdown message. Does not implicitly {@link notify.AutoShutdown.verify|verify} messages.
     * @function encode
     * @memberof notify.AutoShutdown
     * @static
     * @param {notify.IAutoShutdown} message AutoShutdown message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AutoShutdown.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified AutoShutdown message, length delimited. Does not implicitly {@link notify.AutoShutdown.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AutoShutdown
     * @static
     * @param {notify.IAutoShutdown} message AutoShutdown message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AutoShutdown.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AutoShutdown message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AutoShutdown
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AutoShutdown} AutoShutdown
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AutoShutdown.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AutoShutdown();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AutoShutdown message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AutoShutdown
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AutoShutdown} AutoShutdown
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AutoShutdown.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AutoShutdown message.
     * @function verify
     * @memberof notify.AutoShutdown
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AutoShutdown.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates an AutoShutdown message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AutoShutdown
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AutoShutdown} AutoShutdown
     */
    AutoShutdown.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AutoShutdown) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AutoShutdown: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AutoShutdown();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from an AutoShutdown message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AutoShutdown
     * @static
     * @param {notify.AutoShutdown} message AutoShutdown
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AutoShutdown.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this AutoShutdown to JSON.
     * @function toJSON
     * @memberof notify.AutoShutdown
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AutoShutdown.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AutoShutdown
     * @function getTypeUrl
     * @memberof notify.AutoShutdown
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AutoShutdown.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AutoShutdown";
    };

    return AutoShutdown;
  })();

  notify.LensDefog = (function () {
    /**
     * Properties of a LensDefog.
     * @memberof notify
     * @interface ILensDefog
     * @property {number|null} [state] LensDefog state
     */

    /**
     * Constructs a new LensDefog.
     * @memberof notify
     * @classdesc Represents a LensDefog.
     * @implements ILensDefog
     * @constructor
     * @param {notify.ILensDefog=} [properties] Properties to set
     */
    function LensDefog(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * LensDefog state.
     * @member {number} state
     * @memberof notify.LensDefog
     * @instance
     */
    LensDefog.prototype.state = 0;

    /**
     * Creates a new LensDefog instance using the specified properties.
     * @function create
     * @memberof notify.LensDefog
     * @static
     * @param {notify.ILensDefog=} [properties] Properties to set
     * @returns {notify.LensDefog} LensDefog instance
     */
    LensDefog.create = function create(properties) {
      return new LensDefog(properties);
    };

    /**
     * Encodes the specified LensDefog message. Does not implicitly {@link notify.LensDefog.verify|verify} messages.
     * @function encode
     * @memberof notify.LensDefog
     * @static
     * @param {notify.ILensDefog} message LensDefog message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LensDefog.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified LensDefog message, length delimited. Does not implicitly {@link notify.LensDefog.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.LensDefog
     * @static
     * @param {notify.ILensDefog} message LensDefog message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LensDefog.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a LensDefog message from the specified reader or buffer.
     * @function decode
     * @memberof notify.LensDefog
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.LensDefog} LensDefog
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LensDefog.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.LensDefog();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a LensDefog message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.LensDefog
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.LensDefog} LensDefog
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LensDefog.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LensDefog message.
     * @function verify
     * @memberof notify.LensDefog
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LensDefog.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates a LensDefog message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.LensDefog
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.LensDefog} LensDefog
     */
    LensDefog.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.LensDefog) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.LensDefog: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.LensDefog();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from a LensDefog message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.LensDefog
     * @static
     * @param {notify.LensDefog} message LensDefog
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LensDefog.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this LensDefog to JSON.
     * @function toJSON
     * @memberof notify.LensDefog
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LensDefog.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LensDefog
     * @function getTypeUrl
     * @memberof notify.LensDefog
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LensDefog.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.LensDefog";
    };

    return LensDefog;
  })();

  notify.AutoCooling = (function () {
    /**
     * Properties of an AutoCooling.
     * @memberof notify
     * @interface IAutoCooling
     * @property {number|null} [state] AutoCooling state
     */

    /**
     * Constructs a new AutoCooling.
     * @memberof notify
     * @classdesc Represents an AutoCooling.
     * @implements IAutoCooling
     * @constructor
     * @param {notify.IAutoCooling=} [properties] Properties to set
     */
    function AutoCooling(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * AutoCooling state.
     * @member {number} state
     * @memberof notify.AutoCooling
     * @instance
     */
    AutoCooling.prototype.state = 0;

    /**
     * Creates a new AutoCooling instance using the specified properties.
     * @function create
     * @memberof notify.AutoCooling
     * @static
     * @param {notify.IAutoCooling=} [properties] Properties to set
     * @returns {notify.AutoCooling} AutoCooling instance
     */
    AutoCooling.create = function create(properties) {
      return new AutoCooling(properties);
    };

    /**
     * Encodes the specified AutoCooling message. Does not implicitly {@link notify.AutoCooling.verify|verify} messages.
     * @function encode
     * @memberof notify.AutoCooling
     * @static
     * @param {notify.IAutoCooling} message AutoCooling message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AutoCooling.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.state);
      return writer;
    };

    /**
     * Encodes the specified AutoCooling message, length delimited. Does not implicitly {@link notify.AutoCooling.verify|verify} messages.
     * @function encodeDelimited
     * @memberof notify.AutoCooling
     * @static
     * @param {notify.IAutoCooling} message AutoCooling message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AutoCooling.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes an AutoCooling message from the specified reader or buffer.
     * @function decode
     * @memberof notify.AutoCooling
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {notify.AutoCooling} AutoCooling
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AutoCooling.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.notify.AutoCooling();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.state = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes an AutoCooling message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof notify.AutoCooling
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {notify.AutoCooling} AutoCooling
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AutoCooling.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AutoCooling message.
     * @function verify
     * @memberof notify.AutoCooling
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AutoCooling.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        if (!$util.isInteger(message.state)) return "state: integer expected";
      return null;
    };

    /**
     * Creates an AutoCooling message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof notify.AutoCooling
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {notify.AutoCooling} AutoCooling
     */
    AutoCooling.fromObject = function fromObject(object, long) {
      if (object instanceof $root.notify.AutoCooling) return object;
      if (!$util.isObject(object))
        throw TypeError(".notify.AutoCooling: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.notify.AutoCooling();
      if (object.state != null) message.state = object.state | 0;
      return message;
    };

    /**
     * Creates a plain object from an AutoCooling message. Also converts values to other types if specified.
     * @function toObject
     * @memberof notify.AutoCooling
     * @static
     * @param {notify.AutoCooling} message AutoCooling
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AutoCooling.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) object.state = 0;
      if (message.state != null && Object.hasOwnProperty.call(message, "state"))
        object.state = message.state;
      return object;
    };

    /**
     * Converts this AutoCooling to JSON.
     * @function toJSON
     * @memberof notify.AutoCooling
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AutoCooling.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AutoCooling
     * @function getTypeUrl
     * @memberof notify.AutoCooling
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AutoCooling.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/notify.AutoCooling";
    };

    return AutoCooling;
  })();

  return notify;
})();

$root.ReqStartPanoramaByGrid = (function () {
  /**
   * Properties of a ReqStartPanoramaByGrid.
   * @exports IReqStartPanoramaByGrid
   * @interface IReqStartPanoramaByGrid
   */

  /**
   * Constructs a new ReqStartPanoramaByGrid.
   * @exports ReqStartPanoramaByGrid
   * @classdesc Represents a ReqStartPanoramaByGrid.
   * @implements IReqStartPanoramaByGrid
   * @constructor
   * @param {IReqStartPanoramaByGrid=} [properties] Properties to set
   */
  function ReqStartPanoramaByGrid(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStartPanoramaByGrid instance using the specified properties.
   * @function create
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {IReqStartPanoramaByGrid=} [properties] Properties to set
   * @returns {ReqStartPanoramaByGrid} ReqStartPanoramaByGrid instance
   */
  ReqStartPanoramaByGrid.create = function create(properties) {
    return new ReqStartPanoramaByGrid(properties);
  };

  /**
   * Encodes the specified ReqStartPanoramaByGrid message. Does not implicitly {@link ReqStartPanoramaByGrid.verify|verify} messages.
   * @function encode
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {IReqStartPanoramaByGrid} message ReqStartPanoramaByGrid message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaByGrid.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStartPanoramaByGrid message, length delimited. Does not implicitly {@link ReqStartPanoramaByGrid.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {IReqStartPanoramaByGrid} message ReqStartPanoramaByGrid message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaByGrid.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartPanoramaByGrid message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartPanoramaByGrid} ReqStartPanoramaByGrid
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaByGrid.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartPanoramaByGrid();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartPanoramaByGrid message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartPanoramaByGrid} ReqStartPanoramaByGrid
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaByGrid.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartPanoramaByGrid message.
   * @function verify
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartPanoramaByGrid.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStartPanoramaByGrid message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartPanoramaByGrid} ReqStartPanoramaByGrid
   */
  ReqStartPanoramaByGrid.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartPanoramaByGrid) return object;
    return new $root.ReqStartPanoramaByGrid();
  };

  /**
   * Creates a plain object from a ReqStartPanoramaByGrid message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {ReqStartPanoramaByGrid} message ReqStartPanoramaByGrid
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartPanoramaByGrid.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStartPanoramaByGrid to JSON.
   * @function toJSON
   * @memberof ReqStartPanoramaByGrid
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartPanoramaByGrid.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartPanoramaByGrid
   * @function getTypeUrl
   * @memberof ReqStartPanoramaByGrid
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartPanoramaByGrid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartPanoramaByGrid";
  };

  return ReqStartPanoramaByGrid;
})();

$root.ReqStartPanoramaByEulerRange = (function () {
  /**
   * Properties of a ReqStartPanoramaByEulerRange.
   * @exports IReqStartPanoramaByEulerRange
   * @interface IReqStartPanoramaByEulerRange
   * @property {number|null} [yawRange] ReqStartPanoramaByEulerRange yawRange
   * @property {number|null} [pitchRange] ReqStartPanoramaByEulerRange pitchRange
   */

  /**
   * Constructs a new ReqStartPanoramaByEulerRange.
   * @exports ReqStartPanoramaByEulerRange
   * @classdesc Represents a ReqStartPanoramaByEulerRange.
   * @implements IReqStartPanoramaByEulerRange
   * @constructor
   * @param {IReqStartPanoramaByEulerRange=} [properties] Properties to set
   */
  function ReqStartPanoramaByEulerRange(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartPanoramaByEulerRange yawRange.
   * @member {number} yawRange
   * @memberof ReqStartPanoramaByEulerRange
   * @instance
   */
  ReqStartPanoramaByEulerRange.prototype.yawRange = 0;

  /**
   * ReqStartPanoramaByEulerRange pitchRange.
   * @member {number} pitchRange
   * @memberof ReqStartPanoramaByEulerRange
   * @instance
   */
  ReqStartPanoramaByEulerRange.prototype.pitchRange = 0;

  /**
   * Creates a new ReqStartPanoramaByEulerRange instance using the specified properties.
   * @function create
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {IReqStartPanoramaByEulerRange=} [properties] Properties to set
   * @returns {ReqStartPanoramaByEulerRange} ReqStartPanoramaByEulerRange instance
   */
  ReqStartPanoramaByEulerRange.create = function create(properties) {
    return new ReqStartPanoramaByEulerRange(properties);
  };

  /**
   * Encodes the specified ReqStartPanoramaByEulerRange message. Does not implicitly {@link ReqStartPanoramaByEulerRange.verify|verify} messages.
   * @function encode
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {IReqStartPanoramaByEulerRange} message ReqStartPanoramaByEulerRange message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaByEulerRange.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.yawRange != null &&
      Object.hasOwnProperty.call(message, "yawRange")
    )
      writer.uint32(/* id 1, wireType 5 =*/ 13).float(message.yawRange);
    if (
      message.pitchRange != null &&
      Object.hasOwnProperty.call(message, "pitchRange")
    )
      writer.uint32(/* id 2, wireType 5 =*/ 21).float(message.pitchRange);
    return writer;
  };

  /**
   * Encodes the specified ReqStartPanoramaByEulerRange message, length delimited. Does not implicitly {@link ReqStartPanoramaByEulerRange.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {IReqStartPanoramaByEulerRange} message ReqStartPanoramaByEulerRange message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaByEulerRange.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartPanoramaByEulerRange message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartPanoramaByEulerRange} ReqStartPanoramaByEulerRange
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaByEulerRange.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartPanoramaByEulerRange();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.yawRange = reader.float();
          break;
        }
        case 2: {
          message.pitchRange = reader.float();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartPanoramaByEulerRange message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartPanoramaByEulerRange} ReqStartPanoramaByEulerRange
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaByEulerRange.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartPanoramaByEulerRange message.
   * @function verify
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartPanoramaByEulerRange.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.yawRange != null &&
      Object.hasOwnProperty.call(message, "yawRange")
    )
      if (typeof message.yawRange !== "number")
        return "yawRange: number expected";
    if (
      message.pitchRange != null &&
      Object.hasOwnProperty.call(message, "pitchRange")
    )
      if (typeof message.pitchRange !== "number")
        return "pitchRange: number expected";
    return null;
  };

  /**
   * Creates a ReqStartPanoramaByEulerRange message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartPanoramaByEulerRange} ReqStartPanoramaByEulerRange
   */
  ReqStartPanoramaByEulerRange.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartPanoramaByEulerRange) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartPanoramaByEulerRange: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartPanoramaByEulerRange();
    if (object.yawRange != null) message.yawRange = Number(object.yawRange);
    if (object.pitchRange != null)
      message.pitchRange = Number(object.pitchRange);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartPanoramaByEulerRange message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {ReqStartPanoramaByEulerRange} message ReqStartPanoramaByEulerRange
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartPanoramaByEulerRange.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.yawRange = 0;
      object.pitchRange = 0;
    }
    if (
      message.yawRange != null &&
      Object.hasOwnProperty.call(message, "yawRange")
    )
      object.yawRange =
        options.json && !isFinite(message.yawRange)
          ? String(message.yawRange)
          : message.yawRange;
    if (
      message.pitchRange != null &&
      Object.hasOwnProperty.call(message, "pitchRange")
    )
      object.pitchRange =
        options.json && !isFinite(message.pitchRange)
          ? String(message.pitchRange)
          : message.pitchRange;
    return object;
  };

  /**
   * Converts this ReqStartPanoramaByEulerRange to JSON.
   * @function toJSON
   * @memberof ReqStartPanoramaByEulerRange
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartPanoramaByEulerRange.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartPanoramaByEulerRange
   * @function getTypeUrl
   * @memberof ReqStartPanoramaByEulerRange
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartPanoramaByEulerRange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartPanoramaByEulerRange";
  };

  return ReqStartPanoramaByEulerRange;
})();

$root.ReqStartPanoramaStitchUpload = (function () {
  /**
   * Properties of a ReqStartPanoramaStitchUpload.
   * @exports IReqStartPanoramaStitchUpload
   * @interface IReqStartPanoramaStitchUpload
   * @property {number|Long|null} [resourceId] ReqStartPanoramaStitchUpload resourceId
   * @property {string|null} [userId] ReqStartPanoramaStitchUpload userId
   * @property {number|null} [appPlatform] ReqStartPanoramaStitchUpload appPlatform
   * @property {string|null} [panoramaName] ReqStartPanoramaStitchUpload panoramaName
   * @property {string|null} [ak] ReqStartPanoramaStitchUpload ak
   * @property {string|null} [sk] ReqStartPanoramaStitchUpload sk
   * @property {string|null} [token] ReqStartPanoramaStitchUpload token
   * @property {string|null} [bucket] ReqStartPanoramaStitchUpload bucket
   * @property {string|null} [bucketPrefix] ReqStartPanoramaStitchUpload bucketPrefix
   * @property {string|null} [from] ReqStartPanoramaStitchUpload from
   * @property {string|null} [envType] ReqStartPanoramaStitchUpload envType
   */

  /**
   * Constructs a new ReqStartPanoramaStitchUpload.
   * @exports ReqStartPanoramaStitchUpload
   * @classdesc Represents a ReqStartPanoramaStitchUpload.
   * @implements IReqStartPanoramaStitchUpload
   * @constructor
   * @param {IReqStartPanoramaStitchUpload=} [properties] Properties to set
   */
  function ReqStartPanoramaStitchUpload(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartPanoramaStitchUpload resourceId.
   * @member {number|Long} resourceId
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.resourceId = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ReqStartPanoramaStitchUpload userId.
   * @member {string} userId
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.userId = "";

  /**
   * ReqStartPanoramaStitchUpload appPlatform.
   * @member {number} appPlatform
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.appPlatform = 0;

  /**
   * ReqStartPanoramaStitchUpload panoramaName.
   * @member {string} panoramaName
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.panoramaName = "";

  /**
   * ReqStartPanoramaStitchUpload ak.
   * @member {string} ak
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.ak = "";

  /**
   * ReqStartPanoramaStitchUpload sk.
   * @member {string} sk
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.sk = "";

  /**
   * ReqStartPanoramaStitchUpload token.
   * @member {string} token
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.token = "";

  /**
   * ReqStartPanoramaStitchUpload bucket.
   * @member {string} bucket
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.bucket = "";

  /**
   * ReqStartPanoramaStitchUpload bucketPrefix.
   * @member {string} bucketPrefix
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.bucketPrefix = "";

  /**
   * ReqStartPanoramaStitchUpload from.
   * @member {string} from
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.from = "";

  /**
   * ReqStartPanoramaStitchUpload envType.
   * @member {string} envType
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   */
  ReqStartPanoramaStitchUpload.prototype.envType = "";

  /**
   * Creates a new ReqStartPanoramaStitchUpload instance using the specified properties.
   * @function create
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {IReqStartPanoramaStitchUpload=} [properties] Properties to set
   * @returns {ReqStartPanoramaStitchUpload} ReqStartPanoramaStitchUpload instance
   */
  ReqStartPanoramaStitchUpload.create = function create(properties) {
    return new ReqStartPanoramaStitchUpload(properties);
  };

  /**
   * Encodes the specified ReqStartPanoramaStitchUpload message. Does not implicitly {@link ReqStartPanoramaStitchUpload.verify|verify} messages.
   * @function encode
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {IReqStartPanoramaStitchUpload} message ReqStartPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaStitchUpload.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.resourceId != null &&
      Object.hasOwnProperty.call(message, "resourceId")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.resourceId);
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.userId);
    if (
      message.appPlatform != null &&
      Object.hasOwnProperty.call(message, "appPlatform")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.appPlatform);
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.panoramaName);
    if (message.ak != null && Object.hasOwnProperty.call(message, "ak"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.ak);
    if (message.sk != null && Object.hasOwnProperty.call(message, "sk"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.sk);
    if (message.token != null && Object.hasOwnProperty.call(message, "token"))
      writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.token);
    if (message.bucket != null && Object.hasOwnProperty.call(message, "bucket"))
      writer.uint32(/* id 8, wireType 2 =*/ 66).string(message.bucket);
    if (
      message.bucketPrefix != null &&
      Object.hasOwnProperty.call(message, "bucketPrefix")
    )
      writer.uint32(/* id 9, wireType 2 =*/ 74).string(message.bucketPrefix);
    if (message.from != null && Object.hasOwnProperty.call(message, "from"))
      writer.uint32(/* id 10, wireType 2 =*/ 82).string(message.from);
    if (
      message.envType != null &&
      Object.hasOwnProperty.call(message, "envType")
    )
      writer.uint32(/* id 11, wireType 2 =*/ 90).string(message.envType);
    return writer;
  };

  /**
   * Encodes the specified ReqStartPanoramaStitchUpload message, length delimited. Does not implicitly {@link ReqStartPanoramaStitchUpload.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {IReqStartPanoramaStitchUpload} message ReqStartPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaStitchUpload.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartPanoramaStitchUpload message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartPanoramaStitchUpload} ReqStartPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaStitchUpload.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartPanoramaStitchUpload();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.resourceId = reader.uint64();
          break;
        }
        case 2: {
          message.userId = reader.string();
          break;
        }
        case 3: {
          message.appPlatform = reader.int32();
          break;
        }
        case 4: {
          message.panoramaName = reader.string();
          break;
        }
        case 5: {
          message.ak = reader.string();
          break;
        }
        case 6: {
          message.sk = reader.string();
          break;
        }
        case 7: {
          message.token = reader.string();
          break;
        }
        case 8: {
          message.bucket = reader.string();
          break;
        }
        case 9: {
          message.bucketPrefix = reader.string();
          break;
        }
        case 10: {
          message.from = reader.string();
          break;
        }
        case 11: {
          message.envType = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartPanoramaStitchUpload message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartPanoramaStitchUpload} ReqStartPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaStitchUpload.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartPanoramaStitchUpload message.
   * @function verify
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartPanoramaStitchUpload.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.resourceId != null &&
      Object.hasOwnProperty.call(message, "resourceId")
    )
      if (
        !$util.isInteger(message.resourceId) &&
        !(
          message.resourceId &&
          $util.isInteger(message.resourceId.low) &&
          $util.isInteger(message.resourceId.high)
        )
      )
        return "resourceId: integer|Long expected";
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      if (!$util.isString(message.userId)) return "userId: string expected";
    if (
      message.appPlatform != null &&
      Object.hasOwnProperty.call(message, "appPlatform")
    )
      if (!$util.isInteger(message.appPlatform))
        return "appPlatform: integer expected";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    if (message.ak != null && Object.hasOwnProperty.call(message, "ak"))
      if (!$util.isString(message.ak)) return "ak: string expected";
    if (message.sk != null && Object.hasOwnProperty.call(message, "sk"))
      if (!$util.isString(message.sk)) return "sk: string expected";
    if (message.token != null && Object.hasOwnProperty.call(message, "token"))
      if (!$util.isString(message.token)) return "token: string expected";
    if (message.bucket != null && Object.hasOwnProperty.call(message, "bucket"))
      if (!$util.isString(message.bucket)) return "bucket: string expected";
    if (
      message.bucketPrefix != null &&
      Object.hasOwnProperty.call(message, "bucketPrefix")
    )
      if (!$util.isString(message.bucketPrefix))
        return "bucketPrefix: string expected";
    if (message.from != null && Object.hasOwnProperty.call(message, "from"))
      if (!$util.isString(message.from)) return "from: string expected";
    if (
      message.envType != null &&
      Object.hasOwnProperty.call(message, "envType")
    )
      if (!$util.isString(message.envType)) return "envType: string expected";
    return null;
  };

  /**
   * Creates a ReqStartPanoramaStitchUpload message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartPanoramaStitchUpload} ReqStartPanoramaStitchUpload
   */
  ReqStartPanoramaStitchUpload.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartPanoramaStitchUpload) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartPanoramaStitchUpload: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartPanoramaStitchUpload();
    if (object.resourceId != null)
      if ($util.Long)
        message.resourceId = $util.Long.fromValue(object.resourceId, true);
      else if (typeof object.resourceId === "string")
        message.resourceId = parseInt(object.resourceId, 10);
      else if (typeof object.resourceId === "number")
        message.resourceId = object.resourceId;
      else if (typeof object.resourceId === "object")
        message.resourceId = new $util.LongBits(
          object.resourceId.low >>> 0,
          object.resourceId.high >>> 0,
        ).toNumber(true);
    if (object.userId != null) message.userId = String(object.userId);
    if (object.appPlatform != null)
      message.appPlatform = object.appPlatform | 0;
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    if (object.ak != null) message.ak = String(object.ak);
    if (object.sk != null) message.sk = String(object.sk);
    if (object.token != null) message.token = String(object.token);
    if (object.bucket != null) message.bucket = String(object.bucket);
    if (object.bucketPrefix != null)
      message.bucketPrefix = String(object.bucketPrefix);
    if (object.from != null) message.from = String(object.from);
    if (object.envType != null) message.envType = String(object.envType);
    return message;
  };

  /**
   * Creates a plain object from a ReqStartPanoramaStitchUpload message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {ReqStartPanoramaStitchUpload} message ReqStartPanoramaStitchUpload
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartPanoramaStitchUpload.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.resourceId =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.resourceId =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.userId = "";
      object.appPlatform = 0;
      object.panoramaName = "";
      object.ak = "";
      object.sk = "";
      object.token = "";
      object.bucket = "";
      object.bucketPrefix = "";
      object.from = "";
      object.envType = "";
    }
    if (
      message.resourceId != null &&
      Object.hasOwnProperty.call(message, "resourceId")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.resourceId =
          typeof message.resourceId === "number"
            ? BigInt(message.resourceId)
            : $util.Long.fromBits(
                message.resourceId.low >>> 0,
                message.resourceId.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.resourceId === "number")
        object.resourceId =
          options.longs === String
            ? String(message.resourceId)
            : message.resourceId;
      else
        object.resourceId =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.resourceId)
            : options.longs === Number
              ? new $util.LongBits(
                  message.resourceId.low >>> 0,
                  message.resourceId.high >>> 0,
                ).toNumber(true)
              : message.resourceId;
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      object.userId = message.userId;
    if (
      message.appPlatform != null &&
      Object.hasOwnProperty.call(message, "appPlatform")
    )
      object.appPlatform = message.appPlatform;
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    if (message.ak != null && Object.hasOwnProperty.call(message, "ak"))
      object.ak = message.ak;
    if (message.sk != null && Object.hasOwnProperty.call(message, "sk"))
      object.sk = message.sk;
    if (message.token != null && Object.hasOwnProperty.call(message, "token"))
      object.token = message.token;
    if (message.bucket != null && Object.hasOwnProperty.call(message, "bucket"))
      object.bucket = message.bucket;
    if (
      message.bucketPrefix != null &&
      Object.hasOwnProperty.call(message, "bucketPrefix")
    )
      object.bucketPrefix = message.bucketPrefix;
    if (message.from != null && Object.hasOwnProperty.call(message, "from"))
      object.from = message.from;
    if (
      message.envType != null &&
      Object.hasOwnProperty.call(message, "envType")
    )
      object.envType = message.envType;
    return object;
  };

  /**
   * Converts this ReqStartPanoramaStitchUpload to JSON.
   * @function toJSON
   * @memberof ReqStartPanoramaStitchUpload
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartPanoramaStitchUpload.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartPanoramaStitchUpload
   * @function getTypeUrl
   * @memberof ReqStartPanoramaStitchUpload
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartPanoramaStitchUpload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartPanoramaStitchUpload";
  };

  return ReqStartPanoramaStitchUpload;
})();

$root.ResStartPanoramaStitchUpload = (function () {
  /**
   * Properties of a ResStartPanoramaStitchUpload.
   * @exports IResStartPanoramaStitchUpload
   * @interface IResStartPanoramaStitchUpload
   * @property {number|null} [code] ResStartPanoramaStitchUpload code
   * @property {string|null} [currentPanoramaName] ResStartPanoramaStitchUpload currentPanoramaName
   * @property {string|null} [currentUserId] ResStartPanoramaStitchUpload currentUserId
   */

  /**
   * Constructs a new ResStartPanoramaStitchUpload.
   * @exports ResStartPanoramaStitchUpload
   * @classdesc Represents a ResStartPanoramaStitchUpload.
   * @implements IResStartPanoramaStitchUpload
   * @constructor
   * @param {IResStartPanoramaStitchUpload=} [properties] Properties to set
   */
  function ResStartPanoramaStitchUpload(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResStartPanoramaStitchUpload code.
   * @member {number} code
   * @memberof ResStartPanoramaStitchUpload
   * @instance
   */
  ResStartPanoramaStitchUpload.prototype.code = 0;

  /**
   * ResStartPanoramaStitchUpload currentPanoramaName.
   * @member {string} currentPanoramaName
   * @memberof ResStartPanoramaStitchUpload
   * @instance
   */
  ResStartPanoramaStitchUpload.prototype.currentPanoramaName = "";

  /**
   * ResStartPanoramaStitchUpload currentUserId.
   * @member {string} currentUserId
   * @memberof ResStartPanoramaStitchUpload
   * @instance
   */
  ResStartPanoramaStitchUpload.prototype.currentUserId = "";

  /**
   * Creates a new ResStartPanoramaStitchUpload instance using the specified properties.
   * @function create
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {IResStartPanoramaStitchUpload=} [properties] Properties to set
   * @returns {ResStartPanoramaStitchUpload} ResStartPanoramaStitchUpload instance
   */
  ResStartPanoramaStitchUpload.create = function create(properties) {
    return new ResStartPanoramaStitchUpload(properties);
  };

  /**
   * Encodes the specified ResStartPanoramaStitchUpload message. Does not implicitly {@link ResStartPanoramaStitchUpload.verify|verify} messages.
   * @function encode
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {IResStartPanoramaStitchUpload} message ResStartPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartPanoramaStitchUpload.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      writer
        .uint32(/* id 2, wireType 2 =*/ 18)
        .string(message.currentPanoramaName);
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.currentUserId);
    return writer;
  };

  /**
   * Encodes the specified ResStartPanoramaStitchUpload message, length delimited. Does not implicitly {@link ResStartPanoramaStitchUpload.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {IResStartPanoramaStitchUpload} message ResStartPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStartPanoramaStitchUpload.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResStartPanoramaStitchUpload message from the specified reader or buffer.
   * @function decode
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResStartPanoramaStitchUpload} ResStartPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartPanoramaStitchUpload.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResStartPanoramaStitchUpload();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.currentPanoramaName = reader.string();
          break;
        }
        case 3: {
          message.currentUserId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResStartPanoramaStitchUpload message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResStartPanoramaStitchUpload} ResStartPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStartPanoramaStitchUpload.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResStartPanoramaStitchUpload message.
   * @function verify
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResStartPanoramaStitchUpload.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      if (!$util.isString(message.currentPanoramaName))
        return "currentPanoramaName: string expected";
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      if (!$util.isString(message.currentUserId))
        return "currentUserId: string expected";
    return null;
  };

  /**
   * Creates a ResStartPanoramaStitchUpload message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResStartPanoramaStitchUpload} ResStartPanoramaStitchUpload
   */
  ResStartPanoramaStitchUpload.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResStartPanoramaStitchUpload) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResStartPanoramaStitchUpload: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResStartPanoramaStitchUpload();
    if (object.code != null) message.code = object.code | 0;
    if (object.currentPanoramaName != null)
      message.currentPanoramaName = String(object.currentPanoramaName);
    if (object.currentUserId != null)
      message.currentUserId = String(object.currentUserId);
    return message;
  };

  /**
   * Creates a plain object from a ResStartPanoramaStitchUpload message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {ResStartPanoramaStitchUpload} message ResStartPanoramaStitchUpload
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResStartPanoramaStitchUpload.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.currentPanoramaName = "";
      object.currentUserId = "";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      object.currentPanoramaName = message.currentPanoramaName;
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      object.currentUserId = message.currentUserId;
    return object;
  };

  /**
   * Converts this ResStartPanoramaStitchUpload to JSON.
   * @function toJSON
   * @memberof ResStartPanoramaStitchUpload
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResStartPanoramaStitchUpload.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResStartPanoramaStitchUpload
   * @function getTypeUrl
   * @memberof ResStartPanoramaStitchUpload
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResStartPanoramaStitchUpload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResStartPanoramaStitchUpload";
  };

  return ResStartPanoramaStitchUpload;
})();

$root.ResStopPanoramaStitchUpload = (function () {
  /**
   * Properties of a ResStopPanoramaStitchUpload.
   * @exports IResStopPanoramaStitchUpload
   * @interface IResStopPanoramaStitchUpload
   * @property {number|null} [code] ResStopPanoramaStitchUpload code
   * @property {string|null} [currentPanoramaName] ResStopPanoramaStitchUpload currentPanoramaName
   * @property {string|null} [currentUserId] ResStopPanoramaStitchUpload currentUserId
   */

  /**
   * Constructs a new ResStopPanoramaStitchUpload.
   * @exports ResStopPanoramaStitchUpload
   * @classdesc Represents a ResStopPanoramaStitchUpload.
   * @implements IResStopPanoramaStitchUpload
   * @constructor
   * @param {IResStopPanoramaStitchUpload=} [properties] Properties to set
   */
  function ResStopPanoramaStitchUpload(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResStopPanoramaStitchUpload code.
   * @member {number} code
   * @memberof ResStopPanoramaStitchUpload
   * @instance
   */
  ResStopPanoramaStitchUpload.prototype.code = 0;

  /**
   * ResStopPanoramaStitchUpload currentPanoramaName.
   * @member {string} currentPanoramaName
   * @memberof ResStopPanoramaStitchUpload
   * @instance
   */
  ResStopPanoramaStitchUpload.prototype.currentPanoramaName = "";

  /**
   * ResStopPanoramaStitchUpload currentUserId.
   * @member {string} currentUserId
   * @memberof ResStopPanoramaStitchUpload
   * @instance
   */
  ResStopPanoramaStitchUpload.prototype.currentUserId = "";

  /**
   * Creates a new ResStopPanoramaStitchUpload instance using the specified properties.
   * @function create
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {IResStopPanoramaStitchUpload=} [properties] Properties to set
   * @returns {ResStopPanoramaStitchUpload} ResStopPanoramaStitchUpload instance
   */
  ResStopPanoramaStitchUpload.create = function create(properties) {
    return new ResStopPanoramaStitchUpload(properties);
  };

  /**
   * Encodes the specified ResStopPanoramaStitchUpload message. Does not implicitly {@link ResStopPanoramaStitchUpload.verify|verify} messages.
   * @function encode
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {IResStopPanoramaStitchUpload} message ResStopPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStopPanoramaStitchUpload.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      writer
        .uint32(/* id 2, wireType 2 =*/ 18)
        .string(message.currentPanoramaName);
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.currentUserId);
    return writer;
  };

  /**
   * Encodes the specified ResStopPanoramaStitchUpload message, length delimited. Does not implicitly {@link ResStopPanoramaStitchUpload.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {IResStopPanoramaStitchUpload} message ResStopPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResStopPanoramaStitchUpload.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResStopPanoramaStitchUpload message from the specified reader or buffer.
   * @function decode
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResStopPanoramaStitchUpload} ResStopPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStopPanoramaStitchUpload.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResStopPanoramaStitchUpload();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.currentPanoramaName = reader.string();
          break;
        }
        case 3: {
          message.currentUserId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResStopPanoramaStitchUpload message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResStopPanoramaStitchUpload} ResStopPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResStopPanoramaStitchUpload.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResStopPanoramaStitchUpload message.
   * @function verify
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResStopPanoramaStitchUpload.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      if (!$util.isString(message.currentPanoramaName))
        return "currentPanoramaName: string expected";
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      if (!$util.isString(message.currentUserId))
        return "currentUserId: string expected";
    return null;
  };

  /**
   * Creates a ResStopPanoramaStitchUpload message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResStopPanoramaStitchUpload} ResStopPanoramaStitchUpload
   */
  ResStopPanoramaStitchUpload.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResStopPanoramaStitchUpload) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResStopPanoramaStitchUpload: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResStopPanoramaStitchUpload();
    if (object.code != null) message.code = object.code | 0;
    if (object.currentPanoramaName != null)
      message.currentPanoramaName = String(object.currentPanoramaName);
    if (object.currentUserId != null)
      message.currentUserId = String(object.currentUserId);
    return message;
  };

  /**
   * Creates a plain object from a ResStopPanoramaStitchUpload message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {ResStopPanoramaStitchUpload} message ResStopPanoramaStitchUpload
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResStopPanoramaStitchUpload.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.currentPanoramaName = "";
      object.currentUserId = "";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.currentPanoramaName != null &&
      Object.hasOwnProperty.call(message, "currentPanoramaName")
    )
      object.currentPanoramaName = message.currentPanoramaName;
    if (
      message.currentUserId != null &&
      Object.hasOwnProperty.call(message, "currentUserId")
    )
      object.currentUserId = message.currentUserId;
    return object;
  };

  /**
   * Converts this ResStopPanoramaStitchUpload to JSON.
   * @function toJSON
   * @memberof ResStopPanoramaStitchUpload
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResStopPanoramaStitchUpload.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResStopPanoramaStitchUpload
   * @function getTypeUrl
   * @memberof ResStopPanoramaStitchUpload
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResStopPanoramaStitchUpload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResStopPanoramaStitchUpload";
  };

  return ResStopPanoramaStitchUpload;
})();

$root.ReqStopPanorama = (function () {
  /**
   * Properties of a ReqStopPanorama.
   * @exports IReqStopPanorama
   * @interface IReqStopPanorama
   */

  /**
   * Constructs a new ReqStopPanorama.
   * @exports ReqStopPanorama
   * @classdesc Represents a ReqStopPanorama.
   * @implements IReqStopPanorama
   * @constructor
   * @param {IReqStopPanorama=} [properties] Properties to set
   */
  function ReqStopPanorama(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopPanorama instance using the specified properties.
   * @function create
   * @memberof ReqStopPanorama
   * @static
   * @param {IReqStopPanorama=} [properties] Properties to set
   * @returns {ReqStopPanorama} ReqStopPanorama instance
   */
  ReqStopPanorama.create = function create(properties) {
    return new ReqStopPanorama(properties);
  };

  /**
   * Encodes the specified ReqStopPanorama message. Does not implicitly {@link ReqStopPanorama.verify|verify} messages.
   * @function encode
   * @memberof ReqStopPanorama
   * @static
   * @param {IReqStopPanorama} message ReqStopPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanorama.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopPanorama message, length delimited. Does not implicitly {@link ReqStopPanorama.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopPanorama
   * @static
   * @param {IReqStopPanorama} message ReqStopPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanorama.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopPanorama message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopPanorama} ReqStopPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanorama.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopPanorama();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopPanorama message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopPanorama} ReqStopPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanorama.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopPanorama message.
   * @function verify
   * @memberof ReqStopPanorama
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopPanorama.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopPanorama message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopPanorama
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopPanorama} ReqStopPanorama
   */
  ReqStopPanorama.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopPanorama) return object;
    return new $root.ReqStopPanorama();
  };

  /**
   * Creates a plain object from a ReqStopPanorama message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopPanorama
   * @static
   * @param {ReqStopPanorama} message ReqStopPanorama
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopPanorama.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopPanorama to JSON.
   * @function toJSON
   * @memberof ReqStopPanorama
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopPanorama.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopPanorama
   * @function getTypeUrl
   * @memberof ReqStopPanorama
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopPanorama.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopPanorama";
  };

  return ReqStopPanorama;
})();

$root.ReqStopPanoramaStitchUpload = (function () {
  /**
   * Properties of a ReqStopPanoramaStitchUpload.
   * @exports IReqStopPanoramaStitchUpload
   * @interface IReqStopPanoramaStitchUpload
   * @property {string|null} [userId] ReqStopPanoramaStitchUpload userId
   */

  /**
   * Constructs a new ReqStopPanoramaStitchUpload.
   * @exports ReqStopPanoramaStitchUpload
   * @classdesc Represents a ReqStopPanoramaStitchUpload.
   * @implements IReqStopPanoramaStitchUpload
   * @constructor
   * @param {IReqStopPanoramaStitchUpload=} [properties] Properties to set
   */
  function ReqStopPanoramaStitchUpload(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStopPanoramaStitchUpload userId.
   * @member {string} userId
   * @memberof ReqStopPanoramaStitchUpload
   * @instance
   */
  ReqStopPanoramaStitchUpload.prototype.userId = "";

  /**
   * Creates a new ReqStopPanoramaStitchUpload instance using the specified properties.
   * @function create
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {IReqStopPanoramaStitchUpload=} [properties] Properties to set
   * @returns {ReqStopPanoramaStitchUpload} ReqStopPanoramaStitchUpload instance
   */
  ReqStopPanoramaStitchUpload.create = function create(properties) {
    return new ReqStopPanoramaStitchUpload(properties);
  };

  /**
   * Encodes the specified ReqStopPanoramaStitchUpload message. Does not implicitly {@link ReqStopPanoramaStitchUpload.verify|verify} messages.
   * @function encode
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {IReqStopPanoramaStitchUpload} message ReqStopPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaStitchUpload.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.userId);
    return writer;
  };

  /**
   * Encodes the specified ReqStopPanoramaStitchUpload message, length delimited. Does not implicitly {@link ReqStopPanoramaStitchUpload.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {IReqStopPanoramaStitchUpload} message ReqStopPanoramaStitchUpload message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaStitchUpload.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopPanoramaStitchUpload message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopPanoramaStitchUpload} ReqStopPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaStitchUpload.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopPanoramaStitchUpload();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.userId = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopPanoramaStitchUpload message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopPanoramaStitchUpload} ReqStopPanoramaStitchUpload
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaStitchUpload.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopPanoramaStitchUpload message.
   * @function verify
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopPanoramaStitchUpload.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      if (!$util.isString(message.userId)) return "userId: string expected";
    return null;
  };

  /**
   * Creates a ReqStopPanoramaStitchUpload message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopPanoramaStitchUpload} ReqStopPanoramaStitchUpload
   */
  ReqStopPanoramaStitchUpload.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopPanoramaStitchUpload) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStopPanoramaStitchUpload: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStopPanoramaStitchUpload();
    if (object.userId != null) message.userId = String(object.userId);
    return message;
  };

  /**
   * Creates a plain object from a ReqStopPanoramaStitchUpload message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {ReqStopPanoramaStitchUpload} message ReqStopPanoramaStitchUpload
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopPanoramaStitchUpload.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.userId = "";
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      object.userId = message.userId;
    return object;
  };

  /**
   * Converts this ReqStopPanoramaStitchUpload to JSON.
   * @function toJSON
   * @memberof ReqStopPanoramaStitchUpload
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopPanoramaStitchUpload.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopPanoramaStitchUpload
   * @function getTypeUrl
   * @memberof ReqStopPanoramaStitchUpload
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopPanoramaStitchUpload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopPanoramaStitchUpload";
  };

  return ReqStopPanoramaStitchUpload;
})();

$root.ReqGetPanoramaCurrentUploadState = (function () {
  /**
   * Properties of a ReqGetPanoramaCurrentUploadState.
   * @exports IReqGetPanoramaCurrentUploadState
   * @interface IReqGetPanoramaCurrentUploadState
   */

  /**
   * Constructs a new ReqGetPanoramaCurrentUploadState.
   * @exports ReqGetPanoramaCurrentUploadState
   * @classdesc Represents a ReqGetPanoramaCurrentUploadState.
   * @implements IReqGetPanoramaCurrentUploadState
   * @constructor
   * @param {IReqGetPanoramaCurrentUploadState=} [properties] Properties to set
   */
  function ReqGetPanoramaCurrentUploadState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetPanoramaCurrentUploadState instance using the specified properties.
   * @function create
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {IReqGetPanoramaCurrentUploadState=} [properties] Properties to set
   * @returns {ReqGetPanoramaCurrentUploadState} ReqGetPanoramaCurrentUploadState instance
   */
  ReqGetPanoramaCurrentUploadState.create = function create(properties) {
    return new ReqGetPanoramaCurrentUploadState(properties);
  };

  /**
   * Encodes the specified ReqGetPanoramaCurrentUploadState message. Does not implicitly {@link ReqGetPanoramaCurrentUploadState.verify|verify} messages.
   * @function encode
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {IReqGetPanoramaCurrentUploadState} message ReqGetPanoramaCurrentUploadState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetPanoramaCurrentUploadState.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetPanoramaCurrentUploadState message, length delimited. Does not implicitly {@link ReqGetPanoramaCurrentUploadState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {IReqGetPanoramaCurrentUploadState} message ReqGetPanoramaCurrentUploadState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetPanoramaCurrentUploadState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetPanoramaCurrentUploadState message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetPanoramaCurrentUploadState} ReqGetPanoramaCurrentUploadState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetPanoramaCurrentUploadState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetPanoramaCurrentUploadState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetPanoramaCurrentUploadState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetPanoramaCurrentUploadState} ReqGetPanoramaCurrentUploadState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetPanoramaCurrentUploadState.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetPanoramaCurrentUploadState message.
   * @function verify
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetPanoramaCurrentUploadState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetPanoramaCurrentUploadState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetPanoramaCurrentUploadState} ReqGetPanoramaCurrentUploadState
   */
  ReqGetPanoramaCurrentUploadState.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqGetPanoramaCurrentUploadState) return object;
    return new $root.ReqGetPanoramaCurrentUploadState();
  };

  /**
   * Creates a plain object from a ReqGetPanoramaCurrentUploadState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {ReqGetPanoramaCurrentUploadState} message ReqGetPanoramaCurrentUploadState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetPanoramaCurrentUploadState.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetPanoramaCurrentUploadState to JSON.
   * @function toJSON
   * @memberof ReqGetPanoramaCurrentUploadState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetPanoramaCurrentUploadState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetPanoramaCurrentUploadState
   * @function getTypeUrl
   * @memberof ReqGetPanoramaCurrentUploadState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetPanoramaCurrentUploadState.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetPanoramaCurrentUploadState";
  };

  return ReqGetPanoramaCurrentUploadState;
})();

$root.ResGetPanoramaCurrentUploadState = (function () {
  /**
   * Properties of a ResGetPanoramaCurrentUploadState.
   * @exports IResGetPanoramaCurrentUploadState
   * @interface IResGetPanoramaCurrentUploadState
   * @property {number|null} [code] ResGetPanoramaCurrentUploadState code
   * @property {string|null} [userId] ResGetPanoramaCurrentUploadState userId
   * @property {string|null} [busiNo] ResGetPanoramaCurrentUploadState busiNo
   * @property {string|null} [panoramaName] ResGetPanoramaCurrentUploadState panoramaName
   * @property {string|null} [mac] ResGetPanoramaCurrentUploadState mac
   * @property {number|null} [totalFilesNum] ResGetPanoramaCurrentUploadState totalFilesNum
   * @property {number|null} [compressedFilesNum] ResGetPanoramaCurrentUploadState compressedFilesNum
   * @property {number|Long|null} [totalSize] ResGetPanoramaCurrentUploadState totalSize
   * @property {number|Long|null} [uploadedSize] ResGetPanoramaCurrentUploadState uploadedSize
   * @property {number|null} [step] ResGetPanoramaCurrentUploadState step
   */

  /**
   * Constructs a new ResGetPanoramaCurrentUploadState.
   * @exports ResGetPanoramaCurrentUploadState
   * @classdesc Represents a ResGetPanoramaCurrentUploadState.
   * @implements IResGetPanoramaCurrentUploadState
   * @constructor
   * @param {IResGetPanoramaCurrentUploadState=} [properties] Properties to set
   */
  function ResGetPanoramaCurrentUploadState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetPanoramaCurrentUploadState code.
   * @member {number} code
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.code = 0;

  /**
   * ResGetPanoramaCurrentUploadState userId.
   * @member {string} userId
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.userId = "";

  /**
   * ResGetPanoramaCurrentUploadState busiNo.
   * @member {string} busiNo
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.busiNo = "";

  /**
   * ResGetPanoramaCurrentUploadState panoramaName.
   * @member {string} panoramaName
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.panoramaName = "";

  /**
   * ResGetPanoramaCurrentUploadState mac.
   * @member {string} mac
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.mac = "";

  /**
   * ResGetPanoramaCurrentUploadState totalFilesNum.
   * @member {number} totalFilesNum
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.totalFilesNum = 0;

  /**
   * ResGetPanoramaCurrentUploadState compressedFilesNum.
   * @member {number} compressedFilesNum
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.compressedFilesNum = 0;

  /**
   * ResGetPanoramaCurrentUploadState totalSize.
   * @member {number|Long} totalSize
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.totalSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ResGetPanoramaCurrentUploadState uploadedSize.
   * @member {number|Long} uploadedSize
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.uploadedSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ResGetPanoramaCurrentUploadState step.
   * @member {number} step
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   */
  ResGetPanoramaCurrentUploadState.prototype.step = 0;

  /**
   * Creates a new ResGetPanoramaCurrentUploadState instance using the specified properties.
   * @function create
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {IResGetPanoramaCurrentUploadState=} [properties] Properties to set
   * @returns {ResGetPanoramaCurrentUploadState} ResGetPanoramaCurrentUploadState instance
   */
  ResGetPanoramaCurrentUploadState.create = function create(properties) {
    return new ResGetPanoramaCurrentUploadState(properties);
  };

  /**
   * Encodes the specified ResGetPanoramaCurrentUploadState message. Does not implicitly {@link ResGetPanoramaCurrentUploadState.verify|verify} messages.
   * @function encode
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {IResGetPanoramaCurrentUploadState} message ResGetPanoramaCurrentUploadState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetPanoramaCurrentUploadState.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.userId);
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.busiNo);
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.panoramaName);
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.mac);
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).uint32(message.totalFilesNum);
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      writer
        .uint32(/* id 7, wireType 0 =*/ 56)
        .uint32(message.compressedFilesNum);
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).uint64(message.totalSize);
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).uint64(message.uploadedSize);
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      writer.uint32(/* id 10, wireType 0 =*/ 80).uint32(message.step);
    return writer;
  };

  /**
   * Encodes the specified ResGetPanoramaCurrentUploadState message, length delimited. Does not implicitly {@link ResGetPanoramaCurrentUploadState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {IResGetPanoramaCurrentUploadState} message ResGetPanoramaCurrentUploadState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetPanoramaCurrentUploadState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetPanoramaCurrentUploadState message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetPanoramaCurrentUploadState} ResGetPanoramaCurrentUploadState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetPanoramaCurrentUploadState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetPanoramaCurrentUploadState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.userId = reader.string();
          break;
        }
        case 3: {
          message.busiNo = reader.string();
          break;
        }
        case 4: {
          message.panoramaName = reader.string();
          break;
        }
        case 5: {
          message.mac = reader.string();
          break;
        }
        case 6: {
          message.totalFilesNum = reader.uint32();
          break;
        }
        case 7: {
          message.compressedFilesNum = reader.uint32();
          break;
        }
        case 8: {
          message.totalSize = reader.uint64();
          break;
        }
        case 9: {
          message.uploadedSize = reader.uint64();
          break;
        }
        case 10: {
          message.step = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetPanoramaCurrentUploadState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetPanoramaCurrentUploadState} ResGetPanoramaCurrentUploadState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetPanoramaCurrentUploadState.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetPanoramaCurrentUploadState message.
   * @function verify
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetPanoramaCurrentUploadState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      if (!$util.isString(message.userId)) return "userId: string expected";
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      if (!$util.isString(message.busiNo)) return "busiNo: string expected";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      if (!$util.isString(message.mac)) return "mac: string expected";
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      if (!$util.isInteger(message.totalFilesNum))
        return "totalFilesNum: integer expected";
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      if (!$util.isInteger(message.compressedFilesNum))
        return "compressedFilesNum: integer expected";
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      if (
        !$util.isInteger(message.totalSize) &&
        !(
          message.totalSize &&
          $util.isInteger(message.totalSize.low) &&
          $util.isInteger(message.totalSize.high)
        )
      )
        return "totalSize: integer|Long expected";
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      if (
        !$util.isInteger(message.uploadedSize) &&
        !(
          message.uploadedSize &&
          $util.isInteger(message.uploadedSize.low) &&
          $util.isInteger(message.uploadedSize.high)
        )
      )
        return "uploadedSize: integer|Long expected";
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      if (!$util.isInteger(message.step)) return "step: integer expected";
    return null;
  };

  /**
   * Creates a ResGetPanoramaCurrentUploadState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetPanoramaCurrentUploadState} ResGetPanoramaCurrentUploadState
   */
  ResGetPanoramaCurrentUploadState.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ResGetPanoramaCurrentUploadState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetPanoramaCurrentUploadState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetPanoramaCurrentUploadState();
    if (object.code != null) message.code = object.code | 0;
    if (object.userId != null) message.userId = String(object.userId);
    if (object.busiNo != null) message.busiNo = String(object.busiNo);
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    if (object.mac != null) message.mac = String(object.mac);
    if (object.totalFilesNum != null)
      message.totalFilesNum = object.totalFilesNum >>> 0;
    if (object.compressedFilesNum != null)
      message.compressedFilesNum = object.compressedFilesNum >>> 0;
    if (object.totalSize != null)
      if ($util.Long)
        message.totalSize = $util.Long.fromValue(object.totalSize, true);
      else if (typeof object.totalSize === "string")
        message.totalSize = parseInt(object.totalSize, 10);
      else if (typeof object.totalSize === "number")
        message.totalSize = object.totalSize;
      else if (typeof object.totalSize === "object")
        message.totalSize = new $util.LongBits(
          object.totalSize.low >>> 0,
          object.totalSize.high >>> 0,
        ).toNumber(true);
    if (object.uploadedSize != null)
      if ($util.Long)
        message.uploadedSize = $util.Long.fromValue(object.uploadedSize, true);
      else if (typeof object.uploadedSize === "string")
        message.uploadedSize = parseInt(object.uploadedSize, 10);
      else if (typeof object.uploadedSize === "number")
        message.uploadedSize = object.uploadedSize;
      else if (typeof object.uploadedSize === "object")
        message.uploadedSize = new $util.LongBits(
          object.uploadedSize.low >>> 0,
          object.uploadedSize.high >>> 0,
        ).toNumber(true);
    if (object.step != null) message.step = object.step >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetPanoramaCurrentUploadState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {ResGetPanoramaCurrentUploadState} message ResGetPanoramaCurrentUploadState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetPanoramaCurrentUploadState.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.userId = "";
      object.busiNo = "";
      object.panoramaName = "";
      object.mac = "";
      object.totalFilesNum = 0;
      object.compressedFilesNum = 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.totalSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.totalSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.uploadedSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.uploadedSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.step = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      object.userId = message.userId;
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      object.busiNo = message.busiNo;
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      object.mac = message.mac;
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      object.totalFilesNum = message.totalFilesNum;
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      object.compressedFilesNum = message.compressedFilesNum;
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.totalSize =
          typeof message.totalSize === "number"
            ? BigInt(message.totalSize)
            : $util.Long.fromBits(
                message.totalSize.low >>> 0,
                message.totalSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.totalSize === "number")
        object.totalSize =
          options.longs === String
            ? String(message.totalSize)
            : message.totalSize;
      else
        object.totalSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.totalSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.totalSize.low >>> 0,
                  message.totalSize.high >>> 0,
                ).toNumber(true)
              : message.totalSize;
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.uploadedSize =
          typeof message.uploadedSize === "number"
            ? BigInt(message.uploadedSize)
            : $util.Long.fromBits(
                message.uploadedSize.low >>> 0,
                message.uploadedSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.uploadedSize === "number")
        object.uploadedSize =
          options.longs === String
            ? String(message.uploadedSize)
            : message.uploadedSize;
      else
        object.uploadedSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.uploadedSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.uploadedSize.low >>> 0,
                  message.uploadedSize.high >>> 0,
                ).toNumber(true)
              : message.uploadedSize;
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      object.step = message.step;
    return object;
  };

  /**
   * Converts this ResGetPanoramaCurrentUploadState to JSON.
   * @function toJSON
   * @memberof ResGetPanoramaCurrentUploadState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetPanoramaCurrentUploadState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetPanoramaCurrentUploadState
   * @function getTypeUrl
   * @memberof ResGetPanoramaCurrentUploadState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetPanoramaCurrentUploadState.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetPanoramaCurrentUploadState";
  };

  return ResGetPanoramaCurrentUploadState;
})();

$root.ReqGetUploadPredict = (function () {
  /**
   * Properties of a ReqGetUploadPredict.
   * @exports IReqGetUploadPredict
   * @interface IReqGetUploadPredict
   * @property {string|null} [panoramaName] ReqGetUploadPredict panoramaName
   */

  /**
   * Constructs a new ReqGetUploadPredict.
   * @exports ReqGetUploadPredict
   * @classdesc Represents a ReqGetUploadPredict.
   * @implements IReqGetUploadPredict
   * @constructor
   * @param {IReqGetUploadPredict=} [properties] Properties to set
   */
  function ReqGetUploadPredict(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetUploadPredict panoramaName.
   * @member {string} panoramaName
   * @memberof ReqGetUploadPredict
   * @instance
   */
  ReqGetUploadPredict.prototype.panoramaName = "";

  /**
   * Creates a new ReqGetUploadPredict instance using the specified properties.
   * @function create
   * @memberof ReqGetUploadPredict
   * @static
   * @param {IReqGetUploadPredict=} [properties] Properties to set
   * @returns {ReqGetUploadPredict} ReqGetUploadPredict instance
   */
  ReqGetUploadPredict.create = function create(properties) {
    return new ReqGetUploadPredict(properties);
  };

  /**
   * Encodes the specified ReqGetUploadPredict message. Does not implicitly {@link ReqGetUploadPredict.verify|verify} messages.
   * @function encode
   * @memberof ReqGetUploadPredict
   * @static
   * @param {IReqGetUploadPredict} message ReqGetUploadPredict message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetUploadPredict.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.panoramaName);
    return writer;
  };

  /**
   * Encodes the specified ReqGetUploadPredict message, length delimited. Does not implicitly {@link ReqGetUploadPredict.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetUploadPredict
   * @static
   * @param {IReqGetUploadPredict} message ReqGetUploadPredict message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetUploadPredict.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetUploadPredict message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetUploadPredict
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetUploadPredict} ReqGetUploadPredict
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetUploadPredict.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetUploadPredict();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.panoramaName = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetUploadPredict message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetUploadPredict
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetUploadPredict} ReqGetUploadPredict
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetUploadPredict.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetUploadPredict message.
   * @function verify
   * @memberof ReqGetUploadPredict
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetUploadPredict.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    return null;
  };

  /**
   * Creates a ReqGetUploadPredict message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetUploadPredict
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetUploadPredict} ReqGetUploadPredict
   */
  ReqGetUploadPredict.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetUploadPredict) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetUploadPredict: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetUploadPredict();
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetUploadPredict message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetUploadPredict
   * @static
   * @param {ReqGetUploadPredict} message ReqGetUploadPredict
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetUploadPredict.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.panoramaName = "";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    return object;
  };

  /**
   * Converts this ReqGetUploadPredict to JSON.
   * @function toJSON
   * @memberof ReqGetUploadPredict
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetUploadPredict.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetUploadPredict
   * @function getTypeUrl
   * @memberof ReqGetUploadPredict
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetUploadPredict.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetUploadPredict";
  };

  return ReqGetUploadPredict;
})();

$root.ResGetUploadPredict = (function () {
  /**
   * Properties of a ResGetUploadPredict.
   * @exports IResGetUploadPredict
   * @interface IResGetUploadPredict
   * @property {number|null} [code] ResGetUploadPredict code
   * @property {string|null} [panoramaName] ResGetUploadPredict panoramaName
   * @property {number|null} [fileNums] ResGetUploadPredict fileNums
   * @property {string|null} [resolution] ResGetUploadPredict resolution
   * @property {number|Long|null} [cloudDataSize] ResGetUploadPredict cloudDataSize
   * @property {number|Long|null} [zipDataSize] ResGetUploadPredict zipDataSize
   * @property {number|Long|null} [appZipDataSize] ResGetUploadPredict appZipDataSize
   */

  /**
   * Constructs a new ResGetUploadPredict.
   * @exports ResGetUploadPredict
   * @classdesc Represents a ResGetUploadPredict.
   * @implements IResGetUploadPredict
   * @constructor
   * @param {IResGetUploadPredict=} [properties] Properties to set
   */
  function ResGetUploadPredict(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetUploadPredict code.
   * @member {number} code
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.code = 0;

  /**
   * ResGetUploadPredict panoramaName.
   * @member {string} panoramaName
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.panoramaName = "";

  /**
   * ResGetUploadPredict fileNums.
   * @member {number} fileNums
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.fileNums = 0;

  /**
   * ResGetUploadPredict resolution.
   * @member {string} resolution
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.resolution = "";

  /**
   * ResGetUploadPredict cloudDataSize.
   * @member {number|Long} cloudDataSize
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.cloudDataSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ResGetUploadPredict zipDataSize.
   * @member {number|Long} zipDataSize
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.zipDataSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ResGetUploadPredict appZipDataSize.
   * @member {number|Long} appZipDataSize
   * @memberof ResGetUploadPredict
   * @instance
   */
  ResGetUploadPredict.prototype.appZipDataSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * Creates a new ResGetUploadPredict instance using the specified properties.
   * @function create
   * @memberof ResGetUploadPredict
   * @static
   * @param {IResGetUploadPredict=} [properties] Properties to set
   * @returns {ResGetUploadPredict} ResGetUploadPredict instance
   */
  ResGetUploadPredict.create = function create(properties) {
    return new ResGetUploadPredict(properties);
  };

  /**
   * Encodes the specified ResGetUploadPredict message. Does not implicitly {@link ResGetUploadPredict.verify|verify} messages.
   * @function encode
   * @memberof ResGetUploadPredict
   * @static
   * @param {IResGetUploadPredict} message ResGetUploadPredict message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetUploadPredict.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.panoramaName);
    if (
      message.fileNums != null &&
      Object.hasOwnProperty.call(message, "fileNums")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.fileNums);
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.resolution);
    if (
      message.cloudDataSize != null &&
      Object.hasOwnProperty.call(message, "cloudDataSize")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).uint64(message.cloudDataSize);
    if (
      message.zipDataSize != null &&
      Object.hasOwnProperty.call(message, "zipDataSize")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).uint64(message.zipDataSize);
    if (
      message.appZipDataSize != null &&
      Object.hasOwnProperty.call(message, "appZipDataSize")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).uint64(message.appZipDataSize);
    return writer;
  };

  /**
   * Encodes the specified ResGetUploadPredict message, length delimited. Does not implicitly {@link ResGetUploadPredict.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetUploadPredict
   * @static
   * @param {IResGetUploadPredict} message ResGetUploadPredict message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetUploadPredict.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetUploadPredict message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetUploadPredict
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetUploadPredict} ResGetUploadPredict
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetUploadPredict.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetUploadPredict();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.panoramaName = reader.string();
          break;
        }
        case 3: {
          message.fileNums = reader.uint32();
          break;
        }
        case 4: {
          message.resolution = reader.string();
          break;
        }
        case 5: {
          message.cloudDataSize = reader.uint64();
          break;
        }
        case 6: {
          message.zipDataSize = reader.uint64();
          break;
        }
        case 7: {
          message.appZipDataSize = reader.uint64();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetUploadPredict message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetUploadPredict
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetUploadPredict} ResGetUploadPredict
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetUploadPredict.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetUploadPredict message.
   * @function verify
   * @memberof ResGetUploadPredict
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetUploadPredict.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    if (
      message.fileNums != null &&
      Object.hasOwnProperty.call(message, "fileNums")
    )
      if (!$util.isInteger(message.fileNums))
        return "fileNums: integer expected";
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      if (!$util.isString(message.resolution))
        return "resolution: string expected";
    if (
      message.cloudDataSize != null &&
      Object.hasOwnProperty.call(message, "cloudDataSize")
    )
      if (
        !$util.isInteger(message.cloudDataSize) &&
        !(
          message.cloudDataSize &&
          $util.isInteger(message.cloudDataSize.low) &&
          $util.isInteger(message.cloudDataSize.high)
        )
      )
        return "cloudDataSize: integer|Long expected";
    if (
      message.zipDataSize != null &&
      Object.hasOwnProperty.call(message, "zipDataSize")
    )
      if (
        !$util.isInteger(message.zipDataSize) &&
        !(
          message.zipDataSize &&
          $util.isInteger(message.zipDataSize.low) &&
          $util.isInteger(message.zipDataSize.high)
        )
      )
        return "zipDataSize: integer|Long expected";
    if (
      message.appZipDataSize != null &&
      Object.hasOwnProperty.call(message, "appZipDataSize")
    )
      if (
        !$util.isInteger(message.appZipDataSize) &&
        !(
          message.appZipDataSize &&
          $util.isInteger(message.appZipDataSize.low) &&
          $util.isInteger(message.appZipDataSize.high)
        )
      )
        return "appZipDataSize: integer|Long expected";
    return null;
  };

  /**
   * Creates a ResGetUploadPredict message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetUploadPredict
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetUploadPredict} ResGetUploadPredict
   */
  ResGetUploadPredict.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetUploadPredict) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetUploadPredict: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetUploadPredict();
    if (object.code != null) message.code = object.code | 0;
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    if (object.fileNums != null) message.fileNums = object.fileNums >>> 0;
    if (object.resolution != null)
      message.resolution = String(object.resolution);
    if (object.cloudDataSize != null)
      if ($util.Long)
        message.cloudDataSize = $util.Long.fromValue(
          object.cloudDataSize,
          true,
        );
      else if (typeof object.cloudDataSize === "string")
        message.cloudDataSize = parseInt(object.cloudDataSize, 10);
      else if (typeof object.cloudDataSize === "number")
        message.cloudDataSize = object.cloudDataSize;
      else if (typeof object.cloudDataSize === "object")
        message.cloudDataSize = new $util.LongBits(
          object.cloudDataSize.low >>> 0,
          object.cloudDataSize.high >>> 0,
        ).toNumber(true);
    if (object.zipDataSize != null)
      if ($util.Long)
        message.zipDataSize = $util.Long.fromValue(object.zipDataSize, true);
      else if (typeof object.zipDataSize === "string")
        message.zipDataSize = parseInt(object.zipDataSize, 10);
      else if (typeof object.zipDataSize === "number")
        message.zipDataSize = object.zipDataSize;
      else if (typeof object.zipDataSize === "object")
        message.zipDataSize = new $util.LongBits(
          object.zipDataSize.low >>> 0,
          object.zipDataSize.high >>> 0,
        ).toNumber(true);
    if (object.appZipDataSize != null)
      if ($util.Long)
        message.appZipDataSize = $util.Long.fromValue(
          object.appZipDataSize,
          true,
        );
      else if (typeof object.appZipDataSize === "string")
        message.appZipDataSize = parseInt(object.appZipDataSize, 10);
      else if (typeof object.appZipDataSize === "number")
        message.appZipDataSize = object.appZipDataSize;
      else if (typeof object.appZipDataSize === "object")
        message.appZipDataSize = new $util.LongBits(
          object.appZipDataSize.low >>> 0,
          object.appZipDataSize.high >>> 0,
        ).toNumber(true);
    return message;
  };

  /**
   * Creates a plain object from a ResGetUploadPredict message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetUploadPredict
   * @static
   * @param {ResGetUploadPredict} message ResGetUploadPredict
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetUploadPredict.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.panoramaName = "";
      object.fileNums = 0;
      object.resolution = "";
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.cloudDataSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.cloudDataSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.zipDataSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.zipDataSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.appZipDataSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.appZipDataSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    if (
      message.fileNums != null &&
      Object.hasOwnProperty.call(message, "fileNums")
    )
      object.fileNums = message.fileNums;
    if (
      message.resolution != null &&
      Object.hasOwnProperty.call(message, "resolution")
    )
      object.resolution = message.resolution;
    if (
      message.cloudDataSize != null &&
      Object.hasOwnProperty.call(message, "cloudDataSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.cloudDataSize =
          typeof message.cloudDataSize === "number"
            ? BigInt(message.cloudDataSize)
            : $util.Long.fromBits(
                message.cloudDataSize.low >>> 0,
                message.cloudDataSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.cloudDataSize === "number")
        object.cloudDataSize =
          options.longs === String
            ? String(message.cloudDataSize)
            : message.cloudDataSize;
      else
        object.cloudDataSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.cloudDataSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.cloudDataSize.low >>> 0,
                  message.cloudDataSize.high >>> 0,
                ).toNumber(true)
              : message.cloudDataSize;
    if (
      message.zipDataSize != null &&
      Object.hasOwnProperty.call(message, "zipDataSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.zipDataSize =
          typeof message.zipDataSize === "number"
            ? BigInt(message.zipDataSize)
            : $util.Long.fromBits(
                message.zipDataSize.low >>> 0,
                message.zipDataSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.zipDataSize === "number")
        object.zipDataSize =
          options.longs === String
            ? String(message.zipDataSize)
            : message.zipDataSize;
      else
        object.zipDataSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.zipDataSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.zipDataSize.low >>> 0,
                  message.zipDataSize.high >>> 0,
                ).toNumber(true)
              : message.zipDataSize;
    if (
      message.appZipDataSize != null &&
      Object.hasOwnProperty.call(message, "appZipDataSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.appZipDataSize =
          typeof message.appZipDataSize === "number"
            ? BigInt(message.appZipDataSize)
            : $util.Long.fromBits(
                message.appZipDataSize.low >>> 0,
                message.appZipDataSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.appZipDataSize === "number")
        object.appZipDataSize =
          options.longs === String
            ? String(message.appZipDataSize)
            : message.appZipDataSize;
      else
        object.appZipDataSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.appZipDataSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.appZipDataSize.low >>> 0,
                  message.appZipDataSize.high >>> 0,
                ).toNumber(true)
              : message.appZipDataSize;
    return object;
  };

  /**
   * Converts this ResGetUploadPredict to JSON.
   * @function toJSON
   * @memberof ResGetUploadPredict
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetUploadPredict.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetUploadPredict
   * @function getTypeUrl
   * @memberof ResGetUploadPredict
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetUploadPredict.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetUploadPredict";
  };

  return ResGetUploadPredict;
})();

$root.PanoramaUploadParam = (function () {
  /**
   * Properties of a PanoramaUploadParam.
   * @exports IPanoramaUploadParam
   * @interface IPanoramaUploadParam
   * @property {string|null} [userId] PanoramaUploadParam userId
   * @property {string|null} [busiNo] PanoramaUploadParam busiNo
   * @property {string|null} [panoramaName] PanoramaUploadParam panoramaName
   * @property {string|null} [mac] PanoramaUploadParam mac
   * @property {number|null} [totalFilesNum] PanoramaUploadParam totalFilesNum
   * @property {number|null} [compressedFilesNum] PanoramaUploadParam compressedFilesNum
   * @property {number|Long|null} [totalSize] PanoramaUploadParam totalSize
   * @property {number|Long|null} [uploadedSize] PanoramaUploadParam uploadedSize
   * @property {number|null} [step] PanoramaUploadParam step
   */

  /**
   * Constructs a new PanoramaUploadParam.
   * @exports PanoramaUploadParam
   * @classdesc Represents a PanoramaUploadParam.
   * @implements IPanoramaUploadParam
   * @constructor
   * @param {IPanoramaUploadParam=} [properties] Properties to set
   */
  function PanoramaUploadParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * PanoramaUploadParam userId.
   * @member {string} userId
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.userId = "";

  /**
   * PanoramaUploadParam busiNo.
   * @member {string} busiNo
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.busiNo = "";

  /**
   * PanoramaUploadParam panoramaName.
   * @member {string} panoramaName
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.panoramaName = "";

  /**
   * PanoramaUploadParam mac.
   * @member {string} mac
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.mac = "";

  /**
   * PanoramaUploadParam totalFilesNum.
   * @member {number} totalFilesNum
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.totalFilesNum = 0;

  /**
   * PanoramaUploadParam compressedFilesNum.
   * @member {number} compressedFilesNum
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.compressedFilesNum = 0;

  /**
   * PanoramaUploadParam totalSize.
   * @member {number|Long} totalSize
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.totalSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * PanoramaUploadParam uploadedSize.
   * @member {number|Long} uploadedSize
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.uploadedSize = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * PanoramaUploadParam step.
   * @member {number} step
   * @memberof PanoramaUploadParam
   * @instance
   */
  PanoramaUploadParam.prototype.step = 0;

  /**
   * Creates a new PanoramaUploadParam instance using the specified properties.
   * @function create
   * @memberof PanoramaUploadParam
   * @static
   * @param {IPanoramaUploadParam=} [properties] Properties to set
   * @returns {PanoramaUploadParam} PanoramaUploadParam instance
   */
  PanoramaUploadParam.create = function create(properties) {
    return new PanoramaUploadParam(properties);
  };

  /**
   * Encodes the specified PanoramaUploadParam message. Does not implicitly {@link PanoramaUploadParam.verify|verify} messages.
   * @function encode
   * @memberof PanoramaUploadParam
   * @static
   * @param {IPanoramaUploadParam} message PanoramaUploadParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  PanoramaUploadParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.userId);
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.busiNo);
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.panoramaName);
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.mac);
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).uint32(message.totalFilesNum);
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      writer
        .uint32(/* id 6, wireType 0 =*/ 48)
        .uint32(message.compressedFilesNum);
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      writer.uint32(/* id 7, wireType 0 =*/ 56).uint64(message.totalSize);
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).uint64(message.uploadedSize);
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      writer.uint32(/* id 9, wireType 0 =*/ 72).uint32(message.step);
    return writer;
  };

  /**
   * Encodes the specified PanoramaUploadParam message, length delimited. Does not implicitly {@link PanoramaUploadParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof PanoramaUploadParam
   * @static
   * @param {IPanoramaUploadParam} message PanoramaUploadParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  PanoramaUploadParam.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a PanoramaUploadParam message from the specified reader or buffer.
   * @function decode
   * @memberof PanoramaUploadParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {PanoramaUploadParam} PanoramaUploadParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  PanoramaUploadParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.PanoramaUploadParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.userId = reader.string();
          break;
        }
        case 2: {
          message.busiNo = reader.string();
          break;
        }
        case 3: {
          message.panoramaName = reader.string();
          break;
        }
        case 4: {
          message.mac = reader.string();
          break;
        }
        case 5: {
          message.totalFilesNum = reader.uint32();
          break;
        }
        case 6: {
          message.compressedFilesNum = reader.uint32();
          break;
        }
        case 7: {
          message.totalSize = reader.uint64();
          break;
        }
        case 8: {
          message.uploadedSize = reader.uint64();
          break;
        }
        case 9: {
          message.step = reader.uint32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a PanoramaUploadParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof PanoramaUploadParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {PanoramaUploadParam} PanoramaUploadParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  PanoramaUploadParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a PanoramaUploadParam message.
   * @function verify
   * @memberof PanoramaUploadParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  PanoramaUploadParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      if (!$util.isString(message.userId)) return "userId: string expected";
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      if (!$util.isString(message.busiNo)) return "busiNo: string expected";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      if (!$util.isString(message.mac)) return "mac: string expected";
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      if (!$util.isInteger(message.totalFilesNum))
        return "totalFilesNum: integer expected";
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      if (!$util.isInteger(message.compressedFilesNum))
        return "compressedFilesNum: integer expected";
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      if (
        !$util.isInteger(message.totalSize) &&
        !(
          message.totalSize &&
          $util.isInteger(message.totalSize.low) &&
          $util.isInteger(message.totalSize.high)
        )
      )
        return "totalSize: integer|Long expected";
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      if (
        !$util.isInteger(message.uploadedSize) &&
        !(
          message.uploadedSize &&
          $util.isInteger(message.uploadedSize.low) &&
          $util.isInteger(message.uploadedSize.high)
        )
      )
        return "uploadedSize: integer|Long expected";
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      if (!$util.isInteger(message.step)) return "step: integer expected";
    return null;
  };

  /**
   * Creates a PanoramaUploadParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof PanoramaUploadParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {PanoramaUploadParam} PanoramaUploadParam
   */
  PanoramaUploadParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.PanoramaUploadParam) return object;
    if (!$util.isObject(object))
      throw TypeError(".PanoramaUploadParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.PanoramaUploadParam();
    if (object.userId != null) message.userId = String(object.userId);
    if (object.busiNo != null) message.busiNo = String(object.busiNo);
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    if (object.mac != null) message.mac = String(object.mac);
    if (object.totalFilesNum != null)
      message.totalFilesNum = object.totalFilesNum >>> 0;
    if (object.compressedFilesNum != null)
      message.compressedFilesNum = object.compressedFilesNum >>> 0;
    if (object.totalSize != null)
      if ($util.Long)
        message.totalSize = $util.Long.fromValue(object.totalSize, true);
      else if (typeof object.totalSize === "string")
        message.totalSize = parseInt(object.totalSize, 10);
      else if (typeof object.totalSize === "number")
        message.totalSize = object.totalSize;
      else if (typeof object.totalSize === "object")
        message.totalSize = new $util.LongBits(
          object.totalSize.low >>> 0,
          object.totalSize.high >>> 0,
        ).toNumber(true);
    if (object.uploadedSize != null)
      if ($util.Long)
        message.uploadedSize = $util.Long.fromValue(object.uploadedSize, true);
      else if (typeof object.uploadedSize === "string")
        message.uploadedSize = parseInt(object.uploadedSize, 10);
      else if (typeof object.uploadedSize === "number")
        message.uploadedSize = object.uploadedSize;
      else if (typeof object.uploadedSize === "object")
        message.uploadedSize = new $util.LongBits(
          object.uploadedSize.low >>> 0,
          object.uploadedSize.high >>> 0,
        ).toNumber(true);
    if (object.step != null) message.step = object.step >>> 0;
    return message;
  };

  /**
   * Creates a plain object from a PanoramaUploadParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof PanoramaUploadParam
   * @static
   * @param {PanoramaUploadParam} message PanoramaUploadParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  PanoramaUploadParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.userId = "";
      object.busiNo = "";
      object.panoramaName = "";
      object.mac = "";
      object.totalFilesNum = 0;
      object.compressedFilesNum = 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.totalSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.totalSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.uploadedSize =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.uploadedSize =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.step = 0;
    }
    if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
      object.userId = message.userId;
    if (message.busiNo != null && Object.hasOwnProperty.call(message, "busiNo"))
      object.busiNo = message.busiNo;
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
      object.mac = message.mac;
    if (
      message.totalFilesNum != null &&
      Object.hasOwnProperty.call(message, "totalFilesNum")
    )
      object.totalFilesNum = message.totalFilesNum;
    if (
      message.compressedFilesNum != null &&
      Object.hasOwnProperty.call(message, "compressedFilesNum")
    )
      object.compressedFilesNum = message.compressedFilesNum;
    if (
      message.totalSize != null &&
      Object.hasOwnProperty.call(message, "totalSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.totalSize =
          typeof message.totalSize === "number"
            ? BigInt(message.totalSize)
            : $util.Long.fromBits(
                message.totalSize.low >>> 0,
                message.totalSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.totalSize === "number")
        object.totalSize =
          options.longs === String
            ? String(message.totalSize)
            : message.totalSize;
      else
        object.totalSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.totalSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.totalSize.low >>> 0,
                  message.totalSize.high >>> 0,
                ).toNumber(true)
              : message.totalSize;
    if (
      message.uploadedSize != null &&
      Object.hasOwnProperty.call(message, "uploadedSize")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.uploadedSize =
          typeof message.uploadedSize === "number"
            ? BigInt(message.uploadedSize)
            : $util.Long.fromBits(
                message.uploadedSize.low >>> 0,
                message.uploadedSize.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.uploadedSize === "number")
        object.uploadedSize =
          options.longs === String
            ? String(message.uploadedSize)
            : message.uploadedSize;
      else
        object.uploadedSize =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.uploadedSize)
            : options.longs === Number
              ? new $util.LongBits(
                  message.uploadedSize.low >>> 0,
                  message.uploadedSize.high >>> 0,
                ).toNumber(true)
              : message.uploadedSize;
    if (message.step != null && Object.hasOwnProperty.call(message, "step"))
      object.step = message.step;
    return object;
  };

  /**
   * Converts this PanoramaUploadParam to JSON.
   * @function toJSON
   * @memberof PanoramaUploadParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  PanoramaUploadParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for PanoramaUploadParam
   * @function getTypeUrl
   * @memberof PanoramaUploadParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  PanoramaUploadParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/PanoramaUploadParam";
  };

  return PanoramaUploadParam;
})();

$root.ReqCompressPanorama = (function () {
  /**
   * Properties of a ReqCompressPanorama.
   * @exports IReqCompressPanorama
   * @interface IReqCompressPanorama
   * @property {string|null} [panoramaName] ReqCompressPanorama panoramaName
   */

  /**
   * Constructs a new ReqCompressPanorama.
   * @exports ReqCompressPanorama
   * @classdesc Represents a ReqCompressPanorama.
   * @implements IReqCompressPanorama
   * @constructor
   * @param {IReqCompressPanorama=} [properties] Properties to set
   */
  function ReqCompressPanorama(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCompressPanorama panoramaName.
   * @member {string} panoramaName
   * @memberof ReqCompressPanorama
   * @instance
   */
  ReqCompressPanorama.prototype.panoramaName = "";

  /**
   * Creates a new ReqCompressPanorama instance using the specified properties.
   * @function create
   * @memberof ReqCompressPanorama
   * @static
   * @param {IReqCompressPanorama=} [properties] Properties to set
   * @returns {ReqCompressPanorama} ReqCompressPanorama instance
   */
  ReqCompressPanorama.create = function create(properties) {
    return new ReqCompressPanorama(properties);
  };

  /**
   * Encodes the specified ReqCompressPanorama message. Does not implicitly {@link ReqCompressPanorama.verify|verify} messages.
   * @function encode
   * @memberof ReqCompressPanorama
   * @static
   * @param {IReqCompressPanorama} message ReqCompressPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCompressPanorama.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.panoramaName);
    return writer;
  };

  /**
   * Encodes the specified ReqCompressPanorama message, length delimited. Does not implicitly {@link ReqCompressPanorama.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCompressPanorama
   * @static
   * @param {IReqCompressPanorama} message ReqCompressPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCompressPanorama.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCompressPanorama message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCompressPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCompressPanorama} ReqCompressPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCompressPanorama.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCompressPanorama();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.panoramaName = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCompressPanorama message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCompressPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCompressPanorama} ReqCompressPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCompressPanorama.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCompressPanorama message.
   * @function verify
   * @memberof ReqCompressPanorama
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCompressPanorama.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      if (!$util.isString(message.panoramaName))
        return "panoramaName: string expected";
    return null;
  };

  /**
   * Creates a ReqCompressPanorama message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCompressPanorama
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCompressPanorama} ReqCompressPanorama
   */
  ReqCompressPanorama.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCompressPanorama) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCompressPanorama: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCompressPanorama();
    if (object.panoramaName != null)
      message.panoramaName = String(object.panoramaName);
    return message;
  };

  /**
   * Creates a plain object from a ReqCompressPanorama message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCompressPanorama
   * @static
   * @param {ReqCompressPanorama} message ReqCompressPanorama
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCompressPanorama.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.panoramaName = "";
    if (
      message.panoramaName != null &&
      Object.hasOwnProperty.call(message, "panoramaName")
    )
      object.panoramaName = message.panoramaName;
    return object;
  };

  /**
   * Converts this ReqCompressPanorama to JSON.
   * @function toJSON
   * @memberof ReqCompressPanorama
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCompressPanorama.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCompressPanorama
   * @function getTypeUrl
   * @memberof ReqCompressPanorama
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCompressPanorama.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCompressPanorama";
  };

  return ReqCompressPanorama;
})();

$root.ReqStopCompressPanorama = (function () {
  /**
   * Properties of a ReqStopCompressPanorama.
   * @exports IReqStopCompressPanorama
   * @interface IReqStopCompressPanorama
   */

  /**
   * Constructs a new ReqStopCompressPanorama.
   * @exports ReqStopCompressPanorama
   * @classdesc Represents a ReqStopCompressPanorama.
   * @implements IReqStopCompressPanorama
   * @constructor
   * @param {IReqStopCompressPanorama=} [properties] Properties to set
   */
  function ReqStopCompressPanorama(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopCompressPanorama instance using the specified properties.
   * @function create
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {IReqStopCompressPanorama=} [properties] Properties to set
   * @returns {ReqStopCompressPanorama} ReqStopCompressPanorama instance
   */
  ReqStopCompressPanorama.create = function create(properties) {
    return new ReqStopCompressPanorama(properties);
  };

  /**
   * Encodes the specified ReqStopCompressPanorama message. Does not implicitly {@link ReqStopCompressPanorama.verify|verify} messages.
   * @function encode
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {IReqStopCompressPanorama} message ReqStopCompressPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCompressPanorama.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopCompressPanorama message, length delimited. Does not implicitly {@link ReqStopCompressPanorama.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {IReqStopCompressPanorama} message ReqStopCompressPanorama message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopCompressPanorama.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopCompressPanorama message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopCompressPanorama} ReqStopCompressPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCompressPanorama.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopCompressPanorama();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopCompressPanorama message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopCompressPanorama} ReqStopCompressPanorama
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopCompressPanorama.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopCompressPanorama message.
   * @function verify
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopCompressPanorama.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopCompressPanorama message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopCompressPanorama} ReqStopCompressPanorama
   */
  ReqStopCompressPanorama.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopCompressPanorama) return object;
    return new $root.ReqStopCompressPanorama();
  };

  /**
   * Creates a plain object from a ReqStopCompressPanorama message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {ReqStopCompressPanorama} message ReqStopCompressPanorama
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopCompressPanorama.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopCompressPanorama to JSON.
   * @function toJSON
   * @memberof ReqStopCompressPanorama
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopCompressPanorama.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopCompressPanorama
   * @function getTypeUrl
   * @memberof ReqStopCompressPanorama
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopCompressPanorama.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopCompressPanorama";
  };

  return ReqStopCompressPanorama;
})();

$root.ReqStartPanoramaFraming = (function () {
  /**
   * Properties of a ReqStartPanoramaFraming.
   * @exports IReqStartPanoramaFraming
   * @interface IReqStartPanoramaFraming
   */

  /**
   * Constructs a new ReqStartPanoramaFraming.
   * @exports ReqStartPanoramaFraming
   * @classdesc Represents a ReqStartPanoramaFraming.
   * @implements IReqStartPanoramaFraming
   * @constructor
   * @param {IReqStartPanoramaFraming=} [properties] Properties to set
   */
  function ReqStartPanoramaFraming(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStartPanoramaFraming instance using the specified properties.
   * @function create
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {IReqStartPanoramaFraming=} [properties] Properties to set
   * @returns {ReqStartPanoramaFraming} ReqStartPanoramaFraming instance
   */
  ReqStartPanoramaFraming.create = function create(properties) {
    return new ReqStartPanoramaFraming(properties);
  };

  /**
   * Encodes the specified ReqStartPanoramaFraming message. Does not implicitly {@link ReqStartPanoramaFraming.verify|verify} messages.
   * @function encode
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {IReqStartPanoramaFraming} message ReqStartPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaFraming.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStartPanoramaFraming message, length delimited. Does not implicitly {@link ReqStartPanoramaFraming.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {IReqStartPanoramaFraming} message ReqStartPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartPanoramaFraming.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartPanoramaFraming message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartPanoramaFraming} ReqStartPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaFraming.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartPanoramaFraming();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartPanoramaFraming message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartPanoramaFraming} ReqStartPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartPanoramaFraming.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartPanoramaFraming message.
   * @function verify
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartPanoramaFraming.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStartPanoramaFraming message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartPanoramaFraming} ReqStartPanoramaFraming
   */
  ReqStartPanoramaFraming.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartPanoramaFraming) return object;
    return new $root.ReqStartPanoramaFraming();
  };

  /**
   * Creates a plain object from a ReqStartPanoramaFraming message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {ReqStartPanoramaFraming} message ReqStartPanoramaFraming
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartPanoramaFraming.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStartPanoramaFraming to JSON.
   * @function toJSON
   * @memberof ReqStartPanoramaFraming
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartPanoramaFraming.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartPanoramaFraming
   * @function getTypeUrl
   * @memberof ReqStartPanoramaFraming
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartPanoramaFraming.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartPanoramaFraming";
  };

  return ReqStartPanoramaFraming;
})();

$root.ReqResetPanoramaFraming = (function () {
  /**
   * Properties of a ReqResetPanoramaFraming.
   * @exports IReqResetPanoramaFraming
   * @interface IReqResetPanoramaFraming
   */

  /**
   * Constructs a new ReqResetPanoramaFraming.
   * @exports ReqResetPanoramaFraming
   * @classdesc Represents a ReqResetPanoramaFraming.
   * @implements IReqResetPanoramaFraming
   * @constructor
   * @param {IReqResetPanoramaFraming=} [properties] Properties to set
   */
  function ReqResetPanoramaFraming(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqResetPanoramaFraming instance using the specified properties.
   * @function create
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {IReqResetPanoramaFraming=} [properties] Properties to set
   * @returns {ReqResetPanoramaFraming} ReqResetPanoramaFraming instance
   */
  ReqResetPanoramaFraming.create = function create(properties) {
    return new ReqResetPanoramaFraming(properties);
  };

  /**
   * Encodes the specified ReqResetPanoramaFraming message. Does not implicitly {@link ReqResetPanoramaFraming.verify|verify} messages.
   * @function encode
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {IReqResetPanoramaFraming} message ReqResetPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqResetPanoramaFraming.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqResetPanoramaFraming message, length delimited. Does not implicitly {@link ReqResetPanoramaFraming.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {IReqResetPanoramaFraming} message ReqResetPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqResetPanoramaFraming.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqResetPanoramaFraming message from the specified reader or buffer.
   * @function decode
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqResetPanoramaFraming} ReqResetPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqResetPanoramaFraming.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqResetPanoramaFraming();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqResetPanoramaFraming message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqResetPanoramaFraming} ReqResetPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqResetPanoramaFraming.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqResetPanoramaFraming message.
   * @function verify
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqResetPanoramaFraming.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqResetPanoramaFraming message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqResetPanoramaFraming} ReqResetPanoramaFraming
   */
  ReqResetPanoramaFraming.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqResetPanoramaFraming) return object;
    return new $root.ReqResetPanoramaFraming();
  };

  /**
   * Creates a plain object from a ReqResetPanoramaFraming message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {ReqResetPanoramaFraming} message ReqResetPanoramaFraming
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqResetPanoramaFraming.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqResetPanoramaFraming to JSON.
   * @function toJSON
   * @memberof ReqResetPanoramaFraming
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqResetPanoramaFraming.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqResetPanoramaFraming
   * @function getTypeUrl
   * @memberof ReqResetPanoramaFraming
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqResetPanoramaFraming.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqResetPanoramaFraming";
  };

  return ReqResetPanoramaFraming;
})();

$root.ReqStopPanoramaFraming = (function () {
  /**
   * Properties of a ReqStopPanoramaFraming.
   * @exports IReqStopPanoramaFraming
   * @interface IReqStopPanoramaFraming
   */

  /**
   * Constructs a new ReqStopPanoramaFraming.
   * @exports ReqStopPanoramaFraming
   * @classdesc Represents a ReqStopPanoramaFraming.
   * @implements IReqStopPanoramaFraming
   * @constructor
   * @param {IReqStopPanoramaFraming=} [properties] Properties to set
   */
  function ReqStopPanoramaFraming(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopPanoramaFraming instance using the specified properties.
   * @function create
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {IReqStopPanoramaFraming=} [properties] Properties to set
   * @returns {ReqStopPanoramaFraming} ReqStopPanoramaFraming instance
   */
  ReqStopPanoramaFraming.create = function create(properties) {
    return new ReqStopPanoramaFraming(properties);
  };

  /**
   * Encodes the specified ReqStopPanoramaFraming message. Does not implicitly {@link ReqStopPanoramaFraming.verify|verify} messages.
   * @function encode
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {IReqStopPanoramaFraming} message ReqStopPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaFraming.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopPanoramaFraming message, length delimited. Does not implicitly {@link ReqStopPanoramaFraming.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {IReqStopPanoramaFraming} message ReqStopPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaFraming.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopPanoramaFraming message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopPanoramaFraming} ReqStopPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaFraming.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopPanoramaFraming();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopPanoramaFraming message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopPanoramaFraming} ReqStopPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaFraming.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopPanoramaFraming message.
   * @function verify
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopPanoramaFraming.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopPanoramaFraming message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopPanoramaFraming} ReqStopPanoramaFraming
   */
  ReqStopPanoramaFraming.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopPanoramaFraming) return object;
    return new $root.ReqStopPanoramaFraming();
  };

  /**
   * Creates a plain object from a ReqStopPanoramaFraming message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {ReqStopPanoramaFraming} message ReqStopPanoramaFraming
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopPanoramaFraming.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopPanoramaFraming to JSON.
   * @function toJSON
   * @memberof ReqStopPanoramaFraming
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopPanoramaFraming.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopPanoramaFraming
   * @function getTypeUrl
   * @memberof ReqStopPanoramaFraming
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopPanoramaFraming.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopPanoramaFraming";
  };

  return ReqStopPanoramaFraming;
})();

$root.ReqStopPanoramaFramingAndStartGrid = (function () {
  /**
   * Properties of a ReqStopPanoramaFramingAndStartGrid.
   * @exports IReqStopPanoramaFramingAndStartGrid
   * @interface IReqStopPanoramaFramingAndStartGrid
   */

  /**
   * Constructs a new ReqStopPanoramaFramingAndStartGrid.
   * @exports ReqStopPanoramaFramingAndStartGrid
   * @classdesc Represents a ReqStopPanoramaFramingAndStartGrid.
   * @implements IReqStopPanoramaFramingAndStartGrid
   * @constructor
   * @param {IReqStopPanoramaFramingAndStartGrid=} [properties] Properties to set
   */
  function ReqStopPanoramaFramingAndStartGrid(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopPanoramaFramingAndStartGrid instance using the specified properties.
   * @function create
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {IReqStopPanoramaFramingAndStartGrid=} [properties] Properties to set
   * @returns {ReqStopPanoramaFramingAndStartGrid} ReqStopPanoramaFramingAndStartGrid instance
   */
  ReqStopPanoramaFramingAndStartGrid.create = function create(properties) {
    return new ReqStopPanoramaFramingAndStartGrid(properties);
  };

  /**
   * Encodes the specified ReqStopPanoramaFramingAndStartGrid message. Does not implicitly {@link ReqStopPanoramaFramingAndStartGrid.verify|verify} messages.
   * @function encode
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {IReqStopPanoramaFramingAndStartGrid} message ReqStopPanoramaFramingAndStartGrid message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaFramingAndStartGrid.encode = function encode(
    message,
    writer,
    q,
  ) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopPanoramaFramingAndStartGrid message, length delimited. Does not implicitly {@link ReqStopPanoramaFramingAndStartGrid.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {IReqStopPanoramaFramingAndStartGrid} message ReqStopPanoramaFramingAndStartGrid message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopPanoramaFramingAndStartGrid.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopPanoramaFramingAndStartGrid message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopPanoramaFramingAndStartGrid} ReqStopPanoramaFramingAndStartGrid
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaFramingAndStartGrid.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopPanoramaFramingAndStartGrid();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopPanoramaFramingAndStartGrid message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopPanoramaFramingAndStartGrid} ReqStopPanoramaFramingAndStartGrid
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopPanoramaFramingAndStartGrid.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopPanoramaFramingAndStartGrid message.
   * @function verify
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopPanoramaFramingAndStartGrid.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopPanoramaFramingAndStartGrid message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopPanoramaFramingAndStartGrid} ReqStopPanoramaFramingAndStartGrid
   */
  ReqStopPanoramaFramingAndStartGrid.fromObject = function fromObject(
    object,
    long,
  ) {
    if (object instanceof $root.ReqStopPanoramaFramingAndStartGrid)
      return object;
    return new $root.ReqStopPanoramaFramingAndStartGrid();
  };

  /**
   * Creates a plain object from a ReqStopPanoramaFramingAndStartGrid message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {ReqStopPanoramaFramingAndStartGrid} message ReqStopPanoramaFramingAndStartGrid
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopPanoramaFramingAndStartGrid.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopPanoramaFramingAndStartGrid to JSON.
   * @function toJSON
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopPanoramaFramingAndStartGrid.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopPanoramaFramingAndStartGrid
   * @function getTypeUrl
   * @memberof ReqStopPanoramaFramingAndStartGrid
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopPanoramaFramingAndStartGrid.getTypeUrl = function getTypeUrl(
    typeUrlPrefix,
  ) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopPanoramaFramingAndStartGrid";
  };

  return ReqStopPanoramaFramingAndStartGrid;
})();

$root.ReqUpdatePanoramaFramingRect = (function () {
  /**
   * Properties of a ReqUpdatePanoramaFramingRect.
   * @exports IReqUpdatePanoramaFramingRect
   * @interface IReqUpdatePanoramaFramingRect
   * @property {number|null} [normXTl] ReqUpdatePanoramaFramingRect normXTl
   * @property {number|null} [normYTl] ReqUpdatePanoramaFramingRect normYTl
   * @property {number|null} [normXBr] ReqUpdatePanoramaFramingRect normXBr
   * @property {number|null} [normYBr] ReqUpdatePanoramaFramingRect normYBr
   */

  /**
   * Constructs a new ReqUpdatePanoramaFramingRect.
   * @exports ReqUpdatePanoramaFramingRect
   * @classdesc Represents a ReqUpdatePanoramaFramingRect.
   * @implements IReqUpdatePanoramaFramingRect
   * @constructor
   * @param {IReqUpdatePanoramaFramingRect=} [properties] Properties to set
   */
  function ReqUpdatePanoramaFramingRect(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqUpdatePanoramaFramingRect normXTl.
   * @member {number} normXTl
   * @memberof ReqUpdatePanoramaFramingRect
   * @instance
   */
  ReqUpdatePanoramaFramingRect.prototype.normXTl = 0;

  /**
   * ReqUpdatePanoramaFramingRect normYTl.
   * @member {number} normYTl
   * @memberof ReqUpdatePanoramaFramingRect
   * @instance
   */
  ReqUpdatePanoramaFramingRect.prototype.normYTl = 0;

  /**
   * ReqUpdatePanoramaFramingRect normXBr.
   * @member {number} normXBr
   * @memberof ReqUpdatePanoramaFramingRect
   * @instance
   */
  ReqUpdatePanoramaFramingRect.prototype.normXBr = 0;

  /**
   * ReqUpdatePanoramaFramingRect normYBr.
   * @member {number} normYBr
   * @memberof ReqUpdatePanoramaFramingRect
   * @instance
   */
  ReqUpdatePanoramaFramingRect.prototype.normYBr = 0;

  /**
   * Creates a new ReqUpdatePanoramaFramingRect instance using the specified properties.
   * @function create
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {IReqUpdatePanoramaFramingRect=} [properties] Properties to set
   * @returns {ReqUpdatePanoramaFramingRect} ReqUpdatePanoramaFramingRect instance
   */
  ReqUpdatePanoramaFramingRect.create = function create(properties) {
    return new ReqUpdatePanoramaFramingRect(properties);
  };

  /**
   * Encodes the specified ReqUpdatePanoramaFramingRect message. Does not implicitly {@link ReqUpdatePanoramaFramingRect.verify|verify} messages.
   * @function encode
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {IReqUpdatePanoramaFramingRect} message ReqUpdatePanoramaFramingRect message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUpdatePanoramaFramingRect.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.normXTl != null &&
      Object.hasOwnProperty.call(message, "normXTl")
    )
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.normXTl);
    if (
      message.normYTl != null &&
      Object.hasOwnProperty.call(message, "normYTl")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.normYTl);
    if (
      message.normXBr != null &&
      Object.hasOwnProperty.call(message, "normXBr")
    )
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.normXBr);
    if (
      message.normYBr != null &&
      Object.hasOwnProperty.call(message, "normYBr")
    )
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.normYBr);
    return writer;
  };

  /**
   * Encodes the specified ReqUpdatePanoramaFramingRect message, length delimited. Does not implicitly {@link ReqUpdatePanoramaFramingRect.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {IReqUpdatePanoramaFramingRect} message ReqUpdatePanoramaFramingRect message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUpdatePanoramaFramingRect.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqUpdatePanoramaFramingRect message from the specified reader or buffer.
   * @function decode
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqUpdatePanoramaFramingRect} ReqUpdatePanoramaFramingRect
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUpdatePanoramaFramingRect.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqUpdatePanoramaFramingRect();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.normXTl = reader.double();
          break;
        }
        case 2: {
          message.normYTl = reader.double();
          break;
        }
        case 3: {
          message.normXBr = reader.double();
          break;
        }
        case 4: {
          message.normYBr = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqUpdatePanoramaFramingRect message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqUpdatePanoramaFramingRect} ReqUpdatePanoramaFramingRect
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUpdatePanoramaFramingRect.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqUpdatePanoramaFramingRect message.
   * @function verify
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqUpdatePanoramaFramingRect.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.normXTl != null &&
      Object.hasOwnProperty.call(message, "normXTl")
    )
      if (typeof message.normXTl !== "number")
        return "normXTl: number expected";
    if (
      message.normYTl != null &&
      Object.hasOwnProperty.call(message, "normYTl")
    )
      if (typeof message.normYTl !== "number")
        return "normYTl: number expected";
    if (
      message.normXBr != null &&
      Object.hasOwnProperty.call(message, "normXBr")
    )
      if (typeof message.normXBr !== "number")
        return "normXBr: number expected";
    if (
      message.normYBr != null &&
      Object.hasOwnProperty.call(message, "normYBr")
    )
      if (typeof message.normYBr !== "number")
        return "normYBr: number expected";
    return null;
  };

  /**
   * Creates a ReqUpdatePanoramaFramingRect message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqUpdatePanoramaFramingRect} ReqUpdatePanoramaFramingRect
   */
  ReqUpdatePanoramaFramingRect.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqUpdatePanoramaFramingRect) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqUpdatePanoramaFramingRect: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqUpdatePanoramaFramingRect();
    if (object.normXTl != null) message.normXTl = Number(object.normXTl);
    if (object.normYTl != null) message.normYTl = Number(object.normYTl);
    if (object.normXBr != null) message.normXBr = Number(object.normXBr);
    if (object.normYBr != null) message.normYBr = Number(object.normYBr);
    return message;
  };

  /**
   * Creates a plain object from a ReqUpdatePanoramaFramingRect message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {ReqUpdatePanoramaFramingRect} message ReqUpdatePanoramaFramingRect
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqUpdatePanoramaFramingRect.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.normXTl = 0;
      object.normYTl = 0;
      object.normXBr = 0;
      object.normYBr = 0;
    }
    if (
      message.normXTl != null &&
      Object.hasOwnProperty.call(message, "normXTl")
    )
      object.normXTl =
        options.json && !isFinite(message.normXTl)
          ? String(message.normXTl)
          : message.normXTl;
    if (
      message.normYTl != null &&
      Object.hasOwnProperty.call(message, "normYTl")
    )
      object.normYTl =
        options.json && !isFinite(message.normYTl)
          ? String(message.normYTl)
          : message.normYTl;
    if (
      message.normXBr != null &&
      Object.hasOwnProperty.call(message, "normXBr")
    )
      object.normXBr =
        options.json && !isFinite(message.normXBr)
          ? String(message.normXBr)
          : message.normXBr;
    if (
      message.normYBr != null &&
      Object.hasOwnProperty.call(message, "normYBr")
    )
      object.normYBr =
        options.json && !isFinite(message.normYBr)
          ? String(message.normYBr)
          : message.normYBr;
    return object;
  };

  /**
   * Converts this ReqUpdatePanoramaFramingRect to JSON.
   * @function toJSON
   * @memberof ReqUpdatePanoramaFramingRect
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqUpdatePanoramaFramingRect.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqUpdatePanoramaFramingRect
   * @function getTypeUrl
   * @memberof ReqUpdatePanoramaFramingRect
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqUpdatePanoramaFramingRect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqUpdatePanoramaFramingRect";
  };

  return ReqUpdatePanoramaFramingRect;
})();

$root.ReqNotifyPanoramaFraming = (function () {
  /**
   * Properties of a ReqNotifyPanoramaFraming.
   * @exports IReqNotifyPanoramaFraming
   * @interface IReqNotifyPanoramaFraming
   */

  /**
   * Constructs a new ReqNotifyPanoramaFraming.
   * @exports ReqNotifyPanoramaFraming
   * @classdesc Represents a ReqNotifyPanoramaFraming.
   * @implements IReqNotifyPanoramaFraming
   * @constructor
   * @param {IReqNotifyPanoramaFraming=} [properties] Properties to set
   */
  function ReqNotifyPanoramaFraming(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqNotifyPanoramaFraming instance using the specified properties.
   * @function create
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {IReqNotifyPanoramaFraming=} [properties] Properties to set
   * @returns {ReqNotifyPanoramaFraming} ReqNotifyPanoramaFraming instance
   */
  ReqNotifyPanoramaFraming.create = function create(properties) {
    return new ReqNotifyPanoramaFraming(properties);
  };

  /**
   * Encodes the specified ReqNotifyPanoramaFraming message. Does not implicitly {@link ReqNotifyPanoramaFraming.verify|verify} messages.
   * @function encode
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {IReqNotifyPanoramaFraming} message ReqNotifyPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqNotifyPanoramaFraming.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqNotifyPanoramaFraming message, length delimited. Does not implicitly {@link ReqNotifyPanoramaFraming.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {IReqNotifyPanoramaFraming} message ReqNotifyPanoramaFraming message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqNotifyPanoramaFraming.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqNotifyPanoramaFraming message from the specified reader or buffer.
   * @function decode
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqNotifyPanoramaFraming} ReqNotifyPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqNotifyPanoramaFraming.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqNotifyPanoramaFraming();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqNotifyPanoramaFraming message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqNotifyPanoramaFraming} ReqNotifyPanoramaFraming
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqNotifyPanoramaFraming.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqNotifyPanoramaFraming message.
   * @function verify
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqNotifyPanoramaFraming.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqNotifyPanoramaFraming message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqNotifyPanoramaFraming} ReqNotifyPanoramaFraming
   */
  ReqNotifyPanoramaFraming.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqNotifyPanoramaFraming) return object;
    return new $root.ReqNotifyPanoramaFraming();
  };

  /**
   * Creates a plain object from a ReqNotifyPanoramaFraming message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {ReqNotifyPanoramaFraming} message ReqNotifyPanoramaFraming
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqNotifyPanoramaFraming.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqNotifyPanoramaFraming to JSON.
   * @function toJSON
   * @memberof ReqNotifyPanoramaFraming
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqNotifyPanoramaFraming.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqNotifyPanoramaFraming
   * @function getTypeUrl
   * @memberof ReqNotifyPanoramaFraming
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqNotifyPanoramaFraming.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqNotifyPanoramaFraming";
  };

  return ReqNotifyPanoramaFraming;
})();

$root.param = (function () {
  /**
   * Namespace param.
   * @exports param
   * @namespace
   */
  var param = {};

  param.ReqSetExposure = (function () {
    /**
     * Properties of a ReqSetExposure.
     * @memberof param
     * @interface IReqSetExposure
     * @property {number|Long|null} [paramId] ReqSetExposure paramId
     * @property {number|null} [mode] ReqSetExposure mode
     * @property {number|null} [value] ReqSetExposure value
     */

    /**
     * Constructs a new ReqSetExposure.
     * @memberof param
     * @classdesc Represents a ReqSetExposure.
     * @implements IReqSetExposure
     * @constructor
     * @param {param.IReqSetExposure=} [properties] Properties to set
     */
    function ReqSetExposure(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetExposure paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetExposure
     * @instance
     */
    ReqSetExposure.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetExposure mode.
     * @member {number} mode
     * @memberof param.ReqSetExposure
     * @instance
     */
    ReqSetExposure.prototype.mode = 0;

    /**
     * ReqSetExposure value.
     * @member {number} value
     * @memberof param.ReqSetExposure
     * @instance
     */
    ReqSetExposure.prototype.value = 0;

    /**
     * Creates a new ReqSetExposure instance using the specified properties.
     * @function create
     * @memberof param.ReqSetExposure
     * @static
     * @param {param.IReqSetExposure=} [properties] Properties to set
     * @returns {param.ReqSetExposure} ReqSetExposure instance
     */
    ReqSetExposure.create = function create(properties) {
      return new ReqSetExposure(properties);
    };

    /**
     * Encodes the specified ReqSetExposure message. Does not implicitly {@link param.ReqSetExposure.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetExposure
     * @static
     * @param {param.IReqSetExposure} message ReqSetExposure message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetExposure.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetExposure message, length delimited. Does not implicitly {@link param.ReqSetExposure.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetExposure
     * @static
     * @param {param.IReqSetExposure} message ReqSetExposure message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetExposure.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetExposure message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetExposure
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetExposure} ReqSetExposure
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetExposure.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetExposure();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.mode = reader.int32();
            break;
          }
          case 3: {
            message.value = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetExposure message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetExposure
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetExposure} ReqSetExposure
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetExposure.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetExposure message.
     * @function verify
     * @memberof param.ReqSetExposure
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetExposure.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (!$util.isInteger(message.value)) return "value: integer expected";
      return null;
    };

    /**
     * Creates a ReqSetExposure message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetExposure
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetExposure} ReqSetExposure
     */
    ReqSetExposure.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetExposure) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetExposure: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetExposure();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.mode != null) message.mode = object.mode | 0;
      if (object.value != null) message.value = object.value | 0;
      return message;
    };

    /**
     * Creates a plain object from a ReqSetExposure message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetExposure
     * @static
     * @param {param.ReqSetExposure} message ReqSetExposure
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetExposure.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.mode = 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ReqSetExposure to JSON.
     * @function toJSON
     * @memberof param.ReqSetExposure
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetExposure.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetExposure
     * @function getTypeUrl
     * @memberof param.ReqSetExposure
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetExposure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetExposure";
    };

    return ReqSetExposure;
  })();

  param.ReqSetGain = (function () {
    /**
     * Properties of a ReqSetGain.
     * @memberof param
     * @interface IReqSetGain
     * @property {number|Long|null} [paramId] ReqSetGain paramId
     * @property {number|null} [mode] ReqSetGain mode
     * @property {number|null} [value] ReqSetGain value
     */

    /**
     * Constructs a new ReqSetGain.
     * @memberof param
     * @classdesc Represents a ReqSetGain.
     * @implements IReqSetGain
     * @constructor
     * @param {param.IReqSetGain=} [properties] Properties to set
     */
    function ReqSetGain(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetGain paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetGain
     * @instance
     */
    ReqSetGain.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetGain mode.
     * @member {number} mode
     * @memberof param.ReqSetGain
     * @instance
     */
    ReqSetGain.prototype.mode = 0;

    /**
     * ReqSetGain value.
     * @member {number} value
     * @memberof param.ReqSetGain
     * @instance
     */
    ReqSetGain.prototype.value = 0;

    /**
     * Creates a new ReqSetGain instance using the specified properties.
     * @function create
     * @memberof param.ReqSetGain
     * @static
     * @param {param.IReqSetGain=} [properties] Properties to set
     * @returns {param.ReqSetGain} ReqSetGain instance
     */
    ReqSetGain.create = function create(properties) {
      return new ReqSetGain(properties);
    };

    /**
     * Encodes the specified ReqSetGain message. Does not implicitly {@link param.ReqSetGain.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetGain
     * @static
     * @param {param.IReqSetGain} message ReqSetGain message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGain.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetGain message, length delimited. Does not implicitly {@link param.ReqSetGain.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetGain
     * @static
     * @param {param.IReqSetGain} message ReqSetGain message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGain.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetGain message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetGain
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetGain} ReqSetGain
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGain.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetGain();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.mode = reader.int32();
            break;
          }
          case 3: {
            message.value = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetGain message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetGain
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetGain} ReqSetGain
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGain.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetGain message.
     * @function verify
     * @memberof param.ReqSetGain
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetGain.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (!$util.isInteger(message.value)) return "value: integer expected";
      return null;
    };

    /**
     * Creates a ReqSetGain message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetGain
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetGain} ReqSetGain
     */
    ReqSetGain.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetGain) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetGain: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetGain();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.mode != null) message.mode = object.mode | 0;
      if (object.value != null) message.value = object.value | 0;
      return message;
    };

    /**
     * Creates a plain object from a ReqSetGain message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetGain
     * @static
     * @param {param.ReqSetGain} message ReqSetGain
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetGain.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.mode = 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ReqSetGain to JSON.
     * @function toJSON
     * @memberof param.ReqSetGain
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetGain.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetGain
     * @function getTypeUrl
     * @memberof param.ReqSetGain
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetGain.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetGain";
    };

    return ReqSetGain;
  })();

  param.ReqSetWb = (function () {
    /**
     * Properties of a ReqSetWb.
     * @memberof param
     * @interface IReqSetWb
     * @property {number|Long|null} [paramId] ReqSetWb paramId
     * @property {number|null} [mode] ReqSetWb mode
     * @property {number|null} [value] ReqSetWb value
     */

    /**
     * Constructs a new ReqSetWb.
     * @memberof param
     * @classdesc Represents a ReqSetWb.
     * @implements IReqSetWb
     * @constructor
     * @param {param.IReqSetWb=} [properties] Properties to set
     */
    function ReqSetWb(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetWb paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetWb
     * @instance
     */
    ReqSetWb.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetWb mode.
     * @member {number} mode
     * @memberof param.ReqSetWb
     * @instance
     */
    ReqSetWb.prototype.mode = 0;

    /**
     * ReqSetWb value.
     * @member {number} value
     * @memberof param.ReqSetWb
     * @instance
     */
    ReqSetWb.prototype.value = 0;

    /**
     * Creates a new ReqSetWb instance using the specified properties.
     * @function create
     * @memberof param.ReqSetWb
     * @static
     * @param {param.IReqSetWb=} [properties] Properties to set
     * @returns {param.ReqSetWb} ReqSetWb instance
     */
    ReqSetWb.create = function create(properties) {
      return new ReqSetWb(properties);
    };

    /**
     * Encodes the specified ReqSetWb message. Does not implicitly {@link param.ReqSetWb.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetWb
     * @static
     * @param {param.IReqSetWb} message ReqSetWb message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetWb.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.mode);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetWb message, length delimited. Does not implicitly {@link param.ReqSetWb.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetWb
     * @static
     * @param {param.IReqSetWb} message ReqSetWb message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetWb.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetWb message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetWb
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetWb} ReqSetWb
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetWb.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetWb();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.mode = reader.int32();
            break;
          }
          case 3: {
            message.value = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetWb message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetWb
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetWb} ReqSetWb
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetWb.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetWb message.
     * @function verify
     * @memberof param.ReqSetWb
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetWb.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        if (!$util.isInteger(message.mode)) return "mode: integer expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (!$util.isInteger(message.value)) return "value: integer expected";
      return null;
    };

    /**
     * Creates a ReqSetWb message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetWb
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetWb} ReqSetWb
     */
    ReqSetWb.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetWb) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetWb: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetWb();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.mode != null) message.mode = object.mode | 0;
      if (object.value != null) message.value = object.value | 0;
      return message;
    };

    /**
     * Creates a plain object from a ReqSetWb message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetWb
     * @static
     * @param {param.ReqSetWb} message ReqSetWb
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetWb.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.mode = 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
        object.mode = message.mode;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ReqSetWb to JSON.
     * @function toJSON
     * @memberof param.ReqSetWb
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetWb.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetWb
     * @function getTypeUrl
     * @memberof param.ReqSetWb
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetWb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetWb";
    };

    return ReqSetWb;
  })();

  param.ReqSetGeneralIntParam = (function () {
    /**
     * Properties of a ReqSetGeneralIntParam.
     * @memberof param
     * @interface IReqSetGeneralIntParam
     * @property {number|Long|null} [paramId] ReqSetGeneralIntParam paramId
     * @property {number|null} [value] ReqSetGeneralIntParam value
     */

    /**
     * Constructs a new ReqSetGeneralIntParam.
     * @memberof param
     * @classdesc Represents a ReqSetGeneralIntParam.
     * @implements IReqSetGeneralIntParam
     * @constructor
     * @param {param.IReqSetGeneralIntParam=} [properties] Properties to set
     */
    function ReqSetGeneralIntParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetGeneralIntParam paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetGeneralIntParam
     * @instance
     */
    ReqSetGeneralIntParam.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetGeneralIntParam value.
     * @member {number} value
     * @memberof param.ReqSetGeneralIntParam
     * @instance
     */
    ReqSetGeneralIntParam.prototype.value = 0;

    /**
     * Creates a new ReqSetGeneralIntParam instance using the specified properties.
     * @function create
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {param.IReqSetGeneralIntParam=} [properties] Properties to set
     * @returns {param.ReqSetGeneralIntParam} ReqSetGeneralIntParam instance
     */
    ReqSetGeneralIntParam.create = function create(properties) {
      return new ReqSetGeneralIntParam(properties);
    };

    /**
     * Encodes the specified ReqSetGeneralIntParam message. Does not implicitly {@link param.ReqSetGeneralIntParam.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {param.IReqSetGeneralIntParam} message ReqSetGeneralIntParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralIntParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetGeneralIntParam message, length delimited. Does not implicitly {@link param.ReqSetGeneralIntParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {param.IReqSetGeneralIntParam} message ReqSetGeneralIntParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralIntParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetGeneralIntParam message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetGeneralIntParam} ReqSetGeneralIntParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralIntParam.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetGeneralIntParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetGeneralIntParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetGeneralIntParam} ReqSetGeneralIntParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralIntParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetGeneralIntParam message.
     * @function verify
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetGeneralIntParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (!$util.isInteger(message.value)) return "value: integer expected";
      return null;
    };

    /**
     * Creates a ReqSetGeneralIntParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetGeneralIntParam} ReqSetGeneralIntParam
     */
    ReqSetGeneralIntParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetGeneralIntParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetGeneralIntParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetGeneralIntParam();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.value != null) message.value = object.value | 0;
      return message;
    };

    /**
     * Creates a plain object from a ReqSetGeneralIntParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {param.ReqSetGeneralIntParam} message ReqSetGeneralIntParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetGeneralIntParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ReqSetGeneralIntParam to JSON.
     * @function toJSON
     * @memberof param.ReqSetGeneralIntParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetGeneralIntParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetGeneralIntParam
     * @function getTypeUrl
     * @memberof param.ReqSetGeneralIntParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetGeneralIntParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetGeneralIntParam";
    };

    return ReqSetGeneralIntParam;
  })();

  param.ReqSetGeneralFloatParam = (function () {
    /**
     * Properties of a ReqSetGeneralFloatParam.
     * @memberof param
     * @interface IReqSetGeneralFloatParam
     * @property {number|Long|null} [paramId] ReqSetGeneralFloatParam paramId
     * @property {number|null} [value] ReqSetGeneralFloatParam value
     */

    /**
     * Constructs a new ReqSetGeneralFloatParam.
     * @memberof param
     * @classdesc Represents a ReqSetGeneralFloatParam.
     * @implements IReqSetGeneralFloatParam
     * @constructor
     * @param {param.IReqSetGeneralFloatParam=} [properties] Properties to set
     */
    function ReqSetGeneralFloatParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetGeneralFloatParam paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetGeneralFloatParam
     * @instance
     */
    ReqSetGeneralFloatParam.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetGeneralFloatParam value.
     * @member {number} value
     * @memberof param.ReqSetGeneralFloatParam
     * @instance
     */
    ReqSetGeneralFloatParam.prototype.value = 0;

    /**
     * Creates a new ReqSetGeneralFloatParam instance using the specified properties.
     * @function create
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {param.IReqSetGeneralFloatParam=} [properties] Properties to set
     * @returns {param.ReqSetGeneralFloatParam} ReqSetGeneralFloatParam instance
     */
    ReqSetGeneralFloatParam.create = function create(properties) {
      return new ReqSetGeneralFloatParam(properties);
    };

    /**
     * Encodes the specified ReqSetGeneralFloatParam message. Does not implicitly {@link param.ReqSetGeneralFloatParam.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {param.IReqSetGeneralFloatParam} message ReqSetGeneralFloatParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralFloatParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 2, wireType 5 =*/ 21).float(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetGeneralFloatParam message, length delimited. Does not implicitly {@link param.ReqSetGeneralFloatParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {param.IReqSetGeneralFloatParam} message ReqSetGeneralFloatParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralFloatParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetGeneralFloatParam message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetGeneralFloatParam} ReqSetGeneralFloatParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralFloatParam.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetGeneralFloatParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.float();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetGeneralFloatParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetGeneralFloatParam} ReqSetGeneralFloatParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralFloatParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetGeneralFloatParam message.
     * @function verify
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetGeneralFloatParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (typeof message.value !== "number") return "value: number expected";
      return null;
    };

    /**
     * Creates a ReqSetGeneralFloatParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetGeneralFloatParam} ReqSetGeneralFloatParam
     */
    ReqSetGeneralFloatParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetGeneralFloatParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetGeneralFloatParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetGeneralFloatParam();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.value != null) message.value = Number(object.value);
      return message;
    };

    /**
     * Creates a plain object from a ReqSetGeneralFloatParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {param.ReqSetGeneralFloatParam} message ReqSetGeneralFloatParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetGeneralFloatParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.value = 0;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value =
          options.json && !isFinite(message.value)
            ? String(message.value)
            : message.value;
      return object;
    };

    /**
     * Converts this ReqSetGeneralFloatParam to JSON.
     * @function toJSON
     * @memberof param.ReqSetGeneralFloatParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetGeneralFloatParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetGeneralFloatParam
     * @function getTypeUrl
     * @memberof param.ReqSetGeneralFloatParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetGeneralFloatParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetGeneralFloatParam";
    };

    return ReqSetGeneralFloatParam;
  })();

  param.ReqSetGeneralBoolParams = (function () {
    /**
     * Properties of a ReqSetGeneralBoolParams.
     * @memberof param
     * @interface IReqSetGeneralBoolParams
     * @property {number|Long|null} [paramId] ReqSetGeneralBoolParams paramId
     * @property {boolean|null} [value] ReqSetGeneralBoolParams value
     */

    /**
     * Constructs a new ReqSetGeneralBoolParams.
     * @memberof param
     * @classdesc Represents a ReqSetGeneralBoolParams.
     * @implements IReqSetGeneralBoolParams
     * @constructor
     * @param {param.IReqSetGeneralBoolParams=} [properties] Properties to set
     */
    function ReqSetGeneralBoolParams(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetGeneralBoolParams paramId.
     * @member {number|Long} paramId
     * @memberof param.ReqSetGeneralBoolParams
     * @instance
     */
    ReqSetGeneralBoolParams.prototype.paramId = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReqSetGeneralBoolParams value.
     * @member {boolean} value
     * @memberof param.ReqSetGeneralBoolParams
     * @instance
     */
    ReqSetGeneralBoolParams.prototype.value = false;

    /**
     * Creates a new ReqSetGeneralBoolParams instance using the specified properties.
     * @function create
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {param.IReqSetGeneralBoolParams=} [properties] Properties to set
     * @returns {param.ReqSetGeneralBoolParams} ReqSetGeneralBoolParams instance
     */
    ReqSetGeneralBoolParams.create = function create(properties) {
      return new ReqSetGeneralBoolParams(properties);
    };

    /**
     * Encodes the specified ReqSetGeneralBoolParams message. Does not implicitly {@link param.ReqSetGeneralBoolParams.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {param.IReqSetGeneralBoolParams} message ReqSetGeneralBoolParams message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralBoolParams.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.paramId);
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.value);
      return writer;
    };

    /**
     * Encodes the specified ReqSetGeneralBoolParams message, length delimited. Does not implicitly {@link param.ReqSetGeneralBoolParams.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {param.IReqSetGeneralBoolParams} message ReqSetGeneralBoolParams message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetGeneralBoolParams.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetGeneralBoolParams message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetGeneralBoolParams} ReqSetGeneralBoolParams
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralBoolParams.decode = function decode(
      reader,
      length,
      error,
      long,
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetGeneralBoolParams();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.paramId = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.bool();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetGeneralBoolParams message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetGeneralBoolParams} ReqSetGeneralBoolParams
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetGeneralBoolParams.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetGeneralBoolParams message.
     * @function verify
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetGeneralBoolParams.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (
          !$util.isInteger(message.paramId) &&
          !(
            message.paramId &&
            $util.isInteger(message.paramId.low) &&
            $util.isInteger(message.paramId.high)
          )
        )
          return "paramId: integer|Long expected";
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        if (typeof message.value !== "boolean")
          return "value: boolean expected";
      return null;
    };

    /**
     * Creates a ReqSetGeneralBoolParams message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetGeneralBoolParams} ReqSetGeneralBoolParams
     */
    ReqSetGeneralBoolParams.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetGeneralBoolParams) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetGeneralBoolParams: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetGeneralBoolParams();
      if (object.paramId != null)
        if ($util.Long)
          message.paramId = $util.Long.fromValue(object.paramId, true);
        else if (typeof object.paramId === "string")
          message.paramId = parseInt(object.paramId, 10);
        else if (typeof object.paramId === "number")
          message.paramId = object.paramId;
        else if (typeof object.paramId === "object")
          message.paramId = new $util.LongBits(
            object.paramId.low >>> 0,
            object.paramId.high >>> 0,
          ).toNumber(true);
      if (object.value != null) message.value = Boolean(object.value);
      return message;
    };

    /**
     * Creates a plain object from a ReqSetGeneralBoolParams message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {param.ReqSetGeneralBoolParams} message ReqSetGeneralBoolParams
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetGeneralBoolParams.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        if ($util.Long) {
          var long = new $util.Long(0, 0, true);
          object.paramId =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : typeof BigInt !== "undefined" && options.longs === BigInt
                  ? long.toBigInt()
                  : long;
        } else
          object.paramId =
            options.longs === String
              ? "0"
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? BigInt("0")
                : 0;
        object.value = false;
      }
      if (
        message.paramId != null &&
        Object.hasOwnProperty.call(message, "paramId")
      )
        if (typeof BigInt !== "undefined" && options.longs === BigInt)
          object.paramId =
            typeof message.paramId === "number"
              ? BigInt(message.paramId)
              : $util.Long.fromBits(
                  message.paramId.low >>> 0,
                  message.paramId.high >>> 0,
                  true,
                ).toBigInt();
        else if (typeof message.paramId === "number")
          object.paramId =
            options.longs === String
              ? String(message.paramId)
              : message.paramId;
        else
          object.paramId =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.paramId)
              : options.longs === Number
                ? new $util.LongBits(
                    message.paramId.low >>> 0,
                    message.paramId.high >>> 0,
                  ).toNumber(true)
                : message.paramId;
      if (message.value != null && Object.hasOwnProperty.call(message, "value"))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ReqSetGeneralBoolParams to JSON.
     * @function toJSON
     * @memberof param.ReqSetGeneralBoolParams
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetGeneralBoolParams.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetGeneralBoolParams
     * @function getTypeUrl
     * @memberof param.ReqSetGeneralBoolParams
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetGeneralBoolParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetGeneralBoolParams";
    };

    return ReqSetGeneralBoolParams;
  })();

  param.ReqSetAutoParam = (function () {
    /**
     * Properties of a ReqSetAutoParam.
     * @memberof param
     * @interface IReqSetAutoParam
     * @property {number|null} [cameraType] ReqSetAutoParam cameraType
     * @property {number|null} [shootingTech] ReqSetAutoParam shootingTech
     * @property {boolean|null} [isAuto] ReqSetAutoParam isAuto
     */

    /**
     * Constructs a new ReqSetAutoParam.
     * @memberof param
     * @classdesc Represents a ReqSetAutoParam.
     * @implements IReqSetAutoParam
     * @constructor
     * @param {param.IReqSetAutoParam=} [properties] Properties to set
     */
    function ReqSetAutoParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReqSetAutoParam cameraType.
     * @member {number} cameraType
     * @memberof param.ReqSetAutoParam
     * @instance
     */
    ReqSetAutoParam.prototype.cameraType = 0;

    /**
     * ReqSetAutoParam shootingTech.
     * @member {number} shootingTech
     * @memberof param.ReqSetAutoParam
     * @instance
     */
    ReqSetAutoParam.prototype.shootingTech = 0;

    /**
     * ReqSetAutoParam isAuto.
     * @member {boolean} isAuto
     * @memberof param.ReqSetAutoParam
     * @instance
     */
    ReqSetAutoParam.prototype.isAuto = false;

    /**
     * Creates a new ReqSetAutoParam instance using the specified properties.
     * @function create
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {param.IReqSetAutoParam=} [properties] Properties to set
     * @returns {param.ReqSetAutoParam} ReqSetAutoParam instance
     */
    ReqSetAutoParam.create = function create(properties) {
      return new ReqSetAutoParam(properties);
    };

    /**
     * Encodes the specified ReqSetAutoParam message. Does not implicitly {@link param.ReqSetAutoParam.verify|verify} messages.
     * @function encode
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {param.IReqSetAutoParam} message ReqSetAutoParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetAutoParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.cameraType);
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.shootingTech);
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.isAuto);
      return writer;
    };

    /**
     * Encodes the specified ReqSetAutoParam message, length delimited. Does not implicitly {@link param.ReqSetAutoParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {param.IReqSetAutoParam} message ReqSetAutoParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReqSetAutoParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ReqSetAutoParam message from the specified reader or buffer.
     * @function decode
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ReqSetAutoParam} ReqSetAutoParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetAutoParam.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ReqSetAutoParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.cameraType = reader.int32();
            break;
          }
          case 2: {
            message.shootingTech = reader.int32();
            break;
          }
          case 3: {
            message.isAuto = reader.bool();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ReqSetAutoParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ReqSetAutoParam} ReqSetAutoParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReqSetAutoParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReqSetAutoParam message.
     * @function verify
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReqSetAutoParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        if (!$util.isInteger(message.shootingTech))
          return "shootingTech: integer expected";
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        if (typeof message.isAuto !== "boolean")
          return "isAuto: boolean expected";
      return null;
    };

    /**
     * Creates a ReqSetAutoParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ReqSetAutoParam} ReqSetAutoParam
     */
    ReqSetAutoParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ReqSetAutoParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ReqSetAutoParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ReqSetAutoParam();
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      if (object.shootingTech != null)
        message.shootingTech = object.shootingTech | 0;
      if (object.isAuto != null) message.isAuto = Boolean(object.isAuto);
      return message;
    };

    /**
     * Creates a plain object from a ReqSetAutoParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {param.ReqSetAutoParam} message ReqSetAutoParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReqSetAutoParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.cameraType = 0;
        object.shootingTech = 0;
        object.isAuto = false;
      }
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        object.shootingTech = message.shootingTech;
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        object.isAuto = message.isAuto;
      return object;
    };

    /**
     * Converts this ReqSetAutoParam to JSON.
     * @function toJSON
     * @memberof param.ReqSetAutoParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReqSetAutoParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReqSetAutoParam
     * @function getTypeUrl
     * @memberof param.ReqSetAutoParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReqSetAutoParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ReqSetAutoParam";
    };

    return ReqSetAutoParam;
  })();

  param.ResSetAutoParam = (function () {
    /**
     * Properties of a ResSetAutoParam.
     * @memberof param
     * @interface IResSetAutoParam
     * @property {number|null} [shootingMode] ResSetAutoParam shootingMode
     * @property {number|null} [cameraType] ResSetAutoParam cameraType
     * @property {number|null} [shootingTech] ResSetAutoParam shootingTech
     * @property {boolean|null} [isAuto] ResSetAutoParam isAuto
     * @property {boolean|null} [updateAll] ResSetAutoParam updateAll
     * @property {number|null} [code] ResSetAutoParam code
     */

    /**
     * Constructs a new ResSetAutoParam.
     * @memberof param
     * @classdesc Represents a ResSetAutoParam.
     * @implements IResSetAutoParam
     * @constructor
     * @param {param.IResSetAutoParam=} [properties] Properties to set
     */
    function ResSetAutoParam(properties) {
      if (properties)
        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
    }

    /**
     * ResSetAutoParam shootingMode.
     * @member {number} shootingMode
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.shootingMode = 0;

    /**
     * ResSetAutoParam cameraType.
     * @member {number} cameraType
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.cameraType = 0;

    /**
     * ResSetAutoParam shootingTech.
     * @member {number} shootingTech
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.shootingTech = 0;

    /**
     * ResSetAutoParam isAuto.
     * @member {boolean} isAuto
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.isAuto = false;

    /**
     * ResSetAutoParam updateAll.
     * @member {boolean} updateAll
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.updateAll = false;

    /**
     * ResSetAutoParam code.
     * @member {number} code
     * @memberof param.ResSetAutoParam
     * @instance
     */
    ResSetAutoParam.prototype.code = 0;

    /**
     * Creates a new ResSetAutoParam instance using the specified properties.
     * @function create
     * @memberof param.ResSetAutoParam
     * @static
     * @param {param.IResSetAutoParam=} [properties] Properties to set
     * @returns {param.ResSetAutoParam} ResSetAutoParam instance
     */
    ResSetAutoParam.create = function create(properties) {
      return new ResSetAutoParam(properties);
    };

    /**
     * Encodes the specified ResSetAutoParam message. Does not implicitly {@link param.ResSetAutoParam.verify|verify} messages.
     * @function encode
     * @memberof param.ResSetAutoParam
     * @static
     * @param {param.IResSetAutoParam} message ResSetAutoParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResSetAutoParam.encode = function encode(message, writer, q) {
      if (!writer) writer = $Writer.create();
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      if (
        message.shootingMode != null &&
        Object.hasOwnProperty.call(message, "shootingMode")
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.shootingMode);
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.cameraType);
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.shootingTech);
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).bool(message.isAuto);
      if (
        message.updateAll != null &&
        Object.hasOwnProperty.call(message, "updateAll")
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).bool(message.updateAll);
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        writer.uint32(/* id 6, wireType 0 =*/ 48).int32(message.code);
      return writer;
    };

    /**
     * Encodes the specified ResSetAutoParam message, length delimited. Does not implicitly {@link param.ResSetAutoParam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof param.ResSetAutoParam
     * @static
     * @param {param.IResSetAutoParam} message ResSetAutoParam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResSetAutoParam.encodeDelimited = function encodeDelimited(
      message,
      writer,
    ) {
      return this.encode(
        message,
        writer && writer.len ? writer.fork() : writer,
      ).ldelim();
    };

    /**
     * Decodes a ResSetAutoParam message from the specified reader or buffer.
     * @function decode
     * @memberof param.ResSetAutoParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {param.ResSetAutoParam} ResSetAutoParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResSetAutoParam.decode = function decode(reader, length, error, long) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      if (long === undefined) long = 0;
      if (long > $Reader.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var end, message;
      if (length === undefined) end = reader.len;
      else {
        end = reader.pos + length;
        if (end > reader.len) throw RangeError("index out of range");
        length = reader.len;
        reader.len = end;
      }
      message = new $root.param.ResSetAutoParam();
      while (reader.pos < end) {
        var tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.shootingMode = reader.int32();
            break;
          }
          case 2: {
            message.cameraType = reader.int32();
            break;
          }
          case 3: {
            message.shootingTech = reader.int32();
            break;
          }
          case 4: {
            message.isAuto = reader.bool();
            break;
          }
          case 5: {
            message.updateAll = reader.bool();
            break;
          }
          case 6: {
            message.code = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7, long);
            break;
        }
      }
      if (length !== undefined) {
        if (reader.pos !== end) throw RangeError("index out of range");
        reader.len = length;
      }
      return message;
    };

    /**
     * Decodes a ResSetAutoParam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof param.ResSetAutoParam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {param.ResSetAutoParam} ResSetAutoParam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResSetAutoParam.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ResSetAutoParam message.
     * @function verify
     * @memberof param.ResSetAutoParam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ResSetAutoParam.verify = function verify(message, long) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
      if (
        message.shootingMode != null &&
        Object.hasOwnProperty.call(message, "shootingMode")
      )
        if (!$util.isInteger(message.shootingMode))
          return "shootingMode: integer expected";
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        if (!$util.isInteger(message.cameraType))
          return "cameraType: integer expected";
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        if (!$util.isInteger(message.shootingTech))
          return "shootingTech: integer expected";
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        if (typeof message.isAuto !== "boolean")
          return "isAuto: boolean expected";
      if (
        message.updateAll != null &&
        Object.hasOwnProperty.call(message, "updateAll")
      )
        if (typeof message.updateAll !== "boolean")
          return "updateAll: boolean expected";
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        if (!$util.isInteger(message.code)) return "code: integer expected";
      return null;
    };

    /**
     * Creates a ResSetAutoParam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof param.ResSetAutoParam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {param.ResSetAutoParam} ResSetAutoParam
     */
    ResSetAutoParam.fromObject = function fromObject(object, long) {
      if (object instanceof $root.param.ResSetAutoParam) return object;
      if (!$util.isObject(object))
        throw TypeError(".param.ResSetAutoParam: object expected");
      if (long === undefined) long = 0;
      if (long > $util.recursionLimit)
        throw Error("maximum nesting depth exceeded");
      var message = new $root.param.ResSetAutoParam();
      if (object.shootingMode != null)
        message.shootingMode = object.shootingMode | 0;
      if (object.cameraType != null) message.cameraType = object.cameraType | 0;
      if (object.shootingTech != null)
        message.shootingTech = object.shootingTech | 0;
      if (object.isAuto != null) message.isAuto = Boolean(object.isAuto);
      if (object.updateAll != null)
        message.updateAll = Boolean(object.updateAll);
      if (object.code != null) message.code = object.code | 0;
      return message;
    };

    /**
     * Creates a plain object from a ResSetAutoParam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof param.ResSetAutoParam
     * @static
     * @param {param.ResSetAutoParam} message ResSetAutoParam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ResSetAutoParam.toObject = function toObject(message, options, q) {
      if (!options) options = {};
      if (q === undefined) q = 0;
      if (q > $util.recursionLimit) throw Error("max depth exceeded");
      var object = {};
      if (options.defaults) {
        object.shootingMode = 0;
        object.cameraType = 0;
        object.shootingTech = 0;
        object.isAuto = false;
        object.updateAll = false;
        object.code = 0;
      }
      if (
        message.shootingMode != null &&
        Object.hasOwnProperty.call(message, "shootingMode")
      )
        object.shootingMode = message.shootingMode;
      if (
        message.cameraType != null &&
        Object.hasOwnProperty.call(message, "cameraType")
      )
        object.cameraType = message.cameraType;
      if (
        message.shootingTech != null &&
        Object.hasOwnProperty.call(message, "shootingTech")
      )
        object.shootingTech = message.shootingTech;
      if (
        message.isAuto != null &&
        Object.hasOwnProperty.call(message, "isAuto")
      )
        object.isAuto = message.isAuto;
      if (
        message.updateAll != null &&
        Object.hasOwnProperty.call(message, "updateAll")
      )
        object.updateAll = message.updateAll;
      if (message.code != null && Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      return object;
    };

    /**
     * Converts this ResSetAutoParam to JSON.
     * @function toJSON
     * @memberof param.ResSetAutoParam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ResSetAutoParam.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ResSetAutoParam
     * @function getTypeUrl
     * @memberof param.ResSetAutoParam
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ResSetAutoParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = "type.googleapis.com";
      }
      return typeUrlPrefix + "/param.ResSetAutoParam";
    };

    return ResSetAutoParam;
  })();

  return param;
})();

/**
 * ModuleId enum.
 * @exports ModuleId
 * @enum {number}
 * @property {number} MODULE_NONE=0 MODULE_NONE value
 * @property {number} MODULE_CAMERA_TELE=1 MODULE_CAMERA_TELE value
 * @property {number} MODULE_CAMERA_WIDE=2 MODULE_CAMERA_WIDE value
 * @property {number} MODULE_ASTRO=3 MODULE_ASTRO value
 * @property {number} MODULE_SYSTEM=4 MODULE_SYSTEM value
 * @property {number} MODULE_RGB_POWER=5 MODULE_RGB_POWER value
 * @property {number} MODULE_MOTOR=6 MODULE_MOTOR value
 * @property {number} MODULE_TRACK=7 MODULE_TRACK value
 * @property {number} MODULE_FOCUS=8 MODULE_FOCUS value
 * @property {number} MODULE_NOTIFY=9 MODULE_NOTIFY value
 * @property {number} MODULE_PANORAMA=10 MODULE_PANORAMA value
 * @property {number} MODULE_SHOOTING_SCHEDULE=13 MODULE_SHOOTING_SCHEDULE value
 * @property {number} MODULE_DEVICE_CONFIG=14 MODULE_DEVICE_CONFIG value
 * @property {number} MODULE_CAMERA_PARAMS=15 MODULE_CAMERA_PARAMS value
 */
$root.ModuleId = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "MODULE_NONE")] = 0;
  values[(valuesById[1] = "MODULE_CAMERA_TELE")] = 1;
  values[(valuesById[2] = "MODULE_CAMERA_WIDE")] = 2;
  values[(valuesById[3] = "MODULE_ASTRO")] = 3;
  values[(valuesById[4] = "MODULE_SYSTEM")] = 4;
  values[(valuesById[5] = "MODULE_RGB_POWER")] = 5;
  values[(valuesById[6] = "MODULE_MOTOR")] = 6;
  values[(valuesById[7] = "MODULE_TRACK")] = 7;
  values[(valuesById[8] = "MODULE_FOCUS")] = 8;
  values[(valuesById[9] = "MODULE_NOTIFY")] = 9;
  values[(valuesById[10] = "MODULE_PANORAMA")] = 10;
  values[(valuesById[13] = "MODULE_SHOOTING_SCHEDULE")] = 13;
  values[(valuesById[14] = "MODULE_DEVICE_CONFIG")] = 14;
  values[(valuesById[15] = "MODULE_CAMERA_PARAMS")] = 15;
  return values;
})();

/**
 * MessageTypeId enum.
 * @exports MessageTypeId
 * @enum {number}
 * @property {number} TYPE_REQUEST=0 TYPE_REQUEST value
 * @property {number} TYPE_REQUEST_RESPONSE=1 TYPE_REQUEST_RESPONSE value
 * @property {number} TYPE_NOTIFICATION=2 TYPE_NOTIFICATION value
 * @property {number} TYPE_NOTIFICATION_RESPONSE=3 TYPE_NOTIFICATION_RESPONSE value
 */
$root.MessageTypeId = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "TYPE_REQUEST")] = 0;
  values[(valuesById[1] = "TYPE_REQUEST_RESPONSE")] = 1;
  values[(valuesById[2] = "TYPE_NOTIFICATION")] = 2;
  values[(valuesById[3] = "TYPE_NOTIFICATION_RESPONSE")] = 3;
  return values;
})();

/**
 * DwarfCMD enum.
 * @exports DwarfCMD
 * @enum {number}
 * @property {number} NO_CMD=0 NO_CMD value
 * @property {number} CMD_CAMERA_TELE_OPEN_CAMERA=10000 CMD_CAMERA_TELE_OPEN_CAMERA value
 * @property {number} CMD_CAMERA_TELE_CLOSE_CAMERA=10001 CMD_CAMERA_TELE_CLOSE_CAMERA value
 * @property {number} CMD_CAMERA_TELE_PHOTOGRAPH=10002 CMD_CAMERA_TELE_PHOTOGRAPH value
 * @property {number} CMD_CAMERA_TELE_BURST=10003 CMD_CAMERA_TELE_BURST value
 * @property {number} CMD_CAMERA_TELE_STOP_BURST=10004 CMD_CAMERA_TELE_STOP_BURST value
 * @property {number} CMD_CAMERA_TELE_START_RECORD=10005 CMD_CAMERA_TELE_START_RECORD value
 * @property {number} CMD_CAMERA_TELE_STOP_RECORD=10006 CMD_CAMERA_TELE_STOP_RECORD value
 * @property {number} CMD_CAMERA_TELE_SET_EXP_MODE=10007 CMD_CAMERA_TELE_SET_EXP_MODE value
 * @property {number} CMD_CAMERA_TELE_GET_EXP_MODE=10008 CMD_CAMERA_TELE_GET_EXP_MODE value
 * @property {number} CMD_CAMERA_TELE_SET_EXP=10009 CMD_CAMERA_TELE_SET_EXP value
 * @property {number} CMD_CAMERA_TELE_GET_EXP=10010 CMD_CAMERA_TELE_GET_EXP value
 * @property {number} CMD_CAMERA_TELE_SET_GAIN_MODE=10011 CMD_CAMERA_TELE_SET_GAIN_MODE value
 * @property {number} CMD_CAMERA_TELE_GET_GAIN_MODE=10012 CMD_CAMERA_TELE_GET_GAIN_MODE value
 * @property {number} CMD_CAMERA_TELE_SET_GAIN=10013 CMD_CAMERA_TELE_SET_GAIN value
 * @property {number} CMD_CAMERA_TELE_GET_GAIN=10014 CMD_CAMERA_TELE_GET_GAIN value
 * @property {number} CMD_CAMERA_TELE_SET_BRIGHTNESS=10015 CMD_CAMERA_TELE_SET_BRIGHTNESS value
 * @property {number} CMD_CAMERA_TELE_GET_BRIGHTNESS=10016 CMD_CAMERA_TELE_GET_BRIGHTNESS value
 * @property {number} CMD_CAMERA_TELE_SET_CONTRAST=10017 CMD_CAMERA_TELE_SET_CONTRAST value
 * @property {number} CMD_CAMERA_TELE_GET_CONTRAST=10018 CMD_CAMERA_TELE_GET_CONTRAST value
 * @property {number} CMD_CAMERA_TELE_SET_SATURATION=10019 CMD_CAMERA_TELE_SET_SATURATION value
 * @property {number} CMD_CAMERA_TELE_GET_SATURATION=10020 CMD_CAMERA_TELE_GET_SATURATION value
 * @property {number} CMD_CAMERA_TELE_SET_HUE=10021 CMD_CAMERA_TELE_SET_HUE value
 * @property {number} CMD_CAMERA_TELE_GET_HUE=10022 CMD_CAMERA_TELE_GET_HUE value
 * @property {number} CMD_CAMERA_TELE_SET_SHARPNESS=10023 CMD_CAMERA_TELE_SET_SHARPNESS value
 * @property {number} CMD_CAMERA_TELE_GET_SHARPNESS=10024 CMD_CAMERA_TELE_GET_SHARPNESS value
 * @property {number} CMD_CAMERA_TELE_SET_WB_MODE=10025 CMD_CAMERA_TELE_SET_WB_MODE value
 * @property {number} CMD_CAMERA_TELE_GET_WB_MODE=10026 CMD_CAMERA_TELE_GET_WB_MODE value
 * @property {number} CMD_CAMERA_TELE_SET_WB_SCENE=10027 CMD_CAMERA_TELE_SET_WB_SCENE value
 * @property {number} CMD_CAMERA_TELE_GET_WB_SCENE=10028 CMD_CAMERA_TELE_GET_WB_SCENE value
 * @property {number} CMD_CAMERA_TELE_SET_WB_CT=10029 CMD_CAMERA_TELE_SET_WB_CT value
 * @property {number} CMD_CAMERA_TELE_GET_WB_CT=10030 CMD_CAMERA_TELE_GET_WB_CT value
 * @property {number} CMD_CAMERA_TELE_SET_IRCUT=10031 CMD_CAMERA_TELE_SET_IRCUT value
 * @property {number} CMD_CAMERA_TELE_GET_IRCUT=10032 CMD_CAMERA_TELE_GET_IRCUT value
 * @property {number} CMD_CAMERA_TELE_START_TIMELAPSE_PHOTO=10033 CMD_CAMERA_TELE_START_TIMELAPSE_PHOTO value
 * @property {number} CMD_CAMERA_TELE_STOP_TIMELAPSE_PHOTO=10034 CMD_CAMERA_TELE_STOP_TIMELAPSE_PHOTO value
 * @property {number} CMD_CAMERA_TELE_SET_ALL_PARAMS=10035 CMD_CAMERA_TELE_SET_ALL_PARAMS value
 * @property {number} CMD_CAMERA_TELE_GET_ALL_PARAMS=10036 CMD_CAMERA_TELE_GET_ALL_PARAMS value
 * @property {number} CMD_CAMERA_TELE_SET_FEATURE_PARAM=10037 CMD_CAMERA_TELE_SET_FEATURE_PARAM value
 * @property {number} CMD_CAMERA_TELE_GET_ALL_FEATURE_PARAMS=10038 CMD_CAMERA_TELE_GET_ALL_FEATURE_PARAMS value
 * @property {number} CMD_CAMERA_TELE_GET_SYSTEM_WORKING_STATE=10039 CMD_CAMERA_TELE_GET_SYSTEM_WORKING_STATE value
 * @property {number} CMD_CAMERA_TELE_SET_JPG_QUALITY=10040 CMD_CAMERA_TELE_SET_JPG_QUALITY value
 * @property {number} CMD_CAMERA_TELE_PHOTO_RAW=10041 CMD_CAMERA_TELE_PHOTO_RAW value
 * @property {number} CMD_CAMERA_TELE_SET_RTSP_BITRATE_TYPE=10042 CMD_CAMERA_TELE_SET_RTSP_BITRATE_TYPE value
 * @property {number} CMD_CAMERA_TELE_SET_PREVIEW_QUALITY=10050 CMD_CAMERA_TELE_SET_PREVIEW_QUALITY value
 * @property {number} CMD_V3_CAMERA_TELE_OPEN_CAMERA=10050 CMD_V3_CAMERA_TELE_OPEN_CAMERA value
 * @property {number} CMD_ASTRO_START_CALIBRATION=11000 CMD_ASTRO_START_CALIBRATION value
 * @property {number} CMD_ASTRO_STOP_CALIBRATION=11001 CMD_ASTRO_STOP_CALIBRATION value
 * @property {number} CMD_ASTRO_START_GOTO_DSO=11002 CMD_ASTRO_START_GOTO_DSO value
 * @property {number} CMD_ASTRO_START_GOTO_SOLAR_SYSTEM=11003 CMD_ASTRO_START_GOTO_SOLAR_SYSTEM value
 * @property {number} CMD_ASTRO_STOP_GOTO=11004 CMD_ASTRO_STOP_GOTO value
 * @property {number} CMD_ASTRO_START_CAPTURE_RAW_LIVE_STACKING=11005 CMD_ASTRO_START_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_ASTRO_STOP_CAPTURE_RAW_LIVE_STACKING=11006 CMD_ASTRO_STOP_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_ASTRO_START_CAPTURE_RAW_DARK=11007 CMD_ASTRO_START_CAPTURE_RAW_DARK value
 * @property {number} CMD_ASTRO_STOP_CAPTURE_RAW_DARK=11008 CMD_ASTRO_STOP_CAPTURE_RAW_DARK value
 * @property {number} CMD_ASTRO_CHECK_GOT_DARK=11009 CMD_ASTRO_CHECK_GOT_DARK value
 * @property {number} CMD_ASTRO_GO_LIVE=11010 CMD_ASTRO_GO_LIVE value
 * @property {number} CMD_ASTRO_START_TRACK_SPECIAL_TARGET=11011 CMD_ASTRO_START_TRACK_SPECIAL_TARGET value
 * @property {number} CMD_ASTRO_STOP_TRACK_SPECIAL_TARGET=11012 CMD_ASTRO_STOP_TRACK_SPECIAL_TARGET value
 * @property {number} CMD_ASTRO_START_ONE_CLICK_GOTO_DSO=11013 CMD_ASTRO_START_ONE_CLICK_GOTO_DSO value
 * @property {number} CMD_ASTRO_START_ONE_CLICK_GOTO_SOLAR_SYSTEM=11014 CMD_ASTRO_START_ONE_CLICK_GOTO_SOLAR_SYSTEM value
 * @property {number} CMD_ASTRO_STOP_ONE_CLICK_GOTO=11015 CMD_ASTRO_STOP_ONE_CLICK_GOTO value
 * @property {number} CMD_ASTRO_START_WIDE_CAPTURE_LIVE_STACKING=11016 CMD_ASTRO_START_WIDE_CAPTURE_LIVE_STACKING value
 * @property {number} CMD_ASTRO_STOP_WIDE_CAPTURE_LIVE_STACKING=11017 CMD_ASTRO_STOP_WIDE_CAPTURE_LIVE_STACKING value
 * @property {number} CMD_ASTRO_START_EQ_SOLVING=11018 CMD_ASTRO_START_EQ_SOLVING value
 * @property {number} CMD_ASTRO_STOP_EQ_SOLVING=11019 CMD_ASTRO_STOP_EQ_SOLVING value
 * @property {number} CMD_ASTRO_WIDE_GO_LIVE=11020 CMD_ASTRO_WIDE_GO_LIVE value
 * @property {number} CMD_ASTRO_START_CAPTURE_RAW_DARK_WITH_PARAM=11021 CMD_ASTRO_START_CAPTURE_RAW_DARK_WITH_PARAM value
 * @property {number} CMD_ASTRO_STOP_CAPTURE_RAW_DARK_WITH_PARAM=11022 CMD_ASTRO_STOP_CAPTURE_RAW_DARK_WITH_PARAM value
 * @property {number} CMD_ASTRO_GET_DARK_FRAME_LIST=11023 CMD_ASTRO_GET_DARK_FRAME_LIST value
 * @property {number} CMD_ASTRO_DEL_DARK_FRAME_LIST=11024 CMD_ASTRO_DEL_DARK_FRAME_LIST value
 * @property {number} CMD_ASTRO_START_CAPTURE_WIDE_RAW_DARK_WITH_PARAM=11025 CMD_ASTRO_START_CAPTURE_WIDE_RAW_DARK_WITH_PARAM value
 * @property {number} CMD_ASTRO_STOP_CAPTURE_WIDE_RAW_DARK_WITH_PARAM=11026 CMD_ASTRO_STOP_CAPTURE_WIDE_RAW_DARK_WITH_PARAM value
 * @property {number} CMD_ASTRO_GET_WIDE_DARK_FRAME_LIST=11027 CMD_ASTRO_GET_WIDE_DARK_FRAME_LIST value
 * @property {number} CMD_ASTRO_DEL_WIDE_DARK_FRAME_LIST=11028 CMD_ASTRO_DEL_WIDE_DARK_FRAME_LIST value
 * @property {number} CMD_ASTRO_START_MAKE_FITS_THUMB=11033 CMD_ASTRO_START_MAKE_FITS_THUMB value
 * @property {number} CMD_V3_ASTRO_SAVE_STACKED_IMAGE=11033 CMD_V3_ASTRO_SAVE_STACKED_IMAGE value
 * @property {number} CMD_ASTRO_STOP_MAKE_FITS_THUMB=11034 CMD_ASTRO_STOP_MAKE_FITS_THUMB value
 * @property {number} CMD_V3_ASTRO_LIST_SAVED_IMAGES=11034 CMD_V3_ASTRO_LIST_SAVED_IMAGES value
 * @property {number} CMD_ASTRO_STOP_RESTACKED=11036 CMD_ASTRO_STOP_RESTACKED value
 * @property {number} CMD_V3_ASTRO_SAVE_COMPLETE=11036 CMD_V3_ASTRO_SAVE_COMPLETE value
 * @property {number} CMD_ASTRO_GET_ASTRO_SHOOTING_TIME=11039 CMD_ASTRO_GET_ASTRO_SHOOTING_TIME value
 * @property {number} CMD_V3_ASTRO_STATUS_POLLING=11039 CMD_V3_ASTRO_STATUS_POLLING value
 * @property {number} CMD_ASTRO_GET_QUICK_SET_LIST=11040 CMD_ASTRO_GET_QUICK_SET_LIST value
 * @property {number} CMD_V3_ASTRO_GET_PARAMS=11040 CMD_V3_ASTRO_GET_PARAMS value
 * @property {number} CMD_ASTRO_SET_QUICK_SET=11041 CMD_ASTRO_SET_QUICK_SET value
 * @property {number} CMD_V3_ASTRO_SET_PARAMS=11041 CMD_V3_ASTRO_SET_PARAMS value
 * @property {number} CMD_ASTRO_GET_CALI_FRAME_LIST=11043 CMD_ASTRO_GET_CALI_FRAME_LIST value
 * @property {number} CMD_V3_ASTRO_GET_PRESETS=11043 CMD_V3_ASTRO_GET_PRESETS value
 * @property {number} CMD_ASTRO_START_SKY_TARGET_FINDER=11047 CMD_ASTRO_START_SKY_TARGET_FINDER value
 * @property {number} CMD_V3_ASTRO_SET_LOCATION=11047 CMD_V3_ASTRO_SET_LOCATION value
 * @property {number} CMD_ASTRO_STOP_SKY_TARGET_FINDER=11048 CMD_ASTRO_STOP_SKY_TARGET_FINDER value
 * @property {number} CMD_V3_ASTRO_CONFIRM=11048 CMD_V3_ASTRO_CONFIRM value
 * @property {number} CMD_ASTRO_CONTINUE_SHOOTING=11050 CMD_ASTRO_CONTINUE_SHOOTING value
 * @property {number} CMD_CAMERA_WIDE_OPEN_CAMERA=12000 CMD_CAMERA_WIDE_OPEN_CAMERA value
 * @property {number} CMD_CAMERA_WIDE_CLOSE_CAMERA=12001 CMD_CAMERA_WIDE_CLOSE_CAMERA value
 * @property {number} CMD_CAMERA_WIDE_SET_EXP_MODE=12002 CMD_CAMERA_WIDE_SET_EXP_MODE value
 * @property {number} CMD_CAMERA_WIDE_GET_EXP_MODE=12003 CMD_CAMERA_WIDE_GET_EXP_MODE value
 * @property {number} CMD_CAMERA_WIDE_SET_EXP=12004 CMD_CAMERA_WIDE_SET_EXP value
 * @property {number} CMD_CAMERA_WIDE_GET_EXP=12005 CMD_CAMERA_WIDE_GET_EXP value
 * @property {number} CMD_CAMERA_WIDE_SET_GAIN=12006 CMD_CAMERA_WIDE_SET_GAIN value
 * @property {number} CMD_CAMERA_WIDE_GET_GAIN=12007 CMD_CAMERA_WIDE_GET_GAIN value
 * @property {number} CMD_CAMERA_WIDE_SET_BRIGHTNESS=12008 CMD_CAMERA_WIDE_SET_BRIGHTNESS value
 * @property {number} CMD_CAMERA_WIDE_GET_BRIGHTNESS=12009 CMD_CAMERA_WIDE_GET_BRIGHTNESS value
 * @property {number} CMD_CAMERA_WIDE_SET_CONTRAST=12010 CMD_CAMERA_WIDE_SET_CONTRAST value
 * @property {number} CMD_CAMERA_WIDE_GET_CONTRAST=12011 CMD_CAMERA_WIDE_GET_CONTRAST value
 * @property {number} CMD_CAMERA_WIDE_SET_SATURATION=12012 CMD_CAMERA_WIDE_SET_SATURATION value
 * @property {number} CMD_CAMERA_WIDE_GET_SATURATION=12013 CMD_CAMERA_WIDE_GET_SATURATION value
 * @property {number} CMD_CAMERA_WIDE_SET_HUE=12014 CMD_CAMERA_WIDE_SET_HUE value
 * @property {number} CMD_CAMERA_WIDE_GET_HUE=12015 CMD_CAMERA_WIDE_GET_HUE value
 * @property {number} CMD_CAMERA_WIDE_SET_SHARPNESS=12016 CMD_CAMERA_WIDE_SET_SHARPNESS value
 * @property {number} CMD_CAMERA_WIDE_GET_SHARPNESS=12017 CMD_CAMERA_WIDE_GET_SHARPNESS value
 * @property {number} CMD_CAMERA_WIDE_SET_WB_MODE=12018 CMD_CAMERA_WIDE_SET_WB_MODE value
 * @property {number} CMD_CAMERA_WIDE_GET_WB_MODE=12019 CMD_CAMERA_WIDE_GET_WB_MODE value
 * @property {number} CMD_CAMERA_WIDE_SET_WB_CT=12020 CMD_CAMERA_WIDE_SET_WB_CT value
 * @property {number} CMD_CAMERA_WIDE_GET_WB_CT=12021 CMD_CAMERA_WIDE_GET_WB_CT value
 * @property {number} CMD_CAMERA_WIDE_PHOTOGRAPH=12022 CMD_CAMERA_WIDE_PHOTOGRAPH value
 * @property {number} CMD_CAMERA_WIDE_BURST=12023 CMD_CAMERA_WIDE_BURST value
 * @property {number} CMD_CAMERA_WIDE_STOP_BURST=12024 CMD_CAMERA_WIDE_STOP_BURST value
 * @property {number} CMD_CAMERA_WIDE_START_TIMELAPSE_PHOTO=12025 CMD_CAMERA_WIDE_START_TIMELAPSE_PHOTO value
 * @property {number} CMD_CAMERA_WIDE_STOP_TIMELAPSE_PHOTO=12026 CMD_CAMERA_WIDE_STOP_TIMELAPSE_PHOTO value
 * @property {number} CMD_CAMERA_WIDE_GET_ALL_PARAMS=12027 CMD_CAMERA_WIDE_GET_ALL_PARAMS value
 * @property {number} CMD_CAMERA_WIDE_SET_ALL_PARAMS=12028 CMD_CAMERA_WIDE_SET_ALL_PARAMS value
 * @property {number} CMD_CAMERA_WIDE_START_RECORD=12030 CMD_CAMERA_WIDE_START_RECORD value
 * @property {number} CMD_CAMERA_WIDE_STOP_RECORD=12031 CMD_CAMERA_WIDE_STOP_RECORD value
 * @property {number} CMD_CAMERA_WIDE_SET_PREVIEW_QUALITY=12036 CMD_CAMERA_WIDE_SET_PREVIEW_QUALITY value
 * @property {number} CMD_V3_CAMERA_WIDE_OPEN_CAMERA=12036 CMD_V3_CAMERA_WIDE_OPEN_CAMERA value
 * @property {number} CMD_SYSTEM_SET_TIME=13000 CMD_SYSTEM_SET_TIME value
 * @property {number} CMD_SYSTEM_SET_TIME_ZONE=13001 CMD_SYSTEM_SET_TIME_ZONE value
 * @property {number} CMD_SYSTEM_SET_MTP_MODE=13002 CMD_SYSTEM_SET_MTP_MODE value
 * @property {number} CMD_SYSTEM_SET_CPU_MODE=13003 CMD_SYSTEM_SET_CPU_MODE value
 * @property {number} CMD_SYSTEM_SET_MASTER=13004 CMD_SYSTEM_SET_MASTER value
 * @property {number} CMD_SYSTEM_SET_MASTERLOCK=13004 CMD_SYSTEM_SET_MASTERLOCK value
 * @property {number} CMD_SYSTEM_SET_LOCATION=13010 CMD_SYSTEM_SET_LOCATION value
 * @property {number} CMD_V3_SYSTEM_SET_GPS_LOCATION=13010 CMD_V3_SYSTEM_SET_GPS_LOCATION value
 * @property {number} CMD_RGB_POWER_OPEN_RGB=13500 CMD_RGB_POWER_OPEN_RGB value
 * @property {number} CMD_RGB_POWER_CLOSE_RGB=13501 CMD_RGB_POWER_CLOSE_RGB value
 * @property {number} CMD_RGB_POWER_POWER_DOWN=13502 CMD_RGB_POWER_POWER_DOWN value
 * @property {number} CMD_RGB_POWER_POWERIND_ON=13503 CMD_RGB_POWER_POWERIND_ON value
 * @property {number} CMD_RGB_POWER_POWERIND_OFF=13504 CMD_RGB_POWER_POWERIND_OFF value
 * @property {number} CMD_RGB_POWER_REBOOT=13505 CMD_RGB_POWER_REBOOT value
 * @property {number} CMD_STEP_MOTOR_RUN=14000 CMD_STEP_MOTOR_RUN value
 * @property {number} CMD_STEP_MOTOR_RUN_TO=14001 CMD_STEP_MOTOR_RUN_TO value
 * @property {number} CMD_STEP_MOTOR_STOP=14002 CMD_STEP_MOTOR_STOP value
 * @property {number} CMD_STEP_MOTOR_RESET=14003 CMD_STEP_MOTOR_RESET value
 * @property {number} CMD_STEP_MOTOR_CHANGE_SPEED=14004 CMD_STEP_MOTOR_CHANGE_SPEED value
 * @property {number} CMD_STEP_MOTOR_CHANGE_DIRECTION=14005 CMD_STEP_MOTOR_CHANGE_DIRECTION value
 * @property {number} CMD_STEP_MOTOR_SERVICE_JOYSTICK=14006 CMD_STEP_MOTOR_SERVICE_JOYSTICK value
 * @property {number} CMD_STEP_MOTOR_SERVICE_JOYSTICK_FIXED_ANGLE=14007 CMD_STEP_MOTOR_SERVICE_JOYSTICK_FIXED_ANGLE value
 * @property {number} CMD_STEP_MOTOR_SERVICE_JOYSTICK_STOP=14008 CMD_STEP_MOTOR_SERVICE_JOYSTICK_STOP value
 * @property {number} CMD_STEP_MOTOR_SERVICE_DUAL_CAMERA_LINKAGE=14009 CMD_STEP_MOTOR_SERVICE_DUAL_CAMERA_LINKAGE value
 * @property {number} CMD_STEP_MOTOR_RUN_IN_PULSE=14010 CMD_STEP_MOTOR_RUN_IN_PULSE value
 * @property {number} CMD_STEP_MOTOR_GET_POSITION=14011 CMD_STEP_MOTOR_GET_POSITION value
 * @property {number} CMD_TRACK_START_TRACK=14800 CMD_TRACK_START_TRACK value
 * @property {number} CMD_TRACK_STOP_TRACK=14801 CMD_TRACK_STOP_TRACK value
 * @property {number} CMD_SENTRY_MODE_START=14802 CMD_SENTRY_MODE_START value
 * @property {number} CMD_SENTRY_MODE_STOP=14803 CMD_SENTRY_MODE_STOP value
 * @property {number} CMD_MOT_START=14804 CMD_MOT_START value
 * @property {number} CMD_MOT_TRACK_ONE=14805 CMD_MOT_TRACK_ONE value
 * @property {number} CMD_UFOTRACK_MODE_START=14806 CMD_UFOTRACK_MODE_START value
 * @property {number} CMD_UFOTRACK_MODE_STOP=14807 CMD_UFOTRACK_MODE_STOP value
 * @property {number} CMD_MOT_WIDE_TRACK_ONE=14808 CMD_MOT_WIDE_TRACK_ONE value
 * @property {number} CMD_SWITCH_MAIN_PREVIEW=14809 CMD_SWITCH_MAIN_PREVIEW value
 * @property {number} CMD_WIDE_TELE_TRACK_SWITCH=14809 CMD_WIDE_TELE_TRACK_SWITCH value
 * @property {number} CMD_UFO_HAND_AOTO_MODE=14810 CMD_UFO_HAND_AOTO_MODE value
 * @property {number} CMD_FOCUS_AUTO_FOCUS=15000 CMD_FOCUS_AUTO_FOCUS value
 * @property {number} CMD_FOCUS_MANUAL_SINGLE_STEP_FOCUS=15001 CMD_FOCUS_MANUAL_SINGLE_STEP_FOCUS value
 * @property {number} CMD_FOCUS_START_MANUAL_CONTINU_FOCUS=15002 CMD_FOCUS_START_MANUAL_CONTINU_FOCUS value
 * @property {number} CMD_FOCUS_STOP_MANUAL_CONTINU_FOCUS=15003 CMD_FOCUS_STOP_MANUAL_CONTINU_FOCUS value
 * @property {number} CMD_FOCUS_START_ASTRO_AUTO_FOCUS=15004 CMD_FOCUS_START_ASTRO_AUTO_FOCUS value
 * @property {number} CMD_FOCUS_STOP_ASTRO_AUTO_FOCUS=15005 CMD_FOCUS_STOP_ASTRO_AUTO_FOCUS value
 * @property {number} CMD_FOCUS_GET_USER_INFINITY_POS=15011 CMD_FOCUS_GET_USER_INFINITY_POS value
 * @property {number} CMD_V3_FOCUS_INIT=15011 CMD_V3_FOCUS_INIT value
 * @property {number} CMD_NOTIFY_TELE_WIDE_PICTURE_MATCHING=15200 CMD_NOTIFY_TELE_WIDE_PICTURE_MATCHING value
 * @property {number} CMD_NOTIFY_TELE_WIDI_PICTURE_MATCHING=15200 CMD_NOTIFY_TELE_WIDI_PICTURE_MATCHING value
 * @property {number} CMD_NOTIFY_ELE=15201 CMD_NOTIFY_ELE value
 * @property {number} CMD_NOTIFY_CHARGE=15202 CMD_NOTIFY_CHARGE value
 * @property {number} CMD_NOTIFY_SDCARD_INFO=15203 CMD_NOTIFY_SDCARD_INFO value
 * @property {number} CMD_NOTIFY_TELE_RECORD_TIME=15204 CMD_NOTIFY_TELE_RECORD_TIME value
 * @property {number} CMD_NOTIFY_TELE_TIMELAPSE_OUT_TIME=15205 CMD_NOTIFY_TELE_TIMELAPSE_OUT_TIME value
 * @property {number} CMD_NOTIFY_STATE_CAPTURE_RAW_DARK=15206 CMD_NOTIFY_STATE_CAPTURE_RAW_DARK value
 * @property {number} CMD_NOTIFY_PROGRASS_CAPTURE_RAW_DARK=15207 CMD_NOTIFY_PROGRASS_CAPTURE_RAW_DARK value
 * @property {number} CMD_NOTIFY_STATE_CAPTURE_RAW_LIVE_STACKING=15208 CMD_NOTIFY_STATE_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_NOTIFY_PROGRASS_CAPTURE_RAW_LIVE_STACKING=15209 CMD_NOTIFY_PROGRASS_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_NOTIFY_STATE_ASTRO_CALIBRATION=15210 CMD_NOTIFY_STATE_ASTRO_CALIBRATION value
 * @property {number} CMD_NOTIFY_STATE_ASTRO_GOTO=15211 CMD_NOTIFY_STATE_ASTRO_GOTO value
 * @property {number} CMD_NOTIFY_STATE_ASTRO_TRACKING=15212 CMD_NOTIFY_STATE_ASTRO_TRACKING value
 * @property {number} CMD_NOTIFY_TELE_SET_PARAM=15213 CMD_NOTIFY_TELE_SET_PARAM value
 * @property {number} CMD_NOTIFY_WIDE_SET_PARAM=15214 CMD_NOTIFY_WIDE_SET_PARAM value
 * @property {number} CMD_NOTIFY_TELE_FUNCTION_STATE=15215 CMD_NOTIFY_TELE_FUNCTION_STATE value
 * @property {number} CMD_NOTIFY_WIDE_FUNCTION_STATE=15216 CMD_NOTIFY_WIDE_FUNCTION_STATE value
 * @property {number} CMD_NOTIFY_SET_FEATURE_PARAM=15217 CMD_NOTIFY_SET_FEATURE_PARAM value
 * @property {number} CMD_NOTIFY_TELE_BURST_PROGRESS=15218 CMD_NOTIFY_TELE_BURST_PROGRESS value
 * @property {number} CMD_NOTIFY_PANORAMA_PROGRESS=15219 CMD_NOTIFY_PANORAMA_PROGRESS value
 * @property {number} CMD_NOTIFY_WIDE_BURST_PROGRESS=15220 CMD_NOTIFY_WIDE_BURST_PROGRESS value
 * @property {number} CMD_NOTIFY_RGB_STATE=15221 CMD_NOTIFY_RGB_STATE value
 * @property {number} CMD_NOTIFY_POWER_IND_STATE=15222 CMD_NOTIFY_POWER_IND_STATE value
 * @property {number} CMD_NOTIFY_WS_HOST_SLAVE_MODE=15223 CMD_NOTIFY_WS_HOST_SLAVE_MODE value
 * @property {number} CMD_NOTIFY_MTP_STATE=15224 CMD_NOTIFY_MTP_STATE value
 * @property {number} CMD_NOTIFY_TRACK_RESULT=15225 CMD_NOTIFY_TRACK_RESULT value
 * @property {number} CMD_NOTIFY_WIDE_TIMELAPSE_OUT_TIME=15226 CMD_NOTIFY_WIDE_TIMELAPSE_OUT_TIME value
 * @property {number} CMD_NOTIFY_CPU_MODE=15227 CMD_NOTIFY_CPU_MODE value
 * @property {number} CMD_NOTIFY_STATE_ASTRO_TRACKING_SPECIAL=15228 CMD_NOTIFY_STATE_ASTRO_TRACKING_SPECIAL value
 * @property {number} CMD_NOTIFY_POWER_OFF=15229 CMD_NOTIFY_POWER_OFF value
 * @property {number} CMD_NOTIFY_ALBUM_UPDATE=15230 CMD_NOTIFY_ALBUM_UPDATE value
 * @property {number} CMD_NOTIFY_SENTRY_MODE_STATE=15231 CMD_NOTIFY_SENTRY_MODE_STATE value
 * @property {number} CMD_NOTIFY_SENTRY_MODE_TRACK_RESULT=15232 CMD_NOTIFY_SENTRY_MODE_TRACK_RESULT value
 * @property {number} CMD_NOTIFY_STATE_ASTRO_ONE_CLICK_GOTO=15233 CMD_NOTIFY_STATE_ASTRO_ONE_CLICK_GOTO value
 * @property {number} CMD_NOTIFY_STREAM_TYPE=15234 CMD_NOTIFY_STREAM_TYPE value
 * @property {number} CMD_NOTIFY_WIDE_RECORD_TIME=15235 CMD_NOTIFY_WIDE_RECORD_TIME value
 * @property {number} CMD_NOTIFY_STATE_WIDE_CAPTURE_RAW_LIVE_STACKING=15236 CMD_NOTIFY_STATE_WIDE_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_NOTIFY_PROGRASS_WIDE_CAPTURE_RAW_LIVE_STACKING=15237 CMD_NOTIFY_PROGRASS_WIDE_CAPTURE_RAW_LIVE_STACKING value
 * @property {number} CMD_NOTIFY_MULTI_TRACK_RESULT=15238 CMD_NOTIFY_MULTI_TRACK_RESULT value
 * @property {number} CMD_NOTIFY_EQ_SOLVING_STATE=15239 CMD_NOTIFY_EQ_SOLVING_STATE value
 * @property {number} CMD_NOTIFY_UFO_MODE_STATE=15240 CMD_NOTIFY_UFO_MODE_STATE value
 * @property {number} CMD_NOTIFY_TELE_LONG_EXP_PROGRESS=15241 CMD_NOTIFY_TELE_LONG_EXP_PROGRESS value
 * @property {number} CMD_NOTIFY_WIDE_LONG_EXP_PROGRESS=15242 CMD_NOTIFY_WIDE_LONG_EXP_PROGRESS value
 * @property {number} CMD_NOTIFY_TEMPERATURE=15243 CMD_NOTIFY_TEMPERATURE value
 * @property {number} CMD_NOTIFY_PANORAMA_UPLOAD_COMPRESS_PROGRESS=15244 CMD_NOTIFY_PANORAMA_UPLOAD_COMPRESS_PROGRESS value
 * @property {number} CMD_NOTIFY_PANORAMA_UPLOAD_UPLOAD_PROGRESS=15245 CMD_NOTIFY_PANORAMA_UPLOAD_UPLOAD_PROGRESS value
 * @property {number} CMD_NOTIFY_PANORAMA_UPLOAD_COMPLETE=15245 CMD_NOTIFY_PANORAMA_UPLOAD_COMPLETE value
 * @property {number} CMD_NOTIFY_STATE_CAPTURE_WIDE_RAW_DARK=15247 CMD_NOTIFY_STATE_CAPTURE_WIDE_RAW_DARK value
 * @property {number} CMD_NOTIFY_SHOOTING_SCHEDULE_RESULT_AND_STATE=15248 CMD_NOTIFY_SHOOTING_SCHEDULE_RESULT_AND_STATE value
 * @property {number} CMD_NOTIFY_SHOOTING_TASK_STATE=15249 CMD_NOTIFY_SHOOTING_TASK_STATE value
 * @property {number} CMD_NOTIFY_SKY_SEACHER_STATE=15250 CMD_NOTIFY_SKY_SEACHER_STATE value
 * @property {number} CMD_NOTIFY_WIDE_MULTI_TRACK_RESULT=15251 CMD_NOTIFY_WIDE_MULTI_TRACK_RESULT value
 * @property {number} CMD_NOTIFY_WIDE_TRACK_RESULT=15252 CMD_NOTIFY_WIDE_TRACK_RESULT value
 * @property {number} CMD_NOTIFY_CALIBRATION_RESULT=15256 CMD_NOTIFY_CALIBRATION_RESULT value
 * @property {number} CMD_NOTIFY_FOCUS_POSITION=15257 CMD_NOTIFY_FOCUS_POSITION value
 * @property {number} CMD_NOTIFY_FOCUS=15257 CMD_NOTIFY_FOCUS value
 * @property {number} CMD_NOTIFY_WAIT_SHOOTING_PROGRESS=15255 CMD_NOTIFY_WAIT_SHOOTING_PROGRESS value
 * @property {number} CMD_V3_NOTIFY_EXPOSURE_PROGRESS=15255 CMD_V3_NOTIFY_EXPOSURE_PROGRESS value
 * @property {number} CMD_NOTIFY_EXCLUSIVE_SYSTEM_IO_TASK_STATE=15261 CMD_NOTIFY_EXCLUSIVE_SYSTEM_IO_TASK_STATE value
 * @property {number} CMD_V3_NOTIFY_DEVICE_STATE=15261 CMD_V3_NOTIFY_DEVICE_STATE value
 * @property {number} CMD_NOTIFY_GENERAL_INT_PARAM=15264 CMD_NOTIFY_GENERAL_INT_PARAM value
 * @property {number} CMD_V3_NOTIFY_CAMERA_PARAM_STATE=15264 CMD_V3_NOTIFY_CAMERA_PARAM_STATE value
 * @property {number} CMD_NOTIFY_SWITCH_SHOOTING_MODE=15267 CMD_NOTIFY_SWITCH_SHOOTING_MODE value
 * @property {number} CMD_V3_NOTIFY_MODE_CHANGE=15267 CMD_V3_NOTIFY_MODE_CHANGE value
 * @property {number} CMD_NOTIFY_WB=15270 CMD_NOTIFY_WB value
 * @property {number} CMD_V3_NOTIFY_STACKING_DATA=15270 CMD_V3_NOTIFY_STACKING_DATA value
 * @property {number} CMD_NOTIFY_PHOTO_STATE=15273 CMD_NOTIFY_PHOTO_STATE value
 * @property {number} CMD_V3_NOTIFY_PHOTO_STATE=15273 CMD_V3_NOTIFY_PHOTO_STATE value
 * @property {number} CMD_NOTIFY_BURST_STATE=15274 CMD_NOTIFY_BURST_STATE value
 * @property {number} CMD_V3_NOTIFY_BURST_STATE=15274 CMD_V3_NOTIFY_BURST_STATE value
 * @property {number} CMD_NOTIFY_RECORD_STATE=15275 CMD_NOTIFY_RECORD_STATE value
 * @property {number} CMD_V3_NOTIFY_VIDEO_STATE=15275 CMD_V3_NOTIFY_VIDEO_STATE value
 * @property {number} CMD_NOTIFY_TIMELAPSE_STATE=15276 CMD_NOTIFY_TIMELAPSE_STATE value
 * @property {number} CMD_V3_NOTIFY_TIMELAPSE_STATE=15276 CMD_V3_NOTIFY_TIMELAPSE_STATE value
 * @property {number} CMD_NOTIFY_ASTRO_AUTO_FOCUS_STATE=15278 CMD_NOTIFY_ASTRO_AUTO_FOCUS_STATE value
 * @property {number} CMD_V3_NOTIFY_AUTOFOCUS_STATE=15278 CMD_V3_NOTIFY_AUTOFOCUS_STATE value
 * @property {number} CMD_NOTIFY_ASTRO_AUTO_FOCUS_FAST_STATE=15280 CMD_NOTIFY_ASTRO_AUTO_FOCUS_FAST_STATE value
 * @property {number} CMD_V3_NOTIFY_AUTOFOCUS_STATE_ALT=15280 CMD_V3_NOTIFY_AUTOFOCUS_STATE_ALT value
 * @property {number} CMD_NOTIFY_BURST_PROGRESS=15285 CMD_NOTIFY_BURST_PROGRESS value
 * @property {number} CMD_V3_NOTIFY_PHOTO_BURST_PROGRESS=15285 CMD_V3_NOTIFY_PHOTO_BURST_PROGRESS value
 * @property {number} CMD_NOTIFY_RECORD_TIME=15286 CMD_NOTIFY_RECORD_TIME value
 * @property {number} CMD_V3_NOTIFY_VIDEO_PROGRESS=15286 CMD_V3_NOTIFY_VIDEO_PROGRESS value
 * @property {number} CMD_NOTIFY_TIMELAPSE_OUT_TIME=15287 CMD_NOTIFY_TIMELAPSE_OUT_TIME value
 * @property {number} CMD_V3_NOTIFY_TIMELAPSE_PROGRESS=15287 CMD_V3_NOTIFY_TIMELAPSE_PROGRESS value
 * @property {number} CMD_NOTIFY_LONG_EXP_PROGRESS=15288 CMD_NOTIFY_LONG_EXP_PROGRESS value
 * @property {number} CMD_NOTIFY_STATE_CAPTURE_CALI_FRAME=15290 CMD_NOTIFY_STATE_CAPTURE_CALI_FRAME value
 * @property {number} CMD_V3_NOTIFY_CALI_FRAME_STATE=15290 CMD_V3_NOTIFY_CALI_FRAME_STATE value
 * @property {number} CMD_NOTIFY_PROGRESS_CAPTURE_CALI_FRAME=15291 CMD_NOTIFY_PROGRESS_CAPTURE_CALI_FRAME value
 * @property {number} CMD_V3_NOTIFY_CALI_FRAME_PROGRESS=15291 CMD_V3_NOTIFY_CALI_FRAME_PROGRESS value
 * @property {number} CMD_NOTIFY_CMOS_TEMPERATURE=15292 CMD_NOTIFY_CMOS_TEMPERATURE value
 * @property {number} CMD_V3_NOTIFY_TEMPERATURE2=15292 CMD_V3_NOTIFY_TEMPERATURE2 value
 * @property {number} CMD_NOTIFY_SKY_TARGET_FINDER_STATE=15296 CMD_NOTIFY_SKY_TARGET_FINDER_STATE value
 * @property {number} CMD_V3_NOTIFY_OBSERVATION_STATE=15296 CMD_V3_NOTIFY_OBSERVATION_STATE value
 * @property {number} CMD_PANORAMA_START_GRID=15500 CMD_PANORAMA_START_GRID value
 * @property {number} CMD_PANORAMA_STOP=15501 CMD_PANORAMA_STOP value
 * @property {number} CMD_PANORAMA_START_EULER_RANGE=15502 CMD_PANORAMA_START_EULER_RANGE value
 * @property {number} CMD_GET_ALL_SHOOTING_SCHEDULE=16102 CMD_GET_ALL_SHOOTING_SCHEDULE value
 * @property {number} CMD_V3_SCHEDULE_GET=16102 CMD_V3_SCHEDULE_GET value
 * @property {number} CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_MODE=16402 CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_MODE value
 * @property {number} CMD_V3_DEVICE_CONFIG_MODE_QUERY=16402 CMD_V3_DEVICE_CONFIG_MODE_QUERY value
 * @property {number} CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_TECH=16403 CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_TECH value
 * @property {number} CMD_V3_DEVICE_CONFIG_SHOOTING_MODE=16403 CMD_V3_DEVICE_CONFIG_SHOOTING_MODE value
 * @property {number} CMD_GLOBAL_TASK_MANAGER_ENTER_CAMERA=16404 CMD_GLOBAL_TASK_MANAGER_ENTER_CAMERA value
 * @property {number} CMD_V3_DEVICE_CONFIG_MODE_SWITCH=16404 CMD_V3_DEVICE_CONFIG_MODE_SWITCH value
 * @property {number} CMD_GLOBAL_TASK_GET_DEVICE_STATE_INFO=16405 CMD_GLOBAL_TASK_GET_DEVICE_STATE_INFO value
 * @property {number} CMD_V3_DEVICE_CONFIG_GET_CONFIG=16405 CMD_V3_DEVICE_CONFIG_GET_CONFIG value
 * @property {number} CMD_PARAM_SET_EXPOSURE=16700 CMD_PARAM_SET_EXPOSURE value
 * @property {number} CMD_V3_CAMERA_PARAMS_SET_PARAM=16700 CMD_V3_CAMERA_PARAMS_SET_PARAM value
 * @property {number} CMD_PARAM_SET_GAIN=16701 CMD_PARAM_SET_GAIN value
 * @property {number} CMD_V3_CAMERA_PARAMS_SET_EXP_GAIN=16701 CMD_V3_CAMERA_PARAMS_SET_EXP_GAIN value
 * @property {number} CMD_PARAM_SET_GENERAL_INT_PARAM=16703 CMD_PARAM_SET_GENERAL_INT_PARAM value
 * @property {number} CMD_V3_CAMERA_PARAMS_ADJUST=16703 CMD_V3_CAMERA_PARAMS_ADJUST value
 * @property {number} CMD_PARAM_SET_AUTO_PARAMS=16706 CMD_PARAM_SET_AUTO_PARAMS value
 * @property {number} CMD_V3_CAMERA_PARAMS_STREAM_CTRL=16706 CMD_V3_CAMERA_PARAMS_STREAM_CTRL value
 */
$root.DwarfCMD = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "NO_CMD")] = 0;
  values[(valuesById[10000] = "CMD_CAMERA_TELE_OPEN_CAMERA")] = 10000;
  values[(valuesById[10001] = "CMD_CAMERA_TELE_CLOSE_CAMERA")] = 10001;
  values[(valuesById[10002] = "CMD_CAMERA_TELE_PHOTOGRAPH")] = 10002;
  values[(valuesById[10003] = "CMD_CAMERA_TELE_BURST")] = 10003;
  values[(valuesById[10004] = "CMD_CAMERA_TELE_STOP_BURST")] = 10004;
  values[(valuesById[10005] = "CMD_CAMERA_TELE_START_RECORD")] = 10005;
  values[(valuesById[10006] = "CMD_CAMERA_TELE_STOP_RECORD")] = 10006;
  values[(valuesById[10007] = "CMD_CAMERA_TELE_SET_EXP_MODE")] = 10007;
  values[(valuesById[10008] = "CMD_CAMERA_TELE_GET_EXP_MODE")] = 10008;
  values[(valuesById[10009] = "CMD_CAMERA_TELE_SET_EXP")] = 10009;
  values[(valuesById[10010] = "CMD_CAMERA_TELE_GET_EXP")] = 10010;
  values[(valuesById[10011] = "CMD_CAMERA_TELE_SET_GAIN_MODE")] = 10011;
  values[(valuesById[10012] = "CMD_CAMERA_TELE_GET_GAIN_MODE")] = 10012;
  values[(valuesById[10013] = "CMD_CAMERA_TELE_SET_GAIN")] = 10013;
  values[(valuesById[10014] = "CMD_CAMERA_TELE_GET_GAIN")] = 10014;
  values[(valuesById[10015] = "CMD_CAMERA_TELE_SET_BRIGHTNESS")] = 10015;
  values[(valuesById[10016] = "CMD_CAMERA_TELE_GET_BRIGHTNESS")] = 10016;
  values[(valuesById[10017] = "CMD_CAMERA_TELE_SET_CONTRAST")] = 10017;
  values[(valuesById[10018] = "CMD_CAMERA_TELE_GET_CONTRAST")] = 10018;
  values[(valuesById[10019] = "CMD_CAMERA_TELE_SET_SATURATION")] = 10019;
  values[(valuesById[10020] = "CMD_CAMERA_TELE_GET_SATURATION")] = 10020;
  values[(valuesById[10021] = "CMD_CAMERA_TELE_SET_HUE")] = 10021;
  values[(valuesById[10022] = "CMD_CAMERA_TELE_GET_HUE")] = 10022;
  values[(valuesById[10023] = "CMD_CAMERA_TELE_SET_SHARPNESS")] = 10023;
  values[(valuesById[10024] = "CMD_CAMERA_TELE_GET_SHARPNESS")] = 10024;
  values[(valuesById[10025] = "CMD_CAMERA_TELE_SET_WB_MODE")] = 10025;
  values[(valuesById[10026] = "CMD_CAMERA_TELE_GET_WB_MODE")] = 10026;
  values[(valuesById[10027] = "CMD_CAMERA_TELE_SET_WB_SCENE")] = 10027;
  values[(valuesById[10028] = "CMD_CAMERA_TELE_GET_WB_SCENE")] = 10028;
  values[(valuesById[10029] = "CMD_CAMERA_TELE_SET_WB_CT")] = 10029;
  values[(valuesById[10030] = "CMD_CAMERA_TELE_GET_WB_CT")] = 10030;
  values[(valuesById[10031] = "CMD_CAMERA_TELE_SET_IRCUT")] = 10031;
  values[(valuesById[10032] = "CMD_CAMERA_TELE_GET_IRCUT")] = 10032;
  values[(valuesById[10033] = "CMD_CAMERA_TELE_START_TIMELAPSE_PHOTO")] = 10033;
  values[(valuesById[10034] = "CMD_CAMERA_TELE_STOP_TIMELAPSE_PHOTO")] = 10034;
  values[(valuesById[10035] = "CMD_CAMERA_TELE_SET_ALL_PARAMS")] = 10035;
  values[(valuesById[10036] = "CMD_CAMERA_TELE_GET_ALL_PARAMS")] = 10036;
  values[(valuesById[10037] = "CMD_CAMERA_TELE_SET_FEATURE_PARAM")] = 10037;
  values[(valuesById[10038] = "CMD_CAMERA_TELE_GET_ALL_FEATURE_PARAMS")] =
    10038;
  values[(valuesById[10039] = "CMD_CAMERA_TELE_GET_SYSTEM_WORKING_STATE")] =
    10039;
  values[(valuesById[10040] = "CMD_CAMERA_TELE_SET_JPG_QUALITY")] = 10040;
  values[(valuesById[10041] = "CMD_CAMERA_TELE_PHOTO_RAW")] = 10041;
  values[(valuesById[10042] = "CMD_CAMERA_TELE_SET_RTSP_BITRATE_TYPE")] = 10042;
  values[(valuesById[10050] = "CMD_CAMERA_TELE_SET_PREVIEW_QUALITY")] = 10050;
  values["CMD_V3_CAMERA_TELE_OPEN_CAMERA"] = 10050;
  values[(valuesById[11000] = "CMD_ASTRO_START_CALIBRATION")] = 11000;
  values[(valuesById[11001] = "CMD_ASTRO_STOP_CALIBRATION")] = 11001;
  values[(valuesById[11002] = "CMD_ASTRO_START_GOTO_DSO")] = 11002;
  values[(valuesById[11003] = "CMD_ASTRO_START_GOTO_SOLAR_SYSTEM")] = 11003;
  values[(valuesById[11004] = "CMD_ASTRO_STOP_GOTO")] = 11004;
  values[(valuesById[11005] = "CMD_ASTRO_START_CAPTURE_RAW_LIVE_STACKING")] =
    11005;
  values[(valuesById[11006] = "CMD_ASTRO_STOP_CAPTURE_RAW_LIVE_STACKING")] =
    11006;
  values[(valuesById[11007] = "CMD_ASTRO_START_CAPTURE_RAW_DARK")] = 11007;
  values[(valuesById[11008] = "CMD_ASTRO_STOP_CAPTURE_RAW_DARK")] = 11008;
  values[(valuesById[11009] = "CMD_ASTRO_CHECK_GOT_DARK")] = 11009;
  values[(valuesById[11010] = "CMD_ASTRO_GO_LIVE")] = 11010;
  values[(valuesById[11011] = "CMD_ASTRO_START_TRACK_SPECIAL_TARGET")] = 11011;
  values[(valuesById[11012] = "CMD_ASTRO_STOP_TRACK_SPECIAL_TARGET")] = 11012;
  values[(valuesById[11013] = "CMD_ASTRO_START_ONE_CLICK_GOTO_DSO")] = 11013;
  values[(valuesById[11014] = "CMD_ASTRO_START_ONE_CLICK_GOTO_SOLAR_SYSTEM")] =
    11014;
  values[(valuesById[11015] = "CMD_ASTRO_STOP_ONE_CLICK_GOTO")] = 11015;
  values[(valuesById[11016] = "CMD_ASTRO_START_WIDE_CAPTURE_LIVE_STACKING")] =
    11016;
  values[(valuesById[11017] = "CMD_ASTRO_STOP_WIDE_CAPTURE_LIVE_STACKING")] =
    11017;
  values[(valuesById[11018] = "CMD_ASTRO_START_EQ_SOLVING")] = 11018;
  values[(valuesById[11019] = "CMD_ASTRO_STOP_EQ_SOLVING")] = 11019;
  values[(valuesById[11020] = "CMD_ASTRO_WIDE_GO_LIVE")] = 11020;
  values[(valuesById[11021] = "CMD_ASTRO_START_CAPTURE_RAW_DARK_WITH_PARAM")] =
    11021;
  values[(valuesById[11022] = "CMD_ASTRO_STOP_CAPTURE_RAW_DARK_WITH_PARAM")] =
    11022;
  values[(valuesById[11023] = "CMD_ASTRO_GET_DARK_FRAME_LIST")] = 11023;
  values[(valuesById[11024] = "CMD_ASTRO_DEL_DARK_FRAME_LIST")] = 11024;
  values[
    (valuesById[11025] = "CMD_ASTRO_START_CAPTURE_WIDE_RAW_DARK_WITH_PARAM")
  ] = 11025;
  values[
    (valuesById[11026] = "CMD_ASTRO_STOP_CAPTURE_WIDE_RAW_DARK_WITH_PARAM")
  ] = 11026;
  values[(valuesById[11027] = "CMD_ASTRO_GET_WIDE_DARK_FRAME_LIST")] = 11027;
  values[(valuesById[11028] = "CMD_ASTRO_DEL_WIDE_DARK_FRAME_LIST")] = 11028;
  values[(valuesById[11033] = "CMD_ASTRO_START_MAKE_FITS_THUMB")] = 11033;
  values["CMD_V3_ASTRO_SAVE_STACKED_IMAGE"] = 11033;
  values[(valuesById[11034] = "CMD_ASTRO_STOP_MAKE_FITS_THUMB")] = 11034;
  values["CMD_V3_ASTRO_LIST_SAVED_IMAGES"] = 11034;
  values[(valuesById[11036] = "CMD_ASTRO_STOP_RESTACKED")] = 11036;
  values["CMD_V3_ASTRO_SAVE_COMPLETE"] = 11036;
  values[(valuesById[11039] = "CMD_ASTRO_GET_ASTRO_SHOOTING_TIME")] = 11039;
  values["CMD_V3_ASTRO_STATUS_POLLING"] = 11039;
  values[(valuesById[11040] = "CMD_ASTRO_GET_QUICK_SET_LIST")] = 11040;
  values["CMD_V3_ASTRO_GET_PARAMS"] = 11040;
  values[(valuesById[11041] = "CMD_ASTRO_SET_QUICK_SET")] = 11041;
  values["CMD_V3_ASTRO_SET_PARAMS"] = 11041;
  values[(valuesById[11043] = "CMD_ASTRO_GET_CALI_FRAME_LIST")] = 11043;
  values["CMD_V3_ASTRO_GET_PRESETS"] = 11043;
  values[(valuesById[11047] = "CMD_ASTRO_START_SKY_TARGET_FINDER")] = 11047;
  values["CMD_V3_ASTRO_SET_LOCATION"] = 11047;
  values[(valuesById[11048] = "CMD_ASTRO_STOP_SKY_TARGET_FINDER")] = 11048;
  values["CMD_V3_ASTRO_CONFIRM"] = 11048;
  values[(valuesById[11050] = "CMD_ASTRO_CONTINUE_SHOOTING")] = 11050;
  values[(valuesById[12000] = "CMD_CAMERA_WIDE_OPEN_CAMERA")] = 12000;
  values[(valuesById[12001] = "CMD_CAMERA_WIDE_CLOSE_CAMERA")] = 12001;
  values[(valuesById[12002] = "CMD_CAMERA_WIDE_SET_EXP_MODE")] = 12002;
  values[(valuesById[12003] = "CMD_CAMERA_WIDE_GET_EXP_MODE")] = 12003;
  values[(valuesById[12004] = "CMD_CAMERA_WIDE_SET_EXP")] = 12004;
  values[(valuesById[12005] = "CMD_CAMERA_WIDE_GET_EXP")] = 12005;
  values[(valuesById[12006] = "CMD_CAMERA_WIDE_SET_GAIN")] = 12006;
  values[(valuesById[12007] = "CMD_CAMERA_WIDE_GET_GAIN")] = 12007;
  values[(valuesById[12008] = "CMD_CAMERA_WIDE_SET_BRIGHTNESS")] = 12008;
  values[(valuesById[12009] = "CMD_CAMERA_WIDE_GET_BRIGHTNESS")] = 12009;
  values[(valuesById[12010] = "CMD_CAMERA_WIDE_SET_CONTRAST")] = 12010;
  values[(valuesById[12011] = "CMD_CAMERA_WIDE_GET_CONTRAST")] = 12011;
  values[(valuesById[12012] = "CMD_CAMERA_WIDE_SET_SATURATION")] = 12012;
  values[(valuesById[12013] = "CMD_CAMERA_WIDE_GET_SATURATION")] = 12013;
  values[(valuesById[12014] = "CMD_CAMERA_WIDE_SET_HUE")] = 12014;
  values[(valuesById[12015] = "CMD_CAMERA_WIDE_GET_HUE")] = 12015;
  values[(valuesById[12016] = "CMD_CAMERA_WIDE_SET_SHARPNESS")] = 12016;
  values[(valuesById[12017] = "CMD_CAMERA_WIDE_GET_SHARPNESS")] = 12017;
  values[(valuesById[12018] = "CMD_CAMERA_WIDE_SET_WB_MODE")] = 12018;
  values[(valuesById[12019] = "CMD_CAMERA_WIDE_GET_WB_MODE")] = 12019;
  values[(valuesById[12020] = "CMD_CAMERA_WIDE_SET_WB_CT")] = 12020;
  values[(valuesById[12021] = "CMD_CAMERA_WIDE_GET_WB_CT")] = 12021;
  values[(valuesById[12022] = "CMD_CAMERA_WIDE_PHOTOGRAPH")] = 12022;
  values[(valuesById[12023] = "CMD_CAMERA_WIDE_BURST")] = 12023;
  values[(valuesById[12024] = "CMD_CAMERA_WIDE_STOP_BURST")] = 12024;
  values[(valuesById[12025] = "CMD_CAMERA_WIDE_START_TIMELAPSE_PHOTO")] = 12025;
  values[(valuesById[12026] = "CMD_CAMERA_WIDE_STOP_TIMELAPSE_PHOTO")] = 12026;
  values[(valuesById[12027] = "CMD_CAMERA_WIDE_GET_ALL_PARAMS")] = 12027;
  values[(valuesById[12028] = "CMD_CAMERA_WIDE_SET_ALL_PARAMS")] = 12028;
  values[(valuesById[12030] = "CMD_CAMERA_WIDE_START_RECORD")] = 12030;
  values[(valuesById[12031] = "CMD_CAMERA_WIDE_STOP_RECORD")] = 12031;
  values[(valuesById[12036] = "CMD_CAMERA_WIDE_SET_PREVIEW_QUALITY")] = 12036;
  values["CMD_V3_CAMERA_WIDE_OPEN_CAMERA"] = 12036;
  values[(valuesById[13000] = "CMD_SYSTEM_SET_TIME")] = 13000;
  values[(valuesById[13001] = "CMD_SYSTEM_SET_TIME_ZONE")] = 13001;
  values[(valuesById[13002] = "CMD_SYSTEM_SET_MTP_MODE")] = 13002;
  values[(valuesById[13003] = "CMD_SYSTEM_SET_CPU_MODE")] = 13003;
  values[(valuesById[13004] = "CMD_SYSTEM_SET_MASTER")] = 13004;
  values["CMD_SYSTEM_SET_MASTERLOCK"] = 13004;
  values[(valuesById[13010] = "CMD_SYSTEM_SET_LOCATION")] = 13010;
  values["CMD_V3_SYSTEM_SET_GPS_LOCATION"] = 13010;
  values[(valuesById[13500] = "CMD_RGB_POWER_OPEN_RGB")] = 13500;
  values[(valuesById[13501] = "CMD_RGB_POWER_CLOSE_RGB")] = 13501;
  values[(valuesById[13502] = "CMD_RGB_POWER_POWER_DOWN")] = 13502;
  values[(valuesById[13503] = "CMD_RGB_POWER_POWERIND_ON")] = 13503;
  values[(valuesById[13504] = "CMD_RGB_POWER_POWERIND_OFF")] = 13504;
  values[(valuesById[13505] = "CMD_RGB_POWER_REBOOT")] = 13505;
  values[(valuesById[14000] = "CMD_STEP_MOTOR_RUN")] = 14000;
  values[(valuesById[14001] = "CMD_STEP_MOTOR_RUN_TO")] = 14001;
  values[(valuesById[14002] = "CMD_STEP_MOTOR_STOP")] = 14002;
  values[(valuesById[14003] = "CMD_STEP_MOTOR_RESET")] = 14003;
  values[(valuesById[14004] = "CMD_STEP_MOTOR_CHANGE_SPEED")] = 14004;
  values[(valuesById[14005] = "CMD_STEP_MOTOR_CHANGE_DIRECTION")] = 14005;
  values[(valuesById[14006] = "CMD_STEP_MOTOR_SERVICE_JOYSTICK")] = 14006;
  values[(valuesById[14007] = "CMD_STEP_MOTOR_SERVICE_JOYSTICK_FIXED_ANGLE")] =
    14007;
  values[(valuesById[14008] = "CMD_STEP_MOTOR_SERVICE_JOYSTICK_STOP")] = 14008;
  values[(valuesById[14009] = "CMD_STEP_MOTOR_SERVICE_DUAL_CAMERA_LINKAGE")] =
    14009;
  values[(valuesById[14010] = "CMD_STEP_MOTOR_RUN_IN_PULSE")] = 14010;
  values[(valuesById[14011] = "CMD_STEP_MOTOR_GET_POSITION")] = 14011;
  values[(valuesById[14800] = "CMD_TRACK_START_TRACK")] = 14800;
  values[(valuesById[14801] = "CMD_TRACK_STOP_TRACK")] = 14801;
  values[(valuesById[14802] = "CMD_SENTRY_MODE_START")] = 14802;
  values[(valuesById[14803] = "CMD_SENTRY_MODE_STOP")] = 14803;
  values[(valuesById[14804] = "CMD_MOT_START")] = 14804;
  values[(valuesById[14805] = "CMD_MOT_TRACK_ONE")] = 14805;
  values[(valuesById[14806] = "CMD_UFOTRACK_MODE_START")] = 14806;
  values[(valuesById[14807] = "CMD_UFOTRACK_MODE_STOP")] = 14807;
  values[(valuesById[14808] = "CMD_MOT_WIDE_TRACK_ONE")] = 14808;
  values[(valuesById[14809] = "CMD_SWITCH_MAIN_PREVIEW")] = 14809;
  values["CMD_WIDE_TELE_TRACK_SWITCH"] = 14809;
  values[(valuesById[14810] = "CMD_UFO_HAND_AOTO_MODE")] = 14810;
  values[(valuesById[15000] = "CMD_FOCUS_AUTO_FOCUS")] = 15000;
  values[(valuesById[15001] = "CMD_FOCUS_MANUAL_SINGLE_STEP_FOCUS")] = 15001;
  values[(valuesById[15002] = "CMD_FOCUS_START_MANUAL_CONTINU_FOCUS")] = 15002;
  values[(valuesById[15003] = "CMD_FOCUS_STOP_MANUAL_CONTINU_FOCUS")] = 15003;
  values[(valuesById[15004] = "CMD_FOCUS_START_ASTRO_AUTO_FOCUS")] = 15004;
  values[(valuesById[15005] = "CMD_FOCUS_STOP_ASTRO_AUTO_FOCUS")] = 15005;
  values[(valuesById[15011] = "CMD_FOCUS_GET_USER_INFINITY_POS")] = 15011;
  values["CMD_V3_FOCUS_INIT"] = 15011;
  values[(valuesById[15200] = "CMD_NOTIFY_TELE_WIDE_PICTURE_MATCHING")] = 15200;
  values["CMD_NOTIFY_TELE_WIDI_PICTURE_MATCHING"] = 15200;
  values[(valuesById[15201] = "CMD_NOTIFY_ELE")] = 15201;
  values[(valuesById[15202] = "CMD_NOTIFY_CHARGE")] = 15202;
  values[(valuesById[15203] = "CMD_NOTIFY_SDCARD_INFO")] = 15203;
  values[(valuesById[15204] = "CMD_NOTIFY_TELE_RECORD_TIME")] = 15204;
  values[(valuesById[15205] = "CMD_NOTIFY_TELE_TIMELAPSE_OUT_TIME")] = 15205;
  values[(valuesById[15206] = "CMD_NOTIFY_STATE_CAPTURE_RAW_DARK")] = 15206;
  values[(valuesById[15207] = "CMD_NOTIFY_PROGRASS_CAPTURE_RAW_DARK")] = 15207;
  values[(valuesById[15208] = "CMD_NOTIFY_STATE_CAPTURE_RAW_LIVE_STACKING")] =
    15208;
  values[
    (valuesById[15209] = "CMD_NOTIFY_PROGRASS_CAPTURE_RAW_LIVE_STACKING")
  ] = 15209;
  values[(valuesById[15210] = "CMD_NOTIFY_STATE_ASTRO_CALIBRATION")] = 15210;
  values[(valuesById[15211] = "CMD_NOTIFY_STATE_ASTRO_GOTO")] = 15211;
  values[(valuesById[15212] = "CMD_NOTIFY_STATE_ASTRO_TRACKING")] = 15212;
  values[(valuesById[15213] = "CMD_NOTIFY_TELE_SET_PARAM")] = 15213;
  values[(valuesById[15214] = "CMD_NOTIFY_WIDE_SET_PARAM")] = 15214;
  values[(valuesById[15215] = "CMD_NOTIFY_TELE_FUNCTION_STATE")] = 15215;
  values[(valuesById[15216] = "CMD_NOTIFY_WIDE_FUNCTION_STATE")] = 15216;
  values[(valuesById[15217] = "CMD_NOTIFY_SET_FEATURE_PARAM")] = 15217;
  values[(valuesById[15218] = "CMD_NOTIFY_TELE_BURST_PROGRESS")] = 15218;
  values[(valuesById[15219] = "CMD_NOTIFY_PANORAMA_PROGRESS")] = 15219;
  values[(valuesById[15220] = "CMD_NOTIFY_WIDE_BURST_PROGRESS")] = 15220;
  values[(valuesById[15221] = "CMD_NOTIFY_RGB_STATE")] = 15221;
  values[(valuesById[15222] = "CMD_NOTIFY_POWER_IND_STATE")] = 15222;
  values[(valuesById[15223] = "CMD_NOTIFY_WS_HOST_SLAVE_MODE")] = 15223;
  values[(valuesById[15224] = "CMD_NOTIFY_MTP_STATE")] = 15224;
  values[(valuesById[15225] = "CMD_NOTIFY_TRACK_RESULT")] = 15225;
  values[(valuesById[15226] = "CMD_NOTIFY_WIDE_TIMELAPSE_OUT_TIME")] = 15226;
  values[(valuesById[15227] = "CMD_NOTIFY_CPU_MODE")] = 15227;
  values[(valuesById[15228] = "CMD_NOTIFY_STATE_ASTRO_TRACKING_SPECIAL")] =
    15228;
  values[(valuesById[15229] = "CMD_NOTIFY_POWER_OFF")] = 15229;
  values[(valuesById[15230] = "CMD_NOTIFY_ALBUM_UPDATE")] = 15230;
  values[(valuesById[15231] = "CMD_NOTIFY_SENTRY_MODE_STATE")] = 15231;
  values[(valuesById[15232] = "CMD_NOTIFY_SENTRY_MODE_TRACK_RESULT")] = 15232;
  values[(valuesById[15233] = "CMD_NOTIFY_STATE_ASTRO_ONE_CLICK_GOTO")] = 15233;
  values[(valuesById[15234] = "CMD_NOTIFY_STREAM_TYPE")] = 15234;
  values[(valuesById[15235] = "CMD_NOTIFY_WIDE_RECORD_TIME")] = 15235;
  values[
    (valuesById[15236] = "CMD_NOTIFY_STATE_WIDE_CAPTURE_RAW_LIVE_STACKING")
  ] = 15236;
  values[
    (valuesById[15237] = "CMD_NOTIFY_PROGRASS_WIDE_CAPTURE_RAW_LIVE_STACKING")
  ] = 15237;
  values[(valuesById[15238] = "CMD_NOTIFY_MULTI_TRACK_RESULT")] = 15238;
  values[(valuesById[15239] = "CMD_NOTIFY_EQ_SOLVING_STATE")] = 15239;
  values[(valuesById[15240] = "CMD_NOTIFY_UFO_MODE_STATE")] = 15240;
  values[(valuesById[15241] = "CMD_NOTIFY_TELE_LONG_EXP_PROGRESS")] = 15241;
  values[(valuesById[15242] = "CMD_NOTIFY_WIDE_LONG_EXP_PROGRESS")] = 15242;
  values[(valuesById[15243] = "CMD_NOTIFY_TEMPERATURE")] = 15243;
  values[(valuesById[15244] = "CMD_NOTIFY_PANORAMA_UPLOAD_COMPRESS_PROGRESS")] =
    15244;
  values[(valuesById[15245] = "CMD_NOTIFY_PANORAMA_UPLOAD_UPLOAD_PROGRESS")] =
    15245;
  values["CMD_NOTIFY_PANORAMA_UPLOAD_COMPLETE"] = 15245;
  values[(valuesById[15247] = "CMD_NOTIFY_STATE_CAPTURE_WIDE_RAW_DARK")] =
    15247;
  values[
    (valuesById[15248] = "CMD_NOTIFY_SHOOTING_SCHEDULE_RESULT_AND_STATE")
  ] = 15248;
  values[(valuesById[15249] = "CMD_NOTIFY_SHOOTING_TASK_STATE")] = 15249;
  values[(valuesById[15250] = "CMD_NOTIFY_SKY_SEACHER_STATE")] = 15250;
  values[(valuesById[15251] = "CMD_NOTIFY_WIDE_MULTI_TRACK_RESULT")] = 15251;
  values[(valuesById[15252] = "CMD_NOTIFY_WIDE_TRACK_RESULT")] = 15252;
  values[(valuesById[15256] = "CMD_NOTIFY_CALIBRATION_RESULT")] = 15256;
  values[(valuesById[15257] = "CMD_NOTIFY_FOCUS_POSITION")] = 15257;
  values["CMD_NOTIFY_FOCUS"] = 15257;
  values[(valuesById[15255] = "CMD_NOTIFY_WAIT_SHOOTING_PROGRESS")] = 15255;
  values["CMD_V3_NOTIFY_EXPOSURE_PROGRESS"] = 15255;
  values[(valuesById[15261] = "CMD_NOTIFY_EXCLUSIVE_SYSTEM_IO_TASK_STATE")] =
    15261;
  values["CMD_V3_NOTIFY_DEVICE_STATE"] = 15261;
  values[(valuesById[15264] = "CMD_NOTIFY_GENERAL_INT_PARAM")] = 15264;
  values["CMD_V3_NOTIFY_CAMERA_PARAM_STATE"] = 15264;
  values[(valuesById[15267] = "CMD_NOTIFY_SWITCH_SHOOTING_MODE")] = 15267;
  values["CMD_V3_NOTIFY_MODE_CHANGE"] = 15267;
  values[(valuesById[15270] = "CMD_NOTIFY_WB")] = 15270;
  values["CMD_V3_NOTIFY_STACKING_DATA"] = 15270;
  values[(valuesById[15273] = "CMD_NOTIFY_PHOTO_STATE")] = 15273;
  values["CMD_V3_NOTIFY_PHOTO_STATE"] = 15273;
  values[(valuesById[15274] = "CMD_NOTIFY_BURST_STATE")] = 15274;
  values["CMD_V3_NOTIFY_BURST_STATE"] = 15274;
  values[(valuesById[15275] = "CMD_NOTIFY_RECORD_STATE")] = 15275;
  values["CMD_V3_NOTIFY_VIDEO_STATE"] = 15275;
  values[(valuesById[15276] = "CMD_NOTIFY_TIMELAPSE_STATE")] = 15276;
  values["CMD_V3_NOTIFY_TIMELAPSE_STATE"] = 15276;
  values[(valuesById[15278] = "CMD_NOTIFY_ASTRO_AUTO_FOCUS_STATE")] = 15278;
  values["CMD_V3_NOTIFY_AUTOFOCUS_STATE"] = 15278;
  values[(valuesById[15280] = "CMD_NOTIFY_ASTRO_AUTO_FOCUS_FAST_STATE")] =
    15280;
  values["CMD_V3_NOTIFY_AUTOFOCUS_STATE_ALT"] = 15280;
  values[(valuesById[15285] = "CMD_NOTIFY_BURST_PROGRESS")] = 15285;
  values["CMD_V3_NOTIFY_PHOTO_BURST_PROGRESS"] = 15285;
  values[(valuesById[15286] = "CMD_NOTIFY_RECORD_TIME")] = 15286;
  values["CMD_V3_NOTIFY_VIDEO_PROGRESS"] = 15286;
  values[(valuesById[15287] = "CMD_NOTIFY_TIMELAPSE_OUT_TIME")] = 15287;
  values["CMD_V3_NOTIFY_TIMELAPSE_PROGRESS"] = 15287;
  values[(valuesById[15288] = "CMD_NOTIFY_LONG_EXP_PROGRESS")] = 15288;
  values[(valuesById[15290] = "CMD_NOTIFY_STATE_CAPTURE_CALI_FRAME")] = 15290;
  values["CMD_V3_NOTIFY_CALI_FRAME_STATE"] = 15290;
  values[(valuesById[15291] = "CMD_NOTIFY_PROGRESS_CAPTURE_CALI_FRAME")] =
    15291;
  values["CMD_V3_NOTIFY_CALI_FRAME_PROGRESS"] = 15291;
  values[(valuesById[15292] = "CMD_NOTIFY_CMOS_TEMPERATURE")] = 15292;
  values["CMD_V3_NOTIFY_TEMPERATURE2"] = 15292;
  values[(valuesById[15296] = "CMD_NOTIFY_SKY_TARGET_FINDER_STATE")] = 15296;
  values["CMD_V3_NOTIFY_OBSERVATION_STATE"] = 15296;
  values[(valuesById[15500] = "CMD_PANORAMA_START_GRID")] = 15500;
  values[(valuesById[15501] = "CMD_PANORAMA_STOP")] = 15501;
  values[(valuesById[15502] = "CMD_PANORAMA_START_EULER_RANGE")] = 15502;
  values[(valuesById[16102] = "CMD_GET_ALL_SHOOTING_SCHEDULE")] = 16102;
  values["CMD_V3_SCHEDULE_GET"] = 16102;
  values[(valuesById[16402] = "CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_MODE")] =
    16402;
  values["CMD_V3_DEVICE_CONFIG_MODE_QUERY"] = 16402;
  values[(valuesById[16403] = "CMD_GLOBAL_TASK_MANAGER_SWITCH_SHOOTING_TECH")] =
    16403;
  values["CMD_V3_DEVICE_CONFIG_SHOOTING_MODE"] = 16403;
  values[(valuesById[16404] = "CMD_GLOBAL_TASK_MANAGER_ENTER_CAMERA")] = 16404;
  values["CMD_V3_DEVICE_CONFIG_MODE_SWITCH"] = 16404;
  values[(valuesById[16405] = "CMD_GLOBAL_TASK_GET_DEVICE_STATE_INFO")] = 16405;
  values["CMD_V3_DEVICE_CONFIG_GET_CONFIG"] = 16405;
  values[(valuesById[16700] = "CMD_PARAM_SET_EXPOSURE")] = 16700;
  values["CMD_V3_CAMERA_PARAMS_SET_PARAM"] = 16700;
  values[(valuesById[16701] = "CMD_PARAM_SET_GAIN")] = 16701;
  values["CMD_V3_CAMERA_PARAMS_SET_EXP_GAIN"] = 16701;
  values[(valuesById[16703] = "CMD_PARAM_SET_GENERAL_INT_PARAM")] = 16703;
  values["CMD_V3_CAMERA_PARAMS_ADJUST"] = 16703;
  values[(valuesById[16706] = "CMD_PARAM_SET_AUTO_PARAMS")] = 16706;
  values["CMD_V3_CAMERA_PARAMS_STREAM_CTRL"] = 16706;
  return values;
})();

/**
 * DwarfErrorCode enum.
 * @exports DwarfErrorCode
 * @enum {number}
 * @property {number} OK=0 OK value
 * @property {number} WS_OK=0 WS_OK value
 * @property {number} WS_PARSE_PROTOBUF_ERROR=-1 WS_PARSE_PROTOBUF_ERROR value
 * @property {number} WS_SDCARD_NOT_EXIST=-2 WS_SDCARD_NOT_EXIST value
 * @property {number} WS_INVAID_PARAM=-3 WS_INVAID_PARAM value
 * @property {number} WS_SDCARD_WRITE_ERROR=-4 WS_SDCARD_WRITE_ERROR value
 * @property {number} WS_DEVICE_NOT_ACTIVATED=-5 WS_DEVICE_NOT_ACTIVATED value
 * @property {number} WS_SDCARD_FULL_ERROR=-6 WS_SDCARD_FULL_ERROR value
 * @property {number} CODE_CAMERA_TELE_OPENED=-10500 CODE_CAMERA_TELE_OPENED value
 * @property {number} CODE_CAMERA_TELE_CLOSED=-10501 CODE_CAMERA_TELE_CLOSED value
 * @property {number} CODE_CAMERA_TELE_ISP_SET_FAILED=-10502 CODE_CAMERA_TELE_ISP_SET_FAILED value
 * @property {number} CODE_CAMERA_TELE_OPEN_FAILED=-10503 CODE_CAMERA_TELE_OPEN_FAILED value
 * @property {number} CODE_CAMERA_TELE_START_RECORD_FAILED=-10504 CODE_CAMERA_TELE_START_RECORD_FAILED value
 * @property {number} CODE_CAMERA_TELE_STOP_RECORD_FAILED=-10505 CODE_CAMERA_TELE_STOP_RECORD_FAILED value
 * @property {number} CODE_CAMERA_TELE_CAPTURE_RAW_FAILED=-10506 CODE_CAMERA_TELE_CAPTURE_RAW_FAILED value
 * @property {number} CODE_CAMERA_TELE_WORKING_BUSY=-10507 CODE_CAMERA_TELE_WORKING_BUSY value
 * @property {number} CODE_CAMERA_TELE_GET_IMAGE_FAILED=-10508 CODE_CAMERA_TELE_GET_IMAGE_FAILED value
 * @property {number} CODE_CAMERA_TELE_RUNNING_PHOTO=-10509 CODE_CAMERA_TELE_RUNNING_PHOTO value
 * @property {number} CODE_CAMERA_TELE_RUNNING_RECORD=-10510 CODE_CAMERA_TELE_RUNNING_RECORD value
 * @property {number} CODE_CAMERA_TELE_RUNNING_PANORAMA=-10511 CODE_CAMERA_TELE_RUNNING_PANORAMA value
 * @property {number} CODE_CAMERA_TELE_RUNNING_TIMELAPSE=-10512 CODE_CAMERA_TELE_RUNNING_TIMELAPSE value
 * @property {number} CODE_CAMERA_TELE_RUNNING_CAPTURE_DARK=-10513 CODE_CAMERA_TELE_RUNNING_CAPTURE_DARK value
 * @property {number} CODE_CAMERA_TELE_RUNNING_CAPTURE_LIVE_STACKING=-10514 CODE_CAMERA_TELE_RUNNING_CAPTURE_LIVE_STACKING value
 * @property {number} CODE_CAMERA_TELE_EXP_TOO_LONG=-10515 CODE_CAMERA_TELE_EXP_TOO_LONG value
 * @property {number} CODE_CAMERA_TELE_SWITCH_WORK_MODE_FAILED=-10516 CODE_CAMERA_TELE_SWITCH_WORK_MODE_FAILED value
 * @property {number} CODE_CAMERA_TELE_RUNNING_TRACK=-10517 CODE_CAMERA_TELE_RUNNING_TRACK value
 * @property {number} CODE_CAMERA_TELE_RECORD_FILE_ERROR=-10518 CODE_CAMERA_TELE_RECORD_FILE_ERROR value
 * @property {number} CODE_ASTRO_PLATE_SOLVING_FAILED=-11500 CODE_ASTRO_PLATE_SOLVING_FAILED value
 * @property {number} CODE_ASTRO_FUNCTION_BUSY=-11501 CODE_ASTRO_FUNCTION_BUSY value
 * @property {number} CODE_ASTRO_DARK_GAIN_OUT_OF_RANGE=-11502 CODE_ASTRO_DARK_GAIN_OUT_OF_RANGE value
 * @property {number} CODE_ASTRO_DARK_NOT_FOUND=-11503 CODE_ASTRO_DARK_NOT_FOUND value
 * @property {number} CODE_ASTRO_CALIBRATION_FAILED=-11504 CODE_ASTRO_CALIBRATION_FAILED value
 * @property {number} CODE_ASTRO_GOTO_FAILED=-11505 CODE_ASTRO_GOTO_FAILED value
 * @property {number} CODE_ASTRO_DARK_RUNNING=-11506 CODE_ASTRO_DARK_RUNNING value
 * @property {number} CODE_ASTRO_CALIBRATION_RUNNING=-11507 CODE_ASTRO_CALIBRATION_RUNNING value
 * @property {number} CODE_ASTRO_GOTO_RUNNING=-11508 CODE_ASTRO_GOTO_RUNNING value
 * @property {number} CODE_ASTRO_LIVE_STACKING_RUNNING=-11509 CODE_ASTRO_LIVE_STACKING_RUNNING value
 * @property {number} CODE_ASTRO_RESET_PITCH_MOTOR_FAILED=-11510 CODE_ASTRO_RESET_PITCH_MOTOR_FAILED value
 * @property {number} CODE_ASTRO_NEED_CALIBRATION=-11511 CODE_ASTRO_NEED_CALIBRATION value
 * @property {number} CODE_ASTRO_GOTO_READ_MOTOR_POSITION_AND_PLATE_SOLVING_FAILED=-11512 CODE_ASTRO_GOTO_READ_MOTOR_POSITION_AND_PLATE_SOLVING_FAILED value
 * @property {number} CODE_ASTRO_NEED_GOTO=-11513 CODE_ASTRO_NEED_GOTO value
 * @property {number} CODE_ASTRO_NEED_ADJUST_SHOOT_PARAM=-11514 CODE_ASTRO_NEED_ADJUST_SHOOT_PARAM value
 * @property {number} CODE_ASTRO_CALIBRATION_PLATE_SOLVING_FAILED_TOO_MUCH=-11515 CODE_ASTRO_CALIBRATION_PLATE_SOLVING_FAILED_TOO_MUCH value
 * @property {number} CODE_ASTRO_EQ_SOLVING_FAILED=-11516 CODE_ASTRO_EQ_SOLVING_FAILED value
 * @property {number} CODE_ASTRO_SKY_SEARCH_FAILED=-11517 CODE_ASTRO_SKY_SEARCH_FAILED value
 * @property {number} CODE_ASTRO_NEED_GOTO_DSO=-11518 CODE_ASTRO_NEED_GOTO_DSO value
 * @property {number} CODE_ASTRO_RESTACK_CAMERA_MISMATCH=-11519 CODE_ASTRO_RESTACK_CAMERA_MISMATCH value
 * @property {number} CODE_ASTRO_RESTACK_BINNING_MISMATCH=-11520 CODE_ASTRO_RESTACK_BINNING_MISMATCH value
 * @property {number} CODE_ASTRO_RESTACK_FILTER_MISMATCH=-11521 CODE_ASTRO_RESTACK_FILTER_MISMATCH value
 * @property {number} CODE_ASTRO_RESTACK_TARGET_MISMATCH=-11522 CODE_ASTRO_RESTACK_TARGET_MISMATCH value
 * @property {number} CODE_ASTRO_RESTACK_DARKFRAME_MISMATCH=-11523 CODE_ASTRO_RESTACK_DARKFRAME_MISMATCH value
 * @property {number} CODE_ASTRO_RESTACK_FAILED=-11524 CODE_ASTRO_RESTACK_FAILED value
 * @property {number} CODE_ASTRO_RESTACK_INVALID_DATA=-11525 CODE_ASTRO_RESTACK_INVALID_DATA value
 * @property {number} CODE_ASTRO_OVEREXPOSURE_WARNING=-11526 CODE_ASTRO_OVEREXPOSURE_WARNING value
 * @property {number} CODE_ASTRO_EXP_TOO_LONG=-11527 CODE_ASTRO_EXP_TOO_LONG value
 * @property {number} CODE_ASTRO_NEED_EQ=-11528 CODE_ASTRO_NEED_EQ value
 * @property {number} CODE_ASTRO_STAR_TOO_FEW=-11529 CODE_ASTRO_STAR_TOO_FEW value
 * @property {number} CODE_ASTRO_DARK_TEMP_MISMATCH=-11530 CODE_ASTRO_DARK_TEMP_MISMATCH value
 * @property {number} CODE_ASTRO_SUN_MOON_NOT_FOUND=-11531 CODE_ASTRO_SUN_MOON_NOT_FOUND value
 * @property {number} CODE_ASTRO_GUIDING_FAILED_LOWER_EXPOSURE=-11532 CODE_ASTRO_GUIDING_FAILED_LOWER_EXPOSURE value
 * @property {number} CODE_ASTRO_GUIDING_FAILED_TARGET_BLOCKED=-11533 CODE_ASTRO_GUIDING_FAILED_TARGET_BLOCKED value
 * @property {number} CODE_CAMERA_WIDE_OPENED=-12500 CODE_CAMERA_WIDE_OPENED value
 * @property {number} CODE_CAMERA_WIDE_CLOSED=-12501 CODE_CAMERA_WIDE_CLOSED value
 * @property {number} CODE_CAMERA_WIDE_CANNOT_FOUND=-12502 CODE_CAMERA_WIDE_CANNOT_FOUND value
 * @property {number} CODE_CAMERA_WIDE_OPEN_FAILED=-12503 CODE_CAMERA_WIDE_OPEN_FAILED value
 * @property {number} CODE_CAMERA_WIDE_CLOSE_FAILED=-12504 CODE_CAMERA_WIDE_CLOSE_FAILED value
 * @property {number} CODE_CAMERA_WIDE_SET_ISP_FAILED=-12505 CODE_CAMERA_WIDE_SET_ISP_FAILED value
 * @property {number} CODE_CAMERA_WIDE_PHOTOGRAPHING=-12506 CODE_CAMERA_WIDE_PHOTOGRAPHING value
 * @property {number} CODE_CAMERA_WIDE_TIMELAPSE_RECORDING=-12507 CODE_CAMERA_WIDE_TIMELAPSE_RECORDING value
 * @property {number} CODE_CAMERA_WIDE_EXP_TOO_LONG=-12508 CODE_CAMERA_WIDE_EXP_TOO_LONG value
 * @property {number} CODE_CAMERA_WIDE_RECORD_FILE_ERROR=-12509 CODE_CAMERA_WIDE_RECORD_FILE_ERROR value
 * @property {number} CODE_SYSTEM_SET_TIME_FAILED=-13300 CODE_SYSTEM_SET_TIME_FAILED value
 * @property {number} CODE_SYSTEM_SET_TIMEZONE_FAILED=-13301 CODE_SYSTEM_SET_TIMEZONE_FAILED value
 * @property {number} CODE_RGB_POWER_UART_INIT_FAILED=-13800 CODE_RGB_POWER_UART_INIT_FAILED value
 * @property {number} CODE_RGB_POWER_UART_SEND_FAILED=-13801 CODE_RGB_POWER_UART_SEND_FAILED value
 * @property {number} CODE_STEP_MOTOR_IS_RUNNING=-14500 CODE_STEP_MOTOR_IS_RUNNING value
 * @property {number} CODE_STEP_MOTOR_IS_STOPPED=-14501 CODE_STEP_MOTOR_IS_STOPPED value
 * @property {number} CODE_STEP_MOTOR_PARALLEL_IN=-14502 CODE_STEP_MOTOR_PARALLEL_IN value
 * @property {number} CODE_STEP_MOTOR_PARALLEL_END=-14503 CODE_STEP_MOTOR_PARALLEL_END value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_ID=-14504 CODE_STEP_MOTOR_INVALID_PARAMETER_ID value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_ANGLE=-14505 CODE_STEP_MOTOR_INVALID_PARAMETER_ANGLE value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED=-14506 CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED_RAMPING=-14507 CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED_RAMPING value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_RESOLUTION=-14508 CODE_STEP_MOTOR_INVALID_PARAMETER_RESOLUTION value
 * @property {number} CODE_STEP_MOTOR_INVALID_PARAMETER_POSITION=-14509 CODE_STEP_MOTOR_INVALID_PARAMETER_POSITION value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_GET_LIMIT_RETURN=-14510 CODE_STEP_MOTOR_OVERTIME_GET_LIMIT_RETURN value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_GET_RESET_RETURN=-14511 CODE_STEP_MOTOR_OVERTIME_GET_RESET_RETURN value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_GET_ABSOLUTE_POSITION_RETURN=-14512 CODE_STEP_MOTOR_OVERTIME_GET_ABSOLUTE_POSITION_RETURN value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_GET_RELATIVE_POSITION_RETURN=-14513 CODE_STEP_MOTOR_OVERTIME_GET_RELATIVE_POSITION_RETURN value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_WAIT_TO_STOP=-14514 CODE_STEP_MOTOR_OVERTIME_WAIT_TO_STOP value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_WAIT_TO_RUN=-14515 CODE_STEP_MOTOR_OVERTIME_WAIT_TO_RUN value
 * @property {number} CODE_STEP_MOTOR_LIMIT_SPEED_TO_MAX=-14516 CODE_STEP_MOTOR_LIMIT_SPEED_TO_MAX value
 * @property {number} CODE_STEP_MOTOR_LIMIT_SPEED_TO_MIN=-14517 CODE_STEP_MOTOR_LIMIT_SPEED_TO_MIN value
 * @property {number} CODE_STEP_MOTOR_LIMIT_POSITION_WARNING=-14518 CODE_STEP_MOTOR_LIMIT_POSITION_WARNING value
 * @property {number} CODE_STEP_MOTOR_LIMIT_POSITION_HIT=-14519 CODE_STEP_MOTOR_LIMIT_POSITION_HIT value
 * @property {number} CODE_STEP_MOTOR_NEED_RESET=-14520 CODE_STEP_MOTOR_NEED_RESET value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_GET_PE_SWITCH_RETURN=-14521 CODE_STEP_MOTOR_OVERTIME_GET_PE_SWITCH_RETURN value
 * @property {number} CODE_STEP_MOTOR_OVERTIME_TO_RESET=-14522 CODE_STEP_MOTOR_OVERTIME_TO_RESET value
 * @property {number} CODE_STEP_MOTOR_ROLL_LIMIT_ANGLE_WARNING=-14523 CODE_STEP_MOTOR_ROLL_LIMIT_ANGLE_WARNING value
 * @property {number} CODE_TRACK_TRACKER_INITING=-14900 CODE_TRACK_TRACKER_INITING value
 * @property {number} CODE_TRACK_TRACKER_FAILED=-14901 CODE_TRACK_TRACKER_FAILED value
 * @property {number} CODE_TRACK_SENTRY_MODE_INITING=-14902 CODE_TRACK_SENTRY_MODE_INITING value
 * @property {number} CODE_TRACK_SENTRY_MODE_FAILED=-14903 CODE_TRACK_SENTRY_MODE_FAILED value
 * @property {number} CODE_UFOTRACK_MODE_INITING=-14904 CODE_UFOTRACK_MODE_INITING value
 * @property {number} CODE_UFOTRACK_MODE_FAILED=-14905 CODE_UFOTRACK_MODE_FAILED value
 * @property {number} CODE_UFO_DAY_AUTO_MODE=-14906 CODE_UFO_DAY_AUTO_MODE value
 * @property {number} CODE_FOCUS_ASTRO_AUTO_FOCUS_SLOW_ERROR=-15100 CODE_FOCUS_ASTRO_AUTO_FOCUS_SLOW_ERROR value
 * @property {number} CODE_FOCUS_ASTRO_AUTO_FOCUS_FAST_ERROR=-15101 CODE_FOCUS_ASTRO_AUTO_FOCUS_FAST_ERROR value
 * @property {number} CODE_FOCUS_EXP_TOO_LONG=-15106 CODE_FOCUS_EXP_TOO_LONG value
 * @property {number} CODE_FOCUS_INFINITY_POS_ERROR=-15107 CODE_FOCUS_INFINITY_POS_ERROR value
 * @property {number} CODE_FOCUS_GET_NOW_POS_FAILED=-15108 CODE_FOCUS_GET_NOW_POS_FAILED value
 * @property {number} CODE_PANORAMA_PHOTO_FAILED=-15600 CODE_PANORAMA_PHOTO_FAILED value
 * @property {number} CODE_PANORAMA_MOTOR_RESET_FAILED=-15601 CODE_PANORAMA_MOTOR_RESET_FAILED value
 * @property {number} CODE_PANORAMA_UPLOAD_USER_STOP=-15602 CODE_PANORAMA_UPLOAD_USER_STOP value
 * @property {number} CODE_PANORAMA_UPLOAD_FILE_CHECK_FAILED=-15603 CODE_PANORAMA_UPLOAD_FILE_CHECK_FAILED value
 * @property {number} CODE_PANORAMA_UPLOAD_COMPRESS_FAILED=-15604 CODE_PANORAMA_UPLOAD_COMPRESS_FAILED value
 * @property {number} CODE_PANORAMA_UPLOAD_UPLOAD_FAILED=-15605 CODE_PANORAMA_UPLOAD_UPLOAD_FAILED value
 * @property {number} CODE_PANORAMA_UPLOAD_NOT_EXIST=-15606 CODE_PANORAMA_UPLOAD_NOT_EXIST value
 * @property {number} CODE_PANORAMA_UPLOAD_IS_RUNNING=-15607 CODE_PANORAMA_UPLOAD_IS_RUNNING value
 * @property {number} CODE_PANORAMA_UPLOAD_CAMERA_BUSY=-15608 CODE_PANORAMA_UPLOAD_CAMERA_BUSY value
 * @property {number} CODE_PANORAMA_UPLOAD_NOT_IN_STA=-15609 CODE_PANORAMA_UPLOAD_NOT_IN_STA value
 * @property {number} CODE_PANORAMA_COMPRESSION_IS_RUNNING=-15612 CODE_PANORAMA_COMPRESSION_IS_RUNNING value
 * @property {number} CODE_PANORAMA_COMPOSE_IS_IDEL=-15614 CODE_PANORAMA_COMPOSE_IS_IDEL value
 * @property {number} CODE_PANORAMA_COMPOSE_IS_RUNNING=-15615 CODE_PANORAMA_COMPOSE_IS_RUNNING value
 * @property {number} CODE_SHOOTING_SCHEDULE_DEVICE_ID_NOT_MATCH=-16300 CODE_SHOOTING_SCHEDULE_DEVICE_ID_NOT_MATCH value
 * @property {number} CODE_SHOOTING_SCHEDULE_INVALID_SHOOTING_DURATION=-16301 CODE_SHOOTING_SCHEDULE_INVALID_SHOOTING_DURATION value
 * @property {number} CODE_SHOOTING_SCHEDULE_TIME_CONFLICT=-16302 CODE_SHOOTING_SCHEDULE_TIME_CONFLICT value
 * @property {number} CODE_SHOOTING_SCHEDULE_INVALID_TASK_DURATION=-16303 CODE_SHOOTING_SCHEDULE_INVALID_TASK_DURATION value
 * @property {number} CODE_SHOOTING_SCHEDULE_DATABASE_OPERATION_FAILED=-16305 CODE_SHOOTING_SCHEDULE_DATABASE_OPERATION_FAILED value
 * @property {number} CODE_SHOOTING_SCHEDULE_PASSWORD_ERROR=-16306 CODE_SHOOTING_SCHEDULE_PASSWORD_ERROR value
 * @property {number} CODE_SHOOTING_SCHEDULE_SHOOTING=-16307 CODE_SHOOTING_SCHEDULE_SHOOTING value
 * @property {number} CODE_SHOOTING_SCHEDULE_START_TIME_TOO_FAR=-16308 CODE_SHOOTING_SCHEDULE_START_TIME_TOO_FAR value
 * @property {number} CODE_SHOOTING_SCHEDULE_DEVICE_BUSY=-16309 CODE_SHOOTING_SCHEDULE_DEVICE_BUSY value
 * @property {number} CODE_SHOOTING_SCHEDULE_INTERRUPTED=-16310 CODE_SHOOTING_SCHEDULE_INTERRUPTED value
 * @property {number} CODE_SHOOTING_SCHEDULE_NOT_SYNCED=-16311 CODE_SHOOTING_SCHEDULE_NOT_SYNCED value
 * @property {number} CODE_SHOOTING_SCHEDULE_TASK_TIME_TOO_SHORT=-16312 CODE_SHOOTING_SCHEDULE_TASK_TIME_TOO_SHORT value
 * @property {number} CODE_SHOOTING_SCHEDULE_TASK_EXPIRED=-16313 CODE_SHOOTING_SCHEDULE_TASK_EXPIRED value
 * @property {number} CODE_GLOBAL_TASK_MANAGER_BUSY=-16600 CODE_GLOBAL_TASK_MANAGER_BUSY value
 */
$root.DwarfErrorCode = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "OK")] = 0;
  values["WS_OK"] = 0;
  values[(valuesById[-1] = "WS_PARSE_PROTOBUF_ERROR")] = -1;
  values[(valuesById[-2] = "WS_SDCARD_NOT_EXIST")] = -2;
  values[(valuesById[-3] = "WS_INVAID_PARAM")] = -3;
  values[(valuesById[-4] = "WS_SDCARD_WRITE_ERROR")] = -4;
  values[(valuesById[-5] = "WS_DEVICE_NOT_ACTIVATED")] = -5;
  values[(valuesById[-6] = "WS_SDCARD_FULL_ERROR")] = -6;
  values[(valuesById[-10500] = "CODE_CAMERA_TELE_OPENED")] = -10500;
  values[(valuesById[-10501] = "CODE_CAMERA_TELE_CLOSED")] = -10501;
  values[(valuesById[-10502] = "CODE_CAMERA_TELE_ISP_SET_FAILED")] = -10502;
  values[(valuesById[-10503] = "CODE_CAMERA_TELE_OPEN_FAILED")] = -10503;
  values[(valuesById[-10504] = "CODE_CAMERA_TELE_START_RECORD_FAILED")] =
    -10504;
  values[(valuesById[-10505] = "CODE_CAMERA_TELE_STOP_RECORD_FAILED")] = -10505;
  values[(valuesById[-10506] = "CODE_CAMERA_TELE_CAPTURE_RAW_FAILED")] = -10506;
  values[(valuesById[-10507] = "CODE_CAMERA_TELE_WORKING_BUSY")] = -10507;
  values[(valuesById[-10508] = "CODE_CAMERA_TELE_GET_IMAGE_FAILED")] = -10508;
  values[(valuesById[-10509] = "CODE_CAMERA_TELE_RUNNING_PHOTO")] = -10509;
  values[(valuesById[-10510] = "CODE_CAMERA_TELE_RUNNING_RECORD")] = -10510;
  values[(valuesById[-10511] = "CODE_CAMERA_TELE_RUNNING_PANORAMA")] = -10511;
  values[(valuesById[-10512] = "CODE_CAMERA_TELE_RUNNING_TIMELAPSE")] = -10512;
  values[(valuesById[-10513] = "CODE_CAMERA_TELE_RUNNING_CAPTURE_DARK")] =
    -10513;
  values[
    (valuesById[-10514] = "CODE_CAMERA_TELE_RUNNING_CAPTURE_LIVE_STACKING")
  ] = -10514;
  values[(valuesById[-10515] = "CODE_CAMERA_TELE_EXP_TOO_LONG")] = -10515;
  values[(valuesById[-10516] = "CODE_CAMERA_TELE_SWITCH_WORK_MODE_FAILED")] =
    -10516;
  values[(valuesById[-10517] = "CODE_CAMERA_TELE_RUNNING_TRACK")] = -10517;
  values[(valuesById[-10518] = "CODE_CAMERA_TELE_RECORD_FILE_ERROR")] = -10518;
  values[(valuesById[-11500] = "CODE_ASTRO_PLATE_SOLVING_FAILED")] = -11500;
  values[(valuesById[-11501] = "CODE_ASTRO_FUNCTION_BUSY")] = -11501;
  values[(valuesById[-11502] = "CODE_ASTRO_DARK_GAIN_OUT_OF_RANGE")] = -11502;
  values[(valuesById[-11503] = "CODE_ASTRO_DARK_NOT_FOUND")] = -11503;
  values[(valuesById[-11504] = "CODE_ASTRO_CALIBRATION_FAILED")] = -11504;
  values[(valuesById[-11505] = "CODE_ASTRO_GOTO_FAILED")] = -11505;
  values[(valuesById[-11506] = "CODE_ASTRO_DARK_RUNNING")] = -11506;
  values[(valuesById[-11507] = "CODE_ASTRO_CALIBRATION_RUNNING")] = -11507;
  values[(valuesById[-11508] = "CODE_ASTRO_GOTO_RUNNING")] = -11508;
  values[(valuesById[-11509] = "CODE_ASTRO_LIVE_STACKING_RUNNING")] = -11509;
  values[(valuesById[-11510] = "CODE_ASTRO_RESET_PITCH_MOTOR_FAILED")] = -11510;
  values[(valuesById[-11511] = "CODE_ASTRO_NEED_CALIBRATION")] = -11511;
  values[
    (valuesById[-11512] =
      "CODE_ASTRO_GOTO_READ_MOTOR_POSITION_AND_PLATE_SOLVING_FAILED")
  ] = -11512;
  values[(valuesById[-11513] = "CODE_ASTRO_NEED_GOTO")] = -11513;
  values[(valuesById[-11514] = "CODE_ASTRO_NEED_ADJUST_SHOOT_PARAM")] = -11514;
  values[
    (valuesById[-11515] =
      "CODE_ASTRO_CALIBRATION_PLATE_SOLVING_FAILED_TOO_MUCH")
  ] = -11515;
  values[(valuesById[-11516] = "CODE_ASTRO_EQ_SOLVING_FAILED")] = -11516;
  values[(valuesById[-11517] = "CODE_ASTRO_SKY_SEARCH_FAILED")] = -11517;
  values[(valuesById[-11518] = "CODE_ASTRO_NEED_GOTO_DSO")] = -11518;
  values[(valuesById[-11519] = "CODE_ASTRO_RESTACK_CAMERA_MISMATCH")] = -11519;
  values[(valuesById[-11520] = "CODE_ASTRO_RESTACK_BINNING_MISMATCH")] = -11520;
  values[(valuesById[-11521] = "CODE_ASTRO_RESTACK_FILTER_MISMATCH")] = -11521;
  values[(valuesById[-11522] = "CODE_ASTRO_RESTACK_TARGET_MISMATCH")] = -11522;
  values[(valuesById[-11523] = "CODE_ASTRO_RESTACK_DARKFRAME_MISMATCH")] =
    -11523;
  values[(valuesById[-11524] = "CODE_ASTRO_RESTACK_FAILED")] = -11524;
  values[(valuesById[-11525] = "CODE_ASTRO_RESTACK_INVALID_DATA")] = -11525;
  values[(valuesById[-11526] = "CODE_ASTRO_OVEREXPOSURE_WARNING")] = -11526;
  values[(valuesById[-11527] = "CODE_ASTRO_EXP_TOO_LONG")] = -11527;
  values[(valuesById[-11528] = "CODE_ASTRO_NEED_EQ")] = -11528;
  values[(valuesById[-11529] = "CODE_ASTRO_STAR_TOO_FEW")] = -11529;
  values[(valuesById[-11530] = "CODE_ASTRO_DARK_TEMP_MISMATCH")] = -11530;
  values[(valuesById[-11531] = "CODE_ASTRO_SUN_MOON_NOT_FOUND")] = -11531;
  values[(valuesById[-11532] = "CODE_ASTRO_GUIDING_FAILED_LOWER_EXPOSURE")] =
    -11532;
  values[(valuesById[-11533] = "CODE_ASTRO_GUIDING_FAILED_TARGET_BLOCKED")] =
    -11533;
  values[(valuesById[-12500] = "CODE_CAMERA_WIDE_OPENED")] = -12500;
  values[(valuesById[-12501] = "CODE_CAMERA_WIDE_CLOSED")] = -12501;
  values[(valuesById[-12502] = "CODE_CAMERA_WIDE_CANNOT_FOUND")] = -12502;
  values[(valuesById[-12503] = "CODE_CAMERA_WIDE_OPEN_FAILED")] = -12503;
  values[(valuesById[-12504] = "CODE_CAMERA_WIDE_CLOSE_FAILED")] = -12504;
  values[(valuesById[-12505] = "CODE_CAMERA_WIDE_SET_ISP_FAILED")] = -12505;
  values[(valuesById[-12506] = "CODE_CAMERA_WIDE_PHOTOGRAPHING")] = -12506;
  values[(valuesById[-12507] = "CODE_CAMERA_WIDE_TIMELAPSE_RECORDING")] =
    -12507;
  values[(valuesById[-12508] = "CODE_CAMERA_WIDE_EXP_TOO_LONG")] = -12508;
  values[(valuesById[-12509] = "CODE_CAMERA_WIDE_RECORD_FILE_ERROR")] = -12509;
  values[(valuesById[-13300] = "CODE_SYSTEM_SET_TIME_FAILED")] = -13300;
  values[(valuesById[-13301] = "CODE_SYSTEM_SET_TIMEZONE_FAILED")] = -13301;
  values[(valuesById[-13800] = "CODE_RGB_POWER_UART_INIT_FAILED")] = -13800;
  values[(valuesById[-13801] = "CODE_RGB_POWER_UART_SEND_FAILED")] = -13801;
  values[(valuesById[-14500] = "CODE_STEP_MOTOR_IS_RUNNING")] = -14500;
  values[(valuesById[-14501] = "CODE_STEP_MOTOR_IS_STOPPED")] = -14501;
  values[(valuesById[-14502] = "CODE_STEP_MOTOR_PARALLEL_IN")] = -14502;
  values[(valuesById[-14503] = "CODE_STEP_MOTOR_PARALLEL_END")] = -14503;
  values[(valuesById[-14504] = "CODE_STEP_MOTOR_INVALID_PARAMETER_ID")] =
    -14504;
  values[(valuesById[-14505] = "CODE_STEP_MOTOR_INVALID_PARAMETER_ANGLE")] =
    -14505;
  values[(valuesById[-14506] = "CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED")] =
    -14506;
  values[
    (valuesById[-14507] = "CODE_STEP_MOTOR_INVALID_PARAMETER_SPEED_RAMPING")
  ] = -14507;
  values[
    (valuesById[-14508] = "CODE_STEP_MOTOR_INVALID_PARAMETER_RESOLUTION")
  ] = -14508;
  values[(valuesById[-14509] = "CODE_STEP_MOTOR_INVALID_PARAMETER_POSITION")] =
    -14509;
  values[(valuesById[-14510] = "CODE_STEP_MOTOR_OVERTIME_GET_LIMIT_RETURN")] =
    -14510;
  values[(valuesById[-14511] = "CODE_STEP_MOTOR_OVERTIME_GET_RESET_RETURN")] =
    -14511;
  values[
    (valuesById[-14512] =
      "CODE_STEP_MOTOR_OVERTIME_GET_ABSOLUTE_POSITION_RETURN")
  ] = -14512;
  values[
    (valuesById[-14513] =
      "CODE_STEP_MOTOR_OVERTIME_GET_RELATIVE_POSITION_RETURN")
  ] = -14513;
  values[(valuesById[-14514] = "CODE_STEP_MOTOR_OVERTIME_WAIT_TO_STOP")] =
    -14514;
  values[(valuesById[-14515] = "CODE_STEP_MOTOR_OVERTIME_WAIT_TO_RUN")] =
    -14515;
  values[(valuesById[-14516] = "CODE_STEP_MOTOR_LIMIT_SPEED_TO_MAX")] = -14516;
  values[(valuesById[-14517] = "CODE_STEP_MOTOR_LIMIT_SPEED_TO_MIN")] = -14517;
  values[(valuesById[-14518] = "CODE_STEP_MOTOR_LIMIT_POSITION_WARNING")] =
    -14518;
  values[(valuesById[-14519] = "CODE_STEP_MOTOR_LIMIT_POSITION_HIT")] = -14519;
  values[(valuesById[-14520] = "CODE_STEP_MOTOR_NEED_RESET")] = -14520;
  values[
    (valuesById[-14521] = "CODE_STEP_MOTOR_OVERTIME_GET_PE_SWITCH_RETURN")
  ] = -14521;
  values[(valuesById[-14522] = "CODE_STEP_MOTOR_OVERTIME_TO_RESET")] = -14522;
  values[(valuesById[-14523] = "CODE_STEP_MOTOR_ROLL_LIMIT_ANGLE_WARNING")] =
    -14523;
  values[(valuesById[-14900] = "CODE_TRACK_TRACKER_INITING")] = -14900;
  values[(valuesById[-14901] = "CODE_TRACK_TRACKER_FAILED")] = -14901;
  values[(valuesById[-14902] = "CODE_TRACK_SENTRY_MODE_INITING")] = -14902;
  values[(valuesById[-14903] = "CODE_TRACK_SENTRY_MODE_FAILED")] = -14903;
  values[(valuesById[-14904] = "CODE_UFOTRACK_MODE_INITING")] = -14904;
  values[(valuesById[-14905] = "CODE_UFOTRACK_MODE_FAILED")] = -14905;
  values[(valuesById[-14906] = "CODE_UFO_DAY_AUTO_MODE")] = -14906;
  values[(valuesById[-15100] = "CODE_FOCUS_ASTRO_AUTO_FOCUS_SLOW_ERROR")] =
    -15100;
  values[(valuesById[-15101] = "CODE_FOCUS_ASTRO_AUTO_FOCUS_FAST_ERROR")] =
    -15101;
  values[(valuesById[-15106] = "CODE_FOCUS_EXP_TOO_LONG")] = -15106;
  values[(valuesById[-15107] = "CODE_FOCUS_INFINITY_POS_ERROR")] = -15107;
  values[(valuesById[-15108] = "CODE_FOCUS_GET_NOW_POS_FAILED")] = -15108;
  values[(valuesById[-15600] = "CODE_PANORAMA_PHOTO_FAILED")] = -15600;
  values[(valuesById[-15601] = "CODE_PANORAMA_MOTOR_RESET_FAILED")] = -15601;
  values[(valuesById[-15602] = "CODE_PANORAMA_UPLOAD_USER_STOP")] = -15602;
  values[(valuesById[-15603] = "CODE_PANORAMA_UPLOAD_FILE_CHECK_FAILED")] =
    -15603;
  values[(valuesById[-15604] = "CODE_PANORAMA_UPLOAD_COMPRESS_FAILED")] =
    -15604;
  values[(valuesById[-15605] = "CODE_PANORAMA_UPLOAD_UPLOAD_FAILED")] = -15605;
  values[(valuesById[-15606] = "CODE_PANORAMA_UPLOAD_NOT_EXIST")] = -15606;
  values[(valuesById[-15607] = "CODE_PANORAMA_UPLOAD_IS_RUNNING")] = -15607;
  values[(valuesById[-15608] = "CODE_PANORAMA_UPLOAD_CAMERA_BUSY")] = -15608;
  values[(valuesById[-15609] = "CODE_PANORAMA_UPLOAD_NOT_IN_STA")] = -15609;
  values[(valuesById[-15612] = "CODE_PANORAMA_COMPRESSION_IS_RUNNING")] =
    -15612;
  values[(valuesById[-15614] = "CODE_PANORAMA_COMPOSE_IS_IDEL")] = -15614;
  values[(valuesById[-15615] = "CODE_PANORAMA_COMPOSE_IS_RUNNING")] = -15615;
  values[(valuesById[-16300] = "CODE_SHOOTING_SCHEDULE_DEVICE_ID_NOT_MATCH")] =
    -16300;
  values[
    (valuesById[-16301] = "CODE_SHOOTING_SCHEDULE_INVALID_SHOOTING_DURATION")
  ] = -16301;
  values[(valuesById[-16302] = "CODE_SHOOTING_SCHEDULE_TIME_CONFLICT")] =
    -16302;
  values[
    (valuesById[-16303] = "CODE_SHOOTING_SCHEDULE_INVALID_TASK_DURATION")
  ] = -16303;
  values[
    (valuesById[-16305] = "CODE_SHOOTING_SCHEDULE_DATABASE_OPERATION_FAILED")
  ] = -16305;
  values[(valuesById[-16306] = "CODE_SHOOTING_SCHEDULE_PASSWORD_ERROR")] =
    -16306;
  values[(valuesById[-16307] = "CODE_SHOOTING_SCHEDULE_SHOOTING")] = -16307;
  values[(valuesById[-16308] = "CODE_SHOOTING_SCHEDULE_START_TIME_TOO_FAR")] =
    -16308;
  values[(valuesById[-16309] = "CODE_SHOOTING_SCHEDULE_DEVICE_BUSY")] = -16309;
  values[(valuesById[-16310] = "CODE_SHOOTING_SCHEDULE_INTERRUPTED")] = -16310;
  values[(valuesById[-16311] = "CODE_SHOOTING_SCHEDULE_NOT_SYNCED")] = -16311;
  values[(valuesById[-16312] = "CODE_SHOOTING_SCHEDULE_TASK_TIME_TOO_SHORT")] =
    -16312;
  values[(valuesById[-16313] = "CODE_SHOOTING_SCHEDULE_TASK_EXPIRED")] = -16313;
  values[(valuesById[-16600] = "CODE_GLOBAL_TASK_MANAGER_BUSY")] = -16600;
  return values;
})();

/**
 * AstroTrackingSpecial enum.
 * @exports AstroTrackingSpecial
 * @enum {number}
 * @property {number} TRACKING_SUN=0 TRACKING_SUN value
 * @property {number} TRACKING_MOON=1 TRACKING_MOON value
 */
$root.AstroTrackingSpecial = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "TRACKING_SUN")] = 0;
  values[(valuesById[1] = "TRACKING_MOON")] = 1;
  return values;
})();

/**
 * SolarSystemTarget enum.
 * @exports SolarSystemTarget
 * @enum {number}
 * @property {number} Unknown=0 Unknown value
 * @property {number} Mercury=1 Mercury value
 * @property {number} Venus=2 Venus value
 * @property {number} Mars=3 Mars value
 * @property {number} Jupiter=4 Jupiter value
 * @property {number} Saturn=5 Saturn value
 * @property {number} Uranus=6 Uranus value
 * @property {number} Neptune=7 Neptune value
 * @property {number} Moon=8 Moon value
 * @property {number} Sun=9 Sun value
 */
$root.SolarSystemTarget = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "Unknown")] = 0;
  values[(valuesById[1] = "Mercury")] = 1;
  values[(valuesById[2] = "Venus")] = 2;
  values[(valuesById[3] = "Mars")] = 3;
  values[(valuesById[4] = "Jupiter")] = 4;
  values[(valuesById[5] = "Saturn")] = 5;
  values[(valuesById[6] = "Uranus")] = 6;
  values[(valuesById[7] = "Neptune")] = 7;
  values[(valuesById[8] = "Moon")] = 8;
  values[(valuesById[9] = "Sun")] = 9;
  return values;
})();

/**
 * PhotoMode enum.
 * @exports PhotoMode
 * @enum {number}
 * @property {number} Auto=0 Auto value
 * @property {number} Manual=1 Manual value
 */
$root.PhotoMode = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "Auto")] = 0;
  values[(valuesById[1] = "Manual")] = 1;
  return values;
})();

/**
 * WBMode enum.
 * @exports WBMode
 * @enum {number}
 * @property {number} ColorTemperature=0 ColorTemperature value
 * @property {number} SceneMode=1 SceneMode value
 */
$root.WBMode = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "ColorTemperature")] = 0;
  values[(valuesById[1] = "SceneMode")] = 1;
  return values;
})();

/**
 * IrCut enum.
 * @exports IrCut
 * @enum {number}
 * @property {number} CUT=0 CUT value
 * @property {number} PASS=1 PASS value
 */
$root.IrCut = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "CUT")] = 0;
  values[(valuesById[1] = "PASS")] = 1;
  return values;
})();

$root.ReqOpenRgb = (function () {
  /**
   * Properties of a ReqOpenRgb.
   * @exports IReqOpenRgb
   * @interface IReqOpenRgb
   */

  /**
   * Constructs a new ReqOpenRgb.
   * @exports ReqOpenRgb
   * @classdesc Represents a ReqOpenRgb.
   * @implements IReqOpenRgb
   * @constructor
   * @param {IReqOpenRgb=} [properties] Properties to set
   */
  function ReqOpenRgb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqOpenRgb instance using the specified properties.
   * @function create
   * @memberof ReqOpenRgb
   * @static
   * @param {IReqOpenRgb=} [properties] Properties to set
   * @returns {ReqOpenRgb} ReqOpenRgb instance
   */
  ReqOpenRgb.create = function create(properties) {
    return new ReqOpenRgb(properties);
  };

  /**
   * Encodes the specified ReqOpenRgb message. Does not implicitly {@link ReqOpenRgb.verify|verify} messages.
   * @function encode
   * @memberof ReqOpenRgb
   * @static
   * @param {IReqOpenRgb} message ReqOpenRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenRgb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqOpenRgb message, length delimited. Does not implicitly {@link ReqOpenRgb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOpenRgb
   * @static
   * @param {IReqOpenRgb} message ReqOpenRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenRgb.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOpenRgb message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOpenRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOpenRgb} ReqOpenRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenRgb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOpenRgb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOpenRgb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOpenRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOpenRgb} ReqOpenRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenRgb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOpenRgb message.
   * @function verify
   * @memberof ReqOpenRgb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOpenRgb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqOpenRgb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOpenRgb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOpenRgb} ReqOpenRgb
   */
  ReqOpenRgb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOpenRgb) return object;
    return new $root.ReqOpenRgb();
  };

  /**
   * Creates a plain object from a ReqOpenRgb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOpenRgb
   * @static
   * @param {ReqOpenRgb} message ReqOpenRgb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOpenRgb.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqOpenRgb to JSON.
   * @function toJSON
   * @memberof ReqOpenRgb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOpenRgb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOpenRgb
   * @function getTypeUrl
   * @memberof ReqOpenRgb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOpenRgb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOpenRgb";
  };

  return ReqOpenRgb;
})();

$root.ReqCloseRgb = (function () {
  /**
   * Properties of a ReqCloseRgb.
   * @exports IReqCloseRgb
   * @interface IReqCloseRgb
   */

  /**
   * Constructs a new ReqCloseRgb.
   * @exports ReqCloseRgb
   * @classdesc Represents a ReqCloseRgb.
   * @implements IReqCloseRgb
   * @constructor
   * @param {IReqCloseRgb=} [properties] Properties to set
   */
  function ReqCloseRgb(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqCloseRgb instance using the specified properties.
   * @function create
   * @memberof ReqCloseRgb
   * @static
   * @param {IReqCloseRgb=} [properties] Properties to set
   * @returns {ReqCloseRgb} ReqCloseRgb instance
   */
  ReqCloseRgb.create = function create(properties) {
    return new ReqCloseRgb(properties);
  };

  /**
   * Encodes the specified ReqCloseRgb message. Does not implicitly {@link ReqCloseRgb.verify|verify} messages.
   * @function encode
   * @memberof ReqCloseRgb
   * @static
   * @param {IReqCloseRgb} message ReqCloseRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCloseRgb.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqCloseRgb message, length delimited. Does not implicitly {@link ReqCloseRgb.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCloseRgb
   * @static
   * @param {IReqCloseRgb} message ReqCloseRgb message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCloseRgb.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCloseRgb message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCloseRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCloseRgb} ReqCloseRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCloseRgb.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCloseRgb();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCloseRgb message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCloseRgb
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCloseRgb} ReqCloseRgb
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCloseRgb.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCloseRgb message.
   * @function verify
   * @memberof ReqCloseRgb
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCloseRgb.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqCloseRgb message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCloseRgb
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCloseRgb} ReqCloseRgb
   */
  ReqCloseRgb.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCloseRgb) return object;
    return new $root.ReqCloseRgb();
  };

  /**
   * Creates a plain object from a ReqCloseRgb message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCloseRgb
   * @static
   * @param {ReqCloseRgb} message ReqCloseRgb
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCloseRgb.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqCloseRgb to JSON.
   * @function toJSON
   * @memberof ReqCloseRgb
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCloseRgb.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCloseRgb
   * @function getTypeUrl
   * @memberof ReqCloseRgb
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCloseRgb.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCloseRgb";
  };

  return ReqCloseRgb;
})();

$root.ReqPowerDown = (function () {
  /**
   * Properties of a ReqPowerDown.
   * @exports IReqPowerDown
   * @interface IReqPowerDown
   */

  /**
   * Constructs a new ReqPowerDown.
   * @exports ReqPowerDown
   * @classdesc Represents a ReqPowerDown.
   * @implements IReqPowerDown
   * @constructor
   * @param {IReqPowerDown=} [properties] Properties to set
   */
  function ReqPowerDown(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPowerDown instance using the specified properties.
   * @function create
   * @memberof ReqPowerDown
   * @static
   * @param {IReqPowerDown=} [properties] Properties to set
   * @returns {ReqPowerDown} ReqPowerDown instance
   */
  ReqPowerDown.create = function create(properties) {
    return new ReqPowerDown(properties);
  };

  /**
   * Encodes the specified ReqPowerDown message. Does not implicitly {@link ReqPowerDown.verify|verify} messages.
   * @function encode
   * @memberof ReqPowerDown
   * @static
   * @param {IReqPowerDown} message ReqPowerDown message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPowerDown.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPowerDown message, length delimited. Does not implicitly {@link ReqPowerDown.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPowerDown
   * @static
   * @param {IReqPowerDown} message ReqPowerDown message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPowerDown.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPowerDown message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPowerDown
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPowerDown} ReqPowerDown
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPowerDown.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPowerDown();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPowerDown message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPowerDown
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPowerDown} ReqPowerDown
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPowerDown.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPowerDown message.
   * @function verify
   * @memberof ReqPowerDown
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPowerDown.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPowerDown message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPowerDown
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPowerDown} ReqPowerDown
   */
  ReqPowerDown.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPowerDown) return object;
    return new $root.ReqPowerDown();
  };

  /**
   * Creates a plain object from a ReqPowerDown message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPowerDown
   * @static
   * @param {ReqPowerDown} message ReqPowerDown
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPowerDown.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPowerDown to JSON.
   * @function toJSON
   * @memberof ReqPowerDown
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPowerDown.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPowerDown
   * @function getTypeUrl
   * @memberof ReqPowerDown
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPowerDown.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPowerDown";
  };

  return ReqPowerDown;
})();

$root.ReqOpenPowerInd = (function () {
  /**
   * Properties of a ReqOpenPowerInd.
   * @exports IReqOpenPowerInd
   * @interface IReqOpenPowerInd
   */

  /**
   * Constructs a new ReqOpenPowerInd.
   * @exports ReqOpenPowerInd
   * @classdesc Represents a ReqOpenPowerInd.
   * @implements IReqOpenPowerInd
   * @constructor
   * @param {IReqOpenPowerInd=} [properties] Properties to set
   */
  function ReqOpenPowerInd(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqOpenPowerInd instance using the specified properties.
   * @function create
   * @memberof ReqOpenPowerInd
   * @static
   * @param {IReqOpenPowerInd=} [properties] Properties to set
   * @returns {ReqOpenPowerInd} ReqOpenPowerInd instance
   */
  ReqOpenPowerInd.create = function create(properties) {
    return new ReqOpenPowerInd(properties);
  };

  /**
   * Encodes the specified ReqOpenPowerInd message. Does not implicitly {@link ReqOpenPowerInd.verify|verify} messages.
   * @function encode
   * @memberof ReqOpenPowerInd
   * @static
   * @param {IReqOpenPowerInd} message ReqOpenPowerInd message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenPowerInd.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqOpenPowerInd message, length delimited. Does not implicitly {@link ReqOpenPowerInd.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqOpenPowerInd
   * @static
   * @param {IReqOpenPowerInd} message ReqOpenPowerInd message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqOpenPowerInd.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqOpenPowerInd message from the specified reader or buffer.
   * @function decode
   * @memberof ReqOpenPowerInd
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqOpenPowerInd} ReqOpenPowerInd
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenPowerInd.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqOpenPowerInd();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqOpenPowerInd message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqOpenPowerInd
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqOpenPowerInd} ReqOpenPowerInd
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqOpenPowerInd.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqOpenPowerInd message.
   * @function verify
   * @memberof ReqOpenPowerInd
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqOpenPowerInd.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqOpenPowerInd message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqOpenPowerInd
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqOpenPowerInd} ReqOpenPowerInd
   */
  ReqOpenPowerInd.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqOpenPowerInd) return object;
    return new $root.ReqOpenPowerInd();
  };

  /**
   * Creates a plain object from a ReqOpenPowerInd message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqOpenPowerInd
   * @static
   * @param {ReqOpenPowerInd} message ReqOpenPowerInd
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqOpenPowerInd.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqOpenPowerInd to JSON.
   * @function toJSON
   * @memberof ReqOpenPowerInd
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqOpenPowerInd.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqOpenPowerInd
   * @function getTypeUrl
   * @memberof ReqOpenPowerInd
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqOpenPowerInd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqOpenPowerInd";
  };

  return ReqOpenPowerInd;
})();

$root.ReqClosePowerInd = (function () {
  /**
   * Properties of a ReqClosePowerInd.
   * @exports IReqClosePowerInd
   * @interface IReqClosePowerInd
   */

  /**
   * Constructs a new ReqClosePowerInd.
   * @exports ReqClosePowerInd
   * @classdesc Represents a ReqClosePowerInd.
   * @implements IReqClosePowerInd
   * @constructor
   * @param {IReqClosePowerInd=} [properties] Properties to set
   */
  function ReqClosePowerInd(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqClosePowerInd instance using the specified properties.
   * @function create
   * @memberof ReqClosePowerInd
   * @static
   * @param {IReqClosePowerInd=} [properties] Properties to set
   * @returns {ReqClosePowerInd} ReqClosePowerInd instance
   */
  ReqClosePowerInd.create = function create(properties) {
    return new ReqClosePowerInd(properties);
  };

  /**
   * Encodes the specified ReqClosePowerInd message. Does not implicitly {@link ReqClosePowerInd.verify|verify} messages.
   * @function encode
   * @memberof ReqClosePowerInd
   * @static
   * @param {IReqClosePowerInd} message ReqClosePowerInd message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqClosePowerInd.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqClosePowerInd message, length delimited. Does not implicitly {@link ReqClosePowerInd.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqClosePowerInd
   * @static
   * @param {IReqClosePowerInd} message ReqClosePowerInd message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqClosePowerInd.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqClosePowerInd message from the specified reader or buffer.
   * @function decode
   * @memberof ReqClosePowerInd
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqClosePowerInd} ReqClosePowerInd
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqClosePowerInd.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqClosePowerInd();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqClosePowerInd message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqClosePowerInd
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqClosePowerInd} ReqClosePowerInd
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqClosePowerInd.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqClosePowerInd message.
   * @function verify
   * @memberof ReqClosePowerInd
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqClosePowerInd.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqClosePowerInd message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqClosePowerInd
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqClosePowerInd} ReqClosePowerInd
   */
  ReqClosePowerInd.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqClosePowerInd) return object;
    return new $root.ReqClosePowerInd();
  };

  /**
   * Creates a plain object from a ReqClosePowerInd message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqClosePowerInd
   * @static
   * @param {ReqClosePowerInd} message ReqClosePowerInd
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqClosePowerInd.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqClosePowerInd to JSON.
   * @function toJSON
   * @memberof ReqClosePowerInd
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqClosePowerInd.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqClosePowerInd
   * @function getTypeUrl
   * @memberof ReqClosePowerInd
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqClosePowerInd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqClosePowerInd";
  };

  return ReqClosePowerInd;
})();

$root.ReqReboot = (function () {
  /**
   * Properties of a ReqReboot.
   * @exports IReqReboot
   * @interface IReqReboot
   */

  /**
   * Constructs a new ReqReboot.
   * @exports ReqReboot
   * @classdesc Represents a ReqReboot.
   * @implements IReqReboot
   * @constructor
   * @param {IReqReboot=} [properties] Properties to set
   */
  function ReqReboot(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqReboot instance using the specified properties.
   * @function create
   * @memberof ReqReboot
   * @static
   * @param {IReqReboot=} [properties] Properties to set
   * @returns {ReqReboot} ReqReboot instance
   */
  ReqReboot.create = function create(properties) {
    return new ReqReboot(properties);
  };

  /**
   * Encodes the specified ReqReboot message. Does not implicitly {@link ReqReboot.verify|verify} messages.
   * @function encode
   * @memberof ReqReboot
   * @static
   * @param {IReqReboot} message ReqReboot message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReboot.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqReboot message, length delimited. Does not implicitly {@link ReqReboot.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqReboot
   * @static
   * @param {IReqReboot} message ReqReboot message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReboot.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqReboot message from the specified reader or buffer.
   * @function decode
   * @memberof ReqReboot
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqReboot} ReqReboot
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReboot.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqReboot();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqReboot message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqReboot
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqReboot} ReqReboot
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReboot.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqReboot message.
   * @function verify
   * @memberof ReqReboot
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqReboot.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqReboot message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqReboot
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqReboot} ReqReboot
   */
  ReqReboot.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqReboot) return object;
    return new $root.ReqReboot();
  };

  /**
   * Creates a plain object from a ReqReboot message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqReboot
   * @static
   * @param {ReqReboot} message ReqReboot
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqReboot.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqReboot to JSON.
   * @function toJSON
   * @memberof ReqReboot
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqReboot.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqReboot
   * @function getTypeUrl
   * @memberof ReqReboot
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqReboot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqReboot";
  };

  return ReqReboot;
})();

/**
 * ShootingScheduleState enum.
 * @exports ShootingScheduleState
 * @enum {number}
 * @property {number} SHOOTING_SCHEDULE_STATE_INITIALIZED=0 SHOOTING_SCHEDULE_STATE_INITIALIZED value
 * @property {number} SHOOTING_SCHEDULE_STATE_PENDING_SHOOT=1 SHOOTING_SCHEDULE_STATE_PENDING_SHOOT value
 * @property {number} SHOOTING_SCHEDULE_STATE_SHOOTING=2 SHOOTING_SCHEDULE_STATE_SHOOTING value
 * @property {number} SHOOTING_SCHEDULE_STATE_COMPLETED=3 SHOOTING_SCHEDULE_STATE_COMPLETED value
 * @property {number} SHOOTING_SCHEDULE_STATE_EXPIRED=4 SHOOTING_SCHEDULE_STATE_EXPIRED value
 */
$root.ShootingScheduleState = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "SHOOTING_SCHEDULE_STATE_INITIALIZED")] = 0;
  values[(valuesById[1] = "SHOOTING_SCHEDULE_STATE_PENDING_SHOOT")] = 1;
  values[(valuesById[2] = "SHOOTING_SCHEDULE_STATE_SHOOTING")] = 2;
  values[(valuesById[3] = "SHOOTING_SCHEDULE_STATE_COMPLETED")] = 3;
  values[(valuesById[4] = "SHOOTING_SCHEDULE_STATE_EXPIRED")] = 4;
  return values;
})();

/**
 * ShootingScheduleSyncState enum.
 * @exports ShootingScheduleSyncState
 * @enum {number}
 * @property {number} SHOOTING_SCHEDULE_SYNC_STATE_PENDING_SYNC=0 SHOOTING_SCHEDULE_SYNC_STATE_PENDING_SYNC value
 * @property {number} SHOOTING_SCHEDULE_SYNC_STATE_SYNCED=1 SHOOTING_SCHEDULE_SYNC_STATE_SYNCED value
 */
$root.ShootingScheduleSyncState = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "SHOOTING_SCHEDULE_SYNC_STATE_PENDING_SYNC")] = 0;
  values[(valuesById[1] = "SHOOTING_SCHEDULE_SYNC_STATE_SYNCED")] = 1;
  return values;
})();

/**
 * ShootingScheduleResult enum.
 * @exports ShootingScheduleResult
 * @enum {number}
 * @property {number} SHOOTING_SCHEDULE_RESULT_PENDING_START=0 SHOOTING_SCHEDULE_RESULT_PENDING_START value
 * @property {number} SHOOTING_SCHEDULE_RESULT_ALL_COMPLETED=1 SHOOTING_SCHEDULE_RESULT_ALL_COMPLETED value
 * @property {number} SHOOTING_SCHEDULE_RESULT_PARTIALLY_COMPLETED=2 SHOOTING_SCHEDULE_RESULT_PARTIALLY_COMPLETED value
 * @property {number} SHOOTING_SCHEDULE_RESULT_ALL_FAILED=3 SHOOTING_SCHEDULE_RESULT_ALL_FAILED value
 */
$root.ShootingScheduleResult = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "SHOOTING_SCHEDULE_RESULT_PENDING_START")] = 0;
  values[(valuesById[1] = "SHOOTING_SCHEDULE_RESULT_ALL_COMPLETED")] = 1;
  values[(valuesById[2] = "SHOOTING_SCHEDULE_RESULT_PARTIALLY_COMPLETED")] = 2;
  values[(valuesById[3] = "SHOOTING_SCHEDULE_RESULT_ALL_FAILED")] = 3;
  return values;
})();

/**
 * ShootingTaskState enum.
 * @exports ShootingTaskState
 * @enum {number}
 * @property {number} SHOOTING_TASK_STATUS_IDLE=0 SHOOTING_TASK_STATUS_IDLE value
 * @property {number} SHOOTING_TASK_STATUS_SHOOTING=1 SHOOTING_TASK_STATUS_SHOOTING value
 * @property {number} SHOOTING_TASK_STATUS_SUCCESS=2 SHOOTING_TASK_STATUS_SUCCESS value
 * @property {number} SHOOTING_TASK_STATUS_FAILED=3 SHOOTING_TASK_STATUS_FAILED value
 * @property {number} SHOOTING_TASK_STATUS_INTERRUPTED=4 SHOOTING_TASK_STATUS_INTERRUPTED value
 */
$root.ShootingTaskState = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "SHOOTING_TASK_STATUS_IDLE")] = 0;
  values[(valuesById[1] = "SHOOTING_TASK_STATUS_SHOOTING")] = 1;
  values[(valuesById[2] = "SHOOTING_TASK_STATUS_SUCCESS")] = 2;
  values[(valuesById[3] = "SHOOTING_TASK_STATUS_FAILED")] = 3;
  values[(valuesById[4] = "SHOOTING_TASK_STATUS_INTERRUPTED")] = 4;
  return values;
})();

/**
 * ShootingScheduleMode enum.
 * @exports ShootingScheduleMode
 * @enum {number}
 * @property {number} SHOOTING_SCHEDULE_MODE_ASTRO_DEEP_SKY=0 SHOOTING_SCHEDULE_MODE_ASTRO_DEEP_SKY value
 */
$root.ShootingScheduleMode = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "SHOOTING_SCHEDULE_MODE_ASTRO_DEEP_SKY")] = 0;
  return values;
})();

$root.ShootingTaskMsg = (function () {
  /**
   * Properties of a ShootingTaskMsg.
   * @exports IShootingTaskMsg
   * @interface IShootingTaskMsg
   * @property {string|null} [scheduleId] ShootingTaskMsg scheduleId
   * @property {string|null} [params] ShootingTaskMsg params
   * @property {ShootingTaskState|null} [state] ShootingTaskMsg state
   * @property {number|null} [code] ShootingTaskMsg code
   * @property {number|Long|null} [createdTime] ShootingTaskMsg createdTime
   * @property {number|Long|null} [updatedTime] ShootingTaskMsg updatedTime
   * @property {string|null} [scheduleTaskId] ShootingTaskMsg scheduleTaskId
   * @property {number|null} [paramMode] ShootingTaskMsg paramMode
   * @property {number|null} [paramVersion] ShootingTaskMsg paramVersion
   * @property {number|null} [createFrom] ShootingTaskMsg createFrom
   */

  /**
   * Constructs a new ShootingTaskMsg.
   * @exports ShootingTaskMsg
   * @classdesc Represents a ShootingTaskMsg.
   * @implements IShootingTaskMsg
   * @constructor
   * @param {IShootingTaskMsg=} [properties] Properties to set
   */
  function ShootingTaskMsg(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ShootingTaskMsg scheduleId.
   * @member {string} scheduleId
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.scheduleId = "";

  /**
   * ShootingTaskMsg params.
   * @member {string} params
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.params = "";

  /**
   * ShootingTaskMsg state.
   * @member {ShootingTaskState} state
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.state = 0;

  /**
   * ShootingTaskMsg code.
   * @member {number} code
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.code = 0;

  /**
   * ShootingTaskMsg createdTime.
   * @member {number|Long} createdTime
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.createdTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingTaskMsg updatedTime.
   * @member {number|Long} updatedTime
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.updatedTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingTaskMsg scheduleTaskId.
   * @member {string} scheduleTaskId
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.scheduleTaskId = "";

  /**
   * ShootingTaskMsg paramMode.
   * @member {number} paramMode
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.paramMode = 0;

  /**
   * ShootingTaskMsg paramVersion.
   * @member {number} paramVersion
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.paramVersion = 0;

  /**
   * ShootingTaskMsg createFrom.
   * @member {number} createFrom
   * @memberof ShootingTaskMsg
   * @instance
   */
  ShootingTaskMsg.prototype.createFrom = 0;

  /**
   * Creates a new ShootingTaskMsg instance using the specified properties.
   * @function create
   * @memberof ShootingTaskMsg
   * @static
   * @param {IShootingTaskMsg=} [properties] Properties to set
   * @returns {ShootingTaskMsg} ShootingTaskMsg instance
   */
  ShootingTaskMsg.create = function create(properties) {
    return new ShootingTaskMsg(properties);
  };

  /**
   * Encodes the specified ShootingTaskMsg message. Does not implicitly {@link ShootingTaskMsg.verify|verify} messages.
   * @function encode
   * @memberof ShootingTaskMsg
   * @static
   * @param {IShootingTaskMsg} message ShootingTaskMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingTaskMsg.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.scheduleId);
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.params);
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.state);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.code);
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int64(message.createdTime);
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int64(message.updatedTime);
    if (
      message.scheduleTaskId != null &&
      Object.hasOwnProperty.call(message, "scheduleTaskId")
    )
      writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.scheduleTaskId);
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int32(message.paramMode);
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).int32(message.paramVersion);
    if (
      message.createFrom != null &&
      Object.hasOwnProperty.call(message, "createFrom")
    )
      writer.uint32(/* id 10, wireType 0 =*/ 80).int32(message.createFrom);
    return writer;
  };

  /**
   * Encodes the specified ShootingTaskMsg message, length delimited. Does not implicitly {@link ShootingTaskMsg.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ShootingTaskMsg
   * @static
   * @param {IShootingTaskMsg} message ShootingTaskMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingTaskMsg.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ShootingTaskMsg message from the specified reader or buffer.
   * @function decode
   * @memberof ShootingTaskMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ShootingTaskMsg} ShootingTaskMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingTaskMsg.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ShootingTaskMsg();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.scheduleId = reader.string();
          break;
        }
        case 2: {
          message.params = reader.string();
          break;
        }
        case 3: {
          message.state = reader.int32();
          break;
        }
        case 4: {
          message.code = reader.int32();
          break;
        }
        case 5: {
          message.createdTime = reader.int64();
          break;
        }
        case 6: {
          message.updatedTime = reader.int64();
          break;
        }
        case 7: {
          message.scheduleTaskId = reader.string();
          break;
        }
        case 8: {
          message.paramMode = reader.int32();
          break;
        }
        case 9: {
          message.paramVersion = reader.int32();
          break;
        }
        case 10: {
          message.createFrom = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ShootingTaskMsg message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ShootingTaskMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ShootingTaskMsg} ShootingTaskMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingTaskMsg.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ShootingTaskMsg message.
   * @function verify
   * @memberof ShootingTaskMsg
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ShootingTaskMsg.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      if (!$util.isString(message.scheduleId))
        return "scheduleId: string expected";
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      if (!$util.isString(message.params)) return "params: string expected";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      switch (message.state) {
        default:
          return "state: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      if (
        !$util.isInteger(message.createdTime) &&
        !(
          message.createdTime &&
          $util.isInteger(message.createdTime.low) &&
          $util.isInteger(message.createdTime.high)
        )
      )
        return "createdTime: integer|Long expected";
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      if (
        !$util.isInteger(message.updatedTime) &&
        !(
          message.updatedTime &&
          $util.isInteger(message.updatedTime.low) &&
          $util.isInteger(message.updatedTime.high)
        )
      )
        return "updatedTime: integer|Long expected";
    if (
      message.scheduleTaskId != null &&
      Object.hasOwnProperty.call(message, "scheduleTaskId")
    )
      if (!$util.isString(message.scheduleTaskId))
        return "scheduleTaskId: string expected";
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      if (!$util.isInteger(message.paramMode))
        return "paramMode: integer expected";
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      if (!$util.isInteger(message.paramVersion))
        return "paramVersion: integer expected";
    if (
      message.createFrom != null &&
      Object.hasOwnProperty.call(message, "createFrom")
    )
      if (!$util.isInteger(message.createFrom))
        return "createFrom: integer expected";
    return null;
  };

  /**
   * Creates a ShootingTaskMsg message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ShootingTaskMsg
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ShootingTaskMsg} ShootingTaskMsg
   */
  ShootingTaskMsg.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ShootingTaskMsg) return object;
    if (!$util.isObject(object))
      throw TypeError(".ShootingTaskMsg: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ShootingTaskMsg();
    if (object.scheduleId != null)
      message.scheduleId = String(object.scheduleId);
    if (object.params != null) message.params = String(object.params);
    switch (object.state) {
      default:
        if (typeof object.state === "number") {
          message.state = object.state;
          break;
        }
        break;
      case "SHOOTING_TASK_STATUS_IDLE":
      case 0:
        message.state = 0;
        break;
      case "SHOOTING_TASK_STATUS_SHOOTING":
      case 1:
        message.state = 1;
        break;
      case "SHOOTING_TASK_STATUS_SUCCESS":
      case 2:
        message.state = 2;
        break;
      case "SHOOTING_TASK_STATUS_FAILED":
      case 3:
        message.state = 3;
        break;
      case "SHOOTING_TASK_STATUS_INTERRUPTED":
      case 4:
        message.state = 4;
        break;
    }
    if (object.code != null) message.code = object.code | 0;
    if (object.createdTime != null)
      if ($util.Long)
        message.createdTime = $util.Long.fromValue(object.createdTime, false);
      else if (typeof object.createdTime === "string")
        message.createdTime = parseInt(object.createdTime, 10);
      else if (typeof object.createdTime === "number")
        message.createdTime = object.createdTime;
      else if (typeof object.createdTime === "object")
        message.createdTime = new $util.LongBits(
          object.createdTime.low >>> 0,
          object.createdTime.high >>> 0,
        ).toNumber();
    if (object.updatedTime != null)
      if ($util.Long)
        message.updatedTime = $util.Long.fromValue(object.updatedTime, false);
      else if (typeof object.updatedTime === "string")
        message.updatedTime = parseInt(object.updatedTime, 10);
      else if (typeof object.updatedTime === "number")
        message.updatedTime = object.updatedTime;
      else if (typeof object.updatedTime === "object")
        message.updatedTime = new $util.LongBits(
          object.updatedTime.low >>> 0,
          object.updatedTime.high >>> 0,
        ).toNumber();
    if (object.scheduleTaskId != null)
      message.scheduleTaskId = String(object.scheduleTaskId);
    if (object.paramMode != null) message.paramMode = object.paramMode | 0;
    if (object.paramVersion != null)
      message.paramVersion = object.paramVersion | 0;
    if (object.createFrom != null) message.createFrom = object.createFrom | 0;
    return message;
  };

  /**
   * Creates a plain object from a ShootingTaskMsg message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ShootingTaskMsg
   * @static
   * @param {ShootingTaskMsg} message ShootingTaskMsg
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ShootingTaskMsg.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.scheduleId = "";
      object.params = "";
      object.state = options.enums === String ? "SHOOTING_TASK_STATUS_IDLE" : 0;
      object.code = 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.createdTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.createdTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.updatedTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.updatedTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.scheduleTaskId = "";
      object.paramMode = 0;
      object.paramVersion = 0;
      object.createFrom = 0;
    }
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      object.scheduleId = message.scheduleId;
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      object.params = message.params;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state =
        options.enums === String
          ? $root.ShootingTaskState[message.state] === undefined
            ? message.state
            : $root.ShootingTaskState[message.state]
          : message.state;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.createdTime =
          typeof message.createdTime === "number"
            ? BigInt(message.createdTime)
            : $util.Long.fromBits(
                message.createdTime.low >>> 0,
                message.createdTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.createdTime === "number")
        object.createdTime =
          options.longs === String
            ? String(message.createdTime)
            : message.createdTime;
      else
        object.createdTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.createdTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.createdTime.low >>> 0,
                  message.createdTime.high >>> 0,
                ).toNumber()
              : message.createdTime;
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.updatedTime =
          typeof message.updatedTime === "number"
            ? BigInt(message.updatedTime)
            : $util.Long.fromBits(
                message.updatedTime.low >>> 0,
                message.updatedTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.updatedTime === "number")
        object.updatedTime =
          options.longs === String
            ? String(message.updatedTime)
            : message.updatedTime;
      else
        object.updatedTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.updatedTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.updatedTime.low >>> 0,
                  message.updatedTime.high >>> 0,
                ).toNumber()
              : message.updatedTime;
    if (
      message.scheduleTaskId != null &&
      Object.hasOwnProperty.call(message, "scheduleTaskId")
    )
      object.scheduleTaskId = message.scheduleTaskId;
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      object.paramMode = message.paramMode;
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      object.paramVersion = message.paramVersion;
    if (
      message.createFrom != null &&
      Object.hasOwnProperty.call(message, "createFrom")
    )
      object.createFrom = message.createFrom;
    return object;
  };

  /**
   * Converts this ShootingTaskMsg to JSON.
   * @function toJSON
   * @memberof ShootingTaskMsg
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ShootingTaskMsg.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ShootingTaskMsg
   * @function getTypeUrl
   * @memberof ShootingTaskMsg
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ShootingTaskMsg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ShootingTaskMsg";
  };

  return ShootingTaskMsg;
})();

$root.ShootingScheduleMsg = (function () {
  /**
   * Properties of a ShootingScheduleMsg.
   * @exports IShootingScheduleMsg
   * @interface IShootingScheduleMsg
   * @property {string|null} [scheduleId] ShootingScheduleMsg scheduleId
   * @property {string|null} [scheduleName] ShootingScheduleMsg scheduleName
   * @property {number|null} [deviceId] ShootingScheduleMsg deviceId
   * @property {string|null} [macAddress] ShootingScheduleMsg macAddress
   * @property {number|Long|null} [startTime] ShootingScheduleMsg startTime
   * @property {number|Long|null} [endTime] ShootingScheduleMsg endTime
   * @property {ShootingScheduleResult|null} [result] ShootingScheduleMsg result
   * @property {number|Long|null} [createdTime] ShootingScheduleMsg createdTime
   * @property {number|Long|null} [updatedTime] ShootingScheduleMsg updatedTime
   * @property {ShootingScheduleState|null} [state] ShootingScheduleMsg state
   * @property {number|null} [lock] ShootingScheduleMsg lock
   * @property {string|null} [password] ShootingScheduleMsg password
   * @property {Array.<IShootingTaskMsg>|null} [shootingTasks] ShootingScheduleMsg shootingTasks
   * @property {number|null} [paramMode] ShootingScheduleMsg paramMode
   * @property {number|null} [paramVersion] ShootingScheduleMsg paramVersion
   * @property {string|null} [params] ShootingScheduleMsg params
   * @property {number|Long|null} [scheduleTime] ShootingScheduleMsg scheduleTime
   * @property {ShootingScheduleSyncState|null} [syncState] ShootingScheduleMsg syncState
   */

  /**
   * Constructs a new ShootingScheduleMsg.
   * @exports ShootingScheduleMsg
   * @classdesc Represents a ShootingScheduleMsg.
   * @implements IShootingScheduleMsg
   * @constructor
   * @param {IShootingScheduleMsg=} [properties] Properties to set
   */
  function ShootingScheduleMsg(properties) {
    this.shootingTasks = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ShootingScheduleMsg scheduleId.
   * @member {string} scheduleId
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.scheduleId = "";

  /**
   * ShootingScheduleMsg scheduleName.
   * @member {string} scheduleName
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.scheduleName = "";

  /**
   * ShootingScheduleMsg deviceId.
   * @member {number} deviceId
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.deviceId = 0;

  /**
   * ShootingScheduleMsg macAddress.
   * @member {string} macAddress
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.macAddress = "";

  /**
   * ShootingScheduleMsg startTime.
   * @member {number|Long} startTime
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.startTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingScheduleMsg endTime.
   * @member {number|Long} endTime
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.endTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingScheduleMsg result.
   * @member {ShootingScheduleResult} result
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.result = 0;

  /**
   * ShootingScheduleMsg createdTime.
   * @member {number|Long} createdTime
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.createdTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingScheduleMsg updatedTime.
   * @member {number|Long} updatedTime
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.updatedTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingScheduleMsg state.
   * @member {ShootingScheduleState} state
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.state = 0;

  /**
   * ShootingScheduleMsg lock.
   * @member {number} lock
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.lock = 0;

  /**
   * ShootingScheduleMsg password.
   * @member {string} password
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.password = "";

  /**
   * ShootingScheduleMsg shootingTasks.
   * @member {Array.<IShootingTaskMsg>} shootingTasks
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.shootingTasks = $util.emptyArray;

  /**
   * ShootingScheduleMsg paramMode.
   * @member {number} paramMode
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.paramMode = 0;

  /**
   * ShootingScheduleMsg paramVersion.
   * @member {number} paramVersion
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.paramVersion = 0;

  /**
   * ShootingScheduleMsg params.
   * @member {string} params
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.params = "";

  /**
   * ShootingScheduleMsg scheduleTime.
   * @member {number|Long} scheduleTime
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.scheduleTime = $util.Long
    ? $util.Long.fromBits(0, 0, false)
    : 0;

  /**
   * ShootingScheduleMsg syncState.
   * @member {ShootingScheduleSyncState} syncState
   * @memberof ShootingScheduleMsg
   * @instance
   */
  ShootingScheduleMsg.prototype.syncState = 0;

  /**
   * Creates a new ShootingScheduleMsg instance using the specified properties.
   * @function create
   * @memberof ShootingScheduleMsg
   * @static
   * @param {IShootingScheduleMsg=} [properties] Properties to set
   * @returns {ShootingScheduleMsg} ShootingScheduleMsg instance
   */
  ShootingScheduleMsg.create = function create(properties) {
    return new ShootingScheduleMsg(properties);
  };

  /**
   * Encodes the specified ShootingScheduleMsg message. Does not implicitly {@link ShootingScheduleMsg.verify|verify} messages.
   * @function encode
   * @memberof ShootingScheduleMsg
   * @static
   * @param {IShootingScheduleMsg} message ShootingScheduleMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingScheduleMsg.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.scheduleId);
    if (
      message.scheduleName != null &&
      Object.hasOwnProperty.call(message, "scheduleName")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.scheduleName);
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.deviceId);
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.macAddress);
    if (
      message.startTime != null &&
      Object.hasOwnProperty.call(message, "startTime")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).int64(message.startTime);
    if (
      message.endTime != null &&
      Object.hasOwnProperty.call(message, "endTime")
    )
      writer.uint32(/* id 6, wireType 0 =*/ 48).int64(message.endTime);
    if (message.result != null && Object.hasOwnProperty.call(message, "result"))
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.result);
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      writer.uint32(/* id 8, wireType 0 =*/ 64).int64(message.createdTime);
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      writer.uint32(/* id 9, wireType 0 =*/ 72).int64(message.updatedTime);
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      writer.uint32(/* id 10, wireType 0 =*/ 80).int32(message.state);
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      writer.uint32(/* id 11, wireType 0 =*/ 88).int32(message.lock);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 12, wireType 2 =*/ 98).string(message.password);
    if (message.shootingTasks != null && message.shootingTasks.length)
      for (var i = 0; i < message.shootingTasks.length; ++i)
        $root.ShootingTaskMsg.encode(
          message.shootingTasks[i],
          writer.uint32(/* id 13, wireType 2 =*/ 106).fork(),
          q + 1,
        ).ldelim();
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      writer.uint32(/* id 14, wireType 0 =*/ 112).int32(message.paramMode);
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      writer.uint32(/* id 15, wireType 0 =*/ 120).int32(message.paramVersion);
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      writer.uint32(/* id 16, wireType 2 =*/ 130).string(message.params);
    if (
      message.scheduleTime != null &&
      Object.hasOwnProperty.call(message, "scheduleTime")
    )
      writer.uint32(/* id 17, wireType 0 =*/ 136).int64(message.scheduleTime);
    if (
      message.syncState != null &&
      Object.hasOwnProperty.call(message, "syncState")
    )
      writer.uint32(/* id 18, wireType 0 =*/ 144).int32(message.syncState);
    return writer;
  };

  /**
   * Encodes the specified ShootingScheduleMsg message, length delimited. Does not implicitly {@link ShootingScheduleMsg.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ShootingScheduleMsg
   * @static
   * @param {IShootingScheduleMsg} message ShootingScheduleMsg message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingScheduleMsg.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ShootingScheduleMsg message from the specified reader or buffer.
   * @function decode
   * @memberof ShootingScheduleMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ShootingScheduleMsg} ShootingScheduleMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingScheduleMsg.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ShootingScheduleMsg();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.scheduleId = reader.string();
          break;
        }
        case 2: {
          message.scheduleName = reader.string();
          break;
        }
        case 3: {
          message.deviceId = reader.int32();
          break;
        }
        case 4: {
          message.macAddress = reader.string();
          break;
        }
        case 5: {
          message.startTime = reader.int64();
          break;
        }
        case 6: {
          message.endTime = reader.int64();
          break;
        }
        case 7: {
          message.result = reader.int32();
          break;
        }
        case 8: {
          message.createdTime = reader.int64();
          break;
        }
        case 9: {
          message.updatedTime = reader.int64();
          break;
        }
        case 10: {
          message.state = reader.int32();
          break;
        }
        case 11: {
          message.lock = reader.int32();
          break;
        }
        case 12: {
          message.password = reader.string();
          break;
        }
        case 13: {
          if (!(message.shootingTasks && message.shootingTasks.length))
            message.shootingTasks = [];
          message.shootingTasks.push(
            $root.ShootingTaskMsg.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 14: {
          message.paramMode = reader.int32();
          break;
        }
        case 15: {
          message.paramVersion = reader.int32();
          break;
        }
        case 16: {
          message.params = reader.string();
          break;
        }
        case 17: {
          message.scheduleTime = reader.int64();
          break;
        }
        case 18: {
          message.syncState = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ShootingScheduleMsg message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ShootingScheduleMsg
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ShootingScheduleMsg} ShootingScheduleMsg
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingScheduleMsg.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ShootingScheduleMsg message.
   * @function verify
   * @memberof ShootingScheduleMsg
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ShootingScheduleMsg.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      if (!$util.isString(message.scheduleId))
        return "scheduleId: string expected";
    if (
      message.scheduleName != null &&
      Object.hasOwnProperty.call(message, "scheduleName")
    )
      if (!$util.isString(message.scheduleName))
        return "scheduleName: string expected";
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      if (!$util.isInteger(message.deviceId))
        return "deviceId: integer expected";
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      if (!$util.isString(message.macAddress))
        return "macAddress: string expected";
    if (
      message.startTime != null &&
      Object.hasOwnProperty.call(message, "startTime")
    )
      if (
        !$util.isInteger(message.startTime) &&
        !(
          message.startTime &&
          $util.isInteger(message.startTime.low) &&
          $util.isInteger(message.startTime.high)
        )
      )
        return "startTime: integer|Long expected";
    if (
      message.endTime != null &&
      Object.hasOwnProperty.call(message, "endTime")
    )
      if (
        !$util.isInteger(message.endTime) &&
        !(
          message.endTime &&
          $util.isInteger(message.endTime.low) &&
          $util.isInteger(message.endTime.high)
        )
      )
        return "endTime: integer|Long expected";
    if (message.result != null && Object.hasOwnProperty.call(message, "result"))
      switch (message.result) {
        default:
          return "result: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
          break;
      }
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      if (
        !$util.isInteger(message.createdTime) &&
        !(
          message.createdTime &&
          $util.isInteger(message.createdTime.low) &&
          $util.isInteger(message.createdTime.high)
        )
      )
        return "createdTime: integer|Long expected";
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      if (
        !$util.isInteger(message.updatedTime) &&
        !(
          message.updatedTime &&
          $util.isInteger(message.updatedTime.low) &&
          $util.isInteger(message.updatedTime.high)
        )
      )
        return "updatedTime: integer|Long expected";
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      switch (message.state) {
        default:
          return "state: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      if (!$util.isInteger(message.lock)) return "lock: integer expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    if (
      message.shootingTasks != null &&
      Object.hasOwnProperty.call(message, "shootingTasks")
    ) {
      if (!Array.isArray(message.shootingTasks))
        return "shootingTasks: array expected";
      for (var i = 0; i < message.shootingTasks.length; ++i) {
        var error = $root.ShootingTaskMsg.verify(
          message.shootingTasks[i],
          long + 1,
        );
        if (error) return "shootingTasks." + error;
      }
    }
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      if (!$util.isInteger(message.paramMode))
        return "paramMode: integer expected";
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      if (!$util.isInteger(message.paramVersion))
        return "paramVersion: integer expected";
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      if (!$util.isString(message.params)) return "params: string expected";
    if (
      message.scheduleTime != null &&
      Object.hasOwnProperty.call(message, "scheduleTime")
    )
      if (
        !$util.isInteger(message.scheduleTime) &&
        !(
          message.scheduleTime &&
          $util.isInteger(message.scheduleTime.low) &&
          $util.isInteger(message.scheduleTime.high)
        )
      )
        return "scheduleTime: integer|Long expected";
    if (
      message.syncState != null &&
      Object.hasOwnProperty.call(message, "syncState")
    )
      switch (message.syncState) {
        default:
          return "syncState: enum value expected";
        case 0:
        case 1:
          break;
      }
    return null;
  };

  /**
   * Creates a ShootingScheduleMsg message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ShootingScheduleMsg
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ShootingScheduleMsg} ShootingScheduleMsg
   */
  ShootingScheduleMsg.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ShootingScheduleMsg) return object;
    if (!$util.isObject(object))
      throw TypeError(".ShootingScheduleMsg: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ShootingScheduleMsg();
    if (object.scheduleId != null)
      message.scheduleId = String(object.scheduleId);
    if (object.scheduleName != null)
      message.scheduleName = String(object.scheduleName);
    if (object.deviceId != null) message.deviceId = object.deviceId | 0;
    if (object.macAddress != null)
      message.macAddress = String(object.macAddress);
    if (object.startTime != null)
      if ($util.Long)
        message.startTime = $util.Long.fromValue(object.startTime, false);
      else if (typeof object.startTime === "string")
        message.startTime = parseInt(object.startTime, 10);
      else if (typeof object.startTime === "number")
        message.startTime = object.startTime;
      else if (typeof object.startTime === "object")
        message.startTime = new $util.LongBits(
          object.startTime.low >>> 0,
          object.startTime.high >>> 0,
        ).toNumber();
    if (object.endTime != null)
      if ($util.Long)
        message.endTime = $util.Long.fromValue(object.endTime, false);
      else if (typeof object.endTime === "string")
        message.endTime = parseInt(object.endTime, 10);
      else if (typeof object.endTime === "number")
        message.endTime = object.endTime;
      else if (typeof object.endTime === "object")
        message.endTime = new $util.LongBits(
          object.endTime.low >>> 0,
          object.endTime.high >>> 0,
        ).toNumber();
    switch (object.result) {
      default:
        if (typeof object.result === "number") {
          message.result = object.result;
          break;
        }
        break;
      case "SHOOTING_SCHEDULE_RESULT_PENDING_START":
      case 0:
        message.result = 0;
        break;
      case "SHOOTING_SCHEDULE_RESULT_ALL_COMPLETED":
      case 1:
        message.result = 1;
        break;
      case "SHOOTING_SCHEDULE_RESULT_PARTIALLY_COMPLETED":
      case 2:
        message.result = 2;
        break;
      case "SHOOTING_SCHEDULE_RESULT_ALL_FAILED":
      case 3:
        message.result = 3;
        break;
    }
    if (object.createdTime != null)
      if ($util.Long)
        message.createdTime = $util.Long.fromValue(object.createdTime, false);
      else if (typeof object.createdTime === "string")
        message.createdTime = parseInt(object.createdTime, 10);
      else if (typeof object.createdTime === "number")
        message.createdTime = object.createdTime;
      else if (typeof object.createdTime === "object")
        message.createdTime = new $util.LongBits(
          object.createdTime.low >>> 0,
          object.createdTime.high >>> 0,
        ).toNumber();
    if (object.updatedTime != null)
      if ($util.Long)
        message.updatedTime = $util.Long.fromValue(object.updatedTime, false);
      else if (typeof object.updatedTime === "string")
        message.updatedTime = parseInt(object.updatedTime, 10);
      else if (typeof object.updatedTime === "number")
        message.updatedTime = object.updatedTime;
      else if (typeof object.updatedTime === "object")
        message.updatedTime = new $util.LongBits(
          object.updatedTime.low >>> 0,
          object.updatedTime.high >>> 0,
        ).toNumber();
    switch (object.state) {
      default:
        if (typeof object.state === "number") {
          message.state = object.state;
          break;
        }
        break;
      case "SHOOTING_SCHEDULE_STATE_INITIALIZED":
      case 0:
        message.state = 0;
        break;
      case "SHOOTING_SCHEDULE_STATE_PENDING_SHOOT":
      case 1:
        message.state = 1;
        break;
      case "SHOOTING_SCHEDULE_STATE_SHOOTING":
      case 2:
        message.state = 2;
        break;
      case "SHOOTING_SCHEDULE_STATE_COMPLETED":
      case 3:
        message.state = 3;
        break;
      case "SHOOTING_SCHEDULE_STATE_EXPIRED":
      case 4:
        message.state = 4;
        break;
    }
    if (object.lock != null) message.lock = object.lock | 0;
    if (object.password != null) message.password = String(object.password);
    if (object.shootingTasks) {
      if (!Array.isArray(object.shootingTasks))
        throw TypeError(".ShootingScheduleMsg.shootingTasks: array expected");
      message.shootingTasks = [];
      for (var i = 0; i < object.shootingTasks.length; ++i) {
        if (!$util.isObject(object.shootingTasks[i]))
          throw TypeError(
            ".ShootingScheduleMsg.shootingTasks: object expected",
          );
        message.shootingTasks[i] = $root.ShootingTaskMsg.fromObject(
          object.shootingTasks[i],
          long + 1,
        );
      }
    }
    if (object.paramMode != null) message.paramMode = object.paramMode | 0;
    if (object.paramVersion != null)
      message.paramVersion = object.paramVersion | 0;
    if (object.params != null) message.params = String(object.params);
    if (object.scheduleTime != null)
      if ($util.Long)
        message.scheduleTime = $util.Long.fromValue(object.scheduleTime, false);
      else if (typeof object.scheduleTime === "string")
        message.scheduleTime = parseInt(object.scheduleTime, 10);
      else if (typeof object.scheduleTime === "number")
        message.scheduleTime = object.scheduleTime;
      else if (typeof object.scheduleTime === "object")
        message.scheduleTime = new $util.LongBits(
          object.scheduleTime.low >>> 0,
          object.scheduleTime.high >>> 0,
        ).toNumber();
    switch (object.syncState) {
      default:
        if (typeof object.syncState === "number") {
          message.syncState = object.syncState;
          break;
        }
        break;
      case "SHOOTING_SCHEDULE_SYNC_STATE_PENDING_SYNC":
      case 0:
        message.syncState = 0;
        break;
      case "SHOOTING_SCHEDULE_SYNC_STATE_SYNCED":
      case 1:
        message.syncState = 1;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a ShootingScheduleMsg message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ShootingScheduleMsg
   * @static
   * @param {ShootingScheduleMsg} message ShootingScheduleMsg
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ShootingScheduleMsg.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.shootingTasks = [];
    if (options.defaults) {
      object.scheduleId = "";
      object.scheduleName = "";
      object.deviceId = 0;
      object.macAddress = "";
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.startTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.startTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.endTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.endTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.result =
        options.enums === String ? "SHOOTING_SCHEDULE_RESULT_PENDING_START" : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.createdTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.createdTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.updatedTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.updatedTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.state =
        options.enums === String ? "SHOOTING_SCHEDULE_STATE_INITIALIZED" : 0;
      object.lock = 0;
      object.password = "";
      object.paramMode = 0;
      object.paramVersion = 0;
      object.params = "";
      if ($util.Long) {
        var long = new $util.Long(0, 0, false);
        object.scheduleTime =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.scheduleTime =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.syncState =
        options.enums === String
          ? "SHOOTING_SCHEDULE_SYNC_STATE_PENDING_SYNC"
          : 0;
    }
    if (
      message.scheduleId != null &&
      Object.hasOwnProperty.call(message, "scheduleId")
    )
      object.scheduleId = message.scheduleId;
    if (
      message.scheduleName != null &&
      Object.hasOwnProperty.call(message, "scheduleName")
    )
      object.scheduleName = message.scheduleName;
    if (
      message.deviceId != null &&
      Object.hasOwnProperty.call(message, "deviceId")
    )
      object.deviceId = message.deviceId;
    if (
      message.macAddress != null &&
      Object.hasOwnProperty.call(message, "macAddress")
    )
      object.macAddress = message.macAddress;
    if (
      message.startTime != null &&
      Object.hasOwnProperty.call(message, "startTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.startTime =
          typeof message.startTime === "number"
            ? BigInt(message.startTime)
            : $util.Long.fromBits(
                message.startTime.low >>> 0,
                message.startTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.startTime === "number")
        object.startTime =
          options.longs === String
            ? String(message.startTime)
            : message.startTime;
      else
        object.startTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.startTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.startTime.low >>> 0,
                  message.startTime.high >>> 0,
                ).toNumber()
              : message.startTime;
    if (
      message.endTime != null &&
      Object.hasOwnProperty.call(message, "endTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.endTime =
          typeof message.endTime === "number"
            ? BigInt(message.endTime)
            : $util.Long.fromBits(
                message.endTime.low >>> 0,
                message.endTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.endTime === "number")
        object.endTime =
          options.longs === String ? String(message.endTime) : message.endTime;
      else
        object.endTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.endTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.endTime.low >>> 0,
                  message.endTime.high >>> 0,
                ).toNumber()
              : message.endTime;
    if (message.result != null && Object.hasOwnProperty.call(message, "result"))
      object.result =
        options.enums === String
          ? $root.ShootingScheduleResult[message.result] === undefined
            ? message.result
            : $root.ShootingScheduleResult[message.result]
          : message.result;
    if (
      message.createdTime != null &&
      Object.hasOwnProperty.call(message, "createdTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.createdTime =
          typeof message.createdTime === "number"
            ? BigInt(message.createdTime)
            : $util.Long.fromBits(
                message.createdTime.low >>> 0,
                message.createdTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.createdTime === "number")
        object.createdTime =
          options.longs === String
            ? String(message.createdTime)
            : message.createdTime;
      else
        object.createdTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.createdTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.createdTime.low >>> 0,
                  message.createdTime.high >>> 0,
                ).toNumber()
              : message.createdTime;
    if (
      message.updatedTime != null &&
      Object.hasOwnProperty.call(message, "updatedTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.updatedTime =
          typeof message.updatedTime === "number"
            ? BigInt(message.updatedTime)
            : $util.Long.fromBits(
                message.updatedTime.low >>> 0,
                message.updatedTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.updatedTime === "number")
        object.updatedTime =
          options.longs === String
            ? String(message.updatedTime)
            : message.updatedTime;
      else
        object.updatedTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.updatedTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.updatedTime.low >>> 0,
                  message.updatedTime.high >>> 0,
                ).toNumber()
              : message.updatedTime;
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state =
        options.enums === String
          ? $root.ShootingScheduleState[message.state] === undefined
            ? message.state
            : $root.ShootingScheduleState[message.state]
          : message.state;
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      object.lock = message.lock;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    if (message.shootingTasks && message.shootingTasks.length) {
      object.shootingTasks = [];
      for (var j = 0; j < message.shootingTasks.length; ++j)
        object.shootingTasks[j] = $root.ShootingTaskMsg.toObject(
          message.shootingTasks[j],
          options,
          q + 1,
        );
    }
    if (
      message.paramMode != null &&
      Object.hasOwnProperty.call(message, "paramMode")
    )
      object.paramMode = message.paramMode;
    if (
      message.paramVersion != null &&
      Object.hasOwnProperty.call(message, "paramVersion")
    )
      object.paramVersion = message.paramVersion;
    if (message.params != null && Object.hasOwnProperty.call(message, "params"))
      object.params = message.params;
    if (
      message.scheduleTime != null &&
      Object.hasOwnProperty.call(message, "scheduleTime")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.scheduleTime =
          typeof message.scheduleTime === "number"
            ? BigInt(message.scheduleTime)
            : $util.Long.fromBits(
                message.scheduleTime.low >>> 0,
                message.scheduleTime.high >>> 0,
                false,
              ).toBigInt();
      else if (typeof message.scheduleTime === "number")
        object.scheduleTime =
          options.longs === String
            ? String(message.scheduleTime)
            : message.scheduleTime;
      else
        object.scheduleTime =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.scheduleTime)
            : options.longs === Number
              ? new $util.LongBits(
                  message.scheduleTime.low >>> 0,
                  message.scheduleTime.high >>> 0,
                ).toNumber()
              : message.scheduleTime;
    if (
      message.syncState != null &&
      Object.hasOwnProperty.call(message, "syncState")
    )
      object.syncState =
        options.enums === String
          ? $root.ShootingScheduleSyncState[message.syncState] === undefined
            ? message.syncState
            : $root.ShootingScheduleSyncState[message.syncState]
          : message.syncState;
    return object;
  };

  /**
   * Converts this ShootingScheduleMsg to JSON.
   * @function toJSON
   * @memberof ShootingScheduleMsg
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ShootingScheduleMsg.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ShootingScheduleMsg
   * @function getTypeUrl
   * @memberof ShootingScheduleMsg
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ShootingScheduleMsg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ShootingScheduleMsg";
  };

  return ShootingScheduleMsg;
})();

$root.ReqSyncShootingSchedule = (function () {
  /**
   * Properties of a ReqSyncShootingSchedule.
   * @exports IReqSyncShootingSchedule
   * @interface IReqSyncShootingSchedule
   * @property {IShootingScheduleMsg|null} [shootingSchedule] ReqSyncShootingSchedule shootingSchedule
   */

  /**
   * Constructs a new ReqSyncShootingSchedule.
   * @exports ReqSyncShootingSchedule
   * @classdesc Represents a ReqSyncShootingSchedule.
   * @implements IReqSyncShootingSchedule
   * @constructor
   * @param {IReqSyncShootingSchedule=} [properties] Properties to set
   */
  function ReqSyncShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSyncShootingSchedule shootingSchedule.
   * @member {IShootingScheduleMsg|null|undefined} shootingSchedule
   * @memberof ReqSyncShootingSchedule
   * @instance
   */
  ReqSyncShootingSchedule.prototype.shootingSchedule = null;

  /**
   * Creates a new ReqSyncShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {IReqSyncShootingSchedule=} [properties] Properties to set
   * @returns {ReqSyncShootingSchedule} ReqSyncShootingSchedule instance
   */
  ReqSyncShootingSchedule.create = function create(properties) {
    return new ReqSyncShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqSyncShootingSchedule message. Does not implicitly {@link ReqSyncShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {IReqSyncShootingSchedule} message ReqSyncShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSyncShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      $root.ShootingScheduleMsg.encode(
        message.shootingSchedule,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqSyncShootingSchedule message, length delimited. Does not implicitly {@link ReqSyncShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {IReqSyncShootingSchedule} message ReqSyncShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSyncShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSyncShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSyncShootingSchedule} ReqSyncShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSyncShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSyncShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingSchedule = $root.ShootingScheduleMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSyncShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSyncShootingSchedule} ReqSyncShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSyncShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSyncShootingSchedule message.
   * @function verify
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSyncShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      var error = $root.ShootingScheduleMsg.verify(
        message.shootingSchedule,
        long + 1,
      );
      if (error) return "shootingSchedule." + error;
    }
    return null;
  };

  /**
   * Creates a ReqSyncShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSyncShootingSchedule} ReqSyncShootingSchedule
   */
  ReqSyncShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSyncShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSyncShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSyncShootingSchedule();
    if (object.shootingSchedule != null) {
      if (!$util.isObject(object.shootingSchedule))
        throw TypeError(
          ".ReqSyncShootingSchedule.shootingSchedule: object expected",
        );
      message.shootingSchedule = $root.ShootingScheduleMsg.fromObject(
        object.shootingSchedule,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqSyncShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {ReqSyncShootingSchedule} message ReqSyncShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSyncShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.shootingSchedule = null;
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      object.shootingSchedule = $root.ShootingScheduleMsg.toObject(
        message.shootingSchedule,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ReqSyncShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqSyncShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSyncShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSyncShootingSchedule
   * @function getTypeUrl
   * @memberof ReqSyncShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSyncShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSyncShootingSchedule";
  };

  return ReqSyncShootingSchedule;
})();

$root.ResSyncShootingSchedule = (function () {
  /**
   * Properties of a ResSyncShootingSchedule.
   * @exports IResSyncShootingSchedule
   * @interface IResSyncShootingSchedule
   * @property {IShootingScheduleMsg|null} [shootingSchedule] ResSyncShootingSchedule shootingSchedule
   * @property {Array.<string>|null} [timeConflictScheduleIds] ResSyncShootingSchedule timeConflictScheduleIds
   * @property {number|null} [code] ResSyncShootingSchedule code
   * @property {boolean|null} [canReplace] ResSyncShootingSchedule canReplace
   */

  /**
   * Constructs a new ResSyncShootingSchedule.
   * @exports ResSyncShootingSchedule
   * @classdesc Represents a ResSyncShootingSchedule.
   * @implements IResSyncShootingSchedule
   * @constructor
   * @param {IResSyncShootingSchedule=} [properties] Properties to set
   */
  function ResSyncShootingSchedule(properties) {
    this.timeConflictScheduleIds = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSyncShootingSchedule shootingSchedule.
   * @member {IShootingScheduleMsg|null|undefined} shootingSchedule
   * @memberof ResSyncShootingSchedule
   * @instance
   */
  ResSyncShootingSchedule.prototype.shootingSchedule = null;

  /**
   * ResSyncShootingSchedule timeConflictScheduleIds.
   * @member {Array.<string>} timeConflictScheduleIds
   * @memberof ResSyncShootingSchedule
   * @instance
   */
  ResSyncShootingSchedule.prototype.timeConflictScheduleIds = $util.emptyArray;

  /**
   * ResSyncShootingSchedule code.
   * @member {number} code
   * @memberof ResSyncShootingSchedule
   * @instance
   */
  ResSyncShootingSchedule.prototype.code = 0;

  /**
   * ResSyncShootingSchedule canReplace.
   * @member {boolean} canReplace
   * @memberof ResSyncShootingSchedule
   * @instance
   */
  ResSyncShootingSchedule.prototype.canReplace = false;

  /**
   * Creates a new ResSyncShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {IResSyncShootingSchedule=} [properties] Properties to set
   * @returns {ResSyncShootingSchedule} ResSyncShootingSchedule instance
   */
  ResSyncShootingSchedule.create = function create(properties) {
    return new ResSyncShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResSyncShootingSchedule message. Does not implicitly {@link ResSyncShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {IResSyncShootingSchedule} message ResSyncShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSyncShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      $root.ShootingScheduleMsg.encode(
        message.shootingSchedule,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.timeConflictScheduleIds != null &&
      message.timeConflictScheduleIds.length
    )
      for (var i = 0; i < message.timeConflictScheduleIds.length; ++i)
        writer
          .uint32(/* id 2, wireType 2 =*/ 18)
          .string(message.timeConflictScheduleIds[i]);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.code);
    if (
      message.canReplace != null &&
      Object.hasOwnProperty.call(message, "canReplace")
    )
      writer.uint32(/* id 4, wireType 0 =*/ 32).bool(message.canReplace);
    return writer;
  };

  /**
   * Encodes the specified ResSyncShootingSchedule message, length delimited. Does not implicitly {@link ResSyncShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {IResSyncShootingSchedule} message ResSyncShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSyncShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSyncShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSyncShootingSchedule} ResSyncShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSyncShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSyncShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingSchedule = $root.ShootingScheduleMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          if (!(
            message.timeConflictScheduleIds &&
            message.timeConflictScheduleIds.length
          ))
            message.timeConflictScheduleIds = [];
          message.timeConflictScheduleIds.push(reader.string());
          break;
        }
        case 3: {
          message.code = reader.int32();
          break;
        }
        case 4: {
          message.canReplace = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSyncShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSyncShootingSchedule} ResSyncShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSyncShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSyncShootingSchedule message.
   * @function verify
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSyncShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      var error = $root.ShootingScheduleMsg.verify(
        message.shootingSchedule,
        long + 1,
      );
      if (error) return "shootingSchedule." + error;
    }
    if (
      message.timeConflictScheduleIds != null &&
      Object.hasOwnProperty.call(message, "timeConflictScheduleIds")
    ) {
      if (!Array.isArray(message.timeConflictScheduleIds))
        return "timeConflictScheduleIds: array expected";
      for (var i = 0; i < message.timeConflictScheduleIds.length; ++i)
        if (!$util.isString(message.timeConflictScheduleIds[i]))
          return "timeConflictScheduleIds: string[] expected";
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.canReplace != null &&
      Object.hasOwnProperty.call(message, "canReplace")
    )
      if (typeof message.canReplace !== "boolean")
        return "canReplace: boolean expected";
    return null;
  };

  /**
   * Creates a ResSyncShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSyncShootingSchedule} ResSyncShootingSchedule
   */
  ResSyncShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSyncShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResSyncShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSyncShootingSchedule();
    if (object.shootingSchedule != null) {
      if (!$util.isObject(object.shootingSchedule))
        throw TypeError(
          ".ResSyncShootingSchedule.shootingSchedule: object expected",
        );
      message.shootingSchedule = $root.ShootingScheduleMsg.fromObject(
        object.shootingSchedule,
        long + 1,
      );
    }
    if (object.timeConflictScheduleIds) {
      if (!Array.isArray(object.timeConflictScheduleIds))
        throw TypeError(
          ".ResSyncShootingSchedule.timeConflictScheduleIds: array expected",
        );
      message.timeConflictScheduleIds = [];
      for (var i = 0; i < object.timeConflictScheduleIds.length; ++i)
        message.timeConflictScheduleIds[i] = String(
          object.timeConflictScheduleIds[i],
        );
    }
    if (object.code != null) message.code = object.code | 0;
    if (object.canReplace != null)
      message.canReplace = Boolean(object.canReplace);
    return message;
  };

  /**
   * Creates a plain object from a ResSyncShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {ResSyncShootingSchedule} message ResSyncShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSyncShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.timeConflictScheduleIds = [];
    if (options.defaults) {
      object.shootingSchedule = null;
      object.code = 0;
      object.canReplace = false;
    }
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      object.shootingSchedule = $root.ShootingScheduleMsg.toObject(
        message.shootingSchedule,
        options,
        q + 1,
      );
    if (
      message.timeConflictScheduleIds &&
      message.timeConflictScheduleIds.length
    ) {
      object.timeConflictScheduleIds = [];
      for (var j = 0; j < message.timeConflictScheduleIds.length; ++j)
        object.timeConflictScheduleIds[j] = message.timeConflictScheduleIds[j];
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.canReplace != null &&
      Object.hasOwnProperty.call(message, "canReplace")
    )
      object.canReplace = message.canReplace;
    return object;
  };

  /**
   * Converts this ResSyncShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResSyncShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSyncShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSyncShootingSchedule
   * @function getTypeUrl
   * @memberof ResSyncShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSyncShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSyncShootingSchedule";
  };

  return ResSyncShootingSchedule;
})();

$root.ReqCancelShootingSchedule = (function () {
  /**
   * Properties of a ReqCancelShootingSchedule.
   * @exports IReqCancelShootingSchedule
   * @interface IReqCancelShootingSchedule
   * @property {string|null} [id] ReqCancelShootingSchedule id
   * @property {string|null} [password] ReqCancelShootingSchedule password
   */

  /**
   * Constructs a new ReqCancelShootingSchedule.
   * @exports ReqCancelShootingSchedule
   * @classdesc Represents a ReqCancelShootingSchedule.
   * @implements IReqCancelShootingSchedule
   * @constructor
   * @param {IReqCancelShootingSchedule=} [properties] Properties to set
   */
  function ReqCancelShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqCancelShootingSchedule id.
   * @member {string} id
   * @memberof ReqCancelShootingSchedule
   * @instance
   */
  ReqCancelShootingSchedule.prototype.id = "";

  /**
   * ReqCancelShootingSchedule password.
   * @member {string} password
   * @memberof ReqCancelShootingSchedule
   * @instance
   */
  ReqCancelShootingSchedule.prototype.password = "";

  /**
   * Creates a new ReqCancelShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {IReqCancelShootingSchedule=} [properties] Properties to set
   * @returns {ReqCancelShootingSchedule} ReqCancelShootingSchedule instance
   */
  ReqCancelShootingSchedule.create = function create(properties) {
    return new ReqCancelShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqCancelShootingSchedule message. Does not implicitly {@link ReqCancelShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {IReqCancelShootingSchedule} message ReqCancelShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCancelShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.password);
    return writer;
  };

  /**
   * Encodes the specified ReqCancelShootingSchedule message, length delimited. Does not implicitly {@link ReqCancelShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {IReqCancelShootingSchedule} message ReqCancelShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqCancelShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqCancelShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqCancelShootingSchedule} ReqCancelShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCancelShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqCancelShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.password = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqCancelShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqCancelShootingSchedule} ReqCancelShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqCancelShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqCancelShootingSchedule message.
   * @function verify
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqCancelShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    return null;
  };

  /**
   * Creates a ReqCancelShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqCancelShootingSchedule} ReqCancelShootingSchedule
   */
  ReqCancelShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqCancelShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqCancelShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqCancelShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.password != null) message.password = String(object.password);
    return message;
  };

  /**
   * Creates a plain object from a ReqCancelShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {ReqCancelShootingSchedule} message ReqCancelShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqCancelShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.password = "";
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    return object;
  };

  /**
   * Converts this ReqCancelShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqCancelShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqCancelShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqCancelShootingSchedule
   * @function getTypeUrl
   * @memberof ReqCancelShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqCancelShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqCancelShootingSchedule";
  };

  return ReqCancelShootingSchedule;
})();

$root.ResCancelShootingSchedule = (function () {
  /**
   * Properties of a ResCancelShootingSchedule.
   * @exports IResCancelShootingSchedule
   * @interface IResCancelShootingSchedule
   * @property {string|null} [id] ResCancelShootingSchedule id
   * @property {number|null} [code] ResCancelShootingSchedule code
   */

  /**
   * Constructs a new ResCancelShootingSchedule.
   * @exports ResCancelShootingSchedule
   * @classdesc Represents a ResCancelShootingSchedule.
   * @implements IResCancelShootingSchedule
   * @constructor
   * @param {IResCancelShootingSchedule=} [properties] Properties to set
   */
  function ResCancelShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResCancelShootingSchedule id.
   * @member {string} id
   * @memberof ResCancelShootingSchedule
   * @instance
   */
  ResCancelShootingSchedule.prototype.id = "";

  /**
   * ResCancelShootingSchedule code.
   * @member {number} code
   * @memberof ResCancelShootingSchedule
   * @instance
   */
  ResCancelShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResCancelShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {IResCancelShootingSchedule=} [properties] Properties to set
   * @returns {ResCancelShootingSchedule} ResCancelShootingSchedule instance
   */
  ResCancelShootingSchedule.create = function create(properties) {
    return new ResCancelShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResCancelShootingSchedule message. Does not implicitly {@link ResCancelShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {IResCancelShootingSchedule} message ResCancelShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCancelShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResCancelShootingSchedule message, length delimited. Does not implicitly {@link ResCancelShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {IResCancelShootingSchedule} message ResCancelShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResCancelShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResCancelShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResCancelShootingSchedule} ResCancelShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCancelShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResCancelShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResCancelShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResCancelShootingSchedule} ResCancelShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResCancelShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResCancelShootingSchedule message.
   * @function verify
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResCancelShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResCancelShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResCancelShootingSchedule} ResCancelShootingSchedule
   */
  ResCancelShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResCancelShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResCancelShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResCancelShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResCancelShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {ResCancelShootingSchedule} message ResCancelShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResCancelShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.code = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResCancelShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResCancelShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResCancelShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResCancelShootingSchedule
   * @function getTypeUrl
   * @memberof ResCancelShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResCancelShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResCancelShootingSchedule";
  };

  return ResCancelShootingSchedule;
})();

$root.ReqGetAllShootingSchedule = (function () {
  /**
   * Properties of a ReqGetAllShootingSchedule.
   * @exports IReqGetAllShootingSchedule
   * @interface IReqGetAllShootingSchedule
   */

  /**
   * Constructs a new ReqGetAllShootingSchedule.
   * @exports ReqGetAllShootingSchedule
   * @classdesc Represents a ReqGetAllShootingSchedule.
   * @implements IReqGetAllShootingSchedule
   * @constructor
   * @param {IReqGetAllShootingSchedule=} [properties] Properties to set
   */
  function ReqGetAllShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetAllShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {IReqGetAllShootingSchedule=} [properties] Properties to set
   * @returns {ReqGetAllShootingSchedule} ReqGetAllShootingSchedule instance
   */
  ReqGetAllShootingSchedule.create = function create(properties) {
    return new ReqGetAllShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqGetAllShootingSchedule message. Does not implicitly {@link ReqGetAllShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {IReqGetAllShootingSchedule} message ReqGetAllShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetAllShootingSchedule message, length delimited. Does not implicitly {@link ReqGetAllShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {IReqGetAllShootingSchedule} message ReqGetAllShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetAllShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetAllShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetAllShootingSchedule} ReqGetAllShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetAllShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetAllShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetAllShootingSchedule} ReqGetAllShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetAllShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetAllShootingSchedule message.
   * @function verify
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetAllShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetAllShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetAllShootingSchedule} ReqGetAllShootingSchedule
   */
  ReqGetAllShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetAllShootingSchedule) return object;
    return new $root.ReqGetAllShootingSchedule();
  };

  /**
   * Creates a plain object from a ReqGetAllShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {ReqGetAllShootingSchedule} message ReqGetAllShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetAllShootingSchedule.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetAllShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqGetAllShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetAllShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetAllShootingSchedule
   * @function getTypeUrl
   * @memberof ReqGetAllShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetAllShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetAllShootingSchedule";
  };

  return ReqGetAllShootingSchedule;
})();

$root.ResGetAllShootingSchedule = (function () {
  /**
   * Properties of a ResGetAllShootingSchedule.
   * @exports IResGetAllShootingSchedule
   * @interface IResGetAllShootingSchedule
   * @property {Array.<IShootingScheduleMsg>|null} [shootingSchedule] ResGetAllShootingSchedule shootingSchedule
   * @property {number|null} [code] ResGetAllShootingSchedule code
   */

  /**
   * Constructs a new ResGetAllShootingSchedule.
   * @exports ResGetAllShootingSchedule
   * @classdesc Represents a ResGetAllShootingSchedule.
   * @implements IResGetAllShootingSchedule
   * @constructor
   * @param {IResGetAllShootingSchedule=} [properties] Properties to set
   */
  function ResGetAllShootingSchedule(properties) {
    this.shootingSchedule = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetAllShootingSchedule shootingSchedule.
   * @member {Array.<IShootingScheduleMsg>} shootingSchedule
   * @memberof ResGetAllShootingSchedule
   * @instance
   */
  ResGetAllShootingSchedule.prototype.shootingSchedule = $util.emptyArray;

  /**
   * ResGetAllShootingSchedule code.
   * @member {number} code
   * @memberof ResGetAllShootingSchedule
   * @instance
   */
  ResGetAllShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResGetAllShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {IResGetAllShootingSchedule=} [properties] Properties to set
   * @returns {ResGetAllShootingSchedule} ResGetAllShootingSchedule instance
   */
  ResGetAllShootingSchedule.create = function create(properties) {
    return new ResGetAllShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResGetAllShootingSchedule message. Does not implicitly {@link ResGetAllShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {IResGetAllShootingSchedule} message ResGetAllShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.shootingSchedule != null && message.shootingSchedule.length)
      for (var i = 0; i < message.shootingSchedule.length; ++i)
        $root.ShootingScheduleMsg.encode(
          message.shootingSchedule[i],
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
          q + 1,
        ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResGetAllShootingSchedule message, length delimited. Does not implicitly {@link ResGetAllShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {IResGetAllShootingSchedule} message ResGetAllShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetAllShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetAllShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetAllShootingSchedule} ResGetAllShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetAllShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          if (!(message.shootingSchedule && message.shootingSchedule.length))
            message.shootingSchedule = [];
          message.shootingSchedule.push(
            $root.ShootingScheduleMsg.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetAllShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetAllShootingSchedule} ResGetAllShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetAllShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetAllShootingSchedule message.
   * @function verify
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetAllShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      if (!Array.isArray(message.shootingSchedule))
        return "shootingSchedule: array expected";
      for (var i = 0; i < message.shootingSchedule.length; ++i) {
        var error = $root.ShootingScheduleMsg.verify(
          message.shootingSchedule[i],
          long + 1,
        );
        if (error) return "shootingSchedule." + error;
      }
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResGetAllShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetAllShootingSchedule} ResGetAllShootingSchedule
   */
  ResGetAllShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetAllShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetAllShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetAllShootingSchedule();
    if (object.shootingSchedule) {
      if (!Array.isArray(object.shootingSchedule))
        throw TypeError(
          ".ResGetAllShootingSchedule.shootingSchedule: array expected",
        );
      message.shootingSchedule = [];
      for (var i = 0; i < object.shootingSchedule.length; ++i) {
        if (!$util.isObject(object.shootingSchedule[i]))
          throw TypeError(
            ".ResGetAllShootingSchedule.shootingSchedule: object expected",
          );
        message.shootingSchedule[i] = $root.ShootingScheduleMsg.fromObject(
          object.shootingSchedule[i],
          long + 1,
        );
      }
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetAllShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {ResGetAllShootingSchedule} message ResGetAllShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetAllShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.shootingSchedule = [];
    if (options.defaults) object.code = 0;
    if (message.shootingSchedule && message.shootingSchedule.length) {
      object.shootingSchedule = [];
      for (var j = 0; j < message.shootingSchedule.length; ++j)
        object.shootingSchedule[j] = $root.ShootingScheduleMsg.toObject(
          message.shootingSchedule[j],
          options,
          q + 1,
        );
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResGetAllShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResGetAllShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetAllShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetAllShootingSchedule
   * @function getTypeUrl
   * @memberof ResGetAllShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetAllShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetAllShootingSchedule";
  };

  return ResGetAllShootingSchedule;
})();

$root.ReqGetShootingScheduleById = (function () {
  /**
   * Properties of a ReqGetShootingScheduleById.
   * @exports IReqGetShootingScheduleById
   * @interface IReqGetShootingScheduleById
   * @property {string|null} [id] ReqGetShootingScheduleById id
   */

  /**
   * Constructs a new ReqGetShootingScheduleById.
   * @exports ReqGetShootingScheduleById
   * @classdesc Represents a ReqGetShootingScheduleById.
   * @implements IReqGetShootingScheduleById
   * @constructor
   * @param {IReqGetShootingScheduleById=} [properties] Properties to set
   */
  function ReqGetShootingScheduleById(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetShootingScheduleById id.
   * @member {string} id
   * @memberof ReqGetShootingScheduleById
   * @instance
   */
  ReqGetShootingScheduleById.prototype.id = "";

  /**
   * Creates a new ReqGetShootingScheduleById instance using the specified properties.
   * @function create
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {IReqGetShootingScheduleById=} [properties] Properties to set
   * @returns {ReqGetShootingScheduleById} ReqGetShootingScheduleById instance
   */
  ReqGetShootingScheduleById.create = function create(properties) {
    return new ReqGetShootingScheduleById(properties);
  };

  /**
   * Encodes the specified ReqGetShootingScheduleById message. Does not implicitly {@link ReqGetShootingScheduleById.verify|verify} messages.
   * @function encode
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {IReqGetShootingScheduleById} message ReqGetShootingScheduleById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetShootingScheduleById.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    return writer;
  };

  /**
   * Encodes the specified ReqGetShootingScheduleById message, length delimited. Does not implicitly {@link ReqGetShootingScheduleById.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {IReqGetShootingScheduleById} message ReqGetShootingScheduleById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetShootingScheduleById.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetShootingScheduleById message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetShootingScheduleById} ReqGetShootingScheduleById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetShootingScheduleById.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetShootingScheduleById();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetShootingScheduleById message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetShootingScheduleById} ReqGetShootingScheduleById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetShootingScheduleById.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetShootingScheduleById message.
   * @function verify
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetShootingScheduleById.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    return null;
  };

  /**
   * Creates a ReqGetShootingScheduleById message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetShootingScheduleById} ReqGetShootingScheduleById
   */
  ReqGetShootingScheduleById.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetShootingScheduleById) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetShootingScheduleById: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetShootingScheduleById();
    if (object.id != null) message.id = String(object.id);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetShootingScheduleById message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {ReqGetShootingScheduleById} message ReqGetShootingScheduleById
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetShootingScheduleById.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.id = "";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    return object;
  };

  /**
   * Converts this ReqGetShootingScheduleById to JSON.
   * @function toJSON
   * @memberof ReqGetShootingScheduleById
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetShootingScheduleById.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetShootingScheduleById
   * @function getTypeUrl
   * @memberof ReqGetShootingScheduleById
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetShootingScheduleById.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetShootingScheduleById";
  };

  return ReqGetShootingScheduleById;
})();

$root.ResGetShootingScheduleById = (function () {
  /**
   * Properties of a ResGetShootingScheduleById.
   * @exports IResGetShootingScheduleById
   * @interface IResGetShootingScheduleById
   * @property {IShootingScheduleMsg|null} [shootingSchedule] ResGetShootingScheduleById shootingSchedule
   * @property {number|null} [code] ResGetShootingScheduleById code
   */

  /**
   * Constructs a new ResGetShootingScheduleById.
   * @exports ResGetShootingScheduleById
   * @classdesc Represents a ResGetShootingScheduleById.
   * @implements IResGetShootingScheduleById
   * @constructor
   * @param {IResGetShootingScheduleById=} [properties] Properties to set
   */
  function ResGetShootingScheduleById(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetShootingScheduleById shootingSchedule.
   * @member {IShootingScheduleMsg|null|undefined} shootingSchedule
   * @memberof ResGetShootingScheduleById
   * @instance
   */
  ResGetShootingScheduleById.prototype.shootingSchedule = null;

  /**
   * ResGetShootingScheduleById code.
   * @member {number} code
   * @memberof ResGetShootingScheduleById
   * @instance
   */
  ResGetShootingScheduleById.prototype.code = 0;

  /**
   * Creates a new ResGetShootingScheduleById instance using the specified properties.
   * @function create
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {IResGetShootingScheduleById=} [properties] Properties to set
   * @returns {ResGetShootingScheduleById} ResGetShootingScheduleById instance
   */
  ResGetShootingScheduleById.create = function create(properties) {
    return new ResGetShootingScheduleById(properties);
  };

  /**
   * Encodes the specified ResGetShootingScheduleById message. Does not implicitly {@link ResGetShootingScheduleById.verify|verify} messages.
   * @function encode
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {IResGetShootingScheduleById} message ResGetShootingScheduleById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetShootingScheduleById.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      $root.ShootingScheduleMsg.encode(
        message.shootingSchedule,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResGetShootingScheduleById message, length delimited. Does not implicitly {@link ResGetShootingScheduleById.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {IResGetShootingScheduleById} message ResGetShootingScheduleById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetShootingScheduleById.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetShootingScheduleById message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetShootingScheduleById} ResGetShootingScheduleById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetShootingScheduleById.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetShootingScheduleById();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingSchedule = $root.ShootingScheduleMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetShootingScheduleById message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetShootingScheduleById} ResGetShootingScheduleById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetShootingScheduleById.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetShootingScheduleById message.
   * @function verify
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetShootingScheduleById.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      var error = $root.ShootingScheduleMsg.verify(
        message.shootingSchedule,
        long + 1,
      );
      if (error) return "shootingSchedule." + error;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResGetShootingScheduleById message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetShootingScheduleById} ResGetShootingScheduleById
   */
  ResGetShootingScheduleById.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetShootingScheduleById) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetShootingScheduleById: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetShootingScheduleById();
    if (object.shootingSchedule != null) {
      if (!$util.isObject(object.shootingSchedule))
        throw TypeError(
          ".ResGetShootingScheduleById.shootingSchedule: object expected",
        );
      message.shootingSchedule = $root.ShootingScheduleMsg.fromObject(
        object.shootingSchedule,
        long + 1,
      );
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetShootingScheduleById message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {ResGetShootingScheduleById} message ResGetShootingScheduleById
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetShootingScheduleById.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.shootingSchedule = null;
      object.code = 0;
    }
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      object.shootingSchedule = $root.ShootingScheduleMsg.toObject(
        message.shootingSchedule,
        options,
        q + 1,
      );
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResGetShootingScheduleById to JSON.
   * @function toJSON
   * @memberof ResGetShootingScheduleById
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetShootingScheduleById.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetShootingScheduleById
   * @function getTypeUrl
   * @memberof ResGetShootingScheduleById
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetShootingScheduleById.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetShootingScheduleById";
  };

  return ResGetShootingScheduleById;
})();

$root.ReqGetShootingTaskById = (function () {
  /**
   * Properties of a ReqGetShootingTaskById.
   * @exports IReqGetShootingTaskById
   * @interface IReqGetShootingTaskById
   * @property {string|null} [id] ReqGetShootingTaskById id
   */

  /**
   * Constructs a new ReqGetShootingTaskById.
   * @exports ReqGetShootingTaskById
   * @classdesc Represents a ReqGetShootingTaskById.
   * @implements IReqGetShootingTaskById
   * @constructor
   * @param {IReqGetShootingTaskById=} [properties] Properties to set
   */
  function ReqGetShootingTaskById(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqGetShootingTaskById id.
   * @member {string} id
   * @memberof ReqGetShootingTaskById
   * @instance
   */
  ReqGetShootingTaskById.prototype.id = "";

  /**
   * Creates a new ReqGetShootingTaskById instance using the specified properties.
   * @function create
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {IReqGetShootingTaskById=} [properties] Properties to set
   * @returns {ReqGetShootingTaskById} ReqGetShootingTaskById instance
   */
  ReqGetShootingTaskById.create = function create(properties) {
    return new ReqGetShootingTaskById(properties);
  };

  /**
   * Encodes the specified ReqGetShootingTaskById message. Does not implicitly {@link ReqGetShootingTaskById.verify|verify} messages.
   * @function encode
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {IReqGetShootingTaskById} message ReqGetShootingTaskById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetShootingTaskById.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    return writer;
  };

  /**
   * Encodes the specified ReqGetShootingTaskById message, length delimited. Does not implicitly {@link ReqGetShootingTaskById.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {IReqGetShootingTaskById} message ReqGetShootingTaskById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetShootingTaskById.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetShootingTaskById message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetShootingTaskById} ReqGetShootingTaskById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetShootingTaskById.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetShootingTaskById();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetShootingTaskById message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetShootingTaskById} ReqGetShootingTaskById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetShootingTaskById.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetShootingTaskById message.
   * @function verify
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetShootingTaskById.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    return null;
  };

  /**
   * Creates a ReqGetShootingTaskById message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetShootingTaskById} ReqGetShootingTaskById
   */
  ReqGetShootingTaskById.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetShootingTaskById) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqGetShootingTaskById: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqGetShootingTaskById();
    if (object.id != null) message.id = String(object.id);
    return message;
  };

  /**
   * Creates a plain object from a ReqGetShootingTaskById message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {ReqGetShootingTaskById} message ReqGetShootingTaskById
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetShootingTaskById.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.id = "";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    return object;
  };

  /**
   * Converts this ReqGetShootingTaskById to JSON.
   * @function toJSON
   * @memberof ReqGetShootingTaskById
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetShootingTaskById.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetShootingTaskById
   * @function getTypeUrl
   * @memberof ReqGetShootingTaskById
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetShootingTaskById.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetShootingTaskById";
  };

  return ReqGetShootingTaskById;
})();

$root.ResGetShootingTaskById = (function () {
  /**
   * Properties of a ResGetShootingTaskById.
   * @exports IResGetShootingTaskById
   * @interface IResGetShootingTaskById
   * @property {IShootingTaskMsg|null} [shootingTask] ResGetShootingTaskById shootingTask
   * @property {number|null} [code] ResGetShootingTaskById code
   */

  /**
   * Constructs a new ResGetShootingTaskById.
   * @exports ResGetShootingTaskById
   * @classdesc Represents a ResGetShootingTaskById.
   * @implements IResGetShootingTaskById
   * @constructor
   * @param {IResGetShootingTaskById=} [properties] Properties to set
   */
  function ResGetShootingTaskById(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetShootingTaskById shootingTask.
   * @member {IShootingTaskMsg|null|undefined} shootingTask
   * @memberof ResGetShootingTaskById
   * @instance
   */
  ResGetShootingTaskById.prototype.shootingTask = null;

  /**
   * ResGetShootingTaskById code.
   * @member {number} code
   * @memberof ResGetShootingTaskById
   * @instance
   */
  ResGetShootingTaskById.prototype.code = 0;

  /**
   * Creates a new ResGetShootingTaskById instance using the specified properties.
   * @function create
   * @memberof ResGetShootingTaskById
   * @static
   * @param {IResGetShootingTaskById=} [properties] Properties to set
   * @returns {ResGetShootingTaskById} ResGetShootingTaskById instance
   */
  ResGetShootingTaskById.create = function create(properties) {
    return new ResGetShootingTaskById(properties);
  };

  /**
   * Encodes the specified ResGetShootingTaskById message. Does not implicitly {@link ResGetShootingTaskById.verify|verify} messages.
   * @function encode
   * @memberof ResGetShootingTaskById
   * @static
   * @param {IResGetShootingTaskById} message ResGetShootingTaskById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetShootingTaskById.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingTask != null &&
      Object.hasOwnProperty.call(message, "shootingTask")
    )
      $root.ShootingTaskMsg.encode(
        message.shootingTask,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResGetShootingTaskById message, length delimited. Does not implicitly {@link ResGetShootingTaskById.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetShootingTaskById
   * @static
   * @param {IResGetShootingTaskById} message ResGetShootingTaskById message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetShootingTaskById.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetShootingTaskById message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetShootingTaskById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetShootingTaskById} ResGetShootingTaskById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetShootingTaskById.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetShootingTaskById();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingTask = $root.ShootingTaskMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetShootingTaskById message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetShootingTaskById
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetShootingTaskById} ResGetShootingTaskById
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetShootingTaskById.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetShootingTaskById message.
   * @function verify
   * @memberof ResGetShootingTaskById
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetShootingTaskById.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingTask != null &&
      Object.hasOwnProperty.call(message, "shootingTask")
    ) {
      var error = $root.ShootingTaskMsg.verify(message.shootingTask, long + 1);
      if (error) return "shootingTask." + error;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResGetShootingTaskById message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetShootingTaskById
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetShootingTaskById} ResGetShootingTaskById
   */
  ResGetShootingTaskById.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetShootingTaskById) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetShootingTaskById: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetShootingTaskById();
    if (object.shootingTask != null) {
      if (!$util.isObject(object.shootingTask))
        throw TypeError(
          ".ResGetShootingTaskById.shootingTask: object expected",
        );
      message.shootingTask = $root.ShootingTaskMsg.fromObject(
        object.shootingTask,
        long + 1,
      );
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResGetShootingTaskById message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetShootingTaskById
   * @static
   * @param {ResGetShootingTaskById} message ResGetShootingTaskById
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetShootingTaskById.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.shootingTask = null;
      object.code = 0;
    }
    if (
      message.shootingTask != null &&
      Object.hasOwnProperty.call(message, "shootingTask")
    )
      object.shootingTask = $root.ShootingTaskMsg.toObject(
        message.shootingTask,
        options,
        q + 1,
      );
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResGetShootingTaskById to JSON.
   * @function toJSON
   * @memberof ResGetShootingTaskById
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetShootingTaskById.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetShootingTaskById
   * @function getTypeUrl
   * @memberof ResGetShootingTaskById
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetShootingTaskById.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetShootingTaskById";
  };

  return ResGetShootingTaskById;
})();

$root.ReqReplaceShootingSchedule = (function () {
  /**
   * Properties of a ReqReplaceShootingSchedule.
   * @exports IReqReplaceShootingSchedule
   * @interface IReqReplaceShootingSchedule
   * @property {IShootingScheduleMsg|null} [shootingSchedule] ReqReplaceShootingSchedule shootingSchedule
   */

  /**
   * Constructs a new ReqReplaceShootingSchedule.
   * @exports ReqReplaceShootingSchedule
   * @classdesc Represents a ReqReplaceShootingSchedule.
   * @implements IReqReplaceShootingSchedule
   * @constructor
   * @param {IReqReplaceShootingSchedule=} [properties] Properties to set
   */
  function ReqReplaceShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqReplaceShootingSchedule shootingSchedule.
   * @member {IShootingScheduleMsg|null|undefined} shootingSchedule
   * @memberof ReqReplaceShootingSchedule
   * @instance
   */
  ReqReplaceShootingSchedule.prototype.shootingSchedule = null;

  /**
   * Creates a new ReqReplaceShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {IReqReplaceShootingSchedule=} [properties] Properties to set
   * @returns {ReqReplaceShootingSchedule} ReqReplaceShootingSchedule instance
   */
  ReqReplaceShootingSchedule.create = function create(properties) {
    return new ReqReplaceShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqReplaceShootingSchedule message. Does not implicitly {@link ReqReplaceShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {IReqReplaceShootingSchedule} message ReqReplaceShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReplaceShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      $root.ShootingScheduleMsg.encode(
        message.shootingSchedule,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqReplaceShootingSchedule message, length delimited. Does not implicitly {@link ReqReplaceShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {IReqReplaceShootingSchedule} message ReqReplaceShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqReplaceShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqReplaceShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqReplaceShootingSchedule} ReqReplaceShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReplaceShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqReplaceShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingSchedule = $root.ShootingScheduleMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqReplaceShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqReplaceShootingSchedule} ReqReplaceShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqReplaceShootingSchedule.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqReplaceShootingSchedule message.
   * @function verify
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqReplaceShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      var error = $root.ShootingScheduleMsg.verify(
        message.shootingSchedule,
        long + 1,
      );
      if (error) return "shootingSchedule." + error;
    }
    return null;
  };

  /**
   * Creates a ReqReplaceShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqReplaceShootingSchedule} ReqReplaceShootingSchedule
   */
  ReqReplaceShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqReplaceShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqReplaceShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqReplaceShootingSchedule();
    if (object.shootingSchedule != null) {
      if (!$util.isObject(object.shootingSchedule))
        throw TypeError(
          ".ReqReplaceShootingSchedule.shootingSchedule: object expected",
        );
      message.shootingSchedule = $root.ShootingScheduleMsg.fromObject(
        object.shootingSchedule,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqReplaceShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {ReqReplaceShootingSchedule} message ReqReplaceShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqReplaceShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.shootingSchedule = null;
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      object.shootingSchedule = $root.ShootingScheduleMsg.toObject(
        message.shootingSchedule,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ReqReplaceShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqReplaceShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqReplaceShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqReplaceShootingSchedule
   * @function getTypeUrl
   * @memberof ReqReplaceShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqReplaceShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqReplaceShootingSchedule";
  };

  return ReqReplaceShootingSchedule;
})();

$root.ResReplaceShootingSchedule = (function () {
  /**
   * Properties of a ResReplaceShootingSchedule.
   * @exports IResReplaceShootingSchedule
   * @interface IResReplaceShootingSchedule
   * @property {IShootingScheduleMsg|null} [shootingSchedule] ResReplaceShootingSchedule shootingSchedule
   * @property {Array.<IShootingScheduleMsg>|null} [replacedShootingSchedule] ResReplaceShootingSchedule replacedShootingSchedule
   * @property {number|null} [code] ResReplaceShootingSchedule code
   */

  /**
   * Constructs a new ResReplaceShootingSchedule.
   * @exports ResReplaceShootingSchedule
   * @classdesc Represents a ResReplaceShootingSchedule.
   * @implements IResReplaceShootingSchedule
   * @constructor
   * @param {IResReplaceShootingSchedule=} [properties] Properties to set
   */
  function ResReplaceShootingSchedule(properties) {
    this.replacedShootingSchedule = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResReplaceShootingSchedule shootingSchedule.
   * @member {IShootingScheduleMsg|null|undefined} shootingSchedule
   * @memberof ResReplaceShootingSchedule
   * @instance
   */
  ResReplaceShootingSchedule.prototype.shootingSchedule = null;

  /**
   * ResReplaceShootingSchedule replacedShootingSchedule.
   * @member {Array.<IShootingScheduleMsg>} replacedShootingSchedule
   * @memberof ResReplaceShootingSchedule
   * @instance
   */
  ResReplaceShootingSchedule.prototype.replacedShootingSchedule =
    $util.emptyArray;

  /**
   * ResReplaceShootingSchedule code.
   * @member {number} code
   * @memberof ResReplaceShootingSchedule
   * @instance
   */
  ResReplaceShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResReplaceShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {IResReplaceShootingSchedule=} [properties] Properties to set
   * @returns {ResReplaceShootingSchedule} ResReplaceShootingSchedule instance
   */
  ResReplaceShootingSchedule.create = function create(properties) {
    return new ResReplaceShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResReplaceShootingSchedule message. Does not implicitly {@link ResReplaceShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {IResReplaceShootingSchedule} message ResReplaceShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReplaceShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      $root.ShootingScheduleMsg.encode(
        message.shootingSchedule,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.replacedShootingSchedule != null &&
      message.replacedShootingSchedule.length
    )
      for (var i = 0; i < message.replacedShootingSchedule.length; ++i)
        $root.ShootingScheduleMsg.encode(
          message.replacedShootingSchedule[i],
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
          q + 1,
        ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResReplaceShootingSchedule message, length delimited. Does not implicitly {@link ResReplaceShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {IResReplaceShootingSchedule} message ResReplaceShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResReplaceShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResReplaceShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResReplaceShootingSchedule} ResReplaceShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReplaceShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResReplaceShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingSchedule = $root.ShootingScheduleMsg.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          if (!(
            message.replacedShootingSchedule &&
            message.replacedShootingSchedule.length
          ))
            message.replacedShootingSchedule = [];
          message.replacedShootingSchedule.push(
            $root.ShootingScheduleMsg.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 3: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResReplaceShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResReplaceShootingSchedule} ResReplaceShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResReplaceShootingSchedule.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResReplaceShootingSchedule message.
   * @function verify
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResReplaceShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    ) {
      var error = $root.ShootingScheduleMsg.verify(
        message.shootingSchedule,
        long + 1,
      );
      if (error) return "shootingSchedule." + error;
    }
    if (
      message.replacedShootingSchedule != null &&
      Object.hasOwnProperty.call(message, "replacedShootingSchedule")
    ) {
      if (!Array.isArray(message.replacedShootingSchedule))
        return "replacedShootingSchedule: array expected";
      for (var i = 0; i < message.replacedShootingSchedule.length; ++i) {
        var error = $root.ShootingScheduleMsg.verify(
          message.replacedShootingSchedule[i],
          long + 1,
        );
        if (error) return "replacedShootingSchedule." + error;
      }
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResReplaceShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResReplaceShootingSchedule} ResReplaceShootingSchedule
   */
  ResReplaceShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResReplaceShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResReplaceShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResReplaceShootingSchedule();
    if (object.shootingSchedule != null) {
      if (!$util.isObject(object.shootingSchedule))
        throw TypeError(
          ".ResReplaceShootingSchedule.shootingSchedule: object expected",
        );
      message.shootingSchedule = $root.ShootingScheduleMsg.fromObject(
        object.shootingSchedule,
        long + 1,
      );
    }
    if (object.replacedShootingSchedule) {
      if (!Array.isArray(object.replacedShootingSchedule))
        throw TypeError(
          ".ResReplaceShootingSchedule.replacedShootingSchedule: array expected",
        );
      message.replacedShootingSchedule = [];
      for (var i = 0; i < object.replacedShootingSchedule.length; ++i) {
        if (!$util.isObject(object.replacedShootingSchedule[i]))
          throw TypeError(
            ".ResReplaceShootingSchedule.replacedShootingSchedule: object expected",
          );
        message.replacedShootingSchedule[i] =
          $root.ShootingScheduleMsg.fromObject(
            object.replacedShootingSchedule[i],
            long + 1,
          );
      }
    }
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResReplaceShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {ResReplaceShootingSchedule} message ResReplaceShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResReplaceShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults)
      object.replacedShootingSchedule = [];
    if (options.defaults) {
      object.shootingSchedule = null;
      object.code = 0;
    }
    if (
      message.shootingSchedule != null &&
      Object.hasOwnProperty.call(message, "shootingSchedule")
    )
      object.shootingSchedule = $root.ShootingScheduleMsg.toObject(
        message.shootingSchedule,
        options,
        q + 1,
      );
    if (
      message.replacedShootingSchedule &&
      message.replacedShootingSchedule.length
    ) {
      object.replacedShootingSchedule = [];
      for (var j = 0; j < message.replacedShootingSchedule.length; ++j)
        object.replacedShootingSchedule[j] = $root.ShootingScheduleMsg.toObject(
          message.replacedShootingSchedule[j],
          options,
          q + 1,
        );
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResReplaceShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResReplaceShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResReplaceShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResReplaceShootingSchedule
   * @function getTypeUrl
   * @memberof ResReplaceShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResReplaceShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResReplaceShootingSchedule";
  };

  return ResReplaceShootingSchedule;
})();

$root.ReqUnlockShootingSchedule = (function () {
  /**
   * Properties of a ReqUnlockShootingSchedule.
   * @exports IReqUnlockShootingSchedule
   * @interface IReqUnlockShootingSchedule
   * @property {string|null} [id] ReqUnlockShootingSchedule id
   * @property {string|null} [password] ReqUnlockShootingSchedule password
   */

  /**
   * Constructs a new ReqUnlockShootingSchedule.
   * @exports ReqUnlockShootingSchedule
   * @classdesc Represents a ReqUnlockShootingSchedule.
   * @implements IReqUnlockShootingSchedule
   * @constructor
   * @param {IReqUnlockShootingSchedule=} [properties] Properties to set
   */
  function ReqUnlockShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqUnlockShootingSchedule id.
   * @member {string} id
   * @memberof ReqUnlockShootingSchedule
   * @instance
   */
  ReqUnlockShootingSchedule.prototype.id = "";

  /**
   * ReqUnlockShootingSchedule password.
   * @member {string} password
   * @memberof ReqUnlockShootingSchedule
   * @instance
   */
  ReqUnlockShootingSchedule.prototype.password = "";

  /**
   * Creates a new ReqUnlockShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {IReqUnlockShootingSchedule=} [properties] Properties to set
   * @returns {ReqUnlockShootingSchedule} ReqUnlockShootingSchedule instance
   */
  ReqUnlockShootingSchedule.create = function create(properties) {
    return new ReqUnlockShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqUnlockShootingSchedule message. Does not implicitly {@link ReqUnlockShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {IReqUnlockShootingSchedule} message ReqUnlockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUnlockShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.password);
    return writer;
  };

  /**
   * Encodes the specified ReqUnlockShootingSchedule message, length delimited. Does not implicitly {@link ReqUnlockShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {IReqUnlockShootingSchedule} message ReqUnlockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUnlockShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqUnlockShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqUnlockShootingSchedule} ReqUnlockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUnlockShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqUnlockShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.password = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqUnlockShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqUnlockShootingSchedule} ReqUnlockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUnlockShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqUnlockShootingSchedule message.
   * @function verify
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqUnlockShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    return null;
  };

  /**
   * Creates a ReqUnlockShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqUnlockShootingSchedule} ReqUnlockShootingSchedule
   */
  ReqUnlockShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqUnlockShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqUnlockShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqUnlockShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.password != null) message.password = String(object.password);
    return message;
  };

  /**
   * Creates a plain object from a ReqUnlockShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {ReqUnlockShootingSchedule} message ReqUnlockShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqUnlockShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.password = "";
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    return object;
  };

  /**
   * Converts this ReqUnlockShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqUnlockShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqUnlockShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqUnlockShootingSchedule
   * @function getTypeUrl
   * @memberof ReqUnlockShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqUnlockShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqUnlockShootingSchedule";
  };

  return ReqUnlockShootingSchedule;
})();

$root.ResUnlockShootingSchedule = (function () {
  /**
   * Properties of a ResUnlockShootingSchedule.
   * @exports IResUnlockShootingSchedule
   * @interface IResUnlockShootingSchedule
   * @property {string|null} [id] ResUnlockShootingSchedule id
   * @property {number|null} [code] ResUnlockShootingSchedule code
   */

  /**
   * Constructs a new ResUnlockShootingSchedule.
   * @exports ResUnlockShootingSchedule
   * @classdesc Represents a ResUnlockShootingSchedule.
   * @implements IResUnlockShootingSchedule
   * @constructor
   * @param {IResUnlockShootingSchedule=} [properties] Properties to set
   */
  function ResUnlockShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResUnlockShootingSchedule id.
   * @member {string} id
   * @memberof ResUnlockShootingSchedule
   * @instance
   */
  ResUnlockShootingSchedule.prototype.id = "";

  /**
   * ResUnlockShootingSchedule code.
   * @member {number} code
   * @memberof ResUnlockShootingSchedule
   * @instance
   */
  ResUnlockShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResUnlockShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {IResUnlockShootingSchedule=} [properties] Properties to set
   * @returns {ResUnlockShootingSchedule} ResUnlockShootingSchedule instance
   */
  ResUnlockShootingSchedule.create = function create(properties) {
    return new ResUnlockShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResUnlockShootingSchedule message. Does not implicitly {@link ResUnlockShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {IResUnlockShootingSchedule} message ResUnlockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResUnlockShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResUnlockShootingSchedule message, length delimited. Does not implicitly {@link ResUnlockShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {IResUnlockShootingSchedule} message ResUnlockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResUnlockShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResUnlockShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResUnlockShootingSchedule} ResUnlockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResUnlockShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResUnlockShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResUnlockShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResUnlockShootingSchedule} ResUnlockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResUnlockShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResUnlockShootingSchedule message.
   * @function verify
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResUnlockShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResUnlockShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResUnlockShootingSchedule} ResUnlockShootingSchedule
   */
  ResUnlockShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResUnlockShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResUnlockShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResUnlockShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResUnlockShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {ResUnlockShootingSchedule} message ResUnlockShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResUnlockShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.code = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResUnlockShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResUnlockShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResUnlockShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResUnlockShootingSchedule
   * @function getTypeUrl
   * @memberof ResUnlockShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResUnlockShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResUnlockShootingSchedule";
  };

  return ResUnlockShootingSchedule;
})();

$root.ReqLockShootingSchedule = (function () {
  /**
   * Properties of a ReqLockShootingSchedule.
   * @exports IReqLockShootingSchedule
   * @interface IReqLockShootingSchedule
   * @property {string|null} [id] ReqLockShootingSchedule id
   * @property {string|null} [password] ReqLockShootingSchedule password
   */

  /**
   * Constructs a new ReqLockShootingSchedule.
   * @exports ReqLockShootingSchedule
   * @classdesc Represents a ReqLockShootingSchedule.
   * @implements IReqLockShootingSchedule
   * @constructor
   * @param {IReqLockShootingSchedule=} [properties] Properties to set
   */
  function ReqLockShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqLockShootingSchedule id.
   * @member {string} id
   * @memberof ReqLockShootingSchedule
   * @instance
   */
  ReqLockShootingSchedule.prototype.id = "";

  /**
   * ReqLockShootingSchedule password.
   * @member {string} password
   * @memberof ReqLockShootingSchedule
   * @instance
   */
  ReqLockShootingSchedule.prototype.password = "";

  /**
   * Creates a new ReqLockShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {IReqLockShootingSchedule=} [properties] Properties to set
   * @returns {ReqLockShootingSchedule} ReqLockShootingSchedule instance
   */
  ReqLockShootingSchedule.create = function create(properties) {
    return new ReqLockShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqLockShootingSchedule message. Does not implicitly {@link ReqLockShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {IReqLockShootingSchedule} message ReqLockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqLockShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.password);
    return writer;
  };

  /**
   * Encodes the specified ReqLockShootingSchedule message, length delimited. Does not implicitly {@link ReqLockShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {IReqLockShootingSchedule} message ReqLockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqLockShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqLockShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqLockShootingSchedule} ReqLockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqLockShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqLockShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.password = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqLockShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqLockShootingSchedule} ReqLockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqLockShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqLockShootingSchedule message.
   * @function verify
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqLockShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    return null;
  };

  /**
   * Creates a ReqLockShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqLockShootingSchedule} ReqLockShootingSchedule
   */
  ReqLockShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqLockShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqLockShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqLockShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.password != null) message.password = String(object.password);
    return message;
  };

  /**
   * Creates a plain object from a ReqLockShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {ReqLockShootingSchedule} message ReqLockShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqLockShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.password = "";
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    return object;
  };

  /**
   * Converts this ReqLockShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqLockShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqLockShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqLockShootingSchedule
   * @function getTypeUrl
   * @memberof ReqLockShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqLockShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqLockShootingSchedule";
  };

  return ReqLockShootingSchedule;
})();

$root.ResLockShootingSchedule = (function () {
  /**
   * Properties of a ResLockShootingSchedule.
   * @exports IResLockShootingSchedule
   * @interface IResLockShootingSchedule
   * @property {string|null} [id] ResLockShootingSchedule id
   * @property {string|null} [password] ResLockShootingSchedule password
   * @property {number|null} [code] ResLockShootingSchedule code
   */

  /**
   * Constructs a new ResLockShootingSchedule.
   * @exports ResLockShootingSchedule
   * @classdesc Represents a ResLockShootingSchedule.
   * @implements IResLockShootingSchedule
   * @constructor
   * @param {IResLockShootingSchedule=} [properties] Properties to set
   */
  function ResLockShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResLockShootingSchedule id.
   * @member {string} id
   * @memberof ResLockShootingSchedule
   * @instance
   */
  ResLockShootingSchedule.prototype.id = "";

  /**
   * ResLockShootingSchedule password.
   * @member {string} password
   * @memberof ResLockShootingSchedule
   * @instance
   */
  ResLockShootingSchedule.prototype.password = "";

  /**
   * ResLockShootingSchedule code.
   * @member {number} code
   * @memberof ResLockShootingSchedule
   * @instance
   */
  ResLockShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResLockShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResLockShootingSchedule
   * @static
   * @param {IResLockShootingSchedule=} [properties] Properties to set
   * @returns {ResLockShootingSchedule} ResLockShootingSchedule instance
   */
  ResLockShootingSchedule.create = function create(properties) {
    return new ResLockShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResLockShootingSchedule message. Does not implicitly {@link ResLockShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResLockShootingSchedule
   * @static
   * @param {IResLockShootingSchedule} message ResLockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResLockShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.password);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResLockShootingSchedule message, length delimited. Does not implicitly {@link ResLockShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResLockShootingSchedule
   * @static
   * @param {IResLockShootingSchedule} message ResLockShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResLockShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResLockShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResLockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResLockShootingSchedule} ResLockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResLockShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResLockShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.password = reader.string();
          break;
        }
        case 3: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResLockShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResLockShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResLockShootingSchedule} ResLockShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResLockShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResLockShootingSchedule message.
   * @function verify
   * @memberof ResLockShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResLockShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResLockShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResLockShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResLockShootingSchedule} ResLockShootingSchedule
   */
  ResLockShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResLockShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResLockShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResLockShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.password != null) message.password = String(object.password);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResLockShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResLockShootingSchedule
   * @static
   * @param {ResLockShootingSchedule} message ResLockShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResLockShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.password = "";
      object.code = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResLockShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResLockShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResLockShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResLockShootingSchedule
   * @function getTypeUrl
   * @memberof ResLockShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResLockShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResLockShootingSchedule";
  };

  return ResLockShootingSchedule;
})();

$root.ReqDeleteShootingSchedule = (function () {
  /**
   * Properties of a ReqDeleteShootingSchedule.
   * @exports IReqDeleteShootingSchedule
   * @interface IReqDeleteShootingSchedule
   * @property {string|null} [id] ReqDeleteShootingSchedule id
   * @property {string|null} [password] ReqDeleteShootingSchedule password
   */

  /**
   * Constructs a new ReqDeleteShootingSchedule.
   * @exports ReqDeleteShootingSchedule
   * @classdesc Represents a ReqDeleteShootingSchedule.
   * @implements IReqDeleteShootingSchedule
   * @constructor
   * @param {IReqDeleteShootingSchedule=} [properties] Properties to set
   */
  function ReqDeleteShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqDeleteShootingSchedule id.
   * @member {string} id
   * @memberof ReqDeleteShootingSchedule
   * @instance
   */
  ReqDeleteShootingSchedule.prototype.id = "";

  /**
   * ReqDeleteShootingSchedule password.
   * @member {string} password
   * @memberof ReqDeleteShootingSchedule
   * @instance
   */
  ReqDeleteShootingSchedule.prototype.password = "";

  /**
   * Creates a new ReqDeleteShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {IReqDeleteShootingSchedule=} [properties] Properties to set
   * @returns {ReqDeleteShootingSchedule} ReqDeleteShootingSchedule instance
   */
  ReqDeleteShootingSchedule.create = function create(properties) {
    return new ReqDeleteShootingSchedule(properties);
  };

  /**
   * Encodes the specified ReqDeleteShootingSchedule message. Does not implicitly {@link ReqDeleteShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {IReqDeleteShootingSchedule} message ReqDeleteShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDeleteShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.password);
    return writer;
  };

  /**
   * Encodes the specified ReqDeleteShootingSchedule message, length delimited. Does not implicitly {@link ReqDeleteShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {IReqDeleteShootingSchedule} message ReqDeleteShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqDeleteShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqDeleteShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqDeleteShootingSchedule} ReqDeleteShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDeleteShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqDeleteShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.password = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqDeleteShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqDeleteShootingSchedule} ReqDeleteShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqDeleteShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqDeleteShootingSchedule message.
   * @function verify
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqDeleteShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      if (!$util.isString(message.password)) return "password: string expected";
    return null;
  };

  /**
   * Creates a ReqDeleteShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqDeleteShootingSchedule} ReqDeleteShootingSchedule
   */
  ReqDeleteShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqDeleteShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqDeleteShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqDeleteShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.password != null) message.password = String(object.password);
    return message;
  };

  /**
   * Creates a plain object from a ReqDeleteShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {ReqDeleteShootingSchedule} message ReqDeleteShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqDeleteShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.password = "";
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (
      message.password != null &&
      Object.hasOwnProperty.call(message, "password")
    )
      object.password = message.password;
    return object;
  };

  /**
   * Converts this ReqDeleteShootingSchedule to JSON.
   * @function toJSON
   * @memberof ReqDeleteShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqDeleteShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqDeleteShootingSchedule
   * @function getTypeUrl
   * @memberof ReqDeleteShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqDeleteShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqDeleteShootingSchedule";
  };

  return ReqDeleteShootingSchedule;
})();

$root.ResDeleteShootingSchedule = (function () {
  /**
   * Properties of a ResDeleteShootingSchedule.
   * @exports IResDeleteShootingSchedule
   * @interface IResDeleteShootingSchedule
   * @property {string|null} [id] ResDeleteShootingSchedule id
   * @property {number|null} [code] ResDeleteShootingSchedule code
   */

  /**
   * Constructs a new ResDeleteShootingSchedule.
   * @exports ResDeleteShootingSchedule
   * @classdesc Represents a ResDeleteShootingSchedule.
   * @implements IResDeleteShootingSchedule
   * @constructor
   * @param {IResDeleteShootingSchedule=} [properties] Properties to set
   */
  function ResDeleteShootingSchedule(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResDeleteShootingSchedule id.
   * @member {string} id
   * @memberof ResDeleteShootingSchedule
   * @instance
   */
  ResDeleteShootingSchedule.prototype.id = "";

  /**
   * ResDeleteShootingSchedule code.
   * @member {number} code
   * @memberof ResDeleteShootingSchedule
   * @instance
   */
  ResDeleteShootingSchedule.prototype.code = 0;

  /**
   * Creates a new ResDeleteShootingSchedule instance using the specified properties.
   * @function create
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {IResDeleteShootingSchedule=} [properties] Properties to set
   * @returns {ResDeleteShootingSchedule} ResDeleteShootingSchedule instance
   */
  ResDeleteShootingSchedule.create = function create(properties) {
    return new ResDeleteShootingSchedule(properties);
  };

  /**
   * Encodes the specified ResDeleteShootingSchedule message. Does not implicitly {@link ResDeleteShootingSchedule.verify|verify} messages.
   * @function encode
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {IResDeleteShootingSchedule} message ResDeleteShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResDeleteShootingSchedule.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.id);
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.code);
    return writer;
  };

  /**
   * Encodes the specified ResDeleteShootingSchedule message, length delimited. Does not implicitly {@link ResDeleteShootingSchedule.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {IResDeleteShootingSchedule} message ResDeleteShootingSchedule message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResDeleteShootingSchedule.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResDeleteShootingSchedule message from the specified reader or buffer.
   * @function decode
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResDeleteShootingSchedule} ResDeleteShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResDeleteShootingSchedule.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResDeleteShootingSchedule();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.id = reader.string();
          break;
        }
        case 2: {
          message.code = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResDeleteShootingSchedule message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResDeleteShootingSchedule} ResDeleteShootingSchedule
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResDeleteShootingSchedule.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResDeleteShootingSchedule message.
   * @function verify
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResDeleteShootingSchedule.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      if (!$util.isString(message.id)) return "id: string expected";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    return null;
  };

  /**
   * Creates a ResDeleteShootingSchedule message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResDeleteShootingSchedule} ResDeleteShootingSchedule
   */
  ResDeleteShootingSchedule.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResDeleteShootingSchedule) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResDeleteShootingSchedule: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResDeleteShootingSchedule();
    if (object.id != null) message.id = String(object.id);
    if (object.code != null) message.code = object.code | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResDeleteShootingSchedule message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {ResDeleteShootingSchedule} message ResDeleteShootingSchedule
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResDeleteShootingSchedule.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.id = "";
      object.code = 0;
    }
    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
      object.id = message.id;
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    return object;
  };

  /**
   * Converts this ResDeleteShootingSchedule to JSON.
   * @function toJSON
   * @memberof ResDeleteShootingSchedule
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResDeleteShootingSchedule.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResDeleteShootingSchedule
   * @function getTypeUrl
   * @memberof ResDeleteShootingSchedule
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResDeleteShootingSchedule.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResDeleteShootingSchedule";
  };

  return ResDeleteShootingSchedule;
})();

$root.ReqSetTime = (function () {
  /**
   * Properties of a ReqSetTime.
   * @exports IReqSetTime
   * @interface IReqSetTime
   * @property {number|Long|null} [timestamp] ReqSetTime timestamp
   * @property {number|null} [timezoneOffset] ReqSetTime timezoneOffset
   */

  /**
   * Constructs a new ReqSetTime.
   * @exports ReqSetTime
   * @classdesc Represents a ReqSetTime.
   * @implements IReqSetTime
   * @constructor
   * @param {IReqSetTime=} [properties] Properties to set
   */
  function ReqSetTime(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetTime timestamp.
   * @member {number|Long} timestamp
   * @memberof ReqSetTime
   * @instance
   */
  ReqSetTime.prototype.timestamp = $util.Long
    ? $util.Long.fromBits(0, 0, true)
    : 0;

  /**
   * ReqSetTime timezoneOffset.
   * @member {number} timezoneOffset
   * @memberof ReqSetTime
   * @instance
   */
  ReqSetTime.prototype.timezoneOffset = 0;

  /**
   * Creates a new ReqSetTime instance using the specified properties.
   * @function create
   * @memberof ReqSetTime
   * @static
   * @param {IReqSetTime=} [properties] Properties to set
   * @returns {ReqSetTime} ReqSetTime instance
   */
  ReqSetTime.create = function create(properties) {
    return new ReqSetTime(properties);
  };

  /**
   * Encodes the specified ReqSetTime message. Does not implicitly {@link ReqSetTime.verify|verify} messages.
   * @function encode
   * @memberof ReqSetTime
   * @static
   * @param {IReqSetTime} message ReqSetTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetTime.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.timestamp);
    if (
      message.timezoneOffset != null &&
      Object.hasOwnProperty.call(message, "timezoneOffset")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.timezoneOffset);
    return writer;
  };

  /**
   * Encodes the specified ReqSetTime message, length delimited. Does not implicitly {@link ReqSetTime.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetTime
   * @static
   * @param {IReqSetTime} message ReqSetTime message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetTime.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetTime message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetTime} ReqSetTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetTime.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetTime();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.timestamp = reader.uint64();
          break;
        }
        case 2: {
          message.timezoneOffset = reader.double();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetTime message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetTime
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetTime} ReqSetTime
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetTime.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetTime message.
   * @function verify
   * @memberof ReqSetTime
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetTime.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (
        !$util.isInteger(message.timestamp) &&
        !(
          message.timestamp &&
          $util.isInteger(message.timestamp.low) &&
          $util.isInteger(message.timestamp.high)
        )
      )
        return "timestamp: integer|Long expected";
    if (
      message.timezoneOffset != null &&
      Object.hasOwnProperty.call(message, "timezoneOffset")
    )
      if (typeof message.timezoneOffset !== "number")
        return "timezoneOffset: number expected";
    return null;
  };

  /**
   * Creates a ReqSetTime message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetTime
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetTime} ReqSetTime
   */
  ReqSetTime.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetTime) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetTime: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetTime();
    if (object.timestamp != null)
      if ($util.Long)
        message.timestamp = $util.Long.fromValue(object.timestamp, true);
      else if (typeof object.timestamp === "string")
        message.timestamp = parseInt(object.timestamp, 10);
      else if (typeof object.timestamp === "number")
        message.timestamp = object.timestamp;
      else if (typeof object.timestamp === "object")
        message.timestamp = new $util.LongBits(
          object.timestamp.low >>> 0,
          object.timestamp.high >>> 0,
        ).toNumber(true);
    if (object.timezoneOffset != null)
      message.timezoneOffset = Number(object.timezoneOffset);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetTime message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetTime
   * @static
   * @param {ReqSetTime} message ReqSetTime
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetTime.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      if ($util.Long) {
        var long = new $util.Long(0, 0, true);
        object.timestamp =
          options.longs === String
            ? long.toString()
            : options.longs === Number
              ? long.toNumber()
              : typeof BigInt !== "undefined" && options.longs === BigInt
                ? long.toBigInt()
                : long;
      } else
        object.timestamp =
          options.longs === String
            ? "0"
            : typeof BigInt !== "undefined" && options.longs === BigInt
              ? BigInt("0")
              : 0;
      object.timezoneOffset = 0;
    }
    if (
      message.timestamp != null &&
      Object.hasOwnProperty.call(message, "timestamp")
    )
      if (typeof BigInt !== "undefined" && options.longs === BigInt)
        object.timestamp =
          typeof message.timestamp === "number"
            ? BigInt(message.timestamp)
            : $util.Long.fromBits(
                message.timestamp.low >>> 0,
                message.timestamp.high >>> 0,
                true,
              ).toBigInt();
      else if (typeof message.timestamp === "number")
        object.timestamp =
          options.longs === String
            ? String(message.timestamp)
            : message.timestamp;
      else
        object.timestamp =
          options.longs === String
            ? $util.Long.prototype.toString.call(message.timestamp)
            : options.longs === Number
              ? new $util.LongBits(
                  message.timestamp.low >>> 0,
                  message.timestamp.high >>> 0,
                ).toNumber(true)
              : message.timestamp;
    if (
      message.timezoneOffset != null &&
      Object.hasOwnProperty.call(message, "timezoneOffset")
    )
      object.timezoneOffset =
        options.json && !isFinite(message.timezoneOffset)
          ? String(message.timezoneOffset)
          : message.timezoneOffset;
    return object;
  };

  /**
   * Converts this ReqSetTime to JSON.
   * @function toJSON
   * @memberof ReqSetTime
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetTime.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetTime
   * @function getTypeUrl
   * @memberof ReqSetTime
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetTime";
  };

  return ReqSetTime;
})();

$root.ReqSetTimezone = (function () {
  /**
   * Properties of a ReqSetTimezone.
   * @exports IReqSetTimezone
   * @interface IReqSetTimezone
   * @property {string|null} [timezone] ReqSetTimezone timezone
   */

  /**
   * Constructs a new ReqSetTimezone.
   * @exports ReqSetTimezone
   * @classdesc Represents a ReqSetTimezone.
   * @implements IReqSetTimezone
   * @constructor
   * @param {IReqSetTimezone=} [properties] Properties to set
   */
  function ReqSetTimezone(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetTimezone timezone.
   * @member {string} timezone
   * @memberof ReqSetTimezone
   * @instance
   */
  ReqSetTimezone.prototype.timezone = "";

  /**
   * Creates a new ReqSetTimezone instance using the specified properties.
   * @function create
   * @memberof ReqSetTimezone
   * @static
   * @param {IReqSetTimezone=} [properties] Properties to set
   * @returns {ReqSetTimezone} ReqSetTimezone instance
   */
  ReqSetTimezone.create = function create(properties) {
    return new ReqSetTimezone(properties);
  };

  /**
   * Encodes the specified ReqSetTimezone message. Does not implicitly {@link ReqSetTimezone.verify|verify} messages.
   * @function encode
   * @memberof ReqSetTimezone
   * @static
   * @param {IReqSetTimezone} message ReqSetTimezone message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetTimezone.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.timezone != null &&
      Object.hasOwnProperty.call(message, "timezone")
    )
      writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.timezone);
    return writer;
  };

  /**
   * Encodes the specified ReqSetTimezone message, length delimited. Does not implicitly {@link ReqSetTimezone.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetTimezone
   * @static
   * @param {IReqSetTimezone} message ReqSetTimezone message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetTimezone.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetTimezone message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetTimezone
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetTimezone} ReqSetTimezone
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetTimezone.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetTimezone();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.timezone = reader.string();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetTimezone message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetTimezone
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetTimezone} ReqSetTimezone
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetTimezone.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetTimezone message.
   * @function verify
   * @memberof ReqSetTimezone
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetTimezone.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.timezone != null &&
      Object.hasOwnProperty.call(message, "timezone")
    )
      if (!$util.isString(message.timezone)) return "timezone: string expected";
    return null;
  };

  /**
   * Creates a ReqSetTimezone message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetTimezone
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetTimezone} ReqSetTimezone
   */
  ReqSetTimezone.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetTimezone) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetTimezone: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetTimezone();
    if (object.timezone != null) message.timezone = String(object.timezone);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetTimezone message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetTimezone
   * @static
   * @param {ReqSetTimezone} message ReqSetTimezone
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetTimezone.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.timezone = "";
    if (
      message.timezone != null &&
      Object.hasOwnProperty.call(message, "timezone")
    )
      object.timezone = message.timezone;
    return object;
  };

  /**
   * Converts this ReqSetTimezone to JSON.
   * @function toJSON
   * @memberof ReqSetTimezone
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetTimezone.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetTimezone
   * @function getTypeUrl
   * @memberof ReqSetTimezone
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetTimezone.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetTimezone";
  };

  return ReqSetTimezone;
})();

$root.ReqSetMtpMode = (function () {
  /**
   * Properties of a ReqSetMtpMode.
   * @exports IReqSetMtpMode
   * @interface IReqSetMtpMode
   * @property {number|null} [mode] ReqSetMtpMode mode
   */

  /**
   * Constructs a new ReqSetMtpMode.
   * @exports ReqSetMtpMode
   * @classdesc Represents a ReqSetMtpMode.
   * @implements IReqSetMtpMode
   * @constructor
   * @param {IReqSetMtpMode=} [properties] Properties to set
   */
  function ReqSetMtpMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetMtpMode mode.
   * @member {number} mode
   * @memberof ReqSetMtpMode
   * @instance
   */
  ReqSetMtpMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetMtpMode instance using the specified properties.
   * @function create
   * @memberof ReqSetMtpMode
   * @static
   * @param {IReqSetMtpMode=} [properties] Properties to set
   * @returns {ReqSetMtpMode} ReqSetMtpMode instance
   */
  ReqSetMtpMode.create = function create(properties) {
    return new ReqSetMtpMode(properties);
  };

  /**
   * Encodes the specified ReqSetMtpMode message. Does not implicitly {@link ReqSetMtpMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetMtpMode
   * @static
   * @param {IReqSetMtpMode} message ReqSetMtpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetMtpMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetMtpMode message, length delimited. Does not implicitly {@link ReqSetMtpMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetMtpMode
   * @static
   * @param {IReqSetMtpMode} message ReqSetMtpMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetMtpMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetMtpMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetMtpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetMtpMode} ReqSetMtpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetMtpMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetMtpMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetMtpMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetMtpMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetMtpMode} ReqSetMtpMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetMtpMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetMtpMode message.
   * @function verify
   * @memberof ReqSetMtpMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetMtpMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetMtpMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetMtpMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetMtpMode} ReqSetMtpMode
   */
  ReqSetMtpMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetMtpMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetMtpMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetMtpMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetMtpMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetMtpMode
   * @static
   * @param {ReqSetMtpMode} message ReqSetMtpMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetMtpMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetMtpMode to JSON.
   * @function toJSON
   * @memberof ReqSetMtpMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetMtpMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetMtpMode
   * @function getTypeUrl
   * @memberof ReqSetMtpMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetMtpMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetMtpMode";
  };

  return ReqSetMtpMode;
})();

$root.ReqSetCpuMode = (function () {
  /**
   * Properties of a ReqSetCpuMode.
   * @exports IReqSetCpuMode
   * @interface IReqSetCpuMode
   * @property {number|null} [mode] ReqSetCpuMode mode
   */

  /**
   * Constructs a new ReqSetCpuMode.
   * @exports ReqSetCpuMode
   * @classdesc Represents a ReqSetCpuMode.
   * @implements IReqSetCpuMode
   * @constructor
   * @param {IReqSetCpuMode=} [properties] Properties to set
   */
  function ReqSetCpuMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetCpuMode mode.
   * @member {number} mode
   * @memberof ReqSetCpuMode
   * @instance
   */
  ReqSetCpuMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetCpuMode instance using the specified properties.
   * @function create
   * @memberof ReqSetCpuMode
   * @static
   * @param {IReqSetCpuMode=} [properties] Properties to set
   * @returns {ReqSetCpuMode} ReqSetCpuMode instance
   */
  ReqSetCpuMode.create = function create(properties) {
    return new ReqSetCpuMode(properties);
  };

  /**
   * Encodes the specified ReqSetCpuMode message. Does not implicitly {@link ReqSetCpuMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetCpuMode
   * @static
   * @param {IReqSetCpuMode} message ReqSetCpuMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetCpuMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetCpuMode message, length delimited. Does not implicitly {@link ReqSetCpuMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetCpuMode
   * @static
   * @param {IReqSetCpuMode} message ReqSetCpuMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetCpuMode.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetCpuMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetCpuMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetCpuMode} ReqSetCpuMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetCpuMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetCpuMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetCpuMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetCpuMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetCpuMode} ReqSetCpuMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetCpuMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetCpuMode message.
   * @function verify
   * @memberof ReqSetCpuMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetCpuMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetCpuMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetCpuMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetCpuMode} ReqSetCpuMode
   */
  ReqSetCpuMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetCpuMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetCpuMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetCpuMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetCpuMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetCpuMode
   * @static
   * @param {ReqSetCpuMode} message ReqSetCpuMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetCpuMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetCpuMode to JSON.
   * @function toJSON
   * @memberof ReqSetCpuMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetCpuMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetCpuMode
   * @function getTypeUrl
   * @memberof ReqSetCpuMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetCpuMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetCpuMode";
  };

  return ReqSetCpuMode;
})();

$root.ReqsetMasterLock = (function () {
  /**
   * Properties of a ReqsetMasterLock.
   * @exports IReqsetMasterLock
   * @interface IReqsetMasterLock
   * @property {boolean|null} [lock] ReqsetMasterLock lock
   */

  /**
   * Constructs a new ReqsetMasterLock.
   * @exports ReqsetMasterLock
   * @classdesc Represents a ReqsetMasterLock.
   * @implements IReqsetMasterLock
   * @constructor
   * @param {IReqsetMasterLock=} [properties] Properties to set
   */
  function ReqsetMasterLock(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqsetMasterLock lock.
   * @member {boolean} lock
   * @memberof ReqsetMasterLock
   * @instance
   */
  ReqsetMasterLock.prototype.lock = false;

  /**
   * Creates a new ReqsetMasterLock instance using the specified properties.
   * @function create
   * @memberof ReqsetMasterLock
   * @static
   * @param {IReqsetMasterLock=} [properties] Properties to set
   * @returns {ReqsetMasterLock} ReqsetMasterLock instance
   */
  ReqsetMasterLock.create = function create(properties) {
    return new ReqsetMasterLock(properties);
  };

  /**
   * Encodes the specified ReqsetMasterLock message. Does not implicitly {@link ReqsetMasterLock.verify|verify} messages.
   * @function encode
   * @memberof ReqsetMasterLock
   * @static
   * @param {IReqsetMasterLock} message ReqsetMasterLock message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqsetMasterLock.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.lock);
    return writer;
  };

  /**
   * Encodes the specified ReqsetMasterLock message, length delimited. Does not implicitly {@link ReqsetMasterLock.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqsetMasterLock
   * @static
   * @param {IReqsetMasterLock} message ReqsetMasterLock message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqsetMasterLock.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqsetMasterLock message from the specified reader or buffer.
   * @function decode
   * @memberof ReqsetMasterLock
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqsetMasterLock} ReqsetMasterLock
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqsetMasterLock.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqsetMasterLock();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.lock = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqsetMasterLock message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqsetMasterLock
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqsetMasterLock} ReqsetMasterLock
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqsetMasterLock.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqsetMasterLock message.
   * @function verify
   * @memberof ReqsetMasterLock
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqsetMasterLock.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      if (typeof message.lock !== "boolean") return "lock: boolean expected";
    return null;
  };

  /**
   * Creates a ReqsetMasterLock message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqsetMasterLock
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqsetMasterLock} ReqsetMasterLock
   */
  ReqsetMasterLock.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqsetMasterLock) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqsetMasterLock: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqsetMasterLock();
    if (object.lock != null) message.lock = Boolean(object.lock);
    return message;
  };

  /**
   * Creates a plain object from a ReqsetMasterLock message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqsetMasterLock
   * @static
   * @param {ReqsetMasterLock} message ReqsetMasterLock
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqsetMasterLock.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.lock = false;
    if (message.lock != null && Object.hasOwnProperty.call(message, "lock"))
      object.lock = message.lock;
    return object;
  };

  /**
   * Converts this ReqsetMasterLock to JSON.
   * @function toJSON
   * @memberof ReqsetMasterLock
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqsetMasterLock.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqsetMasterLock
   * @function getTypeUrl
   * @memberof ReqsetMasterLock
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqsetMasterLock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqsetMasterLock";
  };

  return ReqsetMasterLock;
})();

$root.ReqSetLowTempProtectionMode = (function () {
  /**
   * Properties of a ReqSetLowTempProtectionMode.
   * @exports IReqSetLowTempProtectionMode
   * @interface IReqSetLowTempProtectionMode
   * @property {number|null} [mode] ReqSetLowTempProtectionMode mode
   */

  /**
   * Constructs a new ReqSetLowTempProtectionMode.
   * @exports ReqSetLowTempProtectionMode
   * @classdesc Represents a ReqSetLowTempProtectionMode.
   * @implements IReqSetLowTempProtectionMode
   * @constructor
   * @param {IReqSetLowTempProtectionMode=} [properties] Properties to set
   */
  function ReqSetLowTempProtectionMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetLowTempProtectionMode mode.
   * @member {number} mode
   * @memberof ReqSetLowTempProtectionMode
   * @instance
   */
  ReqSetLowTempProtectionMode.prototype.mode = 0;

  /**
   * Creates a new ReqSetLowTempProtectionMode instance using the specified properties.
   * @function create
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {IReqSetLowTempProtectionMode=} [properties] Properties to set
   * @returns {ReqSetLowTempProtectionMode} ReqSetLowTempProtectionMode instance
   */
  ReqSetLowTempProtectionMode.create = function create(properties) {
    return new ReqSetLowTempProtectionMode(properties);
  };

  /**
   * Encodes the specified ReqSetLowTempProtectionMode message. Does not implicitly {@link ReqSetLowTempProtectionMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {IReqSetLowTempProtectionMode} message ReqSetLowTempProtectionMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetLowTempProtectionMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSetLowTempProtectionMode message, length delimited. Does not implicitly {@link ReqSetLowTempProtectionMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {IReqSetLowTempProtectionMode} message ReqSetLowTempProtectionMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetLowTempProtectionMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetLowTempProtectionMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetLowTempProtectionMode} ReqSetLowTempProtectionMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetLowTempProtectionMode.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetLowTempProtectionMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetLowTempProtectionMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetLowTempProtectionMode} ReqSetLowTempProtectionMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetLowTempProtectionMode.decodeDelimited = function decodeDelimited(
    reader,
  ) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetLowTempProtectionMode message.
   * @function verify
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetLowTempProtectionMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSetLowTempProtectionMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetLowTempProtectionMode} ReqSetLowTempProtectionMode
   */
  ReqSetLowTempProtectionMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetLowTempProtectionMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetLowTempProtectionMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetLowTempProtectionMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSetLowTempProtectionMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {ReqSetLowTempProtectionMode} message ReqSetLowTempProtectionMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetLowTempProtectionMode.toObject = function toObject(
    message,
    options,
    q,
  ) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSetLowTempProtectionMode to JSON.
   * @function toJSON
   * @memberof ReqSetLowTempProtectionMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetLowTempProtectionMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetLowTempProtectionMode
   * @function getTypeUrl
   * @memberof ReqSetLowTempProtectionMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetLowTempProtectionMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetLowTempProtectionMode";
  };

  return ReqSetLowTempProtectionMode;
})();

$root.ReqSetLocation = (function () {
  /**
   * Properties of a ReqSetLocation.
   * @exports IReqSetLocation
   * @interface IReqSetLocation
   * @property {number|null} [latitude] ReqSetLocation latitude
   * @property {number|null} [longitude] ReqSetLocation longitude
   * @property {number|null} [altitude] ReqSetLocation altitude
   * @property {string|null} [countryRegion] ReqSetLocation countryRegion
   * @property {string|null} [province] ReqSetLocation province
   * @property {string|null} [city] ReqSetLocation city
   * @property {string|null} [district] ReqSetLocation district
   * @property {boolean|null} [enable] ReqSetLocation enable
   */

  /**
   * Constructs a new ReqSetLocation.
   * @exports ReqSetLocation
   * @classdesc Represents a ReqSetLocation.
   * @implements IReqSetLocation
   * @constructor
   * @param {IReqSetLocation=} [properties] Properties to set
   */
  function ReqSetLocation(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSetLocation latitude.
   * @member {number} latitude
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.latitude = 0;

  /**
   * ReqSetLocation longitude.
   * @member {number} longitude
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.longitude = 0;

  /**
   * ReqSetLocation altitude.
   * @member {number} altitude
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.altitude = 0;

  /**
   * ReqSetLocation countryRegion.
   * @member {string} countryRegion
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.countryRegion = "";

  /**
   * ReqSetLocation province.
   * @member {string} province
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.province = "";

  /**
   * ReqSetLocation city.
   * @member {string} city
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.city = "";

  /**
   * ReqSetLocation district.
   * @member {string} district
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.district = "";

  /**
   * ReqSetLocation enable.
   * @member {boolean} enable
   * @memberof ReqSetLocation
   * @instance
   */
  ReqSetLocation.prototype.enable = false;

  /**
   * Creates a new ReqSetLocation instance using the specified properties.
   * @function create
   * @memberof ReqSetLocation
   * @static
   * @param {IReqSetLocation=} [properties] Properties to set
   * @returns {ReqSetLocation} ReqSetLocation instance
   */
  ReqSetLocation.create = function create(properties) {
    return new ReqSetLocation(properties);
  };

  /**
   * Encodes the specified ReqSetLocation message. Does not implicitly {@link ReqSetLocation.verify|verify} messages.
   * @function encode
   * @memberof ReqSetLocation
   * @static
   * @param {IReqSetLocation} message ReqSetLocation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetLocation.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.latitude != null &&
      Object.hasOwnProperty.call(message, "latitude")
    )
      writer.uint32(/* id 1, wireType 1 =*/ 9).double(message.latitude);
    if (
      message.longitude != null &&
      Object.hasOwnProperty.call(message, "longitude")
    )
      writer.uint32(/* id 2, wireType 1 =*/ 17).double(message.longitude);
    if (
      message.altitude != null &&
      Object.hasOwnProperty.call(message, "altitude")
    )
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.altitude);
    if (
      message.countryRegion != null &&
      Object.hasOwnProperty.call(message, "countryRegion")
    )
      writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.countryRegion);
    if (
      message.province != null &&
      Object.hasOwnProperty.call(message, "province")
    )
      writer.uint32(/* id 5, wireType 2 =*/ 42).string(message.province);
    if (message.city != null && Object.hasOwnProperty.call(message, "city"))
      writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.city);
    if (
      message.district != null &&
      Object.hasOwnProperty.call(message, "district")
    )
      writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.district);
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      writer.uint32(/* id 8, wireType 0 =*/ 64).bool(message.enable);
    return writer;
  };

  /**
   * Encodes the specified ReqSetLocation message, length delimited. Does not implicitly {@link ReqSetLocation.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSetLocation
   * @static
   * @param {IReqSetLocation} message ReqSetLocation message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSetLocation.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSetLocation message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSetLocation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSetLocation} ReqSetLocation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetLocation.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSetLocation();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.latitude = reader.double();
          break;
        }
        case 2: {
          message.longitude = reader.double();
          break;
        }
        case 3: {
          message.altitude = reader.double();
          break;
        }
        case 4: {
          message.countryRegion = reader.string();
          break;
        }
        case 5: {
          message.province = reader.string();
          break;
        }
        case 6: {
          message.city = reader.string();
          break;
        }
        case 7: {
          message.district = reader.string();
          break;
        }
        case 8: {
          message.enable = reader.bool();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSetLocation message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSetLocation
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSetLocation} ReqSetLocation
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSetLocation.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSetLocation message.
   * @function verify
   * @memberof ReqSetLocation
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSetLocation.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.latitude != null &&
      Object.hasOwnProperty.call(message, "latitude")
    )
      if (typeof message.latitude !== "number")
        return "latitude: number expected";
    if (
      message.longitude != null &&
      Object.hasOwnProperty.call(message, "longitude")
    )
      if (typeof message.longitude !== "number")
        return "longitude: number expected";
    if (
      message.altitude != null &&
      Object.hasOwnProperty.call(message, "altitude")
    )
      if (typeof message.altitude !== "number")
        return "altitude: number expected";
    if (
      message.countryRegion != null &&
      Object.hasOwnProperty.call(message, "countryRegion")
    )
      if (!$util.isString(message.countryRegion))
        return "countryRegion: string expected";
    if (
      message.province != null &&
      Object.hasOwnProperty.call(message, "province")
    )
      if (!$util.isString(message.province)) return "province: string expected";
    if (message.city != null && Object.hasOwnProperty.call(message, "city"))
      if (!$util.isString(message.city)) return "city: string expected";
    if (
      message.district != null &&
      Object.hasOwnProperty.call(message, "district")
    )
      if (!$util.isString(message.district)) return "district: string expected";
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      if (typeof message.enable !== "boolean")
        return "enable: boolean expected";
    return null;
  };

  /**
   * Creates a ReqSetLocation message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSetLocation
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSetLocation} ReqSetLocation
   */
  ReqSetLocation.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSetLocation) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSetLocation: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSetLocation();
    if (object.latitude != null) message.latitude = Number(object.latitude);
    if (object.longitude != null) message.longitude = Number(object.longitude);
    if (object.altitude != null) message.altitude = Number(object.altitude);
    if (object.countryRegion != null)
      message.countryRegion = String(object.countryRegion);
    if (object.province != null) message.province = String(object.province);
    if (object.city != null) message.city = String(object.city);
    if (object.district != null) message.district = String(object.district);
    if (object.enable != null) message.enable = Boolean(object.enable);
    return message;
  };

  /**
   * Creates a plain object from a ReqSetLocation message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSetLocation
   * @static
   * @param {ReqSetLocation} message ReqSetLocation
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSetLocation.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.latitude = 0;
      object.longitude = 0;
      object.altitude = 0;
      object.countryRegion = "";
      object.province = "";
      object.city = "";
      object.district = "";
      object.enable = false;
    }
    if (
      message.latitude != null &&
      Object.hasOwnProperty.call(message, "latitude")
    )
      object.latitude =
        options.json && !isFinite(message.latitude)
          ? String(message.latitude)
          : message.latitude;
    if (
      message.longitude != null &&
      Object.hasOwnProperty.call(message, "longitude")
    )
      object.longitude =
        options.json && !isFinite(message.longitude)
          ? String(message.longitude)
          : message.longitude;
    if (
      message.altitude != null &&
      Object.hasOwnProperty.call(message, "altitude")
    )
      object.altitude =
        options.json && !isFinite(message.altitude)
          ? String(message.altitude)
          : message.altitude;
    if (
      message.countryRegion != null &&
      Object.hasOwnProperty.call(message, "countryRegion")
    )
      object.countryRegion = message.countryRegion;
    if (
      message.province != null &&
      Object.hasOwnProperty.call(message, "province")
    )
      object.province = message.province;
    if (message.city != null && Object.hasOwnProperty.call(message, "city"))
      object.city = message.city;
    if (
      message.district != null &&
      Object.hasOwnProperty.call(message, "district")
    )
      object.district = message.district;
    if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
      object.enable = message.enable;
    return object;
  };

  /**
   * Converts this ReqSetLocation to JSON.
   * @function toJSON
   * @memberof ReqSetLocation
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSetLocation.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSetLocation
   * @function getTypeUrl
   * @memberof ReqSetLocation
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSetLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSetLocation";
  };

  return ReqSetLocation;
})();

/**
 * TaskId enum.
 * @exports TaskId
 * @enum {number}
 * @property {number} TASK_ID_IDLE=0 TASK_ID_IDLE value
 * @property {number} TASK_ID_PANORAMA_UPLOAD=1 TASK_ID_PANORAMA_UPLOAD value
 * @property {number} TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION=2 TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION value
 * @property {number} TASK_ID_ASTRO_MULTI_STACK=3 TASK_ID_ASTRO_MULTI_STACK value
 * @property {number} TASK_ID_CAPTURE_CALI_FRAME=4 TASK_ID_CAPTURE_CALI_FRAME value
 */
$root.TaskId = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "TASK_ID_IDLE")] = 0;
  values[(valuesById[1] = "TASK_ID_PANORAMA_UPLOAD")] = 1;
  values[(valuesById[2] = "TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION")] =
    2;
  values[(valuesById[3] = "TASK_ID_ASTRO_MULTI_STACK")] = 3;
  values[(valuesById[4] = "TASK_ID_CAPTURE_CALI_FRAME")] = 4;
  return values;
})();

/**
 * ExclusiveTaskType enum.
 * @exports ExclusiveTaskType
 * @enum {number}
 * @property {number} EXCLUSIVE_TYPE_NONE=0 EXCLUSIVE_TYPE_NONE value
 * @property {number} EXCLUSIVE_TYPE_CAMERA=1 EXCLUSIVE_TYPE_CAMERA value
 * @property {number} EXCLUSIVE_TYPE_MOTOR=2 EXCLUSIVE_TYPE_MOTOR value
 * @property {number} EXCLUSIVE_TYPE_FOCUS_MOTOR=4 EXCLUSIVE_TYPE_FOCUS_MOTOR value
 * @property {number} EXCLUSIVE_TYPE_SYSTEM_IO=8 EXCLUSIVE_TYPE_SYSTEM_IO value
 * @property {number} EXCLUSIVE_TYPE_SYSTEM_WIFI=16 EXCLUSIVE_TYPE_SYSTEM_WIFI value
 */
$root.ExclusiveTaskType = (function () {
  var valuesById = {},
    values = Object.create(valuesById);
  values[(valuesById[0] = "EXCLUSIVE_TYPE_NONE")] = 0;
  values[(valuesById[1] = "EXCLUSIVE_TYPE_CAMERA")] = 1;
  values[(valuesById[2] = "EXCLUSIVE_TYPE_MOTOR")] = 2;
  values[(valuesById[4] = "EXCLUSIVE_TYPE_FOCUS_MOTOR")] = 4;
  values[(valuesById[8] = "EXCLUSIVE_TYPE_SYSTEM_IO")] = 8;
  values[(valuesById[16] = "EXCLUSIVE_TYPE_SYSTEM_WIFI")] = 16;
  return values;
})();

$root.TaskAttr = (function () {
  /**
   * Properties of a TaskAttr.
   * @exports ITaskAttr
   * @interface ITaskAttr
   * @property {number|null} [exclusiveMask] TaskAttr exclusiveMask
   * @property {number|null} [priority] TaskAttr priority
   */

  /**
   * Constructs a new TaskAttr.
   * @exports TaskAttr
   * @classdesc Represents a TaskAttr.
   * @implements ITaskAttr
   * @constructor
   * @param {ITaskAttr=} [properties] Properties to set
   */
  function TaskAttr(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * TaskAttr exclusiveMask.
   * @member {number} exclusiveMask
   * @memberof TaskAttr
   * @instance
   */
  TaskAttr.prototype.exclusiveMask = 0;

  /**
   * TaskAttr priority.
   * @member {number} priority
   * @memberof TaskAttr
   * @instance
   */
  TaskAttr.prototype.priority = 0;

  /**
   * Creates a new TaskAttr instance using the specified properties.
   * @function create
   * @memberof TaskAttr
   * @static
   * @param {ITaskAttr=} [properties] Properties to set
   * @returns {TaskAttr} TaskAttr instance
   */
  TaskAttr.create = function create(properties) {
    return new TaskAttr(properties);
  };

  /**
   * Encodes the specified TaskAttr message. Does not implicitly {@link TaskAttr.verify|verify} messages.
   * @function encode
   * @memberof TaskAttr
   * @static
   * @param {ITaskAttr} message TaskAttr message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskAttr.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.exclusiveMask != null &&
      Object.hasOwnProperty.call(message, "exclusiveMask")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.exclusiveMask);
    if (
      message.priority != null &&
      Object.hasOwnProperty.call(message, "priority")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.priority);
    return writer;
  };

  /**
   * Encodes the specified TaskAttr message, length delimited. Does not implicitly {@link TaskAttr.verify|verify} messages.
   * @function encodeDelimited
   * @memberof TaskAttr
   * @static
   * @param {ITaskAttr} message TaskAttr message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskAttr.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a TaskAttr message from the specified reader or buffer.
   * @function decode
   * @memberof TaskAttr
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {TaskAttr} TaskAttr
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskAttr.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.TaskAttr();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.exclusiveMask = reader.int32();
          break;
        }
        case 2: {
          message.priority = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a TaskAttr message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof TaskAttr
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {TaskAttr} TaskAttr
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskAttr.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a TaskAttr message.
   * @function verify
   * @memberof TaskAttr
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  TaskAttr.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.exclusiveMask != null &&
      Object.hasOwnProperty.call(message, "exclusiveMask")
    )
      if (!$util.isInteger(message.exclusiveMask))
        return "exclusiveMask: integer expected";
    if (
      message.priority != null &&
      Object.hasOwnProperty.call(message, "priority")
    )
      if (!$util.isInteger(message.priority))
        return "priority: integer expected";
    return null;
  };

  /**
   * Creates a TaskAttr message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof TaskAttr
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {TaskAttr} TaskAttr
   */
  TaskAttr.fromObject = function fromObject(object, long) {
    if (object instanceof $root.TaskAttr) return object;
    if (!$util.isObject(object)) throw TypeError(".TaskAttr: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.TaskAttr();
    if (object.exclusiveMask != null)
      message.exclusiveMask = object.exclusiveMask | 0;
    if (object.priority != null) message.priority = object.priority | 0;
    return message;
  };

  /**
   * Creates a plain object from a TaskAttr message. Also converts values to other types if specified.
   * @function toObject
   * @memberof TaskAttr
   * @static
   * @param {TaskAttr} message TaskAttr
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  TaskAttr.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.exclusiveMask = 0;
      object.priority = 0;
    }
    if (
      message.exclusiveMask != null &&
      Object.hasOwnProperty.call(message, "exclusiveMask")
    )
      object.exclusiveMask = message.exclusiveMask;
    if (
      message.priority != null &&
      Object.hasOwnProperty.call(message, "priority")
    )
      object.priority = message.priority;
    return object;
  };

  /**
   * Converts this TaskAttr to JSON.
   * @function toJSON
   * @memberof TaskAttr
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  TaskAttr.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for TaskAttr
   * @function getTypeUrl
   * @memberof TaskAttr
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  TaskAttr.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/TaskAttr";
  };

  return TaskAttr;
})();

$root.TaskState = (function () {
  /**
   * Properties of a TaskState.
   * @exports ITaskState
   * @interface ITaskState
   * @property {notify.OperationState|null} [baseState] TaskState baseState
   * @property {notify.AstroState|null} [astroExtendedState] TaskState astroExtendedState
   */

  /**
   * Constructs a new TaskState.
   * @exports TaskState
   * @classdesc Represents a TaskState.
   * @implements ITaskState
   * @constructor
   * @param {ITaskState=} [properties] Properties to set
   */
  function TaskState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * TaskState baseState.
   * @member {notify.OperationState} baseState
   * @memberof TaskState
   * @instance
   */
  TaskState.prototype.baseState = 0;

  /**
   * TaskState astroExtendedState.
   * @member {notify.AstroState|null|undefined} astroExtendedState
   * @memberof TaskState
   * @instance
   */
  TaskState.prototype.astroExtendedState = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * TaskState extendedState.
   * @member {"astroExtendedState"|undefined} extendedState
   * @memberof TaskState
   * @instance
   */
  Object.defineProperty(TaskState.prototype, "extendedState", {
    get: $util.oneOfGetter(($oneOfFields = ["astroExtendedState"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new TaskState instance using the specified properties.
   * @function create
   * @memberof TaskState
   * @static
   * @param {ITaskState=} [properties] Properties to set
   * @returns {TaskState} TaskState instance
   */
  TaskState.create = function create(properties) {
    return new TaskState(properties);
  };

  /**
   * Encodes the specified TaskState message. Does not implicitly {@link TaskState.verify|verify} messages.
   * @function encode
   * @memberof TaskState
   * @static
   * @param {ITaskState} message TaskState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.baseState != null &&
      Object.hasOwnProperty.call(message, "baseState")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.baseState);
    if (
      message.astroExtendedState != null &&
      Object.hasOwnProperty.call(message, "astroExtendedState")
    )
      writer
        .uint32(/* id 2, wireType 0 =*/ 16)
        .int32(message.astroExtendedState);
    return writer;
  };

  /**
   * Encodes the specified TaskState message, length delimited. Does not implicitly {@link TaskState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof TaskState
   * @static
   * @param {ITaskState} message TaskState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskState.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a TaskState message from the specified reader or buffer.
   * @function decode
   * @memberof TaskState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {TaskState} TaskState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.TaskState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.baseState = reader.int32();
          break;
        }
        case 2: {
          message.astroExtendedState = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a TaskState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof TaskState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {TaskState} TaskState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a TaskState message.
   * @function verify
   * @memberof TaskState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  TaskState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.baseState != null &&
      Object.hasOwnProperty.call(message, "baseState")
    )
      switch (message.baseState) {
        default:
          return "baseState: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
          break;
      }
    if (
      message.astroExtendedState != null &&
      Object.hasOwnProperty.call(message, "astroExtendedState")
    ) {
      properties.extendedState = 1;
      switch (message.astroExtendedState) {
        default:
          return "astroExtendedState: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    }
    return null;
  };

  /**
   * Creates a TaskState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof TaskState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {TaskState} TaskState
   */
  TaskState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.TaskState) return object;
    if (!$util.isObject(object)) throw TypeError(".TaskState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.TaskState();
    switch (object.baseState) {
      default:
        if (typeof object.baseState === "number") {
          message.baseState = object.baseState;
          break;
        }
        break;
      case "OPERATION_STATE_IDLE":
      case 0:
        message.baseState = 0;
        break;
      case "OPERATION_STATE_RUNNING":
      case 1:
        message.baseState = 1;
        break;
      case "OPERATION_STATE_STOPPING":
      case 2:
        message.baseState = 2;
        break;
      case "OPERATION_STATE_STOPPED":
      case 3:
        message.baseState = 3;
        break;
    }
    switch (object.astroExtendedState) {
      default:
        if (typeof object.astroExtendedState === "number") {
          message.astroExtendedState = object.astroExtendedState;
          break;
        }
        break;
      case "ASTRO_STATE_IDLE":
      case 0:
        message.astroExtendedState = 0;
        break;
      case "ASTRO_STATE_RUNNING":
      case 1:
        message.astroExtendedState = 1;
        break;
      case "ASTRO_STATE_STOPPING":
      case 2:
        message.astroExtendedState = 2;
        break;
      case "ASTRO_STATE_STOPPED":
      case 3:
        message.astroExtendedState = 3;
        break;
      case "ASTRO_STATE_PLATE_SOLVING":
      case 4:
        message.astroExtendedState = 4;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a TaskState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof TaskState
   * @static
   * @param {TaskState} message TaskState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  TaskState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults)
      object.baseState = options.enums === String ? "OPERATION_STATE_IDLE" : 0;
    if (
      message.baseState != null &&
      Object.hasOwnProperty.call(message, "baseState")
    )
      object.baseState =
        options.enums === String
          ? $root.notify.OperationState[message.baseState] === undefined
            ? message.baseState
            : $root.notify.OperationState[message.baseState]
          : message.baseState;
    if (
      message.astroExtendedState != null &&
      Object.hasOwnProperty.call(message, "astroExtendedState")
    ) {
      object.astroExtendedState =
        options.enums === String
          ? $root.notify.AstroState[message.astroExtendedState] === undefined
            ? message.astroExtendedState
            : $root.notify.AstroState[message.astroExtendedState]
          : message.astroExtendedState;
      if (options.oneofs) object.extendedState = "astroExtendedState";
    }
    return object;
  };

  /**
   * Converts this TaskState to JSON.
   * @function toJSON
   * @memberof TaskState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  TaskState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for TaskState
   * @function getTypeUrl
   * @memberof TaskState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  TaskState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/TaskState";
  };

  return TaskState;
})();

$root.TaskParam = (function () {
  /**
   * Properties of a TaskParam.
   * @exports ITaskParam
   * @interface ITaskParam
   * @property {IPanoramaUploadParam|null} [panoramaUpload] TaskParam panoramaUpload
   * @property {IMakeFitsThumbTaskParam|null} [makeFitsThumbTaskParam] TaskParam makeFitsThumbTaskParam
   * @property {IRepostprocessTaskParam|null} [repostprocessTaskParam] TaskParam repostprocessTaskParam
   * @property {ICaptureCaliFrameTaskParam|null} [captureCaliFrameTaskParam] TaskParam captureCaliFrameTaskParam
   */

  /**
   * Constructs a new TaskParam.
   * @exports TaskParam
   * @classdesc Represents a TaskParam.
   * @implements ITaskParam
   * @constructor
   * @param {ITaskParam=} [properties] Properties to set
   */
  function TaskParam(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * TaskParam panoramaUpload.
   * @member {IPanoramaUploadParam|null|undefined} panoramaUpload
   * @memberof TaskParam
   * @instance
   */
  TaskParam.prototype.panoramaUpload = null;

  /**
   * TaskParam makeFitsThumbTaskParam.
   * @member {IMakeFitsThumbTaskParam|null|undefined} makeFitsThumbTaskParam
   * @memberof TaskParam
   * @instance
   */
  TaskParam.prototype.makeFitsThumbTaskParam = null;

  /**
   * TaskParam repostprocessTaskParam.
   * @member {IRepostprocessTaskParam|null|undefined} repostprocessTaskParam
   * @memberof TaskParam
   * @instance
   */
  TaskParam.prototype.repostprocessTaskParam = null;

  /**
   * TaskParam captureCaliFrameTaskParam.
   * @member {ICaptureCaliFrameTaskParam|null|undefined} captureCaliFrameTaskParam
   * @memberof TaskParam
   * @instance
   */
  TaskParam.prototype.captureCaliFrameTaskParam = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * TaskParam param.
   * @member {"panoramaUpload"|"makeFitsThumbTaskParam"|"repostprocessTaskParam"|"captureCaliFrameTaskParam"|undefined} param
   * @memberof TaskParam
   * @instance
   */
  Object.defineProperty(TaskParam.prototype, "param", {
    get: $util.oneOfGetter(
      ($oneOfFields = [
        "panoramaUpload",
        "makeFitsThumbTaskParam",
        "repostprocessTaskParam",
        "captureCaliFrameTaskParam",
      ]),
    ),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new TaskParam instance using the specified properties.
   * @function create
   * @memberof TaskParam
   * @static
   * @param {ITaskParam=} [properties] Properties to set
   * @returns {TaskParam} TaskParam instance
   */
  TaskParam.create = function create(properties) {
    return new TaskParam(properties);
  };

  /**
   * Encodes the specified TaskParam message. Does not implicitly {@link TaskParam.verify|verify} messages.
   * @function encode
   * @memberof TaskParam
   * @static
   * @param {ITaskParam} message TaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskParam.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.panoramaUpload != null &&
      Object.hasOwnProperty.call(message, "panoramaUpload")
    )
      $root.PanoramaUploadParam.encode(
        message.panoramaUpload,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.makeFitsThumbTaskParam != null &&
      Object.hasOwnProperty.call(message, "makeFitsThumbTaskParam")
    )
      $root.MakeFitsThumbTaskParam.encode(
        message.makeFitsThumbTaskParam,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.repostprocessTaskParam != null &&
      Object.hasOwnProperty.call(message, "repostprocessTaskParam")
    )
      $root.RepostprocessTaskParam.encode(
        message.repostprocessTaskParam,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.captureCaliFrameTaskParam != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameTaskParam")
    )
      $root.CaptureCaliFrameTaskParam.encode(
        message.captureCaliFrameTaskParam,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified TaskParam message, length delimited. Does not implicitly {@link TaskParam.verify|verify} messages.
   * @function encodeDelimited
   * @memberof TaskParam
   * @static
   * @param {ITaskParam} message TaskParam message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TaskParam.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a TaskParam message from the specified reader or buffer.
   * @function decode
   * @memberof TaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {TaskParam} TaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskParam.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.TaskParam();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.panoramaUpload = $root.PanoramaUploadParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.makeFitsThumbTaskParam = $root.MakeFitsThumbTaskParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.repostprocessTaskParam = $root.RepostprocessTaskParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.captureCaliFrameTaskParam =
            $root.CaptureCaliFrameTaskParam.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a TaskParam message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof TaskParam
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {TaskParam} TaskParam
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TaskParam.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a TaskParam message.
   * @function verify
   * @memberof TaskParam
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  TaskParam.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.panoramaUpload != null &&
      Object.hasOwnProperty.call(message, "panoramaUpload")
    ) {
      properties.param = 1;
      {
        var error = $root.PanoramaUploadParam.verify(
          message.panoramaUpload,
          long + 1,
        );
        if (error) return "panoramaUpload." + error;
      }
    }
    if (
      message.makeFitsThumbTaskParam != null &&
      Object.hasOwnProperty.call(message, "makeFitsThumbTaskParam")
    ) {
      if (properties.param === 1) return "param: multiple values";
      properties.param = 1;
      {
        var error = $root.MakeFitsThumbTaskParam.verify(
          message.makeFitsThumbTaskParam,
          long + 1,
        );
        if (error) return "makeFitsThumbTaskParam." + error;
      }
    }
    if (
      message.repostprocessTaskParam != null &&
      Object.hasOwnProperty.call(message, "repostprocessTaskParam")
    ) {
      if (properties.param === 1) return "param: multiple values";
      properties.param = 1;
      {
        var error = $root.RepostprocessTaskParam.verify(
          message.repostprocessTaskParam,
          long + 1,
        );
        if (error) return "repostprocessTaskParam." + error;
      }
    }
    if (
      message.captureCaliFrameTaskParam != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameTaskParam")
    ) {
      if (properties.param === 1) return "param: multiple values";
      properties.param = 1;
      {
        var error = $root.CaptureCaliFrameTaskParam.verify(
          message.captureCaliFrameTaskParam,
          long + 1,
        );
        if (error) return "captureCaliFrameTaskParam." + error;
      }
    }
    return null;
  };

  /**
   * Creates a TaskParam message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof TaskParam
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {TaskParam} TaskParam
   */
  TaskParam.fromObject = function fromObject(object, long) {
    if (object instanceof $root.TaskParam) return object;
    if (!$util.isObject(object)) throw TypeError(".TaskParam: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.TaskParam();
    if (object.panoramaUpload != null) {
      if (!$util.isObject(object.panoramaUpload))
        throw TypeError(".TaskParam.panoramaUpload: object expected");
      message.panoramaUpload = $root.PanoramaUploadParam.fromObject(
        object.panoramaUpload,
        long + 1,
      );
    }
    if (object.makeFitsThumbTaskParam != null) {
      if (!$util.isObject(object.makeFitsThumbTaskParam))
        throw TypeError(".TaskParam.makeFitsThumbTaskParam: object expected");
      message.makeFitsThumbTaskParam = $root.MakeFitsThumbTaskParam.fromObject(
        object.makeFitsThumbTaskParam,
        long + 1,
      );
    }
    if (object.repostprocessTaskParam != null) {
      if (!$util.isObject(object.repostprocessTaskParam))
        throw TypeError(".TaskParam.repostprocessTaskParam: object expected");
      message.repostprocessTaskParam = $root.RepostprocessTaskParam.fromObject(
        object.repostprocessTaskParam,
        long + 1,
      );
    }
    if (object.captureCaliFrameTaskParam != null) {
      if (!$util.isObject(object.captureCaliFrameTaskParam))
        throw TypeError(
          ".TaskParam.captureCaliFrameTaskParam: object expected",
        );
      message.captureCaliFrameTaskParam =
        $root.CaptureCaliFrameTaskParam.fromObject(
          object.captureCaliFrameTaskParam,
          long + 1,
        );
    }
    return message;
  };

  /**
   * Creates a plain object from a TaskParam message. Also converts values to other types if specified.
   * @function toObject
   * @memberof TaskParam
   * @static
   * @param {TaskParam} message TaskParam
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  TaskParam.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.panoramaUpload != null &&
      Object.hasOwnProperty.call(message, "panoramaUpload")
    ) {
      object.panoramaUpload = $root.PanoramaUploadParam.toObject(
        message.panoramaUpload,
        options,
        q + 1,
      );
      if (options.oneofs) object.param = "panoramaUpload";
    }
    if (
      message.makeFitsThumbTaskParam != null &&
      Object.hasOwnProperty.call(message, "makeFitsThumbTaskParam")
    ) {
      object.makeFitsThumbTaskParam = $root.MakeFitsThumbTaskParam.toObject(
        message.makeFitsThumbTaskParam,
        options,
        q + 1,
      );
      if (options.oneofs) object.param = "makeFitsThumbTaskParam";
    }
    if (
      message.repostprocessTaskParam != null &&
      Object.hasOwnProperty.call(message, "repostprocessTaskParam")
    ) {
      object.repostprocessTaskParam = $root.RepostprocessTaskParam.toObject(
        message.repostprocessTaskParam,
        options,
        q + 1,
      );
      if (options.oneofs) object.param = "repostprocessTaskParam";
    }
    if (
      message.captureCaliFrameTaskParam != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameTaskParam")
    ) {
      object.captureCaliFrameTaskParam =
        $root.CaptureCaliFrameTaskParam.toObject(
          message.captureCaliFrameTaskParam,
          options,
          q + 1,
        );
      if (options.oneofs) object.param = "captureCaliFrameTaskParam";
    }
    return object;
  };

  /**
   * Converts this TaskParam to JSON.
   * @function toJSON
   * @memberof TaskParam
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  TaskParam.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for TaskParam
   * @function getTypeUrl
   * @memberof TaskParam
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  TaskParam.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/TaskParam";
  };

  return TaskParam;
})();

$root.ResNotifyTaskState = (function () {
  /**
   * Properties of a ResNotifyTaskState.
   * @exports IResNotifyTaskState
   * @interface IResNotifyTaskState
   * @property {TaskId|null} [taskId] ResNotifyTaskState taskId
   * @property {ITaskAttr|null} [taskAttr] ResNotifyTaskState taskAttr
   * @property {ITaskState|null} [state] ResNotifyTaskState state
   * @property {ITaskParam|null} [param] ResNotifyTaskState param
   */

  /**
   * Constructs a new ResNotifyTaskState.
   * @exports ResNotifyTaskState
   * @classdesc Represents a ResNotifyTaskState.
   * @implements IResNotifyTaskState
   * @constructor
   * @param {IResNotifyTaskState=} [properties] Properties to set
   */
  function ResNotifyTaskState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResNotifyTaskState taskId.
   * @member {TaskId} taskId
   * @memberof ResNotifyTaskState
   * @instance
   */
  ResNotifyTaskState.prototype.taskId = 0;

  /**
   * ResNotifyTaskState taskAttr.
   * @member {ITaskAttr|null|undefined} taskAttr
   * @memberof ResNotifyTaskState
   * @instance
   */
  ResNotifyTaskState.prototype.taskAttr = null;

  /**
   * ResNotifyTaskState state.
   * @member {ITaskState|null|undefined} state
   * @memberof ResNotifyTaskState
   * @instance
   */
  ResNotifyTaskState.prototype.state = null;

  /**
   * ResNotifyTaskState param.
   * @member {ITaskParam|null|undefined} param
   * @memberof ResNotifyTaskState
   * @instance
   */
  ResNotifyTaskState.prototype.param = null;

  /**
   * Creates a new ResNotifyTaskState instance using the specified properties.
   * @function create
   * @memberof ResNotifyTaskState
   * @static
   * @param {IResNotifyTaskState=} [properties] Properties to set
   * @returns {ResNotifyTaskState} ResNotifyTaskState instance
   */
  ResNotifyTaskState.create = function create(properties) {
    return new ResNotifyTaskState(properties);
  };

  /**
   * Encodes the specified ResNotifyTaskState message. Does not implicitly {@link ResNotifyTaskState.verify|verify} messages.
   * @function encode
   * @memberof ResNotifyTaskState
   * @static
   * @param {IResNotifyTaskState} message ResNotifyTaskState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResNotifyTaskState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.taskId);
    if (
      message.taskAttr != null &&
      Object.hasOwnProperty.call(message, "taskAttr")
    )
      $root.TaskAttr.encode(
        message.taskAttr,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      $root.TaskState.encode(
        message.state,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (message.param != null && Object.hasOwnProperty.call(message, "param"))
      $root.TaskParam.encode(
        message.param,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResNotifyTaskState message, length delimited. Does not implicitly {@link ResNotifyTaskState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResNotifyTaskState
   * @static
   * @param {IResNotifyTaskState} message ResNotifyTaskState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResNotifyTaskState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResNotifyTaskState message from the specified reader or buffer.
   * @function decode
   * @memberof ResNotifyTaskState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResNotifyTaskState} ResNotifyTaskState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResNotifyTaskState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResNotifyTaskState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.taskId = reader.int32();
          break;
        }
        case 2: {
          message.taskAttr = $root.TaskAttr.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.state = $root.TaskState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.param = $root.TaskParam.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResNotifyTaskState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResNotifyTaskState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResNotifyTaskState} ResNotifyTaskState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResNotifyTaskState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResNotifyTaskState message.
   * @function verify
   * @memberof ResNotifyTaskState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResNotifyTaskState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      switch (message.taskId) {
        default:
          return "taskId: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    if (
      message.taskAttr != null &&
      Object.hasOwnProperty.call(message, "taskAttr")
    ) {
      var error = $root.TaskAttr.verify(message.taskAttr, long + 1);
      if (error) return "taskAttr." + error;
    }
    if (message.state != null && Object.hasOwnProperty.call(message, "state")) {
      var error = $root.TaskState.verify(message.state, long + 1);
      if (error) return "state." + error;
    }
    if (message.param != null && Object.hasOwnProperty.call(message, "param")) {
      var error = $root.TaskParam.verify(message.param, long + 1);
      if (error) return "param." + error;
    }
    return null;
  };

  /**
   * Creates a ResNotifyTaskState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResNotifyTaskState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResNotifyTaskState} ResNotifyTaskState
   */
  ResNotifyTaskState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResNotifyTaskState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResNotifyTaskState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResNotifyTaskState();
    switch (object.taskId) {
      default:
        if (typeof object.taskId === "number") {
          message.taskId = object.taskId;
          break;
        }
        break;
      case "TASK_ID_IDLE":
      case 0:
        message.taskId = 0;
        break;
      case "TASK_ID_PANORAMA_UPLOAD":
      case 1:
        message.taskId = 1;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION":
      case 2:
        message.taskId = 2;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK":
      case 3:
        message.taskId = 3;
        break;
      case "TASK_ID_CAPTURE_CALI_FRAME":
      case 4:
        message.taskId = 4;
        break;
    }
    if (object.taskAttr != null) {
      if (!$util.isObject(object.taskAttr))
        throw TypeError(".ResNotifyTaskState.taskAttr: object expected");
      message.taskAttr = $root.TaskAttr.fromObject(object.taskAttr, long + 1);
    }
    if (object.state != null) {
      if (!$util.isObject(object.state))
        throw TypeError(".ResNotifyTaskState.state: object expected");
      message.state = $root.TaskState.fromObject(object.state, long + 1);
    }
    if (object.param != null) {
      if (!$util.isObject(object.param))
        throw TypeError(".ResNotifyTaskState.param: object expected");
      message.param = $root.TaskParam.fromObject(object.param, long + 1);
    }
    return message;
  };

  /**
   * Creates a plain object from a ResNotifyTaskState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResNotifyTaskState
   * @static
   * @param {ResNotifyTaskState} message ResNotifyTaskState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResNotifyTaskState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.taskId = options.enums === String ? "TASK_ID_IDLE" : 0;
      object.taskAttr = null;
      object.state = null;
      object.param = null;
    }
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      object.taskId =
        options.enums === String
          ? $root.TaskId[message.taskId] === undefined
            ? message.taskId
            : $root.TaskId[message.taskId]
          : message.taskId;
    if (
      message.taskAttr != null &&
      Object.hasOwnProperty.call(message, "taskAttr")
    )
      object.taskAttr = $root.TaskAttr.toObject(
        message.taskAttr,
        options,
        q + 1,
      );
    if (message.state != null && Object.hasOwnProperty.call(message, "state"))
      object.state = $root.TaskState.toObject(message.state, options, q + 1);
    if (message.param != null && Object.hasOwnProperty.call(message, "param"))
      object.param = $root.TaskParam.toObject(message.param, options, q + 1);
    return object;
  };

  /**
   * Converts this ResNotifyTaskState to JSON.
   * @function toJSON
   * @memberof ResNotifyTaskState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResNotifyTaskState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResNotifyTaskState
   * @function getTypeUrl
   * @memberof ResNotifyTaskState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResNotifyTaskState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResNotifyTaskState";
  };

  return ResNotifyTaskState;
})();

$root.ReqStartTask = (function () {
  /**
   * Properties of a ReqStartTask.
   * @exports IReqStartTask
   * @interface IReqStartTask
   * @property {TaskId|null} [taskId] ReqStartTask taskId
   * @property {IReqStartMakeFitsThumb|null} [reqMakeFitsThumbParam] ReqStartTask reqMakeFitsThumbParam
   * @property {IReqStartRepostprocess|null} [reqRepostprocessParam] ReqStartTask reqRepostprocessParam
   */

  /**
   * Constructs a new ReqStartTask.
   * @exports ReqStartTask
   * @classdesc Represents a ReqStartTask.
   * @implements IReqStartTask
   * @constructor
   * @param {IReqStartTask=} [properties] Properties to set
   */
  function ReqStartTask(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartTask taskId.
   * @member {TaskId} taskId
   * @memberof ReqStartTask
   * @instance
   */
  ReqStartTask.prototype.taskId = 0;

  /**
   * ReqStartTask reqMakeFitsThumbParam.
   * @member {IReqStartMakeFitsThumb|null|undefined} reqMakeFitsThumbParam
   * @memberof ReqStartTask
   * @instance
   */
  ReqStartTask.prototype.reqMakeFitsThumbParam = null;

  /**
   * ReqStartTask reqRepostprocessParam.
   * @member {IReqStartRepostprocess|null|undefined} reqRepostprocessParam
   * @memberof ReqStartTask
   * @instance
   */
  ReqStartTask.prototype.reqRepostprocessParam = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * ReqStartTask param.
   * @member {"reqMakeFitsThumbParam"|"reqRepostprocessParam"|undefined} param
   * @memberof ReqStartTask
   * @instance
   */
  Object.defineProperty(ReqStartTask.prototype, "param", {
    get: $util.oneOfGetter(
      ($oneOfFields = ["reqMakeFitsThumbParam", "reqRepostprocessParam"]),
    ),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ReqStartTask instance using the specified properties.
   * @function create
   * @memberof ReqStartTask
   * @static
   * @param {IReqStartTask=} [properties] Properties to set
   * @returns {ReqStartTask} ReqStartTask instance
   */
  ReqStartTask.create = function create(properties) {
    return new ReqStartTask(properties);
  };

  /**
   * Encodes the specified ReqStartTask message. Does not implicitly {@link ReqStartTask.verify|verify} messages.
   * @function encode
   * @memberof ReqStartTask
   * @static
   * @param {IReqStartTask} message ReqStartTask message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTask.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.taskId);
    if (
      message.reqMakeFitsThumbParam != null &&
      Object.hasOwnProperty.call(message, "reqMakeFitsThumbParam")
    )
      $root.ReqStartMakeFitsThumb.encode(
        message.reqMakeFitsThumbParam,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.reqRepostprocessParam != null &&
      Object.hasOwnProperty.call(message, "reqRepostprocessParam")
    )
      $root.ReqStartRepostprocess.encode(
        message.reqRepostprocessParam,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqStartTask message, length delimited. Does not implicitly {@link ReqStartTask.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartTask
   * @static
   * @param {IReqStartTask} message ReqStartTask message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTask.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartTask message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartTask
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartTask} ReqStartTask
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTask.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartTask();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.taskId = reader.int32();
          break;
        }
        case 2: {
          message.reqMakeFitsThumbParam = $root.ReqStartMakeFitsThumb.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.reqRepostprocessParam = $root.ReqStartRepostprocess.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartTask message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartTask
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartTask} ReqStartTask
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTask.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartTask message.
   * @function verify
   * @memberof ReqStartTask
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartTask.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      switch (message.taskId) {
        default:
          return "taskId: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    if (
      message.reqMakeFitsThumbParam != null &&
      Object.hasOwnProperty.call(message, "reqMakeFitsThumbParam")
    ) {
      properties.param = 1;
      {
        var error = $root.ReqStartMakeFitsThumb.verify(
          message.reqMakeFitsThumbParam,
          long + 1,
        );
        if (error) return "reqMakeFitsThumbParam." + error;
      }
    }
    if (
      message.reqRepostprocessParam != null &&
      Object.hasOwnProperty.call(message, "reqRepostprocessParam")
    ) {
      if (properties.param === 1) return "param: multiple values";
      properties.param = 1;
      {
        var error = $root.ReqStartRepostprocess.verify(
          message.reqRepostprocessParam,
          long + 1,
        );
        if (error) return "reqRepostprocessParam." + error;
      }
    }
    return null;
  };

  /**
   * Creates a ReqStartTask message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartTask
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartTask} ReqStartTask
   */
  ReqStartTask.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartTask) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartTask: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartTask();
    switch (object.taskId) {
      default:
        if (typeof object.taskId === "number") {
          message.taskId = object.taskId;
          break;
        }
        break;
      case "TASK_ID_IDLE":
      case 0:
        message.taskId = 0;
        break;
      case "TASK_ID_PANORAMA_UPLOAD":
      case 1:
        message.taskId = 1;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION":
      case 2:
        message.taskId = 2;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK":
      case 3:
        message.taskId = 3;
        break;
      case "TASK_ID_CAPTURE_CALI_FRAME":
      case 4:
        message.taskId = 4;
        break;
    }
    if (object.reqMakeFitsThumbParam != null) {
      if (!$util.isObject(object.reqMakeFitsThumbParam))
        throw TypeError(".ReqStartTask.reqMakeFitsThumbParam: object expected");
      message.reqMakeFitsThumbParam = $root.ReqStartMakeFitsThumb.fromObject(
        object.reqMakeFitsThumbParam,
        long + 1,
      );
    }
    if (object.reqRepostprocessParam != null) {
      if (!$util.isObject(object.reqRepostprocessParam))
        throw TypeError(".ReqStartTask.reqRepostprocessParam: object expected");
      message.reqRepostprocessParam = $root.ReqStartRepostprocess.fromObject(
        object.reqRepostprocessParam,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqStartTask message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartTask
   * @static
   * @param {ReqStartTask} message ReqStartTask
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartTask.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults)
      object.taskId = options.enums === String ? "TASK_ID_IDLE" : 0;
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      object.taskId =
        options.enums === String
          ? $root.TaskId[message.taskId] === undefined
            ? message.taskId
            : $root.TaskId[message.taskId]
          : message.taskId;
    if (
      message.reqMakeFitsThumbParam != null &&
      Object.hasOwnProperty.call(message, "reqMakeFitsThumbParam")
    ) {
      object.reqMakeFitsThumbParam = $root.ReqStartMakeFitsThumb.toObject(
        message.reqMakeFitsThumbParam,
        options,
        q + 1,
      );
      if (options.oneofs) object.param = "reqMakeFitsThumbParam";
    }
    if (
      message.reqRepostprocessParam != null &&
      Object.hasOwnProperty.call(message, "reqRepostprocessParam")
    ) {
      object.reqRepostprocessParam = $root.ReqStartRepostprocess.toObject(
        message.reqRepostprocessParam,
        options,
        q + 1,
      );
      if (options.oneofs) object.param = "reqRepostprocessParam";
    }
    return object;
  };

  /**
   * Converts this ReqStartTask to JSON.
   * @function toJSON
   * @memberof ReqStartTask
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartTask.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartTask
   * @function getTypeUrl
   * @memberof ReqStartTask
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartTask";
  };

  return ReqStartTask;
})();

$root.ReqStopTask = (function () {
  /**
   * Properties of a ReqStopTask.
   * @exports IReqStopTask
   * @interface IReqStopTask
   * @property {TaskId|null} [taskId] ReqStopTask taskId
   */

  /**
   * Constructs a new ReqStopTask.
   * @exports ReqStopTask
   * @classdesc Represents a ReqStopTask.
   * @implements IReqStopTask
   * @constructor
   * @param {IReqStopTask=} [properties] Properties to set
   */
  function ReqStopTask(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStopTask taskId.
   * @member {TaskId} taskId
   * @memberof ReqStopTask
   * @instance
   */
  ReqStopTask.prototype.taskId = 0;

  /**
   * Creates a new ReqStopTask instance using the specified properties.
   * @function create
   * @memberof ReqStopTask
   * @static
   * @param {IReqStopTask=} [properties] Properties to set
   * @returns {ReqStopTask} ReqStopTask instance
   */
  ReqStopTask.create = function create(properties) {
    return new ReqStopTask(properties);
  };

  /**
   * Encodes the specified ReqStopTask message. Does not implicitly {@link ReqStopTask.verify|verify} messages.
   * @function encode
   * @memberof ReqStopTask
   * @static
   * @param {IReqStopTask} message ReqStopTask message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTask.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.taskId);
    return writer;
  };

  /**
   * Encodes the specified ReqStopTask message, length delimited. Does not implicitly {@link ReqStopTask.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopTask
   * @static
   * @param {IReqStopTask} message ReqStopTask message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTask.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopTask message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopTask
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopTask} ReqStopTask
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTask.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopTask();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.taskId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopTask message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopTask
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopTask} ReqStopTask
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTask.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopTask message.
   * @function verify
   * @memberof ReqStopTask
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopTask.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      switch (message.taskId) {
        default:
          return "taskId: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    return null;
  };

  /**
   * Creates a ReqStopTask message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopTask
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopTask} ReqStopTask
   */
  ReqStopTask.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopTask) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStopTask: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStopTask();
    switch (object.taskId) {
      default:
        if (typeof object.taskId === "number") {
          message.taskId = object.taskId;
          break;
        }
        break;
      case "TASK_ID_IDLE":
      case 0:
        message.taskId = 0;
        break;
      case "TASK_ID_PANORAMA_UPLOAD":
      case 1:
        message.taskId = 1;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION":
      case 2:
        message.taskId = 2;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK":
      case 3:
        message.taskId = 3;
        break;
      case "TASK_ID_CAPTURE_CALI_FRAME":
      case 4:
        message.taskId = 4;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqStopTask message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopTask
   * @static
   * @param {ReqStopTask} message ReqStopTask
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopTask.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults)
      object.taskId = options.enums === String ? "TASK_ID_IDLE" : 0;
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      object.taskId =
        options.enums === String
          ? $root.TaskId[message.taskId] === undefined
            ? message.taskId
            : $root.TaskId[message.taskId]
          : message.taskId;
    return object;
  };

  /**
   * Converts this ReqStopTask to JSON.
   * @function toJSON
   * @memberof ReqStopTask
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopTask.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopTask
   * @function getTypeUrl
   * @memberof ReqStopTask
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopTask";
  };

  return ReqStopTask;
})();

$root.ResTaskCenter = (function () {
  /**
   * Properties of a ResTaskCenter.
   * @exports IResTaskCenter
   * @interface IResTaskCenter
   * @property {number|null} [code] ResTaskCenter code
   * @property {TaskId|null} [taskId] ResTaskCenter taskId
   */

  /**
   * Constructs a new ResTaskCenter.
   * @exports ResTaskCenter
   * @classdesc Represents a ResTaskCenter.
   * @implements IResTaskCenter
   * @constructor
   * @param {IResTaskCenter=} [properties] Properties to set
   */
  function ResTaskCenter(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResTaskCenter code.
   * @member {number} code
   * @memberof ResTaskCenter
   * @instance
   */
  ResTaskCenter.prototype.code = 0;

  /**
   * ResTaskCenter taskId.
   * @member {TaskId} taskId
   * @memberof ResTaskCenter
   * @instance
   */
  ResTaskCenter.prototype.taskId = 0;

  /**
   * Creates a new ResTaskCenter instance using the specified properties.
   * @function create
   * @memberof ResTaskCenter
   * @static
   * @param {IResTaskCenter=} [properties] Properties to set
   * @returns {ResTaskCenter} ResTaskCenter instance
   */
  ResTaskCenter.create = function create(properties) {
    return new ResTaskCenter(properties);
  };

  /**
   * Encodes the specified ResTaskCenter message. Does not implicitly {@link ResTaskCenter.verify|verify} messages.
   * @function encode
   * @memberof ResTaskCenter
   * @static
   * @param {IResTaskCenter} message ResTaskCenter message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResTaskCenter.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.taskId);
    return writer;
  };

  /**
   * Encodes the specified ResTaskCenter message, length delimited. Does not implicitly {@link ResTaskCenter.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResTaskCenter
   * @static
   * @param {IResTaskCenter} message ResTaskCenter message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResTaskCenter.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResTaskCenter message from the specified reader or buffer.
   * @function decode
   * @memberof ResTaskCenter
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResTaskCenter} ResTaskCenter
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResTaskCenter.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResTaskCenter();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.taskId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResTaskCenter message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResTaskCenter
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResTaskCenter} ResTaskCenter
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResTaskCenter.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResTaskCenter message.
   * @function verify
   * @memberof ResTaskCenter
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResTaskCenter.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      switch (message.taskId) {
        default:
          return "taskId: enum value expected";
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
          break;
      }
    return null;
  };

  /**
   * Creates a ResTaskCenter message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResTaskCenter
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResTaskCenter} ResTaskCenter
   */
  ResTaskCenter.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResTaskCenter) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResTaskCenter: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResTaskCenter();
    if (object.code != null) message.code = object.code | 0;
    switch (object.taskId) {
      default:
        if (typeof object.taskId === "number") {
          message.taskId = object.taskId;
          break;
        }
        break;
      case "TASK_ID_IDLE":
      case 0:
        message.taskId = 0;
        break;
      case "TASK_ID_PANORAMA_UPLOAD":
      case 1:
        message.taskId = 1;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK_THUMBNAIL_GENERATION":
      case 2:
        message.taskId = 2;
        break;
      case "TASK_ID_ASTRO_MULTI_STACK":
      case 3:
        message.taskId = 3;
        break;
      case "TASK_ID_CAPTURE_CALI_FRAME":
      case 4:
        message.taskId = 4;
        break;
    }
    return message;
  };

  /**
   * Creates a plain object from a ResTaskCenter message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResTaskCenter
   * @static
   * @param {ResTaskCenter} message ResTaskCenter
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResTaskCenter.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.taskId = options.enums === String ? "TASK_ID_IDLE" : 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (message.taskId != null && Object.hasOwnProperty.call(message, "taskId"))
      object.taskId =
        options.enums === String
          ? $root.TaskId[message.taskId] === undefined
            ? message.taskId
            : $root.TaskId[message.taskId]
          : message.taskId;
    return object;
  };

  /**
   * Converts this ResTaskCenter to JSON.
   * @function toJSON
   * @memberof ResTaskCenter
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResTaskCenter.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResTaskCenter
   * @function getTypeUrl
   * @memberof ResTaskCenter
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResTaskCenter.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResTaskCenter";
  };

  return ResTaskCenter;
})();

$root.ClientParams = (function () {
  /**
   * Properties of a ClientParams.
   * @exports IClientParams
   * @interface IClientParams
   * @property {number|null} [encodeType] ClientParams encodeType
   */

  /**
   * Constructs a new ClientParams.
   * @exports ClientParams
   * @classdesc Represents a ClientParams.
   * @implements IClientParams
   * @constructor
   * @param {IClientParams=} [properties] Properties to set
   */
  function ClientParams(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ClientParams encodeType.
   * @member {number} encodeType
   * @memberof ClientParams
   * @instance
   */
  ClientParams.prototype.encodeType = 0;

  /**
   * Creates a new ClientParams instance using the specified properties.
   * @function create
   * @memberof ClientParams
   * @static
   * @param {IClientParams=} [properties] Properties to set
   * @returns {ClientParams} ClientParams instance
   */
  ClientParams.create = function create(properties) {
    return new ClientParams(properties);
  };

  /**
   * Encodes the specified ClientParams message. Does not implicitly {@link ClientParams.verify|verify} messages.
   * @function encode
   * @memberof ClientParams
   * @static
   * @param {IClientParams} message ClientParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ClientParams.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.encodeType);
    return writer;
  };

  /**
   * Encodes the specified ClientParams message, length delimited. Does not implicitly {@link ClientParams.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ClientParams
   * @static
   * @param {IClientParams} message ClientParams message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ClientParams.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ClientParams message from the specified reader or buffer.
   * @function decode
   * @memberof ClientParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ClientParams} ClientParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ClientParams.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ClientParams();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.encodeType = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ClientParams message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ClientParams
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ClientParams} ClientParams
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ClientParams.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ClientParams message.
   * @function verify
   * @memberof ClientParams
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ClientParams.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      if (!$util.isInteger(message.encodeType))
        return "encodeType: integer expected";
    return null;
  };

  /**
   * Creates a ClientParams message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ClientParams
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ClientParams} ClientParams
   */
  ClientParams.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ClientParams) return object;
    if (!$util.isObject(object))
      throw TypeError(".ClientParams: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ClientParams();
    if (object.encodeType != null) message.encodeType = object.encodeType | 0;
    return message;
  };

  /**
   * Creates a plain object from a ClientParams message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ClientParams
   * @static
   * @param {ClientParams} message ClientParams
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ClientParams.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.encodeType = 0;
    if (
      message.encodeType != null &&
      Object.hasOwnProperty.call(message, "encodeType")
    )
      object.encodeType = message.encodeType;
    return object;
  };

  /**
   * Converts this ClientParams to JSON.
   * @function toJSON
   * @memberof ClientParams
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ClientParams.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ClientParams
   * @function getTypeUrl
   * @memberof ClientParams
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ClientParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ClientParams";
  };

  return ClientParams;
})();

$root.ReqEnterCamera = (function () {
  /**
   * Properties of a ReqEnterCamera.
   * @exports IReqEnterCamera
   * @interface IReqEnterCamera
   * @property {IClientParams|null} [clientParam] ReqEnterCamera clientParam
   */

  /**
   * Constructs a new ReqEnterCamera.
   * @exports ReqEnterCamera
   * @classdesc Represents a ReqEnterCamera.
   * @implements IReqEnterCamera
   * @constructor
   * @param {IReqEnterCamera=} [properties] Properties to set
   */
  function ReqEnterCamera(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqEnterCamera clientParam.
   * @member {IClientParams|null|undefined} clientParam
   * @memberof ReqEnterCamera
   * @instance
   */
  ReqEnterCamera.prototype.clientParam = null;

  /**
   * Creates a new ReqEnterCamera instance using the specified properties.
   * @function create
   * @memberof ReqEnterCamera
   * @static
   * @param {IReqEnterCamera=} [properties] Properties to set
   * @returns {ReqEnterCamera} ReqEnterCamera instance
   */
  ReqEnterCamera.create = function create(properties) {
    return new ReqEnterCamera(properties);
  };

  /**
   * Encodes the specified ReqEnterCamera message. Does not implicitly {@link ReqEnterCamera.verify|verify} messages.
   * @function encode
   * @memberof ReqEnterCamera
   * @static
   * @param {IReqEnterCamera} message ReqEnterCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqEnterCamera.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.clientParam != null &&
      Object.hasOwnProperty.call(message, "clientParam")
    )
      $root.ClientParams.encode(
        message.clientParam,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ReqEnterCamera message, length delimited. Does not implicitly {@link ReqEnterCamera.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqEnterCamera
   * @static
   * @param {IReqEnterCamera} message ReqEnterCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqEnterCamera.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqEnterCamera message from the specified reader or buffer.
   * @function decode
   * @memberof ReqEnterCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqEnterCamera} ReqEnterCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqEnterCamera.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqEnterCamera();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 3: {
          message.clientParam = $root.ClientParams.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqEnterCamera message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqEnterCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqEnterCamera} ReqEnterCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqEnterCamera.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqEnterCamera message.
   * @function verify
   * @memberof ReqEnterCamera
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqEnterCamera.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.clientParam != null &&
      Object.hasOwnProperty.call(message, "clientParam")
    ) {
      var error = $root.ClientParams.verify(message.clientParam, long + 1);
      if (error) return "clientParam." + error;
    }
    return null;
  };

  /**
   * Creates a ReqEnterCamera message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqEnterCamera
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqEnterCamera} ReqEnterCamera
   */
  ReqEnterCamera.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqEnterCamera) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqEnterCamera: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqEnterCamera();
    if (object.clientParam != null) {
      if (!$util.isObject(object.clientParam))
        throw TypeError(".ReqEnterCamera.clientParam: object expected");
      message.clientParam = $root.ClientParams.fromObject(
        object.clientParam,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ReqEnterCamera message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqEnterCamera
   * @static
   * @param {ReqEnterCamera} message ReqEnterCamera
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqEnterCamera.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.clientParam = null;
    if (
      message.clientParam != null &&
      Object.hasOwnProperty.call(message, "clientParam")
    )
      object.clientParam = $root.ClientParams.toObject(
        message.clientParam,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ReqEnterCamera to JSON.
   * @function toJSON
   * @memberof ReqEnterCamera
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqEnterCamera.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqEnterCamera
   * @function getTypeUrl
   * @memberof ReqEnterCamera
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqEnterCamera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqEnterCamera";
  };

  return ReqEnterCamera;
})();

$root.ResEnterCamera = (function () {
  /**
   * Properties of a ResEnterCamera.
   * @exports IResEnterCamera
   * @interface IResEnterCamera
   * @property {number|null} [code] ResEnterCamera code
   * @property {number|null} [shootingModeId] ResEnterCamera shootingModeId
   */

  /**
   * Constructs a new ResEnterCamera.
   * @exports ResEnterCamera
   * @classdesc Represents a ResEnterCamera.
   * @implements IResEnterCamera
   * @constructor
   * @param {IResEnterCamera=} [properties] Properties to set
   */
  function ResEnterCamera(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResEnterCamera code.
   * @member {number} code
   * @memberof ResEnterCamera
   * @instance
   */
  ResEnterCamera.prototype.code = 0;

  /**
   * ResEnterCamera shootingModeId.
   * @member {number} shootingModeId
   * @memberof ResEnterCamera
   * @instance
   */
  ResEnterCamera.prototype.shootingModeId = 0;

  /**
   * Creates a new ResEnterCamera instance using the specified properties.
   * @function create
   * @memberof ResEnterCamera
   * @static
   * @param {IResEnterCamera=} [properties] Properties to set
   * @returns {ResEnterCamera} ResEnterCamera instance
   */
  ResEnterCamera.create = function create(properties) {
    return new ResEnterCamera(properties);
  };

  /**
   * Encodes the specified ResEnterCamera message. Does not implicitly {@link ResEnterCamera.verify|verify} messages.
   * @function encode
   * @memberof ResEnterCamera
   * @static
   * @param {IResEnterCamera} message ResEnterCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResEnterCamera.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.shootingModeId);
    return writer;
  };

  /**
   * Encodes the specified ResEnterCamera message, length delimited. Does not implicitly {@link ResEnterCamera.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResEnterCamera
   * @static
   * @param {IResEnterCamera} message ResEnterCamera message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResEnterCamera.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResEnterCamera message from the specified reader or buffer.
   * @function decode
   * @memberof ResEnterCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResEnterCamera} ResEnterCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResEnterCamera.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResEnterCamera();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.shootingModeId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResEnterCamera message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResEnterCamera
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResEnterCamera} ResEnterCamera
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResEnterCamera.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResEnterCamera message.
   * @function verify
   * @memberof ResEnterCamera
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResEnterCamera.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      if (!$util.isInteger(message.shootingModeId))
        return "shootingModeId: integer expected";
    return null;
  };

  /**
   * Creates a ResEnterCamera message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResEnterCamera
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResEnterCamera} ResEnterCamera
   */
  ResEnterCamera.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResEnterCamera) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResEnterCamera: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResEnterCamera();
    if (object.code != null) message.code = object.code | 0;
    if (object.shootingModeId != null)
      message.shootingModeId = object.shootingModeId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResEnterCamera message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResEnterCamera
   * @static
   * @param {ResEnterCamera} message ResEnterCamera
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResEnterCamera.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.shootingModeId = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      object.shootingModeId = message.shootingModeId;
    return object;
  };

  /**
   * Converts this ResEnterCamera to JSON.
   * @function toJSON
   * @memberof ResEnterCamera
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResEnterCamera.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResEnterCamera
   * @function getTypeUrl
   * @memberof ResEnterCamera
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResEnterCamera.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResEnterCamera";
  };

  return ResEnterCamera;
})();

$root.ReqSwitchShootingMode = (function () {
  /**
   * Properties of a ReqSwitchShootingMode.
   * @exports IReqSwitchShootingMode
   * @interface IReqSwitchShootingMode
   * @property {number|null} [mode] ReqSwitchShootingMode mode
   */

  /**
   * Constructs a new ReqSwitchShootingMode.
   * @exports ReqSwitchShootingMode
   * @classdesc Represents a ReqSwitchShootingMode.
   * @implements IReqSwitchShootingMode
   * @constructor
   * @param {IReqSwitchShootingMode=} [properties] Properties to set
   */
  function ReqSwitchShootingMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSwitchShootingMode mode.
   * @member {number} mode
   * @memberof ReqSwitchShootingMode
   * @instance
   */
  ReqSwitchShootingMode.prototype.mode = 0;

  /**
   * Creates a new ReqSwitchShootingMode instance using the specified properties.
   * @function create
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {IReqSwitchShootingMode=} [properties] Properties to set
   * @returns {ReqSwitchShootingMode} ReqSwitchShootingMode instance
   */
  ReqSwitchShootingMode.create = function create(properties) {
    return new ReqSwitchShootingMode(properties);
  };

  /**
   * Encodes the specified ReqSwitchShootingMode message. Does not implicitly {@link ReqSwitchShootingMode.verify|verify} messages.
   * @function encode
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {IReqSwitchShootingMode} message ReqSwitchShootingMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchShootingMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqSwitchShootingMode message, length delimited. Does not implicitly {@link ReqSwitchShootingMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {IReqSwitchShootingMode} message ReqSwitchShootingMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchShootingMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSwitchShootingMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSwitchShootingMode} ReqSwitchShootingMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchShootingMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSwitchShootingMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSwitchShootingMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSwitchShootingMode} ReqSwitchShootingMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchShootingMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSwitchShootingMode message.
   * @function verify
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSwitchShootingMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqSwitchShootingMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSwitchShootingMode} ReqSwitchShootingMode
   */
  ReqSwitchShootingMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSwitchShootingMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSwitchShootingMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSwitchShootingMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSwitchShootingMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {ReqSwitchShootingMode} message ReqSwitchShootingMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSwitchShootingMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqSwitchShootingMode to JSON.
   * @function toJSON
   * @memberof ReqSwitchShootingMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSwitchShootingMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSwitchShootingMode
   * @function getTypeUrl
   * @memberof ReqSwitchShootingMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSwitchShootingMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSwitchShootingMode";
  };

  return ReqSwitchShootingMode;
})();

$root.ResSwitchShootingMode = (function () {
  /**
   * Properties of a ResSwitchShootingMode.
   * @exports IResSwitchShootingMode
   * @interface IResSwitchShootingMode
   * @property {number|null} [code] ResSwitchShootingMode code
   * @property {number|null} [shootingModeId] ResSwitchShootingMode shootingModeId
   */

  /**
   * Constructs a new ResSwitchShootingMode.
   * @exports ResSwitchShootingMode
   * @classdesc Represents a ResSwitchShootingMode.
   * @implements IResSwitchShootingMode
   * @constructor
   * @param {IResSwitchShootingMode=} [properties] Properties to set
   */
  function ResSwitchShootingMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSwitchShootingMode code.
   * @member {number} code
   * @memberof ResSwitchShootingMode
   * @instance
   */
  ResSwitchShootingMode.prototype.code = 0;

  /**
   * ResSwitchShootingMode shootingModeId.
   * @member {number} shootingModeId
   * @memberof ResSwitchShootingMode
   * @instance
   */
  ResSwitchShootingMode.prototype.shootingModeId = 0;

  /**
   * Creates a new ResSwitchShootingMode instance using the specified properties.
   * @function create
   * @memberof ResSwitchShootingMode
   * @static
   * @param {IResSwitchShootingMode=} [properties] Properties to set
   * @returns {ResSwitchShootingMode} ResSwitchShootingMode instance
   */
  ResSwitchShootingMode.create = function create(properties) {
    return new ResSwitchShootingMode(properties);
  };

  /**
   * Encodes the specified ResSwitchShootingMode message. Does not implicitly {@link ResSwitchShootingMode.verify|verify} messages.
   * @function encode
   * @memberof ResSwitchShootingMode
   * @static
   * @param {IResSwitchShootingMode} message ResSwitchShootingMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSwitchShootingMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.shootingModeId);
    return writer;
  };

  /**
   * Encodes the specified ResSwitchShootingMode message, length delimited. Does not implicitly {@link ResSwitchShootingMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSwitchShootingMode
   * @static
   * @param {IResSwitchShootingMode} message ResSwitchShootingMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSwitchShootingMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSwitchShootingMode message from the specified reader or buffer.
   * @function decode
   * @memberof ResSwitchShootingMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSwitchShootingMode} ResSwitchShootingMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSwitchShootingMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSwitchShootingMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.shootingModeId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSwitchShootingMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSwitchShootingMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSwitchShootingMode} ResSwitchShootingMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSwitchShootingMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSwitchShootingMode message.
   * @function verify
   * @memberof ResSwitchShootingMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSwitchShootingMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      if (!$util.isInteger(message.shootingModeId))
        return "shootingModeId: integer expected";
    return null;
  };

  /**
   * Creates a ResSwitchShootingMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSwitchShootingMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSwitchShootingMode} ResSwitchShootingMode
   */
  ResSwitchShootingMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSwitchShootingMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResSwitchShootingMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSwitchShootingMode();
    if (object.code != null) message.code = object.code | 0;
    if (object.shootingModeId != null)
      message.shootingModeId = object.shootingModeId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResSwitchShootingMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSwitchShootingMode
   * @static
   * @param {ResSwitchShootingMode} message ResSwitchShootingMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSwitchShootingMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.shootingModeId = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.shootingModeId != null &&
      Object.hasOwnProperty.call(message, "shootingModeId")
    )
      object.shootingModeId = message.shootingModeId;
    return object;
  };

  /**
   * Converts this ResSwitchShootingMode to JSON.
   * @function toJSON
   * @memberof ResSwitchShootingMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSwitchShootingMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSwitchShootingMode
   * @function getTypeUrl
   * @memberof ResSwitchShootingMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSwitchShootingMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSwitchShootingMode";
  };

  return ResSwitchShootingMode;
})();

$root.ReqSwitchShootingTech = (function () {
  /**
   * Properties of a ReqSwitchShootingTech.
   * @exports IReqSwitchShootingTech
   * @interface IReqSwitchShootingTech
   * @property {number|null} [tech] ReqSwitchShootingTech tech
   */

  /**
   * Constructs a new ReqSwitchShootingTech.
   * @exports ReqSwitchShootingTech
   * @classdesc Represents a ReqSwitchShootingTech.
   * @implements IReqSwitchShootingTech
   * @constructor
   * @param {IReqSwitchShootingTech=} [properties] Properties to set
   */
  function ReqSwitchShootingTech(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqSwitchShootingTech tech.
   * @member {number} tech
   * @memberof ReqSwitchShootingTech
   * @instance
   */
  ReqSwitchShootingTech.prototype.tech = 0;

  /**
   * Creates a new ReqSwitchShootingTech instance using the specified properties.
   * @function create
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {IReqSwitchShootingTech=} [properties] Properties to set
   * @returns {ReqSwitchShootingTech} ReqSwitchShootingTech instance
   */
  ReqSwitchShootingTech.create = function create(properties) {
    return new ReqSwitchShootingTech(properties);
  };

  /**
   * Encodes the specified ReqSwitchShootingTech message. Does not implicitly {@link ReqSwitchShootingTech.verify|verify} messages.
   * @function encode
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {IReqSwitchShootingTech} message ReqSwitchShootingTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchShootingTech.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.tech != null && Object.hasOwnProperty.call(message, "tech"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.tech);
    return writer;
  };

  /**
   * Encodes the specified ReqSwitchShootingTech message, length delimited. Does not implicitly {@link ReqSwitchShootingTech.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {IReqSwitchShootingTech} message ReqSwitchShootingTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqSwitchShootingTech.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqSwitchShootingTech message from the specified reader or buffer.
   * @function decode
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqSwitchShootingTech} ReqSwitchShootingTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchShootingTech.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqSwitchShootingTech();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.tech = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqSwitchShootingTech message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqSwitchShootingTech} ReqSwitchShootingTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqSwitchShootingTech.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqSwitchShootingTech message.
   * @function verify
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqSwitchShootingTech.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.tech != null && Object.hasOwnProperty.call(message, "tech"))
      if (!$util.isInteger(message.tech)) return "tech: integer expected";
    return null;
  };

  /**
   * Creates a ReqSwitchShootingTech message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqSwitchShootingTech} ReqSwitchShootingTech
   */
  ReqSwitchShootingTech.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqSwitchShootingTech) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqSwitchShootingTech: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqSwitchShootingTech();
    if (object.tech != null) message.tech = object.tech | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqSwitchShootingTech message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {ReqSwitchShootingTech} message ReqSwitchShootingTech
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqSwitchShootingTech.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.tech = 0;
    if (message.tech != null && Object.hasOwnProperty.call(message, "tech"))
      object.tech = message.tech;
    return object;
  };

  /**
   * Converts this ReqSwitchShootingTech to JSON.
   * @function toJSON
   * @memberof ReqSwitchShootingTech
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqSwitchShootingTech.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqSwitchShootingTech
   * @function getTypeUrl
   * @memberof ReqSwitchShootingTech
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqSwitchShootingTech.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqSwitchShootingTech";
  };

  return ReqSwitchShootingTech;
})();

$root.ResSwitchShootingTech = (function () {
  /**
   * Properties of a ResSwitchShootingTech.
   * @exports IResSwitchShootingTech
   * @interface IResSwitchShootingTech
   * @property {number|null} [code] ResSwitchShootingTech code
   * @property {number|null} [shootingTechId] ResSwitchShootingTech shootingTechId
   */

  /**
   * Constructs a new ResSwitchShootingTech.
   * @exports ResSwitchShootingTech
   * @classdesc Represents a ResSwitchShootingTech.
   * @implements IResSwitchShootingTech
   * @constructor
   * @param {IResSwitchShootingTech=} [properties] Properties to set
   */
  function ResSwitchShootingTech(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResSwitchShootingTech code.
   * @member {number} code
   * @memberof ResSwitchShootingTech
   * @instance
   */
  ResSwitchShootingTech.prototype.code = 0;

  /**
   * ResSwitchShootingTech shootingTechId.
   * @member {number} shootingTechId
   * @memberof ResSwitchShootingTech
   * @instance
   */
  ResSwitchShootingTech.prototype.shootingTechId = 0;

  /**
   * Creates a new ResSwitchShootingTech instance using the specified properties.
   * @function create
   * @memberof ResSwitchShootingTech
   * @static
   * @param {IResSwitchShootingTech=} [properties] Properties to set
   * @returns {ResSwitchShootingTech} ResSwitchShootingTech instance
   */
  ResSwitchShootingTech.create = function create(properties) {
    return new ResSwitchShootingTech(properties);
  };

  /**
   * Encodes the specified ResSwitchShootingTech message. Does not implicitly {@link ResSwitchShootingTech.verify|verify} messages.
   * @function encode
   * @memberof ResSwitchShootingTech
   * @static
   * @param {IResSwitchShootingTech} message ResSwitchShootingTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSwitchShootingTech.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.code);
    if (
      message.shootingTechId != null &&
      Object.hasOwnProperty.call(message, "shootingTechId")
    )
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.shootingTechId);
    return writer;
  };

  /**
   * Encodes the specified ResSwitchShootingTech message, length delimited. Does not implicitly {@link ResSwitchShootingTech.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResSwitchShootingTech
   * @static
   * @param {IResSwitchShootingTech} message ResSwitchShootingTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResSwitchShootingTech.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResSwitchShootingTech message from the specified reader or buffer.
   * @function decode
   * @memberof ResSwitchShootingTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResSwitchShootingTech} ResSwitchShootingTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSwitchShootingTech.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResSwitchShootingTech();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.code = reader.int32();
          break;
        }
        case 2: {
          message.shootingTechId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResSwitchShootingTech message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResSwitchShootingTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResSwitchShootingTech} ResSwitchShootingTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResSwitchShootingTech.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResSwitchShootingTech message.
   * @function verify
   * @memberof ResSwitchShootingTech
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResSwitchShootingTech.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.shootingTechId != null &&
      Object.hasOwnProperty.call(message, "shootingTechId")
    )
      if (!$util.isInteger(message.shootingTechId))
        return "shootingTechId: integer expected";
    return null;
  };

  /**
   * Creates a ResSwitchShootingTech message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResSwitchShootingTech
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResSwitchShootingTech} ResSwitchShootingTech
   */
  ResSwitchShootingTech.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResSwitchShootingTech) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResSwitchShootingTech: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResSwitchShootingTech();
    if (object.code != null) message.code = object.code | 0;
    if (object.shootingTechId != null)
      message.shootingTechId = object.shootingTechId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ResSwitchShootingTech message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResSwitchShootingTech
   * @static
   * @param {ResSwitchShootingTech} message ResSwitchShootingTech
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResSwitchShootingTech.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.code = 0;
      object.shootingTechId = 0;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.shootingTechId != null &&
      Object.hasOwnProperty.call(message, "shootingTechId")
    )
      object.shootingTechId = message.shootingTechId;
    return object;
  };

  /**
   * Converts this ResSwitchShootingTech to JSON.
   * @function toJSON
   * @memberof ResSwitchShootingTech
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResSwitchShootingTech.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResSwitchShootingTech
   * @function getTypeUrl
   * @memberof ResSwitchShootingTech
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResSwitchShootingTech.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResSwitchShootingTech";
  };

  return ResSwitchShootingTech;
})();

$root.ReqGetDeviceStateInfo = (function () {
  /**
   * Properties of a ReqGetDeviceStateInfo.
   * @exports IReqGetDeviceStateInfo
   * @interface IReqGetDeviceStateInfo
   */

  /**
   * Constructs a new ReqGetDeviceStateInfo.
   * @exports ReqGetDeviceStateInfo
   * @classdesc Represents a ReqGetDeviceStateInfo.
   * @implements IReqGetDeviceStateInfo
   * @constructor
   * @param {IReqGetDeviceStateInfo=} [properties] Properties to set
   */
  function ReqGetDeviceStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqGetDeviceStateInfo instance using the specified properties.
   * @function create
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {IReqGetDeviceStateInfo=} [properties] Properties to set
   * @returns {ReqGetDeviceStateInfo} ReqGetDeviceStateInfo instance
   */
  ReqGetDeviceStateInfo.create = function create(properties) {
    return new ReqGetDeviceStateInfo(properties);
  };

  /**
   * Encodes the specified ReqGetDeviceStateInfo message. Does not implicitly {@link ReqGetDeviceStateInfo.verify|verify} messages.
   * @function encode
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {IReqGetDeviceStateInfo} message ReqGetDeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetDeviceStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqGetDeviceStateInfo message, length delimited. Does not implicitly {@link ReqGetDeviceStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {IReqGetDeviceStateInfo} message ReqGetDeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqGetDeviceStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqGetDeviceStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqGetDeviceStateInfo} ReqGetDeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetDeviceStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqGetDeviceStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqGetDeviceStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqGetDeviceStateInfo} ReqGetDeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqGetDeviceStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqGetDeviceStateInfo message.
   * @function verify
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqGetDeviceStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqGetDeviceStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqGetDeviceStateInfo} ReqGetDeviceStateInfo
   */
  ReqGetDeviceStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqGetDeviceStateInfo) return object;
    return new $root.ReqGetDeviceStateInfo();
  };

  /**
   * Creates a plain object from a ReqGetDeviceStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {ReqGetDeviceStateInfo} message ReqGetDeviceStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqGetDeviceStateInfo.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqGetDeviceStateInfo to JSON.
   * @function toJSON
   * @memberof ReqGetDeviceStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqGetDeviceStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqGetDeviceStateInfo
   * @function getTypeUrl
   * @memberof ReqGetDeviceStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqGetDeviceStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqGetDeviceStateInfo";
  };

  return ReqGetDeviceStateInfo;
})();

$root.ExclusiveCameraState = (function () {
  /**
   * Properties of an ExclusiveCameraState.
   * @exports IExclusiveCameraState
   * @interface IExclusiveCameraState
   * @property {notify.ICaptureRawState|null} [captureRawState] ExclusiveCameraState captureRawState
   * @property {notify.IPhotoState|null} [photoState] ExclusiveCameraState photoState
   * @property {notify.IBurstState|null} [burstState] ExclusiveCameraState burstState
   * @property {notify.IRecordState|null} [recordState] ExclusiveCameraState recordState
   * @property {notify.ITimeLapseState|null} [timelapseState] ExclusiveCameraState timelapseState
   * @property {notify.ICaptureCaliFrameState|null} [captureCaliFrameState] ExclusiveCameraState captureCaliFrameState
   * @property {notify.IPanoramaState|null} [panoramaState] ExclusiveCameraState panoramaState
   * @property {notify.ISentryState|null} [sentryState] ExclusiveCameraState sentryState
   */

  /**
   * Constructs a new ExclusiveCameraState.
   * @exports ExclusiveCameraState
   * @classdesc Represents an ExclusiveCameraState.
   * @implements IExclusiveCameraState
   * @constructor
   * @param {IExclusiveCameraState=} [properties] Properties to set
   */
  function ExclusiveCameraState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ExclusiveCameraState captureRawState.
   * @member {notify.ICaptureRawState|null|undefined} captureRawState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.captureRawState = null;

  /**
   * ExclusiveCameraState photoState.
   * @member {notify.IPhotoState|null|undefined} photoState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.photoState = null;

  /**
   * ExclusiveCameraState burstState.
   * @member {notify.IBurstState|null|undefined} burstState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.burstState = null;

  /**
   * ExclusiveCameraState recordState.
   * @member {notify.IRecordState|null|undefined} recordState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.recordState = null;

  /**
   * ExclusiveCameraState timelapseState.
   * @member {notify.ITimeLapseState|null|undefined} timelapseState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.timelapseState = null;

  /**
   * ExclusiveCameraState captureCaliFrameState.
   * @member {notify.ICaptureCaliFrameState|null|undefined} captureCaliFrameState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.captureCaliFrameState = null;

  /**
   * ExclusiveCameraState panoramaState.
   * @member {notify.IPanoramaState|null|undefined} panoramaState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.panoramaState = null;

  /**
   * ExclusiveCameraState sentryState.
   * @member {notify.ISentryState|null|undefined} sentryState
   * @memberof ExclusiveCameraState
   * @instance
   */
  ExclusiveCameraState.prototype.sentryState = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * ExclusiveCameraState currentState.
   * @member {"captureRawState"|"photoState"|"burstState"|"recordState"|"timelapseState"|"captureCaliFrameState"|"panoramaState"|"sentryState"|undefined} currentState
   * @memberof ExclusiveCameraState
   * @instance
   */
  Object.defineProperty(ExclusiveCameraState.prototype, "currentState", {
    get: $util.oneOfGetter(
      ($oneOfFields = [
        "captureRawState",
        "photoState",
        "burstState",
        "recordState",
        "timelapseState",
        "captureCaliFrameState",
        "panoramaState",
        "sentryState",
      ]),
    ),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ExclusiveCameraState instance using the specified properties.
   * @function create
   * @memberof ExclusiveCameraState
   * @static
   * @param {IExclusiveCameraState=} [properties] Properties to set
   * @returns {ExclusiveCameraState} ExclusiveCameraState instance
   */
  ExclusiveCameraState.create = function create(properties) {
    return new ExclusiveCameraState(properties);
  };

  /**
   * Encodes the specified ExclusiveCameraState message. Does not implicitly {@link ExclusiveCameraState.verify|verify} messages.
   * @function encode
   * @memberof ExclusiveCameraState
   * @static
   * @param {IExclusiveCameraState} message ExclusiveCameraState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveCameraState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.captureRawState != null &&
      Object.hasOwnProperty.call(message, "captureRawState")
    )
      $root.notify.CaptureRawState.encode(
        message.captureRawState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.photoState != null &&
      Object.hasOwnProperty.call(message, "photoState")
    )
      $root.notify.PhotoState.encode(
        message.photoState,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.burstState != null &&
      Object.hasOwnProperty.call(message, "burstState")
    )
      $root.notify.BurstState.encode(
        message.burstState,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.recordState != null &&
      Object.hasOwnProperty.call(message, "recordState")
    )
      $root.notify.RecordState.encode(
        message.recordState,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    if (
      message.timelapseState != null &&
      Object.hasOwnProperty.call(message, "timelapseState")
    )
      $root.notify.TimeLapseState.encode(
        message.timelapseState,
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
        q + 1,
      ).ldelim();
    if (
      message.captureCaliFrameState != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameState")
    )
      $root.notify.CaptureCaliFrameState.encode(
        message.captureCaliFrameState,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    if (
      message.panoramaState != null &&
      Object.hasOwnProperty.call(message, "panoramaState")
    )
      $root.notify.PanoramaState.encode(
        message.panoramaState,
        writer.uint32(/* id 7, wireType 2 =*/ 58).fork(),
        q + 1,
      ).ldelim();
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    )
      $root.notify.SentryState.encode(
        message.sentryState,
        writer.uint32(/* id 8, wireType 2 =*/ 66).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ExclusiveCameraState message, length delimited. Does not implicitly {@link ExclusiveCameraState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ExclusiveCameraState
   * @static
   * @param {IExclusiveCameraState} message ExclusiveCameraState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveCameraState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes an ExclusiveCameraState message from the specified reader or buffer.
   * @function decode
   * @memberof ExclusiveCameraState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ExclusiveCameraState} ExclusiveCameraState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveCameraState.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ExclusiveCameraState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.captureRawState = $root.notify.CaptureRawState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.photoState = $root.notify.PhotoState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.burstState = $root.notify.BurstState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.recordState = $root.notify.RecordState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 5: {
          message.timelapseState = $root.notify.TimeLapseState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 6: {
          message.captureCaliFrameState =
            $root.notify.CaptureCaliFrameState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        case 7: {
          message.panoramaState = $root.notify.PanoramaState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 8: {
          message.sentryState = $root.notify.SentryState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes an ExclusiveCameraState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ExclusiveCameraState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ExclusiveCameraState} ExclusiveCameraState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveCameraState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies an ExclusiveCameraState message.
   * @function verify
   * @memberof ExclusiveCameraState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ExclusiveCameraState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.captureRawState != null &&
      Object.hasOwnProperty.call(message, "captureRawState")
    ) {
      properties.currentState = 1;
      {
        var error = $root.notify.CaptureRawState.verify(
          message.captureRawState,
          long + 1,
        );
        if (error) return "captureRawState." + error;
      }
    }
    if (
      message.photoState != null &&
      Object.hasOwnProperty.call(message, "photoState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.PhotoState.verify(
          message.photoState,
          long + 1,
        );
        if (error) return "photoState." + error;
      }
    }
    if (
      message.burstState != null &&
      Object.hasOwnProperty.call(message, "burstState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.BurstState.verify(
          message.burstState,
          long + 1,
        );
        if (error) return "burstState." + error;
      }
    }
    if (
      message.recordState != null &&
      Object.hasOwnProperty.call(message, "recordState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.RecordState.verify(
          message.recordState,
          long + 1,
        );
        if (error) return "recordState." + error;
      }
    }
    if (
      message.timelapseState != null &&
      Object.hasOwnProperty.call(message, "timelapseState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.TimeLapseState.verify(
          message.timelapseState,
          long + 1,
        );
        if (error) return "timelapseState." + error;
      }
    }
    if (
      message.captureCaliFrameState != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.CaptureCaliFrameState.verify(
          message.captureCaliFrameState,
          long + 1,
        );
        if (error) return "captureCaliFrameState." + error;
      }
    }
    if (
      message.panoramaState != null &&
      Object.hasOwnProperty.call(message, "panoramaState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.PanoramaState.verify(
          message.panoramaState,
          long + 1,
        );
        if (error) return "panoramaState." + error;
      }
    }
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.SentryState.verify(
          message.sentryState,
          long + 1,
        );
        if (error) return "sentryState." + error;
      }
    }
    return null;
  };

  /**
   * Creates an ExclusiveCameraState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ExclusiveCameraState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ExclusiveCameraState} ExclusiveCameraState
   */
  ExclusiveCameraState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ExclusiveCameraState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ExclusiveCameraState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ExclusiveCameraState();
    if (object.captureRawState != null) {
      if (!$util.isObject(object.captureRawState))
        throw TypeError(
          ".ExclusiveCameraState.captureRawState: object expected",
        );
      message.captureRawState = $root.notify.CaptureRawState.fromObject(
        object.captureRawState,
        long + 1,
      );
    }
    if (object.photoState != null) {
      if (!$util.isObject(object.photoState))
        throw TypeError(".ExclusiveCameraState.photoState: object expected");
      message.photoState = $root.notify.PhotoState.fromObject(
        object.photoState,
        long + 1,
      );
    }
    if (object.burstState != null) {
      if (!$util.isObject(object.burstState))
        throw TypeError(".ExclusiveCameraState.burstState: object expected");
      message.burstState = $root.notify.BurstState.fromObject(
        object.burstState,
        long + 1,
      );
    }
    if (object.recordState != null) {
      if (!$util.isObject(object.recordState))
        throw TypeError(".ExclusiveCameraState.recordState: object expected");
      message.recordState = $root.notify.RecordState.fromObject(
        object.recordState,
        long + 1,
      );
    }
    if (object.timelapseState != null) {
      if (!$util.isObject(object.timelapseState))
        throw TypeError(
          ".ExclusiveCameraState.timelapseState: object expected",
        );
      message.timelapseState = $root.notify.TimeLapseState.fromObject(
        object.timelapseState,
        long + 1,
      );
    }
    if (object.captureCaliFrameState != null) {
      if (!$util.isObject(object.captureCaliFrameState))
        throw TypeError(
          ".ExclusiveCameraState.captureCaliFrameState: object expected",
        );
      message.captureCaliFrameState =
        $root.notify.CaptureCaliFrameState.fromObject(
          object.captureCaliFrameState,
          long + 1,
        );
    }
    if (object.panoramaState != null) {
      if (!$util.isObject(object.panoramaState))
        throw TypeError(".ExclusiveCameraState.panoramaState: object expected");
      message.panoramaState = $root.notify.PanoramaState.fromObject(
        object.panoramaState,
        long + 1,
      );
    }
    if (object.sentryState != null) {
      if (!$util.isObject(object.sentryState))
        throw TypeError(".ExclusiveCameraState.sentryState: object expected");
      message.sentryState = $root.notify.SentryState.fromObject(
        object.sentryState,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from an ExclusiveCameraState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ExclusiveCameraState
   * @static
   * @param {ExclusiveCameraState} message ExclusiveCameraState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ExclusiveCameraState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.captureRawState != null &&
      Object.hasOwnProperty.call(message, "captureRawState")
    ) {
      object.captureRawState = $root.notify.CaptureRawState.toObject(
        message.captureRawState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "captureRawState";
    }
    if (
      message.photoState != null &&
      Object.hasOwnProperty.call(message, "photoState")
    ) {
      object.photoState = $root.notify.PhotoState.toObject(
        message.photoState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "photoState";
    }
    if (
      message.burstState != null &&
      Object.hasOwnProperty.call(message, "burstState")
    ) {
      object.burstState = $root.notify.BurstState.toObject(
        message.burstState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "burstState";
    }
    if (
      message.recordState != null &&
      Object.hasOwnProperty.call(message, "recordState")
    ) {
      object.recordState = $root.notify.RecordState.toObject(
        message.recordState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "recordState";
    }
    if (
      message.timelapseState != null &&
      Object.hasOwnProperty.call(message, "timelapseState")
    ) {
      object.timelapseState = $root.notify.TimeLapseState.toObject(
        message.timelapseState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "timelapseState";
    }
    if (
      message.captureCaliFrameState != null &&
      Object.hasOwnProperty.call(message, "captureCaliFrameState")
    ) {
      object.captureCaliFrameState =
        $root.notify.CaptureCaliFrameState.toObject(
          message.captureCaliFrameState,
          options,
          q + 1,
        );
      if (options.oneofs) object.currentState = "captureCaliFrameState";
    }
    if (
      message.panoramaState != null &&
      Object.hasOwnProperty.call(message, "panoramaState")
    ) {
      object.panoramaState = $root.notify.PanoramaState.toObject(
        message.panoramaState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "panoramaState";
    }
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    ) {
      object.sentryState = $root.notify.SentryState.toObject(
        message.sentryState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "sentryState";
    }
    return object;
  };

  /**
   * Converts this ExclusiveCameraState to JSON.
   * @function toJSON
   * @memberof ExclusiveCameraState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ExclusiveCameraState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ExclusiveCameraState
   * @function getTypeUrl
   * @memberof ExclusiveCameraState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ExclusiveCameraState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ExclusiveCameraState";
  };

  return ExclusiveCameraState;
})();

$root.TeleCameraStateInfo = (function () {
  /**
   * Properties of a TeleCameraStateInfo.
   * @exports ITeleCameraStateInfo
   * @interface ITeleCameraStateInfo
   * @property {IExclusiveCameraState|null} [exclusiveState] TeleCameraStateInfo exclusiveState
   * @property {notify.IStreamType|null} [streamType] TeleCameraStateInfo streamType
   * @property {number|null} [hFov] TeleCameraStateInfo hFov
   * @property {number|null} [vFov] TeleCameraStateInfo vFov
   * @property {number|null} [resolutionWidth] TeleCameraStateInfo resolutionWidth
   * @property {number|null} [resolutionHeight] TeleCameraStateInfo resolutionHeight
   * @property {notify.ICmosTemperature|null} [cmosTemperature] TeleCameraStateInfo cmosTemperature
   */

  /**
   * Constructs a new TeleCameraStateInfo.
   * @exports TeleCameraStateInfo
   * @classdesc Represents a TeleCameraStateInfo.
   * @implements ITeleCameraStateInfo
   * @constructor
   * @param {ITeleCameraStateInfo=} [properties] Properties to set
   */
  function TeleCameraStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * TeleCameraStateInfo exclusiveState.
   * @member {IExclusiveCameraState|null|undefined} exclusiveState
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.exclusiveState = null;

  /**
   * TeleCameraStateInfo streamType.
   * @member {notify.IStreamType|null|undefined} streamType
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.streamType = null;

  /**
   * TeleCameraStateInfo hFov.
   * @member {number} hFov
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.hFov = 0;

  /**
   * TeleCameraStateInfo vFov.
   * @member {number} vFov
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.vFov = 0;

  /**
   * TeleCameraStateInfo resolutionWidth.
   * @member {number} resolutionWidth
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.resolutionWidth = 0;

  /**
   * TeleCameraStateInfo resolutionHeight.
   * @member {number} resolutionHeight
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.resolutionHeight = 0;

  /**
   * TeleCameraStateInfo cmosTemperature.
   * @member {notify.ICmosTemperature|null|undefined} cmosTemperature
   * @memberof TeleCameraStateInfo
   * @instance
   */
  TeleCameraStateInfo.prototype.cmosTemperature = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(TeleCameraStateInfo.prototype, "_cmosTemperature", {
    get: $util.oneOfGetter(($oneOfFields = ["cmosTemperature"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new TeleCameraStateInfo instance using the specified properties.
   * @function create
   * @memberof TeleCameraStateInfo
   * @static
   * @param {ITeleCameraStateInfo=} [properties] Properties to set
   * @returns {TeleCameraStateInfo} TeleCameraStateInfo instance
   */
  TeleCameraStateInfo.create = function create(properties) {
    return new TeleCameraStateInfo(properties);
  };

  /**
   * Encodes the specified TeleCameraStateInfo message. Does not implicitly {@link TeleCameraStateInfo.verify|verify} messages.
   * @function encode
   * @memberof TeleCameraStateInfo
   * @static
   * @param {ITeleCameraStateInfo} message TeleCameraStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TeleCameraStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      $root.ExclusiveCameraState.encode(
        message.exclusiveState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    )
      $root.notify.StreamType.encode(
        message.streamType,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.hFov);
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.vFov);
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).uint32(message.resolutionWidth);
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      writer
        .uint32(/* id 6, wireType 0 =*/ 48)
        .uint32(message.resolutionHeight);
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    )
      $root.notify.CmosTemperature.encode(
        message.cmosTemperature,
        writer.uint32(/* id 7, wireType 2 =*/ 58).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified TeleCameraStateInfo message, length delimited. Does not implicitly {@link TeleCameraStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof TeleCameraStateInfo
   * @static
   * @param {ITeleCameraStateInfo} message TeleCameraStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  TeleCameraStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a TeleCameraStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof TeleCameraStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {TeleCameraStateInfo} TeleCameraStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TeleCameraStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.TeleCameraStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.exclusiveState = $root.ExclusiveCameraState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.streamType = $root.notify.StreamType.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.hFov = reader.double();
          break;
        }
        case 4: {
          message.vFov = reader.double();
          break;
        }
        case 5: {
          message.resolutionWidth = reader.uint32();
          break;
        }
        case 6: {
          message.resolutionHeight = reader.uint32();
          break;
        }
        case 7: {
          message.cmosTemperature = $root.notify.CmosTemperature.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a TeleCameraStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof TeleCameraStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {TeleCameraStateInfo} TeleCameraStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  TeleCameraStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a TeleCameraStateInfo message.
   * @function verify
   * @memberof TeleCameraStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  TeleCameraStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    ) {
      var error = $root.ExclusiveCameraState.verify(
        message.exclusiveState,
        long + 1,
      );
      if (error) return "exclusiveState." + error;
    }
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    ) {
      var error = $root.notify.StreamType.verify(message.streamType, long + 1);
      if (error) return "streamType." + error;
    }
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      if (typeof message.hFov !== "number") return "hFov: number expected";
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      if (typeof message.vFov !== "number") return "vFov: number expected";
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      if (!$util.isInteger(message.resolutionWidth))
        return "resolutionWidth: integer expected";
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      if (!$util.isInteger(message.resolutionHeight))
        return "resolutionHeight: integer expected";
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    ) {
      properties._cmosTemperature = 1;
      {
        var error = $root.notify.CmosTemperature.verify(
          message.cmosTemperature,
          long + 1,
        );
        if (error) return "cmosTemperature." + error;
      }
    }
    return null;
  };

  /**
   * Creates a TeleCameraStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof TeleCameraStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {TeleCameraStateInfo} TeleCameraStateInfo
   */
  TeleCameraStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.TeleCameraStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".TeleCameraStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.TeleCameraStateInfo();
    if (object.exclusiveState != null) {
      if (!$util.isObject(object.exclusiveState))
        throw TypeError(".TeleCameraStateInfo.exclusiveState: object expected");
      message.exclusiveState = $root.ExclusiveCameraState.fromObject(
        object.exclusiveState,
        long + 1,
      );
    }
    if (object.streamType != null) {
      if (!$util.isObject(object.streamType))
        throw TypeError(".TeleCameraStateInfo.streamType: object expected");
      message.streamType = $root.notify.StreamType.fromObject(
        object.streamType,
        long + 1,
      );
    }
    if (object.hFov != null) message.hFov = Number(object.hFov);
    if (object.vFov != null) message.vFov = Number(object.vFov);
    if (object.resolutionWidth != null)
      message.resolutionWidth = object.resolutionWidth >>> 0;
    if (object.resolutionHeight != null)
      message.resolutionHeight = object.resolutionHeight >>> 0;
    if (object.cmosTemperature != null) {
      if (!$util.isObject(object.cmosTemperature))
        throw TypeError(
          ".TeleCameraStateInfo.cmosTemperature: object expected",
        );
      message.cmosTemperature = $root.notify.CmosTemperature.fromObject(
        object.cmosTemperature,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a TeleCameraStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof TeleCameraStateInfo
   * @static
   * @param {TeleCameraStateInfo} message TeleCameraStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  TeleCameraStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.exclusiveState = null;
      object.streamType = null;
      object.hFov = 0;
      object.vFov = 0;
      object.resolutionWidth = 0;
      object.resolutionHeight = 0;
    }
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      object.exclusiveState = $root.ExclusiveCameraState.toObject(
        message.exclusiveState,
        options,
        q + 1,
      );
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    )
      object.streamType = $root.notify.StreamType.toObject(
        message.streamType,
        options,
        q + 1,
      );
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      object.hFov =
        options.json && !isFinite(message.hFov)
          ? String(message.hFov)
          : message.hFov;
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      object.vFov =
        options.json && !isFinite(message.vFov)
          ? String(message.vFov)
          : message.vFov;
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      object.resolutionWidth = message.resolutionWidth;
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      object.resolutionHeight = message.resolutionHeight;
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    ) {
      object.cmosTemperature = $root.notify.CmosTemperature.toObject(
        message.cmosTemperature,
        options,
        q + 1,
      );
      if (options.oneofs) object._cmosTemperature = "cmosTemperature";
    }
    return object;
  };

  /**
   * Converts this TeleCameraStateInfo to JSON.
   * @function toJSON
   * @memberof TeleCameraStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  TeleCameraStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for TeleCameraStateInfo
   * @function getTypeUrl
   * @memberof TeleCameraStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  TeleCameraStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/TeleCameraStateInfo";
  };

  return TeleCameraStateInfo;
})();

$root.WideCameraStateInfo = (function () {
  /**
   * Properties of a WideCameraStateInfo.
   * @exports IWideCameraStateInfo
   * @interface IWideCameraStateInfo
   * @property {IExclusiveCameraState|null} [exclusiveState] WideCameraStateInfo exclusiveState
   * @property {notify.IStreamType|null} [streamType] WideCameraStateInfo streamType
   * @property {number|null} [hFov] WideCameraStateInfo hFov
   * @property {number|null} [vFov] WideCameraStateInfo vFov
   * @property {number|null} [resolutionWidth] WideCameraStateInfo resolutionWidth
   * @property {number|null} [resolutionHeight] WideCameraStateInfo resolutionHeight
   * @property {notify.ICmosTemperature|null} [cmosTemperature] WideCameraStateInfo cmosTemperature
   */

  /**
   * Constructs a new WideCameraStateInfo.
   * @exports WideCameraStateInfo
   * @classdesc Represents a WideCameraStateInfo.
   * @implements IWideCameraStateInfo
   * @constructor
   * @param {IWideCameraStateInfo=} [properties] Properties to set
   */
  function WideCameraStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * WideCameraStateInfo exclusiveState.
   * @member {IExclusiveCameraState|null|undefined} exclusiveState
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.exclusiveState = null;

  /**
   * WideCameraStateInfo streamType.
   * @member {notify.IStreamType|null|undefined} streamType
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.streamType = null;

  /**
   * WideCameraStateInfo hFov.
   * @member {number} hFov
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.hFov = 0;

  /**
   * WideCameraStateInfo vFov.
   * @member {number} vFov
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.vFov = 0;

  /**
   * WideCameraStateInfo resolutionWidth.
   * @member {number} resolutionWidth
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.resolutionWidth = 0;

  /**
   * WideCameraStateInfo resolutionHeight.
   * @member {number} resolutionHeight
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.resolutionHeight = 0;

  /**
   * WideCameraStateInfo cmosTemperature.
   * @member {notify.ICmosTemperature|null|undefined} cmosTemperature
   * @memberof WideCameraStateInfo
   * @instance
   */
  WideCameraStateInfo.prototype.cmosTemperature = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(WideCameraStateInfo.prototype, "_cmosTemperature", {
    get: $util.oneOfGetter(($oneOfFields = ["cmosTemperature"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new WideCameraStateInfo instance using the specified properties.
   * @function create
   * @memberof WideCameraStateInfo
   * @static
   * @param {IWideCameraStateInfo=} [properties] Properties to set
   * @returns {WideCameraStateInfo} WideCameraStateInfo instance
   */
  WideCameraStateInfo.create = function create(properties) {
    return new WideCameraStateInfo(properties);
  };

  /**
   * Encodes the specified WideCameraStateInfo message. Does not implicitly {@link WideCameraStateInfo.verify|verify} messages.
   * @function encode
   * @memberof WideCameraStateInfo
   * @static
   * @param {IWideCameraStateInfo} message WideCameraStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WideCameraStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      $root.ExclusiveCameraState.encode(
        message.exclusiveState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    )
      $root.notify.StreamType.encode(
        message.streamType,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      writer.uint32(/* id 3, wireType 1 =*/ 25).double(message.hFov);
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      writer.uint32(/* id 4, wireType 1 =*/ 33).double(message.vFov);
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      writer.uint32(/* id 5, wireType 0 =*/ 40).uint32(message.resolutionWidth);
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      writer
        .uint32(/* id 6, wireType 0 =*/ 48)
        .uint32(message.resolutionHeight);
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    )
      $root.notify.CmosTemperature.encode(
        message.cmosTemperature,
        writer.uint32(/* id 7, wireType 2 =*/ 58).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified WideCameraStateInfo message, length delimited. Does not implicitly {@link WideCameraStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof WideCameraStateInfo
   * @static
   * @param {IWideCameraStateInfo} message WideCameraStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  WideCameraStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a WideCameraStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof WideCameraStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {WideCameraStateInfo} WideCameraStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WideCameraStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.WideCameraStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.exclusiveState = $root.ExclusiveCameraState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.streamType = $root.notify.StreamType.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.hFov = reader.double();
          break;
        }
        case 4: {
          message.vFov = reader.double();
          break;
        }
        case 5: {
          message.resolutionWidth = reader.uint32();
          break;
        }
        case 6: {
          message.resolutionHeight = reader.uint32();
          break;
        }
        case 7: {
          message.cmosTemperature = $root.notify.CmosTemperature.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a WideCameraStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof WideCameraStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {WideCameraStateInfo} WideCameraStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  WideCameraStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a WideCameraStateInfo message.
   * @function verify
   * @memberof WideCameraStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  WideCameraStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    ) {
      var error = $root.ExclusiveCameraState.verify(
        message.exclusiveState,
        long + 1,
      );
      if (error) return "exclusiveState." + error;
    }
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    ) {
      var error = $root.notify.StreamType.verify(message.streamType, long + 1);
      if (error) return "streamType." + error;
    }
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      if (typeof message.hFov !== "number") return "hFov: number expected";
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      if (typeof message.vFov !== "number") return "vFov: number expected";
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      if (!$util.isInteger(message.resolutionWidth))
        return "resolutionWidth: integer expected";
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      if (!$util.isInteger(message.resolutionHeight))
        return "resolutionHeight: integer expected";
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    ) {
      properties._cmosTemperature = 1;
      {
        var error = $root.notify.CmosTemperature.verify(
          message.cmosTemperature,
          long + 1,
        );
        if (error) return "cmosTemperature." + error;
      }
    }
    return null;
  };

  /**
   * Creates a WideCameraStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof WideCameraStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {WideCameraStateInfo} WideCameraStateInfo
   */
  WideCameraStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.WideCameraStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".WideCameraStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.WideCameraStateInfo();
    if (object.exclusiveState != null) {
      if (!$util.isObject(object.exclusiveState))
        throw TypeError(".WideCameraStateInfo.exclusiveState: object expected");
      message.exclusiveState = $root.ExclusiveCameraState.fromObject(
        object.exclusiveState,
        long + 1,
      );
    }
    if (object.streamType != null) {
      if (!$util.isObject(object.streamType))
        throw TypeError(".WideCameraStateInfo.streamType: object expected");
      message.streamType = $root.notify.StreamType.fromObject(
        object.streamType,
        long + 1,
      );
    }
    if (object.hFov != null) message.hFov = Number(object.hFov);
    if (object.vFov != null) message.vFov = Number(object.vFov);
    if (object.resolutionWidth != null)
      message.resolutionWidth = object.resolutionWidth >>> 0;
    if (object.resolutionHeight != null)
      message.resolutionHeight = object.resolutionHeight >>> 0;
    if (object.cmosTemperature != null) {
      if (!$util.isObject(object.cmosTemperature))
        throw TypeError(
          ".WideCameraStateInfo.cmosTemperature: object expected",
        );
      message.cmosTemperature = $root.notify.CmosTemperature.fromObject(
        object.cmosTemperature,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a WideCameraStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof WideCameraStateInfo
   * @static
   * @param {WideCameraStateInfo} message WideCameraStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  WideCameraStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.exclusiveState = null;
      object.streamType = null;
      object.hFov = 0;
      object.vFov = 0;
      object.resolutionWidth = 0;
      object.resolutionHeight = 0;
    }
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      object.exclusiveState = $root.ExclusiveCameraState.toObject(
        message.exclusiveState,
        options,
        q + 1,
      );
    if (
      message.streamType != null &&
      Object.hasOwnProperty.call(message, "streamType")
    )
      object.streamType = $root.notify.StreamType.toObject(
        message.streamType,
        options,
        q + 1,
      );
    if (message.hFov != null && Object.hasOwnProperty.call(message, "hFov"))
      object.hFov =
        options.json && !isFinite(message.hFov)
          ? String(message.hFov)
          : message.hFov;
    if (message.vFov != null && Object.hasOwnProperty.call(message, "vFov"))
      object.vFov =
        options.json && !isFinite(message.vFov)
          ? String(message.vFov)
          : message.vFov;
    if (
      message.resolutionWidth != null &&
      Object.hasOwnProperty.call(message, "resolutionWidth")
    )
      object.resolutionWidth = message.resolutionWidth;
    if (
      message.resolutionHeight != null &&
      Object.hasOwnProperty.call(message, "resolutionHeight")
    )
      object.resolutionHeight = message.resolutionHeight;
    if (
      message.cmosTemperature != null &&
      Object.hasOwnProperty.call(message, "cmosTemperature")
    ) {
      object.cmosTemperature = $root.notify.CmosTemperature.toObject(
        message.cmosTemperature,
        options,
        q + 1,
      );
      if (options.oneofs) object._cmosTemperature = "cmosTemperature";
    }
    return object;
  };

  /**
   * Converts this WideCameraStateInfo to JSON.
   * @function toJSON
   * @memberof WideCameraStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  WideCameraStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for WideCameraStateInfo
   * @function getTypeUrl
   * @memberof WideCameraStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  WideCameraStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/WideCameraStateInfo";
  };

  return WideCameraStateInfo;
})();

$root.ExclusiveFocusMotorState = (function () {
  /**
   * Properties of an ExclusiveFocusMotorState.
   * @exports IExclusiveFocusMotorState
   * @interface IExclusiveFocusMotorState
   * @property {notify.IAstroAutoFocusState|null} [astroAutoFocusState] ExclusiveFocusMotorState astroAutoFocusState
   * @property {notify.INormalAutoFocusState|null} [normalAutoFocusState] ExclusiveFocusMotorState normalAutoFocusState
   * @property {notify.IAstroAutoFocusFastState|null} [astroAutoFocusFastState] ExclusiveFocusMotorState astroAutoFocusFastState
   * @property {notify.IAreaAutoFocusState|null} [areaAutoFocusState] ExclusiveFocusMotorState areaAutoFocusState
   */

  /**
   * Constructs a new ExclusiveFocusMotorState.
   * @exports ExclusiveFocusMotorState
   * @classdesc Represents an ExclusiveFocusMotorState.
   * @implements IExclusiveFocusMotorState
   * @constructor
   * @param {IExclusiveFocusMotorState=} [properties] Properties to set
   */
  function ExclusiveFocusMotorState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ExclusiveFocusMotorState astroAutoFocusState.
   * @member {notify.IAstroAutoFocusState|null|undefined} astroAutoFocusState
   * @memberof ExclusiveFocusMotorState
   * @instance
   */
  ExclusiveFocusMotorState.prototype.astroAutoFocusState = null;

  /**
   * ExclusiveFocusMotorState normalAutoFocusState.
   * @member {notify.INormalAutoFocusState|null|undefined} normalAutoFocusState
   * @memberof ExclusiveFocusMotorState
   * @instance
   */
  ExclusiveFocusMotorState.prototype.normalAutoFocusState = null;

  /**
   * ExclusiveFocusMotorState astroAutoFocusFastState.
   * @member {notify.IAstroAutoFocusFastState|null|undefined} astroAutoFocusFastState
   * @memberof ExclusiveFocusMotorState
   * @instance
   */
  ExclusiveFocusMotorState.prototype.astroAutoFocusFastState = null;

  /**
   * ExclusiveFocusMotorState areaAutoFocusState.
   * @member {notify.IAreaAutoFocusState|null|undefined} areaAutoFocusState
   * @memberof ExclusiveFocusMotorState
   * @instance
   */
  ExclusiveFocusMotorState.prototype.areaAutoFocusState = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * ExclusiveFocusMotorState currentState.
   * @member {"astroAutoFocusState"|"normalAutoFocusState"|"astroAutoFocusFastState"|"areaAutoFocusState"|undefined} currentState
   * @memberof ExclusiveFocusMotorState
   * @instance
   */
  Object.defineProperty(ExclusiveFocusMotorState.prototype, "currentState", {
    get: $util.oneOfGetter(
      ($oneOfFields = [
        "astroAutoFocusState",
        "normalAutoFocusState",
        "astroAutoFocusFastState",
        "areaAutoFocusState",
      ]),
    ),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ExclusiveFocusMotorState instance using the specified properties.
   * @function create
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {IExclusiveFocusMotorState=} [properties] Properties to set
   * @returns {ExclusiveFocusMotorState} ExclusiveFocusMotorState instance
   */
  ExclusiveFocusMotorState.create = function create(properties) {
    return new ExclusiveFocusMotorState(properties);
  };

  /**
   * Encodes the specified ExclusiveFocusMotorState message. Does not implicitly {@link ExclusiveFocusMotorState.verify|verify} messages.
   * @function encode
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {IExclusiveFocusMotorState} message ExclusiveFocusMotorState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveFocusMotorState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.astroAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusState")
    )
      $root.notify.AstroAutoFocusState.encode(
        message.astroAutoFocusState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.normalAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "normalAutoFocusState")
    )
      $root.notify.NormalAutoFocusState.encode(
        message.normalAutoFocusState,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.astroAutoFocusFastState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusFastState")
    )
      $root.notify.AstroAutoFocusFastState.encode(
        message.astroAutoFocusFastState,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.areaAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "areaAutoFocusState")
    )
      $root.notify.AreaAutoFocusState.encode(
        message.areaAutoFocusState,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ExclusiveFocusMotorState message, length delimited. Does not implicitly {@link ExclusiveFocusMotorState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {IExclusiveFocusMotorState} message ExclusiveFocusMotorState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveFocusMotorState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes an ExclusiveFocusMotorState message from the specified reader or buffer.
   * @function decode
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ExclusiveFocusMotorState} ExclusiveFocusMotorState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveFocusMotorState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ExclusiveFocusMotorState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.astroAutoFocusState = $root.notify.AstroAutoFocusState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.normalAutoFocusState =
            $root.notify.NormalAutoFocusState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        case 3: {
          message.astroAutoFocusFastState =
            $root.notify.AstroAutoFocusFastState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        case 4: {
          message.areaAutoFocusState = $root.notify.AreaAutoFocusState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes an ExclusiveFocusMotorState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ExclusiveFocusMotorState} ExclusiveFocusMotorState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveFocusMotorState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies an ExclusiveFocusMotorState message.
   * @function verify
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ExclusiveFocusMotorState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.astroAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusState")
    ) {
      properties.currentState = 1;
      {
        var error = $root.notify.AstroAutoFocusState.verify(
          message.astroAutoFocusState,
          long + 1,
        );
        if (error) return "astroAutoFocusState." + error;
      }
    }
    if (
      message.normalAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "normalAutoFocusState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.NormalAutoFocusState.verify(
          message.normalAutoFocusState,
          long + 1,
        );
        if (error) return "normalAutoFocusState." + error;
      }
    }
    if (
      message.astroAutoFocusFastState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusFastState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.AstroAutoFocusFastState.verify(
          message.astroAutoFocusFastState,
          long + 1,
        );
        if (error) return "astroAutoFocusFastState." + error;
      }
    }
    if (
      message.areaAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "areaAutoFocusState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.AreaAutoFocusState.verify(
          message.areaAutoFocusState,
          long + 1,
        );
        if (error) return "areaAutoFocusState." + error;
      }
    }
    return null;
  };

  /**
   * Creates an ExclusiveFocusMotorState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ExclusiveFocusMotorState} ExclusiveFocusMotorState
   */
  ExclusiveFocusMotorState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ExclusiveFocusMotorState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ExclusiveFocusMotorState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ExclusiveFocusMotorState();
    if (object.astroAutoFocusState != null) {
      if (!$util.isObject(object.astroAutoFocusState))
        throw TypeError(
          ".ExclusiveFocusMotorState.astroAutoFocusState: object expected",
        );
      message.astroAutoFocusState = $root.notify.AstroAutoFocusState.fromObject(
        object.astroAutoFocusState,
        long + 1,
      );
    }
    if (object.normalAutoFocusState != null) {
      if (!$util.isObject(object.normalAutoFocusState))
        throw TypeError(
          ".ExclusiveFocusMotorState.normalAutoFocusState: object expected",
        );
      message.normalAutoFocusState =
        $root.notify.NormalAutoFocusState.fromObject(
          object.normalAutoFocusState,
          long + 1,
        );
    }
    if (object.astroAutoFocusFastState != null) {
      if (!$util.isObject(object.astroAutoFocusFastState))
        throw TypeError(
          ".ExclusiveFocusMotorState.astroAutoFocusFastState: object expected",
        );
      message.astroAutoFocusFastState =
        $root.notify.AstroAutoFocusFastState.fromObject(
          object.astroAutoFocusFastState,
          long + 1,
        );
    }
    if (object.areaAutoFocusState != null) {
      if (!$util.isObject(object.areaAutoFocusState))
        throw TypeError(
          ".ExclusiveFocusMotorState.areaAutoFocusState: object expected",
        );
      message.areaAutoFocusState = $root.notify.AreaAutoFocusState.fromObject(
        object.areaAutoFocusState,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from an ExclusiveFocusMotorState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {ExclusiveFocusMotorState} message ExclusiveFocusMotorState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ExclusiveFocusMotorState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.astroAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusState")
    ) {
      object.astroAutoFocusState = $root.notify.AstroAutoFocusState.toObject(
        message.astroAutoFocusState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "astroAutoFocusState";
    }
    if (
      message.normalAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "normalAutoFocusState")
    ) {
      object.normalAutoFocusState = $root.notify.NormalAutoFocusState.toObject(
        message.normalAutoFocusState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "normalAutoFocusState";
    }
    if (
      message.astroAutoFocusFastState != null &&
      Object.hasOwnProperty.call(message, "astroAutoFocusFastState")
    ) {
      object.astroAutoFocusFastState =
        $root.notify.AstroAutoFocusFastState.toObject(
          message.astroAutoFocusFastState,
          options,
          q + 1,
        );
      if (options.oneofs) object.currentState = "astroAutoFocusFastState";
    }
    if (
      message.areaAutoFocusState != null &&
      Object.hasOwnProperty.call(message, "areaAutoFocusState")
    ) {
      object.areaAutoFocusState = $root.notify.AreaAutoFocusState.toObject(
        message.areaAutoFocusState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "areaAutoFocusState";
    }
    return object;
  };

  /**
   * Converts this ExclusiveFocusMotorState to JSON.
   * @function toJSON
   * @memberof ExclusiveFocusMotorState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ExclusiveFocusMotorState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ExclusiveFocusMotorState
   * @function getTypeUrl
   * @memberof ExclusiveFocusMotorState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ExclusiveFocusMotorState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ExclusiveFocusMotorState";
  };

  return ExclusiveFocusMotorState;
})();

$root.FocusMotorStateInfo = (function () {
  /**
   * Properties of a FocusMotorStateInfo.
   * @exports IFocusMotorStateInfo
   * @interface IFocusMotorStateInfo
   * @property {IExclusiveFocusMotorState|null} [exclusiveState] FocusMotorStateInfo exclusiveState
   * @property {notify.IFocusPosition|null} [focusPosition] FocusMotorStateInfo focusPosition
   */

  /**
   * Constructs a new FocusMotorStateInfo.
   * @exports FocusMotorStateInfo
   * @classdesc Represents a FocusMotorStateInfo.
   * @implements IFocusMotorStateInfo
   * @constructor
   * @param {IFocusMotorStateInfo=} [properties] Properties to set
   */
  function FocusMotorStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * FocusMotorStateInfo exclusiveState.
   * @member {IExclusiveFocusMotorState|null|undefined} exclusiveState
   * @memberof FocusMotorStateInfo
   * @instance
   */
  FocusMotorStateInfo.prototype.exclusiveState = null;

  /**
   * FocusMotorStateInfo focusPosition.
   * @member {notify.IFocusPosition|null|undefined} focusPosition
   * @memberof FocusMotorStateInfo
   * @instance
   */
  FocusMotorStateInfo.prototype.focusPosition = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(FocusMotorStateInfo.prototype, "_focusPosition", {
    get: $util.oneOfGetter(($oneOfFields = ["focusPosition"])),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new FocusMotorStateInfo instance using the specified properties.
   * @function create
   * @memberof FocusMotorStateInfo
   * @static
   * @param {IFocusMotorStateInfo=} [properties] Properties to set
   * @returns {FocusMotorStateInfo} FocusMotorStateInfo instance
   */
  FocusMotorStateInfo.create = function create(properties) {
    return new FocusMotorStateInfo(properties);
  };

  /**
   * Encodes the specified FocusMotorStateInfo message. Does not implicitly {@link FocusMotorStateInfo.verify|verify} messages.
   * @function encode
   * @memberof FocusMotorStateInfo
   * @static
   * @param {IFocusMotorStateInfo} message FocusMotorStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FocusMotorStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      $root.ExclusiveFocusMotorState.encode(
        message.exclusiveState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.focusPosition != null &&
      Object.hasOwnProperty.call(message, "focusPosition")
    )
      $root.notify.FocusPosition.encode(
        message.focusPosition,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified FocusMotorStateInfo message, length delimited. Does not implicitly {@link FocusMotorStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof FocusMotorStateInfo
   * @static
   * @param {IFocusMotorStateInfo} message FocusMotorStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  FocusMotorStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a FocusMotorStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof FocusMotorStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {FocusMotorStateInfo} FocusMotorStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FocusMotorStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.FocusMotorStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.exclusiveState = $root.ExclusiveFocusMotorState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.focusPosition = $root.notify.FocusPosition.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a FocusMotorStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof FocusMotorStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {FocusMotorStateInfo} FocusMotorStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  FocusMotorStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a FocusMotorStateInfo message.
   * @function verify
   * @memberof FocusMotorStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  FocusMotorStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    ) {
      var error = $root.ExclusiveFocusMotorState.verify(
        message.exclusiveState,
        long + 1,
      );
      if (error) return "exclusiveState." + error;
    }
    if (
      message.focusPosition != null &&
      Object.hasOwnProperty.call(message, "focusPosition")
    ) {
      properties._focusPosition = 1;
      {
        var error = $root.notify.FocusPosition.verify(
          message.focusPosition,
          long + 1,
        );
        if (error) return "focusPosition." + error;
      }
    }
    return null;
  };

  /**
   * Creates a FocusMotorStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof FocusMotorStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {FocusMotorStateInfo} FocusMotorStateInfo
   */
  FocusMotorStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.FocusMotorStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".FocusMotorStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.FocusMotorStateInfo();
    if (object.exclusiveState != null) {
      if (!$util.isObject(object.exclusiveState))
        throw TypeError(".FocusMotorStateInfo.exclusiveState: object expected");
      message.exclusiveState = $root.ExclusiveFocusMotorState.fromObject(
        object.exclusiveState,
        long + 1,
      );
    }
    if (object.focusPosition != null) {
      if (!$util.isObject(object.focusPosition))
        throw TypeError(".FocusMotorStateInfo.focusPosition: object expected");
      message.focusPosition = $root.notify.FocusPosition.fromObject(
        object.focusPosition,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a FocusMotorStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof FocusMotorStateInfo
   * @static
   * @param {FocusMotorStateInfo} message FocusMotorStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  FocusMotorStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.exclusiveState = null;
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      object.exclusiveState = $root.ExclusiveFocusMotorState.toObject(
        message.exclusiveState,
        options,
        q + 1,
      );
    if (
      message.focusPosition != null &&
      Object.hasOwnProperty.call(message, "focusPosition")
    ) {
      object.focusPosition = $root.notify.FocusPosition.toObject(
        message.focusPosition,
        options,
        q + 1,
      );
      if (options.oneofs) object._focusPosition = "focusPosition";
    }
    return object;
  };

  /**
   * Converts this FocusMotorStateInfo to JSON.
   * @function toJSON
   * @memberof FocusMotorStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  FocusMotorStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for FocusMotorStateInfo
   * @function getTypeUrl
   * @memberof FocusMotorStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  FocusMotorStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/FocusMotorStateInfo";
  };

  return FocusMotorStateInfo;
})();

$root.ExclusiveMotionMotorState = (function () {
  /**
   * Properties of an ExclusiveMotionMotorState.
   * @exports IExclusiveMotionMotorState
   * @interface IExclusiveMotionMotorState
   * @property {notify.IAstroCalibrationState|null} [astroCalibrationState] ExclusiveMotionMotorState astroCalibrationState
   * @property {notify.IAstroGotoState|null} [astroGotoState] ExclusiveMotionMotorState astroGotoState
   * @property {notify.IAstroTrackingState|null} [astroTrackingState] ExclusiveMotionMotorState astroTrackingState
   * @property {notify.INormalTrackState|null} [normalTrackState] ExclusiveMotionMotorState normalTrackState
   * @property {notify.IOneClickGotoState|null} [oneClickGotoState] ExclusiveMotionMotorState oneClickGotoState
   * @property {notify.IEqSolvingState|null} [eqState] ExclusiveMotionMotorState eqState
   * @property {notify.ISentryState|null} [sentryState] ExclusiveMotionMotorState sentryState
   * @property {notify.ISkyTargetFinderState|null} [skyTargetFinderState] ExclusiveMotionMotorState skyTargetFinderState
   */

  /**
   * Constructs a new ExclusiveMotionMotorState.
   * @exports ExclusiveMotionMotorState
   * @classdesc Represents an ExclusiveMotionMotorState.
   * @implements IExclusiveMotionMotorState
   * @constructor
   * @param {IExclusiveMotionMotorState=} [properties] Properties to set
   */
  function ExclusiveMotionMotorState(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ExclusiveMotionMotorState astroCalibrationState.
   * @member {notify.IAstroCalibrationState|null|undefined} astroCalibrationState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.astroCalibrationState = null;

  /**
   * ExclusiveMotionMotorState astroGotoState.
   * @member {notify.IAstroGotoState|null|undefined} astroGotoState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.astroGotoState = null;

  /**
   * ExclusiveMotionMotorState astroTrackingState.
   * @member {notify.IAstroTrackingState|null|undefined} astroTrackingState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.astroTrackingState = null;

  /**
   * ExclusiveMotionMotorState normalTrackState.
   * @member {notify.INormalTrackState|null|undefined} normalTrackState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.normalTrackState = null;

  /**
   * ExclusiveMotionMotorState oneClickGotoState.
   * @member {notify.IOneClickGotoState|null|undefined} oneClickGotoState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.oneClickGotoState = null;

  /**
   * ExclusiveMotionMotorState eqState.
   * @member {notify.IEqSolvingState|null|undefined} eqState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.eqState = null;

  /**
   * ExclusiveMotionMotorState sentryState.
   * @member {notify.ISentryState|null|undefined} sentryState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.sentryState = null;

  /**
   * ExclusiveMotionMotorState skyTargetFinderState.
   * @member {notify.ISkyTargetFinderState|null|undefined} skyTargetFinderState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  ExclusiveMotionMotorState.prototype.skyTargetFinderState = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  /**
   * ExclusiveMotionMotorState currentState.
   * @member {"astroCalibrationState"|"astroGotoState"|"astroTrackingState"|"normalTrackState"|"oneClickGotoState"|"eqState"|"sentryState"|"skyTargetFinderState"|undefined} currentState
   * @memberof ExclusiveMotionMotorState
   * @instance
   */
  Object.defineProperty(ExclusiveMotionMotorState.prototype, "currentState", {
    get: $util.oneOfGetter(
      ($oneOfFields = [
        "astroCalibrationState",
        "astroGotoState",
        "astroTrackingState",
        "normalTrackState",
        "oneClickGotoState",
        "eqState",
        "sentryState",
        "skyTargetFinderState",
      ]),
    ),
    set: $util.oneOfSetter($oneOfFields),
  });

  /**
   * Creates a new ExclusiveMotionMotorState instance using the specified properties.
   * @function create
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {IExclusiveMotionMotorState=} [properties] Properties to set
   * @returns {ExclusiveMotionMotorState} ExclusiveMotionMotorState instance
   */
  ExclusiveMotionMotorState.create = function create(properties) {
    return new ExclusiveMotionMotorState(properties);
  };

  /**
   * Encodes the specified ExclusiveMotionMotorState message. Does not implicitly {@link ExclusiveMotionMotorState.verify|verify} messages.
   * @function encode
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {IExclusiveMotionMotorState} message ExclusiveMotionMotorState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveMotionMotorState.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.astroCalibrationState != null &&
      Object.hasOwnProperty.call(message, "astroCalibrationState")
    )
      $root.notify.AstroCalibrationState.encode(
        message.astroCalibrationState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.astroGotoState != null &&
      Object.hasOwnProperty.call(message, "astroGotoState")
    )
      $root.notify.AstroGotoState.encode(
        message.astroGotoState,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.astroTrackingState != null &&
      Object.hasOwnProperty.call(message, "astroTrackingState")
    )
      $root.notify.AstroTrackingState.encode(
        message.astroTrackingState,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.normalTrackState != null &&
      Object.hasOwnProperty.call(message, "normalTrackState")
    )
      $root.notify.NormalTrackState.encode(
        message.normalTrackState,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    if (
      message.oneClickGotoState != null &&
      Object.hasOwnProperty.call(message, "oneClickGotoState")
    )
      $root.notify.OneClickGotoState.encode(
        message.oneClickGotoState,
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
        q + 1,
      ).ldelim();
    if (
      message.eqState != null &&
      Object.hasOwnProperty.call(message, "eqState")
    )
      $root.notify.EqSolvingState.encode(
        message.eqState,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    )
      $root.notify.SentryState.encode(
        message.sentryState,
        writer.uint32(/* id 7, wireType 2 =*/ 58).fork(),
        q + 1,
      ).ldelim();
    if (
      message.skyTargetFinderState != null &&
      Object.hasOwnProperty.call(message, "skyTargetFinderState")
    )
      $root.notify.SkyTargetFinderState.encode(
        message.skyTargetFinderState,
        writer.uint32(/* id 8, wireType 2 =*/ 66).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ExclusiveMotionMotorState message, length delimited. Does not implicitly {@link ExclusiveMotionMotorState.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {IExclusiveMotionMotorState} message ExclusiveMotionMotorState message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ExclusiveMotionMotorState.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes an ExclusiveMotionMotorState message from the specified reader or buffer.
   * @function decode
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ExclusiveMotionMotorState} ExclusiveMotionMotorState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveMotionMotorState.decode = function decode(
    reader,
    length,
    error,
    long,
  ) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ExclusiveMotionMotorState();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.astroCalibrationState =
            $root.notify.AstroCalibrationState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        case 2: {
          message.astroGotoState = $root.notify.AstroGotoState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.astroTrackingState = $root.notify.AstroTrackingState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.normalTrackState = $root.notify.NormalTrackState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 5: {
          message.oneClickGotoState = $root.notify.OneClickGotoState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 6: {
          message.eqState = $root.notify.EqSolvingState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 7: {
          message.sentryState = $root.notify.SentryState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 8: {
          message.skyTargetFinderState =
            $root.notify.SkyTargetFinderState.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes an ExclusiveMotionMotorState message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ExclusiveMotionMotorState} ExclusiveMotionMotorState
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ExclusiveMotionMotorState.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies an ExclusiveMotionMotorState message.
   * @function verify
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ExclusiveMotionMotorState.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.astroCalibrationState != null &&
      Object.hasOwnProperty.call(message, "astroCalibrationState")
    ) {
      properties.currentState = 1;
      {
        var error = $root.notify.AstroCalibrationState.verify(
          message.astroCalibrationState,
          long + 1,
        );
        if (error) return "astroCalibrationState." + error;
      }
    }
    if (
      message.astroGotoState != null &&
      Object.hasOwnProperty.call(message, "astroGotoState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.AstroGotoState.verify(
          message.astroGotoState,
          long + 1,
        );
        if (error) return "astroGotoState." + error;
      }
    }
    if (
      message.astroTrackingState != null &&
      Object.hasOwnProperty.call(message, "astroTrackingState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.AstroTrackingState.verify(
          message.astroTrackingState,
          long + 1,
        );
        if (error) return "astroTrackingState." + error;
      }
    }
    if (
      message.normalTrackState != null &&
      Object.hasOwnProperty.call(message, "normalTrackState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.NormalTrackState.verify(
          message.normalTrackState,
          long + 1,
        );
        if (error) return "normalTrackState." + error;
      }
    }
    if (
      message.oneClickGotoState != null &&
      Object.hasOwnProperty.call(message, "oneClickGotoState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.OneClickGotoState.verify(
          message.oneClickGotoState,
          long + 1,
        );
        if (error) return "oneClickGotoState." + error;
      }
    }
    if (
      message.eqState != null &&
      Object.hasOwnProperty.call(message, "eqState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.EqSolvingState.verify(
          message.eqState,
          long + 1,
        );
        if (error) return "eqState." + error;
      }
    }
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.SentryState.verify(
          message.sentryState,
          long + 1,
        );
        if (error) return "sentryState." + error;
      }
    }
    if (
      message.skyTargetFinderState != null &&
      Object.hasOwnProperty.call(message, "skyTargetFinderState")
    ) {
      if (properties.currentState === 1) return "currentState: multiple values";
      properties.currentState = 1;
      {
        var error = $root.notify.SkyTargetFinderState.verify(
          message.skyTargetFinderState,
          long + 1,
        );
        if (error) return "skyTargetFinderState." + error;
      }
    }
    return null;
  };

  /**
   * Creates an ExclusiveMotionMotorState message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ExclusiveMotionMotorState} ExclusiveMotionMotorState
   */
  ExclusiveMotionMotorState.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ExclusiveMotionMotorState) return object;
    if (!$util.isObject(object))
      throw TypeError(".ExclusiveMotionMotorState: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ExclusiveMotionMotorState();
    if (object.astroCalibrationState != null) {
      if (!$util.isObject(object.astroCalibrationState))
        throw TypeError(
          ".ExclusiveMotionMotorState.astroCalibrationState: object expected",
        );
      message.astroCalibrationState =
        $root.notify.AstroCalibrationState.fromObject(
          object.astroCalibrationState,
          long + 1,
        );
    }
    if (object.astroGotoState != null) {
      if (!$util.isObject(object.astroGotoState))
        throw TypeError(
          ".ExclusiveMotionMotorState.astroGotoState: object expected",
        );
      message.astroGotoState = $root.notify.AstroGotoState.fromObject(
        object.astroGotoState,
        long + 1,
      );
    }
    if (object.astroTrackingState != null) {
      if (!$util.isObject(object.astroTrackingState))
        throw TypeError(
          ".ExclusiveMotionMotorState.astroTrackingState: object expected",
        );
      message.astroTrackingState = $root.notify.AstroTrackingState.fromObject(
        object.astroTrackingState,
        long + 1,
      );
    }
    if (object.normalTrackState != null) {
      if (!$util.isObject(object.normalTrackState))
        throw TypeError(
          ".ExclusiveMotionMotorState.normalTrackState: object expected",
        );
      message.normalTrackState = $root.notify.NormalTrackState.fromObject(
        object.normalTrackState,
        long + 1,
      );
    }
    if (object.oneClickGotoState != null) {
      if (!$util.isObject(object.oneClickGotoState))
        throw TypeError(
          ".ExclusiveMotionMotorState.oneClickGotoState: object expected",
        );
      message.oneClickGotoState = $root.notify.OneClickGotoState.fromObject(
        object.oneClickGotoState,
        long + 1,
      );
    }
    if (object.eqState != null) {
      if (!$util.isObject(object.eqState))
        throw TypeError(".ExclusiveMotionMotorState.eqState: object expected");
      message.eqState = $root.notify.EqSolvingState.fromObject(
        object.eqState,
        long + 1,
      );
    }
    if (object.sentryState != null) {
      if (!$util.isObject(object.sentryState))
        throw TypeError(
          ".ExclusiveMotionMotorState.sentryState: object expected",
        );
      message.sentryState = $root.notify.SentryState.fromObject(
        object.sentryState,
        long + 1,
      );
    }
    if (object.skyTargetFinderState != null) {
      if (!$util.isObject(object.skyTargetFinderState))
        throw TypeError(
          ".ExclusiveMotionMotorState.skyTargetFinderState: object expected",
        );
      message.skyTargetFinderState =
        $root.notify.SkyTargetFinderState.fromObject(
          object.skyTargetFinderState,
          long + 1,
        );
    }
    return message;
  };

  /**
   * Creates a plain object from an ExclusiveMotionMotorState message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {ExclusiveMotionMotorState} message ExclusiveMotionMotorState
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ExclusiveMotionMotorState.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (
      message.astroCalibrationState != null &&
      Object.hasOwnProperty.call(message, "astroCalibrationState")
    ) {
      object.astroCalibrationState =
        $root.notify.AstroCalibrationState.toObject(
          message.astroCalibrationState,
          options,
          q + 1,
        );
      if (options.oneofs) object.currentState = "astroCalibrationState";
    }
    if (
      message.astroGotoState != null &&
      Object.hasOwnProperty.call(message, "astroGotoState")
    ) {
      object.astroGotoState = $root.notify.AstroGotoState.toObject(
        message.astroGotoState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "astroGotoState";
    }
    if (
      message.astroTrackingState != null &&
      Object.hasOwnProperty.call(message, "astroTrackingState")
    ) {
      object.astroTrackingState = $root.notify.AstroTrackingState.toObject(
        message.astroTrackingState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "astroTrackingState";
    }
    if (
      message.normalTrackState != null &&
      Object.hasOwnProperty.call(message, "normalTrackState")
    ) {
      object.normalTrackState = $root.notify.NormalTrackState.toObject(
        message.normalTrackState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "normalTrackState";
    }
    if (
      message.oneClickGotoState != null &&
      Object.hasOwnProperty.call(message, "oneClickGotoState")
    ) {
      object.oneClickGotoState = $root.notify.OneClickGotoState.toObject(
        message.oneClickGotoState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "oneClickGotoState";
    }
    if (
      message.eqState != null &&
      Object.hasOwnProperty.call(message, "eqState")
    ) {
      object.eqState = $root.notify.EqSolvingState.toObject(
        message.eqState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "eqState";
    }
    if (
      message.sentryState != null &&
      Object.hasOwnProperty.call(message, "sentryState")
    ) {
      object.sentryState = $root.notify.SentryState.toObject(
        message.sentryState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "sentryState";
    }
    if (
      message.skyTargetFinderState != null &&
      Object.hasOwnProperty.call(message, "skyTargetFinderState")
    ) {
      object.skyTargetFinderState = $root.notify.SkyTargetFinderState.toObject(
        message.skyTargetFinderState,
        options,
        q + 1,
      );
      if (options.oneofs) object.currentState = "skyTargetFinderState";
    }
    return object;
  };

  /**
   * Converts this ExclusiveMotionMotorState to JSON.
   * @function toJSON
   * @memberof ExclusiveMotionMotorState
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ExclusiveMotionMotorState.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ExclusiveMotionMotorState
   * @function getTypeUrl
   * @memberof ExclusiveMotionMotorState
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ExclusiveMotionMotorState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ExclusiveMotionMotorState";
  };

  return ExclusiveMotionMotorState;
})();

$root.MotionMotorStateInfo = (function () {
  /**
   * Properties of a MotionMotorStateInfo.
   * @exports IMotionMotorStateInfo
   * @interface IMotionMotorStateInfo
   * @property {IExclusiveMotionMotorState|null} [exclusiveState] MotionMotorStateInfo exclusiveState
   * @property {notify.ISentryAutoHand|null} [sentryAutoHand] MotionMotorStateInfo sentryAutoHand
   * @property {notify.IRollLevelCalibrationAngle|null} [rollLevelCalibrationAngle] MotionMotorStateInfo rollLevelCalibrationAngle
   */

  /**
   * Constructs a new MotionMotorStateInfo.
   * @exports MotionMotorStateInfo
   * @classdesc Represents a MotionMotorStateInfo.
   * @implements IMotionMotorStateInfo
   * @constructor
   * @param {IMotionMotorStateInfo=} [properties] Properties to set
   */
  function MotionMotorStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * MotionMotorStateInfo exclusiveState.
   * @member {IExclusiveMotionMotorState|null|undefined} exclusiveState
   * @memberof MotionMotorStateInfo
   * @instance
   */
  MotionMotorStateInfo.prototype.exclusiveState = null;

  /**
   * MotionMotorStateInfo sentryAutoHand.
   * @member {notify.ISentryAutoHand|null|undefined} sentryAutoHand
   * @memberof MotionMotorStateInfo
   * @instance
   */
  MotionMotorStateInfo.prototype.sentryAutoHand = null;

  /**
   * MotionMotorStateInfo rollLevelCalibrationAngle.
   * @member {notify.IRollLevelCalibrationAngle|null|undefined} rollLevelCalibrationAngle
   * @memberof MotionMotorStateInfo
   * @instance
   */
  MotionMotorStateInfo.prototype.rollLevelCalibrationAngle = null;

  // OneOf field names bound to virtual getters and setters
  var $oneOfFields;

  // Virtual OneOf for proto3 optional field
  Object.defineProperty(
    MotionMotorStateInfo.prototype,
    "_rollLevelCalibrationAngle",
    {
      get: $util.oneOfGetter(($oneOfFields = ["rollLevelCalibrationAngle"])),
      set: $util.oneOfSetter($oneOfFields),
    },
  );

  /**
   * Creates a new MotionMotorStateInfo instance using the specified properties.
   * @function create
   * @memberof MotionMotorStateInfo
   * @static
   * @param {IMotionMotorStateInfo=} [properties] Properties to set
   * @returns {MotionMotorStateInfo} MotionMotorStateInfo instance
   */
  MotionMotorStateInfo.create = function create(properties) {
    return new MotionMotorStateInfo(properties);
  };

  /**
   * Encodes the specified MotionMotorStateInfo message. Does not implicitly {@link MotionMotorStateInfo.verify|verify} messages.
   * @function encode
   * @memberof MotionMotorStateInfo
   * @static
   * @param {IMotionMotorStateInfo} message MotionMotorStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  MotionMotorStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      $root.ExclusiveMotionMotorState.encode(
        message.exclusiveState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.sentryAutoHand != null &&
      Object.hasOwnProperty.call(message, "sentryAutoHand")
    )
      $root.notify.SentryAutoHand.encode(
        message.sentryAutoHand,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.rollLevelCalibrationAngle != null &&
      Object.hasOwnProperty.call(message, "rollLevelCalibrationAngle")
    )
      $root.notify.RollLevelCalibrationAngle.encode(
        message.rollLevelCalibrationAngle,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified MotionMotorStateInfo message, length delimited. Does not implicitly {@link MotionMotorStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof MotionMotorStateInfo
   * @static
   * @param {IMotionMotorStateInfo} message MotionMotorStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  MotionMotorStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a MotionMotorStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof MotionMotorStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {MotionMotorStateInfo} MotionMotorStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  MotionMotorStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.MotionMotorStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.exclusiveState = $root.ExclusiveMotionMotorState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.sentryAutoHand = $root.notify.SentryAutoHand.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.rollLevelCalibrationAngle =
            $root.notify.RollLevelCalibrationAngle.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a MotionMotorStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof MotionMotorStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {MotionMotorStateInfo} MotionMotorStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  MotionMotorStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a MotionMotorStateInfo message.
   * @function verify
   * @memberof MotionMotorStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  MotionMotorStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    var properties = {};
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    ) {
      var error = $root.ExclusiveMotionMotorState.verify(
        message.exclusiveState,
        long + 1,
      );
      if (error) return "exclusiveState." + error;
    }
    if (
      message.sentryAutoHand != null &&
      Object.hasOwnProperty.call(message, "sentryAutoHand")
    ) {
      var error = $root.notify.SentryAutoHand.verify(
        message.sentryAutoHand,
        long + 1,
      );
      if (error) return "sentryAutoHand." + error;
    }
    if (
      message.rollLevelCalibrationAngle != null &&
      Object.hasOwnProperty.call(message, "rollLevelCalibrationAngle")
    ) {
      properties._rollLevelCalibrationAngle = 1;
      {
        var error = $root.notify.RollLevelCalibrationAngle.verify(
          message.rollLevelCalibrationAngle,
          long + 1,
        );
        if (error) return "rollLevelCalibrationAngle." + error;
      }
    }
    return null;
  };

  /**
   * Creates a MotionMotorStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof MotionMotorStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {MotionMotorStateInfo} MotionMotorStateInfo
   */
  MotionMotorStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.MotionMotorStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".MotionMotorStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.MotionMotorStateInfo();
    if (object.exclusiveState != null) {
      if (!$util.isObject(object.exclusiveState))
        throw TypeError(
          ".MotionMotorStateInfo.exclusiveState: object expected",
        );
      message.exclusiveState = $root.ExclusiveMotionMotorState.fromObject(
        object.exclusiveState,
        long + 1,
      );
    }
    if (object.sentryAutoHand != null) {
      if (!$util.isObject(object.sentryAutoHand))
        throw TypeError(
          ".MotionMotorStateInfo.sentryAutoHand: object expected",
        );
      message.sentryAutoHand = $root.notify.SentryAutoHand.fromObject(
        object.sentryAutoHand,
        long + 1,
      );
    }
    if (object.rollLevelCalibrationAngle != null) {
      if (!$util.isObject(object.rollLevelCalibrationAngle))
        throw TypeError(
          ".MotionMotorStateInfo.rollLevelCalibrationAngle: object expected",
        );
      message.rollLevelCalibrationAngle =
        $root.notify.RollLevelCalibrationAngle.fromObject(
          object.rollLevelCalibrationAngle,
          long + 1,
        );
    }
    return message;
  };

  /**
   * Creates a plain object from a MotionMotorStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof MotionMotorStateInfo
   * @static
   * @param {MotionMotorStateInfo} message MotionMotorStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  MotionMotorStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.exclusiveState = null;
      object.sentryAutoHand = null;
    }
    if (
      message.exclusiveState != null &&
      Object.hasOwnProperty.call(message, "exclusiveState")
    )
      object.exclusiveState = $root.ExclusiveMotionMotorState.toObject(
        message.exclusiveState,
        options,
        q + 1,
      );
    if (
      message.sentryAutoHand != null &&
      Object.hasOwnProperty.call(message, "sentryAutoHand")
    )
      object.sentryAutoHand = $root.notify.SentryAutoHand.toObject(
        message.sentryAutoHand,
        options,
        q + 1,
      );
    if (
      message.rollLevelCalibrationAngle != null &&
      Object.hasOwnProperty.call(message, "rollLevelCalibrationAngle")
    ) {
      object.rollLevelCalibrationAngle =
        $root.notify.RollLevelCalibrationAngle.toObject(
          message.rollLevelCalibrationAngle,
          options,
          q + 1,
        );
      if (options.oneofs)
        object._rollLevelCalibrationAngle = "rollLevelCalibrationAngle";
    }
    return object;
  };

  /**
   * Converts this MotionMotorStateInfo to JSON.
   * @function toJSON
   * @memberof MotionMotorStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  MotionMotorStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for MotionMotorStateInfo
   * @function getTypeUrl
   * @memberof MotionMotorStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  MotionMotorStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/MotionMotorStateInfo";
  };

  return MotionMotorStateInfo;
})();

$root.DeviceStateInfo = (function () {
  /**
   * Properties of a DeviceStateInfo.
   * @exports IDeviceStateInfo
   * @interface IDeviceStateInfo
   * @property {notify.IRgbState|null} [rgbState] DeviceStateInfo rgbState
   * @property {notify.IPowerIndState|null} [powerIndState] DeviceStateInfo powerIndState
   * @property {notify.IChargingState|null} [chargingState] DeviceStateInfo chargingState
   * @property {notify.IStorageInfo|null} [storageInfo] DeviceStateInfo storageInfo
   * @property {notify.IMTPState|null} [mtpState] DeviceStateInfo mtpState
   * @property {notify.ICPUMode|null} [cpuMode] DeviceStateInfo cpuMode
   * @property {notify.ITemperature|null} [temperature] DeviceStateInfo temperature
   * @property {notify.IBodyStatus|null} [bodyStatus] DeviceStateInfo bodyStatus
   * @property {notify.IBatteryInfo|null} [batteryInfo] DeviceStateInfo batteryInfo
   * @property {notify.ICalibrationResult|null} [calibrationResult] DeviceStateInfo calibrationResult
   * @property {notify.IPictureMatching|null} [pictureMatching] DeviceStateInfo pictureMatching
   * @property {notify.IAutoShutdown|null} [autoShutdown] DeviceStateInfo autoShutdown
   * @property {notify.ILensDefog|null} [lensDefog] DeviceStateInfo lensDefog
   * @property {notify.IAutoCooling|null} [autoCooling] DeviceStateInfo autoCooling
   */

  /**
   * Constructs a new DeviceStateInfo.
   * @exports DeviceStateInfo
   * @classdesc Represents a DeviceStateInfo.
   * @implements IDeviceStateInfo
   * @constructor
   * @param {IDeviceStateInfo=} [properties] Properties to set
   */
  function DeviceStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * DeviceStateInfo rgbState.
   * @member {notify.IRgbState|null|undefined} rgbState
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.rgbState = null;

  /**
   * DeviceStateInfo powerIndState.
   * @member {notify.IPowerIndState|null|undefined} powerIndState
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.powerIndState = null;

  /**
   * DeviceStateInfo chargingState.
   * @member {notify.IChargingState|null|undefined} chargingState
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.chargingState = null;

  /**
   * DeviceStateInfo storageInfo.
   * @member {notify.IStorageInfo|null|undefined} storageInfo
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.storageInfo = null;

  /**
   * DeviceStateInfo mtpState.
   * @member {notify.IMTPState|null|undefined} mtpState
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.mtpState = null;

  /**
   * DeviceStateInfo cpuMode.
   * @member {notify.ICPUMode|null|undefined} cpuMode
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.cpuMode = null;

  /**
   * DeviceStateInfo temperature.
   * @member {notify.ITemperature|null|undefined} temperature
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.temperature = null;

  /**
   * DeviceStateInfo bodyStatus.
   * @member {notify.IBodyStatus|null|undefined} bodyStatus
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.bodyStatus = null;

  /**
   * DeviceStateInfo batteryInfo.
   * @member {notify.IBatteryInfo|null|undefined} batteryInfo
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.batteryInfo = null;

  /**
   * DeviceStateInfo calibrationResult.
   * @member {notify.ICalibrationResult|null|undefined} calibrationResult
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.calibrationResult = null;

  /**
   * DeviceStateInfo pictureMatching.
   * @member {notify.IPictureMatching|null|undefined} pictureMatching
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.pictureMatching = null;

  /**
   * DeviceStateInfo autoShutdown.
   * @member {notify.IAutoShutdown|null|undefined} autoShutdown
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.autoShutdown = null;

  /**
   * DeviceStateInfo lensDefog.
   * @member {notify.ILensDefog|null|undefined} lensDefog
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.lensDefog = null;

  /**
   * DeviceStateInfo autoCooling.
   * @member {notify.IAutoCooling|null|undefined} autoCooling
   * @memberof DeviceStateInfo
   * @instance
   */
  DeviceStateInfo.prototype.autoCooling = null;

  /**
   * Creates a new DeviceStateInfo instance using the specified properties.
   * @function create
   * @memberof DeviceStateInfo
   * @static
   * @param {IDeviceStateInfo=} [properties] Properties to set
   * @returns {DeviceStateInfo} DeviceStateInfo instance
   */
  DeviceStateInfo.create = function create(properties) {
    return new DeviceStateInfo(properties);
  };

  /**
   * Encodes the specified DeviceStateInfo message. Does not implicitly {@link DeviceStateInfo.verify|verify} messages.
   * @function encode
   * @memberof DeviceStateInfo
   * @static
   * @param {IDeviceStateInfo} message DeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DeviceStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.rgbState != null &&
      Object.hasOwnProperty.call(message, "rgbState")
    )
      $root.notify.RgbState.encode(
        message.rgbState,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    if (
      message.powerIndState != null &&
      Object.hasOwnProperty.call(message, "powerIndState")
    )
      $root.notify.PowerIndState.encode(
        message.powerIndState,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.chargingState != null &&
      Object.hasOwnProperty.call(message, "chargingState")
    )
      $root.notify.ChargingState.encode(
        message.chargingState,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.storageInfo != null &&
      Object.hasOwnProperty.call(message, "storageInfo")
    )
      $root.notify.StorageInfo.encode(
        message.storageInfo,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    if (
      message.mtpState != null &&
      Object.hasOwnProperty.call(message, "mtpState")
    )
      $root.notify.MTPState.encode(
        message.mtpState,
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
        q + 1,
      ).ldelim();
    if (
      message.cpuMode != null &&
      Object.hasOwnProperty.call(message, "cpuMode")
    )
      $root.notify.CPUMode.encode(
        message.cpuMode,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    )
      $root.notify.Temperature.encode(
        message.temperature,
        writer.uint32(/* id 7, wireType 2 =*/ 58).fork(),
        q + 1,
      ).ldelim();
    if (
      message.bodyStatus != null &&
      Object.hasOwnProperty.call(message, "bodyStatus")
    )
      $root.notify.BodyStatus.encode(
        message.bodyStatus,
        writer.uint32(/* id 8, wireType 2 =*/ 66).fork(),
        q + 1,
      ).ldelim();
    if (
      message.batteryInfo != null &&
      Object.hasOwnProperty.call(message, "batteryInfo")
    )
      $root.notify.BatteryInfo.encode(
        message.batteryInfo,
        writer.uint32(/* id 9, wireType 2 =*/ 74).fork(),
        q + 1,
      ).ldelim();
    if (
      message.calibrationResult != null &&
      Object.hasOwnProperty.call(message, "calibrationResult")
    )
      $root.notify.CalibrationResult.encode(
        message.calibrationResult,
        writer.uint32(/* id 10, wireType 2 =*/ 82).fork(),
        q + 1,
      ).ldelim();
    if (
      message.pictureMatching != null &&
      Object.hasOwnProperty.call(message, "pictureMatching")
    )
      $root.notify.PictureMatching.encode(
        message.pictureMatching,
        writer.uint32(/* id 11, wireType 2 =*/ 90).fork(),
        q + 1,
      ).ldelim();
    if (
      message.autoShutdown != null &&
      Object.hasOwnProperty.call(message, "autoShutdown")
    )
      $root.notify.AutoShutdown.encode(
        message.autoShutdown,
        writer.uint32(/* id 12, wireType 2 =*/ 98).fork(),
        q + 1,
      ).ldelim();
    if (
      message.lensDefog != null &&
      Object.hasOwnProperty.call(message, "lensDefog")
    )
      $root.notify.LensDefog.encode(
        message.lensDefog,
        writer.uint32(/* id 13, wireType 2 =*/ 106).fork(),
        q + 1,
      ).ldelim();
    if (
      message.autoCooling != null &&
      Object.hasOwnProperty.call(message, "autoCooling")
    )
      $root.notify.AutoCooling.encode(
        message.autoCooling,
        writer.uint32(/* id 14, wireType 2 =*/ 114).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified DeviceStateInfo message, length delimited. Does not implicitly {@link DeviceStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof DeviceStateInfo
   * @static
   * @param {IDeviceStateInfo} message DeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  DeviceStateInfo.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a DeviceStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof DeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {DeviceStateInfo} DeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DeviceStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.DeviceStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.rgbState = $root.notify.RgbState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 2: {
          message.powerIndState = $root.notify.PowerIndState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.chargingState = $root.notify.ChargingState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.storageInfo = $root.notify.StorageInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 5: {
          message.mtpState = $root.notify.MTPState.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 6: {
          message.cpuMode = $root.notify.CPUMode.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 7: {
          message.temperature = $root.notify.Temperature.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 8: {
          message.bodyStatus = $root.notify.BodyStatus.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 9: {
          message.batteryInfo = $root.notify.BatteryInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 10: {
          message.calibrationResult = $root.notify.CalibrationResult.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 11: {
          message.pictureMatching = $root.notify.PictureMatching.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 12: {
          message.autoShutdown = $root.notify.AutoShutdown.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 13: {
          message.lensDefog = $root.notify.LensDefog.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 14: {
          message.autoCooling = $root.notify.AutoCooling.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a DeviceStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof DeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {DeviceStateInfo} DeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  DeviceStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a DeviceStateInfo message.
   * @function verify
   * @memberof DeviceStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  DeviceStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.rgbState != null &&
      Object.hasOwnProperty.call(message, "rgbState")
    ) {
      var error = $root.notify.RgbState.verify(message.rgbState, long + 1);
      if (error) return "rgbState." + error;
    }
    if (
      message.powerIndState != null &&
      Object.hasOwnProperty.call(message, "powerIndState")
    ) {
      var error = $root.notify.PowerIndState.verify(
        message.powerIndState,
        long + 1,
      );
      if (error) return "powerIndState." + error;
    }
    if (
      message.chargingState != null &&
      Object.hasOwnProperty.call(message, "chargingState")
    ) {
      var error = $root.notify.ChargingState.verify(
        message.chargingState,
        long + 1,
      );
      if (error) return "chargingState." + error;
    }
    if (
      message.storageInfo != null &&
      Object.hasOwnProperty.call(message, "storageInfo")
    ) {
      var error = $root.notify.StorageInfo.verify(
        message.storageInfo,
        long + 1,
      );
      if (error) return "storageInfo." + error;
    }
    if (
      message.mtpState != null &&
      Object.hasOwnProperty.call(message, "mtpState")
    ) {
      var error = $root.notify.MTPState.verify(message.mtpState, long + 1);
      if (error) return "mtpState." + error;
    }
    if (
      message.cpuMode != null &&
      Object.hasOwnProperty.call(message, "cpuMode")
    ) {
      var error = $root.notify.CPUMode.verify(message.cpuMode, long + 1);
      if (error) return "cpuMode." + error;
    }
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    ) {
      var error = $root.notify.Temperature.verify(
        message.temperature,
        long + 1,
      );
      if (error) return "temperature." + error;
    }
    if (
      message.bodyStatus != null &&
      Object.hasOwnProperty.call(message, "bodyStatus")
    ) {
      var error = $root.notify.BodyStatus.verify(message.bodyStatus, long + 1);
      if (error) return "bodyStatus." + error;
    }
    if (
      message.batteryInfo != null &&
      Object.hasOwnProperty.call(message, "batteryInfo")
    ) {
      var error = $root.notify.BatteryInfo.verify(
        message.batteryInfo,
        long + 1,
      );
      if (error) return "batteryInfo." + error;
    }
    if (
      message.calibrationResult != null &&
      Object.hasOwnProperty.call(message, "calibrationResult")
    ) {
      var error = $root.notify.CalibrationResult.verify(
        message.calibrationResult,
        long + 1,
      );
      if (error) return "calibrationResult." + error;
    }
    if (
      message.pictureMatching != null &&
      Object.hasOwnProperty.call(message, "pictureMatching")
    ) {
      var error = $root.notify.PictureMatching.verify(
        message.pictureMatching,
        long + 1,
      );
      if (error) return "pictureMatching." + error;
    }
    if (
      message.autoShutdown != null &&
      Object.hasOwnProperty.call(message, "autoShutdown")
    ) {
      var error = $root.notify.AutoShutdown.verify(
        message.autoShutdown,
        long + 1,
      );
      if (error) return "autoShutdown." + error;
    }
    if (
      message.lensDefog != null &&
      Object.hasOwnProperty.call(message, "lensDefog")
    ) {
      var error = $root.notify.LensDefog.verify(message.lensDefog, long + 1);
      if (error) return "lensDefog." + error;
    }
    if (
      message.autoCooling != null &&
      Object.hasOwnProperty.call(message, "autoCooling")
    ) {
      var error = $root.notify.AutoCooling.verify(
        message.autoCooling,
        long + 1,
      );
      if (error) return "autoCooling." + error;
    }
    return null;
  };

  /**
   * Creates a DeviceStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof DeviceStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {DeviceStateInfo} DeviceStateInfo
   */
  DeviceStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.DeviceStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".DeviceStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.DeviceStateInfo();
    if (object.rgbState != null) {
      if (!$util.isObject(object.rgbState))
        throw TypeError(".DeviceStateInfo.rgbState: object expected");
      message.rgbState = $root.notify.RgbState.fromObject(
        object.rgbState,
        long + 1,
      );
    }
    if (object.powerIndState != null) {
      if (!$util.isObject(object.powerIndState))
        throw TypeError(".DeviceStateInfo.powerIndState: object expected");
      message.powerIndState = $root.notify.PowerIndState.fromObject(
        object.powerIndState,
        long + 1,
      );
    }
    if (object.chargingState != null) {
      if (!$util.isObject(object.chargingState))
        throw TypeError(".DeviceStateInfo.chargingState: object expected");
      message.chargingState = $root.notify.ChargingState.fromObject(
        object.chargingState,
        long + 1,
      );
    }
    if (object.storageInfo != null) {
      if (!$util.isObject(object.storageInfo))
        throw TypeError(".DeviceStateInfo.storageInfo: object expected");
      message.storageInfo = $root.notify.StorageInfo.fromObject(
        object.storageInfo,
        long + 1,
      );
    }
    if (object.mtpState != null) {
      if (!$util.isObject(object.mtpState))
        throw TypeError(".DeviceStateInfo.mtpState: object expected");
      message.mtpState = $root.notify.MTPState.fromObject(
        object.mtpState,
        long + 1,
      );
    }
    if (object.cpuMode != null) {
      if (!$util.isObject(object.cpuMode))
        throw TypeError(".DeviceStateInfo.cpuMode: object expected");
      message.cpuMode = $root.notify.CPUMode.fromObject(
        object.cpuMode,
        long + 1,
      );
    }
    if (object.temperature != null) {
      if (!$util.isObject(object.temperature))
        throw TypeError(".DeviceStateInfo.temperature: object expected");
      message.temperature = $root.notify.Temperature.fromObject(
        object.temperature,
        long + 1,
      );
    }
    if (object.bodyStatus != null) {
      if (!$util.isObject(object.bodyStatus))
        throw TypeError(".DeviceStateInfo.bodyStatus: object expected");
      message.bodyStatus = $root.notify.BodyStatus.fromObject(
        object.bodyStatus,
        long + 1,
      );
    }
    if (object.batteryInfo != null) {
      if (!$util.isObject(object.batteryInfo))
        throw TypeError(".DeviceStateInfo.batteryInfo: object expected");
      message.batteryInfo = $root.notify.BatteryInfo.fromObject(
        object.batteryInfo,
        long + 1,
      );
    }
    if (object.calibrationResult != null) {
      if (!$util.isObject(object.calibrationResult))
        throw TypeError(".DeviceStateInfo.calibrationResult: object expected");
      message.calibrationResult = $root.notify.CalibrationResult.fromObject(
        object.calibrationResult,
        long + 1,
      );
    }
    if (object.pictureMatching != null) {
      if (!$util.isObject(object.pictureMatching))
        throw TypeError(".DeviceStateInfo.pictureMatching: object expected");
      message.pictureMatching = $root.notify.PictureMatching.fromObject(
        object.pictureMatching,
        long + 1,
      );
    }
    if (object.autoShutdown != null) {
      if (!$util.isObject(object.autoShutdown))
        throw TypeError(".DeviceStateInfo.autoShutdown: object expected");
      message.autoShutdown = $root.notify.AutoShutdown.fromObject(
        object.autoShutdown,
        long + 1,
      );
    }
    if (object.lensDefog != null) {
      if (!$util.isObject(object.lensDefog))
        throw TypeError(".DeviceStateInfo.lensDefog: object expected");
      message.lensDefog = $root.notify.LensDefog.fromObject(
        object.lensDefog,
        long + 1,
      );
    }
    if (object.autoCooling != null) {
      if (!$util.isObject(object.autoCooling))
        throw TypeError(".DeviceStateInfo.autoCooling: object expected");
      message.autoCooling = $root.notify.AutoCooling.fromObject(
        object.autoCooling,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a DeviceStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof DeviceStateInfo
   * @static
   * @param {DeviceStateInfo} message DeviceStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  DeviceStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.rgbState = null;
      object.powerIndState = null;
      object.chargingState = null;
      object.storageInfo = null;
      object.mtpState = null;
      object.cpuMode = null;
      object.temperature = null;
      object.bodyStatus = null;
      object.batteryInfo = null;
      object.calibrationResult = null;
      object.pictureMatching = null;
      object.autoShutdown = null;
      object.lensDefog = null;
      object.autoCooling = null;
    }
    if (
      message.rgbState != null &&
      Object.hasOwnProperty.call(message, "rgbState")
    )
      object.rgbState = $root.notify.RgbState.toObject(
        message.rgbState,
        options,
        q + 1,
      );
    if (
      message.powerIndState != null &&
      Object.hasOwnProperty.call(message, "powerIndState")
    )
      object.powerIndState = $root.notify.PowerIndState.toObject(
        message.powerIndState,
        options,
        q + 1,
      );
    if (
      message.chargingState != null &&
      Object.hasOwnProperty.call(message, "chargingState")
    )
      object.chargingState = $root.notify.ChargingState.toObject(
        message.chargingState,
        options,
        q + 1,
      );
    if (
      message.storageInfo != null &&
      Object.hasOwnProperty.call(message, "storageInfo")
    )
      object.storageInfo = $root.notify.StorageInfo.toObject(
        message.storageInfo,
        options,
        q + 1,
      );
    if (
      message.mtpState != null &&
      Object.hasOwnProperty.call(message, "mtpState")
    )
      object.mtpState = $root.notify.MTPState.toObject(
        message.mtpState,
        options,
        q + 1,
      );
    if (
      message.cpuMode != null &&
      Object.hasOwnProperty.call(message, "cpuMode")
    )
      object.cpuMode = $root.notify.CPUMode.toObject(
        message.cpuMode,
        options,
        q + 1,
      );
    if (
      message.temperature != null &&
      Object.hasOwnProperty.call(message, "temperature")
    )
      object.temperature = $root.notify.Temperature.toObject(
        message.temperature,
        options,
        q + 1,
      );
    if (
      message.bodyStatus != null &&
      Object.hasOwnProperty.call(message, "bodyStatus")
    )
      object.bodyStatus = $root.notify.BodyStatus.toObject(
        message.bodyStatus,
        options,
        q + 1,
      );
    if (
      message.batteryInfo != null &&
      Object.hasOwnProperty.call(message, "batteryInfo")
    )
      object.batteryInfo = $root.notify.BatteryInfo.toObject(
        message.batteryInfo,
        options,
        q + 1,
      );
    if (
      message.calibrationResult != null &&
      Object.hasOwnProperty.call(message, "calibrationResult")
    )
      object.calibrationResult = $root.notify.CalibrationResult.toObject(
        message.calibrationResult,
        options,
        q + 1,
      );
    if (
      message.pictureMatching != null &&
      Object.hasOwnProperty.call(message, "pictureMatching")
    )
      object.pictureMatching = $root.notify.PictureMatching.toObject(
        message.pictureMatching,
        options,
        q + 1,
      );
    if (
      message.autoShutdown != null &&
      Object.hasOwnProperty.call(message, "autoShutdown")
    )
      object.autoShutdown = $root.notify.AutoShutdown.toObject(
        message.autoShutdown,
        options,
        q + 1,
      );
    if (
      message.lensDefog != null &&
      Object.hasOwnProperty.call(message, "lensDefog")
    )
      object.lensDefog = $root.notify.LensDefog.toObject(
        message.lensDefog,
        options,
        q + 1,
      );
    if (
      message.autoCooling != null &&
      Object.hasOwnProperty.call(message, "autoCooling")
    )
      object.autoCooling = $root.notify.AutoCooling.toObject(
        message.autoCooling,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this DeviceStateInfo to JSON.
   * @function toJSON
   * @memberof DeviceStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  DeviceStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for DeviceStateInfo
   * @function getTypeUrl
   * @memberof DeviceStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  DeviceStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/DeviceStateInfo";
  };

  return DeviceStateInfo;
})();

$root.ConnectionStateInfo = (function () {
  /**
   * Properties of a ConnectionStateInfo.
   * @exports IConnectionStateInfo
   * @interface IConnectionStateInfo
   * @property {notify.IHostSlaveMode|null} [hostSlaveMode] ConnectionStateInfo hostSlaveMode
   */

  /**
   * Constructs a new ConnectionStateInfo.
   * @exports ConnectionStateInfo
   * @classdesc Represents a ConnectionStateInfo.
   * @implements IConnectionStateInfo
   * @constructor
   * @param {IConnectionStateInfo=} [properties] Properties to set
   */
  function ConnectionStateInfo(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ConnectionStateInfo hostSlaveMode.
   * @member {notify.IHostSlaveMode|null|undefined} hostSlaveMode
   * @memberof ConnectionStateInfo
   * @instance
   */
  ConnectionStateInfo.prototype.hostSlaveMode = null;

  /**
   * Creates a new ConnectionStateInfo instance using the specified properties.
   * @function create
   * @memberof ConnectionStateInfo
   * @static
   * @param {IConnectionStateInfo=} [properties] Properties to set
   * @returns {ConnectionStateInfo} ConnectionStateInfo instance
   */
  ConnectionStateInfo.create = function create(properties) {
    return new ConnectionStateInfo(properties);
  };

  /**
   * Encodes the specified ConnectionStateInfo message. Does not implicitly {@link ConnectionStateInfo.verify|verify} messages.
   * @function encode
   * @memberof ConnectionStateInfo
   * @static
   * @param {IConnectionStateInfo} message ConnectionStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ConnectionStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.hostSlaveMode != null &&
      Object.hasOwnProperty.call(message, "hostSlaveMode")
    )
      $root.notify.HostSlaveMode.encode(
        message.hostSlaveMode,
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ConnectionStateInfo message, length delimited. Does not implicitly {@link ConnectionStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ConnectionStateInfo
   * @static
   * @param {IConnectionStateInfo} message ConnectionStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ConnectionStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ConnectionStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof ConnectionStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ConnectionStateInfo} ConnectionStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ConnectionStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ConnectionStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.hostSlaveMode = $root.notify.HostSlaveMode.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ConnectionStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ConnectionStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ConnectionStateInfo} ConnectionStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ConnectionStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ConnectionStateInfo message.
   * @function verify
   * @memberof ConnectionStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ConnectionStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.hostSlaveMode != null &&
      Object.hasOwnProperty.call(message, "hostSlaveMode")
    ) {
      var error = $root.notify.HostSlaveMode.verify(
        message.hostSlaveMode,
        long + 1,
      );
      if (error) return "hostSlaveMode." + error;
    }
    return null;
  };

  /**
   * Creates a ConnectionStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ConnectionStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ConnectionStateInfo} ConnectionStateInfo
   */
  ConnectionStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ConnectionStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ConnectionStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ConnectionStateInfo();
    if (object.hostSlaveMode != null) {
      if (!$util.isObject(object.hostSlaveMode))
        throw TypeError(".ConnectionStateInfo.hostSlaveMode: object expected");
      message.hostSlaveMode = $root.notify.HostSlaveMode.fromObject(
        object.hostSlaveMode,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ConnectionStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ConnectionStateInfo
   * @static
   * @param {ConnectionStateInfo} message ConnectionStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ConnectionStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.hostSlaveMode = null;
    if (
      message.hostSlaveMode != null &&
      Object.hasOwnProperty.call(message, "hostSlaveMode")
    )
      object.hostSlaveMode = $root.notify.HostSlaveMode.toObject(
        message.hostSlaveMode,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ConnectionStateInfo to JSON.
   * @function toJSON
   * @memberof ConnectionStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ConnectionStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ConnectionStateInfo
   * @function getTypeUrl
   * @memberof ConnectionStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ConnectionStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ConnectionStateInfo";
  };

  return ConnectionStateInfo;
})();

$root.ShootingModeAndTech = (function () {
  /**
   * Properties of a ShootingModeAndTech.
   * @exports IShootingModeAndTech
   * @interface IShootingModeAndTech
   * @property {number|null} [shootingMode] ShootingModeAndTech shootingMode
   * @property {number|null} [parentShootingMode] ShootingModeAndTech parentShootingMode
   * @property {Array.<number>|null} [shootingTechs] ShootingModeAndTech shootingTechs
   */

  /**
   * Constructs a new ShootingModeAndTech.
   * @exports ShootingModeAndTech
   * @classdesc Represents a ShootingModeAndTech.
   * @implements IShootingModeAndTech
   * @constructor
   * @param {IShootingModeAndTech=} [properties] Properties to set
   */
  function ShootingModeAndTech(properties) {
    this.shootingTechs = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ShootingModeAndTech shootingMode.
   * @member {number} shootingMode
   * @memberof ShootingModeAndTech
   * @instance
   */
  ShootingModeAndTech.prototype.shootingMode = 0;

  /**
   * ShootingModeAndTech parentShootingMode.
   * @member {number} parentShootingMode
   * @memberof ShootingModeAndTech
   * @instance
   */
  ShootingModeAndTech.prototype.parentShootingMode = 0;

  /**
   * ShootingModeAndTech shootingTechs.
   * @member {Array.<number>} shootingTechs
   * @memberof ShootingModeAndTech
   * @instance
   */
  ShootingModeAndTech.prototype.shootingTechs = $util.emptyArray;

  /**
   * Creates a new ShootingModeAndTech instance using the specified properties.
   * @function create
   * @memberof ShootingModeAndTech
   * @static
   * @param {IShootingModeAndTech=} [properties] Properties to set
   * @returns {ShootingModeAndTech} ShootingModeAndTech instance
   */
  ShootingModeAndTech.create = function create(properties) {
    return new ShootingModeAndTech(properties);
  };

  /**
   * Encodes the specified ShootingModeAndTech message. Does not implicitly {@link ShootingModeAndTech.verify|verify} messages.
   * @function encode
   * @memberof ShootingModeAndTech
   * @static
   * @param {IShootingModeAndTech} message ShootingModeAndTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingModeAndTech.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.shootingMode);
    if (
      message.parentShootingMode != null &&
      Object.hasOwnProperty.call(message, "parentShootingMode")
    )
      writer
        .uint32(/* id 2, wireType 0 =*/ 16)
        .int32(message.parentShootingMode);
    if (message.shootingTechs != null && message.shootingTechs.length) {
      writer.uint32(/* id 3, wireType 2 =*/ 26).fork();
      for (var i = 0; i < message.shootingTechs.length; ++i)
        writer.int32(message.shootingTechs[i]);
      writer.ldelim();
    }
    return writer;
  };

  /**
   * Encodes the specified ShootingModeAndTech message, length delimited. Does not implicitly {@link ShootingModeAndTech.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ShootingModeAndTech
   * @static
   * @param {IShootingModeAndTech} message ShootingModeAndTech message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ShootingModeAndTech.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ShootingModeAndTech message from the specified reader or buffer.
   * @function decode
   * @memberof ShootingModeAndTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ShootingModeAndTech} ShootingModeAndTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingModeAndTech.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ShootingModeAndTech();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingMode = reader.int32();
          break;
        }
        case 2: {
          message.parentShootingMode = reader.int32();
          break;
        }
        case 3: {
          if (!(message.shootingTechs && message.shootingTechs.length))
            message.shootingTechs = [];
          if ((tag & 7) === 2) {
            var end2 = reader.uint32() + reader.pos;
            if (end2 > reader.len) throw RangeError("index out of range");
            reader.len = end2;
            while (reader.pos < end2)
              message.shootingTechs.push(reader.int32());
            if (reader.pos !== end2) throw RangeError("index out of range");
            reader.len = end;
          } else message.shootingTechs.push(reader.int32());
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ShootingModeAndTech message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ShootingModeAndTech
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ShootingModeAndTech} ShootingModeAndTech
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ShootingModeAndTech.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ShootingModeAndTech message.
   * @function verify
   * @memberof ShootingModeAndTech
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ShootingModeAndTech.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    if (
      message.parentShootingMode != null &&
      Object.hasOwnProperty.call(message, "parentShootingMode")
    )
      if (!$util.isInteger(message.parentShootingMode))
        return "parentShootingMode: integer expected";
    if (
      message.shootingTechs != null &&
      Object.hasOwnProperty.call(message, "shootingTechs")
    ) {
      if (!Array.isArray(message.shootingTechs))
        return "shootingTechs: array expected";
      for (var i = 0; i < message.shootingTechs.length; ++i)
        if (!$util.isInteger(message.shootingTechs[i]))
          return "shootingTechs: integer[] expected";
    }
    return null;
  };

  /**
   * Creates a ShootingModeAndTech message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ShootingModeAndTech
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ShootingModeAndTech} ShootingModeAndTech
   */
  ShootingModeAndTech.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ShootingModeAndTech) return object;
    if (!$util.isObject(object))
      throw TypeError(".ShootingModeAndTech: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ShootingModeAndTech();
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    if (object.parentShootingMode != null)
      message.parentShootingMode = object.parentShootingMode | 0;
    if (object.shootingTechs) {
      if (!Array.isArray(object.shootingTechs))
        throw TypeError(".ShootingModeAndTech.shootingTechs: array expected");
      message.shootingTechs = [];
      for (var i = 0; i < object.shootingTechs.length; ++i)
        message.shootingTechs[i] = object.shootingTechs[i] | 0;
    }
    return message;
  };

  /**
   * Creates a plain object from a ShootingModeAndTech message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ShootingModeAndTech
   * @static
   * @param {ShootingModeAndTech} message ShootingModeAndTech
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ShootingModeAndTech.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.shootingTechs = [];
    if (options.defaults) {
      object.shootingMode = 0;
      object.parentShootingMode = 0;
    }
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    if (
      message.parentShootingMode != null &&
      Object.hasOwnProperty.call(message, "parentShootingMode")
    )
      object.parentShootingMode = message.parentShootingMode;
    if (message.shootingTechs && message.shootingTechs.length) {
      object.shootingTechs = [];
      for (var j = 0; j < message.shootingTechs.length; ++j)
        object.shootingTechs[j] = message.shootingTechs[j];
    }
    return object;
  };

  /**
   * Converts this ShootingModeAndTech to JSON.
   * @function toJSON
   * @memberof ShootingModeAndTech
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ShootingModeAndTech.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ShootingModeAndTech
   * @function getTypeUrl
   * @memberof ShootingModeAndTech
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ShootingModeAndTech.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ShootingModeAndTech";
  };

  return ShootingModeAndTech;
})();

$root.ResGetDeviceStateInfo = (function () {
  /**
   * Properties of a ResGetDeviceStateInfo.
   * @exports IResGetDeviceStateInfo
   * @interface IResGetDeviceStateInfo
   * @property {number|null} [shootingMode] ResGetDeviceStateInfo shootingMode
   * @property {ITeleCameraStateInfo|null} [teleCameraStateInfo] ResGetDeviceStateInfo teleCameraStateInfo
   * @property {IWideCameraStateInfo|null} [wideCameraStateInfo] ResGetDeviceStateInfo wideCameraStateInfo
   * @property {IFocusMotorStateInfo|null} [focusMotorStateInfo] ResGetDeviceStateInfo focusMotorStateInfo
   * @property {IMotionMotorStateInfo|null} [motionMotorStateInfo] ResGetDeviceStateInfo motionMotorStateInfo
   * @property {IDeviceStateInfo|null} [deviceStateInfo] ResGetDeviceStateInfo deviceStateInfo
   * @property {number|null} [code] ResGetDeviceStateInfo code
   * @property {IConnectionStateInfo|null} [connectionStateInfo] ResGetDeviceStateInfo connectionStateInfo
   * @property {Array.<IShootingModeAndTech>|null} [shootingModeAndTechs] ResGetDeviceStateInfo shootingModeAndTechs
   * @property {IFocusMotorStateInfo|null} [wideFocusMotorStateInfo] ResGetDeviceStateInfo wideFocusMotorStateInfo
   * @property {IFocusMotorStateInfo|null} [guideFocusMotorStateInfo] ResGetDeviceStateInfo guideFocusMotorStateInfo
   */

  /**
   * Constructs a new ResGetDeviceStateInfo.
   * @exports ResGetDeviceStateInfo
   * @classdesc Represents a ResGetDeviceStateInfo.
   * @implements IResGetDeviceStateInfo
   * @constructor
   * @param {IResGetDeviceStateInfo=} [properties] Properties to set
   */
  function ResGetDeviceStateInfo(properties) {
    this.shootingModeAndTechs = [];
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ResGetDeviceStateInfo shootingMode.
   * @member {number} shootingMode
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.shootingMode = 0;

  /**
   * ResGetDeviceStateInfo teleCameraStateInfo.
   * @member {ITeleCameraStateInfo|null|undefined} teleCameraStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.teleCameraStateInfo = null;

  /**
   * ResGetDeviceStateInfo wideCameraStateInfo.
   * @member {IWideCameraStateInfo|null|undefined} wideCameraStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.wideCameraStateInfo = null;

  /**
   * ResGetDeviceStateInfo focusMotorStateInfo.
   * @member {IFocusMotorStateInfo|null|undefined} focusMotorStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.focusMotorStateInfo = null;

  /**
   * ResGetDeviceStateInfo motionMotorStateInfo.
   * @member {IMotionMotorStateInfo|null|undefined} motionMotorStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.motionMotorStateInfo = null;

  /**
   * ResGetDeviceStateInfo deviceStateInfo.
   * @member {IDeviceStateInfo|null|undefined} deviceStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.deviceStateInfo = null;

  /**
   * ResGetDeviceStateInfo code.
   * @member {number} code
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.code = 0;

  /**
   * ResGetDeviceStateInfo connectionStateInfo.
   * @member {IConnectionStateInfo|null|undefined} connectionStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.connectionStateInfo = null;

  /**
   * ResGetDeviceStateInfo shootingModeAndTechs.
   * @member {Array.<IShootingModeAndTech>} shootingModeAndTechs
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.shootingModeAndTechs = $util.emptyArray;

  /**
   * ResGetDeviceStateInfo wideFocusMotorStateInfo.
   * @member {IFocusMotorStateInfo|null|undefined} wideFocusMotorStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.wideFocusMotorStateInfo = null;

  /**
   * ResGetDeviceStateInfo guideFocusMotorStateInfo.
   * @member {IFocusMotorStateInfo|null|undefined} guideFocusMotorStateInfo
   * @memberof ResGetDeviceStateInfo
   * @instance
   */
  ResGetDeviceStateInfo.prototype.guideFocusMotorStateInfo = null;

  /**
   * Creates a new ResGetDeviceStateInfo instance using the specified properties.
   * @function create
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {IResGetDeviceStateInfo=} [properties] Properties to set
   * @returns {ResGetDeviceStateInfo} ResGetDeviceStateInfo instance
   */
  ResGetDeviceStateInfo.create = function create(properties) {
    return new ResGetDeviceStateInfo(properties);
  };

  /**
   * Encodes the specified ResGetDeviceStateInfo message. Does not implicitly {@link ResGetDeviceStateInfo.verify|verify} messages.
   * @function encode
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {IResGetDeviceStateInfo} message ResGetDeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDeviceStateInfo.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.shootingMode);
    if (
      message.teleCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "teleCameraStateInfo")
    )
      $root.TeleCameraStateInfo.encode(
        message.teleCameraStateInfo,
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork(),
        q + 1,
      ).ldelim();
    if (
      message.wideCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideCameraStateInfo")
    )
      $root.WideCameraStateInfo.encode(
        message.wideCameraStateInfo,
        writer.uint32(/* id 3, wireType 2 =*/ 26).fork(),
        q + 1,
      ).ldelim();
    if (
      message.focusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "focusMotorStateInfo")
    )
      $root.FocusMotorStateInfo.encode(
        message.focusMotorStateInfo,
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork(),
        q + 1,
      ).ldelim();
    if (
      message.motionMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "motionMotorStateInfo")
    )
      $root.MotionMotorStateInfo.encode(
        message.motionMotorStateInfo,
        writer.uint32(/* id 5, wireType 2 =*/ 42).fork(),
        q + 1,
      ).ldelim();
    if (
      message.deviceStateInfo != null &&
      Object.hasOwnProperty.call(message, "deviceStateInfo")
    )
      $root.DeviceStateInfo.encode(
        message.deviceStateInfo,
        writer.uint32(/* id 6, wireType 2 =*/ 50).fork(),
        q + 1,
      ).ldelim();
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      writer.uint32(/* id 7, wireType 0 =*/ 56).int32(message.code);
    if (
      message.connectionStateInfo != null &&
      Object.hasOwnProperty.call(message, "connectionStateInfo")
    )
      $root.ConnectionStateInfo.encode(
        message.connectionStateInfo,
        writer.uint32(/* id 8, wireType 2 =*/ 66).fork(),
        q + 1,
      ).ldelim();
    if (
      message.shootingModeAndTechs != null &&
      message.shootingModeAndTechs.length
    )
      for (var i = 0; i < message.shootingModeAndTechs.length; ++i)
        $root.ShootingModeAndTech.encode(
          message.shootingModeAndTechs[i],
          writer.uint32(/* id 9, wireType 2 =*/ 74).fork(),
          q + 1,
        ).ldelim();
    if (
      message.wideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideFocusMotorStateInfo")
    )
      $root.FocusMotorStateInfo.encode(
        message.wideFocusMotorStateInfo,
        writer.uint32(/* id 10, wireType 2 =*/ 82).fork(),
        q + 1,
      ).ldelim();
    if (
      message.guideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "guideFocusMotorStateInfo")
    )
      $root.FocusMotorStateInfo.encode(
        message.guideFocusMotorStateInfo,
        writer.uint32(/* id 11, wireType 2 =*/ 90).fork(),
        q + 1,
      ).ldelim();
    return writer;
  };

  /**
   * Encodes the specified ResGetDeviceStateInfo message, length delimited. Does not implicitly {@link ResGetDeviceStateInfo.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {IResGetDeviceStateInfo} message ResGetDeviceStateInfo message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ResGetDeviceStateInfo.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ResGetDeviceStateInfo message from the specified reader or buffer.
   * @function decode
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ResGetDeviceStateInfo} ResGetDeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDeviceStateInfo.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ResGetDeviceStateInfo();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.shootingMode = reader.int32();
          break;
        }
        case 2: {
          message.teleCameraStateInfo = $root.TeleCameraStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 3: {
          message.wideCameraStateInfo = $root.WideCameraStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 4: {
          message.focusMotorStateInfo = $root.FocusMotorStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 5: {
          message.motionMotorStateInfo = $root.MotionMotorStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 6: {
          message.deviceStateInfo = $root.DeviceStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 7: {
          message.code = reader.int32();
          break;
        }
        case 8: {
          message.connectionStateInfo = $root.ConnectionStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 9: {
          if (!(
            message.shootingModeAndTechs && message.shootingModeAndTechs.length
          ))
            message.shootingModeAndTechs = [];
          message.shootingModeAndTechs.push(
            $root.ShootingModeAndTech.decode(
              reader,
              reader.uint32(),
              undefined,
              long + 1,
            ),
          );
          break;
        }
        case 10: {
          message.wideFocusMotorStateInfo = $root.FocusMotorStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        case 11: {
          message.guideFocusMotorStateInfo = $root.FocusMotorStateInfo.decode(
            reader,
            reader.uint32(),
            undefined,
            long + 1,
          );
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ResGetDeviceStateInfo message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ResGetDeviceStateInfo} ResGetDeviceStateInfo
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ResGetDeviceStateInfo.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ResGetDeviceStateInfo message.
   * @function verify
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ResGetDeviceStateInfo.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      if (!$util.isInteger(message.shootingMode))
        return "shootingMode: integer expected";
    if (
      message.teleCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "teleCameraStateInfo")
    ) {
      var error = $root.TeleCameraStateInfo.verify(
        message.teleCameraStateInfo,
        long + 1,
      );
      if (error) return "teleCameraStateInfo." + error;
    }
    if (
      message.wideCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideCameraStateInfo")
    ) {
      var error = $root.WideCameraStateInfo.verify(
        message.wideCameraStateInfo,
        long + 1,
      );
      if (error) return "wideCameraStateInfo." + error;
    }
    if (
      message.focusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "focusMotorStateInfo")
    ) {
      var error = $root.FocusMotorStateInfo.verify(
        message.focusMotorStateInfo,
        long + 1,
      );
      if (error) return "focusMotorStateInfo." + error;
    }
    if (
      message.motionMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "motionMotorStateInfo")
    ) {
      var error = $root.MotionMotorStateInfo.verify(
        message.motionMotorStateInfo,
        long + 1,
      );
      if (error) return "motionMotorStateInfo." + error;
    }
    if (
      message.deviceStateInfo != null &&
      Object.hasOwnProperty.call(message, "deviceStateInfo")
    ) {
      var error = $root.DeviceStateInfo.verify(
        message.deviceStateInfo,
        long + 1,
      );
      if (error) return "deviceStateInfo." + error;
    }
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      if (!$util.isInteger(message.code)) return "code: integer expected";
    if (
      message.connectionStateInfo != null &&
      Object.hasOwnProperty.call(message, "connectionStateInfo")
    ) {
      var error = $root.ConnectionStateInfo.verify(
        message.connectionStateInfo,
        long + 1,
      );
      if (error) return "connectionStateInfo." + error;
    }
    if (
      message.shootingModeAndTechs != null &&
      Object.hasOwnProperty.call(message, "shootingModeAndTechs")
    ) {
      if (!Array.isArray(message.shootingModeAndTechs))
        return "shootingModeAndTechs: array expected";
      for (var i = 0; i < message.shootingModeAndTechs.length; ++i) {
        var error = $root.ShootingModeAndTech.verify(
          message.shootingModeAndTechs[i],
          long + 1,
        );
        if (error) return "shootingModeAndTechs." + error;
      }
    }
    if (
      message.wideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideFocusMotorStateInfo")
    ) {
      var error = $root.FocusMotorStateInfo.verify(
        message.wideFocusMotorStateInfo,
        long + 1,
      );
      if (error) return "wideFocusMotorStateInfo." + error;
    }
    if (
      message.guideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "guideFocusMotorStateInfo")
    ) {
      var error = $root.FocusMotorStateInfo.verify(
        message.guideFocusMotorStateInfo,
        long + 1,
      );
      if (error) return "guideFocusMotorStateInfo." + error;
    }
    return null;
  };

  /**
   * Creates a ResGetDeviceStateInfo message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ResGetDeviceStateInfo} ResGetDeviceStateInfo
   */
  ResGetDeviceStateInfo.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ResGetDeviceStateInfo) return object;
    if (!$util.isObject(object))
      throw TypeError(".ResGetDeviceStateInfo: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ResGetDeviceStateInfo();
    if (object.shootingMode != null)
      message.shootingMode = object.shootingMode | 0;
    if (object.teleCameraStateInfo != null) {
      if (!$util.isObject(object.teleCameraStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.teleCameraStateInfo: object expected",
        );
      message.teleCameraStateInfo = $root.TeleCameraStateInfo.fromObject(
        object.teleCameraStateInfo,
        long + 1,
      );
    }
    if (object.wideCameraStateInfo != null) {
      if (!$util.isObject(object.wideCameraStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.wideCameraStateInfo: object expected",
        );
      message.wideCameraStateInfo = $root.WideCameraStateInfo.fromObject(
        object.wideCameraStateInfo,
        long + 1,
      );
    }
    if (object.focusMotorStateInfo != null) {
      if (!$util.isObject(object.focusMotorStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.focusMotorStateInfo: object expected",
        );
      message.focusMotorStateInfo = $root.FocusMotorStateInfo.fromObject(
        object.focusMotorStateInfo,
        long + 1,
      );
    }
    if (object.motionMotorStateInfo != null) {
      if (!$util.isObject(object.motionMotorStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.motionMotorStateInfo: object expected",
        );
      message.motionMotorStateInfo = $root.MotionMotorStateInfo.fromObject(
        object.motionMotorStateInfo,
        long + 1,
      );
    }
    if (object.deviceStateInfo != null) {
      if (!$util.isObject(object.deviceStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.deviceStateInfo: object expected",
        );
      message.deviceStateInfo = $root.DeviceStateInfo.fromObject(
        object.deviceStateInfo,
        long + 1,
      );
    }
    if (object.code != null) message.code = object.code | 0;
    if (object.connectionStateInfo != null) {
      if (!$util.isObject(object.connectionStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.connectionStateInfo: object expected",
        );
      message.connectionStateInfo = $root.ConnectionStateInfo.fromObject(
        object.connectionStateInfo,
        long + 1,
      );
    }
    if (object.shootingModeAndTechs) {
      if (!Array.isArray(object.shootingModeAndTechs))
        throw TypeError(
          ".ResGetDeviceStateInfo.shootingModeAndTechs: array expected",
        );
      message.shootingModeAndTechs = [];
      for (var i = 0; i < object.shootingModeAndTechs.length; ++i) {
        if (!$util.isObject(object.shootingModeAndTechs[i]))
          throw TypeError(
            ".ResGetDeviceStateInfo.shootingModeAndTechs: object expected",
          );
        message.shootingModeAndTechs[i] = $root.ShootingModeAndTech.fromObject(
          object.shootingModeAndTechs[i],
          long + 1,
        );
      }
    }
    if (object.wideFocusMotorStateInfo != null) {
      if (!$util.isObject(object.wideFocusMotorStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.wideFocusMotorStateInfo: object expected",
        );
      message.wideFocusMotorStateInfo = $root.FocusMotorStateInfo.fromObject(
        object.wideFocusMotorStateInfo,
        long + 1,
      );
    }
    if (object.guideFocusMotorStateInfo != null) {
      if (!$util.isObject(object.guideFocusMotorStateInfo))
        throw TypeError(
          ".ResGetDeviceStateInfo.guideFocusMotorStateInfo: object expected",
        );
      message.guideFocusMotorStateInfo = $root.FocusMotorStateInfo.fromObject(
        object.guideFocusMotorStateInfo,
        long + 1,
      );
    }
    return message;
  };

  /**
   * Creates a plain object from a ResGetDeviceStateInfo message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {ResGetDeviceStateInfo} message ResGetDeviceStateInfo
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ResGetDeviceStateInfo.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.arrays || options.defaults) object.shootingModeAndTechs = [];
    if (options.defaults) {
      object.shootingMode = 0;
      object.teleCameraStateInfo = null;
      object.wideCameraStateInfo = null;
      object.focusMotorStateInfo = null;
      object.motionMotorStateInfo = null;
      object.deviceStateInfo = null;
      object.code = 0;
      object.connectionStateInfo = null;
      object.wideFocusMotorStateInfo = null;
      object.guideFocusMotorStateInfo = null;
    }
    if (
      message.shootingMode != null &&
      Object.hasOwnProperty.call(message, "shootingMode")
    )
      object.shootingMode = message.shootingMode;
    if (
      message.teleCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "teleCameraStateInfo")
    )
      object.teleCameraStateInfo = $root.TeleCameraStateInfo.toObject(
        message.teleCameraStateInfo,
        options,
        q + 1,
      );
    if (
      message.wideCameraStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideCameraStateInfo")
    )
      object.wideCameraStateInfo = $root.WideCameraStateInfo.toObject(
        message.wideCameraStateInfo,
        options,
        q + 1,
      );
    if (
      message.focusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "focusMotorStateInfo")
    )
      object.focusMotorStateInfo = $root.FocusMotorStateInfo.toObject(
        message.focusMotorStateInfo,
        options,
        q + 1,
      );
    if (
      message.motionMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "motionMotorStateInfo")
    )
      object.motionMotorStateInfo = $root.MotionMotorStateInfo.toObject(
        message.motionMotorStateInfo,
        options,
        q + 1,
      );
    if (
      message.deviceStateInfo != null &&
      Object.hasOwnProperty.call(message, "deviceStateInfo")
    )
      object.deviceStateInfo = $root.DeviceStateInfo.toObject(
        message.deviceStateInfo,
        options,
        q + 1,
      );
    if (message.code != null && Object.hasOwnProperty.call(message, "code"))
      object.code = message.code;
    if (
      message.connectionStateInfo != null &&
      Object.hasOwnProperty.call(message, "connectionStateInfo")
    )
      object.connectionStateInfo = $root.ConnectionStateInfo.toObject(
        message.connectionStateInfo,
        options,
        q + 1,
      );
    if (message.shootingModeAndTechs && message.shootingModeAndTechs.length) {
      object.shootingModeAndTechs = [];
      for (var j = 0; j < message.shootingModeAndTechs.length; ++j)
        object.shootingModeAndTechs[j] = $root.ShootingModeAndTech.toObject(
          message.shootingModeAndTechs[j],
          options,
          q + 1,
        );
    }
    if (
      message.wideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "wideFocusMotorStateInfo")
    )
      object.wideFocusMotorStateInfo = $root.FocusMotorStateInfo.toObject(
        message.wideFocusMotorStateInfo,
        options,
        q + 1,
      );
    if (
      message.guideFocusMotorStateInfo != null &&
      Object.hasOwnProperty.call(message, "guideFocusMotorStateInfo")
    )
      object.guideFocusMotorStateInfo = $root.FocusMotorStateInfo.toObject(
        message.guideFocusMotorStateInfo,
        options,
        q + 1,
      );
    return object;
  };

  /**
   * Converts this ResGetDeviceStateInfo to JSON.
   * @function toJSON
   * @memberof ResGetDeviceStateInfo
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ResGetDeviceStateInfo.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ResGetDeviceStateInfo
   * @function getTypeUrl
   * @memberof ResGetDeviceStateInfo
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ResGetDeviceStateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ResGetDeviceStateInfo";
  };

  return ResGetDeviceStateInfo;
})();

$root.ReqStartTrack = (function () {
  /**
   * Properties of a ReqStartTrack.
   * @exports IReqStartTrack
   * @interface IReqStartTrack
   * @property {number|null} [x] ReqStartTrack x
   * @property {number|null} [y] ReqStartTrack y
   * @property {number|null} [w] ReqStartTrack w
   * @property {number|null} [h] ReqStartTrack h
   * @property {number|null} [camId] ReqStartTrack camId
   */

  /**
   * Constructs a new ReqStartTrack.
   * @exports ReqStartTrack
   * @classdesc Represents a ReqStartTrack.
   * @implements IReqStartTrack
   * @constructor
   * @param {IReqStartTrack=} [properties] Properties to set
   */
  function ReqStartTrack(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartTrack x.
   * @member {number} x
   * @memberof ReqStartTrack
   * @instance
   */
  ReqStartTrack.prototype.x = 0;

  /**
   * ReqStartTrack y.
   * @member {number} y
   * @memberof ReqStartTrack
   * @instance
   */
  ReqStartTrack.prototype.y = 0;

  /**
   * ReqStartTrack w.
   * @member {number} w
   * @memberof ReqStartTrack
   * @instance
   */
  ReqStartTrack.prototype.w = 0;

  /**
   * ReqStartTrack h.
   * @member {number} h
   * @memberof ReqStartTrack
   * @instance
   */
  ReqStartTrack.prototype.h = 0;

  /**
   * ReqStartTrack camId.
   * @member {number} camId
   * @memberof ReqStartTrack
   * @instance
   */
  ReqStartTrack.prototype.camId = 0;

  /**
   * Creates a new ReqStartTrack instance using the specified properties.
   * @function create
   * @memberof ReqStartTrack
   * @static
   * @param {IReqStartTrack=} [properties] Properties to set
   * @returns {ReqStartTrack} ReqStartTrack instance
   */
  ReqStartTrack.create = function create(properties) {
    return new ReqStartTrack(properties);
  };

  /**
   * Encodes the specified ReqStartTrack message. Does not implicitly {@link ReqStartTrack.verify|verify} messages.
   * @function encode
   * @memberof ReqStartTrack
   * @static
   * @param {IReqStartTrack} message ReqStartTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTrack.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.x);
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.y);
    if (message.w != null && Object.hasOwnProperty.call(message, "w"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.w);
    if (message.h != null && Object.hasOwnProperty.call(message, "h"))
      writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.h);
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.camId);
    return writer;
  };

  /**
   * Encodes the specified ReqStartTrack message, length delimited. Does not implicitly {@link ReqStartTrack.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartTrack
   * @static
   * @param {IReqStartTrack} message ReqStartTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTrack.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartTrack message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartTrack} ReqStartTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTrack.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartTrack();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.x = reader.int32();
          break;
        }
        case 2: {
          message.y = reader.int32();
          break;
        }
        case 3: {
          message.w = reader.int32();
          break;
        }
        case 4: {
          message.h = reader.int32();
          break;
        }
        case 5: {
          message.camId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartTrack message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartTrack} ReqStartTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTrack.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartTrack message.
   * @function verify
   * @memberof ReqStartTrack
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartTrack.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      if (!$util.isInteger(message.x)) return "x: integer expected";
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      if (!$util.isInteger(message.y)) return "y: integer expected";
    if (message.w != null && Object.hasOwnProperty.call(message, "w"))
      if (!$util.isInteger(message.w)) return "w: integer expected";
    if (message.h != null && Object.hasOwnProperty.call(message, "h"))
      if (!$util.isInteger(message.h)) return "h: integer expected";
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      if (!$util.isInteger(message.camId)) return "camId: integer expected";
    return null;
  };

  /**
   * Creates a ReqStartTrack message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartTrack
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartTrack} ReqStartTrack
   */
  ReqStartTrack.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartTrack) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartTrack: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartTrack();
    if (object.x != null) message.x = object.x | 0;
    if (object.y != null) message.y = object.y | 0;
    if (object.w != null) message.w = object.w | 0;
    if (object.h != null) message.h = object.h | 0;
    if (object.camId != null) message.camId = object.camId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStartTrack message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartTrack
   * @static
   * @param {ReqStartTrack} message ReqStartTrack
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartTrack.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.x = 0;
      object.y = 0;
      object.w = 0;
      object.h = 0;
      object.camId = 0;
    }
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      object.x = message.x;
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      object.y = message.y;
    if (message.w != null && Object.hasOwnProperty.call(message, "w"))
      object.w = message.w;
    if (message.h != null && Object.hasOwnProperty.call(message, "h"))
      object.h = message.h;
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      object.camId = message.camId;
    return object;
  };

  /**
   * Converts this ReqStartTrack to JSON.
   * @function toJSON
   * @memberof ReqStartTrack
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartTrack.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartTrack
   * @function getTypeUrl
   * @memberof ReqStartTrack
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartTrack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartTrack";
  };

  return ReqStartTrack;
})();

$root.ReqStopTrack = (function () {
  /**
   * Properties of a ReqStopTrack.
   * @exports IReqStopTrack
   * @interface IReqStopTrack
   */

  /**
   * Constructs a new ReqStopTrack.
   * @exports ReqStopTrack
   * @classdesc Represents a ReqStopTrack.
   * @implements IReqStopTrack
   * @constructor
   * @param {IReqStopTrack=} [properties] Properties to set
   */
  function ReqStopTrack(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopTrack instance using the specified properties.
   * @function create
   * @memberof ReqStopTrack
   * @static
   * @param {IReqStopTrack=} [properties] Properties to set
   * @returns {ReqStopTrack} ReqStopTrack instance
   */
  ReqStopTrack.create = function create(properties) {
    return new ReqStopTrack(properties);
  };

  /**
   * Encodes the specified ReqStopTrack message. Does not implicitly {@link ReqStopTrack.verify|verify} messages.
   * @function encode
   * @memberof ReqStopTrack
   * @static
   * @param {IReqStopTrack} message ReqStopTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTrack.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopTrack message, length delimited. Does not implicitly {@link ReqStopTrack.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopTrack
   * @static
   * @param {IReqStopTrack} message ReqStopTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopTrack.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopTrack message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopTrack} ReqStopTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTrack.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopTrack();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopTrack message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopTrack} ReqStopTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopTrack.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopTrack message.
   * @function verify
   * @memberof ReqStopTrack
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopTrack.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopTrack message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopTrack
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopTrack} ReqStopTrack
   */
  ReqStopTrack.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopTrack) return object;
    return new $root.ReqStopTrack();
  };

  /**
   * Creates a plain object from a ReqStopTrack message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopTrack
   * @static
   * @param {ReqStopTrack} message ReqStopTrack
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopTrack.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopTrack to JSON.
   * @function toJSON
   * @memberof ReqStopTrack
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopTrack.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopTrack
   * @function getTypeUrl
   * @memberof ReqStopTrack
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopTrack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopTrack";
  };

  return ReqStopTrack;
})();

$root.ReqPauseTrack = (function () {
  /**
   * Properties of a ReqPauseTrack.
   * @exports IReqPauseTrack
   * @interface IReqPauseTrack
   */

  /**
   * Constructs a new ReqPauseTrack.
   * @exports ReqPauseTrack
   * @classdesc Represents a ReqPauseTrack.
   * @implements IReqPauseTrack
   * @constructor
   * @param {IReqPauseTrack=} [properties] Properties to set
   */
  function ReqPauseTrack(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqPauseTrack instance using the specified properties.
   * @function create
   * @memberof ReqPauseTrack
   * @static
   * @param {IReqPauseTrack=} [properties] Properties to set
   * @returns {ReqPauseTrack} ReqPauseTrack instance
   */
  ReqPauseTrack.create = function create(properties) {
    return new ReqPauseTrack(properties);
  };

  /**
   * Encodes the specified ReqPauseTrack message. Does not implicitly {@link ReqPauseTrack.verify|verify} messages.
   * @function encode
   * @memberof ReqPauseTrack
   * @static
   * @param {IReqPauseTrack} message ReqPauseTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPauseTrack.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqPauseTrack message, length delimited. Does not implicitly {@link ReqPauseTrack.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqPauseTrack
   * @static
   * @param {IReqPauseTrack} message ReqPauseTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqPauseTrack.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqPauseTrack message from the specified reader or buffer.
   * @function decode
   * @memberof ReqPauseTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqPauseTrack} ReqPauseTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPauseTrack.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqPauseTrack();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqPauseTrack message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqPauseTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqPauseTrack} ReqPauseTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqPauseTrack.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqPauseTrack message.
   * @function verify
   * @memberof ReqPauseTrack
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqPauseTrack.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqPauseTrack message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqPauseTrack
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqPauseTrack} ReqPauseTrack
   */
  ReqPauseTrack.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqPauseTrack) return object;
    return new $root.ReqPauseTrack();
  };

  /**
   * Creates a plain object from a ReqPauseTrack message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqPauseTrack
   * @static
   * @param {ReqPauseTrack} message ReqPauseTrack
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqPauseTrack.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqPauseTrack to JSON.
   * @function toJSON
   * @memberof ReqPauseTrack
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqPauseTrack.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqPauseTrack
   * @function getTypeUrl
   * @memberof ReqPauseTrack
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqPauseTrack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqPauseTrack";
  };

  return ReqPauseTrack;
})();

$root.ReqContinueTrack = (function () {
  /**
   * Properties of a ReqContinueTrack.
   * @exports IReqContinueTrack
   * @interface IReqContinueTrack
   */

  /**
   * Constructs a new ReqContinueTrack.
   * @exports ReqContinueTrack
   * @classdesc Represents a ReqContinueTrack.
   * @implements IReqContinueTrack
   * @constructor
   * @param {IReqContinueTrack=} [properties] Properties to set
   */
  function ReqContinueTrack(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqContinueTrack instance using the specified properties.
   * @function create
   * @memberof ReqContinueTrack
   * @static
   * @param {IReqContinueTrack=} [properties] Properties to set
   * @returns {ReqContinueTrack} ReqContinueTrack instance
   */
  ReqContinueTrack.create = function create(properties) {
    return new ReqContinueTrack(properties);
  };

  /**
   * Encodes the specified ReqContinueTrack message. Does not implicitly {@link ReqContinueTrack.verify|verify} messages.
   * @function encode
   * @memberof ReqContinueTrack
   * @static
   * @param {IReqContinueTrack} message ReqContinueTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqContinueTrack.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqContinueTrack message, length delimited. Does not implicitly {@link ReqContinueTrack.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqContinueTrack
   * @static
   * @param {IReqContinueTrack} message ReqContinueTrack message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqContinueTrack.encodeDelimited = function encodeDelimited(message, writer) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqContinueTrack message from the specified reader or buffer.
   * @function decode
   * @memberof ReqContinueTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqContinueTrack} ReqContinueTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqContinueTrack.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqContinueTrack();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqContinueTrack message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqContinueTrack
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqContinueTrack} ReqContinueTrack
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqContinueTrack.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqContinueTrack message.
   * @function verify
   * @memberof ReqContinueTrack
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqContinueTrack.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqContinueTrack message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqContinueTrack
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqContinueTrack} ReqContinueTrack
   */
  ReqContinueTrack.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqContinueTrack) return object;
    return new $root.ReqContinueTrack();
  };

  /**
   * Creates a plain object from a ReqContinueTrack message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqContinueTrack
   * @static
   * @param {ReqContinueTrack} message ReqContinueTrack
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqContinueTrack.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqContinueTrack to JSON.
   * @function toJSON
   * @memberof ReqContinueTrack
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqContinueTrack.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqContinueTrack
   * @function getTypeUrl
   * @memberof ReqContinueTrack
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqContinueTrack.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqContinueTrack";
  };

  return ReqContinueTrack;
})();

$root.ReqStartSentryMode = (function () {
  /**
   * Properties of a ReqStartSentryMode.
   * @exports IReqStartSentryMode
   * @interface IReqStartSentryMode
   * @property {number|null} [type] ReqStartSentryMode type
   */

  /**
   * Constructs a new ReqStartSentryMode.
   * @exports ReqStartSentryMode
   * @classdesc Represents a ReqStartSentryMode.
   * @implements IReqStartSentryMode
   * @constructor
   * @param {IReqStartSentryMode=} [properties] Properties to set
   */
  function ReqStartSentryMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartSentryMode type.
   * @member {number} type
   * @memberof ReqStartSentryMode
   * @instance
   */
  ReqStartSentryMode.prototype.type = 0;

  /**
   * Creates a new ReqStartSentryMode instance using the specified properties.
   * @function create
   * @memberof ReqStartSentryMode
   * @static
   * @param {IReqStartSentryMode=} [properties] Properties to set
   * @returns {ReqStartSentryMode} ReqStartSentryMode instance
   */
  ReqStartSentryMode.create = function create(properties) {
    return new ReqStartSentryMode(properties);
  };

  /**
   * Encodes the specified ReqStartSentryMode message. Does not implicitly {@link ReqStartSentryMode.verify|verify} messages.
   * @function encode
   * @memberof ReqStartSentryMode
   * @static
   * @param {IReqStartSentryMode} message ReqStartSentryMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartSentryMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.type);
    return writer;
  };

  /**
   * Encodes the specified ReqStartSentryMode message, length delimited. Does not implicitly {@link ReqStartSentryMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartSentryMode
   * @static
   * @param {IReqStartSentryMode} message ReqStartSentryMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartSentryMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartSentryMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartSentryMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartSentryMode} ReqStartSentryMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartSentryMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartSentryMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.type = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartSentryMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartSentryMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartSentryMode} ReqStartSentryMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartSentryMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartSentryMode message.
   * @function verify
   * @memberof ReqStartSentryMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartSentryMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      if (!$util.isInteger(message.type)) return "type: integer expected";
    return null;
  };

  /**
   * Creates a ReqStartSentryMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartSentryMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartSentryMode} ReqStartSentryMode
   */
  ReqStartSentryMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartSentryMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartSentryMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartSentryMode();
    if (object.type != null) message.type = object.type | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStartSentryMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartSentryMode
   * @static
   * @param {ReqStartSentryMode} message ReqStartSentryMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartSentryMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.type = 0;
    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
      object.type = message.type;
    return object;
  };

  /**
   * Converts this ReqStartSentryMode to JSON.
   * @function toJSON
   * @memberof ReqStartSentryMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartSentryMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartSentryMode
   * @function getTypeUrl
   * @memberof ReqStartSentryMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartSentryMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartSentryMode";
  };

  return ReqStartSentryMode;
})();

$root.ReqStopSentryMode = (function () {
  /**
   * Properties of a ReqStopSentryMode.
   * @exports IReqStopSentryMode
   * @interface IReqStopSentryMode
   */

  /**
   * Constructs a new ReqStopSentryMode.
   * @exports ReqStopSentryMode
   * @classdesc Represents a ReqStopSentryMode.
   * @implements IReqStopSentryMode
   * @constructor
   * @param {IReqStopSentryMode=} [properties] Properties to set
   */
  function ReqStopSentryMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * Creates a new ReqStopSentryMode instance using the specified properties.
   * @function create
   * @memberof ReqStopSentryMode
   * @static
   * @param {IReqStopSentryMode=} [properties] Properties to set
   * @returns {ReqStopSentryMode} ReqStopSentryMode instance
   */
  ReqStopSentryMode.create = function create(properties) {
    return new ReqStopSentryMode(properties);
  };

  /**
   * Encodes the specified ReqStopSentryMode message. Does not implicitly {@link ReqStopSentryMode.verify|verify} messages.
   * @function encode
   * @memberof ReqStopSentryMode
   * @static
   * @param {IReqStopSentryMode} message ReqStopSentryMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopSentryMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    return writer;
  };

  /**
   * Encodes the specified ReqStopSentryMode message, length delimited. Does not implicitly {@link ReqStopSentryMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStopSentryMode
   * @static
   * @param {IReqStopSentryMode} message ReqStopSentryMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStopSentryMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStopSentryMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStopSentryMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStopSentryMode} ReqStopSentryMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopSentryMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStopSentryMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStopSentryMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStopSentryMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStopSentryMode} ReqStopSentryMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStopSentryMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStopSentryMode message.
   * @function verify
   * @memberof ReqStopSentryMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStopSentryMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    return null;
  };

  /**
   * Creates a ReqStopSentryMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStopSentryMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStopSentryMode} ReqStopSentryMode
   */
  ReqStopSentryMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStopSentryMode) return object;
    return new $root.ReqStopSentryMode();
  };

  /**
   * Creates a plain object from a ReqStopSentryMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStopSentryMode
   * @static
   * @param {ReqStopSentryMode} message ReqStopSentryMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStopSentryMode.toObject = function toObject() {
    return {};
  };

  /**
   * Converts this ReqStopSentryMode to JSON.
   * @function toJSON
   * @memberof ReqStopSentryMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStopSentryMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStopSentryMode
   * @function getTypeUrl
   * @memberof ReqStopSentryMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStopSentryMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStopSentryMode";
  };

  return ReqStopSentryMode;
})();

$root.ReqUFOAutoHandMode = (function () {
  /**
   * Properties of a ReqUFOAutoHandMode.
   * @exports IReqUFOAutoHandMode
   * @interface IReqUFOAutoHandMode
   * @property {number|null} [mode] ReqUFOAutoHandMode mode
   */

  /**
   * Constructs a new ReqUFOAutoHandMode.
   * @exports ReqUFOAutoHandMode
   * @classdesc Represents a ReqUFOAutoHandMode.
   * @implements IReqUFOAutoHandMode
   * @constructor
   * @param {IReqUFOAutoHandMode=} [properties] Properties to set
   */
  function ReqUFOAutoHandMode(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqUFOAutoHandMode mode.
   * @member {number} mode
   * @memberof ReqUFOAutoHandMode
   * @instance
   */
  ReqUFOAutoHandMode.prototype.mode = 0;

  /**
   * Creates a new ReqUFOAutoHandMode instance using the specified properties.
   * @function create
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {IReqUFOAutoHandMode=} [properties] Properties to set
   * @returns {ReqUFOAutoHandMode} ReqUFOAutoHandMode instance
   */
  ReqUFOAutoHandMode.create = function create(properties) {
    return new ReqUFOAutoHandMode(properties);
  };

  /**
   * Encodes the specified ReqUFOAutoHandMode message. Does not implicitly {@link ReqUFOAutoHandMode.verify|verify} messages.
   * @function encode
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {IReqUFOAutoHandMode} message ReqUFOAutoHandMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUFOAutoHandMode.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.mode);
    return writer;
  };

  /**
   * Encodes the specified ReqUFOAutoHandMode message, length delimited. Does not implicitly {@link ReqUFOAutoHandMode.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {IReqUFOAutoHandMode} message ReqUFOAutoHandMode message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqUFOAutoHandMode.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqUFOAutoHandMode message from the specified reader or buffer.
   * @function decode
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqUFOAutoHandMode} ReqUFOAutoHandMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUFOAutoHandMode.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqUFOAutoHandMode();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.mode = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqUFOAutoHandMode message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqUFOAutoHandMode} ReqUFOAutoHandMode
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqUFOAutoHandMode.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqUFOAutoHandMode message.
   * @function verify
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqUFOAutoHandMode.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      if (!$util.isInteger(message.mode)) return "mode: integer expected";
    return null;
  };

  /**
   * Creates a ReqUFOAutoHandMode message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqUFOAutoHandMode} ReqUFOAutoHandMode
   */
  ReqUFOAutoHandMode.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqUFOAutoHandMode) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqUFOAutoHandMode: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqUFOAutoHandMode();
    if (object.mode != null) message.mode = object.mode | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqUFOAutoHandMode message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {ReqUFOAutoHandMode} message ReqUFOAutoHandMode
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqUFOAutoHandMode.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) object.mode = 0;
    if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
      object.mode = message.mode;
    return object;
  };

  /**
   * Converts this ReqUFOAutoHandMode to JSON.
   * @function toJSON
   * @memberof ReqUFOAutoHandMode
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqUFOAutoHandMode.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqUFOAutoHandMode
   * @function getTypeUrl
   * @memberof ReqUFOAutoHandMode
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqUFOAutoHandMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqUFOAutoHandMode";
  };

  return ReqUFOAutoHandMode;
})();

$root.ReqStartTrackClick = (function () {
  /**
   * Properties of a ReqStartTrackClick.
   * @exports IReqStartTrackClick
   * @interface IReqStartTrackClick
   * @property {number|null} [x] ReqStartTrackClick x
   * @property {number|null} [y] ReqStartTrackClick y
   * @property {number|null} [camId] ReqStartTrackClick camId
   */

  /**
   * Constructs a new ReqStartTrackClick.
   * @exports ReqStartTrackClick
   * @classdesc Represents a ReqStartTrackClick.
   * @implements IReqStartTrackClick
   * @constructor
   * @param {IReqStartTrackClick=} [properties] Properties to set
   */
  function ReqStartTrackClick(properties) {
    if (properties)
      for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
        if (properties[keys[i]] != null && keys[i] !== "__proto__")
          this[keys[i]] = properties[keys[i]];
  }

  /**
   * ReqStartTrackClick x.
   * @member {number} x
   * @memberof ReqStartTrackClick
   * @instance
   */
  ReqStartTrackClick.prototype.x = 0;

  /**
   * ReqStartTrackClick y.
   * @member {number} y
   * @memberof ReqStartTrackClick
   * @instance
   */
  ReqStartTrackClick.prototype.y = 0;

  /**
   * ReqStartTrackClick camId.
   * @member {number} camId
   * @memberof ReqStartTrackClick
   * @instance
   */
  ReqStartTrackClick.prototype.camId = 0;

  /**
   * Creates a new ReqStartTrackClick instance using the specified properties.
   * @function create
   * @memberof ReqStartTrackClick
   * @static
   * @param {IReqStartTrackClick=} [properties] Properties to set
   * @returns {ReqStartTrackClick} ReqStartTrackClick instance
   */
  ReqStartTrackClick.create = function create(properties) {
    return new ReqStartTrackClick(properties);
  };

  /**
   * Encodes the specified ReqStartTrackClick message. Does not implicitly {@link ReqStartTrackClick.verify|verify} messages.
   * @function encode
   * @memberof ReqStartTrackClick
   * @static
   * @param {IReqStartTrackClick} message ReqStartTrackClick message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTrackClick.encode = function encode(message, writer, q) {
    if (!writer) writer = $Writer.create();
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.x);
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.y);
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.camId);
    return writer;
  };

  /**
   * Encodes the specified ReqStartTrackClick message, length delimited. Does not implicitly {@link ReqStartTrackClick.verify|verify} messages.
   * @function encodeDelimited
   * @memberof ReqStartTrackClick
   * @static
   * @param {IReqStartTrackClick} message ReqStartTrackClick message or plain object to encode
   * @param {$protobuf.Writer} [writer] Writer to encode to
   * @returns {$protobuf.Writer} Writer
   */
  ReqStartTrackClick.encodeDelimited = function encodeDelimited(
    message,
    writer,
  ) {
    return this.encode(
      message,
      writer && writer.len ? writer.fork() : writer,
    ).ldelim();
  };

  /**
   * Decodes a ReqStartTrackClick message from the specified reader or buffer.
   * @function decode
   * @memberof ReqStartTrackClick
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @param {number} [length] Message length if known beforehand
   * @returns {ReqStartTrackClick} ReqStartTrackClick
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTrackClick.decode = function decode(reader, length, error, long) {
    if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
    if (long === undefined) long = 0;
    if (long > $Reader.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var end, message;
    if (length === undefined) end = reader.len;
    else {
      end = reader.pos + length;
      if (end > reader.len) throw RangeError("index out of range");
      length = reader.len;
      reader.len = end;
    }
    message = new $root.ReqStartTrackClick();
    while (reader.pos < end) {
      var tag = reader.uint32();
      if (tag === error) break;
      switch (tag >>> 3) {
        case 1: {
          message.x = reader.int32();
          break;
        }
        case 2: {
          message.y = reader.int32();
          break;
        }
        case 3: {
          message.camId = reader.int32();
          break;
        }
        default:
          reader.skipType(tag & 7, long);
          break;
      }
    }
    if (length !== undefined) {
      if (reader.pos !== end) throw RangeError("index out of range");
      reader.len = length;
    }
    return message;
  };

  /**
   * Decodes a ReqStartTrackClick message from the specified reader or buffer, length delimited.
   * @function decodeDelimited
   * @memberof ReqStartTrackClick
   * @static
   * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
   * @returns {ReqStartTrackClick} ReqStartTrackClick
   * @throws {Error} If the payload is not a reader or valid buffer
   * @throws {$protobuf.util.ProtocolError} If required fields are missing
   */
  ReqStartTrackClick.decodeDelimited = function decodeDelimited(reader) {
    if (!(reader instanceof $Reader)) reader = new $Reader(reader);
    return this.decode(reader, reader.uint32());
  };

  /**
   * Verifies a ReqStartTrackClick message.
   * @function verify
   * @memberof ReqStartTrackClick
   * @static
   * @param {Object.<string,*>} message Plain object to verify
   * @returns {string|null} `null` if valid, otherwise the reason why it is not
   */
  ReqStartTrackClick.verify = function verify(message, long) {
    if (typeof message !== "object" || message === null)
      return "object expected";
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit) return "maximum nesting depth exceeded";
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      if (!$util.isInteger(message.x)) return "x: integer expected";
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      if (!$util.isInteger(message.y)) return "y: integer expected";
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      if (!$util.isInteger(message.camId)) return "camId: integer expected";
    return null;
  };

  /**
   * Creates a ReqStartTrackClick message from a plain object. Also converts values to their respective internal types.
   * @function fromObject
   * @memberof ReqStartTrackClick
   * @static
   * @param {Object.<string,*>} object Plain object
   * @returns {ReqStartTrackClick} ReqStartTrackClick
   */
  ReqStartTrackClick.fromObject = function fromObject(object, long) {
    if (object instanceof $root.ReqStartTrackClick) return object;
    if (!$util.isObject(object))
      throw TypeError(".ReqStartTrackClick: object expected");
    if (long === undefined) long = 0;
    if (long > $util.recursionLimit)
      throw Error("maximum nesting depth exceeded");
    var message = new $root.ReqStartTrackClick();
    if (object.x != null) message.x = object.x | 0;
    if (object.y != null) message.y = object.y | 0;
    if (object.camId != null) message.camId = object.camId | 0;
    return message;
  };

  /**
   * Creates a plain object from a ReqStartTrackClick message. Also converts values to other types if specified.
   * @function toObject
   * @memberof ReqStartTrackClick
   * @static
   * @param {ReqStartTrackClick} message ReqStartTrackClick
   * @param {$protobuf.IConversionOptions} [options] Conversion options
   * @returns {Object.<string,*>} Plain object
   */
  ReqStartTrackClick.toObject = function toObject(message, options, q) {
    if (!options) options = {};
    if (q === undefined) q = 0;
    if (q > $util.recursionLimit) throw Error("max depth exceeded");
    var object = {};
    if (options.defaults) {
      object.x = 0;
      object.y = 0;
      object.camId = 0;
    }
    if (message.x != null && Object.hasOwnProperty.call(message, "x"))
      object.x = message.x;
    if (message.y != null && Object.hasOwnProperty.call(message, "y"))
      object.y = message.y;
    if (message.camId != null && Object.hasOwnProperty.call(message, "camId"))
      object.camId = message.camId;
    return object;
  };

  /**
   * Converts this ReqStartTrackClick to JSON.
   * @function toJSON
   * @memberof ReqStartTrackClick
   * @instance
   * @returns {Object.<string,*>} JSON object
   */
  ReqStartTrackClick.prototype.toJSON = function toJSON() {
    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
  };

  /**
   * Gets the default type url for ReqStartTrackClick
   * @function getTypeUrl
   * @memberof ReqStartTrackClick
   * @static
   * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
   * @returns {string} The default type url
   */
  ReqStartTrackClick.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
    if (typeUrlPrefix === undefined) {
      typeUrlPrefix = "type.googleapis.com";
    }
    return typeUrlPrefix + "/ReqStartTrackClick";
  };

  return ReqStartTrackClick;
})();

const typedRoot = /** @type {any} */ ($root);
export default typedRoot;
